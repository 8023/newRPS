# 更新记录

### v3.1.4（2026-09-02）

本版本整理上一轮重构遗留的无调用代码和样式，并修复主宠关系图谱的节点边界处理；不改变 Protobuf 协议、数据库结构、发布产物路径或玩家端玩法。

#### 代码精简与依赖

- **删除无调用前端代码**：移除 React/Svelte 迁移后已没有组件引用的旧样式、展示辅助函数和表单样式，减少维护面与 CSS 产物体积。
- **收窄无必要抽象**：Go 座位占用从单一实现接口收窄为 `*HumanSeat` 别名，直接使用已有空统计值；删除纯转发随机函数和无调用构造器，保留现有 `HumanSeat` 包装、座位访问形状和协议转换。
- **移除直接依赖**：`d3-shape` 不再作为前端顶层依赖；当前代码未直接导入它，LayerChart 所需的传递依赖仍由依赖树提供。
- **使用标准库测试辅助**：delta 测试改用 Go 标准库 `bytes.Contains`，删除重复的手写字节查找实现。

#### 主宠关系图谱

- **节点边界修复**：坐标夹紧现在直接写回 d3 `ForceSimulation` 实际持有的节点对象，拖拽和力模拟都不会再把节点逐步推到可视区域之外；节点仍可在原有边界内正常移动。

#### 验证

- `CGO_ENABLED=1 /usr/local/go/bin/go test ./...`
- `CGO_ENABLED=1 /usr/local/go/bin/go vet ./...`
- `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run test --prefix web`（39 项测试）
- `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run build --prefix web`（`svelte-check` 0 errors/0 warnings，Vite 构建通过）
- `git diff --check`

### v3.1.3（2026-08-31）

本版本在 v3.1.2 的数据分析面板基础上继续优化后台响应速度与图表布局，同时修复若干持久化、安全边界和房间交互问题；不改变分析指标口径或 Protobuf 协议。

#### 性能与分析面板

- **分析应答预编码**：后台聚合快照发布时预先编好 7/30/90 天三档 protobuf 应答，管理员查询直接复用不可变载荷，避免在全局状态锁内重复构造大型 `Struct`，降低分析请求对对局、聊天和落子的阻塞。
- **分析图表按需挂载**：管理员悬停或键盘聚焦「数据分析」时预加载组件；分析分区内的图表滚动到视口附近才实例化，并用等高占位块保持滚动位置稳定，减少首次打开时的主线程等待。
- **图表布局统一**：趋势图、分布图统一绘图区高度和数值轴留白；热门惩罚标签改为横向多系列堆叠条形图，单系列/多系列共用静态图例、悬停明细和圆角规则；长类目轴标签按实际字体宽度省略，悬停原生提示保留完整名称；窄屏下相关卡片正确收为单列。

#### 安全与持久化

- **敏感文件权限收紧**：活动安全日志、SQLite 主库及 WAL/共享内存边车文件创建或打开后统一收紧到 `0600`，避免同机其他系统用户读取 IP、设备指纹、会话和身份凭据。
- **持久化竞态修复**：串行化玩家快照的“捕获脏集合 + 落盘”过程，并补齐白给自救板、名字争夺战状态和管理员查看认领密钥等路径的持久化/审计记录，避免并发写入覆盖新状态或重启回滚。
- **请求与缓存边界修复**：限制匿名 session 请求体大小；编辑已通过的共建投稿后立即刷新惩罚缓存，使旧版本不会继续被抽取。

#### 房间与前端交互

- **黑白棋极限连胜结算**：正常胜负和管理员强制判定现在与其他回合制游戏一致，正确清理败方连胜并处理胜方极限连胜风险。
- **拖拽与离房修复**：主宠关系图谱拖拽节点会被限制在可见区域且真正驱动物理模拟；离开房间后立即清理本地房态，避免过期更新把玩家带回旧房间界面。
- **惩罚证明上传保护**：图片仍在上传或转码时禁止提交证明，并显示明确状态，避免把本地 blob 地址误传给服务端。
- **Svelte 样式兼容**：`styleString` 按 React 的无量纲 CSS 属性规则补齐数字 `px`，恢复迁移前的动态样式表现。

#### 验证

- `go test ./...`
- `npm run test --prefix web`
- `npm run build`
- `git diff --check`

### v3.1.2（2026-08-30）

本版本继续打磨后台「数据分析」面板的阅读体验，不改变分析口径、采集链路、RPC、数据库或玩家端玩法。

#### 数据分析面板

- **统一图例与数值提示**：折线图、双轴图、堆叠柱图、组合图和圆环图统一在绘图区下方显示静态图例；详细数值继续通过悬停气泡查看，双轴图的左右轴系列也会一并展示。
- **移除重复的图表/表格切换**：删除各图表卡片上的切换按钮和仅服务于旧切换器的系列表组件；转化漏斗下的页面浏览、来源、省份和最近会话明细仍保留表格页签。
- **面板分组与响应式布局**：新增核心趋势、设备与来源、游戏与惩罚、互动与行为、转化与明细五个分组标题，优化筛选条、KPI 卡片、图表卡片、留存热力图以及窄屏布局。
- **图表可读性修正**：统一明暗主题下的分析配色，改善坐标轴和折线视觉，长类目轴标签自动截断但悬停时保留完整名称，并补齐猜硬币的游戏名称映射。

#### 验证

- `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run test --prefix web`
- `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run build --prefix web`
- `CGO_ENABLED=1 /usr/local/go/bin/go test ./...`
- `git diff --check`

### v3.1.1（2026-08-29）

本版本完成前端从 React 到 Svelte 5 的整体迁移，保持 Go 后端、Protobuf 通信、业务状态与部署产物路径不变；同时收尾修正前端构建边界、无障碍检查与登出推送清理。

#### 前端重构

- **React → Svelte 5**：登录、大厅、房间、8 款游戏、聊天、个人资料、社交、共建、后台与数据分析全部改由 Svelte 5（runes）组件承载；状态按 `uiStore` / `routerStore` / `sessionStore` / `adminStore` 分层，保留原有页面路由、协议调用、主题与交互行为。
- **组件目录化**：前端从原先的集中式 TSX 页面拆分为 `shell`、`lobby`、`room`、`games`、`chat`、`contribute`、`social`、`profile`、`about`、`admin` 等按功能组织的 Svelte 组件，图表封装统一使用 LayerChart。
- **依赖与入口切换**：移除 React、Recharts 及对应 Vite/图标依赖，改用 Svelte、`@lucide/svelte` 与 LayerChart；入口切换为 `main.ts`，`svelte-check` 纳入前端构建类型门禁，前端产物仍输出到 `bin/dist/`。

#### 构建与兼容性修正

- **懒加载边界守卫**：Vite 构建新增后台与数据分析场景的手动分包规则，并在构建期检查首屏是否反向静态引用懒加载场景；WebP、HEIF 图片处理保持独立按需加载。
- **登出清理推送订阅**：Web Push 解绑改为静态导入，登出时仍会先清理当前设备订阅，失败不阻断身份凭据清理。
- **可访问性检查**：为弹窗遮罩、图谱交互节点、管理聊天列表等必须响应指针事件的非语义容器补充局部 Svelte 检查说明，保留键盘可操作控件的语义。
- **对局说明提示**：关于页明确提示说明文档覆盖全部 8 款游戏。

#### 验证

- `CGO_ENABLED=1 /usr/local/go/bin/go test ./...`
- `git diff --check`
- `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run test --prefix web`（6 个文件、39 项测试）
- `TMPDIR=/tmp TMP=/tmp TEMP=/tmp npm run build --prefix web`（`svelte-check` 0 errors/0 warnings，Vite 构建通过；懒加载 chunk 已生成）

### v2.8.4（2026-08-28）

修正新建猜硬币/大话骰房间在无主题房名词库时被错误兜底成「新的锤子剪刀布房间」的问题。

#### 修正

- **房名兜底按游戏区分**：`generatedRoomName`（后端）与 `generateRoomName`（前端）在没有可用主题房名词库时，不再统一兜底成 RPS 的占位名，而是按 `GameID` 返回各自游戏的占位默认名（新增猜硬币、大话骰兜底分支）；`normalizeRoomName` 同步识别全部 8 款游戏的占位名，避免误传的占位符被当作正式房名落地。
- **大厅信息标签**：补充猜硬币的游戏标签（🪙）与「抛硬币中」阶段标签。

#### 验证

- `CGO_ENABLED=1 go test ./...`（新增 `room_name_test.go` 回归覆盖房名兜底逻辑）
- `cd web && npx tsc --noEmit`

### v2.8.3（2026-08-27）

本版本新增单人惩罚小游戏「猜硬币」，并改善共建列表与全站排行榜的使用体验；同时收紧公开玩家快照中的真实积分字段，保留管理员后台所需的真实分排序能力。

#### 新增猜硬币

- **单人玩法**：新增「猜硬币」房间，建房后自动坐入参战席 A，无需等待对手或准备；座位 B 结构性锁死，其他玩家可以观战，座位空出后可接棒继续玩。
- **即时抛掷**：玩家选择「字」或「花」后由服务端立即随机开奖，前端展示翻面动画和结果；每次猜错立即派发随机或系列惩罚任务，猜中则直接进入下一次抛掷。
- **自动验收**：没有真人对手来审核证明，提交证明即自动通过并恢复抛掷；猜硬币不计入排位积分、胜负场次、分游戏战绩或白给值，但受罚次数和房间内个人惩罚难度进度照常累加。
- **配置与协议**：新增 `CoinFlipState`、猜硬币历史字段和 `coin_flip_winner_label` 配置；管理员可自定义惩罚文案中 `{winner}` 的展示名。前后端 Protobuf、转换、归一化、分析白名单和游戏配置已同步。

#### 共建与排行榜

- **投稿列表控件**：玩家「参与共建」和后台「共建审核」支持按标题/投稿者搜索、按时间或点赞率排序，系列任务额外支持按完成率排序；玩家端「投稿新任务/系列」入口固定在列表顶部，空结果会明确提示。
- **列表预览与响应速度**：共建预览封面图按比例放大且适配窄屏；全站排行榜使用短时浏览器缓存、首次分页后并发拉取剩余页面，服务端对分页重复使用的共建统计增加短 TTL 缓存。

#### 安全与兼容修正

- **真实积分不再向普通连接泄露**：普通玩家的公开资料、排行榜和房间快照中，`Sort*` 字段与已封顶展示值一致；管理员玩家管理、关联账号和宠物关系图谱专属回执保留真实存储分。名字争夺战失格判断改为直接读取服务端真实分，避免展示封顶影响业务规则。
- **称号覆盖顺序修正**：极限模式逃跑称号不会覆盖玩家自设称号、主人称号或管理员自定义称号；分析事件白名单同步支持 `coinflip`。
- **兼容性**：新增字段均使用未占用的 Protobuf 编号并同步生成 Go/TS 代码；旧的房间与历史字段继续保留，未知游戏战绩和旧客户端默认值处理不变。

#### 验证

- `CGO_ENABLED=1 /usr/local/go/bin/go test ./...`
- `node web/node_modules/typescript/bin/tsc -p web/tsconfig.json --noEmit`
- 前端 Vite 生产构建已通过；构建日志仅保留既有的动态/静态导入和大 chunk 非阻断警告。

### v2.8.2（2026-08-24）

本版本修正共建投稿、惩罚事件、数据分析和多款游戏结算链路，并补充 RPS 撤回出拳与共建排行榜。重点是让“正式上线时间、实际评价对象、当前版本、历史结算”四组数据口径保持一致；同时补齐数据库迁移、前后端默认值和发布前回归测试。

#### 共建投稿与评价

- **系列默认步数统一为 5–20 步**：后端配置归一化、服务端校验、Protobuf 注释、前端协议默认值和投稿表单使用同一组默认值；草稿可以少于最低步数分次填写，但不能超过最高步数。
- **系列评价改为真实票数加权**：系列总体点赞率把当前仍有效的每一步在所有历史版本上的赞踩票合并计算，票数多的步骤权重更高，不再对各步骤百分比做等权平均；投稿编辑不会让旧评价消失。
- **评价时机前移**：正式任务分配后，受罚者和审核者即可赞/踩；证明提交、通过、驳回或重做都不再影响评价资格，已经投出的票也不会回滚。
- **管理员终审改稿**：已通过投稿可以由管理员直接编辑并立即发布，仍插入新版本并受版本数量与 JSON 载荷上限保护；无新内容的空批准请求会被拒绝。
- **后台查询批量化**：共建审核列表一次性读取评价与系列完成率，避免逐条请求触发限流；共建排行榜按当前最新且已通过的独立任务及系列步骤数量统计。
- **性别配置保存修复**：后台“性别与阵营”与普通配置保存共用同一套清洗校验和 SQLite 覆盖写入流程，避免编辑后被旧数据库内容覆盖。

#### 数据库、统计与事件一致性

- **Schema v49**：`sub_tasks` / `series` 新增跨版本继承的 `first_approved_at`。任务池/系列池的“新增”按第一次正式审批通过（上线）时间归日，不再把草稿创建日算作新增；可恢复的历史时间自动回填，并重算已封板日聚合。
- **惩罚重做原子化**：驳回旧证明、写入 `redo_id` 和插入新事件合并到同一事务，避免留下悬空审计链；新事件成功落库后才向客户端公布 `eventId`。
- **历史统计快照**：系列进度记录保留玩家首次实际体验的系列版本与步数快照，避免房间内热更新或下架后丢失完成率样本；异常/空文案替补仍会推进系列进度。
- **玩家战绩兼容**：加载分游戏战绩时忽略未知游戏枚举，避免未来或手工导入的未知行静默覆盖 RPS 战绩。
- **安全与性能**：共建审核内容增加统一载荷上限；排行榜和后台统计查询使用只读连接并在 SQL 期间释放全局状态锁。

#### 游戏与前端体验

- **RPS 撤回出拳**：对手尚未出拳时可以撤回并重新选择；主人强制的白给不能撤回。白给点击次数和数值加成统一在回合真正结算时发放，不能通过撤回刷取。
- **棋类结算链路统一**：国际象棋、斗兽棋、黑白棋、五子棋、井字棋和 RPS 的终局历史明确区分 `round_end` 与 `piece_capture`，修正短名称、座位标签、棋钟阻塞和每子惩罚恢复对局时的结算展示。
- **后台/投稿表单校验反馈**：难度为空或越界时直接标红对应输入框；数字配置使用统一数字输入控件，未保存草稿的放弃/撤回操作位置和提示更明确。
- **排行榜新增“共建”页签**：全局排行榜由十个维度扩展为十一个，按通过审批的随机任务和系列步骤数量排序。
- **配置安全**：仓库内站点配置不再携带硬编码管理员口令，改为部署占位值。

### v2.8.1（2026-08-22）

本版本对**共建投稿系统架构**进行深度重构：从双层投稿信封模型（`contribution_submissions` 信封层 + 版本层）改为单一版本化表（`sub_tasks` / `series`）；性别投稿功能正式下线，性别与阵营管理移至后台独占；投稿编辑流程调整，编辑已通过内容**立即下线、需重新审批**后才生效；玩家表拆分为主表 + 子表模式，减少主表宽度。八次数据库迁移（v41–v48）清理冗余列与表、简化数据模型并优化索引；其中一次性迁移（v43）在失败时回滚确保无中间状态。

#### 共建投稿存储重构

- **单一版本化表**：原有 `punishment_tasks`（单条任务）/ `punishment_series`（系列集合）/ `contribution_submissions`（投稿信封）/ 投稿版本表四层结构，统一为 `sub_tasks`（所有任务所有版本）/ `series`（系列元信息及版本）两张表，数据模式为"插入不更新"（每次修改都插入新行，历史版本永久保留）。
- **投稿编辑流程变化**：原有"编辑已通过内容 → 新版本待审时旧版本继续在线"改为"编辑已通过内容 → 新版本草稿状态下旧版本**立即下线** → 重新审批通过后才生效"。这避免了修订期间长期维持两个版本在线的混乱局面，保障玩家得到的任务总是最终通过审批的内容。
- **数据完整迁移**（v43）：在单一事务内依次转换旧任务、系列、投稿状态、事件审计、投票计数到新表结构；任一步失败则整体回滚，不存在中间状态。迁移成功后自动删除旧表。旧的投稿展开式存储（一行投稿 + N 个版本行）无需人工干预，自动转入新的版本化模型。
- **性别投稿下线**：玩家不再能通过「参与共建」投稿性别/阵营（原有界面入口已移除）。运行时性别与阵营的权威来源仍为 SQLite，但编辑只能通过后台「性别与阵营」卡片进行。旧性别投稿信封及其版本历史随 v43 旧投稿表一并删除，不迁移为审计档案；已发布的性别/阵营运行时数据不受影响。

#### 数据库清理与优化

- **v41**：`gender_options` 表新增 `submission_id` 列，支持一份性别投稿勾选多个阵营（从一一对应改为一对多）。存量数据自动回填。
- **v42**：`contribution_images` 表新增 `active` 列，将图片访问权限判断从"对应投稿是否启用"改为"这张图当前是否仍被引用"。换新图时旧图标记 inactive 但文件保留，作为审计材料永久存档。
- **v44**：`sub_tasks` 表新增 `active` 列，系列缩短时为移除的尾部步骤生成墓碑行（`active=0`），在保留完整版本历史的同时明确区分"当前系列成员"与"历史上曾属于该系列的步骤"。
- **v45**：清理早期 v43 草案阶段的遗留旧表（若存在 `punishment_tasks` 等会在数据库启动时被隔离，此版本显式删除）。
- **v46**：删除 `gender_factions` 和 `gender_options` 表的冗余 `normalized_label` 列（仅用于大小写不敏感的唯一约束，应用层已现算）。`gender_options` 约束从 `UNIQUE(faction_id, normalized_label)` 改为 `UNIQUE(faction_id, label)`，放宽至字面匹配；受约束放宽影响的现有数据集合为空。

#### 玩家表拆分

- **v47**：将 `players` 表的 4 组共 50 列字段（名争、白给、极限模式、分游戏胜负平）拆分到四张独立子表（`player_name_war` / `player_giveaway` / `player_extreme_mode` / `player_game_stats`），减少主表宽度、改善查询性能。子表按 `player_id` 外键关联，单点一致性由 SQLite 的 FK constraint 保证。

#### 数据模型简化

- **v48**：删除 `punishment_events` 的 `task_source` / `formal_series_id` / `formal_series_version` / `contributor_player_id` / `contributor_name_snapshot` / `contributor_anonymous` 六列，以及 `sub_tasks` / `series` 的 `contributor_name` 列。这些字段可由 `formal_task_id` + `formal_task_version` + 对 `sub_tasks` 的 JOIN 现查得到；昵称快照违反本站"改名后全历史生效"的设计原则，不予保留。
- **协议与存储**：`punishment_events.performer_vote` / `approver_vote` 两列仍为原子写（`UPDATE ... WHERE <col>=0`），用于投票防重；点赞点踩粒度恒定为"具体任务"（系列每一步各自独立），不支持按整个系列投票。

#### 投稿编辑流程与展示

- **编辑已上线内容**：玩家在「参与共建」点"修改"已通过的内容时，编辑界面会明确显示「**修订草稿**」（新版本已保存为草稿）、「**修订审核中**」（新版本已提交待审）或「**修订被驳回**」（新版本被驳回，可继续编辑重新提交）。旧版本内容在修订期间不再可见、无法被抽到，新版本通过审批后才上线替换。
- **后台管理**：管理员依然可以直接"编辑后发布"（`contributionPublish` 操作，改名为 `contributionReview`），绕过待审期直接生效。此时新版本直接成为 `approved` 状态、不走 `pending` 等待。
- **撤回与下架**：玩家"申请下架"与管理员"下架"都改为同义操作（`contributionUnpublish`），直接把状态改为 `withdrawn`，一步到位（旧版本有"下架待审"的中间态，已移除）。

#### 后台界面调整

- **性别与阵营**：独立为一张卡片（原属投稿「总体」中的一项），管理员可直接增删改，无需走投稿审批流。新增 `id` 与 `label` 字段的长度校验（最多 40 字符）。
- **共建审核**：列表一次性下发已通过投稿的点赞率与系列完成率（`fillContributionListMeta`），避免前端逐条查询触发限流。
- **待审总览**：新增"投稿池统计"矩阵，显示各类型各状态的投稿数（与数据分析中的"增长图"口径一致）。

#### 其他调整

- **帮助文档**：移除「参与共建」中关于性别投稿的内容；更新系列任务进入房间的流程描述（"坐下前"改为"进入房间前"，措辞更准确）；编辑完成后的状态流转（新增"修订下线→重新审批"的工作流说明）。
- **前端**：样式调整与投稿编辑组件重构，适配新的版本化存储模型与编辑流程。

#### 协议

- `PunishmentRandomSettings` 删除 `series_faction_fallback_text` 字段（不再发送兜底文案，改由服务端在未找到匹配变体时下发文本内容）；所有现有字段继续支持。

### v2.7.1（2026-08-21）

本版本上线**共建投稿系统**和**惩罚难度独立计数**，彻底重构玩家创作体验与惩罚系统。大厅新增「参与共建」，持久玩家可投稿随机任务、系列任务，经后台审核后进入正式数据；性别与阵营改为仅由后台管理。管理员不再有绕过投稿记录、直接编辑任务池/系列表的入口，所有正式内容统一走投稿版本事务和审计记录。惩罚难度进度从房间级共享改为房间内按玩家个人计数，系列任务进度同步改为按玩家独立。系列提交与审批时强制每一步覆盖其声明的全部目标阵营；非目标阵营玩家进房前会收到警告，仍进入后遇到未覆盖的步骤则从该玩家阵营的随机池临时替补。惩罚证明图缓存改为 6 小时，共建图片缓存为 30 天并永久保留作为审计材料。

#### 共建投稿系统
- **玩家投稿渠道**：大厅「参与共建」可投稿随机任务、系列任务。创作者权限自动按身份等级控制（新号不开放）。
- **投稿与审核**：投稿可保存为草稿、提交审批，投稿者可直接撤回。管理员在「后台 → 共建审核」可批准、驳回并批注、编辑原稿后立即发布、下架、撤销驳回或取消撤回。状态统一为草稿、待审批、已通过、已驳回、已撤回；玩家编辑已通过内容时，新版会立即取代旧版成为最新版并退出正式池，直到重新审批通过。
- **版本与审计**：每次内容修订都插入新版本，纯状态流转只更新最新版本；同一投稿最多保留 1000 个版本。所有正式任务/系列统一记录投稿版本和发布时的事务快照。
- **文案变体**：一条投稿由 1..N 份文案变体组成，每份变体仅有「文案」+「适用阵营」两项，不存在「通用/精修/限定」等人为分类。
- **随机任务投稿**：恒定进入随机抽取池，必填难度（参考范围 1–99）；可选标签与封面图。
- **系列任务投稿**：包含多个有序步骤，每步可选「同时发布到随机任务」（勾选时需填难度和标签）；系列声明面向的阵营集合，发布时校验每步阵营覆盖。步数需在配置范围内（后台可设，默认 10–20），超限会报错；保存草稿不受限。系列每一步都不需要填名称（发布后有全局唯一 ID）。
- **投稿列表展示**：待审时显示「YY/MM/DD · 待审批」，已通过显示「YY/MM/DD · 点赞 n%」；系列另加「完成 n%」（房间销毁时按参与者的步序百分比样本取均值）。
- **投票与评价**：随机/系列正式任务完成后，胜负双方可赞或踩，也可跳过（无倒计时压迫）；观战者与投稿者本人无投票权。评价粒度恒定为「具体任务」（独立任务的所有阵营变体共享一组，系列每一步各自一组），不能投给整个系列；系列点赞率为已有投票步骤的点赞率算术平均。投票前服务端不下发点赞率、评价人数和贡献者，投票成功后才揭示，避免口碑影响第一判断。
- **性别与阵营**：玩家端性别投稿已下线；管理员可在「后台 → 性别与阵营」批量管理。性别与阵营运行时权威在 SQLite（`gender_factions` / `gender_options`），`genders.json` / `gender-factions.json` 仅作空库首次导入种子。
- **真实评价口径**：展示点赞率只由玩家实际赞/踩汇总计算，「管理员覆盖展示点赞率」功能已移除，旧覆盖表在数据迁移成功后自动删除。

#### 惩罚难度独立计数
- **随机任务难度**：进度改为房间内**按玩家个人**计数（`RoomState.PunishmentTaskProgress` 从 `int` 改为 `map[string]int`）——你自己挨罚得越多次，后面轮到你的目标难度越高，房间里别人的被罚次数不影响你。你退座位/进观众席/断线重连/退房再进**同一房间**，进度继续保留；换一个房间，进度从 0 开始；房间销毁即释放，不落盘。替代此前「整间房共用一个计数」的规则。
- **系列任务进度**：同步改为房间内**按玩家个人**计数（`RoomState.PunishmentSeriesPlayerProgress`）——谁挨罚就推进谁自己的步数，房间里别人不会把你往前推。保留逻辑同随机任务：换房间（哪怕系列相同）、房内切换系列或房间销毁都重置为 0。
- **系列未覆盖阵营**：某一步没有变体命中受罚者阵营时，从随机任务池临时抽一条替补（难度按「当前步序 ÷ 总步数」比例定目标值、套用随机模式的倒伽马加权；标签由系列自身词频推导并逐级放宽），进度照常推进。该玩家自己的步数计数不会冻结，命中或替补都往前走。

#### 上传与缓存
- **惩罚证明图**：浏览器/共享缓存改为 6 小时（原 30 天）；房间销毁后一律拒绝访问。
- **共建投稿封面**：浏览器/共享缓存为 30 天；成功上传后作为监管审计材料**永久留存**，不因草稿、修订、图片替换或下架而自动清理（换新图必须换新 URL，旧图继续保留）。如需清理，由站外专用审计程序另行执行。
- **Referer 校验完善**：`GET /uploads/*` 继续要求 `Referer` 为本站页面；非浏览器客户端可伪造但作纵深防御；共享缓存下仍可能绕过，上传和分享链接时请自行注意。

#### 协议与数据库
- 消息类型新增：`contribution:list` / `get` / `saveDraft` / `submit` / `withdraw`（玩家投稿 RPC）；`contribution:votePreview` / `vote`（评价相关）；`admin:action` 的 `contribution*` / `genders*` 系列（管理员操作）。
- HTTP 端点新增：`POST /api/contribution-image`（投稿封面上传）。
- Schema v33–v45：v33 将性别与阵营库化；v34–v40 补充旧惩罚表的目标阵营、投票粒度、分析时间与索引；v43 在单一事务内将旧任务、系列、投稿版本、状态、审核信息和投票迁移到 `sub_tasks` / `series`，成功后自动删除旧表，任一步失败则整体回滚；v44 增加系列步骤 `active` 墓碑；v45 清理早期重构版本可能误重建的空旧表。旧的管理员点赞率覆盖数据不迁移。
- Protobuf：`RoomSettings` 新增 `enable_per_piece_punishment`；`PunishmentRandomSettings` 新增 `min_series_steps` / `max_series_steps`；`PunishmentSeriesSummary` 新增 `target_faction_ids`；删除 `series_faction_fallback_text`。前后端需同步发布。

#### 其他调整
- **帮助文档**：新增「参与共建」章节与难度对照表（1–99，从前戏到高潮），供创作者参考。更新随机/系列任务规则说明，强调房间内按玩家独立计数的新逻辑。
- **后台界面**：「惩罚任务」整块改为「共建审核」，移除旧的「任务池」「系列任务」直接编辑界面；新增「性别与阵营」独立卡片（SQLite 管理）；「系列任务步数」配置卡片用于设置最低/最高步数。

### 修复
- 国际象棋 / 斗兽棋「每子惩罚」：证明提交、驳回、对手确认这三个入口审批完成后会错误地强制开出新局，而不是恢复正在进行的对局。
- 白给自救板（板上内容、赞踩计数）与「通用改名处」极限强关一侧的可改名标记/改名保护期/最后改名者，此前从未落库，服务器每次重启都会清空。补齐落库与加载逻辑（schema v38）。

### v2.6.2（2026-08-17）

本版本深化棋类惩罚与白给体验，并重做惩罚体系三项规则：国际象棋与斗兽棋新增「每子惩罚」——吃子即触发惩罚、审批后对局继续（棋钟暂停、不计分不改白给值）；斗兽棋补上白给机制（轮到你走子时按白给值一半的概率直接跳过本手，主人也可强制宠物跳过）；排位赌分档位改为按游戏在后台配置，用于平衡各游戏耗时。系列任务进度改为房间级共享、某一步未覆盖阵营时改为下发兜底文案而非整系列作废；任务池未勾选标签的任务改为常驻随机池（标签不再是排除维度，难度 -1 与阵营留空仍能把任务挡在随机池外）；后台惩罚任务界面同步调整。

#### 每子惩罚（国际象棋 / 斗兽棋）

- **触发**：开房勾选「每子惩罚」后，国际象棋 / 斗兽棋对局中每被吃一枚棋子（或兽）就立即触发一次惩罚，棋钟暂停到审批结束；审批结束后**恢复对局**而非开新局。
- **不计分不改白给**：中途吃子惩罚不结算排位分、不触碰白给值，只走任务本身。
- **终局避重**：终局那一步如果已经因吃子触发过惩罚，不再重复罚；走进兽穴、将死、超时、认输这类没吃子的结束，仍按终局惩罚处理。
- **棋钟联动**：每子惩罚期间暂停双方棋钟（与悔棋 / 认输请求同一阻塞路径），恢复后重新起算。
- **新增开房选项**：房间设置 `enable_per_piece_punishment`（协议 `RoomSettings` 新增字段，前后端需同步发布）。

#### 斗兽棋白给

- **走子白给**：开启白给后，轮到你走子时按白给值 **一半** 的概率直接跳过本手、对方继续走——除以 2 是为了避免双方白给值都很高时无限互相跳过。
- **主人强制**：主人可以强制宠物跳过本手（与其它游戏的强制白给同级），强制时宠物白给值按激活加成累加。
- **回合流转**：开局与每次走子后都会检查白给触发，可连环跳过（最多连续 4 手），只有实际轮到的玩家才吃超时判负。

#### 各游戏排位档位可配置

- **后台配置**：`games.json`（后台「称号与排位 → 游戏排位分值」）可为每个游戏单独设置建房可选的赌分档位（最多 4 档、升序、第一个为默认档）。
- **默认档位**：黑白棋 1/2/5/10 分/子（按每子实时结算），其余游戏 5/10/20 分/局。
- **用途**：用于平衡各游戏耗时——例如把国际象棋档位调成 15/30/60，赢一局相当于三局默认赌注的猜拳。
- **协议**：`GameConfig` 新增 `stakes` 字段（前后端需同步发布）；建房档位不在档位内时回退默认档，结算逻辑零改动。

#### 系列任务：房间级共享进度

- **共享计数器**：进度改为房间级内存计数器，房间内全员共用一个步数——谁挨罚都推进同一步，A 做完 5 条离开、B 从第 6 条继续。
- **不落盘不跨房**：换房间（即便开同一个系列）从头开始，房间销毁进度即释放。
- **schema v32**：删除废弃表 `player_punishment_series_progress`；后台「重置所有人进度」action 与按钮一并移除——房间共享进度无需人工重置，散场即清零。

#### 系列任务：阵营覆盖放宽为兜底文案

- **不再整体作废**：某一步的候选任务没有覆盖受罚者阵营时，系列照常保存、生效（此前整个系列会被判不可用：建房面板选不到、运行时不产出任务）。
- **兜底文案**：该步无阵营命中时，下发管理员可配的兜底文案（后台「惩罚任务 → 惩罚配置 → 系列任务兜底文案」，支持 `{loser}`/`{winner}`，默认「该任务不适用于你所在的阵营」），进度照常推进。
- **可用性判断简化**：系列只要有步骤就可用（进入公开摘要、可被房间选择）。
- **协议**：`PunishmentRandomSettings` 新增 `series_faction_fallback_text` 字段（存于 `punishments.json`），前后端需同步发布。

#### 无标签任务常驻随机池

- **行为变化**：任务池里未勾选任何标签的任务不再被随机模式排除——它们无视建房时的标签选中 / 拒绝筛选，任何随机房间都可能抽到（此前无标签任务永远抽不到）。
- **标签不再是排除维度**：想专门留给系列任务用的任务，仍有两种办法——把难度设为 -1，或不勾选阵营（未勾选阵营的任务永远不参与随机匹配）。本次变化只针对标签：留空标签不再排除随机候选，房主也无法用标签勾选/拒绝拦住它们。

#### 后台惩罚任务界面

- **保存按钮上移**：「保存任务池」「保存系列任务」按钮移至「惩罚配置 / 系列任务 / 任务池」子 Tab 行右侧（原底部保存行移除）。
- **列表加高**：左侧列表（标签 / 任务 / 系列索引）高度约 +65%（约 7.5 条 → 12 条）。
- **修复「添加」不展开**：系列任务第二步及以后点「添加」不再被 `<details>` 折叠态遮住选择面板。

#### 其它

- **国际象棋棋子字形**：棋子字形加 U+FE0E 强制可重着色文本字形，修复 Safari 部分棋子回退成黑色彩色字体的问题。
- **棋盘图例精简**：五子棋 / 斗兽棋 / 国际象棋面板移除底部图例行。
- **建房面板排位提示**：各游戏档位选项与说明改为读取服务端配置动态渲染。
- **复选框 / 单选框样式修复**：全局 input 样式排除 checkbox/radio，修复 WebKit（Safari）下勾选框错位、被 padding 撑变形的问题。
- `help.md` 同步斗兽棋白给机制、每子惩罚规则、系列任务房间共享进度与阵营兜底文案。

### v2.6.1（2026-08-16）

本版本新增第七款玩法国际象棋，并把悔棋从五子棋扩展到黑白棋、斗兽棋与国际象棋；棋钟抽成各棋类共用实现。同时修了大厅精简快照冲掉分游戏战绩的问题，并微调版本提示、后台白给设置与分析图边距。

#### 新增国际象棋

- **座位制 1v1**：8×8 棋盘，双方准备后随机执白，白方先走。将死对方的王获胜；逼和、双方子力不足、连续五十步没吃子也没动兵、同一局面出现三次则判和。
- **规则**：支持王车易位、吃过路兵、兵走到对方底线必须升变成后/车/象/马。超时一般判负；对方已经没有将死子力（例如只剩光王）则判和。
- **对局体验**：棋盘双方统一白下黑上（与观战同一视角）；选子后高亮可走格/吃子环；将军提示按己方/对方区分；a1 按惯例为深色格；建房提供经典褐/深木纹/深夜蓝/大理石/赛场绿五套主题。
- **配套机制**：认输请求（需对方确认）、棋钟（每步 + 每局）、排位、惩罚、断线判负；不支持白给。惩罚结束后下一局回到准备阶段，不会误走进锤子剪刀布的出拳路径。
- **事件**：`chess:ready` / `chess:move` / `chess:undoRequest` / `chess:undoRespond` / `chess:resignRequest` / `chess:resignRespond` / `chess:restart`。
- **schema v31**：`players` 表增加 `chess_wins` / `chess_losses` / `chess_draws`。

#### 棋类悔棋扩展

- **覆盖范围**：斗兽棋、国际象棋、黑白棋现与五子棋同样支持建房设置每人 0/1/3/10 次悔棋。
- **规则**：仅当前行棋方可申请；对方 30 秒内确认，超时自动拒绝；同意后回退最近两手；等待期间暂停棋钟。
- **黑白棋**：同步逆转被撤销两手已经实时结算的排位分与白给值，不会出现棋盘退了、分却还留着。
- **国际象棋**：完整恢复易位权、吃过路兵、升变、半子钟与三次重复局面历史（走快照回退，不靠逆推）。
- **事件**：`othello:undoRequest` / `othello:undoRespond`、`jungle:undoRequest` / `jungle:undoRespond`、`chess:undoRequest` / `chess:undoRespond`。

#### 其它

- **大厅精简快照不再冲掉分游戏战绩**：实时大厅会省略 `GameStats`，合并回完整资料时若补丁没有任何分项记录则保留本地战绩，避免个人设置里的分游戏胜负被抹成 0。
- **版本不一致提示**：前后端构建版本不一致时的横幅改为「刷新 / 忽略」两个明确按钮。
- **后台白给自救板设置分组**：普通用户 / 对自己宠物 / 对自己主人三档投票次数与白给值变化拆成独立卡片。
- **分析图表边距**：数值 Y 轴统一压到 40px，图例补上国际象棋。
- `help.md` 同步七款游戏与悔棋规则。

### v2.5.2（2026-08-15）

本版本聚焦后台与对局体验：后台配置重组——「防多开」并入「网站管理」，限流策略按设备/IP/房间/证明图片分组展示，提示语改用中文名称，「新用户注册开关」移至提示公告，新增服务器运行状态卡片，用户管理并入「用户与房间」；用户分析「对局时长」改为均值口径（单局/单房均值分钟数、保留 1 位小数），删除对局结果统计，图表日期刻度压缩；修复斗兽棋初始布局非对称问题，双方棋子按各自视角 180° 旋转对称摆放。

#### 后台配置重组

- **「防多开」并入「网站管理」**：原「防多开」一级菜单删除，其内容重组为「网站管理」下的「限流策略」区块——按「设备指纹限制 / IP 限制 / 房间与证明图片限制」分组展示，字段配中文标签与说明文字。
- **提示语独立区块**：原「提示语」配置独立为「网站管理」下的单独区块，内部键名改用易读的中文名称（配置格式错误、配置保存成功、名字校验失败、房间密码错误、缺少惩罚证明、房间创建成功），不再暴露原始配置键。
- **「禁止新用户注册」开关移至「提示公告」**：与公告板、安全声明同区展示，并增加醒目警示样式。
- **新增「运行状态」卡片**：展示当前在线人数、房间数、运行时长、房间/大厅广播次数（总览与最近 1 分钟）、断线/重连次数、房间与大厅快照大小（最近与平均）。
- **用户管理并入「用户与房间」**：原「用户管理」一级菜单删除，作为「用户与房间」下的「用户管理」页签，与房间管理同区。

#### 用户分析：对局时长改均值口径

- **删除「对局结果」统计**：`game_result` 维度（分胜负/平局/双输）不再聚合，对应图表移除。
- **「对局时长」均值图**：原「每局时长」「房间时长」两张总时长图合并为一张「对局时长」图——按游戏拆分的单局均值分钟数（当天该游戏总耗时 ÷ 总局数，保留 1 位小数，避免 RPS 这类单局 <1 分钟的游戏被整数取整拍成 0）以左轴堆叠柱展示，全站单房均值分钟数（当天房间存活总时长 ÷ 开房总数，不拆分游戏）以右轴折线展示。
- **日期刻度压缩**：图表横轴日期刻度省略年份只显示 mm/dd，悬停提示与明细表格仍展示完整 yyyy-mm-dd。

#### 斗兽棋布局与规则说明

- **初始布局对称修正**：双方棋子改为按各自视角 180° 旋转对称摆放（修正此前「不镜像」布局下后排象/狮左右不一致的问题），对局体验两侧完全一致。
- **规则说明补充**：JunglePanel 规则补充「敌兽进入己方陷阱，可被任意己方兽吃掉」。

### v2.5.1（2026-08-14）

本版本聚焦后台运营与访问控制：新增聊天历史检索与单条/批量软删除、恢复，玩家管理新增昵称筛选与"小号"（同设备关联账号）查询；修复转化漏斗倒挂并统一历史日重算口径；上传文件新增 Referer 校验、证明图随所属房间销毁而失效；统一五个双座位游戏的结算文案并修复称号显示不一致；随机惩罚标签偏好改为浏览器本地保存。

#### 后台聊天管理（新）

- **聊天历史检索**：新增 `admin:chatSearch`，支持按大厅/房间作用域、发送者昵称、消息内容、房间名组合检索历史消息。
- **软删除与恢复**：聊天改为软删除（`deleted`/`deleted_at`，schema v28），不再有"一键清空大厅/房间聊天"——只能对检索出的具体消息单条/批量删除或恢复。删除即时落库并推送 `chat:deleted` 让在线客户端摘除本地视图；普通聊天历史（recent/older）自动过滤已删除消息，新老访客都看不到。
- **房间名快照**：房间聊天落盘时顺带保存发送时刻的房间名（`room_messages.room_name`），房间之后改名/关闭，后台仍能按当时的房间名检索历史消息。

#### 用户管理：昵称筛选与小号查询

- **昵称筛选**：玩家列表支持按昵称检索。
- **关联设备账号查询（小号）**：新增 `admin:action` `altAccounts`，BFS 传递展开与目标玩家共用过同一设备指纹（`sha256(ip||fingerprint)`）的其它账号（含防死循环与遍历上限）。查询走独立只读连接（`activityRO`，不受 `ANALYTICS_ENABLED` 开关影响）与新增 `connection_events(device)` 索引，同时覆盖历史连接与尚未断开的当前连接，不堵塞主写连接。

#### 用户分析修复与新增统计

- **修复转化漏斗倒挂**：两个根因——「访问」层此前只查 `analytics_sessions`，会话行首次落库后不再随 UPSERT 回写 visitor/day，换网/换 IP/跨零点保持同一会话会让 lobby/room/round/finish 各层反而比 visit 还多；「开局」埋点 `game_start` 晚于 `game_round` 上线，上线当天「开局」远小于「完成对局」。schema v27/v29 改为「深层向浅层逐级并集回填」（`computeFunnelUV`），已封板历史日一次性按新口径重写（并修正"先删旧行再枚举日期会把极简历史日永久漏掉"的顺序问题）。
- **新增随机惩罚标签统计**：建房选随机任务时勾选/排除的标签各记一笔分析事件（`punish_tag_include`/`punish_tag_exclude`，detail=tagId），面板新增对应分布图。
- **新增系列任务选择统计**：建房选系列任务时记一笔 `punish_series_select`（detail=seriesId）。
- **惩罚任务图按互斥状态展示**：进行中/完成/驳回三态互斥统计，不再重叠计数。

#### 证明图与上传文件访问控制

- **Referer 校验**：`GET /uploads/*`（证明图/头像/后台图）要求 `Referer` 为本站页面（与 WS/POST 的 Origin 校验同一套白名单逻辑），缺失或跨站一律 404，拦住跨站热链与站外直接粘贴访问；仍非强鉴权（非浏览器客户端可伪造 `Referer`），只作纵深防御一层，详见 README「证明图」小节。
- **缓存窗口收窄**：`/uploads/*` 的 `Cache-Control` `max-age` 由 30 天收窄到 6 小时（仍为 `public`，兼顾非盈利小站的流量成本），缩小共享缓存（CDN/反代）按 URL 缓存响应体、绕开 Referer 校验的窗口。
- **证明图随房间销毁失效**：上传时记录所属房间（内存态、不落盘），房间销毁（清空/管理员关房/进程重启）后一律 404，图片不会比它所属的房间活得更久；与 Referer 校验互补而非互相替代。

#### 对局结算文案与称号显示修复

- **结算文案统一**：RPS/黑白棋/井字棋/五子棋/斗兽棋五个双座位游戏的胜方文案统一为「玩家A/B「昵称」胜利」（断线判负同口径），不再混用完整「性别-称号-昵称」或裸短名。
- **称号显示修复**：认主认宠的宠物称号、自设称号此前只在玩家资料页生效，结算文案/聊天署名的 `DisplayName` 缓存停在积分档称号——现在两处共用同一套 `resolveDisplayTitle` 优先级（管理员自定义 > 宠物称号 > 自设称号 > 积分档），设置/解除称号、解除主宠关系时同步刷新缓存。
- **RPS 结算实时提示**：RPS 结算后把结果说明（白给/命运干预/排位加减分等 `ResultText`）作为房间通知推送一次，实时提示可见，不只留在对局历史卡片里。

#### 其它

- **随机惩罚标签偏好改浏览器本地保存**：开房面板的标签三态偏好只存 `localStorage`（`rps-punishment-tag-prefs`），不再落玩家档案、不再跨设备同步，也不再进 `player:join` 应答；废弃 `player_punishment_tag_prefs` 表（schema v30，标签数据分析统计走独立 `analytics_events`，不受影响）。
- **房间内直接发大厅聊天**：房间聊天旁新增「大厅」页签，不用退房就能发言、看实时消息。
- **建房惩罚入口合并为四选一**：无惩罚/随机/系列/玩家发布。
- `help.md` 同步更新（房间内发大厅聊天、"放过对方"机制描述与实际行为对齐）。

### v2.4.3（2026-08-12）

本版本重做惩罚任务系统（标签化随机任务 + 个人进度系列任务，任务池/系列详情迁出 `AppConfig` 落 SQLite），完善用户分析（转化漏斗重定义、用户留存矩阵、单房对局统计），新增前后端版本一致性提示，并统一后台图片上传规范。

#### 惩罚任务系统重做

- **随机任务改标签化**：`punishmentSource` 三态改为 `random`（原 `system`）/ `series`/`player`。建房面板对一排**标签**三态点选（缺省/选中/拒绝），系统从任务池里抽取——必须覆盖所有选中标签、不沾任何拒绝标签，全缺省时只按拒绝排除。玩家的标签偏好会被记住，下次开房自动预填（新表 `player_punishment_tag_prefs`）。
- **难度改按房间走、倒伽马加权抽取**：目标难度随房间已抽任务数走高，抽题在目标难度附近浮动，更难一侧设硬顶（含硬顶档不抽），逐步把气氛往更深处推而不会一步抽太狠。
- **新增系列任务模式**：房主建房挑一个系列，对每个玩家是**跨房间/跨对手的个人进度**（新表 `player_punishment_series_progress`），每次受罚推进一步，到最后一步后反复停留，只有管理员在后台清零才能重来。系列每步引用任务池 ID（`steps[].taskIds`），运行时按阵营精确命中优先、空阵营通用兜底，悬空 ID 跳过；任一步未覆盖全部已定义阵营则整个系列不进入建房可选列表。
- **任务池与系列任务迁出 `AppConfig`，落 SQLite**：新表 `punishment_tasks`、`punishment_series`（schema v25 两张进度/偏好表 + v26 两张任务表，共新增 5 个迁移 v22–v26）。`punishments.json` 只保留 `tags`（含房名词库/背景图库）与全局难度参数 `orderStep`/`maxDifficultyOvershoot`；启动时一次性从旧 JSON 的 `tasks`/`seriesTasks` 导入（系列 `variants` 按阵营打散为任务池条目）。
- **建房系列目录用公开摘要**：`AppConfig.punishmentSeriesSummaries`（id/name/房名/背景/步数，不含任务文案）随 config 频道广播，保存系列后 `emitConfigUpdate`。
- **后台三个二级 tab 重排**：惩罚配置 / 系列任务 / 任务池，新增独立 `admin:action` RPC（`punishmentTasksGet`/`Save`、`punishmentSeriesGet`/`Save`），不新增顶层事件。删除标签时级联从任务池 `tagIds` 摘除（不阻断保存）。
- **后台表单精简**：标签、阵营的内部 ID 字段从界面隐藏（系统自动生成），改为一行内「名称输入框 + 删除按钮」布局；任务池「任务名称」输入框收窄、右侧放删除按钮，样式与标签/阵营统一。
- `help.md` 同步更新为「随机任务 / 系列任务 / 玩家发布任务」三种模式的玩家向说明。

#### 用户分析升级

- **转化漏斗重定义**：五层（访问/进大厅/进房/开局/完成对局）统一按设备 UV 口径重新定义，废弃旧的 `login`/`punish_done` 键（schema v23/v24 对已封板历史日重算漏斗行）。
- **修复"惩罚完成"指标恒为 0**：审核通过写的是 `approved`，聚合时误按 `status='done'` 统计，导致完成数/完成率面板长期挂零（schema v22 按源事件重算历史日）。
- **新增用户留存矩阵**：按账号 `playerId` 首次注册日分 cohort（跨设备合并），区别于原有的按设备指纹维度的新老访客对比。
- **新增"新老用户"（按账号）维度**：与原有"新老设备"并列展示，同一账号换设备不再被误判成新用户。
- **新增单房对局数统计**：当日有对局的房间里，单房局数最大值与均值。
- **大厅/房间发言人数拆分统计**，会话与惩罚证明时长分桶进一步细化。

#### 前后端版本一致性提示（新）

- 后端构建时用 `-ldflags -X` 注入 git 短哈希（`internal/server.BuildID`），前端 `vite.config.ts` 用 `define` 注入同一份哈希（`__APP_BUILD_ID__`）；两者在同一次发布构建里天然相同。
- 连接建立时一次性推送 `server:hello`，并在 25 秒心跳 `ping` 应答里附带兜底，前端拿到与自身不一致的构建版本后展示一条持久横幅，提示手动刷新——不做自动强制刷新，避免打断正在进行的输入/对局，也从根上避免"版本提示触发自动刷新"被当作 DoS 放大手段。全程复用既有的动态 Struct 载荷通道，未改动 `.proto`。

#### 后台图片上传规范统一

- `/api/admin-image`（房间信息卡图库、任务背景图库）收紧为与证明图一致的规则：仅接受前端压缩后的 WebP，≤2MB（原先接受 jpg/png/webp，≤8MB）。

### v2.4.2（2026-08-10）

本版本重做白给自救板投票系统（按 actor→target 独立计额 + 认主认宠三档），完善分析面板（活动拆分、新老用户维度、时长桶细化），并统一构建产物到 `bin/dist/`。

#### 白给自救板投票系统重构

- **额度从"全体共用一个窗口"改为"按 actor→target 独立计时/计次"**：`GiveawayVoteQuotas` map，每个目标窗口各自起算/过期，互不影响。移除旧的 `GiveawayVotedTargets`（同一条自救内容只能投一次），改为真正的按每小时次数上限限流——同一 target 在额度内可反复投票，额度耗尽后等窗口滚动刷新。
- **新增认主认宠三档独立配置**：普通/直系宠物/直系主人各有一套独立的每小时点赞/倒赞次数上限和升降百分比（`giveaway.json` 新增 `petLikeVoteLimitPerHour` 等 8 个字段）。只认直系关系（不含宠物的宠物等二级以上关系），未命中走普通档。
- **新增 `giveaway:voteQuotas` RPC**：按目标玩家 ID 批量查询自己对每个目标的剩余额度，供自救板列表初次渲染和关系变化时补齐每张卡片下方的额度展示。
- **白给值精度从 1 位小数提升到 2 位小数**：`clampGiveawayValue` 的 `Round(x*10)/10` 改为 `Round(x*100)/100`。
- **Protobuf 旧聚合字段标记 deprecated**：`PublicPlayer`/`LobbyPlayer` 里的 `giveawayVoteWindowStartedAt`/`giveawayVoteCount`/`giveawayVoteLikesThisHour`/`giveawayVoteDislikesThisHour` 保留原字段号但标记弃用——服务端不再用它们做新额度判断，只在成功投票后继续更新/广播以兼容旧客户端显示。
- **新增 `fixGiveawayVoteLimits`**：老部署的 `giveaway.json` 没有新三档字段时（JSON unmarshal 留 0），自动回退到普通档对应字段值——`LoadConfig` 和 `SaveConfig` 两处都调用，防止升级后因 Validate 拒绝而启动失败，也保证管理员从旧版前端提交配置时不报错。

#### 构建产物路径整理

- **`dist/` 并入 `bin/dist/`**：`web/vite.config.ts` 的 `outDir` 从 `../dist` 改为 `../bin/dist`，`docker-compose.yml` 挂载路径从 `./dist:/app/dist:ro` 改为 `./bin/dist:/app/dist:ro`，服务端静态托管依次探测 `bin/dist` → `dist`（兼容 Docker 部署拍平路径）。Release 包现在只有一个 `bin/` 目录需要覆盖，不再有独立的顶层 `dist/`。README 各章节同步更新。

#### 用户分析面板升级

- **活动拆分为「用户信息变更」和「名争·白给」两个分组**：后端新增 `profileChanges`（性别/阵营变更、改名、更换头像、自定义称号）和 `nameWarGiveaway`（开启名争、开启白给、白给留言板、名争改名、开启极限模式），前端图表分别展示。
- **新增「新老用户」维度**：按账号 `playerId` 维度拆分"当日新建账号数"和"老用户登录数"，与设备指纹维度的"新老设备"（`newVsReturning`）并列对照。
- **会话时长桶颗粒度细化**：从 4 桶（0-10s/10-60s/1-5m/5-15m/15-60m/60m+）细化为 8 桶（10s/1min/2min/5min/10min/30min/60min/60m+）；惩罚证明时长桶同步细化（10s/30s/1min/5min/10min/20min/20m+）。
- **修复 `avgMs` SQL null type 错误**：`rebuildDay` 里 `AVG(duration_ms)` 是 REAL 类型，错用 `sql.NullInt64` 接收——非整数平均值（如 1.5ms）会导致整行 Scan 失败，当天全部聚合维度（来源/省份/ISP）都出不来。改为 `sql.NullFloat64` 接收后 `int64` 转。追加测试用例防回归。
- **新老访客从堆叠柱状图改为双折线图**，趋势更直观。
- **堆叠柱状图分段顶端圆角动态判断**：不再固定最后一个 series 带圆角——当天该 series 值为 0 时，实际露在顶端的前一个 series 会缺角，改为按当前柱各 series 取值动态判断谁在顶端。

#### 宠物乐园关系图

- **宽度自适应容器**：`ResizeObserver` 监听实际渲染宽度，力导向图不再固定 720px。
- **新增 BFS 筛选功能**：选一位玩家 + 跳数 depth，按无向图 BFS 只保留指定跳数内可达的主宠关系——depth=2 可直观展示"主人社交圈"。输入框自带玩家搜索。
- **高度从 460px 扩大到 690px**，给筛选控件留出空间。
- **修复头像首字拖拽分离 bug**：`<foreignObject>` 内子元素用 `transform: translateY()` 上移首字时，部分浏览器会把它当成独立合成层缓存，导致头像动而字不动。改为 `margin-top` 走正常文档流。

#### 活动埋点优化

- **开关类只记开启不记关闭**：`nameWar_enable`/`giveaway_enable`/`extreme_enable` 只在开启时记事件，关闭不再记录。
- **清除头像并入 `avatar_change` 计数**：不再区分 `avatar_clear` 和 `avatar_change`，统一计为更换头像。
- **名争改名新增 `nameWar_rename` 埋点**：每次改名记一条（含改名前后的昵称），供面板追踪名争活跃度。

#### 其它

- **前端 `formatGiveawayValue`**：展示从 `.toFixed(1)` 改为 `.toFixed(2).replace(/0+$/, "").replace(/\.$/, "")`，适配精度提升的同时去掉末尾无意义 0（`1.10` → `1.1`，`1.00` → `1`）。
- **`game-choice-icon` 样式**：`width: 46px` 改为 `min-width: 46px` + `white-space: nowrap` + `padding: 0 4px`，防止图标文字换行挤压。
- **`help.md`** 同步更新自救板额度按目标独立计算 + 认主认宠规则的说明。

### v2.4.1（2026-08-08）

本版本核心是从零引入用户分析（类 Google Analytics）能力，同时顺带完成两处目录结构调整、认主认宠的信息展示与上线提醒、以及一个大话骰死锁修复。**包含破坏性升级步骤，见下方「目录结构调整」。**

#### 新功能：用户分析 + 后台数据面板

- **三层解耦架构，RPC 路径上一句 SQL 都没有**：① 独立只读 SQLite 连接（`mode=ro`，WAL 下读不阻塞写）跑聚合查询；② 后台协程每分钟重算未封板的今天/昨天写入 `analytics_daily` 日聚合表，已封板的历史日不再重算；③ `atomic.Pointer` 无锁发布快照，`admin:analytics` RPC 只做指针 Load + 切片，不碰数据库。
- **双路采集**：前端埋点（页面浏览/会话/来源，`web/src/lib/analytics.ts` 经 `analytics:collect` 批量上报）+ 站内既有四张审计表（`connection_events`/`room_events`/`punishment_events`/`player_activity_events`）与服务端 `game_round` 事件（RPS/黑白棋/井字棋/五子棋/斗兽棋/大话骰对局结果，不依赖前端上报）。历史审计表首次启动会全量回填进日聚合，面板上线当天即有完整历史趋势曲线；只有页面浏览/来源/浏览器/归属地这几个新维度是从上线日起才有数据。
- **IP 归属地**：引入 [ip2region](https://github.com/lionsoul2014/ip2region) 离线库解析省份/ISP，回环/内网显示「本地」。IPv4 库（`config/xdb/ip2region_v4.xdb`）默认必需，缺失且未关闭该功能会直接启动失败；IPv6 库（`ip2region_v6.xdb`）可选，缺失只是 IPv6 来源访客解析不到归属地，不影响启动。新增 `npm run fetch-geoip` 一键拉取两份 xdb。
- **隐私设计**：分析表不落 IP/指纹/原始 UA；访客 id 为 `sha256(deviceKey ‖ salt)` 取前 16 位 hex（`work/analytics.salt` 首启随机生成，0600 权限）；地域/来源展示做 k-匿名（访客数 < 3 折进「其他」）；面板只显示省份 + ISP，不下钻到城市/具体 IP。
- **后台「数据分析」分区**（recharts）：DAU/会话/页面浏览/新访客/平均时长/在线峰值等核心指标+环比，设备/浏览器/系统/来源/省份/ISP 分布，游戏对局与结果分布，房间创建趋势，惩罚发布/完成/驳回与完成率，站点玩法（认主认宠/名争/白给）活跃度，聊天量，转化漏斗，留存矩阵，新老访客对比。
- **总开关**：`ANALYTICS_ENABLED=0` 关闭全部采集与聚合协程；`ANALYTICS_GEO_ENABLED=0` 关闭归属地解析（不加载 11MB xdb，也不做启动时的文件存在性检查）。另有 `ANALYTICS_TZ_OFFSET_MIN`（默认 480，即 UTC+8，决定「今天」何时切换）、`ANALYTICS_RAW_RETENTION_DAYS`（默认 90 天，原始事件保留期；会话保留 180 天；日聚合永久保留）。
- **开发中发现并修正的字段错位**：`ip2region_v4/v6.xdb` 实际字段顺序是「国家|省份|城市|ISP」，早期实现按旧版文档「国家|区域|省份|城市|ISP」误读，导致省份/ISP 错位。已在同一开发周期内修好解析并通过迁移清理/重算受影响的历史行（`connection_events` 重新解析回正确值；`analytics_sessions`/`analytics_visitors` 因不存原始 IP 无法重新解析，只能清空对应字段，面板显示「无归属地数据」而非错误值），不影响本次发布后的行为。

#### 目录结构调整（⚠️ 破坏性升级）

- **`data/` → `work/db/`**：`database.db`（及 `-wal`/`-shm`）现在位于 `work/db/` 下，`data/` 目录不再使用。
- **`config/` → `config/json/` + `config/xdb/`**：原按功能拆分的 JSON 配置整体挪进 `config/json/`；新增 `config/xdb/` 存放上面提到的 IP 归属地离线库（不入 git）。
- 旧版 `cp -a "$tmpdir/bin" "$tmpdir/dist" ...` 升级命令不会带上 xdb，按新流程升级的用户请参照 README「Docker Compose」章节的一次性手动步骤（放置 xdb 或设置 `ANALYTICS_GEO_ENABLED=0`），否则新版本会因缺少必需的 `config/xdb/ip2region_v4.xdb` 直接启动失败。
- Docker Compose 卷挂载相应从 `data:/app/data` 改为随 `work` 一起挂载；新增 6 个 `ANALYTICS_*`/`GEOIP_*` 环境变量透传。

#### 认主认宠（宠物乐园）

- **成员/候选列表补全性别/称号/阵营配色/⚡极限模式/⚔️名争/白给徽章**：此前这些字段只能靠前端在大厅在线名单里反查，对方离线一段时间后大厅列表会把人摘掉，徽章直接消失。现服务端在 `petbond:xxx` 视图里随成员/候选一起下发完整字段，离线也能正常显示。
- **主人/宠物上线推送提醒**：新增「我的主人/宠物上线」推送偏好开关（默认关闭，「个人设置 → 推送通知」可开）。判定为离线→在线的真实转换（而非同一会话刷新/重连保活）才触发，逐条通知该玩家名下每一位主人/每一只宠物。`help.md` 同步补充说明。

#### Bugfix

- **大话骰：不能开自己刚叫的点**：此前任意在场玩家均可开牌的改动（v2.3.3）留了一个漏洞——开自己叫的点会导致赢家=输家=自己，既没法给自己发布/审核惩罚任务，又会连带触发「受罚未完成不能离房」卡死退出流程。现服务端拒绝该操作并提示改由其他玩家开牌。

#### 其它

- 新增 5 个 SQLite schema 迁移（v17–v21）：为审计表补时间维度索引供聚合查询使用；新增 `players.push_bond_enabled` 列承载上线推送开关。
- `internal/geoip` 新包封装 ip2region 查询与启动期加载/降级逻辑；新增 `go.mod` 依赖 `github.com/lionsoul2014/ip2region`。

### v2.3.5（2026-07-31）

紧接 v2.3.4 的两处线上回归修复，以及一个前端字段规范化补漏。

#### Bugfix

- **Chrome/Edge WebSocket 握手失败**：v2.3.4 把 token/指纹从 `?token=` 查询串迁进 `Sec-WebSocket-Protocol` 握手头后，服务端 `AcceptOptions` 没有回选任何子协议。Safari/WebKit 不校验这条能连上，但 Chrome/Edge 严格按 RFC6455 校验——请求带了 `Sec-WebSocket-Protocol`，响应必须回选其中之一，否则握手直接失败。现在服务端固定回选 `auth.<token>`，Chrome/Edge 恢复正常。
- **Safari/iOS Web Push 全部收 403 BadJwtToken**：`VAPID_SUBSCRIBER` 默认值原为 `mailto:admin@rps.rbq.io`，但 `webpush-go` 内部只要 subscriber 不以 `https:` 开头就无条件再拼一个 `mailto:` 前缀，不检查是否已经带了——结果 JWT 里的 sub claim 被拼成 `mailto:mailto:xxx`。Apple 的 `web.push.apple.com` 严格校验 sub 的 URI 语法，直接 403 BadJwtToken 拒收；FCM/Samsung 网关不严格校验才让这个问题一直没被发现。现在环境变量约定直接填邮箱（不带 `mailto:` 前缀），服务端在传给库前统一 `normalizeVAPIDSubscriberForLibrary` 去掉 `mailto:`/`MAILTO:` 前缀，由库自己补一次。`.env.example`、`docker-compose.yml`、README 环境变量表全部同步更新，并加了单元测试防回归。

#### 可观测性

- **推送投递失败附带网关 body 片段**：之前非 2xx 响应只记状态码，出问题时只能靠猜（VAPID 签名错？密钥不匹配？认证头格式？）。现在限量读取 2KB body 片段一起塞进 `push_send_rejected` 审计日志——正是靠这一条才快速定位到上面 Apple 的 `BadJwtToken` 具体原因。

#### 前端

- **`normalizePublicStats` 补上 `selfTitle` 字段**：v2.3.2 引入的"玩家自定义称号"在归一化时被丢弃，前端某些视图因此看不到自设称号。补齐字符串类型透传。

### v2.3.4（2026-07-31）

#### 安全加固

- **Token/指纹从 URL 迁入 WebSocket 握手头**：之前 token 直接拼在 `/ws?token=` 查询串里，会被反向代理访问日志以明文记录（24 小时有效，拿到即可冒充该玩家上传证明图/头像）。现在通过 `Sec-WebSocket-Protocol` 头（`auth.<token>`、`fp.<base64url 指纹>`）传递——这是浏览器 WebSocket API 唯一能在握手阶段附带自定义值、又不出现在 URL 里的字段。指纹内容不受控，编码为 base64url 后再拼前缀，避免其中出现 HTTP token 语法不允许的字符导致握手失败。`wsprobe` 调试工具同步适配。
- **Web Push 端点白名单校验**：浏览器上报的 PushSubscription.endpoint 完全不可信——若不校验，任何登录用户都能把 endpoint 设为内网/回环地址，把服务器变成一个 SSRF 探测器。现在只接受已知浏览器厂商推送网关域名（googleapis.com / mozilla.com / windows.com / apple.com / samsungosp.com），非白名单直接拒绝并记审计日志。
- **管理员登录失败锁定**：按 IP 8 次（单桶）/ 全局 40 次（兜底）的 10 分钟滑动窗口，触发后冷却 10 分钟。只统计失败次数，正确口令不消耗配额。锁定与失败均记安全日志。
- **房间密码爆破防护**：按房间 + IP 维度 5 次/分钟的限流桶，触发后冷却 5 分钟。密码比对只在失败时扣配额（用恒定时间比较拆开的写法），正确密码不消耗。
- **推送订阅删除增加玩家归属校验**（`removeSubscriptionForPlayer`）：仅当 endpoint 确实属于该玩家时才删除，防止已登录用户通过换 endpoint 删除他人订阅。
- **房间聊天的 @ 提醒限定同房参与者**：此前任何人都能把陌生玩家 ID 塞进 mentions 字段触发推送，现在房间内只接受实际在该房间的玩家 ID，大厅聊天不受影响。
- **@ 推送通知加节流**：同一发送者对同一被 @ 玩家，每 10 分钟最多触发一次 Web Push（消息本身仍正常送达）。
- **白给投票防重放**：Add `GiveawayVotedTargets` 按投票时目标的板子版本（GiveawayBoardSubmittedAt）去重，同一条自救内容无法被重放反复计分，目标重新上板后可以再投。
- **Origin 校验全面收紧**：空 Origin 不再直接放行；ALLOWED_ORIGINS 里显式带 scheme 的条目必须与 Origin scheme 一致；localhost 旁路要求 Origin 与请求 Host 同时为本机地址（防止声称 `Origin=http://localhost` 绕过生产环境校验）。
- **活动日志从 CSV 改为纯文本 logfmt 格式**（`.csv` → `.log`）：CSV 里的字段（userAgent、错误信息等）完全来自客户端可控输入，被 Excel/WPS 打开时会执行公式注入；logfmt 是 `key=value` 纯文本，不会被电子表格误解析。含换行/特殊字符的 value 做双引号转义。
- **Service Worker 缓存策略加固**：从 `no-cache` 改为 `no-store, no-cache, must-revalidate, max-age=0` + CDN 专用头 + `Expires: 0` + `Service-Worker-Allowed: /`，确保任何缓存层都不会缓存旧版 SW。

#### 性能优化

- **大厅实时快照大幅裁剪**：全站历史玩家不再塞进 lobby FULL/DELTA——实时通道仅包含在线 + 断线 30 分钟窗口内 + 白给上板未过期者（`lobbyPlayerInLiveSnapshot` + `ToLiveLobbyPlayer` 去掉 GameStats）。全站档案改为 `players:roster` 鉴权+分页+限流按需拉取。这是流量与 CPU 最大的单一优化。
- **roomClients 反向索引**：房间/频道广播从 O（全服连接）降到 O（订阅者），`emitWireToRoom` 不再每次遍历全服所有连接做 `inRoom` 过滤。频道成员关系由 `clientJoinRoom`/`clientLeaveRoom` 双向维护。
- **lobbyPlayerToProto 手写字段映射**：替代 JSON marshal/unmarshal 的反射路径，大厅广播热路径（玩家数 × 广播频率）零反射开销。TitleColors 恒为非 nil 以对齐前端规范化树，防止 CRC 分叉。
- **Delta diff 算法优化**：`equalJSON` 不再每节点调用 `normalize` + `reflect.DeepEqual`，改为递归浅层类型分派；数组 diff 不再预检所有元素是否全等才下钻，直接逐元素 diff，未变叶子自然无 op，避免接近 O(N²) 的行为。
- **在线时长展示量化为整分钟**：毫秒级实时递增会让每次广播文档哈希都变，触发无意义 DELTA；现量化到 1 分钟步长后只在分钟边界才脏。
- **增量写盘**：新增 `markPlayerDirty` + `dirtyPlayerIDs` 集合——`writeSnapshot` 优先只 UPSERT 脏玩家，集合为空才退回全量（漏 mark 的兜底保证）。补上大量遗漏的 persist 调用点（战绩结算、白给值变动、在线时长 checkpoint、衰减游标、头像上传等）。
- **宠物乐园改为订阅制**：原 `notifyAllOnlinePetBondStates` 每次上线/关系变更时对全体在线玩家 O(O·P) 广播；现通过 `petbond:subscribe`/`unsubscribe` 只推给打开面板或需要强制白给的连接。
- **前端 player:batch 合并优化**：从每 patch 各一次 O(P log P) 双榜重排 + O(R) versus 扫描，改为单遍 O(P+R+k+P log P) 批量合并。
- **废弃 "players" 同步频道**：`sync:full` 旁路曾经能一次性无鉴权 dump 全站含 GameStats 的完整档案（O(P) 流量放大），现直接移除该路径——`sendFullChannel` 不再响应 `channel=="players"`，全站档案必须走 `players:roster`。
- **前端 useNow 钩子**：局部时钟替代 Room 组件全局 1Hz `setInterval`——棋盘/棋钟/倒计时只在各自需要时轮询，未启用时完全静默。
- **Delta CRC 校验优化**：连续 seq 的 delta 信任服务端 hash 跳过 CRC 重算；每 32 条抽检一次防归一化分叉潜伏；乱序/缺口时仍做全量 CRC。

#### Web Push 完善

- **Service Worker 全面升级为 push-sw-v3.js**：旧 `sw.js` 删除，新 SW 文件名带版本，配合 `no-store` 缓存策略确保及时更新。
- **前端协议版本校验**：订阅前检查 `/api/push/vapid-key` 返回的 `protocolVersion`，低于 3 时拒绝订阅并提示重建后端。`VAPID_SUBSCRIBER` 环境变量控制 VAPID 联系 URI。
- **测试推送精确反馈**：`push:test` 不再只显示盲目的"已发送"——返回订阅数、接受数、失败数、过期数。投递失败时按错误类型给出用户可理解的提示（过期需重订 / TLS 证书 / 网关拒绝等）。
- **推送投递共享 HTTP 客户端**：10 秒超时，防止某个卡住的网关把 goroutine 挂到 TCP 默认超时。
- **登出时自动解绑本机推送**：不写入"用户主动停止"标记（`markStopped: false`），下次登录仍可自动重订。开启推送偏好前先请求通知权限，避免卡在 permission-required。

#### 数据完整性

- **recordGameOutcome 改为 Server 方法**：替代旧的裸函数，统一调用 `markPlayerDirty` + `requestPersist("lazy")`，避免战绩更新后只靠 60 秒 checkpoint 兜底。
- **惩罚证明状态门控**：proof 只处于 pending 时才能被审核——此前已 rejected 的证明可被反复调用 reject 导致 RejectCount 无限递增扣分失控。
- **房间对局历史内存上限 100 条**，快照仍只下发最近 pageSize 条。
- **极限模式衰减游标落盘**：即便本小时扣分为 0 也落盘，避免重启后重复扫全表。排位分日衰减同理。
- **白给值变动立即 persist**：此前 `addGiveawayValue` 不掉盘，进程被强杀期间的白给值变动会丢失。

#### 前端优化

- **全局排行榜改为按需拉取全站档案**：`players:roster` 分页（500×10=5000 上限），含离线 + 分游戏战绩，不再依赖大厅实时快照。加载失败时降级为大厅在线名单。
- **Delta 连续 seq 优化**：连续 seq 的补丁原地打在文档上（跳过 structuredClone），信任服务端 hash；每 32 条做一次 CRC 抽检。
- **聊天加载上翻硬顶 800 条**：超过自动丢弃最旧条目，防止无限上翻撑爆内存。
- **图片上传 - 修复跨版本 chunk 404**：按需 import 的动态 chunk 在 `emptyOutDir` 重建后旧哈希丢失，页面停留跨越发布时首次触发会报 «Importing a module script failed»——现捕获后自动整页刷新换取最新产物，短时间重复命中不再刷新。

#### 其它

- **引入 `x509roots/fallback`**：修复 Docker 容器（基于 debian:trixie-slim）缺少系统 CA 根证书时 Web Push TLS 握手失败的问题。
- **sync:full 与 player:get 纳入事件处理器表**：此前作为 switch 分支提前 return 绕过 `checkRateLimit` 限流保护，现与其它事件统一受 rate limit + IP backstop 双重约束。
- **构建顺序改为先 server 后 web**：后端编译失败时不会留下"新前端 + 旧后端"的不一致状态。
- **README 升级指引补全**：Docker 升级加上 `--force-recreate`、`curl vapid-key` 版本校验步骤；`VAPID_SUBSCRIBER` 环境变量文档化。
- **新增 11 个测试文件**：覆盖管理员登录锁定、WebSocket 认证协议、Origin 校验、安全日志格式、聊天 @ 校验、白给投票限流、惩罚审核状态门控、sync:full 限流、大厅快照裁剪、排行榜分页拉取、称号配色 CRC 一致性等。
- **配置模块增加 `SetRootDirForTest`**：仅测试用，允许临时更换 rootDir。

### v2.3.2（2026-07-28）

- **删除自定义性别，回归查表法定阵营**：v2.2.5 引入的「自定义…」性别文本与阵营独立选择被撤销，性别只能从预设列表里选，阵营完全由所选性别的 `factionId` 查表决定（`resolveGender`/`validGenderSubmission` 不再接受自定义文本/独立阵营参数）；「女跨男」阵营（`trans_male_faction`，此前只有自定义文本能选中）新增 `FTM`/`假小子` 两个预设性别，使其重新可选。schema v15：存量选了自定义性别的玩家按其当前阵营改写成该阵营的默认预设性别，`players.custom_gender_label` 列随后删除。
- **新增自定义称号**：个人设置-修改资料新增「称号」输入框（留空即清除），展示优先级为管理员自定义 > 主人为宠物设置的称号 > 玩家自设称号 > 系统按排位分自动计算的称号（`PublicStats.SelfTitle`，`applyDisplayTitle` 补上第三层判断）；改称号记一条 `player_activity_events`（action `self_title_change`）。个人设置修改资料表单第一行改为「名字、称号」，第二行改为「阵营、性别」两个下拉框。
- **后台认领密钥显示框改版**：用户管理面板不再是「签发按钮 + 只读展示框」两件套，改成单个只读输入框，默认显示「点击显示认领密钥」；聚焦即签发一把新密钥（旧密钥立即作废）并自动复制到剪贴板，失焦 5 秒后（期间重新聚焦会取消这次复原）恢复默认文案，再次聚焦视为新一轮、又会签发新密钥。同一行的「阵营、性别、自定义性别」三栏相应改成「阵营、性别、认领密钥显示框」，其中阵营/性别固定 14em 宽，剩余空间给密钥框。
- **性别预设管理后台 UI 重做**：不再每条独立一行带阵营下拉，改为按显示文字分组的勾选框布局——同一文案可同时勾选多个阵营，删除时按文案整组删除，新增时自动起名「新性别N」。性别 ID 不再展示给管理员编辑，去掉了此前三栏等分布局里冗余的自定义性别占位栏。
- **新增帮助面板（新玩家入坑指南）**：`help.md` 在构建期由 Vite 插件编译为静态 HTML 嵌入前端产物（`import helpHtml from "help.md?html"`），通过顶栏「关于」→「看看怎么玩」打开，覆盖六款游戏规则与全局玩法说明。`web/vite-plugins/markdownHtml.ts` 负责编译，前端 `web/src/ui/HelpPanel.tsx` 负责展示。
- **在线时长展示改为小数小时**：1-24 小时区间不再拆成「x 小时 y 分钟」，改成一位小数（如 17 小时 30 分钟显示为「17.5 小时」）。
- **统一认主/认宠/解除关系的在线要求**：三种申请都要求双方在同意前保持在线，任一方提前下线即自动撤销该申请、回到未申请状态（此前只有认主/认宠如此，解除关系申请离线也不会失效，现已统一）；对方不在线时隐藏「申请解除关系」按钮。
- **删除 `pet_bond_request_approvals` 表**：逐条同意记录只需要在内存里追踪（`petBondRequest.Approvals`），服务器重启清空重来完全可以接受，不必落库；schema v16 对存量库执行 `DROP TABLE IF EXISTS`。
- **称号刷新丢失自设内容修复**：`player:batch`（大厅广播，他人上下线/刷新都会触发）携带的精简 `LobbyStats` 此前没有 `selfTitle` 字段，合并进 `me.player` 时会把刚设置好的自设称号覆盖抹空，导致重新打开个人设置或刷新页面后称号输入框读到空值；`LobbyStats` 补上该字段后随 `AsPublicPlayer` 一并带上。个人设置的称号输入框同时去掉了多余的默认值提示与优先级说明文案。
- **惩罚证明连续被驳回额外扣分（全部游戏通用）**：同一局内，胜方对同一条惩罚任务连续第 3 次判定「需要重做」时，系统额外扣除受罚方 100 排位分，第 4 次 200 分、第 5 次 300 分，此后每多一次连续驳回再加罚 100 分（`proofRejectionPenaltyPoints`），用于遏制败方靠反复敷衍证明拖延惩罚。计数挂在当局 `PunishmentTask.RejectCount` 上，随每局重新生成任务自然清零，一次审批通过即结束当次任务，不会跨局累计。触发扣分时房间内会有一条系统提示（`roomNotice`）告知双方本次扣除的积分，双方均可在聊天区看到。逻辑挂在通用的 `onPunishmentReview` 驳回分支上，六款游戏（含不走 Seats 体系的大话骰）共用同一套审核入口，无需按游戏单独接入。
- **TRUSTED_PROXY_COUNT 默认值从 1 改为 0（fail-closed）**：旧默认值 1 在无反代直连部署时，客户端可伪造 X-Forwarded-For 绕过限流/防多开；新默认值 0=直连且完全忽略 XFF（防伪造），部署在反代之后必须显式设为实际代理层数。收到 XFF 头但 TRUSTED_PROXY_COUNT=0 时，启动后首次会打印一条 WARNING 提示运维检查配置。
- **Web Push 发送与 WS 慢消费者关停异步化，避免冻结全局锁**：`sendPush` 原先在持有 s.mu 时同步查 SQLite 并发送 HTTP 推送，会阻塞全服所有玩家/计时器/断线流程，现整个查询+发送挪到独立 goroutine，锁内只做 nil 判断和值捕获。慢消费者的 `conn.Close()` 可能阻塞约 5 秒写 WS close 帧，此前同样在锁内同步执行，现改为只关 channel 不关连接，close 帧挪到独立 goroutine。
- **夜间模式开关从顶栏迁入个人设置**：顶栏移除日/夜模式切换按钮（站点名在房间内也恢复显示）；个人资料面板新增「夜间模式」开关卡片。
- **五子棋最后落子高亮改为双层描边**：深色/浅色/同色系棋盘上都能清晰辨认最后一步——白子内环深色描边、黑子内环白色描边，外环统一高饱和红色带外发光。
- **HEIC 图片解码从 heic2any 更换为 libheif-js**：支持更广的 HEIC/HEIF 变体；Node 端的 fs/path/crypto 模块通过空 shim 兼容浏览器打包。
- **对局记录远端图片懒加载**：`ProofImage` 改用 IntersectionObserver（rootMargin 400px），只加载滚入视口附近的图片，避免瀑布流一次性发起大量请求。
- **斗兽棋状态树 row/col 零值归一化**：`NormalizeFrontTree` 补齐 jungle 棋盘的 `lastFrom`/`lastTo` 的 row/col 零值，防止 protojson 丢弃零值 key 导致 DELTA CRC32 哈希分歧、强制回退 sync:full。
- **RPS 出拳阶段双方都未出拳时允许离席**：此前 RPS 进入 choosing 阶段后禁止一切离席/换房/观战，即便对手还没加入房间也会被卡住；现在只要 room.Choices 为空（双方均未出拳），就允许玩家自由离开战斗席。
- **对局记录滚动加载触发阈值从 48px 放宽到 200px**：减少敏感触发导致的意外加载，同时将聊天/对局记录的历史页大小拆分为不同常量（首屏 100/20，增量 50/10）。

### v2.3.1（2026-07-27）

- **新增斗兽棋**：与五子棋/井字棋同级的座位制玩法，7×9 棋盘，象>狮>虎>豹>狼>狗>猫>鼠；狮虎可跳河（河中有鼠阻挡则不可跳），仅鼠可入水，鼠可吃象（不能从水中上岸吃）、象不能吃鼠；陷阱中的敌子可被任意己方子吃掉；不可进入己方兽穴，先入对方兽穴或令对方无子可走者胜。双方准备后随机先手（A 执下方、B 执上方），支持认输请求、棋钟、排位、惩罚与断线判负。schema v11 为 `players` 表增加 `jungle_wins/losses/draws`。
- **新增认主 / 认宠（宠物乐园）**：大厅社交玩法，玩家可互为「主人—宠物」关系（允许一人多宠、一宠多主，默认各最多 3，可配置）。个人资料页可独立开关「开启认主」「开启认宠」「公开展示」——关闭开关不会解除已有关系，只禁止新增；关闭公开展示后不会出现在大厅关系图谱（已有关系仍保留）。申请流程：`seek_master`（向对方申请认主）/`seek_pet`（向对方申请认宠）/`release`（任一方申请解除），均需相关方同意；宠物在已有主人时再认新主，须已有主人全体同意。主人可为宠物设置称号（替换积分百分比称号标签展示）。配置见 `config/pet-bond.json`（面板标题、配额、称号长度上限），SQLite 落库 `pet_bonds` / `pet_bond_requests`（逐条同意记录只在内存追踪，不落库，见 v2.3.2 变更）；schema v10 为 `players` 增加 `bond_master_enabled` / `bond_pet_enabled` / `bond_public_display`。
- **大话骰多人平局记分**：3 人及以上对局中，本局非赢家/非输家的其余参战玩家结算时记为平局（不计排位分、不受罚），与两人对局的胜负判定分开处理，断线判负结算同步生效；大话骰不适用的「平局双方各罚一次」选项在该玩法下隐藏并强制关闭。
- **总局数 / 累计在线时长排行榜**：全局排行榜新增按「总胜场」与「累计在线时长」排序的榜单页签（`total_online_ms`，schema v9）。
- **顶部栏合并在线人数**：顶栏连接状态徽标与在线人数合并展示；修复自定义性别玩家断线重连时，本地缓存把合法的空 `genderId`（自定义性别的标志）误判为空值、回退成默认「男」性别的问题。
- **移除机器人（Bot）支持**：`config/bots.json`、相关 proto 字段、房间座位与锤子剪刀布 AI 策略一并删除，现仅支持真人在线对局。
- **认主强制宠物白给**：主人可在锤子剪刀布 / 黑白棋（排位房）/ 井字棋 / 五子棋对局中，强制已开启白给的宠物本轮/本局直接白给（锤子剪刀布下双方同时被强制白给时按平局结算，不再判双输）；后台新增「认主/认宠关系图谱」面板与配套管理接口。
- **累计在线时长统计修复**：此前只有正常断线时才会把当前连接时长写入 `TotalOnlineMs`，进程被强杀（`kill -9`）或热更新重启会丢失这段尚未落盘的时长；现在改为每 60 秒 checkpoint 一次，并在优雅关闭时兜底落盘，同时修复了加载时读丢已持久化数值的问题；schema v12/v13 从历史连接事件回填存量数据。
- **房间事件旧表迁移**：早期版本的 `rooms`/`room_join_events` 两张旧表虽已被 `room_events` 统一事件日志取代，但历史数据此前一直原地冻结、旧表也未删除；schema v14 把这两张表的历史行无损转换进 `room_events`（create/join/close 三种事件），转换完成后彻底删表。
- **名字争夺战徽标视觉统一**：受罚改名期间，房间/聊天里的名字展示不再使用单独隐藏性别/称号标签的特殊徽标样式，统一为与正常状态相同的结构（仅名字部分套一个高亮「胶囊」），聊天区同时补上此前遗漏的模式标签（极限模式等）。
- **管理员用户管理优化**：「积分」栏改为显示/编辑真实存储分——此前展示的是按后台 `ranked-score.json` 封顶后的值，管理员直接保存会把真实分误砍到展示上限；玩家列表每行的标签前新增头像展示，与排行榜一致。

### v2.2.5（2026-07-23）

- **性别与阵营解耦，新增自定义性别与「女跨男」阵营**：`config/genders.json`（新增，性别预设扁平列表，每项只带 `factionId` 归属）与 `config/gender-factions.json`（阵营，含配色与 `taskGroup`）拆成两个独立配置文件，阵营不再嵌套 `genders` 数组；后台「性别与阵营」面板分别管理两者。阵营从 4 个扩到 5 个并重新命名（`男性阵营`→`顺性别男`、`女性阵营`→`顺性别女`、`男娘阵营`→`男跨女`，新增`女跨男`，`其他阵营`→`无性别`），称号/惩罚任务仍按 `taskGroup`（male/female/default）取文案，与阵营具体是哪几个、叫什么名字解耦。个人资料页/建号页新增「自定义…」选项，可填 1-9 字符的自定义性别文本（不占用预设性别名额，不能与已有性别文案重复），阵营选择与性别选择彻底独立，不再由性别反推阵营。
- **后台新增「玩家管理」面板**：可按在线 / 名争开启 / 白给值降序 / 积分降序 / 最近 7 天登录 / 积分非零筛选并分页浏览全体玩家（默认 200 条上限，硬上限 500），支持编辑名字、排位积分、称号（含「自定义称号」标记，见下方「排位积分」章节）、性别/阵营、白给值（数字输入框，>0 直接设置并开启，其余视为清空并关闭白给），以及代玩家查看认领密钥（帮玩家找回账号，无需玩家提供设备）。
- **白给值新增「胜利递减」机制**：白给已开启的玩家每赢一局（含各游戏的断线判负）都会按 `config/giveaway.json` 的 `winPenaltyValue`（默认 1）扣减白给值，五款游戏统一生效——此前只有点赞会降低白给值，赢局本身完全不影响白给值。主动加成、点赞/倒赞增减值与每小时次数上限等原先硬编码的数值，一并改为 `config/giveaway.json` 可调（`activeBoostValue`/`likeVoteValue`/`likeVoteLimitPerHour`/`dislikeVoteValue`/`dislikeVoteLimitPerHour`）。
- **管理员自定义称号**：见下方「排位积分」章节。
- **通用强制判定胜负**：黑白棋专属的强制结束/重开操作，改为覆盖 RPS/井字棋/五子棋/黑白棋四种座位制游戏的通用后台操作（`forceSeatOutcome`：判 A 胜/判 B 胜/判平，`forceNext`：重开）。
- **房间生命周期事件统一记录**：`rooms`/`room_join_events` 两张旧表冻结不再写入，改为单张 `room_events` 追加日志（`action` 取 create/join/close 一行一事件），创建事件额外记录房间密码的无盐 SHA256 哈希（供事后审计追溯，不落明文），历史行不做迁移搬迁。
- **手机端模块折叠记忆**：房间玩家列表、对局历史、聊天面板、排行榜、白给自救板、名字争夺战败方看板等区块在手机端新增折叠三角，展开/折叠状态按模块记入 `localStorage`，跨大厅/房间重进沿用；桌面端不受影响，始终展开。
- **后台管理面板按需加载**：拆分为独立的 `AdminViews.tsx` 模块，普通玩家不再下载这部分代码，减小首屏包体积。
- 旧版前端双 UUID 拼接格式的身份凭据（`playerSecret`）在校验通过后会被静默换发为新版单 token 格式，原地替换、用户无感知。
- schema v7/v8：`players` 表新增 `title_custom`（管理员自定义称号标记）、`faction_id`/`custom_gender_label`（性别/阵营解耦后各自独立存储）列。

### v2.2.2（2026-07-21）

- **黑白棋/五子棋新增计时（棋钟）**：建房可选「每子时长」（0/30/45/60/90/120/180 秒，默认不限）与「每局时长」（0/5/10/15/20/30/45/60 分钟，默认不限，双方各自独立累计，类似传统棋钟）。轮到己方时开始计时，落子后暂停直到轮到对方；五子棋的悔棋/认输请求处理期间同样暂停双方计时，处理完毕后重新起算，不占用任何一方的时间；黑白棋白给/上贡结算等待窗口走独立的超时机制，同样不占用棋钟。任一方某步超过「每子时长」或总时长耗尽即超时判负（黑白棋按既有强制结束流程结算，触发排位保底；五子棋按普通终局流程结算）。棋盘上方新增倒计时条，本步剩余 ≤10 秒变色提醒。
- **房间/大厅卡片新增设置标签**：黑白棋/五子棋显示"计时：xx秒/xx分钟"（未启用计时则不显示）；五子棋额外显示悔棋次数（"禁止悔棋"/"允许悔棋N次"）；大话骰显示参战人数区间（如"3-7人"）。这些设置此前进房后才能看到，现在大厅列表就能看到。
- **身份认证迁移期兼容代码删除**：早期版本的单值哈希凭据字段 `PlayerSecretHash`、其读写与自动迁移分支（`verifyPlayerSecret` 里的兼容判断、`hashSecret` 函数）已随现网存量部署完成迁移后一并删除，身份校验现在只查明文 `PlayerSecrets` 列表；`players` 表的 `player_secret_hash` 列由新的 schema v6 迁移在启动时自动清理。
- **`data/players.json` → SQLite 一次性迁移代码删除**：`migratePlayersJSONIfNeeded`/`migrateGameStats`/`LegacyOthelloStats` 均已移除，`loadPlayersFromDisk` 现在纯粹从 SQLite 加载；SQLite 写失败时的降级保底写（`writePlayersJSONFallback`）不受影响，仍然保留，二者方向相反、互不依赖。
- **`punishment_events_legacy` 隔离表新增自动转换**：schema v4 迁移（`convertLegacyPunishmentEvents`）按房间+被罚玩家+任务文本，把旧版一次性隔离改名保留的 `punishment_events_legacy`（`kind=task`/`kind=proof` 两行式旧结构）尽量拼回新版一行一任务结构，转换完即丢弃该表，不再无限期滞留。
- **清理两个历史遗留列**：schema v5 删除 `rooms.code`（旧版房间码，新代码已不再读写）；schema v6 删除 `players.player_secret_hash`（见上）。

### v2.2.0（2026-07-19）

- **排位积分不再设上下限，展示改为可配置封顶**：数据库/内存里的真实积分永久无上下限（胜负结算、管理员改分、每日衰减均不 clamp）；`config/ranked-score.json`（后台「排位分设置」）配置的展示上限/下限/名字争夺战下限只影响大厅、房间、个人资料、排行榜等下发展示时的封顶值，默认 `+4999` / `-4999` / `-9999`。后台调低上下限不会清零/拉低老用户的真实分数，只是展示层跟着新范围封顶。
- **新增每日积分衰减**：每 24 小时把每位玩家的真实排位分乘以可配置比例（默认 `0.98`）并向 0 截断，正负分都朝 0 收缩，避免头部玩家躺分不再游玩、也让新人更容易追上——与「极限模式」原有的整点扣分是两套独立机制。
- **新增历史最高/最低分记录**：个人资料页与全局排行榜新增"历史最高分"/"历史最低分"，永久记录真实分数触及过的极值，不受每日衰减和展示封顶影响，用于在积分被封顶展示后仍保留"刷榜"成就感。
- **新增「暂停注册」开关**：`config/access-control.json` 的 `registrationDisabled` 打开后，拒绝一切新建玩家（已有账号仍可正常登录游玩），用于临时限流或维护期间控制新增用户。
- **新增纯 IP 维度的兜底限流**：浏览器指纹由客户端上报、脚本可随意伪造来绕过按设备的限制，现在同一出口 IP 的同时在线人数（`maxOnlinePerIpTotal`）、10 分钟新建玩家次数（`maxCreatesPerIp`）、session 签发次数（`maxSessionIssuePerIp`）、WS 事件频率（`ipBackstopMultiplier`/`ipBackstopMinLimit`）都叠加了一层与设备指纹无关的硬上限。
- **新增按玩家维度的证明图片上传限流**（`maxProofUploadsPerPlayer`），在原有按 IP 限流之外再防"多账号分散上传"绕过。
- **新增单个房主可同时持有的活跃房间数上限**（`maxActiveRoomsPerOwner`，默认 3），超过上限需先关闭其他房间才能再开新房。
- 修复：管理员在玩家管理面板编辑资料时，若压根没有改动积分栏，不会再把该玩家已超出展示上限/下限的真实存储分数误覆盖为封顶值。
- 修复：`RankedScoreConfig` 当初只加进了 Go/TS 的类型定义，没有同步加进 `api/proto/game.proto` 的 `AppConfig` 消息（以及重新生成 `internal/wire/game.pb.go` / `web/src/gen/proto.js`），导致前端拿到的 `config.rankedScore` 恒为 `undefined`，个人资料页读取 `config.rankedScore.nameWarMin` 时抛异常、直接白屏。现已补上 proto 字段并重新生成两端代码；前端 `materializeConfig` 再补一层默认值兜底。
- 排行榜补齐"历史正"/"历史负"两个榜单页签；排序用真实分（`sortRankedPoints` 等），展示仍按配置封顶；历史最高/最低展示同样封顶。
- 称号分段改为相对展示上下限的百分比（`minPercent`/`maxPercent`）；名争失格线改为可配置 `nameWar.penaltyThreshold`（默认 -4999）。
- 修复：称号分段表中间若留有空隙（未覆盖到的百分比区间），落在空隙里的分数此前会被无条件夹到最高档称号，现改为就近夹到距离最近的分段。
- 修复：`schema_version==0` 的旧库会跳过加列迁移却标成最新版本，导致 `highest_score` 等列缺失、玩家加载失败——迁移改为幂等且 version=0 也会执行。
- 修复：服务优雅关停时给仍存活的连接/房间补写审计记录（`closeLiveStateOnShutdown`）存在与正常关闭路径并发写同一房间审计行的窗口，现改为把"摘出待关闭房间"和关闭动作放进同一段临界区，消除该重复写入窗口。
- 聊天历史加载附带 `authors`（按 playerId 从内存玩家表取当前资料，含离线），前端与在线名单合并后渲染，不再依赖发送时快照。

### v2.1.28（2026-07-18）

- **新增五子棋（Gomoku）**：与黑白棋/井字棋同级的第五种玩法，15x15 棋盘，双人回合制，五子连线（横/竖/斜均可）判胜；建房可选悔棋次数 0/1/3/10（默认禁止悔棋），申请后对方 30 秒无响应自动拒绝；支持认输请求（需对方确认）；断线判负、排位结算、惩罚任务与其余座位制玩法走同一套逻辑。
- **头像上传**：个人资料页可上传头像，前端本地压缩裁切成正方形 WebP（≤20KB）后上传，服务端校验真实格式；可清空恢复默认的"首字头像"。头像会同步显示在大厅列表、房间座位、聊天等所有展示玩家的地方。
- **玩家档案迁移到 SQLite**：`data/players.json` 改存进 `data/database.db`（与聊天/房间事件共用同一连接），旧文件启动时幂等自动导入后改名为 `players.json.migrated`；SQLite 不可用时自动降级回写 JSON，不影响功能。
- **战绩统计模型泛化**：原来只有黑白棋单独维护一套"胜/负/平/局数/吃子/被吃"统计，现在改成锤子剪刀布/黑白棋/井字棋/五子棋/大话骰五个玩法各自独立的胜/负/平计数（不再单独记吃子数等黑白棋专属字段），个人资料页的战绩明细相应更新。
- **每日公告弹窗 → 平台用户安全与免责声明**：新增一个先于连接完成展示的强制声明页（文案固定写在前端，不可在后台自定义，仅保留 `security-disclaimer.json` 的整体开关）。声明页至少停留 3 秒（3 秒内选完两道单选题也不会点亮「确定」），只有「清楚」+「能」都选中且满 3 秒才能进入网站；声明页在 WS 连接建立之前就展示，连接过程与这 3 秒强制停留并行，不再叠加等待。每天每个浏览器只需确认一次，建角色前也会展示。
- **顶栏「赞助」→「关于」**：原「每日公告弹窗」的内容（标题+正文，后台可编辑）搬到「关于」面板里，展示为一块常驻的公告板，不再是弹窗，也不再需要按钮文字/版本标识两个字段。
- **锤子剪刀布"放过对方"机制微调**：平局不再消耗待生效的命运安排名额（此前只有双白给不消耗）；白给回合的结果与出拳无关，命中白给时会直接清空待生效的名额而不是拖到未来某局才生效；命中"命运的干预"时结算文案会说明原因。
- **配置文件迁移健壮性修复**：本版本把 `daily-announcement.json` 改名为 `announcement-board.json` 并新增 `security-disclaimer.json`；补上了对应的自动迁移/兜底创建逻辑，避免已在跑的部署按官方升级流程（保留旧 `config/` 目录）升级后因缺文件启动失败。
- **大厅房间列表排序修复**：Go 的 map 遍历顺序本身是随机的，之前每次广播大厅快照时房间顺序会跟着抖动，现按房间 id 稳定排序。
- **wire 协议清理**：删掉了几十个手工维护的 `has_XXX` 占位字段（原本用来在 protobuf 里模拟"字段是否被显式设置过"），改成前后端一致的"按字段名单统一补零"机制，减少协议体积和后续新增字段时的样板代码。
- 惩罚任务事件日志表结构调整：一次任务从发布到完成算一行（按 id 更新），而不是发布/提交/驳回各开一行，审计记录更容易按任务串起来看。
- 新增/补充了一批后端单测（玩家 SQLite 存取、头像上传、战绩统计、配置迁移、大话骰惩罚事件等）。

### v2.1.27（2026-07-16）

- **大话骰体验修复**：
  - 叫点面板颗数改为下拉选择（原为数字输入），并按"上家点数 &lt;6 则颗数不变点数+1，=6 则颗数+1点数回到1"自动预选最小合法叫点；除首次摇骰展示自己手牌外，其余叫点/结算文案不再用骰子字体，直接显示数字。
  - 开牌结果面板"下一局"按钮与骰子列表贴在一起的问题修掉，胜负配色改用页面既有的蓝/粉色系（不再是刺眼红绿）。
  - 房间标签/默认房间名此前会显示成"锤子剪刀布""出拳中"/"新的锤子剪刀布房间"，补上大话骰专属文案与配色。
  - 对局记录里大话骰此前显示无意义的"未出拳"，改为按胜负显示；切到后台时收不到"轮到你了"提醒的问题（判断逻辑只认 A/B 座位，大话骰没有座位）一并修复。
  - **开惩罚的大话骰房间结算时看不到每人骰子点数、直接跳到惩罚阶段**：惩罚流程会在同一次结算里把房间阶段从"结算展示"直接推进到"惩罚阶段"（这是通用设计，其它玩法的结算面板本就同时兼容这两种阶段），大话骰的开牌揭晓面板之前漏了这个兼容判断，现已修复。

### v2.1.26（2026-07-16）

- **多端身份认领**：个人资料页新增一次性「认领密钥」，可在另一台设备粘贴认领当前身份；一个身份最多同时"记住" 3 台设备（挤掉最早一条，不挤当前活跃会话）；服务端仍是单 socket 模型，新设备登录会先确认是否顶替旧设备在线会话，被顶替端收到提示。新增登出按钮（清空本地凭据）。详见「多端身份认领」章节。⚠️ 旧版哈希凭据自动迁移为明文列表，一个月后将删除迁移兼容代码。
- **推送通知**：个人资料页可分别开启「聊天 @ 我」「轮到我出招/落子」「我的房间来人了」三类提醒。页面在前台/后台切换时优先走浏览器原生 `Notification`（Level 1）；玩家完全断线（关闭标签页/浏览器）时改由 Service Worker + Web Push 推送（Level 2），自建 VAPID 密钥，不依赖第三方推送服务。**需要 HTTPS（或 `localhost`）才能授权通知权限**，纯 HTTP 内网访问会一直提示无权限，属浏览器安全限制。
- **新增大话骰（Liar's Dice）**：与锤子剪刀布/黑白棋/井字棋同级的第四种玩法，支持 2-8 人（房间设置可调最少/最多参战人数）。进房默认观战，对局开始前可自由加入/离开参战席；全员准备且名单 5 秒无变动后自动开局，每人摇 5 颗私有骰子（只推送给玩家本人）；按入席顺序轮流叫点或开牌，1 为万能点（一旦被喊出则本局失效）；断线判负规则为"上家"（入席顺序前一位）胜。支持排位积分与惩罚（含"玩家发布任务"模式）。详见「大话骰」章节。

### v2.1.25（2026-07-15）

- **聊天持久化**：房间聊天与大厅留言板改为写入 SQLite（`data/chat.db`，`mattn/go-sqlite3`），重启不再丢失历史；历史通过 `chat:load` / `chat:loadOlder` 分页拉取（瀑布流，滚到顶部自动加载更早 100 条），实时增量走新的 `chat:new` 频道推送。
- **留言板并入聊天**：大厅原有的独立「留言板」（`suggestion:add`）下线，与大厅聊天合并为同一套 UI/存储，房间内的「大厅」tab 与大厅页共用同一份数据。
- **@提及**：聊天可以点头像 @ 某位玩家，被 @ 的消息气泡高亮显示；每条消息最多记录 20 个提及对象。
- **构建方式变更**：`bin/server` 恢复为 CGO 动态链接（`CGO_ENABLED=1`，因为 sqlite3 驱动需要 CGO），不再是 v2.1.24 的纯静态二进制；`docker-compose.yml` 基础镜像由 `debian:bookworm-slim` 换为 `debian:trixie-slim`（更高版本 glibc），避免构建机 glibc 版本高于运行环境导致二进制无法启动。**升级注意**：沿用旧部署包（`docker-compose.yml` 仍是 `bookworm-slim`）的用户需要拉取本次 Release 包中的新 `docker-compose.yml`，否则新二进制可能因 glibc 版本不够而无法启动。

### v2.1.24（2026-07-15）

- **黑白棋棋子清晰度**：`disabled` 格子整体变暗（`opacity:.6`）会连带压暗棋子；棋子改为格子外层的独立兄弟节点，格子变暗与棋子透明度（固定 0.8）互不影响，格子仍是正方形、棋子占格子边长 50%。
- **Chrome 无法选择 HEIC 证明图**：`<input accept="image/*">` 通配符会用 Chrome 内置的图片扩展名表过滤文件选择器，该表不含 heic/heif，导致即使额外列了 `.heic,.heif` 也可能被隐藏；改为只列具体扩展名/MIME，不再用通配符，选完文件后的校验/压缩/上传流程不变。
- **安全与稳定性加固**：
  - WebSocket 广播/应答不再在持有全局锁时做同步网络写，改为每连接独立发送队列 + 写协程，避免单个慢客户端拖慢全服；连接队列写满会主动断开该连接。
  - 修复握手阶段在锁外操作共享同步状态可能触发的并发 panic。
  - `X-Forwarded-For` / `X-Forwarded-Host` 改为按 `TRUSTED_PROXY_COUNT`（默认 1）解析可信代理层数，不再无条件信任客户端可伪造的最左侧值。
  - 管理员口令改为恒定时间比较；`config/*.json` 权限收紧为 `0600`；`/api/config/export` 增加专属限流，口令改用请求头传递（不再出现在 URL / 反代访问日志里）。
  - 管理员踢人（kick）不再删除持久玩家的存档（积分/战绩/称号），只强制其下线，账号可正常重新登录。
  - 惩罚/房名等随机选择改为真随机（原实现按时间戳取模，可预测且同毫秒内取值相同）。
  - 限流、防多开、玩家历史出拳等内存状态定期清理，避免长期运行内存增长。
  - 进程收到关停信号时改为 `Shutdown`（原为 `Close`），给在途 HTTP 请求收尾时间，而不是硬切。
- **日志体系重做**：
  - 命令行只保留启动横幅和「进程无法继续运行」级别的致命错误；连接、建号、改名、白给/名字争夺战/极限模式开关、留言、聊天、惩罚等事件不再打印到终端，全部落盘到 `work/logs/{ISO年+周}/*.csv`。
  - 新增 `error.csv` 记录鉴权失败、限流、越权等拒绝/异常事件，字段拆成标准 CSV 列（而非一整块 JSON 字符串）。
  - `players.csv` 补充 `rename`（改名）、`giveaway_enable`/`giveaway_disable`、`giveaway_board_submit`（白给自救板内容）、`nameWar_enable`/`nameWar_disable`、`extreme_enable`/`extreme_disable` 等操作记录。
  - `chat.csv` 的 `channel` 只保留 `room` / `lobby` 两种取值；留言板（原 `lobby_suggestion`）并入 `lobby`，对应真正在用的「大厅聊天」体验。
- **文档**：README 删除反向代理配置示例与维护约定说明；补充 `TRUSTED_PROXY_COUNT`、`MAX_SOCKETS_PER_IP` 环境变量说明。

### v2.1.23（2026-07-13）

- **配置拆分**：取消 `default.json` / `active.json` 双轨；按功能拆成 `config/*.json` 原地读写；旧单体启动时自动迁移为 `*.bak`。构建与启动修正 `config/*.json` 写权限、`bin/server` 执行权限。
- **独立部署包**：Release 含 `bin/server`、`dist/`、`config/*.json`、`docker-compose.yml`、`.env.example` 与简要 README，解压即可 `docker compose up -d`。
- **战绩展示**：protobuf 省略 `0` 导致个人资料/排行榜出现 `undefined`/`NaN`（如总局数、胜负平、排位积分）；`materializePlayer` + `safePlayerStats` 统一补 0，空称号显示「暂无称号」。
- **防多开配置字段**：`maxCreatesPer10Min` 与 protobufjs `maxCreatesPer_10Min` 错位导致后台显示 undefined、保存后看似不生效；前端归一化并修正 gen 字段名。
- **惩罚任务占位符**：系统/玩家任务文案支持 `{loser}`（败者）、`{winner}`（胜者）昵称替换。
- **加密房密码框**：`stripHasFlags` 不再误删业务字段 `hasPassword`；presence 标记仅在有 companion 时剥离，并还原 `false`/`0` 省略值。
- **房间坐标 / 增量状态**：黑白棋·井字 `row/col=0` 归一化；DELTA 路径补丁 + CRC-32 合并校验；`ready`/`score` pair 缺省零值修复。
- **房间信息标签**：`room-info-tags.json` 中文名与配色；前端 key 缺失回退默认文案。
- **其它**：惩罚提交/审批链路加固；RPC pending 与 reply 错误回传；Safari 房间布局；产物不入库改走 GitHub Release。

### 2026-07-13（前序）

- **全链路 Protobuf**、状态增量方案 A、前端结构拆分、handlers guard、null 兜底等（见 git 历史）。

### 2026-07-12

- **在线人数**：`player:batch` 支持插入新玩家并按 `connected` 重算人数；新上线 `forceBroadcastLobby`，修复「下线 -1 正常、上线需刷新才 +1」。
- **WebSocket 稳定性（生产 / Safari）**：
  - 根因确认：Safari 对 `permessage-deflate` 不兼容；`NoContextTakeover` 仍是同一扩展，不能单独救 Safari。
  - **UA 分流压缩**：Safari / iOS 关闭压缩，Chrome 等使用 `CompressionContextTakeover` 省流量。
  - 服务端 20s 协议 Ping；客户端 25s 应用层心跳、半开检测、回前台探活。
  - 反代：`X-Forwarded-Host` Origin 校验；同 SID 重连先释放连接名额；WS 升级少塞响应头。
- **防多开**：引入 FingerprintJS；`deviceKey = sha256(ip||fingerprint)`；同时在线 / 10 分钟新建 / 套接字上限均按设备键（配置字段名兼容旧版）。
- **体验**：惩罚阶段任务图、证明图可点击放大（与对局记录一致）。
- **会话 token**：WS 握手失败（含 401→浏览器 1006）自动丢弃本地 token 并 `POST /api/session` 换发（限次），避免密钥轮换后旧 token 永久卡在「正在连接…」。
- **同 SID 双连**：旧 socket 的 onclose 不再误清新连接；`replaced` 不盲目重连，降低双标签互踢。
- **稳定性**：修复 `socket_duplicate` 后 `assignment to entry in nil map` panic（顶替连接与 device map 竞态）；相关 map 写入统一 ensure。
- **欢迎公告**：默认文案增加交流 QQ 群 **432398160**（Bug 反馈 / 新功能）。
- **配置加载**：去掉 `config.go` 内嵌默认文案，只认 `config/*.json` 拆分文件；缺文件/校验失败直接报错。

### 2026-07-11

- **目录整理**：前端迁入 `web/`；删除旧 Node.js 后端（`src/server`、socket.io/express 等）；根目录只保留 Go 模块与编排脚本（`npm run dev` / `npm run build` / `npm start`）。
- 后端由 Node.js/Express/Socket.IO **完整重写为 Go**；Protobuf 线协议 + 通用增量同步 + 大厅 `LobbyPlayer` 精简 + 玩家更新 100ms 聚合 + 房间广播默认 100ms。
- 证明图前后端校验与长期静态缓存；默认端口 **9988**。
- 修复 `nil` 切片 JSON `null` 白屏、player:batch 误删状态、哈希 HTML 转义不一致等。
- **登录态刷新**：修复已登录用户反复刷新时在登录页/大厅间跳转；仅在 WS 连通后恢复会话，避免「未连接」误清 token。
- **实时推送**：
  - 房间广播带上 recent `roundHistory`（自定义任务、证明不再需要手动刷新）。
  - 空房删除时大厅立即全量刷新；离房后补发大厅 FULL，避免幽灵房间。
  - `roomNotice` 同步 `chat:append`，系统提示即时可见。
- **惩罚流程**：创建房间默认开启「惩罚需对手确认」；系统任务与自定义任务一致，败方提交后进入 `pending` 由胜方审批，不再直接开新局。
- **对局记录**：展示任务完成证明（状态/文字/图片/审核备注）；前端按 id 合并 history，避免覆盖丢失。
- **黑白棋终局文案**：白给/上贡改为统计摘要，不再把每一手明细拼进结果句。
- **Docker 部署**：Release 独立部署包（含 compose / config / env 模板 + bin/dist）；也可源码本机构建后挂载。
