# dependencies
node_modules/
web/node_modules/

# 构建产物：不入库，通过 GitHub Release 分发（见 README）
bin/
dist/
web/dist/
dist-server/
*.exe
*.test

# runtime data
work/
outputs/
config/json/*.bak
config/json/*.json.bak
# IP 归属地库（约 11MB，不入 git；本地用 npm run fetch-geoip 拉取）
config/xdb/*.xdb

# tooling / OS
.omo/
.claude/
CLAUDE.md
.DS_Store
*.log
vendor/

# docker local env
.env
online/
*.tar.gz
