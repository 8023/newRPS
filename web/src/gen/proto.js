/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $TypeError = $util.global.TypeError, $String = $util.global.String, $Number = $util.global.Number, $Boolean = $util.global.Boolean, $parseInt = $util.global.parseInt, $BigInt = $util.global.BigInt, $isFinite = $util.global.isFinite, $Array = $util.global.Array;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const game = $root.game = (() => {

    /**
     * Namespace game.
     * @exports game
     * @namespace
     */
    const game = {};

    game.GenderColors = (function() {

        /**
         * Properties of a GenderColors.
         * @typedef {Object} game.GenderColors.$Properties
         * @property {string|null} [textColor] GenderColors textColor
         * @property {string|null} [backgroundColor] GenderColors backgroundColor
         * @property {string|null} [borderColor] GenderColors borderColor
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GenderColors.
         * @memberof game
         * @interface IGenderColors
         * @augments game.GenderColors.$Properties
         * @deprecated Use game.GenderColors.$Properties instead.
         */

        /**
         * Shape of a GenderColors.
         * @typedef {game.GenderColors.$Properties} game.GenderColors.$Shape
         */

        /**
         * Constructs a new GenderColors.
         * @memberof game
         * @classdesc Represents a GenderColors.
         * @constructor
         * @param {game.GenderColors.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GenderColors = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GenderColors textColor.
         * @member {string} textColor
         * @memberof game.GenderColors
         * @instance
         */
        GenderColors.prototype.textColor = "";

        /**
         * GenderColors backgroundColor.
         * @member {string} backgroundColor
         * @memberof game.GenderColors
         * @instance
         */
        GenderColors.prototype.backgroundColor = "";

        /**
         * GenderColors borderColor.
         * @member {string} borderColor
         * @memberof game.GenderColors
         * @instance
         */
        GenderColors.prototype.borderColor = "";

        /**
         * Creates a new GenderColors instance using the specified properties.
         * @function create
         * @memberof game.GenderColors
         * @static
         * @param {game.GenderColors.$Properties=} [properties] Properties to set
         * @returns {game.GenderColors} GenderColors instance
         * @type {{
         *   (properties: game.GenderColors.$Shape): game.GenderColors & game.GenderColors.$Shape;
         *   (properties?: game.GenderColors.$Properties): game.GenderColors;
         * }}
         */
        GenderColors.create = function(properties) {
            return new GenderColors(properties);
        };

        /**
         * Encodes the specified GenderColors message. Does not implicitly {@link game.GenderColors.verify|verify} messages.
         * @function encode
         * @memberof game.GenderColors
         * @static
         * @param {game.GenderColors.$Properties} message GenderColors message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenderColors.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor") && message.textColor !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.textColor);
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor") && message.backgroundColor !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.backgroundColor);
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor") && message.borderColor !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.borderColor);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GenderColors message, length delimited. Does not implicitly {@link game.GenderColors.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GenderColors
         * @static
         * @param {game.GenderColors.$Properties} message GenderColors message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenderColors.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GenderColors message from the specified reader or buffer.
         * @function decode
         * @memberof game.GenderColors
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GenderColors & game.GenderColors.$Shape} GenderColors
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenderColors.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GenderColors(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.textColor = value;
                        else
                            delete message.textColor;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.backgroundColor = value;
                        else
                            delete message.backgroundColor;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.borderColor = value;
                        else
                            delete message.borderColor;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GenderColors message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GenderColors
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GenderColors & game.GenderColors.$Shape} GenderColors
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenderColors.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GenderColors message.
         * @function verify
         * @memberof game.GenderColors
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GenderColors.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor"))
                if (!$util.isString(message.textColor))
                    return "textColor: string expected";
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor"))
                if (!$util.isString(message.backgroundColor))
                    return "backgroundColor: string expected";
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor"))
                if (!$util.isString(message.borderColor))
                    return "borderColor: string expected";
            return null;
        };

        /**
         * Creates a GenderColors message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GenderColors
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GenderColors} GenderColors
         */
        GenderColors.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GenderColors)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GenderColors: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GenderColors();
            if (object.textColor != null)
                if (typeof object.textColor !== "string" || object.textColor.length)
                    message.textColor = $String(object.textColor);
            if (object.backgroundColor != null)
                if (typeof object.backgroundColor !== "string" || object.backgroundColor.length)
                    message.backgroundColor = $String(object.backgroundColor);
            if (object.borderColor != null)
                if (typeof object.borderColor !== "string" || object.borderColor.length)
                    message.borderColor = $String(object.borderColor);
            return message;
        };

        /**
         * Creates a plain object from a GenderColors message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GenderColors
         * @static
         * @param {game.GenderColors} message GenderColors
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GenderColors.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.textColor = "";
                object.backgroundColor = "";
                object.borderColor = "";
            }
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor"))
                object.textColor = message.textColor;
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor"))
                object.backgroundColor = message.backgroundColor;
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor"))
                object.borderColor = message.borderColor;
            return object;
        };

        /**
         * Converts this GenderColors to JSON.
         * @function toJSON
         * @memberof game.GenderColors
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GenderColors.prototype.toJSON = function() {
            return GenderColors.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GenderColors
         * @function getTypeUrl
         * @memberof game.GenderColors
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GenderColors.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GenderColors";
        };

        return GenderColors;
    })();

    game.GenderOption = (function() {

        /**
         * Properties of a GenderOption.
         * @typedef {Object} game.GenderOption.$Properties
         * @property {string|null} [id] GenderOption id
         * @property {string|null} [label] GenderOption label
         * @property {string|null} [factionId] GenderOption factionId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GenderOption.
         * @memberof game
         * @interface IGenderOption
         * @augments game.GenderOption.$Properties
         * @deprecated Use game.GenderOption.$Properties instead.
         */

        /**
         * Shape of a GenderOption.
         * @typedef {game.GenderOption.$Properties} game.GenderOption.$Shape
         */

        /**
         * Constructs a new GenderOption.
         * @memberof game
         * @classdesc Represents a GenderOption.
         * @constructor
         * @param {game.GenderOption.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GenderOption = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GenderOption id.
         * @member {string} id
         * @memberof game.GenderOption
         * @instance
         */
        GenderOption.prototype.id = "";

        /**
         * GenderOption label.
         * @member {string} label
         * @memberof game.GenderOption
         * @instance
         */
        GenderOption.prototype.label = "";

        /**
         * GenderOption factionId.
         * @member {string} factionId
         * @memberof game.GenderOption
         * @instance
         */
        GenderOption.prototype.factionId = "";

        /**
         * Creates a new GenderOption instance using the specified properties.
         * @function create
         * @memberof game.GenderOption
         * @static
         * @param {game.GenderOption.$Properties=} [properties] Properties to set
         * @returns {game.GenderOption} GenderOption instance
         * @type {{
         *   (properties: game.GenderOption.$Shape): game.GenderOption & game.GenderOption.$Shape;
         *   (properties?: game.GenderOption.$Properties): game.GenderOption;
         * }}
         */
        GenderOption.create = function(properties) {
            return new GenderOption(properties);
        };

        /**
         * Encodes the specified GenderOption message. Does not implicitly {@link game.GenderOption.verify|verify} messages.
         * @function encode
         * @memberof game.GenderOption
         * @static
         * @param {game.GenderOption.$Properties} message GenderOption message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenderOption.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.label);
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId") && message.factionId !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.factionId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GenderOption message, length delimited. Does not implicitly {@link game.GenderOption.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GenderOption
         * @static
         * @param {game.GenderOption.$Properties} message GenderOption message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenderOption.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GenderOption message from the specified reader or buffer.
         * @function decode
         * @memberof game.GenderOption
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GenderOption & game.GenderOption.$Shape} GenderOption
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenderOption.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GenderOption(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.factionId = value;
                        else
                            delete message.factionId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GenderOption message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GenderOption
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GenderOption & game.GenderOption.$Shape} GenderOption
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenderOption.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GenderOption message.
         * @function verify
         * @memberof game.GenderOption
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GenderOption.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                if (!$util.isString(message.factionId))
                    return "factionId: string expected";
            return null;
        };

        /**
         * Creates a GenderOption message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GenderOption
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GenderOption} GenderOption
         */
        GenderOption.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GenderOption)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GenderOption: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GenderOption();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.factionId != null)
                if (typeof object.factionId !== "string" || object.factionId.length)
                    message.factionId = $String(object.factionId);
            return message;
        };

        /**
         * Creates a plain object from a GenderOption message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GenderOption
         * @static
         * @param {game.GenderOption} message GenderOption
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GenderOption.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.label = "";
                object.factionId = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                object.factionId = message.factionId;
            return object;
        };

        /**
         * Converts this GenderOption to JSON.
         * @function toJSON
         * @memberof game.GenderOption
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GenderOption.prototype.toJSON = function() {
            return GenderOption.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GenderOption
         * @function getTypeUrl
         * @memberof game.GenderOption
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GenderOption.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GenderOption";
        };

        return GenderOption;
    })();

    game.GenderFaction = (function() {

        /**
         * Properties of a GenderFaction.
         * @typedef {Object} game.GenderFaction.$Properties
         * @property {string|null} [id] GenderFaction id
         * @property {string|null} [label] GenderFaction label
         * @property {string|null} [textColor] GenderFaction textColor
         * @property {string|null} [backgroundColor] GenderFaction backgroundColor
         * @property {string|null} [borderColor] GenderFaction borderColor
         * @property {string|null} [taskGroup] GenderFaction taskGroup
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GenderFaction.
         * @memberof game
         * @interface IGenderFaction
         * @augments game.GenderFaction.$Properties
         * @deprecated Use game.GenderFaction.$Properties instead.
         */

        /**
         * Shape of a GenderFaction.
         * @typedef {game.GenderFaction.$Properties} game.GenderFaction.$Shape
         */

        /**
         * Constructs a new GenderFaction.
         * @memberof game
         * @classdesc Represents a GenderFaction.
         * @constructor
         * @param {game.GenderFaction.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GenderFaction = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GenderFaction id.
         * @member {string} id
         * @memberof game.GenderFaction
         * @instance
         */
        GenderFaction.prototype.id = "";

        /**
         * GenderFaction label.
         * @member {string} label
         * @memberof game.GenderFaction
         * @instance
         */
        GenderFaction.prototype.label = "";

        /**
         * GenderFaction textColor.
         * @member {string} textColor
         * @memberof game.GenderFaction
         * @instance
         */
        GenderFaction.prototype.textColor = "";

        /**
         * GenderFaction backgroundColor.
         * @member {string} backgroundColor
         * @memberof game.GenderFaction
         * @instance
         */
        GenderFaction.prototype.backgroundColor = "";

        /**
         * GenderFaction borderColor.
         * @member {string} borderColor
         * @memberof game.GenderFaction
         * @instance
         */
        GenderFaction.prototype.borderColor = "";

        /**
         * GenderFaction taskGroup.
         * @member {string} taskGroup
         * @memberof game.GenderFaction
         * @instance
         */
        GenderFaction.prototype.taskGroup = "";

        /**
         * Creates a new GenderFaction instance using the specified properties.
         * @function create
         * @memberof game.GenderFaction
         * @static
         * @param {game.GenderFaction.$Properties=} [properties] Properties to set
         * @returns {game.GenderFaction} GenderFaction instance
         * @type {{
         *   (properties: game.GenderFaction.$Shape): game.GenderFaction & game.GenderFaction.$Shape;
         *   (properties?: game.GenderFaction.$Properties): game.GenderFaction;
         * }}
         */
        GenderFaction.create = function(properties) {
            return new GenderFaction(properties);
        };

        /**
         * Encodes the specified GenderFaction message. Does not implicitly {@link game.GenderFaction.verify|verify} messages.
         * @function encode
         * @memberof game.GenderFaction
         * @static
         * @param {game.GenderFaction.$Properties} message GenderFaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenderFaction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.label);
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor") && message.textColor !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.textColor);
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor") && message.backgroundColor !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.backgroundColor);
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor") && message.borderColor !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.borderColor);
            if (message.taskGroup != null && $Object.hasOwnProperty.call(message, "taskGroup") && message.taskGroup !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.taskGroup);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GenderFaction message, length delimited. Does not implicitly {@link game.GenderFaction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GenderFaction
         * @static
         * @param {game.GenderFaction.$Properties} message GenderFaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenderFaction.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GenderFaction message from the specified reader or buffer.
         * @function decode
         * @memberof game.GenderFaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GenderFaction & game.GenderFaction.$Shape} GenderFaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenderFaction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GenderFaction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.textColor = value;
                        else
                            delete message.textColor;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.backgroundColor = value;
                        else
                            delete message.backgroundColor;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.borderColor = value;
                        else
                            delete message.borderColor;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.taskGroup = value;
                        else
                            delete message.taskGroup;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GenderFaction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GenderFaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GenderFaction & game.GenderFaction.$Shape} GenderFaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenderFaction.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GenderFaction message.
         * @function verify
         * @memberof game.GenderFaction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GenderFaction.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor"))
                if (!$util.isString(message.textColor))
                    return "textColor: string expected";
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor"))
                if (!$util.isString(message.backgroundColor))
                    return "backgroundColor: string expected";
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor"))
                if (!$util.isString(message.borderColor))
                    return "borderColor: string expected";
            if (message.taskGroup != null && $Object.hasOwnProperty.call(message, "taskGroup"))
                if (!$util.isString(message.taskGroup))
                    return "taskGroup: string expected";
            return null;
        };

        /**
         * Creates a GenderFaction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GenderFaction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GenderFaction} GenderFaction
         */
        GenderFaction.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GenderFaction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GenderFaction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GenderFaction();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.textColor != null)
                if (typeof object.textColor !== "string" || object.textColor.length)
                    message.textColor = $String(object.textColor);
            if (object.backgroundColor != null)
                if (typeof object.backgroundColor !== "string" || object.backgroundColor.length)
                    message.backgroundColor = $String(object.backgroundColor);
            if (object.borderColor != null)
                if (typeof object.borderColor !== "string" || object.borderColor.length)
                    message.borderColor = $String(object.borderColor);
            if (object.taskGroup != null)
                if (typeof object.taskGroup !== "string" || object.taskGroup.length)
                    message.taskGroup = $String(object.taskGroup);
            return message;
        };

        /**
         * Creates a plain object from a GenderFaction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GenderFaction
         * @static
         * @param {game.GenderFaction} message GenderFaction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GenderFaction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.label = "";
                object.textColor = "";
                object.backgroundColor = "";
                object.borderColor = "";
                object.taskGroup = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor"))
                object.textColor = message.textColor;
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor"))
                object.backgroundColor = message.backgroundColor;
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor"))
                object.borderColor = message.borderColor;
            if (message.taskGroup != null && $Object.hasOwnProperty.call(message, "taskGroup"))
                object.taskGroup = message.taskGroup;
            return object;
        };

        /**
         * Converts this GenderFaction to JSON.
         * @function toJSON
         * @memberof game.GenderFaction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GenderFaction.prototype.toJSON = function() {
            return GenderFaction.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GenderFaction
         * @function getTypeUrl
         * @memberof game.GenderFaction
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GenderFaction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GenderFaction";
        };

        return GenderFaction;
    })();

    game.Pos = (function() {

        /**
         * Properties of a Pos.
         * @typedef {Object} game.Pos.$Properties
         * @property {number|null} [row] Pos row
         * @property {number|null} [col] Pos col
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Pos.
         * @memberof game
         * @interface IPos
         * @augments game.Pos.$Properties
         * @deprecated Use game.Pos.$Properties instead.
         */

        /**
         * Shape of a Pos.
         * @typedef {game.Pos.$Properties} game.Pos.$Shape
         */

        /**
         * Constructs a new Pos.
         * @memberof game
         * @classdesc Represents a Pos.
         * @constructor
         * @param {game.Pos.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Pos = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Pos row.
         * @member {number} row
         * @memberof game.Pos
         * @instance
         */
        Pos.prototype.row = 0;

        /**
         * Pos col.
         * @member {number} col
         * @memberof game.Pos
         * @instance
         */
        Pos.prototype.col = 0;

        /**
         * Creates a new Pos instance using the specified properties.
         * @function create
         * @memberof game.Pos
         * @static
         * @param {game.Pos.$Properties=} [properties] Properties to set
         * @returns {game.Pos} Pos instance
         * @type {{
         *   (properties: game.Pos.$Shape): game.Pos & game.Pos.$Shape;
         *   (properties?: game.Pos.$Properties): game.Pos;
         * }}
         */
        Pos.create = function(properties) {
            return new Pos(properties);
        };

        /**
         * Encodes the specified Pos message. Does not implicitly {@link game.Pos.verify|verify} messages.
         * @function encode
         * @memberof game.Pos
         * @static
         * @param {game.Pos.$Properties} message Pos message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Pos.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.row != null && $Object.hasOwnProperty.call(message, "row") && message.row !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.row);
            if (message.col != null && $Object.hasOwnProperty.call(message, "col") && message.col !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.col);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Pos message, length delimited. Does not implicitly {@link game.Pos.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Pos
         * @static
         * @param {game.Pos.$Properties} message Pos message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Pos.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Pos message from the specified reader or buffer.
         * @function decode
         * @memberof game.Pos
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Pos & game.Pos.$Shape} Pos
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Pos.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Pos(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.row = value;
                        else
                            delete message.row;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.col = value;
                        else
                            delete message.col;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Pos message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Pos
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Pos & game.Pos.$Shape} Pos
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Pos.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Pos message.
         * @function verify
         * @memberof game.Pos
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Pos.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.row != null && $Object.hasOwnProperty.call(message, "row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.col != null && $Object.hasOwnProperty.call(message, "col"))
                if (!$util.isInteger(message.col))
                    return "col: integer expected";
            return null;
        };

        /**
         * Creates a Pos message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Pos
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Pos} Pos
         */
        Pos.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Pos)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Pos: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Pos();
            if (object.row != null)
                if ($Number(object.row) !== 0)
                    message.row = object.row | 0;
            if (object.col != null)
                if ($Number(object.col) !== 0)
                    message.col = object.col | 0;
            return message;
        };

        /**
         * Creates a plain object from a Pos message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Pos
         * @static
         * @param {game.Pos} message Pos
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Pos.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.col = 0;
            }
            if (message.row != null && $Object.hasOwnProperty.call(message, "row"))
                object.row = message.row;
            if (message.col != null && $Object.hasOwnProperty.call(message, "col"))
                object.col = message.col;
            return object;
        };

        /**
         * Converts this Pos to JSON.
         * @function toJSON
         * @memberof game.Pos
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Pos.prototype.toJSON = function() {
            return Pos.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Pos
         * @function getTypeUrl
         * @memberof game.Pos
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Pos.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Pos";
        };

        return Pos;
    })();

    game.PublicStats = (function() {

        /**
         * Properties of a PublicStats.
         * @typedef {Object} game.PublicStats.$Properties
         * @property {number|null} [wins] PublicStats wins
         * @property {number|null} [losses] PublicStats losses
         * @property {number|null} [draws] PublicStats draws
         * @property {number|null} [punishments] PublicStats punishments
         * @property {number|null} [rankedPoints] PublicStats rankedPoints
         * @property {string|null} [title] PublicStats title
         * @property {string|null} [titleSegmentId] PublicStats titleSegmentId
         * @property {number|null} [highestScore] PublicStats highestScore
         * @property {number|null} [lowestScore] PublicStats lowestScore
         * @property {number|null} [sortRankedPoints] PublicStats sortRankedPoints
         * @property {number|null} [sortHighestScore] PublicStats sortHighestScore
         * @property {number|null} [sortLowestScore] PublicStats sortLowestScore
         * @property {boolean|null} [titleCustom] PublicStats titleCustom
         * @property {number|Long|null} [totalOnlineMs] PublicStats totalOnlineMs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PublicStats.
         * @memberof game
         * @interface IPublicStats
         * @augments game.PublicStats.$Properties
         * @deprecated Use game.PublicStats.$Properties instead.
         */

        /**
         * Shape of a PublicStats.
         * @typedef {game.PublicStats.$Properties} game.PublicStats.$Shape
         */

        /**
         * Constructs a new PublicStats.
         * @memberof game
         * @classdesc Represents a PublicStats.
         * @constructor
         * @param {game.PublicStats.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PublicStats = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PublicStats wins.
         * @member {number} wins
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.wins = 0;

        /**
         * PublicStats losses.
         * @member {number} losses
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.losses = 0;

        /**
         * PublicStats draws.
         * @member {number} draws
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.draws = 0;

        /**
         * PublicStats punishments.
         * @member {number} punishments
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.punishments = 0;

        /**
         * PublicStats rankedPoints.
         * @member {number} rankedPoints
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.rankedPoints = 0;

        /**
         * PublicStats title.
         * @member {string} title
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.title = "";

        /**
         * PublicStats titleSegmentId.
         * @member {string} titleSegmentId
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.titleSegmentId = "";

        /**
         * PublicStats highestScore.
         * @member {number} highestScore
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.highestScore = 0;

        /**
         * PublicStats lowestScore.
         * @member {number} lowestScore
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.lowestScore = 0;

        /**
         * PublicStats sortRankedPoints.
         * @member {number} sortRankedPoints
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.sortRankedPoints = 0;

        /**
         * PublicStats sortHighestScore.
         * @member {number} sortHighestScore
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.sortHighestScore = 0;

        /**
         * PublicStats sortLowestScore.
         * @member {number} sortLowestScore
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.sortLowestScore = 0;

        /**
         * PublicStats titleCustom.
         * @member {boolean} titleCustom
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.titleCustom = false;

        /**
         * PublicStats totalOnlineMs.
         * @member {number|Long} totalOnlineMs
         * @memberof game.PublicStats
         * @instance
         */
        PublicStats.prototype.totalOnlineMs = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new PublicStats instance using the specified properties.
         * @function create
         * @memberof game.PublicStats
         * @static
         * @param {game.PublicStats.$Properties=} [properties] Properties to set
         * @returns {game.PublicStats} PublicStats instance
         * @type {{
         *   (properties: game.PublicStats.$Shape): game.PublicStats & game.PublicStats.$Shape;
         *   (properties?: game.PublicStats.$Properties): game.PublicStats;
         * }}
         */
        PublicStats.create = function(properties) {
            return new PublicStats(properties);
        };

        /**
         * Encodes the specified PublicStats message. Does not implicitly {@link game.PublicStats.verify|verify} messages.
         * @function encode
         * @memberof game.PublicStats
         * @static
         * @param {game.PublicStats.$Properties} message PublicStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublicStats.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins") && message.wins !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.wins);
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses") && message.losses !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.losses);
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws") && message.draws !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.draws);
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments") && message.punishments !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.punishments);
            if (message.rankedPoints != null && $Object.hasOwnProperty.call(message, "rankedPoints") && message.rankedPoints !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.rankedPoints);
            if (message.title != null && $Object.hasOwnProperty.call(message, "title") && message.title !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.title);
            if (message.titleSegmentId != null && $Object.hasOwnProperty.call(message, "titleSegmentId") && message.titleSegmentId !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.titleSegmentId);
            if (message.highestScore != null && $Object.hasOwnProperty.call(message, "highestScore") && message.highestScore !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.highestScore);
            if (message.lowestScore != null && $Object.hasOwnProperty.call(message, "lowestScore") && message.lowestScore !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.lowestScore);
            if (message.sortRankedPoints != null && $Object.hasOwnProperty.call(message, "sortRankedPoints") && message.sortRankedPoints !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.sortRankedPoints);
            if (message.sortHighestScore != null && $Object.hasOwnProperty.call(message, "sortHighestScore") && message.sortHighestScore !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.sortHighestScore);
            if (message.sortLowestScore != null && $Object.hasOwnProperty.call(message, "sortLowestScore") && message.sortLowestScore !== 0)
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.sortLowestScore);
            if (message.titleCustom != null && $Object.hasOwnProperty.call(message, "titleCustom") && message.titleCustom !== false)
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.titleCustom);
            if (message.totalOnlineMs != null && $Object.hasOwnProperty.call(message, "totalOnlineMs") && (typeof message.totalOnlineMs === "object" ? message.totalOnlineMs.low || message.totalOnlineMs.high : message.totalOnlineMs !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.totalOnlineMs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PublicStats message, length delimited. Does not implicitly {@link game.PublicStats.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PublicStats
         * @static
         * @param {game.PublicStats.$Properties} message PublicStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublicStats.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PublicStats message from the specified reader or buffer.
         * @function decode
         * @memberof game.PublicStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PublicStats & game.PublicStats.$Shape} PublicStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublicStats.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PublicStats(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.wins = value;
                        else
                            delete message.wins;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.losses = value;
                        else
                            delete message.losses;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.draws = value;
                        else
                            delete message.draws;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.punishments = value;
                        else
                            delete message.punishments;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.rankedPoints = value;
                        else
                            delete message.rankedPoints;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.titleSegmentId = value;
                        else
                            delete message.titleSegmentId;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.highestScore = value;
                        else
                            delete message.highestScore;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lowestScore = value;
                        else
                            delete message.lowestScore;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.sortRankedPoints = value;
                        else
                            delete message.sortRankedPoints;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.sortHighestScore = value;
                        else
                            delete message.sortHighestScore;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.sortLowestScore = value;
                        else
                            delete message.sortLowestScore;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.titleCustom = value;
                        else
                            delete message.titleCustom;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.totalOnlineMs = value;
                        else
                            delete message.totalOnlineMs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PublicStats message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PublicStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PublicStats & game.PublicStats.$Shape} PublicStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublicStats.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PublicStats message.
         * @function verify
         * @memberof game.PublicStats
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PublicStats.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                if (!$util.isInteger(message.wins))
                    return "wins: integer expected";
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                if (!$util.isInteger(message.losses))
                    return "losses: integer expected";
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws"))
                if (!$util.isInteger(message.draws))
                    return "draws: integer expected";
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments"))
                if (!$util.isInteger(message.punishments))
                    return "punishments: integer expected";
            if (message.rankedPoints != null && $Object.hasOwnProperty.call(message, "rankedPoints"))
                if (!$util.isInteger(message.rankedPoints))
                    return "rankedPoints: integer expected";
            if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.titleSegmentId != null && $Object.hasOwnProperty.call(message, "titleSegmentId"))
                if (!$util.isString(message.titleSegmentId))
                    return "titleSegmentId: string expected";
            if (message.highestScore != null && $Object.hasOwnProperty.call(message, "highestScore"))
                if (!$util.isInteger(message.highestScore))
                    return "highestScore: integer expected";
            if (message.lowestScore != null && $Object.hasOwnProperty.call(message, "lowestScore"))
                if (!$util.isInteger(message.lowestScore))
                    return "lowestScore: integer expected";
            if (message.sortRankedPoints != null && $Object.hasOwnProperty.call(message, "sortRankedPoints"))
                if (!$util.isInteger(message.sortRankedPoints))
                    return "sortRankedPoints: integer expected";
            if (message.sortHighestScore != null && $Object.hasOwnProperty.call(message, "sortHighestScore"))
                if (!$util.isInteger(message.sortHighestScore))
                    return "sortHighestScore: integer expected";
            if (message.sortLowestScore != null && $Object.hasOwnProperty.call(message, "sortLowestScore"))
                if (!$util.isInteger(message.sortLowestScore))
                    return "sortLowestScore: integer expected";
            if (message.titleCustom != null && $Object.hasOwnProperty.call(message, "titleCustom"))
                if (typeof message.titleCustom !== "boolean")
                    return "titleCustom: boolean expected";
            if (message.totalOnlineMs != null && $Object.hasOwnProperty.call(message, "totalOnlineMs"))
                if (!$util.isInteger(message.totalOnlineMs) && !(message.totalOnlineMs && $util.isInteger(message.totalOnlineMs.low) && $util.isInteger(message.totalOnlineMs.high)))
                    return "totalOnlineMs: integer|Long expected";
            return null;
        };

        /**
         * Creates a PublicStats message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PublicStats
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PublicStats} PublicStats
         */
        PublicStats.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PublicStats)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PublicStats: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PublicStats();
            if (object.wins != null)
                if ($Number(object.wins) !== 0)
                    message.wins = object.wins | 0;
            if (object.losses != null)
                if ($Number(object.losses) !== 0)
                    message.losses = object.losses | 0;
            if (object.draws != null)
                if ($Number(object.draws) !== 0)
                    message.draws = object.draws | 0;
            if (object.punishments != null)
                if ($Number(object.punishments) !== 0)
                    message.punishments = object.punishments | 0;
            if (object.rankedPoints != null)
                if ($Number(object.rankedPoints) !== 0)
                    message.rankedPoints = object.rankedPoints | 0;
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = $String(object.title);
            if (object.titleSegmentId != null)
                if (typeof object.titleSegmentId !== "string" || object.titleSegmentId.length)
                    message.titleSegmentId = $String(object.titleSegmentId);
            if (object.highestScore != null)
                if ($Number(object.highestScore) !== 0)
                    message.highestScore = object.highestScore | 0;
            if (object.lowestScore != null)
                if ($Number(object.lowestScore) !== 0)
                    message.lowestScore = object.lowestScore | 0;
            if (object.sortRankedPoints != null)
                if ($Number(object.sortRankedPoints) !== 0)
                    message.sortRankedPoints = object.sortRankedPoints | 0;
            if (object.sortHighestScore != null)
                if ($Number(object.sortHighestScore) !== 0)
                    message.sortHighestScore = object.sortHighestScore | 0;
            if (object.sortLowestScore != null)
                if ($Number(object.sortLowestScore) !== 0)
                    message.sortLowestScore = object.sortLowestScore | 0;
            if (object.titleCustom != null)
                if (object.titleCustom)
                    message.titleCustom = $Boolean(object.titleCustom);
            if (object.totalOnlineMs != null)
                if (typeof object.totalOnlineMs === "object" ? object.totalOnlineMs.low || object.totalOnlineMs.high : $Number(object.totalOnlineMs) !== 0)
                    if ($util.Long)
                        message.totalOnlineMs = $util.Long.fromValue(object.totalOnlineMs, false);
                    else if (typeof object.totalOnlineMs === "string")
                        message.totalOnlineMs = $parseInt(object.totalOnlineMs, 10);
                    else if (typeof object.totalOnlineMs === "number")
                        message.totalOnlineMs = object.totalOnlineMs;
                    else if (typeof object.totalOnlineMs === "object")
                        message.totalOnlineMs = new $util.LongBits(object.totalOnlineMs.low >>> 0, object.totalOnlineMs.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a PublicStats message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PublicStats
         * @static
         * @param {game.PublicStats} message PublicStats
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PublicStats.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.wins = 0;
                object.losses = 0;
                object.draws = 0;
                object.punishments = 0;
                object.rankedPoints = 0;
                object.title = "";
                object.titleSegmentId = "";
                object.highestScore = 0;
                object.lowestScore = 0;
                object.sortRankedPoints = 0;
                object.sortHighestScore = 0;
                object.sortLowestScore = 0;
                object.titleCustom = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.totalOnlineMs = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.totalOnlineMs = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                object.wins = message.wins;
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                object.losses = message.losses;
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws"))
                object.draws = message.draws;
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments"))
                object.punishments = message.punishments;
            if (message.rankedPoints != null && $Object.hasOwnProperty.call(message, "rankedPoints"))
                object.rankedPoints = message.rankedPoints;
            if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            if (message.titleSegmentId != null && $Object.hasOwnProperty.call(message, "titleSegmentId"))
                object.titleSegmentId = message.titleSegmentId;
            if (message.highestScore != null && $Object.hasOwnProperty.call(message, "highestScore"))
                object.highestScore = message.highestScore;
            if (message.lowestScore != null && $Object.hasOwnProperty.call(message, "lowestScore"))
                object.lowestScore = message.lowestScore;
            if (message.sortRankedPoints != null && $Object.hasOwnProperty.call(message, "sortRankedPoints"))
                object.sortRankedPoints = message.sortRankedPoints;
            if (message.sortHighestScore != null && $Object.hasOwnProperty.call(message, "sortHighestScore"))
                object.sortHighestScore = message.sortHighestScore;
            if (message.sortLowestScore != null && $Object.hasOwnProperty.call(message, "sortLowestScore"))
                object.sortLowestScore = message.sortLowestScore;
            if (message.titleCustom != null && $Object.hasOwnProperty.call(message, "titleCustom"))
                object.titleCustom = message.titleCustom;
            if (message.totalOnlineMs != null && $Object.hasOwnProperty.call(message, "totalOnlineMs"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.totalOnlineMs = typeof message.totalOnlineMs === "number" ? $BigInt(message.totalOnlineMs) : $util.Long.fromBits(message.totalOnlineMs.low >>> 0, message.totalOnlineMs.high >>> 0, false).toBigInt();
                else if (typeof message.totalOnlineMs === "number")
                    object.totalOnlineMs = options.longs === $String ? $String(message.totalOnlineMs) : message.totalOnlineMs;
                else
                    object.totalOnlineMs = options.longs === $String ? $util.Long.prototype.toString.call(message.totalOnlineMs) : options.longs === $Number ? new $util.LongBits(message.totalOnlineMs.low >>> 0, message.totalOnlineMs.high >>> 0).toNumber() : message.totalOnlineMs;
            return object;
        };

        /**
         * Converts this PublicStats to JSON.
         * @function toJSON
         * @memberof game.PublicStats
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PublicStats.prototype.toJSON = function() {
            return PublicStats.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PublicStats
         * @function getTypeUrl
         * @memberof game.PublicStats
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PublicStats.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PublicStats";
        };

        return PublicStats;
    })();

    game.LobbyStats = (function() {

        /**
         * Properties of a LobbyStats.
         * @typedef {Object} game.LobbyStats.$Properties
         * @property {number|null} [wins] LobbyStats wins
         * @property {number|null} [losses] LobbyStats losses
         * @property {number|null} [draws] LobbyStats draws
         * @property {number|null} [punishments] LobbyStats punishments
         * @property {number|null} [rankedPoints] LobbyStats rankedPoints
         * @property {string|null} [title] LobbyStats title
         * @property {number|null} [highestScore] LobbyStats highestScore
         * @property {number|null} [lowestScore] LobbyStats lowestScore
         * @property {number|null} [sortRankedPoints] LobbyStats sortRankedPoints
         * @property {number|null} [sortHighestScore] LobbyStats sortHighestScore
         * @property {number|null} [sortLowestScore] LobbyStats sortLowestScore
         * @property {boolean|null} [titleCustom] LobbyStats titleCustom
         * @property {number|Long|null} [totalOnlineMs] LobbyStats totalOnlineMs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LobbyStats.
         * @memberof game
         * @interface ILobbyStats
         * @augments game.LobbyStats.$Properties
         * @deprecated Use game.LobbyStats.$Properties instead.
         */

        /**
         * Shape of a LobbyStats.
         * @typedef {game.LobbyStats.$Properties} game.LobbyStats.$Shape
         */

        /**
         * Constructs a new LobbyStats.
         * @memberof game
         * @classdesc Represents a LobbyStats.
         * @constructor
         * @param {game.LobbyStats.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LobbyStats = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LobbyStats wins.
         * @member {number} wins
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.wins = 0;

        /**
         * LobbyStats losses.
         * @member {number} losses
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.losses = 0;

        /**
         * LobbyStats draws.
         * @member {number} draws
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.draws = 0;

        /**
         * LobbyStats punishments.
         * @member {number} punishments
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.punishments = 0;

        /**
         * LobbyStats rankedPoints.
         * @member {number} rankedPoints
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.rankedPoints = 0;

        /**
         * LobbyStats title.
         * @member {string} title
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.title = "";

        /**
         * LobbyStats highestScore.
         * @member {number} highestScore
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.highestScore = 0;

        /**
         * LobbyStats lowestScore.
         * @member {number} lowestScore
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.lowestScore = 0;

        /**
         * LobbyStats sortRankedPoints.
         * @member {number} sortRankedPoints
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.sortRankedPoints = 0;

        /**
         * LobbyStats sortHighestScore.
         * @member {number} sortHighestScore
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.sortHighestScore = 0;

        /**
         * LobbyStats sortLowestScore.
         * @member {number} sortLowestScore
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.sortLowestScore = 0;

        /**
         * LobbyStats titleCustom.
         * @member {boolean} titleCustom
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.titleCustom = false;

        /**
         * LobbyStats totalOnlineMs.
         * @member {number|Long} totalOnlineMs
         * @memberof game.LobbyStats
         * @instance
         */
        LobbyStats.prototype.totalOnlineMs = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new LobbyStats instance using the specified properties.
         * @function create
         * @memberof game.LobbyStats
         * @static
         * @param {game.LobbyStats.$Properties=} [properties] Properties to set
         * @returns {game.LobbyStats} LobbyStats instance
         * @type {{
         *   (properties: game.LobbyStats.$Shape): game.LobbyStats & game.LobbyStats.$Shape;
         *   (properties?: game.LobbyStats.$Properties): game.LobbyStats;
         * }}
         */
        LobbyStats.create = function(properties) {
            return new LobbyStats(properties);
        };

        /**
         * Encodes the specified LobbyStats message. Does not implicitly {@link game.LobbyStats.verify|verify} messages.
         * @function encode
         * @memberof game.LobbyStats
         * @static
         * @param {game.LobbyStats.$Properties} message LobbyStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyStats.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins") && message.wins !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.wins);
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses") && message.losses !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.losses);
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws") && message.draws !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.draws);
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments") && message.punishments !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.punishments);
            if (message.rankedPoints != null && $Object.hasOwnProperty.call(message, "rankedPoints") && message.rankedPoints !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.rankedPoints);
            if (message.title != null && $Object.hasOwnProperty.call(message, "title") && message.title !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.title);
            if (message.highestScore != null && $Object.hasOwnProperty.call(message, "highestScore") && message.highestScore !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.highestScore);
            if (message.lowestScore != null && $Object.hasOwnProperty.call(message, "lowestScore") && message.lowestScore !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.lowestScore);
            if (message.sortRankedPoints != null && $Object.hasOwnProperty.call(message, "sortRankedPoints") && message.sortRankedPoints !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.sortRankedPoints);
            if (message.sortHighestScore != null && $Object.hasOwnProperty.call(message, "sortHighestScore") && message.sortHighestScore !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.sortHighestScore);
            if (message.sortLowestScore != null && $Object.hasOwnProperty.call(message, "sortLowestScore") && message.sortLowestScore !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.sortLowestScore);
            if (message.titleCustom != null && $Object.hasOwnProperty.call(message, "titleCustom") && message.titleCustom !== false)
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.titleCustom);
            if (message.totalOnlineMs != null && $Object.hasOwnProperty.call(message, "totalOnlineMs") && (typeof message.totalOnlineMs === "object" ? message.totalOnlineMs.low || message.totalOnlineMs.high : message.totalOnlineMs !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.totalOnlineMs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LobbyStats message, length delimited. Does not implicitly {@link game.LobbyStats.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LobbyStats
         * @static
         * @param {game.LobbyStats.$Properties} message LobbyStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyStats.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LobbyStats message from the specified reader or buffer.
         * @function decode
         * @memberof game.LobbyStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LobbyStats & game.LobbyStats.$Shape} LobbyStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyStats.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LobbyStats(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.wins = value;
                        else
                            delete message.wins;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.losses = value;
                        else
                            delete message.losses;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.draws = value;
                        else
                            delete message.draws;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.punishments = value;
                        else
                            delete message.punishments;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.rankedPoints = value;
                        else
                            delete message.rankedPoints;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.highestScore = value;
                        else
                            delete message.highestScore;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lowestScore = value;
                        else
                            delete message.lowestScore;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.sortRankedPoints = value;
                        else
                            delete message.sortRankedPoints;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.sortHighestScore = value;
                        else
                            delete message.sortHighestScore;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.sortLowestScore = value;
                        else
                            delete message.sortLowestScore;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.titleCustom = value;
                        else
                            delete message.titleCustom;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.totalOnlineMs = value;
                        else
                            delete message.totalOnlineMs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LobbyStats message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LobbyStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LobbyStats & game.LobbyStats.$Shape} LobbyStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyStats.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LobbyStats message.
         * @function verify
         * @memberof game.LobbyStats
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LobbyStats.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                if (!$util.isInteger(message.wins))
                    return "wins: integer expected";
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                if (!$util.isInteger(message.losses))
                    return "losses: integer expected";
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws"))
                if (!$util.isInteger(message.draws))
                    return "draws: integer expected";
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments"))
                if (!$util.isInteger(message.punishments))
                    return "punishments: integer expected";
            if (message.rankedPoints != null && $Object.hasOwnProperty.call(message, "rankedPoints"))
                if (!$util.isInteger(message.rankedPoints))
                    return "rankedPoints: integer expected";
            if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.highestScore != null && $Object.hasOwnProperty.call(message, "highestScore"))
                if (!$util.isInteger(message.highestScore))
                    return "highestScore: integer expected";
            if (message.lowestScore != null && $Object.hasOwnProperty.call(message, "lowestScore"))
                if (!$util.isInteger(message.lowestScore))
                    return "lowestScore: integer expected";
            if (message.sortRankedPoints != null && $Object.hasOwnProperty.call(message, "sortRankedPoints"))
                if (!$util.isInteger(message.sortRankedPoints))
                    return "sortRankedPoints: integer expected";
            if (message.sortHighestScore != null && $Object.hasOwnProperty.call(message, "sortHighestScore"))
                if (!$util.isInteger(message.sortHighestScore))
                    return "sortHighestScore: integer expected";
            if (message.sortLowestScore != null && $Object.hasOwnProperty.call(message, "sortLowestScore"))
                if (!$util.isInteger(message.sortLowestScore))
                    return "sortLowestScore: integer expected";
            if (message.titleCustom != null && $Object.hasOwnProperty.call(message, "titleCustom"))
                if (typeof message.titleCustom !== "boolean")
                    return "titleCustom: boolean expected";
            if (message.totalOnlineMs != null && $Object.hasOwnProperty.call(message, "totalOnlineMs"))
                if (!$util.isInteger(message.totalOnlineMs) && !(message.totalOnlineMs && $util.isInteger(message.totalOnlineMs.low) && $util.isInteger(message.totalOnlineMs.high)))
                    return "totalOnlineMs: integer|Long expected";
            return null;
        };

        /**
         * Creates a LobbyStats message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LobbyStats
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LobbyStats} LobbyStats
         */
        LobbyStats.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LobbyStats)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LobbyStats: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LobbyStats();
            if (object.wins != null)
                if ($Number(object.wins) !== 0)
                    message.wins = object.wins | 0;
            if (object.losses != null)
                if ($Number(object.losses) !== 0)
                    message.losses = object.losses | 0;
            if (object.draws != null)
                if ($Number(object.draws) !== 0)
                    message.draws = object.draws | 0;
            if (object.punishments != null)
                if ($Number(object.punishments) !== 0)
                    message.punishments = object.punishments | 0;
            if (object.rankedPoints != null)
                if ($Number(object.rankedPoints) !== 0)
                    message.rankedPoints = object.rankedPoints | 0;
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = $String(object.title);
            if (object.highestScore != null)
                if ($Number(object.highestScore) !== 0)
                    message.highestScore = object.highestScore | 0;
            if (object.lowestScore != null)
                if ($Number(object.lowestScore) !== 0)
                    message.lowestScore = object.lowestScore | 0;
            if (object.sortRankedPoints != null)
                if ($Number(object.sortRankedPoints) !== 0)
                    message.sortRankedPoints = object.sortRankedPoints | 0;
            if (object.sortHighestScore != null)
                if ($Number(object.sortHighestScore) !== 0)
                    message.sortHighestScore = object.sortHighestScore | 0;
            if (object.sortLowestScore != null)
                if ($Number(object.sortLowestScore) !== 0)
                    message.sortLowestScore = object.sortLowestScore | 0;
            if (object.titleCustom != null)
                if (object.titleCustom)
                    message.titleCustom = $Boolean(object.titleCustom);
            if (object.totalOnlineMs != null)
                if (typeof object.totalOnlineMs === "object" ? object.totalOnlineMs.low || object.totalOnlineMs.high : $Number(object.totalOnlineMs) !== 0)
                    if ($util.Long)
                        message.totalOnlineMs = $util.Long.fromValue(object.totalOnlineMs, false);
                    else if (typeof object.totalOnlineMs === "string")
                        message.totalOnlineMs = $parseInt(object.totalOnlineMs, 10);
                    else if (typeof object.totalOnlineMs === "number")
                        message.totalOnlineMs = object.totalOnlineMs;
                    else if (typeof object.totalOnlineMs === "object")
                        message.totalOnlineMs = new $util.LongBits(object.totalOnlineMs.low >>> 0, object.totalOnlineMs.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a LobbyStats message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LobbyStats
         * @static
         * @param {game.LobbyStats} message LobbyStats
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LobbyStats.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.wins = 0;
                object.losses = 0;
                object.draws = 0;
                object.punishments = 0;
                object.rankedPoints = 0;
                object.title = "";
                object.highestScore = 0;
                object.lowestScore = 0;
                object.sortRankedPoints = 0;
                object.sortHighestScore = 0;
                object.sortLowestScore = 0;
                object.titleCustom = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.totalOnlineMs = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.totalOnlineMs = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                object.wins = message.wins;
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                object.losses = message.losses;
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws"))
                object.draws = message.draws;
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments"))
                object.punishments = message.punishments;
            if (message.rankedPoints != null && $Object.hasOwnProperty.call(message, "rankedPoints"))
                object.rankedPoints = message.rankedPoints;
            if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            if (message.highestScore != null && $Object.hasOwnProperty.call(message, "highestScore"))
                object.highestScore = message.highestScore;
            if (message.lowestScore != null && $Object.hasOwnProperty.call(message, "lowestScore"))
                object.lowestScore = message.lowestScore;
            if (message.sortRankedPoints != null && $Object.hasOwnProperty.call(message, "sortRankedPoints"))
                object.sortRankedPoints = message.sortRankedPoints;
            if (message.sortHighestScore != null && $Object.hasOwnProperty.call(message, "sortHighestScore"))
                object.sortHighestScore = message.sortHighestScore;
            if (message.sortLowestScore != null && $Object.hasOwnProperty.call(message, "sortLowestScore"))
                object.sortLowestScore = message.sortLowestScore;
            if (message.titleCustom != null && $Object.hasOwnProperty.call(message, "titleCustom"))
                object.titleCustom = message.titleCustom;
            if (message.totalOnlineMs != null && $Object.hasOwnProperty.call(message, "totalOnlineMs"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.totalOnlineMs = typeof message.totalOnlineMs === "number" ? $BigInt(message.totalOnlineMs) : $util.Long.fromBits(message.totalOnlineMs.low >>> 0, message.totalOnlineMs.high >>> 0, false).toBigInt();
                else if (typeof message.totalOnlineMs === "number")
                    object.totalOnlineMs = options.longs === $String ? $String(message.totalOnlineMs) : message.totalOnlineMs;
                else
                    object.totalOnlineMs = options.longs === $String ? $util.Long.prototype.toString.call(message.totalOnlineMs) : options.longs === $Number ? new $util.LongBits(message.totalOnlineMs.low >>> 0, message.totalOnlineMs.high >>> 0).toNumber() : message.totalOnlineMs;
            return object;
        };

        /**
         * Converts this LobbyStats to JSON.
         * @function toJSON
         * @memberof game.LobbyStats
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LobbyStats.prototype.toJSON = function() {
            return LobbyStats.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LobbyStats
         * @function getTypeUrl
         * @memberof game.LobbyStats
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LobbyStats.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LobbyStats";
        };

        return LobbyStats;
    })();

    game.GameWLD = (function() {

        /**
         * Properties of a GameWLD.
         * @typedef {Object} game.GameWLD.$Properties
         * @property {number|null} [wins] GameWLD wins
         * @property {number|null} [losses] GameWLD losses
         * @property {number|null} [draws] GameWLD draws
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GameWLD.
         * @memberof game
         * @interface IGameWLD
         * @augments game.GameWLD.$Properties
         * @deprecated Use game.GameWLD.$Properties instead.
         */

        /**
         * Shape of a GameWLD.
         * @typedef {game.GameWLD.$Properties} game.GameWLD.$Shape
         */

        /**
         * Constructs a new GameWLD.
         * @memberof game
         * @classdesc Represents a GameWLD.
         * @constructor
         * @param {game.GameWLD.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GameWLD = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GameWLD wins.
         * @member {number} wins
         * @memberof game.GameWLD
         * @instance
         */
        GameWLD.prototype.wins = 0;

        /**
         * GameWLD losses.
         * @member {number} losses
         * @memberof game.GameWLD
         * @instance
         */
        GameWLD.prototype.losses = 0;

        /**
         * GameWLD draws.
         * @member {number} draws
         * @memberof game.GameWLD
         * @instance
         */
        GameWLD.prototype.draws = 0;

        /**
         * Creates a new GameWLD instance using the specified properties.
         * @function create
         * @memberof game.GameWLD
         * @static
         * @param {game.GameWLD.$Properties=} [properties] Properties to set
         * @returns {game.GameWLD} GameWLD instance
         * @type {{
         *   (properties: game.GameWLD.$Shape): game.GameWLD & game.GameWLD.$Shape;
         *   (properties?: game.GameWLD.$Properties): game.GameWLD;
         * }}
         */
        GameWLD.create = function(properties) {
            return new GameWLD(properties);
        };

        /**
         * Encodes the specified GameWLD message. Does not implicitly {@link game.GameWLD.verify|verify} messages.
         * @function encode
         * @memberof game.GameWLD
         * @static
         * @param {game.GameWLD.$Properties} message GameWLD message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameWLD.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins") && message.wins !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.wins);
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses") && message.losses !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.losses);
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws") && message.draws !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.draws);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GameWLD message, length delimited. Does not implicitly {@link game.GameWLD.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GameWLD
         * @static
         * @param {game.GameWLD.$Properties} message GameWLD message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameWLD.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GameWLD message from the specified reader or buffer.
         * @function decode
         * @memberof game.GameWLD
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GameWLD & game.GameWLD.$Shape} GameWLD
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameWLD.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GameWLD(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.wins = value;
                        else
                            delete message.wins;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.losses = value;
                        else
                            delete message.losses;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.draws = value;
                        else
                            delete message.draws;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GameWLD message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GameWLD
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GameWLD & game.GameWLD.$Shape} GameWLD
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameWLD.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameWLD message.
         * @function verify
         * @memberof game.GameWLD
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameWLD.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                if (!$util.isInteger(message.wins))
                    return "wins: integer expected";
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                if (!$util.isInteger(message.losses))
                    return "losses: integer expected";
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws"))
                if (!$util.isInteger(message.draws))
                    return "draws: integer expected";
            return null;
        };

        /**
         * Creates a GameWLD message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GameWLD
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GameWLD} GameWLD
         */
        GameWLD.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GameWLD)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GameWLD: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GameWLD();
            if (object.wins != null)
                if ($Number(object.wins) !== 0)
                    message.wins = object.wins | 0;
            if (object.losses != null)
                if ($Number(object.losses) !== 0)
                    message.losses = object.losses | 0;
            if (object.draws != null)
                if ($Number(object.draws) !== 0)
                    message.draws = object.draws | 0;
            return message;
        };

        /**
         * Creates a plain object from a GameWLD message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GameWLD
         * @static
         * @param {game.GameWLD} message GameWLD
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameWLD.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.wins = 0;
                object.losses = 0;
                object.draws = 0;
            }
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                object.wins = message.wins;
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                object.losses = message.losses;
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws"))
                object.draws = message.draws;
            return object;
        };

        /**
         * Converts this GameWLD to JSON.
         * @function toJSON
         * @memberof game.GameWLD
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameWLD.prototype.toJSON = function() {
            return GameWLD.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GameWLD
         * @function getTypeUrl
         * @memberof game.GameWLD
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GameWLD.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GameWLD";
        };

        return GameWLD;
    })();

    game.GameStats = (function() {

        /**
         * Properties of a GameStats.
         * @typedef {Object} game.GameStats.$Properties
         * @property {game.GameWLD.$Properties|null} [rps] GameStats rps
         * @property {game.GameWLD.$Properties|null} [othello] GameStats othello
         * @property {game.GameWLD.$Properties|null} [tictactoe] GameStats tictactoe
         * @property {game.GameWLD.$Properties|null} [gomoku] GameStats gomoku
         * @property {game.GameWLD.$Properties|null} [liarsdice] GameStats liarsdice
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GameStats.
         * @memberof game
         * @interface IGameStats
         * @augments game.GameStats.$Properties
         * @deprecated Use game.GameStats.$Properties instead.
         */

        /**
         * Shape of a GameStats.
         * @typedef {game.GameStats.$Properties} game.GameStats.$Shape
         */

        /**
         * Constructs a new GameStats.
         * @memberof game
         * @classdesc Represents a GameStats.
         * @constructor
         * @param {game.GameStats.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GameStats = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GameStats rps.
         * @member {game.GameWLD.$Properties|null|undefined} rps
         * @memberof game.GameStats
         * @instance
         */
        GameStats.prototype.rps = null;

        /**
         * GameStats othello.
         * @member {game.GameWLD.$Properties|null|undefined} othello
         * @memberof game.GameStats
         * @instance
         */
        GameStats.prototype.othello = null;

        /**
         * GameStats tictactoe.
         * @member {game.GameWLD.$Properties|null|undefined} tictactoe
         * @memberof game.GameStats
         * @instance
         */
        GameStats.prototype.tictactoe = null;

        /**
         * GameStats gomoku.
         * @member {game.GameWLD.$Properties|null|undefined} gomoku
         * @memberof game.GameStats
         * @instance
         */
        GameStats.prototype.gomoku = null;

        /**
         * GameStats liarsdice.
         * @member {game.GameWLD.$Properties|null|undefined} liarsdice
         * @memberof game.GameStats
         * @instance
         */
        GameStats.prototype.liarsdice = null;

        /**
         * Creates a new GameStats instance using the specified properties.
         * @function create
         * @memberof game.GameStats
         * @static
         * @param {game.GameStats.$Properties=} [properties] Properties to set
         * @returns {game.GameStats} GameStats instance
         * @type {{
         *   (properties: game.GameStats.$Shape): game.GameStats & game.GameStats.$Shape;
         *   (properties?: game.GameStats.$Properties): game.GameStats;
         * }}
         */
        GameStats.create = function(properties) {
            return new GameStats(properties);
        };

        /**
         * Encodes the specified GameStats message. Does not implicitly {@link game.GameStats.verify|verify} messages.
         * @function encode
         * @memberof game.GameStats
         * @static
         * @param {game.GameStats.$Properties} message GameStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameStats.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.rps != null && $Object.hasOwnProperty.call(message, "rps"))
                $root.game.GameWLD.encode(message.rps, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.othello != null && $Object.hasOwnProperty.call(message, "othello"))
                $root.game.GameWLD.encode(message.othello, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.tictactoe != null && $Object.hasOwnProperty.call(message, "tictactoe"))
                $root.game.GameWLD.encode(message.tictactoe, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.gomoku != null && $Object.hasOwnProperty.call(message, "gomoku"))
                $root.game.GameWLD.encode(message.gomoku, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.liarsdice != null && $Object.hasOwnProperty.call(message, "liarsdice"))
                $root.game.GameWLD.encode(message.liarsdice, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GameStats message, length delimited. Does not implicitly {@link game.GameStats.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GameStats
         * @static
         * @param {game.GameStats.$Properties} message GameStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameStats.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GameStats message from the specified reader or buffer.
         * @function decode
         * @memberof game.GameStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GameStats & game.GameStats.$Shape} GameStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameStats.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GameStats(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.rps = $root.game.GameWLD.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rps);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.othello = $root.game.GameWLD.decode(reader, reader.uint32(), $undefined, _depth + 1, message.othello);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.tictactoe = $root.game.GameWLD.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tictactoe);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.gomoku = $root.game.GameWLD.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gomoku);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.liarsdice = $root.game.GameWLD.decode(reader, reader.uint32(), $undefined, _depth + 1, message.liarsdice);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GameStats message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GameStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GameStats & game.GameStats.$Shape} GameStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameStats.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameStats message.
         * @function verify
         * @memberof game.GameStats
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameStats.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.rps != null && $Object.hasOwnProperty.call(message, "rps")) {
                let error = $root.game.GameWLD.verify(message.rps, _depth + 1);
                if (error)
                    return "rps." + error;
            }
            if (message.othello != null && $Object.hasOwnProperty.call(message, "othello")) {
                let error = $root.game.GameWLD.verify(message.othello, _depth + 1);
                if (error)
                    return "othello." + error;
            }
            if (message.tictactoe != null && $Object.hasOwnProperty.call(message, "tictactoe")) {
                let error = $root.game.GameWLD.verify(message.tictactoe, _depth + 1);
                if (error)
                    return "tictactoe." + error;
            }
            if (message.gomoku != null && $Object.hasOwnProperty.call(message, "gomoku")) {
                let error = $root.game.GameWLD.verify(message.gomoku, _depth + 1);
                if (error)
                    return "gomoku." + error;
            }
            if (message.liarsdice != null && $Object.hasOwnProperty.call(message, "liarsdice")) {
                let error = $root.game.GameWLD.verify(message.liarsdice, _depth + 1);
                if (error)
                    return "liarsdice." + error;
            }
            return null;
        };

        /**
         * Creates a GameStats message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GameStats
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GameStats} GameStats
         */
        GameStats.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GameStats)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GameStats: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GameStats();
            if (object.rps != null) {
                if (!$util.isObject(object.rps))
                    throw $TypeError(".game.GameStats.rps: object expected");
                message.rps = $root.game.GameWLD.fromObject(object.rps, _depth + 1);
            }
            if (object.othello != null) {
                if (!$util.isObject(object.othello))
                    throw $TypeError(".game.GameStats.othello: object expected");
                message.othello = $root.game.GameWLD.fromObject(object.othello, _depth + 1);
            }
            if (object.tictactoe != null) {
                if (!$util.isObject(object.tictactoe))
                    throw $TypeError(".game.GameStats.tictactoe: object expected");
                message.tictactoe = $root.game.GameWLD.fromObject(object.tictactoe, _depth + 1);
            }
            if (object.gomoku != null) {
                if (!$util.isObject(object.gomoku))
                    throw $TypeError(".game.GameStats.gomoku: object expected");
                message.gomoku = $root.game.GameWLD.fromObject(object.gomoku, _depth + 1);
            }
            if (object.liarsdice != null) {
                if (!$util.isObject(object.liarsdice))
                    throw $TypeError(".game.GameStats.liarsdice: object expected");
                message.liarsdice = $root.game.GameWLD.fromObject(object.liarsdice, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameStats message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GameStats
         * @static
         * @param {game.GameStats} message GameStats
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameStats.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.rps = null;
                object.othello = null;
                object.tictactoe = null;
                object.gomoku = null;
                object.liarsdice = null;
            }
            if (message.rps != null && $Object.hasOwnProperty.call(message, "rps"))
                object.rps = $root.game.GameWLD.toObject(message.rps, options, _depth + 1);
            if (message.othello != null && $Object.hasOwnProperty.call(message, "othello"))
                object.othello = $root.game.GameWLD.toObject(message.othello, options, _depth + 1);
            if (message.tictactoe != null && $Object.hasOwnProperty.call(message, "tictactoe"))
                object.tictactoe = $root.game.GameWLD.toObject(message.tictactoe, options, _depth + 1);
            if (message.gomoku != null && $Object.hasOwnProperty.call(message, "gomoku"))
                object.gomoku = $root.game.GameWLD.toObject(message.gomoku, options, _depth + 1);
            if (message.liarsdice != null && $Object.hasOwnProperty.call(message, "liarsdice"))
                object.liarsdice = $root.game.GameWLD.toObject(message.liarsdice, options, _depth + 1);
            return object;
        };

        /**
         * Converts this GameStats to JSON.
         * @function toJSON
         * @memberof game.GameStats
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameStats.prototype.toJSON = function() {
            return GameStats.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GameStats
         * @function getTypeUrl
         * @memberof game.GameStats
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GameStats.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GameStats";
        };

        return GameStats;
    })();

    game.PublicPlayer = (function() {

        /**
         * Properties of a PublicPlayer.
         * @typedef {Object} game.PublicPlayer.$Properties
         * @property {string|null} [id] PublicPlayer id
         * @property {string|null} [name] PublicPlayer name
         * @property {string|null} [genderId] PublicPlayer genderId
         * @property {string|null} [genderLabel] PublicPlayer genderLabel
         * @property {string|null} [factionId] PublicPlayer factionId
         * @property {string|null} [avatarUrl] PublicPlayer avatarUrl
         * @property {string|null} [factionLabel] PublicPlayer factionLabel
         * @property {game.GenderColors.$Properties|null} [factionColors] PublicPlayer factionColors
         * @property {string|null} [displayName] PublicPlayer displayName
         * @property {boolean|null} [connected] PublicPlayer connected
         * @property {number|Long|null} [disconnectedAt] PublicPlayer disconnectedAt
         * @property {number|Long|null} [disconnectExpiresAt] PublicPlayer disconnectExpiresAt
         * @property {number|Long|null} [profileUpdatedAt] PublicPlayer profileUpdatedAt
         * @property {boolean|null} [nameWarEnabled] PublicPlayer nameWarEnabled
         * @property {number|Long|null} [nameWarToggledAt] PublicPlayer nameWarToggledAt
         * @property {string|null} [nameWarOriginalName] PublicPlayer nameWarOriginalName
         * @property {string|null} [nameWarPenaltyName] PublicPlayer nameWarPenaltyName
         * @property {boolean|null} [nameWarPunished] PublicPlayer nameWarPunished
         * @property {boolean|null} [nameWarAllowRename] PublicPlayer nameWarAllowRename
         * @property {number|Long|null} [nameWarRenameProtectedUntil] PublicPlayer nameWarRenameProtectedUntil
         * @property {string|null} [nameWarRenamedBy] PublicPlayer nameWarRenamedBy
         * @property {string|null} [nameWarRenamedByName] PublicPlayer nameWarRenamedByName
         * @property {number|Long|null} [nameWarRenameWindowStartedAt] PublicPlayer nameWarRenameWindowStartedAt
         * @property {number|null} [nameWarRenameCount] PublicPlayer nameWarRenameCount
         * @property {boolean|null} [giveawayEnabled] PublicPlayer giveawayEnabled
         * @property {number|null} [giveawayValue] PublicPlayer giveawayValue
         * @property {number|null} [giveawayClicks] PublicPlayer giveawayClicks
         * @property {string|null} [giveawayBoardText] PublicPlayer giveawayBoardText
         * @property {number|Long|null} [giveawayBoardSubmittedAt] PublicPlayer giveawayBoardSubmittedAt
         * @property {number|Long|null} [giveawayBoardExpiresAt] PublicPlayer giveawayBoardExpiresAt
         * @property {number|null} [giveawayBoardLikes] PublicPlayer giveawayBoardLikes
         * @property {number|null} [giveawayBoardDislikes] PublicPlayer giveawayBoardDislikes
         * @property {number|null} [giveawayBoardLikesThisHour] PublicPlayer giveawayBoardLikesThisHour
         * @property {number|Long|null} [giveawayBoardLikeWindowStartedAt] PublicPlayer giveawayBoardLikeWindowStartedAt
         * @property {number|Long|null} [giveawayVoteWindowStartedAt] PublicPlayer giveawayVoteWindowStartedAt
         * @property {number|null} [giveawayVoteCount] PublicPlayer giveawayVoteCount
         * @property {number|null} [giveawayVoteLikesThisHour] PublicPlayer giveawayVoteLikesThisHour
         * @property {number|null} [giveawayVoteDislikesThisHour] PublicPlayer giveawayVoteDislikesThisHour
         * @property {boolean|null} [rankMultiplierUnlocked] PublicPlayer rankMultiplierUnlocked
         * @property {boolean|null} [extremeModeEnabled] PublicPlayer extremeModeEnabled
         * @property {number|Long|null} [extremeModeToggledAt] PublicPlayer extremeModeToggledAt
         * @property {number|Long|null} [extremeModeCooldownUntil] PublicPlayer extremeModeCooldownUntil
         * @property {number|null} [extremeWinStreak] PublicPlayer extremeWinStreak
         * @property {number|Long|null} [extremeLastDecayHour] PublicPlayer extremeLastDecayHour
         * @property {boolean|null} [extremeForceClosed] PublicPlayer extremeForceClosed
         * @property {number|Long|null} [extremeForceClosedAt] PublicPlayer extremeForceClosedAt
         * @property {number|Long|null} [extremeRenameProtectedUntil] PublicPlayer extremeRenameProtectedUntil
         * @property {string|null} [extremeRenamedBy] PublicPlayer extremeRenamedBy
         * @property {string|null} [extremeRenamedByName] PublicPlayer extremeRenamedByName
         * @property {string|null} [roomId] PublicPlayer roomId
         * @property {boolean|null} [isAdmin] PublicPlayer isAdmin
         * @property {game.PublicStats.$Properties|null} [stats] PublicPlayer stats
         * @property {game.GameStats.$Properties|null} [gameStats] PublicPlayer gameStats
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PublicPlayer.
         * @memberof game
         * @interface IPublicPlayer
         * @augments game.PublicPlayer.$Properties
         * @deprecated Use game.PublicPlayer.$Properties instead.
         */

        /**
         * Shape of a PublicPlayer.
         * @typedef {game.PublicPlayer.$Properties} game.PublicPlayer.$Shape
         */

        /**
         * Constructs a new PublicPlayer.
         * @memberof game
         * @classdesc Represents a PublicPlayer.
         * @constructor
         * @param {game.PublicPlayer.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PublicPlayer = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PublicPlayer id.
         * @member {string} id
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.id = "";

        /**
         * PublicPlayer name.
         * @member {string} name
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.name = "";

        /**
         * PublicPlayer genderId.
         * @member {string} genderId
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.genderId = "";

        /**
         * PublicPlayer genderLabel.
         * @member {string} genderLabel
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.genderLabel = "";

        /**
         * PublicPlayer factionId.
         * @member {string} factionId
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.factionId = "";

        /**
         * PublicPlayer avatarUrl.
         * @member {string} avatarUrl
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.avatarUrl = "";

        /**
         * PublicPlayer factionLabel.
         * @member {string} factionLabel
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.factionLabel = "";

        /**
         * PublicPlayer factionColors.
         * @member {game.GenderColors.$Properties|null|undefined} factionColors
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.factionColors = null;

        /**
         * PublicPlayer displayName.
         * @member {string} displayName
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.displayName = "";

        /**
         * PublicPlayer connected.
         * @member {boolean} connected
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.connected = false;

        /**
         * PublicPlayer disconnectedAt.
         * @member {number|Long} disconnectedAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.disconnectedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer disconnectExpiresAt.
         * @member {number|Long} disconnectExpiresAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.disconnectExpiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer profileUpdatedAt.
         * @member {number|Long} profileUpdatedAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.profileUpdatedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer nameWarEnabled.
         * @member {boolean} nameWarEnabled
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarEnabled = false;

        /**
         * PublicPlayer nameWarToggledAt.
         * @member {number|Long} nameWarToggledAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarToggledAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer nameWarOriginalName.
         * @member {string} nameWarOriginalName
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarOriginalName = "";

        /**
         * PublicPlayer nameWarPenaltyName.
         * @member {string} nameWarPenaltyName
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarPenaltyName = "";

        /**
         * PublicPlayer nameWarPunished.
         * @member {boolean} nameWarPunished
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarPunished = false;

        /**
         * PublicPlayer nameWarAllowRename.
         * @member {boolean} nameWarAllowRename
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarAllowRename = false;

        /**
         * PublicPlayer nameWarRenameProtectedUntil.
         * @member {number|Long} nameWarRenameProtectedUntil
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarRenameProtectedUntil = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer nameWarRenamedBy.
         * @member {string} nameWarRenamedBy
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarRenamedBy = "";

        /**
         * PublicPlayer nameWarRenamedByName.
         * @member {string} nameWarRenamedByName
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarRenamedByName = "";

        /**
         * PublicPlayer nameWarRenameWindowStartedAt.
         * @member {number|Long} nameWarRenameWindowStartedAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarRenameWindowStartedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer nameWarRenameCount.
         * @member {number} nameWarRenameCount
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.nameWarRenameCount = 0;

        /**
         * PublicPlayer giveawayEnabled.
         * @member {boolean} giveawayEnabled
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayEnabled = false;

        /**
         * PublicPlayer giveawayValue.
         * @member {number} giveawayValue
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayValue = 0;

        /**
         * PublicPlayer giveawayClicks.
         * @member {number} giveawayClicks
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayClicks = 0;

        /**
         * PublicPlayer giveawayBoardText.
         * @member {string} giveawayBoardText
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayBoardText = "";

        /**
         * PublicPlayer giveawayBoardSubmittedAt.
         * @member {number|Long} giveawayBoardSubmittedAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayBoardSubmittedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer giveawayBoardExpiresAt.
         * @member {number|Long} giveawayBoardExpiresAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayBoardExpiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer giveawayBoardLikes.
         * @member {number} giveawayBoardLikes
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayBoardLikes = 0;

        /**
         * PublicPlayer giveawayBoardDislikes.
         * @member {number} giveawayBoardDislikes
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayBoardDislikes = 0;

        /**
         * PublicPlayer giveawayBoardLikesThisHour.
         * @member {number} giveawayBoardLikesThisHour
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayBoardLikesThisHour = 0;

        /**
         * PublicPlayer giveawayBoardLikeWindowStartedAt.
         * @member {number|Long} giveawayBoardLikeWindowStartedAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayBoardLikeWindowStartedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer giveawayVoteWindowStartedAt.
         * @member {number|Long} giveawayVoteWindowStartedAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayVoteWindowStartedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer giveawayVoteCount.
         * @member {number} giveawayVoteCount
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayVoteCount = 0;

        /**
         * PublicPlayer giveawayVoteLikesThisHour.
         * @member {number} giveawayVoteLikesThisHour
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayVoteLikesThisHour = 0;

        /**
         * PublicPlayer giveawayVoteDislikesThisHour.
         * @member {number} giveawayVoteDislikesThisHour
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.giveawayVoteDislikesThisHour = 0;

        /**
         * PublicPlayer rankMultiplierUnlocked.
         * @member {boolean} rankMultiplierUnlocked
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.rankMultiplierUnlocked = false;

        /**
         * PublicPlayer extremeModeEnabled.
         * @member {boolean} extremeModeEnabled
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeModeEnabled = false;

        /**
         * PublicPlayer extremeModeToggledAt.
         * @member {number|Long} extremeModeToggledAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeModeToggledAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer extremeModeCooldownUntil.
         * @member {number|Long} extremeModeCooldownUntil
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeModeCooldownUntil = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer extremeWinStreak.
         * @member {number} extremeWinStreak
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeWinStreak = 0;

        /**
         * PublicPlayer extremeLastDecayHour.
         * @member {number|Long} extremeLastDecayHour
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeLastDecayHour = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer extremeForceClosed.
         * @member {boolean} extremeForceClosed
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeForceClosed = false;

        /**
         * PublicPlayer extremeForceClosedAt.
         * @member {number|Long} extremeForceClosedAt
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeForceClosedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer extremeRenameProtectedUntil.
         * @member {number|Long} extremeRenameProtectedUntil
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeRenameProtectedUntil = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PublicPlayer extremeRenamedBy.
         * @member {string} extremeRenamedBy
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeRenamedBy = "";

        /**
         * PublicPlayer extremeRenamedByName.
         * @member {string} extremeRenamedByName
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.extremeRenamedByName = "";

        /**
         * PublicPlayer roomId.
         * @member {string} roomId
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.roomId = "";

        /**
         * PublicPlayer isAdmin.
         * @member {boolean} isAdmin
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.isAdmin = false;

        /**
         * PublicPlayer stats.
         * @member {game.PublicStats.$Properties|null|undefined} stats
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.stats = null;

        /**
         * PublicPlayer gameStats.
         * @member {game.GameStats.$Properties|null|undefined} gameStats
         * @memberof game.PublicPlayer
         * @instance
         */
        PublicPlayer.prototype.gameStats = null;

        /**
         * Creates a new PublicPlayer instance using the specified properties.
         * @function create
         * @memberof game.PublicPlayer
         * @static
         * @param {game.PublicPlayer.$Properties=} [properties] Properties to set
         * @returns {game.PublicPlayer} PublicPlayer instance
         * @type {{
         *   (properties: game.PublicPlayer.$Shape): game.PublicPlayer & game.PublicPlayer.$Shape;
         *   (properties?: game.PublicPlayer.$Properties): game.PublicPlayer;
         * }}
         */
        PublicPlayer.create = function(properties) {
            return new PublicPlayer(properties);
        };

        /**
         * Encodes the specified PublicPlayer message. Does not implicitly {@link game.PublicPlayer.verify|verify} messages.
         * @function encode
         * @memberof game.PublicPlayer
         * @static
         * @param {game.PublicPlayer.$Properties} message PublicPlayer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublicPlayer.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.genderId != null && $Object.hasOwnProperty.call(message, "genderId") && message.genderId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.genderId);
            if (message.genderLabel != null && $Object.hasOwnProperty.call(message, "genderLabel") && message.genderLabel !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.genderLabel);
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId") && message.factionId !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.factionId);
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel") && message.factionLabel !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.factionLabel);
            if (message.factionColors != null && $Object.hasOwnProperty.call(message, "factionColors"))
                $root.game.GenderColors.encode(message.factionColors, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.displayName != null && $Object.hasOwnProperty.call(message, "displayName") && message.displayName !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.displayName);
            if (message.connected != null && $Object.hasOwnProperty.call(message, "connected") && message.connected !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.connected);
            if (message.disconnectedAt != null && $Object.hasOwnProperty.call(message, "disconnectedAt") && (typeof message.disconnectedAt === "object" ? message.disconnectedAt.low || message.disconnectedAt.high : message.disconnectedAt !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.disconnectedAt);
            if (message.disconnectExpiresAt != null && $Object.hasOwnProperty.call(message, "disconnectExpiresAt") && (typeof message.disconnectExpiresAt === "object" ? message.disconnectExpiresAt.low || message.disconnectExpiresAt.high : message.disconnectExpiresAt !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.disconnectExpiresAt);
            if (message.profileUpdatedAt != null && $Object.hasOwnProperty.call(message, "profileUpdatedAt") && (typeof message.profileUpdatedAt === "object" ? message.profileUpdatedAt.low || message.profileUpdatedAt.high : message.profileUpdatedAt !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.profileUpdatedAt);
            if (message.nameWarEnabled != null && $Object.hasOwnProperty.call(message, "nameWarEnabled") && message.nameWarEnabled !== false)
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.nameWarEnabled);
            if (message.nameWarToggledAt != null && $Object.hasOwnProperty.call(message, "nameWarToggledAt") && (typeof message.nameWarToggledAt === "object" ? message.nameWarToggledAt.low || message.nameWarToggledAt.high : message.nameWarToggledAt !== 0))
                writer.uint32(/* id 18, wireType 0 =*/144).int64(message.nameWarToggledAt);
            if (message.nameWarOriginalName != null && $Object.hasOwnProperty.call(message, "nameWarOriginalName") && message.nameWarOriginalName !== "")
                writer.uint32(/* id 20, wireType 2 =*/162).string(message.nameWarOriginalName);
            if (message.nameWarPenaltyName != null && $Object.hasOwnProperty.call(message, "nameWarPenaltyName") && message.nameWarPenaltyName !== "")
                writer.uint32(/* id 21, wireType 2 =*/170).string(message.nameWarPenaltyName);
            if (message.nameWarPunished != null && $Object.hasOwnProperty.call(message, "nameWarPunished") && message.nameWarPunished !== false)
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.nameWarPunished);
            if (message.nameWarAllowRename != null && $Object.hasOwnProperty.call(message, "nameWarAllowRename") && message.nameWarAllowRename !== false)
                writer.uint32(/* id 24, wireType 0 =*/192).bool(message.nameWarAllowRename);
            if (message.nameWarRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "nameWarRenameProtectedUntil") && (typeof message.nameWarRenameProtectedUntil === "object" ? message.nameWarRenameProtectedUntil.low || message.nameWarRenameProtectedUntil.high : message.nameWarRenameProtectedUntil !== 0))
                writer.uint32(/* id 26, wireType 0 =*/208).int64(message.nameWarRenameProtectedUntil);
            if (message.nameWarRenamedBy != null && $Object.hasOwnProperty.call(message, "nameWarRenamedBy") && message.nameWarRenamedBy !== "")
                writer.uint32(/* id 28, wireType 2 =*/226).string(message.nameWarRenamedBy);
            if (message.nameWarRenamedByName != null && $Object.hasOwnProperty.call(message, "nameWarRenamedByName") && message.nameWarRenamedByName !== "")
                writer.uint32(/* id 29, wireType 2 =*/234).string(message.nameWarRenamedByName);
            if (message.nameWarRenameWindowStartedAt != null && $Object.hasOwnProperty.call(message, "nameWarRenameWindowStartedAt") && (typeof message.nameWarRenameWindowStartedAt === "object" ? message.nameWarRenameWindowStartedAt.low || message.nameWarRenameWindowStartedAt.high : message.nameWarRenameWindowStartedAt !== 0))
                writer.uint32(/* id 30, wireType 0 =*/240).int64(message.nameWarRenameWindowStartedAt);
            if (message.nameWarRenameCount != null && $Object.hasOwnProperty.call(message, "nameWarRenameCount") && message.nameWarRenameCount !== 0)
                writer.uint32(/* id 32, wireType 0 =*/256).int32(message.nameWarRenameCount);
            if (message.giveawayEnabled != null && $Object.hasOwnProperty.call(message, "giveawayEnabled") && message.giveawayEnabled !== false)
                writer.uint32(/* id 34, wireType 0 =*/272).bool(message.giveawayEnabled);
            if (message.giveawayValue != null && $Object.hasOwnProperty.call(message, "giveawayValue") && !$Object.is(message.giveawayValue, 0))
                writer.uint32(/* id 36, wireType 1 =*/289).double(message.giveawayValue);
            if (message.giveawayClicks != null && $Object.hasOwnProperty.call(message, "giveawayClicks") && message.giveawayClicks !== 0)
                writer.uint32(/* id 38, wireType 0 =*/304).int32(message.giveawayClicks);
            if (message.giveawayBoardText != null && $Object.hasOwnProperty.call(message, "giveawayBoardText") && message.giveawayBoardText !== "")
                writer.uint32(/* id 40, wireType 2 =*/322).string(message.giveawayBoardText);
            if (message.giveawayBoardSubmittedAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardSubmittedAt") && (typeof message.giveawayBoardSubmittedAt === "object" ? message.giveawayBoardSubmittedAt.low || message.giveawayBoardSubmittedAt.high : message.giveawayBoardSubmittedAt !== 0))
                writer.uint32(/* id 41, wireType 0 =*/328).int64(message.giveawayBoardSubmittedAt);
            if (message.giveawayBoardExpiresAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardExpiresAt") && (typeof message.giveawayBoardExpiresAt === "object" ? message.giveawayBoardExpiresAt.low || message.giveawayBoardExpiresAt.high : message.giveawayBoardExpiresAt !== 0))
                writer.uint32(/* id 43, wireType 0 =*/344).int64(message.giveawayBoardExpiresAt);
            if (message.giveawayBoardLikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikes") && message.giveawayBoardLikes !== 0)
                writer.uint32(/* id 45, wireType 0 =*/360).int32(message.giveawayBoardLikes);
            if (message.giveawayBoardDislikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardDislikes") && message.giveawayBoardDislikes !== 0)
                writer.uint32(/* id 47, wireType 0 =*/376).int32(message.giveawayBoardDislikes);
            if (message.giveawayBoardLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikesThisHour") && message.giveawayBoardLikesThisHour !== 0)
                writer.uint32(/* id 49, wireType 0 =*/392).int32(message.giveawayBoardLikesThisHour);
            if (message.giveawayBoardLikeWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikeWindowStartedAt") && (typeof message.giveawayBoardLikeWindowStartedAt === "object" ? message.giveawayBoardLikeWindowStartedAt.low || message.giveawayBoardLikeWindowStartedAt.high : message.giveawayBoardLikeWindowStartedAt !== 0))
                writer.uint32(/* id 51, wireType 0 =*/408).int64(message.giveawayBoardLikeWindowStartedAt);
            if (message.giveawayVoteWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayVoteWindowStartedAt") && (typeof message.giveawayVoteWindowStartedAt === "object" ? message.giveawayVoteWindowStartedAt.low || message.giveawayVoteWindowStartedAt.high : message.giveawayVoteWindowStartedAt !== 0))
                writer.uint32(/* id 53, wireType 0 =*/424).int64(message.giveawayVoteWindowStartedAt);
            if (message.giveawayVoteCount != null && $Object.hasOwnProperty.call(message, "giveawayVoteCount") && message.giveawayVoteCount !== 0)
                writer.uint32(/* id 55, wireType 0 =*/440).int32(message.giveawayVoteCount);
            if (message.giveawayVoteLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteLikesThisHour") && message.giveawayVoteLikesThisHour !== 0)
                writer.uint32(/* id 57, wireType 0 =*/456).int32(message.giveawayVoteLikesThisHour);
            if (message.giveawayVoteDislikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteDislikesThisHour") && message.giveawayVoteDislikesThisHour !== 0)
                writer.uint32(/* id 59, wireType 0 =*/472).int32(message.giveawayVoteDislikesThisHour);
            if (message.rankMultiplierUnlocked != null && $Object.hasOwnProperty.call(message, "rankMultiplierUnlocked") && message.rankMultiplierUnlocked !== false)
                writer.uint32(/* id 61, wireType 0 =*/488).bool(message.rankMultiplierUnlocked);
            if (message.extremeModeEnabled != null && $Object.hasOwnProperty.call(message, "extremeModeEnabled") && message.extremeModeEnabled !== false)
                writer.uint32(/* id 63, wireType 0 =*/504).bool(message.extremeModeEnabled);
            if (message.extremeModeToggledAt != null && $Object.hasOwnProperty.call(message, "extremeModeToggledAt") && (typeof message.extremeModeToggledAt === "object" ? message.extremeModeToggledAt.low || message.extremeModeToggledAt.high : message.extremeModeToggledAt !== 0))
                writer.uint32(/* id 65, wireType 0 =*/520).int64(message.extremeModeToggledAt);
            if (message.extremeModeCooldownUntil != null && $Object.hasOwnProperty.call(message, "extremeModeCooldownUntil") && (typeof message.extremeModeCooldownUntil === "object" ? message.extremeModeCooldownUntil.low || message.extremeModeCooldownUntil.high : message.extremeModeCooldownUntil !== 0))
                writer.uint32(/* id 67, wireType 0 =*/536).int64(message.extremeModeCooldownUntil);
            if (message.extremeWinStreak != null && $Object.hasOwnProperty.call(message, "extremeWinStreak") && message.extremeWinStreak !== 0)
                writer.uint32(/* id 69, wireType 0 =*/552).int32(message.extremeWinStreak);
            if (message.extremeLastDecayHour != null && $Object.hasOwnProperty.call(message, "extremeLastDecayHour") && (typeof message.extremeLastDecayHour === "object" ? message.extremeLastDecayHour.low || message.extremeLastDecayHour.high : message.extremeLastDecayHour !== 0))
                writer.uint32(/* id 71, wireType 0 =*/568).int64(message.extremeLastDecayHour);
            if (message.extremeForceClosed != null && $Object.hasOwnProperty.call(message, "extremeForceClosed") && message.extremeForceClosed !== false)
                writer.uint32(/* id 73, wireType 0 =*/584).bool(message.extremeForceClosed);
            if (message.extremeForceClosedAt != null && $Object.hasOwnProperty.call(message, "extremeForceClosedAt") && (typeof message.extremeForceClosedAt === "object" ? message.extremeForceClosedAt.low || message.extremeForceClosedAt.high : message.extremeForceClosedAt !== 0))
                writer.uint32(/* id 75, wireType 0 =*/600).int64(message.extremeForceClosedAt);
            if (message.extremeRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "extremeRenameProtectedUntil") && (typeof message.extremeRenameProtectedUntil === "object" ? message.extremeRenameProtectedUntil.low || message.extremeRenameProtectedUntil.high : message.extremeRenameProtectedUntil !== 0))
                writer.uint32(/* id 77, wireType 0 =*/616).int64(message.extremeRenameProtectedUntil);
            if (message.extremeRenamedBy != null && $Object.hasOwnProperty.call(message, "extremeRenamedBy") && message.extremeRenamedBy !== "")
                writer.uint32(/* id 79, wireType 2 =*/634).string(message.extremeRenamedBy);
            if (message.extremeRenamedByName != null && $Object.hasOwnProperty.call(message, "extremeRenamedByName") && message.extremeRenamedByName !== "")
                writer.uint32(/* id 80, wireType 2 =*/642).string(message.extremeRenamedByName);
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId") && message.roomId !== "")
                writer.uint32(/* id 81, wireType 2 =*/650).string(message.roomId);
            if (message.isAdmin != null && $Object.hasOwnProperty.call(message, "isAdmin") && message.isAdmin !== false)
                writer.uint32(/* id 82, wireType 0 =*/656).bool(message.isAdmin);
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats"))
                $root.game.PublicStats.encode(message.stats, writer.uint32(/* id 84, wireType 2 =*/674).fork(), _depth + 1).ldelim();
            if (message.gameStats != null && $Object.hasOwnProperty.call(message, "gameStats"))
                $root.game.GameStats.encode(message.gameStats, writer.uint32(/* id 85, wireType 2 =*/682).fork(), _depth + 1).ldelim();
            if (message.avatarUrl != null && $Object.hasOwnProperty.call(message, "avatarUrl") && message.avatarUrl !== "")
                writer.uint32(/* id 86, wireType 2 =*/690).string(message.avatarUrl);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PublicPlayer message, length delimited. Does not implicitly {@link game.PublicPlayer.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PublicPlayer
         * @static
         * @param {game.PublicPlayer.$Properties} message PublicPlayer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublicPlayer.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PublicPlayer message from the specified reader or buffer.
         * @function decode
         * @memberof game.PublicPlayer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PublicPlayer & game.PublicPlayer.$Shape} PublicPlayer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublicPlayer.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PublicPlayer(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.genderId = value;
                        else
                            delete message.genderId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.genderLabel = value;
                        else
                            delete message.genderLabel;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.factionId = value;
                        else
                            delete message.factionId;
                        continue;
                    }
                case 86: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.avatarUrl = value;
                        else
                            delete message.avatarUrl;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.factionLabel = value;
                        else
                            delete message.factionLabel;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.factionColors = $root.game.GenderColors.decode(reader, reader.uint32(), $undefined, _depth + 1, message.factionColors);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.displayName = value;
                        else
                            delete message.displayName;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.connected = value;
                        else
                            delete message.connected;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.disconnectedAt = value;
                        else
                            delete message.disconnectedAt;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.disconnectExpiresAt = value;
                        else
                            delete message.disconnectExpiresAt;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.profileUpdatedAt = value;
                        else
                            delete message.profileUpdatedAt;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.nameWarEnabled = value;
                        else
                            delete message.nameWarEnabled;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nameWarToggledAt = value;
                        else
                            delete message.nameWarToggledAt;
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nameWarOriginalName = value;
                        else
                            delete message.nameWarOriginalName;
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nameWarPenaltyName = value;
                        else
                            delete message.nameWarPenaltyName;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.nameWarPunished = value;
                        else
                            delete message.nameWarPunished;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.nameWarAllowRename = value;
                        else
                            delete message.nameWarAllowRename;
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nameWarRenameProtectedUntil = value;
                        else
                            delete message.nameWarRenameProtectedUntil;
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nameWarRenamedBy = value;
                        else
                            delete message.nameWarRenamedBy;
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nameWarRenamedByName = value;
                        else
                            delete message.nameWarRenamedByName;
                        continue;
                    }
                case 30: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nameWarRenameWindowStartedAt = value;
                        else
                            delete message.nameWarRenameWindowStartedAt;
                        continue;
                    }
                case 32: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.nameWarRenameCount = value;
                        else
                            delete message.nameWarRenameCount;
                        continue;
                    }
                case 34: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.giveawayEnabled = value;
                        else
                            delete message.giveawayEnabled;
                        continue;
                    }
                case 36: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.giveawayValue = value;
                        else
                            delete message.giveawayValue;
                        continue;
                    }
                case 38: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayClicks = value;
                        else
                            delete message.giveawayClicks;
                        continue;
                    }
                case 40: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.giveawayBoardText = value;
                        else
                            delete message.giveawayBoardText;
                        continue;
                    }
                case 41: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.giveawayBoardSubmittedAt = value;
                        else
                            delete message.giveawayBoardSubmittedAt;
                        continue;
                    }
                case 43: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.giveawayBoardExpiresAt = value;
                        else
                            delete message.giveawayBoardExpiresAt;
                        continue;
                    }
                case 45: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayBoardLikes = value;
                        else
                            delete message.giveawayBoardLikes;
                        continue;
                    }
                case 47: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayBoardDislikes = value;
                        else
                            delete message.giveawayBoardDislikes;
                        continue;
                    }
                case 49: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayBoardLikesThisHour = value;
                        else
                            delete message.giveawayBoardLikesThisHour;
                        continue;
                    }
                case 51: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.giveawayBoardLikeWindowStartedAt = value;
                        else
                            delete message.giveawayBoardLikeWindowStartedAt;
                        continue;
                    }
                case 53: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.giveawayVoteWindowStartedAt = value;
                        else
                            delete message.giveawayVoteWindowStartedAt;
                        continue;
                    }
                case 55: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayVoteCount = value;
                        else
                            delete message.giveawayVoteCount;
                        continue;
                    }
                case 57: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayVoteLikesThisHour = value;
                        else
                            delete message.giveawayVoteLikesThisHour;
                        continue;
                    }
                case 59: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayVoteDislikesThisHour = value;
                        else
                            delete message.giveawayVoteDislikesThisHour;
                        continue;
                    }
                case 61: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.rankMultiplierUnlocked = value;
                        else
                            delete message.rankMultiplierUnlocked;
                        continue;
                    }
                case 63: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.extremeModeEnabled = value;
                        else
                            delete message.extremeModeEnabled;
                        continue;
                    }
                case 65: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.extremeModeToggledAt = value;
                        else
                            delete message.extremeModeToggledAt;
                        continue;
                    }
                case 67: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.extremeModeCooldownUntil = value;
                        else
                            delete message.extremeModeCooldownUntil;
                        continue;
                    }
                case 69: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.extremeWinStreak = value;
                        else
                            delete message.extremeWinStreak;
                        continue;
                    }
                case 71: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.extremeLastDecayHour = value;
                        else
                            delete message.extremeLastDecayHour;
                        continue;
                    }
                case 73: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.extremeForceClosed = value;
                        else
                            delete message.extremeForceClosed;
                        continue;
                    }
                case 75: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.extremeForceClosedAt = value;
                        else
                            delete message.extremeForceClosedAt;
                        continue;
                    }
                case 77: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.extremeRenameProtectedUntil = value;
                        else
                            delete message.extremeRenameProtectedUntil;
                        continue;
                    }
                case 79: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.extremeRenamedBy = value;
                        else
                            delete message.extremeRenamedBy;
                        continue;
                    }
                case 80: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.extremeRenamedByName = value;
                        else
                            delete message.extremeRenamedByName;
                        continue;
                    }
                case 81: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.roomId = value;
                        else
                            delete message.roomId;
                        continue;
                    }
                case 82: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isAdmin = value;
                        else
                            delete message.isAdmin;
                        continue;
                    }
                case 84: {
                        if (wireType !== 2)
                            break;
                        message.stats = $root.game.PublicStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stats);
                        continue;
                    }
                case 85: {
                        if (wireType !== 2)
                            break;
                        message.gameStats = $root.game.GameStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gameStats);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PublicPlayer message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PublicPlayer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PublicPlayer & game.PublicPlayer.$Shape} PublicPlayer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublicPlayer.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PublicPlayer message.
         * @function verify
         * @memberof game.PublicPlayer
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PublicPlayer.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.genderId != null && $Object.hasOwnProperty.call(message, "genderId"))
                if (!$util.isString(message.genderId))
                    return "genderId: string expected";
            if (message.genderLabel != null && $Object.hasOwnProperty.call(message, "genderLabel"))
                if (!$util.isString(message.genderLabel))
                    return "genderLabel: string expected";
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                if (!$util.isString(message.factionId))
                    return "factionId: string expected";
            if (message.avatarUrl != null && $Object.hasOwnProperty.call(message, "avatarUrl"))
                if (!$util.isString(message.avatarUrl))
                    return "avatarUrl: string expected";
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel"))
                if (!$util.isString(message.factionLabel))
                    return "factionLabel: string expected";
            if (message.factionColors != null && $Object.hasOwnProperty.call(message, "factionColors")) {
                let error = $root.game.GenderColors.verify(message.factionColors, _depth + 1);
                if (error)
                    return "factionColors." + error;
            }
            if (message.displayName != null && $Object.hasOwnProperty.call(message, "displayName"))
                if (!$util.isString(message.displayName))
                    return "displayName: string expected";
            if (message.connected != null && $Object.hasOwnProperty.call(message, "connected"))
                if (typeof message.connected !== "boolean")
                    return "connected: boolean expected";
            if (message.disconnectedAt != null && $Object.hasOwnProperty.call(message, "disconnectedAt"))
                if (!$util.isInteger(message.disconnectedAt) && !(message.disconnectedAt && $util.isInteger(message.disconnectedAt.low) && $util.isInteger(message.disconnectedAt.high)))
                    return "disconnectedAt: integer|Long expected";
            if (message.disconnectExpiresAt != null && $Object.hasOwnProperty.call(message, "disconnectExpiresAt"))
                if (!$util.isInteger(message.disconnectExpiresAt) && !(message.disconnectExpiresAt && $util.isInteger(message.disconnectExpiresAt.low) && $util.isInteger(message.disconnectExpiresAt.high)))
                    return "disconnectExpiresAt: integer|Long expected";
            if (message.profileUpdatedAt != null && $Object.hasOwnProperty.call(message, "profileUpdatedAt"))
                if (!$util.isInteger(message.profileUpdatedAt) && !(message.profileUpdatedAt && $util.isInteger(message.profileUpdatedAt.low) && $util.isInteger(message.profileUpdatedAt.high)))
                    return "profileUpdatedAt: integer|Long expected";
            if (message.nameWarEnabled != null && $Object.hasOwnProperty.call(message, "nameWarEnabled"))
                if (typeof message.nameWarEnabled !== "boolean")
                    return "nameWarEnabled: boolean expected";
            if (message.nameWarToggledAt != null && $Object.hasOwnProperty.call(message, "nameWarToggledAt"))
                if (!$util.isInteger(message.nameWarToggledAt) && !(message.nameWarToggledAt && $util.isInteger(message.nameWarToggledAt.low) && $util.isInteger(message.nameWarToggledAt.high)))
                    return "nameWarToggledAt: integer|Long expected";
            if (message.nameWarOriginalName != null && $Object.hasOwnProperty.call(message, "nameWarOriginalName"))
                if (!$util.isString(message.nameWarOriginalName))
                    return "nameWarOriginalName: string expected";
            if (message.nameWarPenaltyName != null && $Object.hasOwnProperty.call(message, "nameWarPenaltyName"))
                if (!$util.isString(message.nameWarPenaltyName))
                    return "nameWarPenaltyName: string expected";
            if (message.nameWarPunished != null && $Object.hasOwnProperty.call(message, "nameWarPunished"))
                if (typeof message.nameWarPunished !== "boolean")
                    return "nameWarPunished: boolean expected";
            if (message.nameWarAllowRename != null && $Object.hasOwnProperty.call(message, "nameWarAllowRename"))
                if (typeof message.nameWarAllowRename !== "boolean")
                    return "nameWarAllowRename: boolean expected";
            if (message.nameWarRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "nameWarRenameProtectedUntil"))
                if (!$util.isInteger(message.nameWarRenameProtectedUntil) && !(message.nameWarRenameProtectedUntil && $util.isInteger(message.nameWarRenameProtectedUntil.low) && $util.isInteger(message.nameWarRenameProtectedUntil.high)))
                    return "nameWarRenameProtectedUntil: integer|Long expected";
            if (message.nameWarRenamedBy != null && $Object.hasOwnProperty.call(message, "nameWarRenamedBy"))
                if (!$util.isString(message.nameWarRenamedBy))
                    return "nameWarRenamedBy: string expected";
            if (message.nameWarRenamedByName != null && $Object.hasOwnProperty.call(message, "nameWarRenamedByName"))
                if (!$util.isString(message.nameWarRenamedByName))
                    return "nameWarRenamedByName: string expected";
            if (message.nameWarRenameWindowStartedAt != null && $Object.hasOwnProperty.call(message, "nameWarRenameWindowStartedAt"))
                if (!$util.isInteger(message.nameWarRenameWindowStartedAt) && !(message.nameWarRenameWindowStartedAt && $util.isInteger(message.nameWarRenameWindowStartedAt.low) && $util.isInteger(message.nameWarRenameWindowStartedAt.high)))
                    return "nameWarRenameWindowStartedAt: integer|Long expected";
            if (message.nameWarRenameCount != null && $Object.hasOwnProperty.call(message, "nameWarRenameCount"))
                if (!$util.isInteger(message.nameWarRenameCount))
                    return "nameWarRenameCount: integer expected";
            if (message.giveawayEnabled != null && $Object.hasOwnProperty.call(message, "giveawayEnabled"))
                if (typeof message.giveawayEnabled !== "boolean")
                    return "giveawayEnabled: boolean expected";
            if (message.giveawayValue != null && $Object.hasOwnProperty.call(message, "giveawayValue"))
                if (typeof message.giveawayValue !== "number")
                    return "giveawayValue: number expected";
            if (message.giveawayClicks != null && $Object.hasOwnProperty.call(message, "giveawayClicks"))
                if (!$util.isInteger(message.giveawayClicks))
                    return "giveawayClicks: integer expected";
            if (message.giveawayBoardText != null && $Object.hasOwnProperty.call(message, "giveawayBoardText"))
                if (!$util.isString(message.giveawayBoardText))
                    return "giveawayBoardText: string expected";
            if (message.giveawayBoardSubmittedAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardSubmittedAt"))
                if (!$util.isInteger(message.giveawayBoardSubmittedAt) && !(message.giveawayBoardSubmittedAt && $util.isInteger(message.giveawayBoardSubmittedAt.low) && $util.isInteger(message.giveawayBoardSubmittedAt.high)))
                    return "giveawayBoardSubmittedAt: integer|Long expected";
            if (message.giveawayBoardExpiresAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardExpiresAt"))
                if (!$util.isInteger(message.giveawayBoardExpiresAt) && !(message.giveawayBoardExpiresAt && $util.isInteger(message.giveawayBoardExpiresAt.low) && $util.isInteger(message.giveawayBoardExpiresAt.high)))
                    return "giveawayBoardExpiresAt: integer|Long expected";
            if (message.giveawayBoardLikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikes"))
                if (!$util.isInteger(message.giveawayBoardLikes))
                    return "giveawayBoardLikes: integer expected";
            if (message.giveawayBoardDislikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardDislikes"))
                if (!$util.isInteger(message.giveawayBoardDislikes))
                    return "giveawayBoardDislikes: integer expected";
            if (message.giveawayBoardLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikesThisHour"))
                if (!$util.isInteger(message.giveawayBoardLikesThisHour))
                    return "giveawayBoardLikesThisHour: integer expected";
            if (message.giveawayBoardLikeWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikeWindowStartedAt"))
                if (!$util.isInteger(message.giveawayBoardLikeWindowStartedAt) && !(message.giveawayBoardLikeWindowStartedAt && $util.isInteger(message.giveawayBoardLikeWindowStartedAt.low) && $util.isInteger(message.giveawayBoardLikeWindowStartedAt.high)))
                    return "giveawayBoardLikeWindowStartedAt: integer|Long expected";
            if (message.giveawayVoteWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayVoteWindowStartedAt"))
                if (!$util.isInteger(message.giveawayVoteWindowStartedAt) && !(message.giveawayVoteWindowStartedAt && $util.isInteger(message.giveawayVoteWindowStartedAt.low) && $util.isInteger(message.giveawayVoteWindowStartedAt.high)))
                    return "giveawayVoteWindowStartedAt: integer|Long expected";
            if (message.giveawayVoteCount != null && $Object.hasOwnProperty.call(message, "giveawayVoteCount"))
                if (!$util.isInteger(message.giveawayVoteCount))
                    return "giveawayVoteCount: integer expected";
            if (message.giveawayVoteLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteLikesThisHour"))
                if (!$util.isInteger(message.giveawayVoteLikesThisHour))
                    return "giveawayVoteLikesThisHour: integer expected";
            if (message.giveawayVoteDislikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteDislikesThisHour"))
                if (!$util.isInteger(message.giveawayVoteDislikesThisHour))
                    return "giveawayVoteDislikesThisHour: integer expected";
            if (message.rankMultiplierUnlocked != null && $Object.hasOwnProperty.call(message, "rankMultiplierUnlocked"))
                if (typeof message.rankMultiplierUnlocked !== "boolean")
                    return "rankMultiplierUnlocked: boolean expected";
            if (message.extremeModeEnabled != null && $Object.hasOwnProperty.call(message, "extremeModeEnabled"))
                if (typeof message.extremeModeEnabled !== "boolean")
                    return "extremeModeEnabled: boolean expected";
            if (message.extremeModeToggledAt != null && $Object.hasOwnProperty.call(message, "extremeModeToggledAt"))
                if (!$util.isInteger(message.extremeModeToggledAt) && !(message.extremeModeToggledAt && $util.isInteger(message.extremeModeToggledAt.low) && $util.isInteger(message.extremeModeToggledAt.high)))
                    return "extremeModeToggledAt: integer|Long expected";
            if (message.extremeModeCooldownUntil != null && $Object.hasOwnProperty.call(message, "extremeModeCooldownUntil"))
                if (!$util.isInteger(message.extremeModeCooldownUntil) && !(message.extremeModeCooldownUntil && $util.isInteger(message.extremeModeCooldownUntil.low) && $util.isInteger(message.extremeModeCooldownUntil.high)))
                    return "extremeModeCooldownUntil: integer|Long expected";
            if (message.extremeWinStreak != null && $Object.hasOwnProperty.call(message, "extremeWinStreak"))
                if (!$util.isInteger(message.extremeWinStreak))
                    return "extremeWinStreak: integer expected";
            if (message.extremeLastDecayHour != null && $Object.hasOwnProperty.call(message, "extremeLastDecayHour"))
                if (!$util.isInteger(message.extremeLastDecayHour) && !(message.extremeLastDecayHour && $util.isInteger(message.extremeLastDecayHour.low) && $util.isInteger(message.extremeLastDecayHour.high)))
                    return "extremeLastDecayHour: integer|Long expected";
            if (message.extremeForceClosed != null && $Object.hasOwnProperty.call(message, "extremeForceClosed"))
                if (typeof message.extremeForceClosed !== "boolean")
                    return "extremeForceClosed: boolean expected";
            if (message.extremeForceClosedAt != null && $Object.hasOwnProperty.call(message, "extremeForceClosedAt"))
                if (!$util.isInteger(message.extremeForceClosedAt) && !(message.extremeForceClosedAt && $util.isInteger(message.extremeForceClosedAt.low) && $util.isInteger(message.extremeForceClosedAt.high)))
                    return "extremeForceClosedAt: integer|Long expected";
            if (message.extremeRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "extremeRenameProtectedUntil"))
                if (!$util.isInteger(message.extremeRenameProtectedUntil) && !(message.extremeRenameProtectedUntil && $util.isInteger(message.extremeRenameProtectedUntil.low) && $util.isInteger(message.extremeRenameProtectedUntil.high)))
                    return "extremeRenameProtectedUntil: integer|Long expected";
            if (message.extremeRenamedBy != null && $Object.hasOwnProperty.call(message, "extremeRenamedBy"))
                if (!$util.isString(message.extremeRenamedBy))
                    return "extremeRenamedBy: string expected";
            if (message.extremeRenamedByName != null && $Object.hasOwnProperty.call(message, "extremeRenamedByName"))
                if (!$util.isString(message.extremeRenamedByName))
                    return "extremeRenamedByName: string expected";
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                if (!$util.isString(message.roomId))
                    return "roomId: string expected";
            if (message.isAdmin != null && $Object.hasOwnProperty.call(message, "isAdmin"))
                if (typeof message.isAdmin !== "boolean")
                    return "isAdmin: boolean expected";
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats")) {
                let error = $root.game.PublicStats.verify(message.stats, _depth + 1);
                if (error)
                    return "stats." + error;
            }
            if (message.gameStats != null && $Object.hasOwnProperty.call(message, "gameStats")) {
                let error = $root.game.GameStats.verify(message.gameStats, _depth + 1);
                if (error)
                    return "gameStats." + error;
            }
            return null;
        };

        /**
         * Creates a PublicPlayer message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PublicPlayer
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PublicPlayer} PublicPlayer
         */
        PublicPlayer.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PublicPlayer)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PublicPlayer: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PublicPlayer();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.genderId != null)
                if (typeof object.genderId !== "string" || object.genderId.length)
                    message.genderId = $String(object.genderId);
            if (object.genderLabel != null)
                if (typeof object.genderLabel !== "string" || object.genderLabel.length)
                    message.genderLabel = $String(object.genderLabel);
            if (object.factionId != null)
                if (typeof object.factionId !== "string" || object.factionId.length)
                    message.factionId = $String(object.factionId);
            if (object.avatarUrl != null)
                if (typeof object.avatarUrl !== "string" || object.avatarUrl.length)
                    message.avatarUrl = $String(object.avatarUrl);
            if (object.factionLabel != null)
                if (typeof object.factionLabel !== "string" || object.factionLabel.length)
                    message.factionLabel = $String(object.factionLabel);
            if (object.factionColors != null) {
                if (!$util.isObject(object.factionColors))
                    throw $TypeError(".game.PublicPlayer.factionColors: object expected");
                message.factionColors = $root.game.GenderColors.fromObject(object.factionColors, _depth + 1);
            }
            if (object.displayName != null)
                if (typeof object.displayName !== "string" || object.displayName.length)
                    message.displayName = $String(object.displayName);
            if (object.connected != null)
                if (object.connected)
                    message.connected = $Boolean(object.connected);
            if (object.disconnectedAt != null)
                if (typeof object.disconnectedAt === "object" ? object.disconnectedAt.low || object.disconnectedAt.high : $Number(object.disconnectedAt) !== 0)
                    if ($util.Long)
                        message.disconnectedAt = $util.Long.fromValue(object.disconnectedAt, false);
                    else if (typeof object.disconnectedAt === "string")
                        message.disconnectedAt = $parseInt(object.disconnectedAt, 10);
                    else if (typeof object.disconnectedAt === "number")
                        message.disconnectedAt = object.disconnectedAt;
                    else if (typeof object.disconnectedAt === "object")
                        message.disconnectedAt = new $util.LongBits(object.disconnectedAt.low >>> 0, object.disconnectedAt.high >>> 0).toNumber();
            if (object.disconnectExpiresAt != null)
                if (typeof object.disconnectExpiresAt === "object" ? object.disconnectExpiresAt.low || object.disconnectExpiresAt.high : $Number(object.disconnectExpiresAt) !== 0)
                    if ($util.Long)
                        message.disconnectExpiresAt = $util.Long.fromValue(object.disconnectExpiresAt, false);
                    else if (typeof object.disconnectExpiresAt === "string")
                        message.disconnectExpiresAt = $parseInt(object.disconnectExpiresAt, 10);
                    else if (typeof object.disconnectExpiresAt === "number")
                        message.disconnectExpiresAt = object.disconnectExpiresAt;
                    else if (typeof object.disconnectExpiresAt === "object")
                        message.disconnectExpiresAt = new $util.LongBits(object.disconnectExpiresAt.low >>> 0, object.disconnectExpiresAt.high >>> 0).toNumber();
            if (object.profileUpdatedAt != null)
                if (typeof object.profileUpdatedAt === "object" ? object.profileUpdatedAt.low || object.profileUpdatedAt.high : $Number(object.profileUpdatedAt) !== 0)
                    if ($util.Long)
                        message.profileUpdatedAt = $util.Long.fromValue(object.profileUpdatedAt, false);
                    else if (typeof object.profileUpdatedAt === "string")
                        message.profileUpdatedAt = $parseInt(object.profileUpdatedAt, 10);
                    else if (typeof object.profileUpdatedAt === "number")
                        message.profileUpdatedAt = object.profileUpdatedAt;
                    else if (typeof object.profileUpdatedAt === "object")
                        message.profileUpdatedAt = new $util.LongBits(object.profileUpdatedAt.low >>> 0, object.profileUpdatedAt.high >>> 0).toNumber();
            if (object.nameWarEnabled != null)
                if (object.nameWarEnabled)
                    message.nameWarEnabled = $Boolean(object.nameWarEnabled);
            if (object.nameWarToggledAt != null)
                if (typeof object.nameWarToggledAt === "object" ? object.nameWarToggledAt.low || object.nameWarToggledAt.high : $Number(object.nameWarToggledAt) !== 0)
                    if ($util.Long)
                        message.nameWarToggledAt = $util.Long.fromValue(object.nameWarToggledAt, false);
                    else if (typeof object.nameWarToggledAt === "string")
                        message.nameWarToggledAt = $parseInt(object.nameWarToggledAt, 10);
                    else if (typeof object.nameWarToggledAt === "number")
                        message.nameWarToggledAt = object.nameWarToggledAt;
                    else if (typeof object.nameWarToggledAt === "object")
                        message.nameWarToggledAt = new $util.LongBits(object.nameWarToggledAt.low >>> 0, object.nameWarToggledAt.high >>> 0).toNumber();
            if (object.nameWarOriginalName != null)
                if (typeof object.nameWarOriginalName !== "string" || object.nameWarOriginalName.length)
                    message.nameWarOriginalName = $String(object.nameWarOriginalName);
            if (object.nameWarPenaltyName != null)
                if (typeof object.nameWarPenaltyName !== "string" || object.nameWarPenaltyName.length)
                    message.nameWarPenaltyName = $String(object.nameWarPenaltyName);
            if (object.nameWarPunished != null)
                if (object.nameWarPunished)
                    message.nameWarPunished = $Boolean(object.nameWarPunished);
            if (object.nameWarAllowRename != null)
                if (object.nameWarAllowRename)
                    message.nameWarAllowRename = $Boolean(object.nameWarAllowRename);
            if (object.nameWarRenameProtectedUntil != null)
                if (typeof object.nameWarRenameProtectedUntil === "object" ? object.nameWarRenameProtectedUntil.low || object.nameWarRenameProtectedUntil.high : $Number(object.nameWarRenameProtectedUntil) !== 0)
                    if ($util.Long)
                        message.nameWarRenameProtectedUntil = $util.Long.fromValue(object.nameWarRenameProtectedUntil, false);
                    else if (typeof object.nameWarRenameProtectedUntil === "string")
                        message.nameWarRenameProtectedUntil = $parseInt(object.nameWarRenameProtectedUntil, 10);
                    else if (typeof object.nameWarRenameProtectedUntil === "number")
                        message.nameWarRenameProtectedUntil = object.nameWarRenameProtectedUntil;
                    else if (typeof object.nameWarRenameProtectedUntil === "object")
                        message.nameWarRenameProtectedUntil = new $util.LongBits(object.nameWarRenameProtectedUntil.low >>> 0, object.nameWarRenameProtectedUntil.high >>> 0).toNumber();
            if (object.nameWarRenamedBy != null)
                if (typeof object.nameWarRenamedBy !== "string" || object.nameWarRenamedBy.length)
                    message.nameWarRenamedBy = $String(object.nameWarRenamedBy);
            if (object.nameWarRenamedByName != null)
                if (typeof object.nameWarRenamedByName !== "string" || object.nameWarRenamedByName.length)
                    message.nameWarRenamedByName = $String(object.nameWarRenamedByName);
            if (object.nameWarRenameWindowStartedAt != null)
                if (typeof object.nameWarRenameWindowStartedAt === "object" ? object.nameWarRenameWindowStartedAt.low || object.nameWarRenameWindowStartedAt.high : $Number(object.nameWarRenameWindowStartedAt) !== 0)
                    if ($util.Long)
                        message.nameWarRenameWindowStartedAt = $util.Long.fromValue(object.nameWarRenameWindowStartedAt, false);
                    else if (typeof object.nameWarRenameWindowStartedAt === "string")
                        message.nameWarRenameWindowStartedAt = $parseInt(object.nameWarRenameWindowStartedAt, 10);
                    else if (typeof object.nameWarRenameWindowStartedAt === "number")
                        message.nameWarRenameWindowStartedAt = object.nameWarRenameWindowStartedAt;
                    else if (typeof object.nameWarRenameWindowStartedAt === "object")
                        message.nameWarRenameWindowStartedAt = new $util.LongBits(object.nameWarRenameWindowStartedAt.low >>> 0, object.nameWarRenameWindowStartedAt.high >>> 0).toNumber();
            if (object.nameWarRenameCount != null)
                if ($Number(object.nameWarRenameCount) !== 0)
                    message.nameWarRenameCount = object.nameWarRenameCount | 0;
            if (object.giveawayEnabled != null)
                if (object.giveawayEnabled)
                    message.giveawayEnabled = $Boolean(object.giveawayEnabled);
            if (object.giveawayValue != null)
                if (!$Object.is($Number(object.giveawayValue), 0))
                    message.giveawayValue = $Number(object.giveawayValue);
            if (object.giveawayClicks != null)
                if ($Number(object.giveawayClicks) !== 0)
                    message.giveawayClicks = object.giveawayClicks | 0;
            if (object.giveawayBoardText != null)
                if (typeof object.giveawayBoardText !== "string" || object.giveawayBoardText.length)
                    message.giveawayBoardText = $String(object.giveawayBoardText);
            if (object.giveawayBoardSubmittedAt != null)
                if (typeof object.giveawayBoardSubmittedAt === "object" ? object.giveawayBoardSubmittedAt.low || object.giveawayBoardSubmittedAt.high : $Number(object.giveawayBoardSubmittedAt) !== 0)
                    if ($util.Long)
                        message.giveawayBoardSubmittedAt = $util.Long.fromValue(object.giveawayBoardSubmittedAt, false);
                    else if (typeof object.giveawayBoardSubmittedAt === "string")
                        message.giveawayBoardSubmittedAt = $parseInt(object.giveawayBoardSubmittedAt, 10);
                    else if (typeof object.giveawayBoardSubmittedAt === "number")
                        message.giveawayBoardSubmittedAt = object.giveawayBoardSubmittedAt;
                    else if (typeof object.giveawayBoardSubmittedAt === "object")
                        message.giveawayBoardSubmittedAt = new $util.LongBits(object.giveawayBoardSubmittedAt.low >>> 0, object.giveawayBoardSubmittedAt.high >>> 0).toNumber();
            if (object.giveawayBoardExpiresAt != null)
                if (typeof object.giveawayBoardExpiresAt === "object" ? object.giveawayBoardExpiresAt.low || object.giveawayBoardExpiresAt.high : $Number(object.giveawayBoardExpiresAt) !== 0)
                    if ($util.Long)
                        message.giveawayBoardExpiresAt = $util.Long.fromValue(object.giveawayBoardExpiresAt, false);
                    else if (typeof object.giveawayBoardExpiresAt === "string")
                        message.giveawayBoardExpiresAt = $parseInt(object.giveawayBoardExpiresAt, 10);
                    else if (typeof object.giveawayBoardExpiresAt === "number")
                        message.giveawayBoardExpiresAt = object.giveawayBoardExpiresAt;
                    else if (typeof object.giveawayBoardExpiresAt === "object")
                        message.giveawayBoardExpiresAt = new $util.LongBits(object.giveawayBoardExpiresAt.low >>> 0, object.giveawayBoardExpiresAt.high >>> 0).toNumber();
            if (object.giveawayBoardLikes != null)
                if ($Number(object.giveawayBoardLikes) !== 0)
                    message.giveawayBoardLikes = object.giveawayBoardLikes | 0;
            if (object.giveawayBoardDislikes != null)
                if ($Number(object.giveawayBoardDislikes) !== 0)
                    message.giveawayBoardDislikes = object.giveawayBoardDislikes | 0;
            if (object.giveawayBoardLikesThisHour != null)
                if ($Number(object.giveawayBoardLikesThisHour) !== 0)
                    message.giveawayBoardLikesThisHour = object.giveawayBoardLikesThisHour | 0;
            if (object.giveawayBoardLikeWindowStartedAt != null)
                if (typeof object.giveawayBoardLikeWindowStartedAt === "object" ? object.giveawayBoardLikeWindowStartedAt.low || object.giveawayBoardLikeWindowStartedAt.high : $Number(object.giveawayBoardLikeWindowStartedAt) !== 0)
                    if ($util.Long)
                        message.giveawayBoardLikeWindowStartedAt = $util.Long.fromValue(object.giveawayBoardLikeWindowStartedAt, false);
                    else if (typeof object.giveawayBoardLikeWindowStartedAt === "string")
                        message.giveawayBoardLikeWindowStartedAt = $parseInt(object.giveawayBoardLikeWindowStartedAt, 10);
                    else if (typeof object.giveawayBoardLikeWindowStartedAt === "number")
                        message.giveawayBoardLikeWindowStartedAt = object.giveawayBoardLikeWindowStartedAt;
                    else if (typeof object.giveawayBoardLikeWindowStartedAt === "object")
                        message.giveawayBoardLikeWindowStartedAt = new $util.LongBits(object.giveawayBoardLikeWindowStartedAt.low >>> 0, object.giveawayBoardLikeWindowStartedAt.high >>> 0).toNumber();
            if (object.giveawayVoteWindowStartedAt != null)
                if (typeof object.giveawayVoteWindowStartedAt === "object" ? object.giveawayVoteWindowStartedAt.low || object.giveawayVoteWindowStartedAt.high : $Number(object.giveawayVoteWindowStartedAt) !== 0)
                    if ($util.Long)
                        message.giveawayVoteWindowStartedAt = $util.Long.fromValue(object.giveawayVoteWindowStartedAt, false);
                    else if (typeof object.giveawayVoteWindowStartedAt === "string")
                        message.giveawayVoteWindowStartedAt = $parseInt(object.giveawayVoteWindowStartedAt, 10);
                    else if (typeof object.giveawayVoteWindowStartedAt === "number")
                        message.giveawayVoteWindowStartedAt = object.giveawayVoteWindowStartedAt;
                    else if (typeof object.giveawayVoteWindowStartedAt === "object")
                        message.giveawayVoteWindowStartedAt = new $util.LongBits(object.giveawayVoteWindowStartedAt.low >>> 0, object.giveawayVoteWindowStartedAt.high >>> 0).toNumber();
            if (object.giveawayVoteCount != null)
                if ($Number(object.giveawayVoteCount) !== 0)
                    message.giveawayVoteCount = object.giveawayVoteCount | 0;
            if (object.giveawayVoteLikesThisHour != null)
                if ($Number(object.giveawayVoteLikesThisHour) !== 0)
                    message.giveawayVoteLikesThisHour = object.giveawayVoteLikesThisHour | 0;
            if (object.giveawayVoteDislikesThisHour != null)
                if ($Number(object.giveawayVoteDislikesThisHour) !== 0)
                    message.giveawayVoteDislikesThisHour = object.giveawayVoteDislikesThisHour | 0;
            if (object.rankMultiplierUnlocked != null)
                if (object.rankMultiplierUnlocked)
                    message.rankMultiplierUnlocked = $Boolean(object.rankMultiplierUnlocked);
            if (object.extremeModeEnabled != null)
                if (object.extremeModeEnabled)
                    message.extremeModeEnabled = $Boolean(object.extremeModeEnabled);
            if (object.extremeModeToggledAt != null)
                if (typeof object.extremeModeToggledAt === "object" ? object.extremeModeToggledAt.low || object.extremeModeToggledAt.high : $Number(object.extremeModeToggledAt) !== 0)
                    if ($util.Long)
                        message.extremeModeToggledAt = $util.Long.fromValue(object.extremeModeToggledAt, false);
                    else if (typeof object.extremeModeToggledAt === "string")
                        message.extremeModeToggledAt = $parseInt(object.extremeModeToggledAt, 10);
                    else if (typeof object.extremeModeToggledAt === "number")
                        message.extremeModeToggledAt = object.extremeModeToggledAt;
                    else if (typeof object.extremeModeToggledAt === "object")
                        message.extremeModeToggledAt = new $util.LongBits(object.extremeModeToggledAt.low >>> 0, object.extremeModeToggledAt.high >>> 0).toNumber();
            if (object.extremeModeCooldownUntil != null)
                if (typeof object.extremeModeCooldownUntil === "object" ? object.extremeModeCooldownUntil.low || object.extremeModeCooldownUntil.high : $Number(object.extremeModeCooldownUntil) !== 0)
                    if ($util.Long)
                        message.extremeModeCooldownUntil = $util.Long.fromValue(object.extremeModeCooldownUntil, false);
                    else if (typeof object.extremeModeCooldownUntil === "string")
                        message.extremeModeCooldownUntil = $parseInt(object.extremeModeCooldownUntil, 10);
                    else if (typeof object.extremeModeCooldownUntil === "number")
                        message.extremeModeCooldownUntil = object.extremeModeCooldownUntil;
                    else if (typeof object.extremeModeCooldownUntil === "object")
                        message.extremeModeCooldownUntil = new $util.LongBits(object.extremeModeCooldownUntil.low >>> 0, object.extremeModeCooldownUntil.high >>> 0).toNumber();
            if (object.extremeWinStreak != null)
                if ($Number(object.extremeWinStreak) !== 0)
                    message.extremeWinStreak = object.extremeWinStreak | 0;
            if (object.extremeLastDecayHour != null)
                if (typeof object.extremeLastDecayHour === "object" ? object.extremeLastDecayHour.low || object.extremeLastDecayHour.high : $Number(object.extremeLastDecayHour) !== 0)
                    if ($util.Long)
                        message.extremeLastDecayHour = $util.Long.fromValue(object.extremeLastDecayHour, false);
                    else if (typeof object.extremeLastDecayHour === "string")
                        message.extremeLastDecayHour = $parseInt(object.extremeLastDecayHour, 10);
                    else if (typeof object.extremeLastDecayHour === "number")
                        message.extremeLastDecayHour = object.extremeLastDecayHour;
                    else if (typeof object.extremeLastDecayHour === "object")
                        message.extremeLastDecayHour = new $util.LongBits(object.extremeLastDecayHour.low >>> 0, object.extremeLastDecayHour.high >>> 0).toNumber();
            if (object.extremeForceClosed != null)
                if (object.extremeForceClosed)
                    message.extremeForceClosed = $Boolean(object.extremeForceClosed);
            if (object.extremeForceClosedAt != null)
                if (typeof object.extremeForceClosedAt === "object" ? object.extremeForceClosedAt.low || object.extremeForceClosedAt.high : $Number(object.extremeForceClosedAt) !== 0)
                    if ($util.Long)
                        message.extremeForceClosedAt = $util.Long.fromValue(object.extremeForceClosedAt, false);
                    else if (typeof object.extremeForceClosedAt === "string")
                        message.extremeForceClosedAt = $parseInt(object.extremeForceClosedAt, 10);
                    else if (typeof object.extremeForceClosedAt === "number")
                        message.extremeForceClosedAt = object.extremeForceClosedAt;
                    else if (typeof object.extremeForceClosedAt === "object")
                        message.extremeForceClosedAt = new $util.LongBits(object.extremeForceClosedAt.low >>> 0, object.extremeForceClosedAt.high >>> 0).toNumber();
            if (object.extremeRenameProtectedUntil != null)
                if (typeof object.extremeRenameProtectedUntil === "object" ? object.extremeRenameProtectedUntil.low || object.extremeRenameProtectedUntil.high : $Number(object.extremeRenameProtectedUntil) !== 0)
                    if ($util.Long)
                        message.extremeRenameProtectedUntil = $util.Long.fromValue(object.extremeRenameProtectedUntil, false);
                    else if (typeof object.extremeRenameProtectedUntil === "string")
                        message.extremeRenameProtectedUntil = $parseInt(object.extremeRenameProtectedUntil, 10);
                    else if (typeof object.extremeRenameProtectedUntil === "number")
                        message.extremeRenameProtectedUntil = object.extremeRenameProtectedUntil;
                    else if (typeof object.extremeRenameProtectedUntil === "object")
                        message.extremeRenameProtectedUntil = new $util.LongBits(object.extremeRenameProtectedUntil.low >>> 0, object.extremeRenameProtectedUntil.high >>> 0).toNumber();
            if (object.extremeRenamedBy != null)
                if (typeof object.extremeRenamedBy !== "string" || object.extremeRenamedBy.length)
                    message.extremeRenamedBy = $String(object.extremeRenamedBy);
            if (object.extremeRenamedByName != null)
                if (typeof object.extremeRenamedByName !== "string" || object.extremeRenamedByName.length)
                    message.extremeRenamedByName = $String(object.extremeRenamedByName);
            if (object.roomId != null)
                if (typeof object.roomId !== "string" || object.roomId.length)
                    message.roomId = $String(object.roomId);
            if (object.isAdmin != null)
                if (object.isAdmin)
                    message.isAdmin = $Boolean(object.isAdmin);
            if (object.stats != null) {
                if (!$util.isObject(object.stats))
                    throw $TypeError(".game.PublicPlayer.stats: object expected");
                message.stats = $root.game.PublicStats.fromObject(object.stats, _depth + 1);
            }
            if (object.gameStats != null) {
                if (!$util.isObject(object.gameStats))
                    throw $TypeError(".game.PublicPlayer.gameStats: object expected");
                message.gameStats = $root.game.GameStats.fromObject(object.gameStats, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PublicPlayer message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PublicPlayer
         * @static
         * @param {game.PublicPlayer} message PublicPlayer
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PublicPlayer.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.name = "";
                object.genderId = "";
                object.genderLabel = "";
                object.factionId = "";
                object.factionLabel = "";
                object.factionColors = null;
                object.displayName = "";
                object.connected = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.disconnectedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.disconnectedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.disconnectExpiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.disconnectExpiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.profileUpdatedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.profileUpdatedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nameWarEnabled = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nameWarToggledAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nameWarToggledAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nameWarOriginalName = "";
                object.nameWarPenaltyName = "";
                object.nameWarPunished = false;
                object.nameWarAllowRename = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nameWarRenameProtectedUntil = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nameWarRenameProtectedUntil = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nameWarRenamedBy = "";
                object.nameWarRenamedByName = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nameWarRenameWindowStartedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nameWarRenameWindowStartedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nameWarRenameCount = 0;
                object.giveawayEnabled = false;
                object.giveawayValue = 0;
                object.giveawayClicks = 0;
                object.giveawayBoardText = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.giveawayBoardSubmittedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.giveawayBoardSubmittedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.giveawayBoardExpiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.giveawayBoardExpiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.giveawayBoardLikes = 0;
                object.giveawayBoardDislikes = 0;
                object.giveawayBoardLikesThisHour = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.giveawayBoardLikeWindowStartedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.giveawayBoardLikeWindowStartedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.giveawayVoteWindowStartedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.giveawayVoteWindowStartedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.giveawayVoteCount = 0;
                object.giveawayVoteLikesThisHour = 0;
                object.giveawayVoteDislikesThisHour = 0;
                object.rankMultiplierUnlocked = false;
                object.extremeModeEnabled = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.extremeModeToggledAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.extremeModeToggledAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.extremeModeCooldownUntil = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.extremeModeCooldownUntil = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.extremeWinStreak = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.extremeLastDecayHour = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.extremeLastDecayHour = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.extremeForceClosed = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.extremeForceClosedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.extremeForceClosedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.extremeRenameProtectedUntil = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.extremeRenameProtectedUntil = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.extremeRenamedBy = "";
                object.extremeRenamedByName = "";
                object.roomId = "";
                object.isAdmin = false;
                object.stats = null;
                object.gameStats = null;
                object.avatarUrl = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.genderId != null && $Object.hasOwnProperty.call(message, "genderId"))
                object.genderId = message.genderId;
            if (message.genderLabel != null && $Object.hasOwnProperty.call(message, "genderLabel"))
                object.genderLabel = message.genderLabel;
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                object.factionId = message.factionId;
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel"))
                object.factionLabel = message.factionLabel;
            if (message.factionColors != null && $Object.hasOwnProperty.call(message, "factionColors"))
                object.factionColors = $root.game.GenderColors.toObject(message.factionColors, options, _depth + 1);
            if (message.displayName != null && $Object.hasOwnProperty.call(message, "displayName"))
                object.displayName = message.displayName;
            if (message.connected != null && $Object.hasOwnProperty.call(message, "connected"))
                object.connected = message.connected;
            if (message.disconnectedAt != null && $Object.hasOwnProperty.call(message, "disconnectedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.disconnectedAt = typeof message.disconnectedAt === "number" ? $BigInt(message.disconnectedAt) : $util.Long.fromBits(message.disconnectedAt.low >>> 0, message.disconnectedAt.high >>> 0, false).toBigInt();
                else if (typeof message.disconnectedAt === "number")
                    object.disconnectedAt = options.longs === $String ? $String(message.disconnectedAt) : message.disconnectedAt;
                else
                    object.disconnectedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.disconnectedAt) : options.longs === $Number ? new $util.LongBits(message.disconnectedAt.low >>> 0, message.disconnectedAt.high >>> 0).toNumber() : message.disconnectedAt;
            if (message.disconnectExpiresAt != null && $Object.hasOwnProperty.call(message, "disconnectExpiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.disconnectExpiresAt = typeof message.disconnectExpiresAt === "number" ? $BigInt(message.disconnectExpiresAt) : $util.Long.fromBits(message.disconnectExpiresAt.low >>> 0, message.disconnectExpiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.disconnectExpiresAt === "number")
                    object.disconnectExpiresAt = options.longs === $String ? $String(message.disconnectExpiresAt) : message.disconnectExpiresAt;
                else
                    object.disconnectExpiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.disconnectExpiresAt) : options.longs === $Number ? new $util.LongBits(message.disconnectExpiresAt.low >>> 0, message.disconnectExpiresAt.high >>> 0).toNumber() : message.disconnectExpiresAt;
            if (message.profileUpdatedAt != null && $Object.hasOwnProperty.call(message, "profileUpdatedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.profileUpdatedAt = typeof message.profileUpdatedAt === "number" ? $BigInt(message.profileUpdatedAt) : $util.Long.fromBits(message.profileUpdatedAt.low >>> 0, message.profileUpdatedAt.high >>> 0, false).toBigInt();
                else if (typeof message.profileUpdatedAt === "number")
                    object.profileUpdatedAt = options.longs === $String ? $String(message.profileUpdatedAt) : message.profileUpdatedAt;
                else
                    object.profileUpdatedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.profileUpdatedAt) : options.longs === $Number ? new $util.LongBits(message.profileUpdatedAt.low >>> 0, message.profileUpdatedAt.high >>> 0).toNumber() : message.profileUpdatedAt;
            if (message.nameWarEnabled != null && $Object.hasOwnProperty.call(message, "nameWarEnabled"))
                object.nameWarEnabled = message.nameWarEnabled;
            if (message.nameWarToggledAt != null && $Object.hasOwnProperty.call(message, "nameWarToggledAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nameWarToggledAt = typeof message.nameWarToggledAt === "number" ? $BigInt(message.nameWarToggledAt) : $util.Long.fromBits(message.nameWarToggledAt.low >>> 0, message.nameWarToggledAt.high >>> 0, false).toBigInt();
                else if (typeof message.nameWarToggledAt === "number")
                    object.nameWarToggledAt = options.longs === $String ? $String(message.nameWarToggledAt) : message.nameWarToggledAt;
                else
                    object.nameWarToggledAt = options.longs === $String ? $util.Long.prototype.toString.call(message.nameWarToggledAt) : options.longs === $Number ? new $util.LongBits(message.nameWarToggledAt.low >>> 0, message.nameWarToggledAt.high >>> 0).toNumber() : message.nameWarToggledAt;
            if (message.nameWarOriginalName != null && $Object.hasOwnProperty.call(message, "nameWarOriginalName"))
                object.nameWarOriginalName = message.nameWarOriginalName;
            if (message.nameWarPenaltyName != null && $Object.hasOwnProperty.call(message, "nameWarPenaltyName"))
                object.nameWarPenaltyName = message.nameWarPenaltyName;
            if (message.nameWarPunished != null && $Object.hasOwnProperty.call(message, "nameWarPunished"))
                object.nameWarPunished = message.nameWarPunished;
            if (message.nameWarAllowRename != null && $Object.hasOwnProperty.call(message, "nameWarAllowRename"))
                object.nameWarAllowRename = message.nameWarAllowRename;
            if (message.nameWarRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "nameWarRenameProtectedUntil"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nameWarRenameProtectedUntil = typeof message.nameWarRenameProtectedUntil === "number" ? $BigInt(message.nameWarRenameProtectedUntil) : $util.Long.fromBits(message.nameWarRenameProtectedUntil.low >>> 0, message.nameWarRenameProtectedUntil.high >>> 0, false).toBigInt();
                else if (typeof message.nameWarRenameProtectedUntil === "number")
                    object.nameWarRenameProtectedUntil = options.longs === $String ? $String(message.nameWarRenameProtectedUntil) : message.nameWarRenameProtectedUntil;
                else
                    object.nameWarRenameProtectedUntil = options.longs === $String ? $util.Long.prototype.toString.call(message.nameWarRenameProtectedUntil) : options.longs === $Number ? new $util.LongBits(message.nameWarRenameProtectedUntil.low >>> 0, message.nameWarRenameProtectedUntil.high >>> 0).toNumber() : message.nameWarRenameProtectedUntil;
            if (message.nameWarRenamedBy != null && $Object.hasOwnProperty.call(message, "nameWarRenamedBy"))
                object.nameWarRenamedBy = message.nameWarRenamedBy;
            if (message.nameWarRenamedByName != null && $Object.hasOwnProperty.call(message, "nameWarRenamedByName"))
                object.nameWarRenamedByName = message.nameWarRenamedByName;
            if (message.nameWarRenameWindowStartedAt != null && $Object.hasOwnProperty.call(message, "nameWarRenameWindowStartedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nameWarRenameWindowStartedAt = typeof message.nameWarRenameWindowStartedAt === "number" ? $BigInt(message.nameWarRenameWindowStartedAt) : $util.Long.fromBits(message.nameWarRenameWindowStartedAt.low >>> 0, message.nameWarRenameWindowStartedAt.high >>> 0, false).toBigInt();
                else if (typeof message.nameWarRenameWindowStartedAt === "number")
                    object.nameWarRenameWindowStartedAt = options.longs === $String ? $String(message.nameWarRenameWindowStartedAt) : message.nameWarRenameWindowStartedAt;
                else
                    object.nameWarRenameWindowStartedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.nameWarRenameWindowStartedAt) : options.longs === $Number ? new $util.LongBits(message.nameWarRenameWindowStartedAt.low >>> 0, message.nameWarRenameWindowStartedAt.high >>> 0).toNumber() : message.nameWarRenameWindowStartedAt;
            if (message.nameWarRenameCount != null && $Object.hasOwnProperty.call(message, "nameWarRenameCount"))
                object.nameWarRenameCount = message.nameWarRenameCount;
            if (message.giveawayEnabled != null && $Object.hasOwnProperty.call(message, "giveawayEnabled"))
                object.giveawayEnabled = message.giveawayEnabled;
            if (message.giveawayValue != null && $Object.hasOwnProperty.call(message, "giveawayValue"))
                object.giveawayValue = options.json && !$isFinite(message.giveawayValue) ? $String(message.giveawayValue) : message.giveawayValue;
            if (message.giveawayClicks != null && $Object.hasOwnProperty.call(message, "giveawayClicks"))
                object.giveawayClicks = message.giveawayClicks;
            if (message.giveawayBoardText != null && $Object.hasOwnProperty.call(message, "giveawayBoardText"))
                object.giveawayBoardText = message.giveawayBoardText;
            if (message.giveawayBoardSubmittedAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardSubmittedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.giveawayBoardSubmittedAt = typeof message.giveawayBoardSubmittedAt === "number" ? $BigInt(message.giveawayBoardSubmittedAt) : $util.Long.fromBits(message.giveawayBoardSubmittedAt.low >>> 0, message.giveawayBoardSubmittedAt.high >>> 0, false).toBigInt();
                else if (typeof message.giveawayBoardSubmittedAt === "number")
                    object.giveawayBoardSubmittedAt = options.longs === $String ? $String(message.giveawayBoardSubmittedAt) : message.giveawayBoardSubmittedAt;
                else
                    object.giveawayBoardSubmittedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.giveawayBoardSubmittedAt) : options.longs === $Number ? new $util.LongBits(message.giveawayBoardSubmittedAt.low >>> 0, message.giveawayBoardSubmittedAt.high >>> 0).toNumber() : message.giveawayBoardSubmittedAt;
            if (message.giveawayBoardExpiresAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardExpiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.giveawayBoardExpiresAt = typeof message.giveawayBoardExpiresAt === "number" ? $BigInt(message.giveawayBoardExpiresAt) : $util.Long.fromBits(message.giveawayBoardExpiresAt.low >>> 0, message.giveawayBoardExpiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.giveawayBoardExpiresAt === "number")
                    object.giveawayBoardExpiresAt = options.longs === $String ? $String(message.giveawayBoardExpiresAt) : message.giveawayBoardExpiresAt;
                else
                    object.giveawayBoardExpiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.giveawayBoardExpiresAt) : options.longs === $Number ? new $util.LongBits(message.giveawayBoardExpiresAt.low >>> 0, message.giveawayBoardExpiresAt.high >>> 0).toNumber() : message.giveawayBoardExpiresAt;
            if (message.giveawayBoardLikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikes"))
                object.giveawayBoardLikes = message.giveawayBoardLikes;
            if (message.giveawayBoardDislikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardDislikes"))
                object.giveawayBoardDislikes = message.giveawayBoardDislikes;
            if (message.giveawayBoardLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikesThisHour"))
                object.giveawayBoardLikesThisHour = message.giveawayBoardLikesThisHour;
            if (message.giveawayBoardLikeWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikeWindowStartedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.giveawayBoardLikeWindowStartedAt = typeof message.giveawayBoardLikeWindowStartedAt === "number" ? $BigInt(message.giveawayBoardLikeWindowStartedAt) : $util.Long.fromBits(message.giveawayBoardLikeWindowStartedAt.low >>> 0, message.giveawayBoardLikeWindowStartedAt.high >>> 0, false).toBigInt();
                else if (typeof message.giveawayBoardLikeWindowStartedAt === "number")
                    object.giveawayBoardLikeWindowStartedAt = options.longs === $String ? $String(message.giveawayBoardLikeWindowStartedAt) : message.giveawayBoardLikeWindowStartedAt;
                else
                    object.giveawayBoardLikeWindowStartedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.giveawayBoardLikeWindowStartedAt) : options.longs === $Number ? new $util.LongBits(message.giveawayBoardLikeWindowStartedAt.low >>> 0, message.giveawayBoardLikeWindowStartedAt.high >>> 0).toNumber() : message.giveawayBoardLikeWindowStartedAt;
            if (message.giveawayVoteWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayVoteWindowStartedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.giveawayVoteWindowStartedAt = typeof message.giveawayVoteWindowStartedAt === "number" ? $BigInt(message.giveawayVoteWindowStartedAt) : $util.Long.fromBits(message.giveawayVoteWindowStartedAt.low >>> 0, message.giveawayVoteWindowStartedAt.high >>> 0, false).toBigInt();
                else if (typeof message.giveawayVoteWindowStartedAt === "number")
                    object.giveawayVoteWindowStartedAt = options.longs === $String ? $String(message.giveawayVoteWindowStartedAt) : message.giveawayVoteWindowStartedAt;
                else
                    object.giveawayVoteWindowStartedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.giveawayVoteWindowStartedAt) : options.longs === $Number ? new $util.LongBits(message.giveawayVoteWindowStartedAt.low >>> 0, message.giveawayVoteWindowStartedAt.high >>> 0).toNumber() : message.giveawayVoteWindowStartedAt;
            if (message.giveawayVoteCount != null && $Object.hasOwnProperty.call(message, "giveawayVoteCount"))
                object.giveawayVoteCount = message.giveawayVoteCount;
            if (message.giveawayVoteLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteLikesThisHour"))
                object.giveawayVoteLikesThisHour = message.giveawayVoteLikesThisHour;
            if (message.giveawayVoteDislikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteDislikesThisHour"))
                object.giveawayVoteDislikesThisHour = message.giveawayVoteDislikesThisHour;
            if (message.rankMultiplierUnlocked != null && $Object.hasOwnProperty.call(message, "rankMultiplierUnlocked"))
                object.rankMultiplierUnlocked = message.rankMultiplierUnlocked;
            if (message.extremeModeEnabled != null && $Object.hasOwnProperty.call(message, "extremeModeEnabled"))
                object.extremeModeEnabled = message.extremeModeEnabled;
            if (message.extremeModeToggledAt != null && $Object.hasOwnProperty.call(message, "extremeModeToggledAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.extremeModeToggledAt = typeof message.extremeModeToggledAt === "number" ? $BigInt(message.extremeModeToggledAt) : $util.Long.fromBits(message.extremeModeToggledAt.low >>> 0, message.extremeModeToggledAt.high >>> 0, false).toBigInt();
                else if (typeof message.extremeModeToggledAt === "number")
                    object.extremeModeToggledAt = options.longs === $String ? $String(message.extremeModeToggledAt) : message.extremeModeToggledAt;
                else
                    object.extremeModeToggledAt = options.longs === $String ? $util.Long.prototype.toString.call(message.extremeModeToggledAt) : options.longs === $Number ? new $util.LongBits(message.extremeModeToggledAt.low >>> 0, message.extremeModeToggledAt.high >>> 0).toNumber() : message.extremeModeToggledAt;
            if (message.extremeModeCooldownUntil != null && $Object.hasOwnProperty.call(message, "extremeModeCooldownUntil"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.extremeModeCooldownUntil = typeof message.extremeModeCooldownUntil === "number" ? $BigInt(message.extremeModeCooldownUntil) : $util.Long.fromBits(message.extremeModeCooldownUntil.low >>> 0, message.extremeModeCooldownUntil.high >>> 0, false).toBigInt();
                else if (typeof message.extremeModeCooldownUntil === "number")
                    object.extremeModeCooldownUntil = options.longs === $String ? $String(message.extremeModeCooldownUntil) : message.extremeModeCooldownUntil;
                else
                    object.extremeModeCooldownUntil = options.longs === $String ? $util.Long.prototype.toString.call(message.extremeModeCooldownUntil) : options.longs === $Number ? new $util.LongBits(message.extremeModeCooldownUntil.low >>> 0, message.extremeModeCooldownUntil.high >>> 0).toNumber() : message.extremeModeCooldownUntil;
            if (message.extremeWinStreak != null && $Object.hasOwnProperty.call(message, "extremeWinStreak"))
                object.extremeWinStreak = message.extremeWinStreak;
            if (message.extremeLastDecayHour != null && $Object.hasOwnProperty.call(message, "extremeLastDecayHour"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.extremeLastDecayHour = typeof message.extremeLastDecayHour === "number" ? $BigInt(message.extremeLastDecayHour) : $util.Long.fromBits(message.extremeLastDecayHour.low >>> 0, message.extremeLastDecayHour.high >>> 0, false).toBigInt();
                else if (typeof message.extremeLastDecayHour === "number")
                    object.extremeLastDecayHour = options.longs === $String ? $String(message.extremeLastDecayHour) : message.extremeLastDecayHour;
                else
                    object.extremeLastDecayHour = options.longs === $String ? $util.Long.prototype.toString.call(message.extremeLastDecayHour) : options.longs === $Number ? new $util.LongBits(message.extremeLastDecayHour.low >>> 0, message.extremeLastDecayHour.high >>> 0).toNumber() : message.extremeLastDecayHour;
            if (message.extremeForceClosed != null && $Object.hasOwnProperty.call(message, "extremeForceClosed"))
                object.extremeForceClosed = message.extremeForceClosed;
            if (message.extremeForceClosedAt != null && $Object.hasOwnProperty.call(message, "extremeForceClosedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.extremeForceClosedAt = typeof message.extremeForceClosedAt === "number" ? $BigInt(message.extremeForceClosedAt) : $util.Long.fromBits(message.extremeForceClosedAt.low >>> 0, message.extremeForceClosedAt.high >>> 0, false).toBigInt();
                else if (typeof message.extremeForceClosedAt === "number")
                    object.extremeForceClosedAt = options.longs === $String ? $String(message.extremeForceClosedAt) : message.extremeForceClosedAt;
                else
                    object.extremeForceClosedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.extremeForceClosedAt) : options.longs === $Number ? new $util.LongBits(message.extremeForceClosedAt.low >>> 0, message.extremeForceClosedAt.high >>> 0).toNumber() : message.extremeForceClosedAt;
            if (message.extremeRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "extremeRenameProtectedUntil"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.extremeRenameProtectedUntil = typeof message.extremeRenameProtectedUntil === "number" ? $BigInt(message.extremeRenameProtectedUntil) : $util.Long.fromBits(message.extremeRenameProtectedUntil.low >>> 0, message.extremeRenameProtectedUntil.high >>> 0, false).toBigInt();
                else if (typeof message.extremeRenameProtectedUntil === "number")
                    object.extremeRenameProtectedUntil = options.longs === $String ? $String(message.extremeRenameProtectedUntil) : message.extremeRenameProtectedUntil;
                else
                    object.extremeRenameProtectedUntil = options.longs === $String ? $util.Long.prototype.toString.call(message.extremeRenameProtectedUntil) : options.longs === $Number ? new $util.LongBits(message.extremeRenameProtectedUntil.low >>> 0, message.extremeRenameProtectedUntil.high >>> 0).toNumber() : message.extremeRenameProtectedUntil;
            if (message.extremeRenamedBy != null && $Object.hasOwnProperty.call(message, "extremeRenamedBy"))
                object.extremeRenamedBy = message.extremeRenamedBy;
            if (message.extremeRenamedByName != null && $Object.hasOwnProperty.call(message, "extremeRenamedByName"))
                object.extremeRenamedByName = message.extremeRenamedByName;
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                object.roomId = message.roomId;
            if (message.isAdmin != null && $Object.hasOwnProperty.call(message, "isAdmin"))
                object.isAdmin = message.isAdmin;
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats"))
                object.stats = $root.game.PublicStats.toObject(message.stats, options, _depth + 1);
            if (message.gameStats != null && $Object.hasOwnProperty.call(message, "gameStats"))
                object.gameStats = $root.game.GameStats.toObject(message.gameStats, options, _depth + 1);
            if (message.avatarUrl != null && $Object.hasOwnProperty.call(message, "avatarUrl"))
                object.avatarUrl = message.avatarUrl;
            return object;
        };

        /**
         * Converts this PublicPlayer to JSON.
         * @function toJSON
         * @memberof game.PublicPlayer
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PublicPlayer.prototype.toJSON = function() {
            return PublicPlayer.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PublicPlayer
         * @function getTypeUrl
         * @memberof game.PublicPlayer
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PublicPlayer.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PublicPlayer";
        };

        return PublicPlayer;
    })();

    game.LobbyPlayer = (function() {

        /**
         * Properties of a LobbyPlayer.
         * @typedef {Object} game.LobbyPlayer.$Properties
         * @property {string|null} [id] LobbyPlayer id
         * @property {string|null} [name] LobbyPlayer name
         * @property {string|null} [genderId] LobbyPlayer genderId
         * @property {string|null} [genderLabel] LobbyPlayer genderLabel
         * @property {string|null} [factionId] LobbyPlayer factionId
         * @property {string|null} [factionLabel] LobbyPlayer factionLabel
         * @property {game.GenderColors.$Properties|null} [factionColors] LobbyPlayer factionColors
         * @property {string|null} [displayName] LobbyPlayer displayName
         * @property {boolean|null} [connected] LobbyPlayer connected
         * @property {number|Long|null} [disconnectedAt] LobbyPlayer disconnectedAt
         * @property {number|Long|null} [disconnectExpiresAt] LobbyPlayer disconnectExpiresAt
         * @property {boolean|null} [nameWarEnabled] LobbyPlayer nameWarEnabled
         * @property {string|null} [nameWarPenaltyName] LobbyPlayer nameWarPenaltyName
         * @property {boolean|null} [nameWarPunished] LobbyPlayer nameWarPunished
         * @property {boolean|null} [nameWarAllowRename] LobbyPlayer nameWarAllowRename
         * @property {number|Long|null} [nameWarRenameProtectedUntil] LobbyPlayer nameWarRenameProtectedUntil
         * @property {string|null} [nameWarRenamedByName] LobbyPlayer nameWarRenamedByName
         * @property {boolean|null} [giveawayEnabled] LobbyPlayer giveawayEnabled
         * @property {number|null} [giveawayValue] LobbyPlayer giveawayValue
         * @property {string|null} [giveawayBoardText] LobbyPlayer giveawayBoardText
         * @property {number|Long|null} [giveawayBoardExpiresAt] LobbyPlayer giveawayBoardExpiresAt
         * @property {number|null} [giveawayBoardLikes] LobbyPlayer giveawayBoardLikes
         * @property {number|null} [giveawayBoardDislikes] LobbyPlayer giveawayBoardDislikes
         * @property {number|Long|null} [giveawayVoteWindowStartedAt] LobbyPlayer giveawayVoteWindowStartedAt
         * @property {number|null} [giveawayVoteLikesThisHour] LobbyPlayer giveawayVoteLikesThisHour
         * @property {number|null} [giveawayVoteDislikesThisHour] LobbyPlayer giveawayVoteDislikesThisHour
         * @property {boolean|null} [rankMultiplierUnlocked] LobbyPlayer rankMultiplierUnlocked
         * @property {boolean|null} [extremeModeEnabled] LobbyPlayer extremeModeEnabled
         * @property {number|null} [extremeWinStreak] LobbyPlayer extremeWinStreak
         * @property {boolean|null} [extremeForceClosed] LobbyPlayer extremeForceClosed
         * @property {number|Long|null} [extremeForceClosedAt] LobbyPlayer extremeForceClosedAt
         * @property {number|Long|null} [extremeRenameProtectedUntil] LobbyPlayer extremeRenameProtectedUntil
         * @property {string|null} [extremeRenamedByName] LobbyPlayer extremeRenamedByName
         * @property {string|null} [roomId] LobbyPlayer roomId
         * @property {game.LobbyStats.$Properties|null} [stats] LobbyPlayer stats
         * @property {game.GameStats.$Properties|null} [gameStats] LobbyPlayer gameStats
         * @property {string|null} [avatarUrl] LobbyPlayer avatarUrl
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LobbyPlayer.
         * @memberof game
         * @interface ILobbyPlayer
         * @augments game.LobbyPlayer.$Properties
         * @deprecated Use game.LobbyPlayer.$Properties instead.
         */

        /**
         * Shape of a LobbyPlayer.
         * @typedef {game.LobbyPlayer.$Properties} game.LobbyPlayer.$Shape
         */

        /**
         * Constructs a new LobbyPlayer.
         * @memberof game
         * @classdesc Represents a LobbyPlayer.
         * @constructor
         * @param {game.LobbyPlayer.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LobbyPlayer = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LobbyPlayer id.
         * @member {string} id
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.id = "";

        /**
         * LobbyPlayer name.
         * @member {string} name
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.name = "";

        /**
         * LobbyPlayer genderId.
         * @member {string} genderId
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.genderId = "";

        /**
         * LobbyPlayer genderLabel.
         * @member {string} genderLabel
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.genderLabel = "";

        /**
         * LobbyPlayer factionId.
         * @member {string} factionId
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.factionId = "";

        /**
         * LobbyPlayer factionLabel.
         * @member {string} factionLabel
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.factionLabel = "";

        /**
         * LobbyPlayer factionColors.
         * @member {game.GenderColors.$Properties|null|undefined} factionColors
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.factionColors = null;

        /**
         * LobbyPlayer displayName.
         * @member {string} displayName
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.displayName = "";

        /**
         * LobbyPlayer connected.
         * @member {boolean} connected
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.connected = false;

        /**
         * LobbyPlayer disconnectedAt.
         * @member {number|Long} disconnectedAt
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.disconnectedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LobbyPlayer disconnectExpiresAt.
         * @member {number|Long} disconnectExpiresAt
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.disconnectExpiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LobbyPlayer nameWarEnabled.
         * @member {boolean} nameWarEnabled
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.nameWarEnabled = false;

        /**
         * LobbyPlayer nameWarPenaltyName.
         * @member {string} nameWarPenaltyName
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.nameWarPenaltyName = "";

        /**
         * LobbyPlayer nameWarPunished.
         * @member {boolean} nameWarPunished
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.nameWarPunished = false;

        /**
         * LobbyPlayer nameWarAllowRename.
         * @member {boolean} nameWarAllowRename
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.nameWarAllowRename = false;

        /**
         * LobbyPlayer nameWarRenameProtectedUntil.
         * @member {number|Long} nameWarRenameProtectedUntil
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.nameWarRenameProtectedUntil = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LobbyPlayer nameWarRenamedByName.
         * @member {string} nameWarRenamedByName
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.nameWarRenamedByName = "";

        /**
         * LobbyPlayer giveawayEnabled.
         * @member {boolean} giveawayEnabled
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayEnabled = false;

        /**
         * LobbyPlayer giveawayValue.
         * @member {number} giveawayValue
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayValue = 0;

        /**
         * LobbyPlayer giveawayBoardText.
         * @member {string} giveawayBoardText
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayBoardText = "";

        /**
         * LobbyPlayer giveawayBoardExpiresAt.
         * @member {number|Long} giveawayBoardExpiresAt
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayBoardExpiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LobbyPlayer giveawayBoardLikes.
         * @member {number} giveawayBoardLikes
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayBoardLikes = 0;

        /**
         * LobbyPlayer giveawayBoardDislikes.
         * @member {number} giveawayBoardDislikes
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayBoardDislikes = 0;

        /**
         * LobbyPlayer giveawayVoteWindowStartedAt.
         * @member {number|Long} giveawayVoteWindowStartedAt
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayVoteWindowStartedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LobbyPlayer giveawayVoteLikesThisHour.
         * @member {number} giveawayVoteLikesThisHour
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayVoteLikesThisHour = 0;

        /**
         * LobbyPlayer giveawayVoteDislikesThisHour.
         * @member {number} giveawayVoteDislikesThisHour
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.giveawayVoteDislikesThisHour = 0;

        /**
         * LobbyPlayer rankMultiplierUnlocked.
         * @member {boolean} rankMultiplierUnlocked
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.rankMultiplierUnlocked = false;

        /**
         * LobbyPlayer extremeModeEnabled.
         * @member {boolean} extremeModeEnabled
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.extremeModeEnabled = false;

        /**
         * LobbyPlayer extremeWinStreak.
         * @member {number} extremeWinStreak
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.extremeWinStreak = 0;

        /**
         * LobbyPlayer extremeForceClosed.
         * @member {boolean} extremeForceClosed
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.extremeForceClosed = false;

        /**
         * LobbyPlayer extremeForceClosedAt.
         * @member {number|Long} extremeForceClosedAt
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.extremeForceClosedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LobbyPlayer extremeRenameProtectedUntil.
         * @member {number|Long} extremeRenameProtectedUntil
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.extremeRenameProtectedUntil = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LobbyPlayer extremeRenamedByName.
         * @member {string} extremeRenamedByName
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.extremeRenamedByName = "";

        /**
         * LobbyPlayer roomId.
         * @member {string} roomId
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.roomId = "";

        /**
         * LobbyPlayer stats.
         * @member {game.LobbyStats.$Properties|null|undefined} stats
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.stats = null;

        /**
         * LobbyPlayer gameStats.
         * @member {game.GameStats.$Properties|null|undefined} gameStats
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.gameStats = null;

        /**
         * LobbyPlayer avatarUrl.
         * @member {string} avatarUrl
         * @memberof game.LobbyPlayer
         * @instance
         */
        LobbyPlayer.prototype.avatarUrl = "";

        /**
         * Creates a new LobbyPlayer instance using the specified properties.
         * @function create
         * @memberof game.LobbyPlayer
         * @static
         * @param {game.LobbyPlayer.$Properties=} [properties] Properties to set
         * @returns {game.LobbyPlayer} LobbyPlayer instance
         * @type {{
         *   (properties: game.LobbyPlayer.$Shape): game.LobbyPlayer & game.LobbyPlayer.$Shape;
         *   (properties?: game.LobbyPlayer.$Properties): game.LobbyPlayer;
         * }}
         */
        LobbyPlayer.create = function(properties) {
            return new LobbyPlayer(properties);
        };

        /**
         * Encodes the specified LobbyPlayer message. Does not implicitly {@link game.LobbyPlayer.verify|verify} messages.
         * @function encode
         * @memberof game.LobbyPlayer
         * @static
         * @param {game.LobbyPlayer.$Properties} message LobbyPlayer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyPlayer.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.genderId != null && $Object.hasOwnProperty.call(message, "genderId") && message.genderId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.genderId);
            if (message.genderLabel != null && $Object.hasOwnProperty.call(message, "genderLabel") && message.genderLabel !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.genderLabel);
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId") && message.factionId !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.factionId);
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel") && message.factionLabel !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.factionLabel);
            if (message.factionColors != null && $Object.hasOwnProperty.call(message, "factionColors"))
                $root.game.GenderColors.encode(message.factionColors, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.displayName != null && $Object.hasOwnProperty.call(message, "displayName") && message.displayName !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.displayName);
            if (message.connected != null && $Object.hasOwnProperty.call(message, "connected") && message.connected !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.connected);
            if (message.disconnectedAt != null && $Object.hasOwnProperty.call(message, "disconnectedAt") && (typeof message.disconnectedAt === "object" ? message.disconnectedAt.low || message.disconnectedAt.high : message.disconnectedAt !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.disconnectedAt);
            if (message.disconnectExpiresAt != null && $Object.hasOwnProperty.call(message, "disconnectExpiresAt") && (typeof message.disconnectExpiresAt === "object" ? message.disconnectExpiresAt.low || message.disconnectExpiresAt.high : message.disconnectExpiresAt !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.disconnectExpiresAt);
            if (message.nameWarEnabled != null && $Object.hasOwnProperty.call(message, "nameWarEnabled") && message.nameWarEnabled !== false)
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.nameWarEnabled);
            if (message.nameWarPenaltyName != null && $Object.hasOwnProperty.call(message, "nameWarPenaltyName") && message.nameWarPenaltyName !== "")
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.nameWarPenaltyName);
            if (message.nameWarPunished != null && $Object.hasOwnProperty.call(message, "nameWarPunished") && message.nameWarPunished !== false)
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.nameWarPunished);
            if (message.nameWarAllowRename != null && $Object.hasOwnProperty.call(message, "nameWarAllowRename") && message.nameWarAllowRename !== false)
                writer.uint32(/* id 19, wireType 0 =*/152).bool(message.nameWarAllowRename);
            if (message.nameWarRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "nameWarRenameProtectedUntil") && (typeof message.nameWarRenameProtectedUntil === "object" ? message.nameWarRenameProtectedUntil.low || message.nameWarRenameProtectedUntil.high : message.nameWarRenameProtectedUntil !== 0))
                writer.uint32(/* id 21, wireType 0 =*/168).int64(message.nameWarRenameProtectedUntil);
            if (message.nameWarRenamedByName != null && $Object.hasOwnProperty.call(message, "nameWarRenamedByName") && message.nameWarRenamedByName !== "")
                writer.uint32(/* id 23, wireType 2 =*/186).string(message.nameWarRenamedByName);
            if (message.giveawayEnabled != null && $Object.hasOwnProperty.call(message, "giveawayEnabled") && message.giveawayEnabled !== false)
                writer.uint32(/* id 24, wireType 0 =*/192).bool(message.giveawayEnabled);
            if (message.giveawayValue != null && $Object.hasOwnProperty.call(message, "giveawayValue") && !$Object.is(message.giveawayValue, 0))
                writer.uint32(/* id 26, wireType 1 =*/209).double(message.giveawayValue);
            if (message.giveawayBoardText != null && $Object.hasOwnProperty.call(message, "giveawayBoardText") && message.giveawayBoardText !== "")
                writer.uint32(/* id 28, wireType 2 =*/226).string(message.giveawayBoardText);
            if (message.giveawayBoardExpiresAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardExpiresAt") && (typeof message.giveawayBoardExpiresAt === "object" ? message.giveawayBoardExpiresAt.low || message.giveawayBoardExpiresAt.high : message.giveawayBoardExpiresAt !== 0))
                writer.uint32(/* id 29, wireType 0 =*/232).int64(message.giveawayBoardExpiresAt);
            if (message.giveawayBoardLikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikes") && message.giveawayBoardLikes !== 0)
                writer.uint32(/* id 31, wireType 0 =*/248).int32(message.giveawayBoardLikes);
            if (message.giveawayBoardDislikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardDislikes") && message.giveawayBoardDislikes !== 0)
                writer.uint32(/* id 33, wireType 0 =*/264).int32(message.giveawayBoardDislikes);
            if (message.rankMultiplierUnlocked != null && $Object.hasOwnProperty.call(message, "rankMultiplierUnlocked") && message.rankMultiplierUnlocked !== false)
                writer.uint32(/* id 35, wireType 0 =*/280).bool(message.rankMultiplierUnlocked);
            if (message.extremeModeEnabled != null && $Object.hasOwnProperty.call(message, "extremeModeEnabled") && message.extremeModeEnabled !== false)
                writer.uint32(/* id 37, wireType 0 =*/296).bool(message.extremeModeEnabled);
            if (message.extremeWinStreak != null && $Object.hasOwnProperty.call(message, "extremeWinStreak") && message.extremeWinStreak !== 0)
                writer.uint32(/* id 39, wireType 0 =*/312).int32(message.extremeWinStreak);
            if (message.extremeForceClosed != null && $Object.hasOwnProperty.call(message, "extremeForceClosed") && message.extremeForceClosed !== false)
                writer.uint32(/* id 41, wireType 0 =*/328).bool(message.extremeForceClosed);
            if (message.extremeForceClosedAt != null && $Object.hasOwnProperty.call(message, "extremeForceClosedAt") && (typeof message.extremeForceClosedAt === "object" ? message.extremeForceClosedAt.low || message.extremeForceClosedAt.high : message.extremeForceClosedAt !== 0))
                writer.uint32(/* id 43, wireType 0 =*/344).int64(message.extremeForceClosedAt);
            if (message.extremeRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "extremeRenameProtectedUntil") && (typeof message.extremeRenameProtectedUntil === "object" ? message.extremeRenameProtectedUntil.low || message.extremeRenameProtectedUntil.high : message.extremeRenameProtectedUntil !== 0))
                writer.uint32(/* id 45, wireType 0 =*/360).int64(message.extremeRenameProtectedUntil);
            if (message.extremeRenamedByName != null && $Object.hasOwnProperty.call(message, "extremeRenamedByName") && message.extremeRenamedByName !== "")
                writer.uint32(/* id 47, wireType 2 =*/378).string(message.extremeRenamedByName);
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId") && message.roomId !== "")
                writer.uint32(/* id 48, wireType 2 =*/386).string(message.roomId);
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats"))
                $root.game.LobbyStats.encode(message.stats, writer.uint32(/* id 49, wireType 2 =*/394).fork(), _depth + 1).ldelim();
            if (message.gameStats != null && $Object.hasOwnProperty.call(message, "gameStats"))
                $root.game.GameStats.encode(message.gameStats, writer.uint32(/* id 50, wireType 2 =*/402).fork(), _depth + 1).ldelim();
            if (message.avatarUrl != null && $Object.hasOwnProperty.call(message, "avatarUrl") && message.avatarUrl !== "")
                writer.uint32(/* id 51, wireType 2 =*/410).string(message.avatarUrl);
            if (message.giveawayVoteWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayVoteWindowStartedAt") && (typeof message.giveawayVoteWindowStartedAt === "object" ? message.giveawayVoteWindowStartedAt.low || message.giveawayVoteWindowStartedAt.high : message.giveawayVoteWindowStartedAt !== 0))
                writer.uint32(/* id 52, wireType 0 =*/416).int64(message.giveawayVoteWindowStartedAt);
            if (message.giveawayVoteLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteLikesThisHour") && message.giveawayVoteLikesThisHour !== 0)
                writer.uint32(/* id 53, wireType 0 =*/424).int32(message.giveawayVoteLikesThisHour);
            if (message.giveawayVoteDislikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteDislikesThisHour") && message.giveawayVoteDislikesThisHour !== 0)
                writer.uint32(/* id 54, wireType 0 =*/432).int32(message.giveawayVoteDislikesThisHour);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LobbyPlayer message, length delimited. Does not implicitly {@link game.LobbyPlayer.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LobbyPlayer
         * @static
         * @param {game.LobbyPlayer.$Properties} message LobbyPlayer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyPlayer.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LobbyPlayer message from the specified reader or buffer.
         * @function decode
         * @memberof game.LobbyPlayer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LobbyPlayer & game.LobbyPlayer.$Shape} LobbyPlayer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyPlayer.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LobbyPlayer(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.genderId = value;
                        else
                            delete message.genderId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.genderLabel = value;
                        else
                            delete message.genderLabel;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.factionId = value;
                        else
                            delete message.factionId;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.factionLabel = value;
                        else
                            delete message.factionLabel;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.factionColors = $root.game.GenderColors.decode(reader, reader.uint32(), $undefined, _depth + 1, message.factionColors);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.displayName = value;
                        else
                            delete message.displayName;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.connected = value;
                        else
                            delete message.connected;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.disconnectedAt = value;
                        else
                            delete message.disconnectedAt;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.disconnectExpiresAt = value;
                        else
                            delete message.disconnectExpiresAt;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.nameWarEnabled = value;
                        else
                            delete message.nameWarEnabled;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nameWarPenaltyName = value;
                        else
                            delete message.nameWarPenaltyName;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.nameWarPunished = value;
                        else
                            delete message.nameWarPunished;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.nameWarAllowRename = value;
                        else
                            delete message.nameWarAllowRename;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nameWarRenameProtectedUntil = value;
                        else
                            delete message.nameWarRenameProtectedUntil;
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nameWarRenamedByName = value;
                        else
                            delete message.nameWarRenamedByName;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.giveawayEnabled = value;
                        else
                            delete message.giveawayEnabled;
                        continue;
                    }
                case 26: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.giveawayValue = value;
                        else
                            delete message.giveawayValue;
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.giveawayBoardText = value;
                        else
                            delete message.giveawayBoardText;
                        continue;
                    }
                case 29: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.giveawayBoardExpiresAt = value;
                        else
                            delete message.giveawayBoardExpiresAt;
                        continue;
                    }
                case 31: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayBoardLikes = value;
                        else
                            delete message.giveawayBoardLikes;
                        continue;
                    }
                case 33: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayBoardDislikes = value;
                        else
                            delete message.giveawayBoardDislikes;
                        continue;
                    }
                case 52: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.giveawayVoteWindowStartedAt = value;
                        else
                            delete message.giveawayVoteWindowStartedAt;
                        continue;
                    }
                case 53: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayVoteLikesThisHour = value;
                        else
                            delete message.giveawayVoteLikesThisHour;
                        continue;
                    }
                case 54: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.giveawayVoteDislikesThisHour = value;
                        else
                            delete message.giveawayVoteDislikesThisHour;
                        continue;
                    }
                case 35: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.rankMultiplierUnlocked = value;
                        else
                            delete message.rankMultiplierUnlocked;
                        continue;
                    }
                case 37: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.extremeModeEnabled = value;
                        else
                            delete message.extremeModeEnabled;
                        continue;
                    }
                case 39: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.extremeWinStreak = value;
                        else
                            delete message.extremeWinStreak;
                        continue;
                    }
                case 41: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.extremeForceClosed = value;
                        else
                            delete message.extremeForceClosed;
                        continue;
                    }
                case 43: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.extremeForceClosedAt = value;
                        else
                            delete message.extremeForceClosedAt;
                        continue;
                    }
                case 45: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.extremeRenameProtectedUntil = value;
                        else
                            delete message.extremeRenameProtectedUntil;
                        continue;
                    }
                case 47: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.extremeRenamedByName = value;
                        else
                            delete message.extremeRenamedByName;
                        continue;
                    }
                case 48: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.roomId = value;
                        else
                            delete message.roomId;
                        continue;
                    }
                case 49: {
                        if (wireType !== 2)
                            break;
                        message.stats = $root.game.LobbyStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stats);
                        continue;
                    }
                case 50: {
                        if (wireType !== 2)
                            break;
                        message.gameStats = $root.game.GameStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gameStats);
                        continue;
                    }
                case 51: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.avatarUrl = value;
                        else
                            delete message.avatarUrl;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LobbyPlayer message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LobbyPlayer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LobbyPlayer & game.LobbyPlayer.$Shape} LobbyPlayer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyPlayer.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LobbyPlayer message.
         * @function verify
         * @memberof game.LobbyPlayer
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LobbyPlayer.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.genderId != null && $Object.hasOwnProperty.call(message, "genderId"))
                if (!$util.isString(message.genderId))
                    return "genderId: string expected";
            if (message.genderLabel != null && $Object.hasOwnProperty.call(message, "genderLabel"))
                if (!$util.isString(message.genderLabel))
                    return "genderLabel: string expected";
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                if (!$util.isString(message.factionId))
                    return "factionId: string expected";
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel"))
                if (!$util.isString(message.factionLabel))
                    return "factionLabel: string expected";
            if (message.factionColors != null && $Object.hasOwnProperty.call(message, "factionColors")) {
                let error = $root.game.GenderColors.verify(message.factionColors, _depth + 1);
                if (error)
                    return "factionColors." + error;
            }
            if (message.displayName != null && $Object.hasOwnProperty.call(message, "displayName"))
                if (!$util.isString(message.displayName))
                    return "displayName: string expected";
            if (message.connected != null && $Object.hasOwnProperty.call(message, "connected"))
                if (typeof message.connected !== "boolean")
                    return "connected: boolean expected";
            if (message.disconnectedAt != null && $Object.hasOwnProperty.call(message, "disconnectedAt"))
                if (!$util.isInteger(message.disconnectedAt) && !(message.disconnectedAt && $util.isInteger(message.disconnectedAt.low) && $util.isInteger(message.disconnectedAt.high)))
                    return "disconnectedAt: integer|Long expected";
            if (message.disconnectExpiresAt != null && $Object.hasOwnProperty.call(message, "disconnectExpiresAt"))
                if (!$util.isInteger(message.disconnectExpiresAt) && !(message.disconnectExpiresAt && $util.isInteger(message.disconnectExpiresAt.low) && $util.isInteger(message.disconnectExpiresAt.high)))
                    return "disconnectExpiresAt: integer|Long expected";
            if (message.nameWarEnabled != null && $Object.hasOwnProperty.call(message, "nameWarEnabled"))
                if (typeof message.nameWarEnabled !== "boolean")
                    return "nameWarEnabled: boolean expected";
            if (message.nameWarPenaltyName != null && $Object.hasOwnProperty.call(message, "nameWarPenaltyName"))
                if (!$util.isString(message.nameWarPenaltyName))
                    return "nameWarPenaltyName: string expected";
            if (message.nameWarPunished != null && $Object.hasOwnProperty.call(message, "nameWarPunished"))
                if (typeof message.nameWarPunished !== "boolean")
                    return "nameWarPunished: boolean expected";
            if (message.nameWarAllowRename != null && $Object.hasOwnProperty.call(message, "nameWarAllowRename"))
                if (typeof message.nameWarAllowRename !== "boolean")
                    return "nameWarAllowRename: boolean expected";
            if (message.nameWarRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "nameWarRenameProtectedUntil"))
                if (!$util.isInteger(message.nameWarRenameProtectedUntil) && !(message.nameWarRenameProtectedUntil && $util.isInteger(message.nameWarRenameProtectedUntil.low) && $util.isInteger(message.nameWarRenameProtectedUntil.high)))
                    return "nameWarRenameProtectedUntil: integer|Long expected";
            if (message.nameWarRenamedByName != null && $Object.hasOwnProperty.call(message, "nameWarRenamedByName"))
                if (!$util.isString(message.nameWarRenamedByName))
                    return "nameWarRenamedByName: string expected";
            if (message.giveawayEnabled != null && $Object.hasOwnProperty.call(message, "giveawayEnabled"))
                if (typeof message.giveawayEnabled !== "boolean")
                    return "giveawayEnabled: boolean expected";
            if (message.giveawayValue != null && $Object.hasOwnProperty.call(message, "giveawayValue"))
                if (typeof message.giveawayValue !== "number")
                    return "giveawayValue: number expected";
            if (message.giveawayBoardText != null && $Object.hasOwnProperty.call(message, "giveawayBoardText"))
                if (!$util.isString(message.giveawayBoardText))
                    return "giveawayBoardText: string expected";
            if (message.giveawayBoardExpiresAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardExpiresAt"))
                if (!$util.isInteger(message.giveawayBoardExpiresAt) && !(message.giveawayBoardExpiresAt && $util.isInteger(message.giveawayBoardExpiresAt.low) && $util.isInteger(message.giveawayBoardExpiresAt.high)))
                    return "giveawayBoardExpiresAt: integer|Long expected";
            if (message.giveawayBoardLikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikes"))
                if (!$util.isInteger(message.giveawayBoardLikes))
                    return "giveawayBoardLikes: integer expected";
            if (message.giveawayBoardDislikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardDislikes"))
                if (!$util.isInteger(message.giveawayBoardDislikes))
                    return "giveawayBoardDislikes: integer expected";
            if (message.giveawayVoteWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayVoteWindowStartedAt"))
                if (!$util.isInteger(message.giveawayVoteWindowStartedAt) && !(message.giveawayVoteWindowStartedAt && $util.isInteger(message.giveawayVoteWindowStartedAt.low) && $util.isInteger(message.giveawayVoteWindowStartedAt.high)))
                    return "giveawayVoteWindowStartedAt: integer|Long expected";
            if (message.giveawayVoteLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteLikesThisHour"))
                if (!$util.isInteger(message.giveawayVoteLikesThisHour))
                    return "giveawayVoteLikesThisHour: integer expected";
            if (message.giveawayVoteDislikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteDislikesThisHour"))
                if (!$util.isInteger(message.giveawayVoteDislikesThisHour))
                    return "giveawayVoteDislikesThisHour: integer expected";
            if (message.rankMultiplierUnlocked != null && $Object.hasOwnProperty.call(message, "rankMultiplierUnlocked"))
                if (typeof message.rankMultiplierUnlocked !== "boolean")
                    return "rankMultiplierUnlocked: boolean expected";
            if (message.extremeModeEnabled != null && $Object.hasOwnProperty.call(message, "extremeModeEnabled"))
                if (typeof message.extremeModeEnabled !== "boolean")
                    return "extremeModeEnabled: boolean expected";
            if (message.extremeWinStreak != null && $Object.hasOwnProperty.call(message, "extremeWinStreak"))
                if (!$util.isInteger(message.extremeWinStreak))
                    return "extremeWinStreak: integer expected";
            if (message.extremeForceClosed != null && $Object.hasOwnProperty.call(message, "extremeForceClosed"))
                if (typeof message.extremeForceClosed !== "boolean")
                    return "extremeForceClosed: boolean expected";
            if (message.extremeForceClosedAt != null && $Object.hasOwnProperty.call(message, "extremeForceClosedAt"))
                if (!$util.isInteger(message.extremeForceClosedAt) && !(message.extremeForceClosedAt && $util.isInteger(message.extremeForceClosedAt.low) && $util.isInteger(message.extremeForceClosedAt.high)))
                    return "extremeForceClosedAt: integer|Long expected";
            if (message.extremeRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "extremeRenameProtectedUntil"))
                if (!$util.isInteger(message.extremeRenameProtectedUntil) && !(message.extremeRenameProtectedUntil && $util.isInteger(message.extremeRenameProtectedUntil.low) && $util.isInteger(message.extremeRenameProtectedUntil.high)))
                    return "extremeRenameProtectedUntil: integer|Long expected";
            if (message.extremeRenamedByName != null && $Object.hasOwnProperty.call(message, "extremeRenamedByName"))
                if (!$util.isString(message.extremeRenamedByName))
                    return "extremeRenamedByName: string expected";
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                if (!$util.isString(message.roomId))
                    return "roomId: string expected";
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats")) {
                let error = $root.game.LobbyStats.verify(message.stats, _depth + 1);
                if (error)
                    return "stats." + error;
            }
            if (message.gameStats != null && $Object.hasOwnProperty.call(message, "gameStats")) {
                let error = $root.game.GameStats.verify(message.gameStats, _depth + 1);
                if (error)
                    return "gameStats." + error;
            }
            if (message.avatarUrl != null && $Object.hasOwnProperty.call(message, "avatarUrl"))
                if (!$util.isString(message.avatarUrl))
                    return "avatarUrl: string expected";
            return null;
        };

        /**
         * Creates a LobbyPlayer message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LobbyPlayer
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LobbyPlayer} LobbyPlayer
         */
        LobbyPlayer.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LobbyPlayer)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LobbyPlayer: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LobbyPlayer();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.genderId != null)
                if (typeof object.genderId !== "string" || object.genderId.length)
                    message.genderId = $String(object.genderId);
            if (object.genderLabel != null)
                if (typeof object.genderLabel !== "string" || object.genderLabel.length)
                    message.genderLabel = $String(object.genderLabel);
            if (object.factionId != null)
                if (typeof object.factionId !== "string" || object.factionId.length)
                    message.factionId = $String(object.factionId);
            if (object.factionLabel != null)
                if (typeof object.factionLabel !== "string" || object.factionLabel.length)
                    message.factionLabel = $String(object.factionLabel);
            if (object.factionColors != null) {
                if (!$util.isObject(object.factionColors))
                    throw $TypeError(".game.LobbyPlayer.factionColors: object expected");
                message.factionColors = $root.game.GenderColors.fromObject(object.factionColors, _depth + 1);
            }
            if (object.displayName != null)
                if (typeof object.displayName !== "string" || object.displayName.length)
                    message.displayName = $String(object.displayName);
            if (object.connected != null)
                if (object.connected)
                    message.connected = $Boolean(object.connected);
            if (object.disconnectedAt != null)
                if (typeof object.disconnectedAt === "object" ? object.disconnectedAt.low || object.disconnectedAt.high : $Number(object.disconnectedAt) !== 0)
                    if ($util.Long)
                        message.disconnectedAt = $util.Long.fromValue(object.disconnectedAt, false);
                    else if (typeof object.disconnectedAt === "string")
                        message.disconnectedAt = $parseInt(object.disconnectedAt, 10);
                    else if (typeof object.disconnectedAt === "number")
                        message.disconnectedAt = object.disconnectedAt;
                    else if (typeof object.disconnectedAt === "object")
                        message.disconnectedAt = new $util.LongBits(object.disconnectedAt.low >>> 0, object.disconnectedAt.high >>> 0).toNumber();
            if (object.disconnectExpiresAt != null)
                if (typeof object.disconnectExpiresAt === "object" ? object.disconnectExpiresAt.low || object.disconnectExpiresAt.high : $Number(object.disconnectExpiresAt) !== 0)
                    if ($util.Long)
                        message.disconnectExpiresAt = $util.Long.fromValue(object.disconnectExpiresAt, false);
                    else if (typeof object.disconnectExpiresAt === "string")
                        message.disconnectExpiresAt = $parseInt(object.disconnectExpiresAt, 10);
                    else if (typeof object.disconnectExpiresAt === "number")
                        message.disconnectExpiresAt = object.disconnectExpiresAt;
                    else if (typeof object.disconnectExpiresAt === "object")
                        message.disconnectExpiresAt = new $util.LongBits(object.disconnectExpiresAt.low >>> 0, object.disconnectExpiresAt.high >>> 0).toNumber();
            if (object.nameWarEnabled != null)
                if (object.nameWarEnabled)
                    message.nameWarEnabled = $Boolean(object.nameWarEnabled);
            if (object.nameWarPenaltyName != null)
                if (typeof object.nameWarPenaltyName !== "string" || object.nameWarPenaltyName.length)
                    message.nameWarPenaltyName = $String(object.nameWarPenaltyName);
            if (object.nameWarPunished != null)
                if (object.nameWarPunished)
                    message.nameWarPunished = $Boolean(object.nameWarPunished);
            if (object.nameWarAllowRename != null)
                if (object.nameWarAllowRename)
                    message.nameWarAllowRename = $Boolean(object.nameWarAllowRename);
            if (object.nameWarRenameProtectedUntil != null)
                if (typeof object.nameWarRenameProtectedUntil === "object" ? object.nameWarRenameProtectedUntil.low || object.nameWarRenameProtectedUntil.high : $Number(object.nameWarRenameProtectedUntil) !== 0)
                    if ($util.Long)
                        message.nameWarRenameProtectedUntil = $util.Long.fromValue(object.nameWarRenameProtectedUntil, false);
                    else if (typeof object.nameWarRenameProtectedUntil === "string")
                        message.nameWarRenameProtectedUntil = $parseInt(object.nameWarRenameProtectedUntil, 10);
                    else if (typeof object.nameWarRenameProtectedUntil === "number")
                        message.nameWarRenameProtectedUntil = object.nameWarRenameProtectedUntil;
                    else if (typeof object.nameWarRenameProtectedUntil === "object")
                        message.nameWarRenameProtectedUntil = new $util.LongBits(object.nameWarRenameProtectedUntil.low >>> 0, object.nameWarRenameProtectedUntil.high >>> 0).toNumber();
            if (object.nameWarRenamedByName != null)
                if (typeof object.nameWarRenamedByName !== "string" || object.nameWarRenamedByName.length)
                    message.nameWarRenamedByName = $String(object.nameWarRenamedByName);
            if (object.giveawayEnabled != null)
                if (object.giveawayEnabled)
                    message.giveawayEnabled = $Boolean(object.giveawayEnabled);
            if (object.giveawayValue != null)
                if (!$Object.is($Number(object.giveawayValue), 0))
                    message.giveawayValue = $Number(object.giveawayValue);
            if (object.giveawayBoardText != null)
                if (typeof object.giveawayBoardText !== "string" || object.giveawayBoardText.length)
                    message.giveawayBoardText = $String(object.giveawayBoardText);
            if (object.giveawayBoardExpiresAt != null)
                if (typeof object.giveawayBoardExpiresAt === "object" ? object.giveawayBoardExpiresAt.low || object.giveawayBoardExpiresAt.high : $Number(object.giveawayBoardExpiresAt) !== 0)
                    if ($util.Long)
                        message.giveawayBoardExpiresAt = $util.Long.fromValue(object.giveawayBoardExpiresAt, false);
                    else if (typeof object.giveawayBoardExpiresAt === "string")
                        message.giveawayBoardExpiresAt = $parseInt(object.giveawayBoardExpiresAt, 10);
                    else if (typeof object.giveawayBoardExpiresAt === "number")
                        message.giveawayBoardExpiresAt = object.giveawayBoardExpiresAt;
                    else if (typeof object.giveawayBoardExpiresAt === "object")
                        message.giveawayBoardExpiresAt = new $util.LongBits(object.giveawayBoardExpiresAt.low >>> 0, object.giveawayBoardExpiresAt.high >>> 0).toNumber();
            if (object.giveawayBoardLikes != null)
                if ($Number(object.giveawayBoardLikes) !== 0)
                    message.giveawayBoardLikes = object.giveawayBoardLikes | 0;
            if (object.giveawayBoardDislikes != null)
                if ($Number(object.giveawayBoardDislikes) !== 0)
                    message.giveawayBoardDislikes = object.giveawayBoardDislikes | 0;
            if (object.giveawayVoteWindowStartedAt != null)
                if (typeof object.giveawayVoteWindowStartedAt === "object" ? object.giveawayVoteWindowStartedAt.low || object.giveawayVoteWindowStartedAt.high : $Number(object.giveawayVoteWindowStartedAt) !== 0)
                    if ($util.Long)
                        message.giveawayVoteWindowStartedAt = $util.Long.fromValue(object.giveawayVoteWindowStartedAt, false);
                    else if (typeof object.giveawayVoteWindowStartedAt === "string")
                        message.giveawayVoteWindowStartedAt = $parseInt(object.giveawayVoteWindowStartedAt, 10);
                    else if (typeof object.giveawayVoteWindowStartedAt === "number")
                        message.giveawayVoteWindowStartedAt = object.giveawayVoteWindowStartedAt;
                    else if (typeof object.giveawayVoteWindowStartedAt === "object")
                        message.giveawayVoteWindowStartedAt = new $util.LongBits(object.giveawayVoteWindowStartedAt.low >>> 0, object.giveawayVoteWindowStartedAt.high >>> 0).toNumber();
            if (object.giveawayVoteLikesThisHour != null)
                if ($Number(object.giveawayVoteLikesThisHour) !== 0)
                    message.giveawayVoteLikesThisHour = object.giveawayVoteLikesThisHour | 0;
            if (object.giveawayVoteDislikesThisHour != null)
                if ($Number(object.giveawayVoteDislikesThisHour) !== 0)
                    message.giveawayVoteDislikesThisHour = object.giveawayVoteDislikesThisHour | 0;
            if (object.rankMultiplierUnlocked != null)
                if (object.rankMultiplierUnlocked)
                    message.rankMultiplierUnlocked = $Boolean(object.rankMultiplierUnlocked);
            if (object.extremeModeEnabled != null)
                if (object.extremeModeEnabled)
                    message.extremeModeEnabled = $Boolean(object.extremeModeEnabled);
            if (object.extremeWinStreak != null)
                if ($Number(object.extremeWinStreak) !== 0)
                    message.extremeWinStreak = object.extremeWinStreak | 0;
            if (object.extremeForceClosed != null)
                if (object.extremeForceClosed)
                    message.extremeForceClosed = $Boolean(object.extremeForceClosed);
            if (object.extremeForceClosedAt != null)
                if (typeof object.extremeForceClosedAt === "object" ? object.extremeForceClosedAt.low || object.extremeForceClosedAt.high : $Number(object.extremeForceClosedAt) !== 0)
                    if ($util.Long)
                        message.extremeForceClosedAt = $util.Long.fromValue(object.extremeForceClosedAt, false);
                    else if (typeof object.extremeForceClosedAt === "string")
                        message.extremeForceClosedAt = $parseInt(object.extremeForceClosedAt, 10);
                    else if (typeof object.extremeForceClosedAt === "number")
                        message.extremeForceClosedAt = object.extremeForceClosedAt;
                    else if (typeof object.extremeForceClosedAt === "object")
                        message.extremeForceClosedAt = new $util.LongBits(object.extremeForceClosedAt.low >>> 0, object.extremeForceClosedAt.high >>> 0).toNumber();
            if (object.extremeRenameProtectedUntil != null)
                if (typeof object.extremeRenameProtectedUntil === "object" ? object.extremeRenameProtectedUntil.low || object.extremeRenameProtectedUntil.high : $Number(object.extremeRenameProtectedUntil) !== 0)
                    if ($util.Long)
                        message.extremeRenameProtectedUntil = $util.Long.fromValue(object.extremeRenameProtectedUntil, false);
                    else if (typeof object.extremeRenameProtectedUntil === "string")
                        message.extremeRenameProtectedUntil = $parseInt(object.extremeRenameProtectedUntil, 10);
                    else if (typeof object.extremeRenameProtectedUntil === "number")
                        message.extremeRenameProtectedUntil = object.extremeRenameProtectedUntil;
                    else if (typeof object.extremeRenameProtectedUntil === "object")
                        message.extremeRenameProtectedUntil = new $util.LongBits(object.extremeRenameProtectedUntil.low >>> 0, object.extremeRenameProtectedUntil.high >>> 0).toNumber();
            if (object.extremeRenamedByName != null)
                if (typeof object.extremeRenamedByName !== "string" || object.extremeRenamedByName.length)
                    message.extremeRenamedByName = $String(object.extremeRenamedByName);
            if (object.roomId != null)
                if (typeof object.roomId !== "string" || object.roomId.length)
                    message.roomId = $String(object.roomId);
            if (object.stats != null) {
                if (!$util.isObject(object.stats))
                    throw $TypeError(".game.LobbyPlayer.stats: object expected");
                message.stats = $root.game.LobbyStats.fromObject(object.stats, _depth + 1);
            }
            if (object.gameStats != null) {
                if (!$util.isObject(object.gameStats))
                    throw $TypeError(".game.LobbyPlayer.gameStats: object expected");
                message.gameStats = $root.game.GameStats.fromObject(object.gameStats, _depth + 1);
            }
            if (object.avatarUrl != null)
                if (typeof object.avatarUrl !== "string" || object.avatarUrl.length)
                    message.avatarUrl = $String(object.avatarUrl);
            return message;
        };

        /**
         * Creates a plain object from a LobbyPlayer message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LobbyPlayer
         * @static
         * @param {game.LobbyPlayer} message LobbyPlayer
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LobbyPlayer.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.name = "";
                object.genderId = "";
                object.genderLabel = "";
                object.factionId = "";
                object.factionLabel = "";
                object.factionColors = null;
                object.displayName = "";
                object.connected = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.disconnectedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.disconnectedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.disconnectExpiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.disconnectExpiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nameWarEnabled = false;
                object.nameWarPenaltyName = "";
                object.nameWarPunished = false;
                object.nameWarAllowRename = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nameWarRenameProtectedUntil = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nameWarRenameProtectedUntil = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nameWarRenamedByName = "";
                object.giveawayEnabled = false;
                object.giveawayValue = 0;
                object.giveawayBoardText = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.giveawayBoardExpiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.giveawayBoardExpiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.giveawayBoardLikes = 0;
                object.giveawayBoardDislikes = 0;
                object.rankMultiplierUnlocked = false;
                object.extremeModeEnabled = false;
                object.extremeWinStreak = 0;
                object.extremeForceClosed = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.extremeForceClosedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.extremeForceClosedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.extremeRenameProtectedUntil = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.extremeRenameProtectedUntil = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.extremeRenamedByName = "";
                object.roomId = "";
                object.stats = null;
                object.gameStats = null;
                object.avatarUrl = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.giveawayVoteWindowStartedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.giveawayVoteWindowStartedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.giveawayVoteLikesThisHour = 0;
                object.giveawayVoteDislikesThisHour = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.genderId != null && $Object.hasOwnProperty.call(message, "genderId"))
                object.genderId = message.genderId;
            if (message.genderLabel != null && $Object.hasOwnProperty.call(message, "genderLabel"))
                object.genderLabel = message.genderLabel;
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                object.factionId = message.factionId;
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel"))
                object.factionLabel = message.factionLabel;
            if (message.factionColors != null && $Object.hasOwnProperty.call(message, "factionColors"))
                object.factionColors = $root.game.GenderColors.toObject(message.factionColors, options, _depth + 1);
            if (message.displayName != null && $Object.hasOwnProperty.call(message, "displayName"))
                object.displayName = message.displayName;
            if (message.connected != null && $Object.hasOwnProperty.call(message, "connected"))
                object.connected = message.connected;
            if (message.disconnectedAt != null && $Object.hasOwnProperty.call(message, "disconnectedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.disconnectedAt = typeof message.disconnectedAt === "number" ? $BigInt(message.disconnectedAt) : $util.Long.fromBits(message.disconnectedAt.low >>> 0, message.disconnectedAt.high >>> 0, false).toBigInt();
                else if (typeof message.disconnectedAt === "number")
                    object.disconnectedAt = options.longs === $String ? $String(message.disconnectedAt) : message.disconnectedAt;
                else
                    object.disconnectedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.disconnectedAt) : options.longs === $Number ? new $util.LongBits(message.disconnectedAt.low >>> 0, message.disconnectedAt.high >>> 0).toNumber() : message.disconnectedAt;
            if (message.disconnectExpiresAt != null && $Object.hasOwnProperty.call(message, "disconnectExpiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.disconnectExpiresAt = typeof message.disconnectExpiresAt === "number" ? $BigInt(message.disconnectExpiresAt) : $util.Long.fromBits(message.disconnectExpiresAt.low >>> 0, message.disconnectExpiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.disconnectExpiresAt === "number")
                    object.disconnectExpiresAt = options.longs === $String ? $String(message.disconnectExpiresAt) : message.disconnectExpiresAt;
                else
                    object.disconnectExpiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.disconnectExpiresAt) : options.longs === $Number ? new $util.LongBits(message.disconnectExpiresAt.low >>> 0, message.disconnectExpiresAt.high >>> 0).toNumber() : message.disconnectExpiresAt;
            if (message.nameWarEnabled != null && $Object.hasOwnProperty.call(message, "nameWarEnabled"))
                object.nameWarEnabled = message.nameWarEnabled;
            if (message.nameWarPenaltyName != null && $Object.hasOwnProperty.call(message, "nameWarPenaltyName"))
                object.nameWarPenaltyName = message.nameWarPenaltyName;
            if (message.nameWarPunished != null && $Object.hasOwnProperty.call(message, "nameWarPunished"))
                object.nameWarPunished = message.nameWarPunished;
            if (message.nameWarAllowRename != null && $Object.hasOwnProperty.call(message, "nameWarAllowRename"))
                object.nameWarAllowRename = message.nameWarAllowRename;
            if (message.nameWarRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "nameWarRenameProtectedUntil"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nameWarRenameProtectedUntil = typeof message.nameWarRenameProtectedUntil === "number" ? $BigInt(message.nameWarRenameProtectedUntil) : $util.Long.fromBits(message.nameWarRenameProtectedUntil.low >>> 0, message.nameWarRenameProtectedUntil.high >>> 0, false).toBigInt();
                else if (typeof message.nameWarRenameProtectedUntil === "number")
                    object.nameWarRenameProtectedUntil = options.longs === $String ? $String(message.nameWarRenameProtectedUntil) : message.nameWarRenameProtectedUntil;
                else
                    object.nameWarRenameProtectedUntil = options.longs === $String ? $util.Long.prototype.toString.call(message.nameWarRenameProtectedUntil) : options.longs === $Number ? new $util.LongBits(message.nameWarRenameProtectedUntil.low >>> 0, message.nameWarRenameProtectedUntil.high >>> 0).toNumber() : message.nameWarRenameProtectedUntil;
            if (message.nameWarRenamedByName != null && $Object.hasOwnProperty.call(message, "nameWarRenamedByName"))
                object.nameWarRenamedByName = message.nameWarRenamedByName;
            if (message.giveawayEnabled != null && $Object.hasOwnProperty.call(message, "giveawayEnabled"))
                object.giveawayEnabled = message.giveawayEnabled;
            if (message.giveawayValue != null && $Object.hasOwnProperty.call(message, "giveawayValue"))
                object.giveawayValue = options.json && !$isFinite(message.giveawayValue) ? $String(message.giveawayValue) : message.giveawayValue;
            if (message.giveawayBoardText != null && $Object.hasOwnProperty.call(message, "giveawayBoardText"))
                object.giveawayBoardText = message.giveawayBoardText;
            if (message.giveawayBoardExpiresAt != null && $Object.hasOwnProperty.call(message, "giveawayBoardExpiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.giveawayBoardExpiresAt = typeof message.giveawayBoardExpiresAt === "number" ? $BigInt(message.giveawayBoardExpiresAt) : $util.Long.fromBits(message.giveawayBoardExpiresAt.low >>> 0, message.giveawayBoardExpiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.giveawayBoardExpiresAt === "number")
                    object.giveawayBoardExpiresAt = options.longs === $String ? $String(message.giveawayBoardExpiresAt) : message.giveawayBoardExpiresAt;
                else
                    object.giveawayBoardExpiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.giveawayBoardExpiresAt) : options.longs === $Number ? new $util.LongBits(message.giveawayBoardExpiresAt.low >>> 0, message.giveawayBoardExpiresAt.high >>> 0).toNumber() : message.giveawayBoardExpiresAt;
            if (message.giveawayBoardLikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardLikes"))
                object.giveawayBoardLikes = message.giveawayBoardLikes;
            if (message.giveawayBoardDislikes != null && $Object.hasOwnProperty.call(message, "giveawayBoardDislikes"))
                object.giveawayBoardDislikes = message.giveawayBoardDislikes;
            if (message.rankMultiplierUnlocked != null && $Object.hasOwnProperty.call(message, "rankMultiplierUnlocked"))
                object.rankMultiplierUnlocked = message.rankMultiplierUnlocked;
            if (message.extremeModeEnabled != null && $Object.hasOwnProperty.call(message, "extremeModeEnabled"))
                object.extremeModeEnabled = message.extremeModeEnabled;
            if (message.extremeWinStreak != null && $Object.hasOwnProperty.call(message, "extremeWinStreak"))
                object.extremeWinStreak = message.extremeWinStreak;
            if (message.extremeForceClosed != null && $Object.hasOwnProperty.call(message, "extremeForceClosed"))
                object.extremeForceClosed = message.extremeForceClosed;
            if (message.extremeForceClosedAt != null && $Object.hasOwnProperty.call(message, "extremeForceClosedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.extremeForceClosedAt = typeof message.extremeForceClosedAt === "number" ? $BigInt(message.extremeForceClosedAt) : $util.Long.fromBits(message.extremeForceClosedAt.low >>> 0, message.extremeForceClosedAt.high >>> 0, false).toBigInt();
                else if (typeof message.extremeForceClosedAt === "number")
                    object.extremeForceClosedAt = options.longs === $String ? $String(message.extremeForceClosedAt) : message.extremeForceClosedAt;
                else
                    object.extremeForceClosedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.extremeForceClosedAt) : options.longs === $Number ? new $util.LongBits(message.extremeForceClosedAt.low >>> 0, message.extremeForceClosedAt.high >>> 0).toNumber() : message.extremeForceClosedAt;
            if (message.extremeRenameProtectedUntil != null && $Object.hasOwnProperty.call(message, "extremeRenameProtectedUntil"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.extremeRenameProtectedUntil = typeof message.extremeRenameProtectedUntil === "number" ? $BigInt(message.extremeRenameProtectedUntil) : $util.Long.fromBits(message.extremeRenameProtectedUntil.low >>> 0, message.extremeRenameProtectedUntil.high >>> 0, false).toBigInt();
                else if (typeof message.extremeRenameProtectedUntil === "number")
                    object.extremeRenameProtectedUntil = options.longs === $String ? $String(message.extremeRenameProtectedUntil) : message.extremeRenameProtectedUntil;
                else
                    object.extremeRenameProtectedUntil = options.longs === $String ? $util.Long.prototype.toString.call(message.extremeRenameProtectedUntil) : options.longs === $Number ? new $util.LongBits(message.extremeRenameProtectedUntil.low >>> 0, message.extremeRenameProtectedUntil.high >>> 0).toNumber() : message.extremeRenameProtectedUntil;
            if (message.extremeRenamedByName != null && $Object.hasOwnProperty.call(message, "extremeRenamedByName"))
                object.extremeRenamedByName = message.extremeRenamedByName;
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                object.roomId = message.roomId;
            if (message.stats != null && $Object.hasOwnProperty.call(message, "stats"))
                object.stats = $root.game.LobbyStats.toObject(message.stats, options, _depth + 1);
            if (message.gameStats != null && $Object.hasOwnProperty.call(message, "gameStats"))
                object.gameStats = $root.game.GameStats.toObject(message.gameStats, options, _depth + 1);
            if (message.avatarUrl != null && $Object.hasOwnProperty.call(message, "avatarUrl"))
                object.avatarUrl = message.avatarUrl;
            if (message.giveawayVoteWindowStartedAt != null && $Object.hasOwnProperty.call(message, "giveawayVoteWindowStartedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.giveawayVoteWindowStartedAt = typeof message.giveawayVoteWindowStartedAt === "number" ? $BigInt(message.giveawayVoteWindowStartedAt) : $util.Long.fromBits(message.giveawayVoteWindowStartedAt.low >>> 0, message.giveawayVoteWindowStartedAt.high >>> 0, false).toBigInt();
                else if (typeof message.giveawayVoteWindowStartedAt === "number")
                    object.giveawayVoteWindowStartedAt = options.longs === $String ? $String(message.giveawayVoteWindowStartedAt) : message.giveawayVoteWindowStartedAt;
                else
                    object.giveawayVoteWindowStartedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.giveawayVoteWindowStartedAt) : options.longs === $Number ? new $util.LongBits(message.giveawayVoteWindowStartedAt.low >>> 0, message.giveawayVoteWindowStartedAt.high >>> 0).toNumber() : message.giveawayVoteWindowStartedAt;
            if (message.giveawayVoteLikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteLikesThisHour"))
                object.giveawayVoteLikesThisHour = message.giveawayVoteLikesThisHour;
            if (message.giveawayVoteDislikesThisHour != null && $Object.hasOwnProperty.call(message, "giveawayVoteDislikesThisHour"))
                object.giveawayVoteDislikesThisHour = message.giveawayVoteDislikesThisHour;
            return object;
        };

        /**
         * Converts this LobbyPlayer to JSON.
         * @function toJSON
         * @memberof game.LobbyPlayer
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LobbyPlayer.prototype.toJSON = function() {
            return LobbyPlayer.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LobbyPlayer
         * @function getTypeUrl
         * @memberof game.LobbyPlayer
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LobbyPlayer.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LobbyPlayer";
        };

        return LobbyPlayer;
    })();

    game.SeatOccupant = (function() {

        /**
         * Properties of a SeatOccupant.
         * @typedef {Object} game.SeatOccupant.$Properties
         * @property {game.PublicPlayer.$Properties|null} [player] SeatOccupant player
         * @property {"player"} [kind] SeatOccupant kind
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SeatOccupant.
         * @memberof game
         * @interface ISeatOccupant
         * @augments game.SeatOccupant.$Properties
         * @deprecated Use game.SeatOccupant.$Properties instead.
         */

        /**
         * Narrowed shape of a SeatOccupant.
         * @typedef {{
         *   player?: game.PublicPlayer.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ kind?: undefined; player?: null }|{ kind?: "player"; player: game.PublicPlayer.$Shape })
         * )} game.SeatOccupant.$Shape
         */

        /**
         * Constructs a new SeatOccupant.
         * @memberof game
         * @classdesc Represents a SeatOccupant.
         * @constructor
         * @param {game.SeatOccupant.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SeatOccupant = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SeatOccupant player.
         * @member {game.PublicPlayer.$Properties|null|undefined} player
         * @memberof game.SeatOccupant
         * @instance
         */
        SeatOccupant.prototype.player = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * SeatOccupant kind.
         * @member {"player"|undefined} kind
         * @memberof game.SeatOccupant
         * @instance
         */
        $Object.defineProperty(SeatOccupant.prototype, "kind", {
            get: $util.oneOfGetter($oneOfFields = ["player"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SeatOccupant instance using the specified properties.
         * @function create
         * @memberof game.SeatOccupant
         * @static
         * @param {game.SeatOccupant.$Properties=} [properties] Properties to set
         * @returns {game.SeatOccupant} SeatOccupant instance
         * @type {{
         *   (properties: game.SeatOccupant.$Shape): game.SeatOccupant & game.SeatOccupant.$Shape;
         *   (properties?: game.SeatOccupant.$Properties): game.SeatOccupant;
         * }}
         */
        SeatOccupant.create = function(properties) {
            return new SeatOccupant(properties);
        };

        /**
         * Encodes the specified SeatOccupant message. Does not implicitly {@link game.SeatOccupant.verify|verify} messages.
         * @function encode
         * @memberof game.SeatOccupant
         * @static
         * @param {game.SeatOccupant.$Properties} message SeatOccupant message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SeatOccupant.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.game.PublicPlayer.encode(message.player, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SeatOccupant message, length delimited. Does not implicitly {@link game.SeatOccupant.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SeatOccupant
         * @static
         * @param {game.SeatOccupant.$Properties} message SeatOccupant message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SeatOccupant.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SeatOccupant message from the specified reader or buffer.
         * @function decode
         * @memberof game.SeatOccupant
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SeatOccupant & game.SeatOccupant.$Shape} SeatOccupant
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SeatOccupant.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SeatOccupant();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.game.PublicPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        message.kind = "player";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SeatOccupant message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SeatOccupant
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SeatOccupant & game.SeatOccupant.$Shape} SeatOccupant
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SeatOccupant.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SeatOccupant message.
         * @function verify
         * @memberof game.SeatOccupant
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SeatOccupant.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                properties.kind = 1;
                {
                    let error = $root.game.PublicPlayer.verify(message.player, _depth + 1);
                    if (error)
                        return "player." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SeatOccupant message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SeatOccupant
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SeatOccupant} SeatOccupant
         */
        SeatOccupant.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SeatOccupant)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SeatOccupant: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SeatOccupant();
            if (object.player != null) {
                if (!$util.isObject(object.player))
                    throw $TypeError(".game.SeatOccupant.player: object expected");
                message.player = $root.game.PublicPlayer.fromObject(object.player, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SeatOccupant message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SeatOccupant
         * @static
         * @param {game.SeatOccupant} message SeatOccupant
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SeatOccupant.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                object.player = $root.game.PublicPlayer.toObject(message.player, options, _depth + 1);
                if (options.oneofs)
                    object.kind = "player";
            }
            return object;
        };

        /**
         * Converts this SeatOccupant to JSON.
         * @function toJSON
         * @memberof game.SeatOccupant
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SeatOccupant.prototype.toJSON = function() {
            return SeatOccupant.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SeatOccupant
         * @function getTypeUrl
         * @memberof game.SeatOccupant
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SeatOccupant.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SeatOccupant";
        };

        return SeatOccupant;
    })();

    game.ChatMessage = (function() {

        /**
         * Properties of a ChatMessage.
         * @typedef {Object} game.ChatMessage.$Properties
         * @property {string|null} [id] ChatMessage id
         * @property {string|null} [roomId] ChatMessage roomId
         * @property {string|null} [playerId] ChatMessage playerId
         * @property {string|null} [author] ChatMessage author
         * @property {string|null} [authorRole] ChatMessage authorRole
         * @property {string|null} [text] ChatMessage text
         * @property {number|Long|null} [at] ChatMessage at
         * @property {boolean|null} [system] ChatMessage system
         * @property {boolean|null} [transient] ChatMessage transient
         * @property {number|Long|null} [expiresAt] ChatMessage expiresAt
         * @property {Array.<string>|null} [mentions] ChatMessage mentions
         * @property {number|Long|null} [seq] ChatMessage seq
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ChatMessage.
         * @memberof game
         * @interface IChatMessage
         * @augments game.ChatMessage.$Properties
         * @deprecated Use game.ChatMessage.$Properties instead.
         */

        /**
         * Shape of a ChatMessage.
         * @typedef {game.ChatMessage.$Properties} game.ChatMessage.$Shape
         */

        /**
         * Constructs a new ChatMessage.
         * @memberof game
         * @classdesc Represents a ChatMessage.
         * @constructor
         * @param {game.ChatMessage.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ChatMessage = function (properties) {
            this.mentions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ChatMessage id.
         * @member {string} id
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.id = "";

        /**
         * ChatMessage roomId.
         * @member {string} roomId
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.roomId = "";

        /**
         * ChatMessage playerId.
         * @member {string} playerId
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.playerId = "";

        /**
         * ChatMessage author.
         * @member {string} author
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.author = "";

        /**
         * ChatMessage authorRole.
         * @member {string} authorRole
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.authorRole = "";

        /**
         * ChatMessage text.
         * @member {string} text
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.text = "";

        /**
         * ChatMessage at.
         * @member {number|Long} at
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.at = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChatMessage system.
         * @member {boolean} system
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.system = false;

        /**
         * ChatMessage transient.
         * @member {boolean} transient
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.transient = false;

        /**
         * ChatMessage expiresAt.
         * @member {number|Long} expiresAt
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.expiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChatMessage mentions.
         * @member {Array.<string>} mentions
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.mentions = $util.emptyArray;

        /**
         * ChatMessage seq.
         * @member {number|Long} seq
         * @memberof game.ChatMessage
         * @instance
         */
        ChatMessage.prototype.seq = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ChatMessage instance using the specified properties.
         * @function create
         * @memberof game.ChatMessage
         * @static
         * @param {game.ChatMessage.$Properties=} [properties] Properties to set
         * @returns {game.ChatMessage} ChatMessage instance
         * @type {{
         *   (properties: game.ChatMessage.$Shape): game.ChatMessage & game.ChatMessage.$Shape;
         *   (properties?: game.ChatMessage.$Properties): game.ChatMessage;
         * }}
         */
        ChatMessage.create = function(properties) {
            return new ChatMessage(properties);
        };

        /**
         * Encodes the specified ChatMessage message. Does not implicitly {@link game.ChatMessage.verify|verify} messages.
         * @function encode
         * @memberof game.ChatMessage
         * @static
         * @param {game.ChatMessage.$Properties} message ChatMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChatMessage.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId") && message.roomId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.roomId);
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId") && message.playerId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.playerId);
            if (message.author != null && $Object.hasOwnProperty.call(message, "author") && message.author !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.author);
            if (message.authorRole != null && $Object.hasOwnProperty.call(message, "authorRole") && message.authorRole !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.authorRole);
            if (message.text != null && $Object.hasOwnProperty.call(message, "text") && message.text !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.text);
            if (message.at != null && $Object.hasOwnProperty.call(message, "at") && (typeof message.at === "object" ? message.at.low || message.at.high : message.at !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.at);
            if (message.system != null && $Object.hasOwnProperty.call(message, "system") && message.system !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.system);
            if (message.transient != null && $Object.hasOwnProperty.call(message, "transient") && message.transient !== false)
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.transient);
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt") && (typeof message.expiresAt === "object" ? message.expiresAt.low || message.expiresAt.high : message.expiresAt !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.expiresAt);
            if (message.mentions != null && message.mentions.length)
                for (let i = 0; i < message.mentions.length; ++i)
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.mentions[i]);
            if (message.seq != null && $Object.hasOwnProperty.call(message, "seq") && (typeof message.seq === "object" ? message.seq.low || message.seq.high : message.seq !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.seq);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChatMessage message, length delimited. Does not implicitly {@link game.ChatMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.ChatMessage
         * @static
         * @param {game.ChatMessage.$Properties} message ChatMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChatMessage.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ChatMessage message from the specified reader or buffer.
         * @function decode
         * @memberof game.ChatMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.ChatMessage & game.ChatMessage.$Shape} ChatMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChatMessage.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.ChatMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.roomId = value;
                        else
                            delete message.roomId;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerId = value;
                        else
                            delete message.playerId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.author = value;
                        else
                            delete message.author;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.authorRole = value;
                        else
                            delete message.authorRole;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.text = value;
                        else
                            delete message.text;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.at = value;
                        else
                            delete message.at;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.system = value;
                        else
                            delete message.system;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.transient = value;
                        else
                            delete message.transient;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiresAt = value;
                        else
                            delete message.expiresAt;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.mentions && message.mentions.length))
                            message.mentions = [];
                        message.mentions.push(reader.stringVerify());
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.seq = value;
                        else
                            delete message.seq;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChatMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.ChatMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.ChatMessage & game.ChatMessage.$Shape} ChatMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChatMessage.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChatMessage message.
         * @function verify
         * @memberof game.ChatMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChatMessage.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                if (!$util.isString(message.roomId))
                    return "roomId: string expected";
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                if (!$util.isString(message.playerId))
                    return "playerId: string expected";
            if (message.author != null && $Object.hasOwnProperty.call(message, "author"))
                if (!$util.isString(message.author))
                    return "author: string expected";
            if (message.authorRole != null && $Object.hasOwnProperty.call(message, "authorRole"))
                if (!$util.isString(message.authorRole))
                    return "authorRole: string expected";
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                if (!$util.isString(message.text))
                    return "text: string expected";
            if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                if (!$util.isInteger(message.at) && !(message.at && $util.isInteger(message.at.low) && $util.isInteger(message.at.high)))
                    return "at: integer|Long expected";
            if (message.system != null && $Object.hasOwnProperty.call(message, "system"))
                if (typeof message.system !== "boolean")
                    return "system: boolean expected";
            if (message.transient != null && $Object.hasOwnProperty.call(message, "transient"))
                if (typeof message.transient !== "boolean")
                    return "transient: boolean expected";
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (!$util.isInteger(message.expiresAt) && !(message.expiresAt && $util.isInteger(message.expiresAt.low) && $util.isInteger(message.expiresAt.high)))
                    return "expiresAt: integer|Long expected";
            if (message.mentions != null && $Object.hasOwnProperty.call(message, "mentions")) {
                if (!$Array.isArray(message.mentions))
                    return "mentions: array expected";
                for (let i = 0; i < message.mentions.length; ++i)
                    if (!$util.isString(message.mentions[i]))
                        return "mentions: string[] expected";
            }
            if (message.seq != null && $Object.hasOwnProperty.call(message, "seq"))
                if (!$util.isInteger(message.seq) && !(message.seq && $util.isInteger(message.seq.low) && $util.isInteger(message.seq.high)))
                    return "seq: integer|Long expected";
            return null;
        };

        /**
         * Creates a ChatMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.ChatMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.ChatMessage} ChatMessage
         */
        ChatMessage.fromObject = function (object, _depth) {
            if (object instanceof $root.game.ChatMessage)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.ChatMessage: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.ChatMessage();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.roomId != null)
                if (typeof object.roomId !== "string" || object.roomId.length)
                    message.roomId = $String(object.roomId);
            if (object.playerId != null)
                if (typeof object.playerId !== "string" || object.playerId.length)
                    message.playerId = $String(object.playerId);
            if (object.author != null)
                if (typeof object.author !== "string" || object.author.length)
                    message.author = $String(object.author);
            if (object.authorRole != null)
                if (typeof object.authorRole !== "string" || object.authorRole.length)
                    message.authorRole = $String(object.authorRole);
            if (object.text != null)
                if (typeof object.text !== "string" || object.text.length)
                    message.text = $String(object.text);
            if (object.at != null)
                if (typeof object.at === "object" ? object.at.low || object.at.high : $Number(object.at) !== 0)
                    if ($util.Long)
                        message.at = $util.Long.fromValue(object.at, false);
                    else if (typeof object.at === "string")
                        message.at = $parseInt(object.at, 10);
                    else if (typeof object.at === "number")
                        message.at = object.at;
                    else if (typeof object.at === "object")
                        message.at = new $util.LongBits(object.at.low >>> 0, object.at.high >>> 0).toNumber();
            if (object.system != null)
                if (object.system)
                    message.system = $Boolean(object.system);
            if (object.transient != null)
                if (object.transient)
                    message.transient = $Boolean(object.transient);
            if (object.expiresAt != null)
                if (typeof object.expiresAt === "object" ? object.expiresAt.low || object.expiresAt.high : $Number(object.expiresAt) !== 0)
                    if ($util.Long)
                        message.expiresAt = $util.Long.fromValue(object.expiresAt, false);
                    else if (typeof object.expiresAt === "string")
                        message.expiresAt = $parseInt(object.expiresAt, 10);
                    else if (typeof object.expiresAt === "number")
                        message.expiresAt = object.expiresAt;
                    else if (typeof object.expiresAt === "object")
                        message.expiresAt = new $util.LongBits(object.expiresAt.low >>> 0, object.expiresAt.high >>> 0).toNumber();
            if (object.mentions) {
                if (!$Array.isArray(object.mentions))
                    throw $TypeError(".game.ChatMessage.mentions: array expected");
                message.mentions = $Array(object.mentions.length);
                for (let i = 0; i < object.mentions.length; ++i)
                    message.mentions[i] = $String(object.mentions[i]);
            }
            if (object.seq != null)
                if (typeof object.seq === "object" ? object.seq.low || object.seq.high : $Number(object.seq) !== 0)
                    if ($util.Long)
                        message.seq = $util.Long.fromValue(object.seq, false);
                    else if (typeof object.seq === "string")
                        message.seq = $parseInt(object.seq, 10);
                    else if (typeof object.seq === "number")
                        message.seq = object.seq;
                    else if (typeof object.seq === "object")
                        message.seq = new $util.LongBits(object.seq.low >>> 0, object.seq.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ChatMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.ChatMessage
         * @static
         * @param {game.ChatMessage} message ChatMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChatMessage.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.mentions = [];
            if (options.defaults) {
                object.id = "";
                object.roomId = "";
                object.playerId = "";
                object.author = "";
                object.authorRole = "";
                object.text = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.at = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.at = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.system = false;
                object.transient = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.seq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.seq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                object.roomId = message.roomId;
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                object.playerId = message.playerId;
            if (message.author != null && $Object.hasOwnProperty.call(message, "author"))
                object.author = message.author;
            if (message.authorRole != null && $Object.hasOwnProperty.call(message, "authorRole"))
                object.authorRole = message.authorRole;
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.at = typeof message.at === "number" ? $BigInt(message.at) : $util.Long.fromBits(message.at.low >>> 0, message.at.high >>> 0, false).toBigInt();
                else if (typeof message.at === "number")
                    object.at = options.longs === $String ? $String(message.at) : message.at;
                else
                    object.at = options.longs === $String ? $util.Long.prototype.toString.call(message.at) : options.longs === $Number ? new $util.LongBits(message.at.low >>> 0, message.at.high >>> 0).toNumber() : message.at;
            if (message.system != null && $Object.hasOwnProperty.call(message, "system"))
                object.system = message.system;
            if (message.transient != null && $Object.hasOwnProperty.call(message, "transient"))
                object.transient = message.transient;
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiresAt = typeof message.expiresAt === "number" ? $BigInt(message.expiresAt) : $util.Long.fromBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.expiresAt === "number")
                    object.expiresAt = options.longs === $String ? $String(message.expiresAt) : message.expiresAt;
                else
                    object.expiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.expiresAt) : options.longs === $Number ? new $util.LongBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0).toNumber() : message.expiresAt;
            if (message.mentions && message.mentions.length) {
                object.mentions = $Array(message.mentions.length);
                for (let j = 0; j < message.mentions.length; ++j)
                    object.mentions[j] = message.mentions[j];
            }
            if (message.seq != null && $Object.hasOwnProperty.call(message, "seq"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.seq = typeof message.seq === "number" ? $BigInt(message.seq) : $util.Long.fromBits(message.seq.low >>> 0, message.seq.high >>> 0, false).toBigInt();
                else if (typeof message.seq === "number")
                    object.seq = options.longs === $String ? $String(message.seq) : message.seq;
                else
                    object.seq = options.longs === $String ? $util.Long.prototype.toString.call(message.seq) : options.longs === $Number ? new $util.LongBits(message.seq.low >>> 0, message.seq.high >>> 0).toNumber() : message.seq;
            return object;
        };

        /**
         * Converts this ChatMessage to JSON.
         * @function toJSON
         * @memberof game.ChatMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChatMessage.prototype.toJSON = function() {
            return ChatMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ChatMessage
         * @function getTypeUrl
         * @memberof game.ChatMessage
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ChatMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.ChatMessage";
        };

        return ChatMessage;
    })();

    game.Suggestion = (function() {

        /**
         * Properties of a Suggestion.
         * @typedef {Object} game.Suggestion.$Properties
         * @property {string|null} [id] Suggestion id
         * @property {string|null} [playerId] Suggestion playerId
         * @property {string|null} [author] Suggestion author
         * @property {game.PublicPlayer.$Properties|null} [authorPlayer] Suggestion authorPlayer
         * @property {string|null} [text] Suggestion text
         * @property {number|Long|null} [at] Suggestion at
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Suggestion.
         * @memberof game
         * @interface ISuggestion
         * @augments game.Suggestion.$Properties
         * @deprecated Use game.Suggestion.$Properties instead.
         */

        /**
         * Shape of a Suggestion.
         * @typedef {game.Suggestion.$Properties} game.Suggestion.$Shape
         */

        /**
         * Constructs a new Suggestion.
         * @memberof game
         * @classdesc Represents a Suggestion.
         * @constructor
         * @param {game.Suggestion.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Suggestion = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Suggestion id.
         * @member {string} id
         * @memberof game.Suggestion
         * @instance
         */
        Suggestion.prototype.id = "";

        /**
         * Suggestion playerId.
         * @member {string} playerId
         * @memberof game.Suggestion
         * @instance
         */
        Suggestion.prototype.playerId = "";

        /**
         * Suggestion author.
         * @member {string} author
         * @memberof game.Suggestion
         * @instance
         */
        Suggestion.prototype.author = "";

        /**
         * Suggestion authorPlayer.
         * @member {game.PublicPlayer.$Properties|null|undefined} authorPlayer
         * @memberof game.Suggestion
         * @instance
         */
        Suggestion.prototype.authorPlayer = null;

        /**
         * Suggestion text.
         * @member {string} text
         * @memberof game.Suggestion
         * @instance
         */
        Suggestion.prototype.text = "";

        /**
         * Suggestion at.
         * @member {number|Long} at
         * @memberof game.Suggestion
         * @instance
         */
        Suggestion.prototype.at = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Suggestion instance using the specified properties.
         * @function create
         * @memberof game.Suggestion
         * @static
         * @param {game.Suggestion.$Properties=} [properties] Properties to set
         * @returns {game.Suggestion} Suggestion instance
         * @type {{
         *   (properties: game.Suggestion.$Shape): game.Suggestion & game.Suggestion.$Shape;
         *   (properties?: game.Suggestion.$Properties): game.Suggestion;
         * }}
         */
        Suggestion.create = function(properties) {
            return new Suggestion(properties);
        };

        /**
         * Encodes the specified Suggestion message. Does not implicitly {@link game.Suggestion.verify|verify} messages.
         * @function encode
         * @memberof game.Suggestion
         * @static
         * @param {game.Suggestion.$Properties} message Suggestion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Suggestion.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId") && message.playerId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.playerId);
            if (message.author != null && $Object.hasOwnProperty.call(message, "author") && message.author !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.author);
            if (message.authorPlayer != null && $Object.hasOwnProperty.call(message, "authorPlayer"))
                $root.game.PublicPlayer.encode(message.authorPlayer, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.text != null && $Object.hasOwnProperty.call(message, "text") && message.text !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.text);
            if (message.at != null && $Object.hasOwnProperty.call(message, "at") && (typeof message.at === "object" ? message.at.low || message.at.high : message.at !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.at);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Suggestion message, length delimited. Does not implicitly {@link game.Suggestion.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Suggestion
         * @static
         * @param {game.Suggestion.$Properties} message Suggestion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Suggestion.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Suggestion message from the specified reader or buffer.
         * @function decode
         * @memberof game.Suggestion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Suggestion & game.Suggestion.$Shape} Suggestion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Suggestion.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Suggestion(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerId = value;
                        else
                            delete message.playerId;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.author = value;
                        else
                            delete message.author;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.authorPlayer = $root.game.PublicPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.authorPlayer);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.text = value;
                        else
                            delete message.text;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.at = value;
                        else
                            delete message.at;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Suggestion message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Suggestion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Suggestion & game.Suggestion.$Shape} Suggestion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Suggestion.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Suggestion message.
         * @function verify
         * @memberof game.Suggestion
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Suggestion.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                if (!$util.isString(message.playerId))
                    return "playerId: string expected";
            if (message.author != null && $Object.hasOwnProperty.call(message, "author"))
                if (!$util.isString(message.author))
                    return "author: string expected";
            if (message.authorPlayer != null && $Object.hasOwnProperty.call(message, "authorPlayer")) {
                let error = $root.game.PublicPlayer.verify(message.authorPlayer, _depth + 1);
                if (error)
                    return "authorPlayer." + error;
            }
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                if (!$util.isString(message.text))
                    return "text: string expected";
            if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                if (!$util.isInteger(message.at) && !(message.at && $util.isInteger(message.at.low) && $util.isInteger(message.at.high)))
                    return "at: integer|Long expected";
            return null;
        };

        /**
         * Creates a Suggestion message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Suggestion
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Suggestion} Suggestion
         */
        Suggestion.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Suggestion)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Suggestion: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Suggestion();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.playerId != null)
                if (typeof object.playerId !== "string" || object.playerId.length)
                    message.playerId = $String(object.playerId);
            if (object.author != null)
                if (typeof object.author !== "string" || object.author.length)
                    message.author = $String(object.author);
            if (object.authorPlayer != null) {
                if (!$util.isObject(object.authorPlayer))
                    throw $TypeError(".game.Suggestion.authorPlayer: object expected");
                message.authorPlayer = $root.game.PublicPlayer.fromObject(object.authorPlayer, _depth + 1);
            }
            if (object.text != null)
                if (typeof object.text !== "string" || object.text.length)
                    message.text = $String(object.text);
            if (object.at != null)
                if (typeof object.at === "object" ? object.at.low || object.at.high : $Number(object.at) !== 0)
                    if ($util.Long)
                        message.at = $util.Long.fromValue(object.at, false);
                    else if (typeof object.at === "string")
                        message.at = $parseInt(object.at, 10);
                    else if (typeof object.at === "number")
                        message.at = object.at;
                    else if (typeof object.at === "object")
                        message.at = new $util.LongBits(object.at.low >>> 0, object.at.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a Suggestion message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Suggestion
         * @static
         * @param {game.Suggestion} message Suggestion
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Suggestion.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.playerId = "";
                object.author = "";
                object.authorPlayer = null;
                object.text = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.at = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.at = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                object.playerId = message.playerId;
            if (message.author != null && $Object.hasOwnProperty.call(message, "author"))
                object.author = message.author;
            if (message.authorPlayer != null && $Object.hasOwnProperty.call(message, "authorPlayer"))
                object.authorPlayer = $root.game.PublicPlayer.toObject(message.authorPlayer, options, _depth + 1);
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.at = typeof message.at === "number" ? $BigInt(message.at) : $util.Long.fromBits(message.at.low >>> 0, message.at.high >>> 0, false).toBigInt();
                else if (typeof message.at === "number")
                    object.at = options.longs === $String ? $String(message.at) : message.at;
                else
                    object.at = options.longs === $String ? $util.Long.prototype.toString.call(message.at) : options.longs === $Number ? new $util.LongBits(message.at.low >>> 0, message.at.high >>> 0).toNumber() : message.at;
            return object;
        };

        /**
         * Converts this Suggestion to JSON.
         * @function toJSON
         * @memberof game.Suggestion
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Suggestion.prototype.toJSON = function() {
            return Suggestion.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Suggestion
         * @function getTypeUrl
         * @memberof game.Suggestion
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Suggestion.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Suggestion";
        };

        return Suggestion;
    })();

    game.RoomSettings = (function() {

        /**
         * Properties of a RoomSettings.
         * @typedef {Object} game.RoomSettings.$Properties
         * @property {string|null} [name] RoomSettings name
         * @property {string|null} [password] RoomSettings password
         * @property {string|null} [gameId] RoomSettings gameId
         * @property {boolean|null} [enablePunishment] RoomSettings enablePunishment
         * @property {string|null} [punishmentSource] RoomSettings punishmentSource
         * @property {string|null} [punishmentId] RoomSettings punishmentId
         * @property {Array.<string>|null} [punishmentIds] RoomSettings punishmentIds
         * @property {string|null} [roomBackgroundImage] RoomSettings roomBackgroundImage
         * @property {boolean|null} [enableTags] RoomSettings enableTags
         * @property {Array.<string>|null} [tags] RoomSettings tags
         * @property {boolean|null} [allowProofImage] RoomSettings allowProofImage
         * @property {boolean|null} [tieDoublePunish] RoomSettings tieDoublePunish
         * @property {boolean|null} [requireOpponentConfirm] RoomSettings requireOpponentConfirm
         * @property {boolean|null} [enableRanked] RoomSettings enableRanked
         * @property {number|null} [stake] RoomSettings stake
         * @property {boolean|null} [enableRankMultiplier] RoomSettings enableRankMultiplier
         * @property {number|null} [rankMultiplier] RoomSettings rankMultiplier
         * @property {boolean|null} [enableExtremeRanked] RoomSettings enableExtremeRanked
         * @property {string|null} [othelloBoardTheme] RoomSettings othelloBoardTheme
         * @property {string|null} [tictactoeBoardTheme] RoomSettings tictactoeBoardTheme
         * @property {number|null} [liarsDiceMinPlayers] RoomSettings liarsDiceMinPlayers
         * @property {number|null} [liarsDiceMaxPlayers] RoomSettings liarsDiceMaxPlayers
         * @property {string|null} [gomokuBoardTheme] RoomSettings gomokuBoardTheme
         * @property {number|null} [gomokuUndoLimit] RoomSettings gomokuUndoLimit
         * @property {number|null} [othelloMoveSeconds] RoomSettings othelloMoveSeconds
         * @property {number|null} [othelloGameMinutes] RoomSettings othelloGameMinutes
         * @property {number|null} [gomokuMoveSeconds] RoomSettings gomokuMoveSeconds
         * @property {number|null} [gomokuGameMinutes] RoomSettings gomokuGameMinutes
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomSettings.
         * @memberof game
         * @interface IRoomSettings
         * @augments game.RoomSettings.$Properties
         * @deprecated Use game.RoomSettings.$Properties instead.
         */

        /**
         * Shape of a RoomSettings.
         * @typedef {game.RoomSettings.$Properties} game.RoomSettings.$Shape
         */

        /**
         * Constructs a new RoomSettings.
         * @memberof game
         * @classdesc Represents a RoomSettings.
         * @constructor
         * @param {game.RoomSettings.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomSettings = function (properties) {
            this.punishmentIds = [];
            this.tags = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomSettings name.
         * @member {string} name
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.name = "";

        /**
         * RoomSettings password.
         * @member {string} password
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.password = "";

        /**
         * RoomSettings gameId.
         * @member {string} gameId
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.gameId = "";

        /**
         * RoomSettings enablePunishment.
         * @member {boolean} enablePunishment
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.enablePunishment = false;

        /**
         * RoomSettings punishmentSource.
         * @member {string} punishmentSource
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.punishmentSource = "";

        /**
         * RoomSettings punishmentId.
         * @member {string} punishmentId
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.punishmentId = "";

        /**
         * RoomSettings punishmentIds.
         * @member {Array.<string>} punishmentIds
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.punishmentIds = $util.emptyArray;

        /**
         * RoomSettings roomBackgroundImage.
         * @member {string} roomBackgroundImage
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.roomBackgroundImage = "";

        /**
         * RoomSettings enableTags.
         * @member {boolean} enableTags
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.enableTags = false;

        /**
         * RoomSettings tags.
         * @member {Array.<string>} tags
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.tags = $util.emptyArray;

        /**
         * RoomSettings allowProofImage.
         * @member {boolean} allowProofImage
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.allowProofImage = false;

        /**
         * RoomSettings tieDoublePunish.
         * @member {boolean} tieDoublePunish
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.tieDoublePunish = false;

        /**
         * RoomSettings requireOpponentConfirm.
         * @member {boolean} requireOpponentConfirm
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.requireOpponentConfirm = false;

        /**
         * RoomSettings enableRanked.
         * @member {boolean} enableRanked
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.enableRanked = false;

        /**
         * RoomSettings stake.
         * @member {number} stake
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.stake = 0;

        /**
         * RoomSettings enableRankMultiplier.
         * @member {boolean} enableRankMultiplier
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.enableRankMultiplier = false;

        /**
         * RoomSettings rankMultiplier.
         * @member {number} rankMultiplier
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.rankMultiplier = 0;

        /**
         * RoomSettings enableExtremeRanked.
         * @member {boolean} enableExtremeRanked
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.enableExtremeRanked = false;

        /**
         * RoomSettings othelloBoardTheme.
         * @member {string} othelloBoardTheme
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.othelloBoardTheme = "";

        /**
         * RoomSettings tictactoeBoardTheme.
         * @member {string} tictactoeBoardTheme
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.tictactoeBoardTheme = "";

        /**
         * RoomSettings liarsDiceMinPlayers.
         * @member {number} liarsDiceMinPlayers
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.liarsDiceMinPlayers = 0;

        /**
         * RoomSettings liarsDiceMaxPlayers.
         * @member {number} liarsDiceMaxPlayers
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.liarsDiceMaxPlayers = 0;

        /**
         * RoomSettings gomokuBoardTheme.
         * @member {string} gomokuBoardTheme
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.gomokuBoardTheme = "";

        /**
         * RoomSettings gomokuUndoLimit.
         * @member {number} gomokuUndoLimit
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.gomokuUndoLimit = 0;

        /**
         * RoomSettings othelloMoveSeconds.
         * @member {number} othelloMoveSeconds
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.othelloMoveSeconds = 0;

        /**
         * RoomSettings othelloGameMinutes.
         * @member {number} othelloGameMinutes
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.othelloGameMinutes = 0;

        /**
         * RoomSettings gomokuMoveSeconds.
         * @member {number} gomokuMoveSeconds
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.gomokuMoveSeconds = 0;

        /**
         * RoomSettings gomokuGameMinutes.
         * @member {number} gomokuGameMinutes
         * @memberof game.RoomSettings
         * @instance
         */
        RoomSettings.prototype.gomokuGameMinutes = 0;

        /**
         * Creates a new RoomSettings instance using the specified properties.
         * @function create
         * @memberof game.RoomSettings
         * @static
         * @param {game.RoomSettings.$Properties=} [properties] Properties to set
         * @returns {game.RoomSettings} RoomSettings instance
         * @type {{
         *   (properties: game.RoomSettings.$Shape): game.RoomSettings & game.RoomSettings.$Shape;
         *   (properties?: game.RoomSettings.$Properties): game.RoomSettings;
         * }}
         */
        RoomSettings.create = function(properties) {
            return new RoomSettings(properties);
        };

        /**
         * Encodes the specified RoomSettings message. Does not implicitly {@link game.RoomSettings.verify|verify} messages.
         * @function encode
         * @memberof game.RoomSettings
         * @static
         * @param {game.RoomSettings.$Properties} message RoomSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomSettings.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.password);
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId") && message.gameId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.gameId);
            if (message.enablePunishment != null && $Object.hasOwnProperty.call(message, "enablePunishment") && message.enablePunishment !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.enablePunishment);
            if (message.punishmentSource != null && $Object.hasOwnProperty.call(message, "punishmentSource") && message.punishmentSource !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.punishmentSource);
            if (message.punishmentId != null && $Object.hasOwnProperty.call(message, "punishmentId") && message.punishmentId !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.punishmentId);
            if (message.punishmentIds != null && message.punishmentIds.length)
                for (let i = 0; i < message.punishmentIds.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.punishmentIds[i]);
            if (message.roomBackgroundImage != null && $Object.hasOwnProperty.call(message, "roomBackgroundImage") && message.roomBackgroundImage !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.roomBackgroundImage);
            if (message.enableTags != null && $Object.hasOwnProperty.call(message, "enableTags") && message.enableTags !== false)
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.enableTags);
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.tags[i]);
            if (message.allowProofImage != null && $Object.hasOwnProperty.call(message, "allowProofImage") && message.allowProofImage !== false)
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.allowProofImage);
            if (message.tieDoublePunish != null && $Object.hasOwnProperty.call(message, "tieDoublePunish") && message.tieDoublePunish !== false)
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.tieDoublePunish);
            if (message.requireOpponentConfirm != null && $Object.hasOwnProperty.call(message, "requireOpponentConfirm") && message.requireOpponentConfirm !== false)
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.requireOpponentConfirm);
            if (message.enableRanked != null && $Object.hasOwnProperty.call(message, "enableRanked") && message.enableRanked !== false)
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.enableRanked);
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake") && message.stake !== 0)
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.stake);
            if (message.enableRankMultiplier != null && $Object.hasOwnProperty.call(message, "enableRankMultiplier") && message.enableRankMultiplier !== false)
                writer.uint32(/* id 19, wireType 0 =*/152).bool(message.enableRankMultiplier);
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier") && message.rankMultiplier !== 0)
                writer.uint32(/* id 20, wireType 0 =*/160).int32(message.rankMultiplier);
            if (message.enableExtremeRanked != null && $Object.hasOwnProperty.call(message, "enableExtremeRanked") && message.enableExtremeRanked !== false)
                writer.uint32(/* id 21, wireType 0 =*/168).bool(message.enableExtremeRanked);
            if (message.othelloBoardTheme != null && $Object.hasOwnProperty.call(message, "othelloBoardTheme") && message.othelloBoardTheme !== "")
                writer.uint32(/* id 22, wireType 2 =*/178).string(message.othelloBoardTheme);
            if (message.tictactoeBoardTheme != null && $Object.hasOwnProperty.call(message, "tictactoeBoardTheme") && message.tictactoeBoardTheme !== "")
                writer.uint32(/* id 23, wireType 2 =*/186).string(message.tictactoeBoardTheme);
            if (message.liarsDiceMinPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMinPlayers") && message.liarsDiceMinPlayers !== 0)
                writer.uint32(/* id 24, wireType 0 =*/192).int32(message.liarsDiceMinPlayers);
            if (message.liarsDiceMaxPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMaxPlayers") && message.liarsDiceMaxPlayers !== 0)
                writer.uint32(/* id 25, wireType 0 =*/200).int32(message.liarsDiceMaxPlayers);
            if (message.gomokuBoardTheme != null && $Object.hasOwnProperty.call(message, "gomokuBoardTheme") && message.gomokuBoardTheme !== "")
                writer.uint32(/* id 26, wireType 2 =*/210).string(message.gomokuBoardTheme);
            if (message.gomokuUndoLimit != null && $Object.hasOwnProperty.call(message, "gomokuUndoLimit") && message.gomokuUndoLimit !== 0)
                writer.uint32(/* id 27, wireType 0 =*/216).int32(message.gomokuUndoLimit);
            if (message.othelloMoveSeconds != null && $Object.hasOwnProperty.call(message, "othelloMoveSeconds") && message.othelloMoveSeconds !== 0)
                writer.uint32(/* id 28, wireType 0 =*/224).int32(message.othelloMoveSeconds);
            if (message.othelloGameMinutes != null && $Object.hasOwnProperty.call(message, "othelloGameMinutes") && message.othelloGameMinutes !== 0)
                writer.uint32(/* id 29, wireType 0 =*/232).int32(message.othelloGameMinutes);
            if (message.gomokuMoveSeconds != null && $Object.hasOwnProperty.call(message, "gomokuMoveSeconds") && message.gomokuMoveSeconds !== 0)
                writer.uint32(/* id 30, wireType 0 =*/240).int32(message.gomokuMoveSeconds);
            if (message.gomokuGameMinutes != null && $Object.hasOwnProperty.call(message, "gomokuGameMinutes") && message.gomokuGameMinutes !== 0)
                writer.uint32(/* id 31, wireType 0 =*/248).int32(message.gomokuGameMinutes);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoomSettings message, length delimited. Does not implicitly {@link game.RoomSettings.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RoomSettings
         * @static
         * @param {game.RoomSettings.$Properties} message RoomSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomSettings.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RoomSettings message from the specified reader or buffer.
         * @function decode
         * @memberof game.RoomSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RoomSettings & game.RoomSettings.$Shape} RoomSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomSettings.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RoomSettings(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.gameId = value;
                        else
                            delete message.gameId;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enablePunishment = value;
                        else
                            delete message.enablePunishment;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.punishmentSource = value;
                        else
                            delete message.punishmentSource;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.punishmentId = value;
                        else
                            delete message.punishmentId;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.punishmentIds && message.punishmentIds.length))
                            message.punishmentIds = [];
                        message.punishmentIds.push(reader.stringVerify());
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.roomBackgroundImage = value;
                        else
                            delete message.roomBackgroundImage;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enableTags = value;
                        else
                            delete message.enableTags;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.stringVerify());
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.allowProofImage = value;
                        else
                            delete message.allowProofImage;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.tieDoublePunish = value;
                        else
                            delete message.tieDoublePunish;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.requireOpponentConfirm = value;
                        else
                            delete message.requireOpponentConfirm;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enableRanked = value;
                        else
                            delete message.enableRanked;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.stake = value;
                        else
                            delete message.stake;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enableRankMultiplier = value;
                        else
                            delete message.enableRankMultiplier;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.rankMultiplier = value;
                        else
                            delete message.rankMultiplier;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enableExtremeRanked = value;
                        else
                            delete message.enableExtremeRanked;
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.othelloBoardTheme = value;
                        else
                            delete message.othelloBoardTheme;
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.tictactoeBoardTheme = value;
                        else
                            delete message.tictactoeBoardTheme;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.liarsDiceMinPlayers = value;
                        else
                            delete message.liarsDiceMinPlayers;
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.liarsDiceMaxPlayers = value;
                        else
                            delete message.liarsDiceMaxPlayers;
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.gomokuBoardTheme = value;
                        else
                            delete message.gomokuBoardTheme;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.gomokuUndoLimit = value;
                        else
                            delete message.gomokuUndoLimit;
                        continue;
                    }
                case 28: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.othelloMoveSeconds = value;
                        else
                            delete message.othelloMoveSeconds;
                        continue;
                    }
                case 29: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.othelloGameMinutes = value;
                        else
                            delete message.othelloGameMinutes;
                        continue;
                    }
                case 30: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.gomokuMoveSeconds = value;
                        else
                            delete message.gomokuMoveSeconds;
                        continue;
                    }
                case 31: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.gomokuGameMinutes = value;
                        else
                            delete message.gomokuGameMinutes;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoomSettings message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RoomSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RoomSettings & game.RoomSettings.$Shape} RoomSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomSettings.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoomSettings message.
         * @function verify
         * @memberof game.RoomSettings
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomSettings.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId"))
                if (!$util.isString(message.gameId))
                    return "gameId: string expected";
            if (message.enablePunishment != null && $Object.hasOwnProperty.call(message, "enablePunishment"))
                if (typeof message.enablePunishment !== "boolean")
                    return "enablePunishment: boolean expected";
            if (message.punishmentSource != null && $Object.hasOwnProperty.call(message, "punishmentSource"))
                if (!$util.isString(message.punishmentSource))
                    return "punishmentSource: string expected";
            if (message.punishmentId != null && $Object.hasOwnProperty.call(message, "punishmentId"))
                if (!$util.isString(message.punishmentId))
                    return "punishmentId: string expected";
            if (message.punishmentIds != null && $Object.hasOwnProperty.call(message, "punishmentIds")) {
                if (!$Array.isArray(message.punishmentIds))
                    return "punishmentIds: array expected";
                for (let i = 0; i < message.punishmentIds.length; ++i)
                    if (!$util.isString(message.punishmentIds[i]))
                        return "punishmentIds: string[] expected";
            }
            if (message.roomBackgroundImage != null && $Object.hasOwnProperty.call(message, "roomBackgroundImage"))
                if (!$util.isString(message.roomBackgroundImage))
                    return "roomBackgroundImage: string expected";
            if (message.enableTags != null && $Object.hasOwnProperty.call(message, "enableTags"))
                if (typeof message.enableTags !== "boolean")
                    return "enableTags: boolean expected";
            if (message.tags != null && $Object.hasOwnProperty.call(message, "tags")) {
                if (!$Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.allowProofImage != null && $Object.hasOwnProperty.call(message, "allowProofImage"))
                if (typeof message.allowProofImage !== "boolean")
                    return "allowProofImage: boolean expected";
            if (message.tieDoublePunish != null && $Object.hasOwnProperty.call(message, "tieDoublePunish"))
                if (typeof message.tieDoublePunish !== "boolean")
                    return "tieDoublePunish: boolean expected";
            if (message.requireOpponentConfirm != null && $Object.hasOwnProperty.call(message, "requireOpponentConfirm"))
                if (typeof message.requireOpponentConfirm !== "boolean")
                    return "requireOpponentConfirm: boolean expected";
            if (message.enableRanked != null && $Object.hasOwnProperty.call(message, "enableRanked"))
                if (typeof message.enableRanked !== "boolean")
                    return "enableRanked: boolean expected";
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (!$util.isInteger(message.stake))
                    return "stake: integer expected";
            if (message.enableRankMultiplier != null && $Object.hasOwnProperty.call(message, "enableRankMultiplier"))
                if (typeof message.enableRankMultiplier !== "boolean")
                    return "enableRankMultiplier: boolean expected";
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier"))
                if (!$util.isInteger(message.rankMultiplier))
                    return "rankMultiplier: integer expected";
            if (message.enableExtremeRanked != null && $Object.hasOwnProperty.call(message, "enableExtremeRanked"))
                if (typeof message.enableExtremeRanked !== "boolean")
                    return "enableExtremeRanked: boolean expected";
            if (message.othelloBoardTheme != null && $Object.hasOwnProperty.call(message, "othelloBoardTheme"))
                if (!$util.isString(message.othelloBoardTheme))
                    return "othelloBoardTheme: string expected";
            if (message.tictactoeBoardTheme != null && $Object.hasOwnProperty.call(message, "tictactoeBoardTheme"))
                if (!$util.isString(message.tictactoeBoardTheme))
                    return "tictactoeBoardTheme: string expected";
            if (message.liarsDiceMinPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMinPlayers"))
                if (!$util.isInteger(message.liarsDiceMinPlayers))
                    return "liarsDiceMinPlayers: integer expected";
            if (message.liarsDiceMaxPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMaxPlayers"))
                if (!$util.isInteger(message.liarsDiceMaxPlayers))
                    return "liarsDiceMaxPlayers: integer expected";
            if (message.gomokuBoardTheme != null && $Object.hasOwnProperty.call(message, "gomokuBoardTheme"))
                if (!$util.isString(message.gomokuBoardTheme))
                    return "gomokuBoardTheme: string expected";
            if (message.gomokuUndoLimit != null && $Object.hasOwnProperty.call(message, "gomokuUndoLimit"))
                if (!$util.isInteger(message.gomokuUndoLimit))
                    return "gomokuUndoLimit: integer expected";
            if (message.othelloMoveSeconds != null && $Object.hasOwnProperty.call(message, "othelloMoveSeconds"))
                if (!$util.isInteger(message.othelloMoveSeconds))
                    return "othelloMoveSeconds: integer expected";
            if (message.othelloGameMinutes != null && $Object.hasOwnProperty.call(message, "othelloGameMinutes"))
                if (!$util.isInteger(message.othelloGameMinutes))
                    return "othelloGameMinutes: integer expected";
            if (message.gomokuMoveSeconds != null && $Object.hasOwnProperty.call(message, "gomokuMoveSeconds"))
                if (!$util.isInteger(message.gomokuMoveSeconds))
                    return "gomokuMoveSeconds: integer expected";
            if (message.gomokuGameMinutes != null && $Object.hasOwnProperty.call(message, "gomokuGameMinutes"))
                if (!$util.isInteger(message.gomokuGameMinutes))
                    return "gomokuGameMinutes: integer expected";
            return null;
        };

        /**
         * Creates a RoomSettings message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RoomSettings
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RoomSettings} RoomSettings
         */
        RoomSettings.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RoomSettings)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RoomSettings: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RoomSettings();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = $String(object.password);
            if (object.gameId != null)
                if (typeof object.gameId !== "string" || object.gameId.length)
                    message.gameId = $String(object.gameId);
            if (object.enablePunishment != null)
                if (object.enablePunishment)
                    message.enablePunishment = $Boolean(object.enablePunishment);
            if (object.punishmentSource != null)
                if (typeof object.punishmentSource !== "string" || object.punishmentSource.length)
                    message.punishmentSource = $String(object.punishmentSource);
            if (object.punishmentId != null)
                if (typeof object.punishmentId !== "string" || object.punishmentId.length)
                    message.punishmentId = $String(object.punishmentId);
            if (object.punishmentIds) {
                if (!$Array.isArray(object.punishmentIds))
                    throw $TypeError(".game.RoomSettings.punishmentIds: array expected");
                message.punishmentIds = $Array(object.punishmentIds.length);
                for (let i = 0; i < object.punishmentIds.length; ++i)
                    message.punishmentIds[i] = $String(object.punishmentIds[i]);
            }
            if (object.roomBackgroundImage != null)
                if (typeof object.roomBackgroundImage !== "string" || object.roomBackgroundImage.length)
                    message.roomBackgroundImage = $String(object.roomBackgroundImage);
            if (object.enableTags != null)
                if (object.enableTags)
                    message.enableTags = $Boolean(object.enableTags);
            if (object.tags) {
                if (!$Array.isArray(object.tags))
                    throw $TypeError(".game.RoomSettings.tags: array expected");
                message.tags = $Array(object.tags.length);
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = $String(object.tags[i]);
            }
            if (object.allowProofImage != null)
                if (object.allowProofImage)
                    message.allowProofImage = $Boolean(object.allowProofImage);
            if (object.tieDoublePunish != null)
                if (object.tieDoublePunish)
                    message.tieDoublePunish = $Boolean(object.tieDoublePunish);
            if (object.requireOpponentConfirm != null)
                if (object.requireOpponentConfirm)
                    message.requireOpponentConfirm = $Boolean(object.requireOpponentConfirm);
            if (object.enableRanked != null)
                if (object.enableRanked)
                    message.enableRanked = $Boolean(object.enableRanked);
            if (object.stake != null)
                if ($Number(object.stake) !== 0)
                    message.stake = object.stake | 0;
            if (object.enableRankMultiplier != null)
                if (object.enableRankMultiplier)
                    message.enableRankMultiplier = $Boolean(object.enableRankMultiplier);
            if (object.rankMultiplier != null)
                if ($Number(object.rankMultiplier) !== 0)
                    message.rankMultiplier = object.rankMultiplier | 0;
            if (object.enableExtremeRanked != null)
                if (object.enableExtremeRanked)
                    message.enableExtremeRanked = $Boolean(object.enableExtremeRanked);
            if (object.othelloBoardTheme != null)
                if (typeof object.othelloBoardTheme !== "string" || object.othelloBoardTheme.length)
                    message.othelloBoardTheme = $String(object.othelloBoardTheme);
            if (object.tictactoeBoardTheme != null)
                if (typeof object.tictactoeBoardTheme !== "string" || object.tictactoeBoardTheme.length)
                    message.tictactoeBoardTheme = $String(object.tictactoeBoardTheme);
            if (object.liarsDiceMinPlayers != null)
                if ($Number(object.liarsDiceMinPlayers) !== 0)
                    message.liarsDiceMinPlayers = object.liarsDiceMinPlayers | 0;
            if (object.liarsDiceMaxPlayers != null)
                if ($Number(object.liarsDiceMaxPlayers) !== 0)
                    message.liarsDiceMaxPlayers = object.liarsDiceMaxPlayers | 0;
            if (object.gomokuBoardTheme != null)
                if (typeof object.gomokuBoardTheme !== "string" || object.gomokuBoardTheme.length)
                    message.gomokuBoardTheme = $String(object.gomokuBoardTheme);
            if (object.gomokuUndoLimit != null)
                if ($Number(object.gomokuUndoLimit) !== 0)
                    message.gomokuUndoLimit = object.gomokuUndoLimit | 0;
            if (object.othelloMoveSeconds != null)
                if ($Number(object.othelloMoveSeconds) !== 0)
                    message.othelloMoveSeconds = object.othelloMoveSeconds | 0;
            if (object.othelloGameMinutes != null)
                if ($Number(object.othelloGameMinutes) !== 0)
                    message.othelloGameMinutes = object.othelloGameMinutes | 0;
            if (object.gomokuMoveSeconds != null)
                if ($Number(object.gomokuMoveSeconds) !== 0)
                    message.gomokuMoveSeconds = object.gomokuMoveSeconds | 0;
            if (object.gomokuGameMinutes != null)
                if ($Number(object.gomokuGameMinutes) !== 0)
                    message.gomokuGameMinutes = object.gomokuGameMinutes | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoomSettings message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RoomSettings
         * @static
         * @param {game.RoomSettings} message RoomSettings
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoomSettings.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.punishmentIds = [];
                object.tags = [];
            }
            if (options.defaults) {
                object.name = "";
                object.password = "";
                object.gameId = "";
                object.enablePunishment = false;
                object.punishmentSource = "";
                object.punishmentId = "";
                object.roomBackgroundImage = "";
                object.enableTags = false;
                object.allowProofImage = false;
                object.tieDoublePunish = false;
                object.requireOpponentConfirm = false;
                object.enableRanked = false;
                object.stake = 0;
                object.enableRankMultiplier = false;
                object.rankMultiplier = 0;
                object.enableExtremeRanked = false;
                object.othelloBoardTheme = "";
                object.tictactoeBoardTheme = "";
                object.liarsDiceMinPlayers = 0;
                object.liarsDiceMaxPlayers = 0;
                object.gomokuBoardTheme = "";
                object.gomokuUndoLimit = 0;
                object.othelloMoveSeconds = 0;
                object.othelloGameMinutes = 0;
                object.gomokuMoveSeconds = 0;
                object.gomokuGameMinutes = 0;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                object.password = message.password;
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId"))
                object.gameId = message.gameId;
            if (message.enablePunishment != null && $Object.hasOwnProperty.call(message, "enablePunishment"))
                object.enablePunishment = message.enablePunishment;
            if (message.punishmentSource != null && $Object.hasOwnProperty.call(message, "punishmentSource"))
                object.punishmentSource = message.punishmentSource;
            if (message.punishmentId != null && $Object.hasOwnProperty.call(message, "punishmentId"))
                object.punishmentId = message.punishmentId;
            if (message.punishmentIds && message.punishmentIds.length) {
                object.punishmentIds = $Array(message.punishmentIds.length);
                for (let j = 0; j < message.punishmentIds.length; ++j)
                    object.punishmentIds[j] = message.punishmentIds[j];
            }
            if (message.roomBackgroundImage != null && $Object.hasOwnProperty.call(message, "roomBackgroundImage"))
                object.roomBackgroundImage = message.roomBackgroundImage;
            if (message.enableTags != null && $Object.hasOwnProperty.call(message, "enableTags"))
                object.enableTags = message.enableTags;
            if (message.tags && message.tags.length) {
                object.tags = $Array(message.tags.length);
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.allowProofImage != null && $Object.hasOwnProperty.call(message, "allowProofImage"))
                object.allowProofImage = message.allowProofImage;
            if (message.tieDoublePunish != null && $Object.hasOwnProperty.call(message, "tieDoublePunish"))
                object.tieDoublePunish = message.tieDoublePunish;
            if (message.requireOpponentConfirm != null && $Object.hasOwnProperty.call(message, "requireOpponentConfirm"))
                object.requireOpponentConfirm = message.requireOpponentConfirm;
            if (message.enableRanked != null && $Object.hasOwnProperty.call(message, "enableRanked"))
                object.enableRanked = message.enableRanked;
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                object.stake = message.stake;
            if (message.enableRankMultiplier != null && $Object.hasOwnProperty.call(message, "enableRankMultiplier"))
                object.enableRankMultiplier = message.enableRankMultiplier;
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier"))
                object.rankMultiplier = message.rankMultiplier;
            if (message.enableExtremeRanked != null && $Object.hasOwnProperty.call(message, "enableExtremeRanked"))
                object.enableExtremeRanked = message.enableExtremeRanked;
            if (message.othelloBoardTheme != null && $Object.hasOwnProperty.call(message, "othelloBoardTheme"))
                object.othelloBoardTheme = message.othelloBoardTheme;
            if (message.tictactoeBoardTheme != null && $Object.hasOwnProperty.call(message, "tictactoeBoardTheme"))
                object.tictactoeBoardTheme = message.tictactoeBoardTheme;
            if (message.liarsDiceMinPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMinPlayers"))
                object.liarsDiceMinPlayers = message.liarsDiceMinPlayers;
            if (message.liarsDiceMaxPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMaxPlayers"))
                object.liarsDiceMaxPlayers = message.liarsDiceMaxPlayers;
            if (message.gomokuBoardTheme != null && $Object.hasOwnProperty.call(message, "gomokuBoardTheme"))
                object.gomokuBoardTheme = message.gomokuBoardTheme;
            if (message.gomokuUndoLimit != null && $Object.hasOwnProperty.call(message, "gomokuUndoLimit"))
                object.gomokuUndoLimit = message.gomokuUndoLimit;
            if (message.othelloMoveSeconds != null && $Object.hasOwnProperty.call(message, "othelloMoveSeconds"))
                object.othelloMoveSeconds = message.othelloMoveSeconds;
            if (message.othelloGameMinutes != null && $Object.hasOwnProperty.call(message, "othelloGameMinutes"))
                object.othelloGameMinutes = message.othelloGameMinutes;
            if (message.gomokuMoveSeconds != null && $Object.hasOwnProperty.call(message, "gomokuMoveSeconds"))
                object.gomokuMoveSeconds = message.gomokuMoveSeconds;
            if (message.gomokuGameMinutes != null && $Object.hasOwnProperty.call(message, "gomokuGameMinutes"))
                object.gomokuGameMinutes = message.gomokuGameMinutes;
            return object;
        };

        /**
         * Converts this RoomSettings to JSON.
         * @function toJSON
         * @memberof game.RoomSettings
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoomSettings.prototype.toJSON = function() {
            return RoomSettings.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoomSettings
         * @function getTypeUrl
         * @memberof game.RoomSettings
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoomSettings.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RoomSettings";
        };

        return RoomSettings;
    })();

    game.GomokuUndoRequest = (function() {

        /**
         * Properties of a GomokuUndoRequest.
         * @typedef {Object} game.GomokuUndoRequest.$Properties
         * @property {string|null} [fromSeat] GomokuUndoRequest fromSeat
         * @property {string|null} [toSeat] GomokuUndoRequest toSeat
         * @property {number|Long|null} [createdAt] GomokuUndoRequest createdAt
         * @property {number|Long|null} [expiresAt] GomokuUndoRequest expiresAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GomokuUndoRequest.
         * @memberof game
         * @interface IGomokuUndoRequest
         * @augments game.GomokuUndoRequest.$Properties
         * @deprecated Use game.GomokuUndoRequest.$Properties instead.
         */

        /**
         * Shape of a GomokuUndoRequest.
         * @typedef {game.GomokuUndoRequest.$Properties} game.GomokuUndoRequest.$Shape
         */

        /**
         * Constructs a new GomokuUndoRequest.
         * @memberof game
         * @classdesc Represents a GomokuUndoRequest.
         * @constructor
         * @param {game.GomokuUndoRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GomokuUndoRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GomokuUndoRequest fromSeat.
         * @member {string} fromSeat
         * @memberof game.GomokuUndoRequest
         * @instance
         */
        GomokuUndoRequest.prototype.fromSeat = "";

        /**
         * GomokuUndoRequest toSeat.
         * @member {string} toSeat
         * @memberof game.GomokuUndoRequest
         * @instance
         */
        GomokuUndoRequest.prototype.toSeat = "";

        /**
         * GomokuUndoRequest createdAt.
         * @member {number|Long} createdAt
         * @memberof game.GomokuUndoRequest
         * @instance
         */
        GomokuUndoRequest.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GomokuUndoRequest expiresAt.
         * @member {number|Long} expiresAt
         * @memberof game.GomokuUndoRequest
         * @instance
         */
        GomokuUndoRequest.prototype.expiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new GomokuUndoRequest instance using the specified properties.
         * @function create
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {game.GomokuUndoRequest.$Properties=} [properties] Properties to set
         * @returns {game.GomokuUndoRequest} GomokuUndoRequest instance
         * @type {{
         *   (properties: game.GomokuUndoRequest.$Shape): game.GomokuUndoRequest & game.GomokuUndoRequest.$Shape;
         *   (properties?: game.GomokuUndoRequest.$Properties): game.GomokuUndoRequest;
         * }}
         */
        GomokuUndoRequest.create = function(properties) {
            return new GomokuUndoRequest(properties);
        };

        /**
         * Encodes the specified GomokuUndoRequest message. Does not implicitly {@link game.GomokuUndoRequest.verify|verify} messages.
         * @function encode
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {game.GomokuUndoRequest.$Properties} message GomokuUndoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GomokuUndoRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat") && message.fromSeat !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fromSeat);
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat") && message.toSeat !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.toSeat);
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.createdAt);
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt") && (typeof message.expiresAt === "object" ? message.expiresAt.low || message.expiresAt.high : message.expiresAt !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.expiresAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GomokuUndoRequest message, length delimited. Does not implicitly {@link game.GomokuUndoRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {game.GomokuUndoRequest.$Properties} message GomokuUndoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GomokuUndoRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GomokuUndoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GomokuUndoRequest & game.GomokuUndoRequest.$Shape} GomokuUndoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GomokuUndoRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GomokuUndoRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.fromSeat = value;
                        else
                            delete message.fromSeat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.toSeat = value;
                        else
                            delete message.toSeat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.createdAt = value;
                        else
                            delete message.createdAt;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiresAt = value;
                        else
                            delete message.expiresAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GomokuUndoRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GomokuUndoRequest & game.GomokuUndoRequest.$Shape} GomokuUndoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GomokuUndoRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GomokuUndoRequest message.
         * @function verify
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GomokuUndoRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat"))
                if (!$util.isString(message.fromSeat))
                    return "fromSeat: string expected";
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat"))
                if (!$util.isString(message.toSeat))
                    return "toSeat: string expected";
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
                if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                    return "createdAt: integer|Long expected";
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (!$util.isInteger(message.expiresAt) && !(message.expiresAt && $util.isInteger(message.expiresAt.low) && $util.isInteger(message.expiresAt.high)))
                    return "expiresAt: integer|Long expected";
            return null;
        };

        /**
         * Creates a GomokuUndoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GomokuUndoRequest} GomokuUndoRequest
         */
        GomokuUndoRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GomokuUndoRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GomokuUndoRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GomokuUndoRequest();
            if (object.fromSeat != null)
                if (typeof object.fromSeat !== "string" || object.fromSeat.length)
                    message.fromSeat = $String(object.fromSeat);
            if (object.toSeat != null)
                if (typeof object.toSeat !== "string" || object.toSeat.length)
                    message.toSeat = $String(object.toSeat);
            if (object.createdAt != null)
                if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = $parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
            if (object.expiresAt != null)
                if (typeof object.expiresAt === "object" ? object.expiresAt.low || object.expiresAt.high : $Number(object.expiresAt) !== 0)
                    if ($util.Long)
                        message.expiresAt = $util.Long.fromValue(object.expiresAt, false);
                    else if (typeof object.expiresAt === "string")
                        message.expiresAt = $parseInt(object.expiresAt, 10);
                    else if (typeof object.expiresAt === "number")
                        message.expiresAt = object.expiresAt;
                    else if (typeof object.expiresAt === "object")
                        message.expiresAt = new $util.LongBits(object.expiresAt.low >>> 0, object.expiresAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a GomokuUndoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {game.GomokuUndoRequest} message GomokuUndoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GomokuUndoRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fromSeat = "";
                object.toSeat = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat"))
                object.fromSeat = message.fromSeat;
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat"))
                object.toSeat = message.toSeat;
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                else if (typeof message.createdAt === "number")
                    object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
                else
                    object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiresAt = typeof message.expiresAt === "number" ? $BigInt(message.expiresAt) : $util.Long.fromBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.expiresAt === "number")
                    object.expiresAt = options.longs === $String ? $String(message.expiresAt) : message.expiresAt;
                else
                    object.expiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.expiresAt) : options.longs === $Number ? new $util.LongBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0).toNumber() : message.expiresAt;
            return object;
        };

        /**
         * Converts this GomokuUndoRequest to JSON.
         * @function toJSON
         * @memberof game.GomokuUndoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GomokuUndoRequest.prototype.toJSON = function() {
            return GomokuUndoRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GomokuUndoRequest
         * @function getTypeUrl
         * @memberof game.GomokuUndoRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GomokuUndoRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GomokuUndoRequest";
        };

        return GomokuUndoRequest;
    })();

    game.GomokuResignRequest = (function() {

        /**
         * Properties of a GomokuResignRequest.
         * @typedef {Object} game.GomokuResignRequest.$Properties
         * @property {string|null} [fromSeat] GomokuResignRequest fromSeat
         * @property {string|null} [toSeat] GomokuResignRequest toSeat
         * @property {number|Long|null} [createdAt] GomokuResignRequest createdAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GomokuResignRequest.
         * @memberof game
         * @interface IGomokuResignRequest
         * @augments game.GomokuResignRequest.$Properties
         * @deprecated Use game.GomokuResignRequest.$Properties instead.
         */

        /**
         * Shape of a GomokuResignRequest.
         * @typedef {game.GomokuResignRequest.$Properties} game.GomokuResignRequest.$Shape
         */

        /**
         * Constructs a new GomokuResignRequest.
         * @memberof game
         * @classdesc Represents a GomokuResignRequest.
         * @constructor
         * @param {game.GomokuResignRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GomokuResignRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GomokuResignRequest fromSeat.
         * @member {string} fromSeat
         * @memberof game.GomokuResignRequest
         * @instance
         */
        GomokuResignRequest.prototype.fromSeat = "";

        /**
         * GomokuResignRequest toSeat.
         * @member {string} toSeat
         * @memberof game.GomokuResignRequest
         * @instance
         */
        GomokuResignRequest.prototype.toSeat = "";

        /**
         * GomokuResignRequest createdAt.
         * @member {number|Long} createdAt
         * @memberof game.GomokuResignRequest
         * @instance
         */
        GomokuResignRequest.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new GomokuResignRequest instance using the specified properties.
         * @function create
         * @memberof game.GomokuResignRequest
         * @static
         * @param {game.GomokuResignRequest.$Properties=} [properties] Properties to set
         * @returns {game.GomokuResignRequest} GomokuResignRequest instance
         * @type {{
         *   (properties: game.GomokuResignRequest.$Shape): game.GomokuResignRequest & game.GomokuResignRequest.$Shape;
         *   (properties?: game.GomokuResignRequest.$Properties): game.GomokuResignRequest;
         * }}
         */
        GomokuResignRequest.create = function(properties) {
            return new GomokuResignRequest(properties);
        };

        /**
         * Encodes the specified GomokuResignRequest message. Does not implicitly {@link game.GomokuResignRequest.verify|verify} messages.
         * @function encode
         * @memberof game.GomokuResignRequest
         * @static
         * @param {game.GomokuResignRequest.$Properties} message GomokuResignRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GomokuResignRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat") && message.fromSeat !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fromSeat);
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat") && message.toSeat !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.toSeat);
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.createdAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GomokuResignRequest message, length delimited. Does not implicitly {@link game.GomokuResignRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GomokuResignRequest
         * @static
         * @param {game.GomokuResignRequest.$Properties} message GomokuResignRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GomokuResignRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GomokuResignRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.GomokuResignRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GomokuResignRequest & game.GomokuResignRequest.$Shape} GomokuResignRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GomokuResignRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GomokuResignRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.fromSeat = value;
                        else
                            delete message.fromSeat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.toSeat = value;
                        else
                            delete message.toSeat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.createdAt = value;
                        else
                            delete message.createdAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GomokuResignRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GomokuResignRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GomokuResignRequest & game.GomokuResignRequest.$Shape} GomokuResignRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GomokuResignRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GomokuResignRequest message.
         * @function verify
         * @memberof game.GomokuResignRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GomokuResignRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat"))
                if (!$util.isString(message.fromSeat))
                    return "fromSeat: string expected";
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat"))
                if (!$util.isString(message.toSeat))
                    return "toSeat: string expected";
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
                if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                    return "createdAt: integer|Long expected";
            return null;
        };

        /**
         * Creates a GomokuResignRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GomokuResignRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GomokuResignRequest} GomokuResignRequest
         */
        GomokuResignRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GomokuResignRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GomokuResignRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GomokuResignRequest();
            if (object.fromSeat != null)
                if (typeof object.fromSeat !== "string" || object.fromSeat.length)
                    message.fromSeat = $String(object.fromSeat);
            if (object.toSeat != null)
                if (typeof object.toSeat !== "string" || object.toSeat.length)
                    message.toSeat = $String(object.toSeat);
            if (object.createdAt != null)
                if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = $parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a GomokuResignRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GomokuResignRequest
         * @static
         * @param {game.GomokuResignRequest} message GomokuResignRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GomokuResignRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fromSeat = "";
                object.toSeat = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat"))
                object.fromSeat = message.fromSeat;
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat"))
                object.toSeat = message.toSeat;
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                else if (typeof message.createdAt === "number")
                    object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
                else
                    object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
            return object;
        };

        /**
         * Converts this GomokuResignRequest to JSON.
         * @function toJSON
         * @memberof game.GomokuResignRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GomokuResignRequest.prototype.toJSON = function() {
            return GomokuResignRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GomokuResignRequest
         * @function getTypeUrl
         * @memberof game.GomokuResignRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GomokuResignRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GomokuResignRequest";
        };

        return GomokuResignRequest;
    })();

    game.GomokuState = (function() {

        /**
         * Properties of a GomokuState.
         * @typedef {Object} game.GomokuState.$Properties
         * @property {Array.<game.BoardRow.$Properties>|null} [board] GomokuState board
         * @property {string|null} [turn] GomokuState turn
         * @property {string|null} [blackSeat] GomokuState blackSeat
         * @property {number|null} [moveCount] GomokuState moveCount
         * @property {Array.<game.Pos.$Properties>|null} [moves] GomokuState moves
         * @property {Array.<game.Pos.$Properties>|null} [winningLine] GomokuState winningLine
         * @property {Array.<game.IntPair.$Properties>|null} [rankedDelta] GomokuState rankedDelta
         * @property {Array.<game.IntPair.$Properties>|null} [undoCount] GomokuState undoCount
         * @property {game.GomokuUndoRequest.$Properties|null} [undoRequest] GomokuState undoRequest
         * @property {game.GomokuResignRequest.$Properties|null} [resignRequest] GomokuState resignRequest
         * @property {boolean|null} [ended] GomokuState ended
         * @property {string|null} [winner] GomokuState winner
         * @property {number|Long|null} [moveDeadlineAt] GomokuState moveDeadlineAt
         * @property {number|Long|null} [clockDeadlineAt] GomokuState clockDeadlineAt
         * @property {Array.<game.IntPair.$Properties>|null} [clockRemaining] GomokuState clockRemaining
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GomokuState.
         * @memberof game
         * @interface IGomokuState
         * @augments game.GomokuState.$Properties
         * @deprecated Use game.GomokuState.$Properties instead.
         */

        /**
         * Shape of a GomokuState.
         * @typedef {game.GomokuState.$Properties} game.GomokuState.$Shape
         */

        /**
         * Constructs a new GomokuState.
         * @memberof game
         * @classdesc Represents a GomokuState.
         * @constructor
         * @param {game.GomokuState.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GomokuState = function (properties) {
            this.board = [];
            this.moves = [];
            this.winningLine = [];
            this.rankedDelta = [];
            this.undoCount = [];
            this.clockRemaining = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GomokuState board.
         * @member {Array.<game.BoardRow.$Properties>} board
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.board = $util.emptyArray;

        /**
         * GomokuState turn.
         * @member {string} turn
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.turn = "";

        /**
         * GomokuState blackSeat.
         * @member {string} blackSeat
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.blackSeat = "";

        /**
         * GomokuState moveCount.
         * @member {number} moveCount
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.moveCount = 0;

        /**
         * GomokuState moves.
         * @member {Array.<game.Pos.$Properties>} moves
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.moves = $util.emptyArray;

        /**
         * GomokuState winningLine.
         * @member {Array.<game.Pos.$Properties>} winningLine
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.winningLine = $util.emptyArray;

        /**
         * GomokuState rankedDelta.
         * @member {Array.<game.IntPair.$Properties>} rankedDelta
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.rankedDelta = $util.emptyArray;

        /**
         * GomokuState undoCount.
         * @member {Array.<game.IntPair.$Properties>} undoCount
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.undoCount = $util.emptyArray;

        /**
         * GomokuState undoRequest.
         * @member {game.GomokuUndoRequest.$Properties|null|undefined} undoRequest
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.undoRequest = null;

        /**
         * GomokuState resignRequest.
         * @member {game.GomokuResignRequest.$Properties|null|undefined} resignRequest
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.resignRequest = null;

        /**
         * GomokuState ended.
         * @member {boolean} ended
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.ended = false;

        /**
         * GomokuState winner.
         * @member {string} winner
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.winner = "";

        /**
         * GomokuState moveDeadlineAt.
         * @member {number|Long} moveDeadlineAt
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.moveDeadlineAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GomokuState clockDeadlineAt.
         * @member {number|Long} clockDeadlineAt
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.clockDeadlineAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GomokuState clockRemaining.
         * @member {Array.<game.IntPair.$Properties>} clockRemaining
         * @memberof game.GomokuState
         * @instance
         */
        GomokuState.prototype.clockRemaining = $util.emptyArray;

        /**
         * Creates a new GomokuState instance using the specified properties.
         * @function create
         * @memberof game.GomokuState
         * @static
         * @param {game.GomokuState.$Properties=} [properties] Properties to set
         * @returns {game.GomokuState} GomokuState instance
         * @type {{
         *   (properties: game.GomokuState.$Shape): game.GomokuState & game.GomokuState.$Shape;
         *   (properties?: game.GomokuState.$Properties): game.GomokuState;
         * }}
         */
        GomokuState.create = function(properties) {
            return new GomokuState(properties);
        };

        /**
         * Encodes the specified GomokuState message. Does not implicitly {@link game.GomokuState.verify|verify} messages.
         * @function encode
         * @memberof game.GomokuState
         * @static
         * @param {game.GomokuState.$Properties} message GomokuState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GomokuState.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.board != null && message.board.length)
                for (let i = 0; i < message.board.length; ++i)
                    $root.game.BoardRow.encode(message.board[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn") && message.turn !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.turn);
            if (message.blackSeat != null && $Object.hasOwnProperty.call(message, "blackSeat") && message.blackSeat !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.blackSeat);
            if (message.moveCount != null && $Object.hasOwnProperty.call(message, "moveCount") && message.moveCount !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.moveCount);
            if (message.moves != null && message.moves.length)
                for (let i = 0; i < message.moves.length; ++i)
                    $root.game.Pos.encode(message.moves[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.winningLine != null && message.winningLine.length)
                for (let i = 0; i < message.winningLine.length; ++i)
                    $root.game.Pos.encode(message.winningLine[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.rankedDelta != null && message.rankedDelta.length)
                for (let i = 0; i < message.rankedDelta.length; ++i)
                    $root.game.IntPair.encode(message.rankedDelta[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.undoCount != null && message.undoCount.length)
                for (let i = 0; i < message.undoCount.length; ++i)
                    $root.game.IntPair.encode(message.undoCount[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.undoRequest != null && $Object.hasOwnProperty.call(message, "undoRequest"))
                $root.game.GomokuUndoRequest.encode(message.undoRequest, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.resignRequest != null && $Object.hasOwnProperty.call(message, "resignRequest"))
                $root.game.GomokuResignRequest.encode(message.resignRequest, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended") && message.ended !== false)
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.ended);
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner") && message.winner !== "")
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.winner);
            if (message.moveDeadlineAt != null && $Object.hasOwnProperty.call(message, "moveDeadlineAt") && (typeof message.moveDeadlineAt === "object" ? message.moveDeadlineAt.low || message.moveDeadlineAt.high : message.moveDeadlineAt !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.moveDeadlineAt);
            if (message.clockDeadlineAt != null && $Object.hasOwnProperty.call(message, "clockDeadlineAt") && (typeof message.clockDeadlineAt === "object" ? message.clockDeadlineAt.low || message.clockDeadlineAt.high : message.clockDeadlineAt !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.clockDeadlineAt);
            if (message.clockRemaining != null && message.clockRemaining.length)
                for (let i = 0; i < message.clockRemaining.length; ++i)
                    $root.game.IntPair.encode(message.clockRemaining[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GomokuState message, length delimited. Does not implicitly {@link game.GomokuState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GomokuState
         * @static
         * @param {game.GomokuState.$Properties} message GomokuState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GomokuState.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GomokuState message from the specified reader or buffer.
         * @function decode
         * @memberof game.GomokuState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GomokuState & game.GomokuState.$Shape} GomokuState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GomokuState.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GomokuState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.board && message.board.length))
                            message.board = [];
                        message.board.push($root.game.BoardRow.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.turn = value;
                        else
                            delete message.turn;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.blackSeat = value;
                        else
                            delete message.blackSeat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.moveCount = value;
                        else
                            delete message.moveCount;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.moves && message.moves.length))
                            message.moves = [];
                        message.moves.push($root.game.Pos.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.winningLine && message.winningLine.length))
                            message.winningLine = [];
                        message.winningLine.push($root.game.Pos.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rankedDelta && message.rankedDelta.length))
                            message.rankedDelta = [];
                        message.rankedDelta.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.undoCount && message.undoCount.length))
                            message.undoCount = [];
                        message.undoCount.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.undoRequest = $root.game.GomokuUndoRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.undoRequest);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.resignRequest = $root.game.GomokuResignRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.resignRequest);
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ended = value;
                        else
                            delete message.ended;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.winner = value;
                        else
                            delete message.winner;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.moveDeadlineAt = value;
                        else
                            delete message.moveDeadlineAt;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.clockDeadlineAt = value;
                        else
                            delete message.clockDeadlineAt;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.clockRemaining && message.clockRemaining.length))
                            message.clockRemaining = [];
                        message.clockRemaining.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GomokuState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GomokuState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GomokuState & game.GomokuState.$Shape} GomokuState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GomokuState.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GomokuState message.
         * @function verify
         * @memberof game.GomokuState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GomokuState.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.board != null && $Object.hasOwnProperty.call(message, "board")) {
                if (!$Array.isArray(message.board))
                    return "board: array expected";
                for (let i = 0; i < message.board.length; ++i) {
                    let error = $root.game.BoardRow.verify(message.board[i], _depth + 1);
                    if (error)
                        return "board." + error;
                }
            }
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn"))
                if (!$util.isString(message.turn))
                    return "turn: string expected";
            if (message.blackSeat != null && $Object.hasOwnProperty.call(message, "blackSeat"))
                if (!$util.isString(message.blackSeat))
                    return "blackSeat: string expected";
            if (message.moveCount != null && $Object.hasOwnProperty.call(message, "moveCount"))
                if (!$util.isInteger(message.moveCount))
                    return "moveCount: integer expected";
            if (message.moves != null && $Object.hasOwnProperty.call(message, "moves")) {
                if (!$Array.isArray(message.moves))
                    return "moves: array expected";
                for (let i = 0; i < message.moves.length; ++i) {
                    let error = $root.game.Pos.verify(message.moves[i], _depth + 1);
                    if (error)
                        return "moves." + error;
                }
            }
            if (message.winningLine != null && $Object.hasOwnProperty.call(message, "winningLine")) {
                if (!$Array.isArray(message.winningLine))
                    return "winningLine: array expected";
                for (let i = 0; i < message.winningLine.length; ++i) {
                    let error = $root.game.Pos.verify(message.winningLine[i], _depth + 1);
                    if (error)
                        return "winningLine." + error;
                }
            }
            if (message.rankedDelta != null && $Object.hasOwnProperty.call(message, "rankedDelta")) {
                if (!$Array.isArray(message.rankedDelta))
                    return "rankedDelta: array expected";
                for (let i = 0; i < message.rankedDelta.length; ++i) {
                    let error = $root.game.IntPair.verify(message.rankedDelta[i], _depth + 1);
                    if (error)
                        return "rankedDelta." + error;
                }
            }
            if (message.undoCount != null && $Object.hasOwnProperty.call(message, "undoCount")) {
                if (!$Array.isArray(message.undoCount))
                    return "undoCount: array expected";
                for (let i = 0; i < message.undoCount.length; ++i) {
                    let error = $root.game.IntPair.verify(message.undoCount[i], _depth + 1);
                    if (error)
                        return "undoCount." + error;
                }
            }
            if (message.undoRequest != null && $Object.hasOwnProperty.call(message, "undoRequest")) {
                let error = $root.game.GomokuUndoRequest.verify(message.undoRequest, _depth + 1);
                if (error)
                    return "undoRequest." + error;
            }
            if (message.resignRequest != null && $Object.hasOwnProperty.call(message, "resignRequest")) {
                let error = $root.game.GomokuResignRequest.verify(message.resignRequest, _depth + 1);
                if (error)
                    return "resignRequest." + error;
            }
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended"))
                if (typeof message.ended !== "boolean")
                    return "ended: boolean expected";
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner"))
                if (!$util.isString(message.winner))
                    return "winner: string expected";
            if (message.moveDeadlineAt != null && $Object.hasOwnProperty.call(message, "moveDeadlineAt"))
                if (!$util.isInteger(message.moveDeadlineAt) && !(message.moveDeadlineAt && $util.isInteger(message.moveDeadlineAt.low) && $util.isInteger(message.moveDeadlineAt.high)))
                    return "moveDeadlineAt: integer|Long expected";
            if (message.clockDeadlineAt != null && $Object.hasOwnProperty.call(message, "clockDeadlineAt"))
                if (!$util.isInteger(message.clockDeadlineAt) && !(message.clockDeadlineAt && $util.isInteger(message.clockDeadlineAt.low) && $util.isInteger(message.clockDeadlineAt.high)))
                    return "clockDeadlineAt: integer|Long expected";
            if (message.clockRemaining != null && $Object.hasOwnProperty.call(message, "clockRemaining")) {
                if (!$Array.isArray(message.clockRemaining))
                    return "clockRemaining: array expected";
                for (let i = 0; i < message.clockRemaining.length; ++i) {
                    let error = $root.game.IntPair.verify(message.clockRemaining[i], _depth + 1);
                    if (error)
                        return "clockRemaining." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GomokuState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GomokuState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GomokuState} GomokuState
         */
        GomokuState.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GomokuState)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GomokuState: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GomokuState();
            if (object.board) {
                if (!$Array.isArray(object.board))
                    throw $TypeError(".game.GomokuState.board: array expected");
                message.board = $Array(object.board.length);
                for (let i = 0; i < object.board.length; ++i) {
                    if (!$util.isObject(object.board[i]))
                        throw $TypeError(".game.GomokuState.board: object expected");
                    message.board[i] = $root.game.BoardRow.fromObject(object.board[i], _depth + 1);
                }
            }
            if (object.turn != null)
                if (typeof object.turn !== "string" || object.turn.length)
                    message.turn = $String(object.turn);
            if (object.blackSeat != null)
                if (typeof object.blackSeat !== "string" || object.blackSeat.length)
                    message.blackSeat = $String(object.blackSeat);
            if (object.moveCount != null)
                if ($Number(object.moveCount) !== 0)
                    message.moveCount = object.moveCount | 0;
            if (object.moves) {
                if (!$Array.isArray(object.moves))
                    throw $TypeError(".game.GomokuState.moves: array expected");
                message.moves = $Array(object.moves.length);
                for (let i = 0; i < object.moves.length; ++i) {
                    if (!$util.isObject(object.moves[i]))
                        throw $TypeError(".game.GomokuState.moves: object expected");
                    message.moves[i] = $root.game.Pos.fromObject(object.moves[i], _depth + 1);
                }
            }
            if (object.winningLine) {
                if (!$Array.isArray(object.winningLine))
                    throw $TypeError(".game.GomokuState.winningLine: array expected");
                message.winningLine = $Array(object.winningLine.length);
                for (let i = 0; i < object.winningLine.length; ++i) {
                    if (!$util.isObject(object.winningLine[i]))
                        throw $TypeError(".game.GomokuState.winningLine: object expected");
                    message.winningLine[i] = $root.game.Pos.fromObject(object.winningLine[i], _depth + 1);
                }
            }
            if (object.rankedDelta) {
                if (!$Array.isArray(object.rankedDelta))
                    throw $TypeError(".game.GomokuState.rankedDelta: array expected");
                message.rankedDelta = $Array(object.rankedDelta.length);
                for (let i = 0; i < object.rankedDelta.length; ++i) {
                    if (!$util.isObject(object.rankedDelta[i]))
                        throw $TypeError(".game.GomokuState.rankedDelta: object expected");
                    message.rankedDelta[i] = $root.game.IntPair.fromObject(object.rankedDelta[i], _depth + 1);
                }
            }
            if (object.undoCount) {
                if (!$Array.isArray(object.undoCount))
                    throw $TypeError(".game.GomokuState.undoCount: array expected");
                message.undoCount = $Array(object.undoCount.length);
                for (let i = 0; i < object.undoCount.length; ++i) {
                    if (!$util.isObject(object.undoCount[i]))
                        throw $TypeError(".game.GomokuState.undoCount: object expected");
                    message.undoCount[i] = $root.game.IntPair.fromObject(object.undoCount[i], _depth + 1);
                }
            }
            if (object.undoRequest != null) {
                if (!$util.isObject(object.undoRequest))
                    throw $TypeError(".game.GomokuState.undoRequest: object expected");
                message.undoRequest = $root.game.GomokuUndoRequest.fromObject(object.undoRequest, _depth + 1);
            }
            if (object.resignRequest != null) {
                if (!$util.isObject(object.resignRequest))
                    throw $TypeError(".game.GomokuState.resignRequest: object expected");
                message.resignRequest = $root.game.GomokuResignRequest.fromObject(object.resignRequest, _depth + 1);
            }
            if (object.ended != null)
                if (object.ended)
                    message.ended = $Boolean(object.ended);
            if (object.winner != null)
                if (typeof object.winner !== "string" || object.winner.length)
                    message.winner = $String(object.winner);
            if (object.moveDeadlineAt != null)
                if (typeof object.moveDeadlineAt === "object" ? object.moveDeadlineAt.low || object.moveDeadlineAt.high : $Number(object.moveDeadlineAt) !== 0)
                    if ($util.Long)
                        message.moveDeadlineAt = $util.Long.fromValue(object.moveDeadlineAt, false);
                    else if (typeof object.moveDeadlineAt === "string")
                        message.moveDeadlineAt = $parseInt(object.moveDeadlineAt, 10);
                    else if (typeof object.moveDeadlineAt === "number")
                        message.moveDeadlineAt = object.moveDeadlineAt;
                    else if (typeof object.moveDeadlineAt === "object")
                        message.moveDeadlineAt = new $util.LongBits(object.moveDeadlineAt.low >>> 0, object.moveDeadlineAt.high >>> 0).toNumber();
            if (object.clockDeadlineAt != null)
                if (typeof object.clockDeadlineAt === "object" ? object.clockDeadlineAt.low || object.clockDeadlineAt.high : $Number(object.clockDeadlineAt) !== 0)
                    if ($util.Long)
                        message.clockDeadlineAt = $util.Long.fromValue(object.clockDeadlineAt, false);
                    else if (typeof object.clockDeadlineAt === "string")
                        message.clockDeadlineAt = $parseInt(object.clockDeadlineAt, 10);
                    else if (typeof object.clockDeadlineAt === "number")
                        message.clockDeadlineAt = object.clockDeadlineAt;
                    else if (typeof object.clockDeadlineAt === "object")
                        message.clockDeadlineAt = new $util.LongBits(object.clockDeadlineAt.low >>> 0, object.clockDeadlineAt.high >>> 0).toNumber();
            if (object.clockRemaining) {
                if (!$Array.isArray(object.clockRemaining))
                    throw $TypeError(".game.GomokuState.clockRemaining: array expected");
                message.clockRemaining = $Array(object.clockRemaining.length);
                for (let i = 0; i < object.clockRemaining.length; ++i) {
                    if (!$util.isObject(object.clockRemaining[i]))
                        throw $TypeError(".game.GomokuState.clockRemaining: object expected");
                    message.clockRemaining[i] = $root.game.IntPair.fromObject(object.clockRemaining[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GomokuState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GomokuState
         * @static
         * @param {game.GomokuState} message GomokuState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GomokuState.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.board = [];
                object.moves = [];
                object.winningLine = [];
                object.rankedDelta = [];
                object.undoCount = [];
                object.clockRemaining = [];
            }
            if (options.defaults) {
                object.turn = "";
                object.blackSeat = "";
                object.moveCount = 0;
                object.undoRequest = null;
                object.resignRequest = null;
                object.ended = false;
                object.winner = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.moveDeadlineAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.moveDeadlineAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.clockDeadlineAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.clockDeadlineAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.board && message.board.length) {
                object.board = $Array(message.board.length);
                for (let j = 0; j < message.board.length; ++j)
                    object.board[j] = $root.game.BoardRow.toObject(message.board[j], options, _depth + 1);
            }
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn"))
                object.turn = message.turn;
            if (message.blackSeat != null && $Object.hasOwnProperty.call(message, "blackSeat"))
                object.blackSeat = message.blackSeat;
            if (message.moveCount != null && $Object.hasOwnProperty.call(message, "moveCount"))
                object.moveCount = message.moveCount;
            if (message.moves && message.moves.length) {
                object.moves = $Array(message.moves.length);
                for (let j = 0; j < message.moves.length; ++j)
                    object.moves[j] = $root.game.Pos.toObject(message.moves[j], options, _depth + 1);
            }
            if (message.winningLine && message.winningLine.length) {
                object.winningLine = $Array(message.winningLine.length);
                for (let j = 0; j < message.winningLine.length; ++j)
                    object.winningLine[j] = $root.game.Pos.toObject(message.winningLine[j], options, _depth + 1);
            }
            if (message.rankedDelta && message.rankedDelta.length) {
                object.rankedDelta = $Array(message.rankedDelta.length);
                for (let j = 0; j < message.rankedDelta.length; ++j)
                    object.rankedDelta[j] = $root.game.IntPair.toObject(message.rankedDelta[j], options, _depth + 1);
            }
            if (message.undoCount && message.undoCount.length) {
                object.undoCount = $Array(message.undoCount.length);
                for (let j = 0; j < message.undoCount.length; ++j)
                    object.undoCount[j] = $root.game.IntPair.toObject(message.undoCount[j], options, _depth + 1);
            }
            if (message.undoRequest != null && $Object.hasOwnProperty.call(message, "undoRequest"))
                object.undoRequest = $root.game.GomokuUndoRequest.toObject(message.undoRequest, options, _depth + 1);
            if (message.resignRequest != null && $Object.hasOwnProperty.call(message, "resignRequest"))
                object.resignRequest = $root.game.GomokuResignRequest.toObject(message.resignRequest, options, _depth + 1);
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended"))
                object.ended = message.ended;
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner"))
                object.winner = message.winner;
            if (message.moveDeadlineAt != null && $Object.hasOwnProperty.call(message, "moveDeadlineAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.moveDeadlineAt = typeof message.moveDeadlineAt === "number" ? $BigInt(message.moveDeadlineAt) : $util.Long.fromBits(message.moveDeadlineAt.low >>> 0, message.moveDeadlineAt.high >>> 0, false).toBigInt();
                else if (typeof message.moveDeadlineAt === "number")
                    object.moveDeadlineAt = options.longs === $String ? $String(message.moveDeadlineAt) : message.moveDeadlineAt;
                else
                    object.moveDeadlineAt = options.longs === $String ? $util.Long.prototype.toString.call(message.moveDeadlineAt) : options.longs === $Number ? new $util.LongBits(message.moveDeadlineAt.low >>> 0, message.moveDeadlineAt.high >>> 0).toNumber() : message.moveDeadlineAt;
            if (message.clockDeadlineAt != null && $Object.hasOwnProperty.call(message, "clockDeadlineAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.clockDeadlineAt = typeof message.clockDeadlineAt === "number" ? $BigInt(message.clockDeadlineAt) : $util.Long.fromBits(message.clockDeadlineAt.low >>> 0, message.clockDeadlineAt.high >>> 0, false).toBigInt();
                else if (typeof message.clockDeadlineAt === "number")
                    object.clockDeadlineAt = options.longs === $String ? $String(message.clockDeadlineAt) : message.clockDeadlineAt;
                else
                    object.clockDeadlineAt = options.longs === $String ? $util.Long.prototype.toString.call(message.clockDeadlineAt) : options.longs === $Number ? new $util.LongBits(message.clockDeadlineAt.low >>> 0, message.clockDeadlineAt.high >>> 0).toNumber() : message.clockDeadlineAt;
            if (message.clockRemaining && message.clockRemaining.length) {
                object.clockRemaining = $Array(message.clockRemaining.length);
                for (let j = 0; j < message.clockRemaining.length; ++j)
                    object.clockRemaining[j] = $root.game.IntPair.toObject(message.clockRemaining[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this GomokuState to JSON.
         * @function toJSON
         * @memberof game.GomokuState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GomokuState.prototype.toJSON = function() {
            return GomokuState.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GomokuState
         * @function getTypeUrl
         * @memberof game.GomokuState
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GomokuState.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GomokuState";
        };

        return GomokuState;
    })();

    game.PunishmentProof = (function() {

        /**
         * Properties of a PunishmentProof.
         * @typedef {Object} game.PunishmentProof.$Properties
         * @property {string|null} [playerId] PunishmentProof playerId
         * @property {string|null} [text] PunishmentProof text
         * @property {string|null} [imageUrl] PunishmentProof imageUrl
         * @property {string|null} [taskText] PunishmentProof taskText
         * @property {string|null} [status] PunishmentProof status
         * @property {string|null} [confirmedBy] PunishmentProof confirmedBy
         * @property {string|null} [reviewedBy] PunishmentProof reviewedBy
         * @property {number|Long|null} [reviewedAt] PunishmentProof reviewedAt
         * @property {string|null} [rejectReason] PunishmentProof rejectReason
         * @property {string|null} [redoTaskText] PunishmentProof redoTaskText
         * @property {number|Long|null} [submittedAt] PunishmentProof submittedAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PunishmentProof.
         * @memberof game
         * @interface IPunishmentProof
         * @augments game.PunishmentProof.$Properties
         * @deprecated Use game.PunishmentProof.$Properties instead.
         */

        /**
         * Shape of a PunishmentProof.
         * @typedef {game.PunishmentProof.$Properties} game.PunishmentProof.$Shape
         */

        /**
         * Constructs a new PunishmentProof.
         * @memberof game
         * @classdesc Represents a PunishmentProof.
         * @constructor
         * @param {game.PunishmentProof.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PunishmentProof = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PunishmentProof playerId.
         * @member {string} playerId
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.playerId = "";

        /**
         * PunishmentProof text.
         * @member {string} text
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.text = "";

        /**
         * PunishmentProof imageUrl.
         * @member {string} imageUrl
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.imageUrl = "";

        /**
         * PunishmentProof taskText.
         * @member {string} taskText
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.taskText = "";

        /**
         * PunishmentProof status.
         * @member {string} status
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.status = "";

        /**
         * PunishmentProof confirmedBy.
         * @member {string} confirmedBy
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.confirmedBy = "";

        /**
         * PunishmentProof reviewedBy.
         * @member {string} reviewedBy
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.reviewedBy = "";

        /**
         * PunishmentProof reviewedAt.
         * @member {number|Long} reviewedAt
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.reviewedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PunishmentProof rejectReason.
         * @member {string} rejectReason
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.rejectReason = "";

        /**
         * PunishmentProof redoTaskText.
         * @member {string} redoTaskText
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.redoTaskText = "";

        /**
         * PunishmentProof submittedAt.
         * @member {number|Long} submittedAt
         * @memberof game.PunishmentProof
         * @instance
         */
        PunishmentProof.prototype.submittedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new PunishmentProof instance using the specified properties.
         * @function create
         * @memberof game.PunishmentProof
         * @static
         * @param {game.PunishmentProof.$Properties=} [properties] Properties to set
         * @returns {game.PunishmentProof} PunishmentProof instance
         * @type {{
         *   (properties: game.PunishmentProof.$Shape): game.PunishmentProof & game.PunishmentProof.$Shape;
         *   (properties?: game.PunishmentProof.$Properties): game.PunishmentProof;
         * }}
         */
        PunishmentProof.create = function(properties) {
            return new PunishmentProof(properties);
        };

        /**
         * Encodes the specified PunishmentProof message. Does not implicitly {@link game.PunishmentProof.verify|verify} messages.
         * @function encode
         * @memberof game.PunishmentProof
         * @static
         * @param {game.PunishmentProof.$Properties} message PunishmentProof message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PunishmentProof.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId") && message.playerId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.playerId);
            if (message.text != null && $Object.hasOwnProperty.call(message, "text") && message.text !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.text);
            if (message.imageUrl != null && $Object.hasOwnProperty.call(message, "imageUrl") && message.imageUrl !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.imageUrl);
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText") && message.taskText !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.taskText);
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.status);
            if (message.confirmedBy != null && $Object.hasOwnProperty.call(message, "confirmedBy") && message.confirmedBy !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.confirmedBy);
            if (message.reviewedBy != null && $Object.hasOwnProperty.call(message, "reviewedBy") && message.reviewedBy !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.reviewedBy);
            if (message.reviewedAt != null && $Object.hasOwnProperty.call(message, "reviewedAt") && (typeof message.reviewedAt === "object" ? message.reviewedAt.low || message.reviewedAt.high : message.reviewedAt !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.reviewedAt);
            if (message.rejectReason != null && $Object.hasOwnProperty.call(message, "rejectReason") && message.rejectReason !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.rejectReason);
            if (message.redoTaskText != null && $Object.hasOwnProperty.call(message, "redoTaskText") && message.redoTaskText !== "")
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.redoTaskText);
            if (message.submittedAt != null && $Object.hasOwnProperty.call(message, "submittedAt") && (typeof message.submittedAt === "object" ? message.submittedAt.low || message.submittedAt.high : message.submittedAt !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.submittedAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PunishmentProof message, length delimited. Does not implicitly {@link game.PunishmentProof.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PunishmentProof
         * @static
         * @param {game.PunishmentProof.$Properties} message PunishmentProof message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PunishmentProof.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PunishmentProof message from the specified reader or buffer.
         * @function decode
         * @memberof game.PunishmentProof
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PunishmentProof & game.PunishmentProof.$Shape} PunishmentProof
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PunishmentProof.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PunishmentProof(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerId = value;
                        else
                            delete message.playerId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.text = value;
                        else
                            delete message.text;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.imageUrl = value;
                        else
                            delete message.imageUrl;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.taskText = value;
                        else
                            delete message.taskText;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.confirmedBy = value;
                        else
                            delete message.confirmedBy;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.reviewedBy = value;
                        else
                            delete message.reviewedBy;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.reviewedAt = value;
                        else
                            delete message.reviewedAt;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.rejectReason = value;
                        else
                            delete message.rejectReason;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.redoTaskText = value;
                        else
                            delete message.redoTaskText;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.submittedAt = value;
                        else
                            delete message.submittedAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PunishmentProof message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PunishmentProof
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PunishmentProof & game.PunishmentProof.$Shape} PunishmentProof
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PunishmentProof.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PunishmentProof message.
         * @function verify
         * @memberof game.PunishmentProof
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PunishmentProof.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                if (!$util.isString(message.playerId))
                    return "playerId: string expected";
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                if (!$util.isString(message.text))
                    return "text: string expected";
            if (message.imageUrl != null && $Object.hasOwnProperty.call(message, "imageUrl"))
                if (!$util.isString(message.imageUrl))
                    return "imageUrl: string expected";
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText"))
                if (!$util.isString(message.taskText))
                    return "taskText: string expected";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.confirmedBy != null && $Object.hasOwnProperty.call(message, "confirmedBy"))
                if (!$util.isString(message.confirmedBy))
                    return "confirmedBy: string expected";
            if (message.reviewedBy != null && $Object.hasOwnProperty.call(message, "reviewedBy"))
                if (!$util.isString(message.reviewedBy))
                    return "reviewedBy: string expected";
            if (message.reviewedAt != null && $Object.hasOwnProperty.call(message, "reviewedAt"))
                if (!$util.isInteger(message.reviewedAt) && !(message.reviewedAt && $util.isInteger(message.reviewedAt.low) && $util.isInteger(message.reviewedAt.high)))
                    return "reviewedAt: integer|Long expected";
            if (message.rejectReason != null && $Object.hasOwnProperty.call(message, "rejectReason"))
                if (!$util.isString(message.rejectReason))
                    return "rejectReason: string expected";
            if (message.redoTaskText != null && $Object.hasOwnProperty.call(message, "redoTaskText"))
                if (!$util.isString(message.redoTaskText))
                    return "redoTaskText: string expected";
            if (message.submittedAt != null && $Object.hasOwnProperty.call(message, "submittedAt"))
                if (!$util.isInteger(message.submittedAt) && !(message.submittedAt && $util.isInteger(message.submittedAt.low) && $util.isInteger(message.submittedAt.high)))
                    return "submittedAt: integer|Long expected";
            return null;
        };

        /**
         * Creates a PunishmentProof message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PunishmentProof
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PunishmentProof} PunishmentProof
         */
        PunishmentProof.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PunishmentProof)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PunishmentProof: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PunishmentProof();
            if (object.playerId != null)
                if (typeof object.playerId !== "string" || object.playerId.length)
                    message.playerId = $String(object.playerId);
            if (object.text != null)
                if (typeof object.text !== "string" || object.text.length)
                    message.text = $String(object.text);
            if (object.imageUrl != null)
                if (typeof object.imageUrl !== "string" || object.imageUrl.length)
                    message.imageUrl = $String(object.imageUrl);
            if (object.taskText != null)
                if (typeof object.taskText !== "string" || object.taskText.length)
                    message.taskText = $String(object.taskText);
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            if (object.confirmedBy != null)
                if (typeof object.confirmedBy !== "string" || object.confirmedBy.length)
                    message.confirmedBy = $String(object.confirmedBy);
            if (object.reviewedBy != null)
                if (typeof object.reviewedBy !== "string" || object.reviewedBy.length)
                    message.reviewedBy = $String(object.reviewedBy);
            if (object.reviewedAt != null)
                if (typeof object.reviewedAt === "object" ? object.reviewedAt.low || object.reviewedAt.high : $Number(object.reviewedAt) !== 0)
                    if ($util.Long)
                        message.reviewedAt = $util.Long.fromValue(object.reviewedAt, false);
                    else if (typeof object.reviewedAt === "string")
                        message.reviewedAt = $parseInt(object.reviewedAt, 10);
                    else if (typeof object.reviewedAt === "number")
                        message.reviewedAt = object.reviewedAt;
                    else if (typeof object.reviewedAt === "object")
                        message.reviewedAt = new $util.LongBits(object.reviewedAt.low >>> 0, object.reviewedAt.high >>> 0).toNumber();
            if (object.rejectReason != null)
                if (typeof object.rejectReason !== "string" || object.rejectReason.length)
                    message.rejectReason = $String(object.rejectReason);
            if (object.redoTaskText != null)
                if (typeof object.redoTaskText !== "string" || object.redoTaskText.length)
                    message.redoTaskText = $String(object.redoTaskText);
            if (object.submittedAt != null)
                if (typeof object.submittedAt === "object" ? object.submittedAt.low || object.submittedAt.high : $Number(object.submittedAt) !== 0)
                    if ($util.Long)
                        message.submittedAt = $util.Long.fromValue(object.submittedAt, false);
                    else if (typeof object.submittedAt === "string")
                        message.submittedAt = $parseInt(object.submittedAt, 10);
                    else if (typeof object.submittedAt === "number")
                        message.submittedAt = object.submittedAt;
                    else if (typeof object.submittedAt === "object")
                        message.submittedAt = new $util.LongBits(object.submittedAt.low >>> 0, object.submittedAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a PunishmentProof message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PunishmentProof
         * @static
         * @param {game.PunishmentProof} message PunishmentProof
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PunishmentProof.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.playerId = "";
                object.text = "";
                object.imageUrl = "";
                object.taskText = "";
                object.status = "";
                object.confirmedBy = "";
                object.reviewedBy = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.reviewedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.reviewedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.rejectReason = "";
                object.redoTaskText = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.submittedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.submittedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                object.playerId = message.playerId;
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            if (message.imageUrl != null && $Object.hasOwnProperty.call(message, "imageUrl"))
                object.imageUrl = message.imageUrl;
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText"))
                object.taskText = message.taskText;
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            if (message.confirmedBy != null && $Object.hasOwnProperty.call(message, "confirmedBy"))
                object.confirmedBy = message.confirmedBy;
            if (message.reviewedBy != null && $Object.hasOwnProperty.call(message, "reviewedBy"))
                object.reviewedBy = message.reviewedBy;
            if (message.reviewedAt != null && $Object.hasOwnProperty.call(message, "reviewedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.reviewedAt = typeof message.reviewedAt === "number" ? $BigInt(message.reviewedAt) : $util.Long.fromBits(message.reviewedAt.low >>> 0, message.reviewedAt.high >>> 0, false).toBigInt();
                else if (typeof message.reviewedAt === "number")
                    object.reviewedAt = options.longs === $String ? $String(message.reviewedAt) : message.reviewedAt;
                else
                    object.reviewedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.reviewedAt) : options.longs === $Number ? new $util.LongBits(message.reviewedAt.low >>> 0, message.reviewedAt.high >>> 0).toNumber() : message.reviewedAt;
            if (message.rejectReason != null && $Object.hasOwnProperty.call(message, "rejectReason"))
                object.rejectReason = message.rejectReason;
            if (message.redoTaskText != null && $Object.hasOwnProperty.call(message, "redoTaskText"))
                object.redoTaskText = message.redoTaskText;
            if (message.submittedAt != null && $Object.hasOwnProperty.call(message, "submittedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.submittedAt = typeof message.submittedAt === "number" ? $BigInt(message.submittedAt) : $util.Long.fromBits(message.submittedAt.low >>> 0, message.submittedAt.high >>> 0, false).toBigInt();
                else if (typeof message.submittedAt === "number")
                    object.submittedAt = options.longs === $String ? $String(message.submittedAt) : message.submittedAt;
                else
                    object.submittedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.submittedAt) : options.longs === $Number ? new $util.LongBits(message.submittedAt.low >>> 0, message.submittedAt.high >>> 0).toNumber() : message.submittedAt;
            return object;
        };

        /**
         * Converts this PunishmentProof to JSON.
         * @function toJSON
         * @memberof game.PunishmentProof
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PunishmentProof.prototype.toJSON = function() {
            return PunishmentProof.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PunishmentProof
         * @function getTypeUrl
         * @memberof game.PunishmentProof
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PunishmentProof.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PunishmentProof";
        };

        return PunishmentProof;
    })();

    game.SeatStats = (function() {

        /**
         * Properties of a SeatStats.
         * @typedef {Object} game.SeatStats.$Properties
         * @property {number|null} [wins] SeatStats wins
         * @property {number|null} [losses] SeatStats losses
         * @property {number|null} [draws] SeatStats draws
         * @property {number|null} [punishments] SeatStats punishments
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SeatStats.
         * @memberof game
         * @interface ISeatStats
         * @augments game.SeatStats.$Properties
         * @deprecated Use game.SeatStats.$Properties instead.
         */

        /**
         * Shape of a SeatStats.
         * @typedef {game.SeatStats.$Properties} game.SeatStats.$Shape
         */

        /**
         * Constructs a new SeatStats.
         * @memberof game
         * @classdesc Represents a SeatStats.
         * @constructor
         * @param {game.SeatStats.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SeatStats = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SeatStats wins.
         * @member {number} wins
         * @memberof game.SeatStats
         * @instance
         */
        SeatStats.prototype.wins = 0;

        /**
         * SeatStats losses.
         * @member {number} losses
         * @memberof game.SeatStats
         * @instance
         */
        SeatStats.prototype.losses = 0;

        /**
         * SeatStats draws.
         * @member {number} draws
         * @memberof game.SeatStats
         * @instance
         */
        SeatStats.prototype.draws = 0;

        /**
         * SeatStats punishments.
         * @member {number} punishments
         * @memberof game.SeatStats
         * @instance
         */
        SeatStats.prototype.punishments = 0;

        /**
         * Creates a new SeatStats instance using the specified properties.
         * @function create
         * @memberof game.SeatStats
         * @static
         * @param {game.SeatStats.$Properties=} [properties] Properties to set
         * @returns {game.SeatStats} SeatStats instance
         * @type {{
         *   (properties: game.SeatStats.$Shape): game.SeatStats & game.SeatStats.$Shape;
         *   (properties?: game.SeatStats.$Properties): game.SeatStats;
         * }}
         */
        SeatStats.create = function(properties) {
            return new SeatStats(properties);
        };

        /**
         * Encodes the specified SeatStats message. Does not implicitly {@link game.SeatStats.verify|verify} messages.
         * @function encode
         * @memberof game.SeatStats
         * @static
         * @param {game.SeatStats.$Properties} message SeatStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SeatStats.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins") && message.wins !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.wins);
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses") && message.losses !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.losses);
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws") && message.draws !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.draws);
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments") && message.punishments !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.punishments);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SeatStats message, length delimited. Does not implicitly {@link game.SeatStats.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SeatStats
         * @static
         * @param {game.SeatStats.$Properties} message SeatStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SeatStats.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SeatStats message from the specified reader or buffer.
         * @function decode
         * @memberof game.SeatStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SeatStats & game.SeatStats.$Shape} SeatStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SeatStats.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SeatStats(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.wins = value;
                        else
                            delete message.wins;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.losses = value;
                        else
                            delete message.losses;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.draws = value;
                        else
                            delete message.draws;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.punishments = value;
                        else
                            delete message.punishments;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SeatStats message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SeatStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SeatStats & game.SeatStats.$Shape} SeatStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SeatStats.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SeatStats message.
         * @function verify
         * @memberof game.SeatStats
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SeatStats.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                if (!$util.isInteger(message.wins))
                    return "wins: integer expected";
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                if (!$util.isInteger(message.losses))
                    return "losses: integer expected";
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws"))
                if (!$util.isInteger(message.draws))
                    return "draws: integer expected";
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments"))
                if (!$util.isInteger(message.punishments))
                    return "punishments: integer expected";
            return null;
        };

        /**
         * Creates a SeatStats message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SeatStats
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SeatStats} SeatStats
         */
        SeatStats.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SeatStats)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SeatStats: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SeatStats();
            if (object.wins != null)
                if ($Number(object.wins) !== 0)
                    message.wins = object.wins | 0;
            if (object.losses != null)
                if ($Number(object.losses) !== 0)
                    message.losses = object.losses | 0;
            if (object.draws != null)
                if ($Number(object.draws) !== 0)
                    message.draws = object.draws | 0;
            if (object.punishments != null)
                if ($Number(object.punishments) !== 0)
                    message.punishments = object.punishments | 0;
            return message;
        };

        /**
         * Creates a plain object from a SeatStats message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SeatStats
         * @static
         * @param {game.SeatStats} message SeatStats
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SeatStats.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.wins = 0;
                object.losses = 0;
                object.draws = 0;
                object.punishments = 0;
            }
            if (message.wins != null && $Object.hasOwnProperty.call(message, "wins"))
                object.wins = message.wins;
            if (message.losses != null && $Object.hasOwnProperty.call(message, "losses"))
                object.losses = message.losses;
            if (message.draws != null && $Object.hasOwnProperty.call(message, "draws"))
                object.draws = message.draws;
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments"))
                object.punishments = message.punishments;
            return object;
        };

        /**
         * Converts this SeatStats to JSON.
         * @function toJSON
         * @memberof game.SeatStats
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SeatStats.prototype.toJSON = function() {
            return SeatStats.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SeatStats
         * @function getTypeUrl
         * @memberof game.SeatStats
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SeatStats.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SeatStats";
        };

        return SeatStats;
    })();

    game.OthelloScore = (function() {

        /**
         * Properties of an OthelloScore.
         * @typedef {Object} game.OthelloScore.$Properties
         * @property {number|null} [black] OthelloScore black
         * @property {number|null} [white] OthelloScore white
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an OthelloScore.
         * @memberof game
         * @interface IOthelloScore
         * @augments game.OthelloScore.$Properties
         * @deprecated Use game.OthelloScore.$Properties instead.
         */

        /**
         * Shape of an OthelloScore.
         * @typedef {game.OthelloScore.$Properties} game.OthelloScore.$Shape
         */

        /**
         * Constructs a new OthelloScore.
         * @memberof game
         * @classdesc Represents an OthelloScore.
         * @constructor
         * @param {game.OthelloScore.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const OthelloScore = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OthelloScore black.
         * @member {number} black
         * @memberof game.OthelloScore
         * @instance
         */
        OthelloScore.prototype.black = 0;

        /**
         * OthelloScore white.
         * @member {number} white
         * @memberof game.OthelloScore
         * @instance
         */
        OthelloScore.prototype.white = 0;

        /**
         * Creates a new OthelloScore instance using the specified properties.
         * @function create
         * @memberof game.OthelloScore
         * @static
         * @param {game.OthelloScore.$Properties=} [properties] Properties to set
         * @returns {game.OthelloScore} OthelloScore instance
         * @type {{
         *   (properties: game.OthelloScore.$Shape): game.OthelloScore & game.OthelloScore.$Shape;
         *   (properties?: game.OthelloScore.$Properties): game.OthelloScore;
         * }}
         */
        OthelloScore.create = function(properties) {
            return new OthelloScore(properties);
        };

        /**
         * Encodes the specified OthelloScore message. Does not implicitly {@link game.OthelloScore.verify|verify} messages.
         * @function encode
         * @memberof game.OthelloScore
         * @static
         * @param {game.OthelloScore.$Properties} message OthelloScore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OthelloScore.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.black != null && $Object.hasOwnProperty.call(message, "black") && message.black !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.black);
            if (message.white != null && $Object.hasOwnProperty.call(message, "white") && message.white !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.white);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OthelloScore message, length delimited. Does not implicitly {@link game.OthelloScore.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.OthelloScore
         * @static
         * @param {game.OthelloScore.$Properties} message OthelloScore message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OthelloScore.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an OthelloScore message from the specified reader or buffer.
         * @function decode
         * @memberof game.OthelloScore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.OthelloScore & game.OthelloScore.$Shape} OthelloScore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OthelloScore.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.OthelloScore(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.black = value;
                        else
                            delete message.black;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.white = value;
                        else
                            delete message.white;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OthelloScore message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.OthelloScore
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.OthelloScore & game.OthelloScore.$Shape} OthelloScore
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OthelloScore.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OthelloScore message.
         * @function verify
         * @memberof game.OthelloScore
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OthelloScore.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.black != null && $Object.hasOwnProperty.call(message, "black"))
                if (!$util.isInteger(message.black))
                    return "black: integer expected";
            if (message.white != null && $Object.hasOwnProperty.call(message, "white"))
                if (!$util.isInteger(message.white))
                    return "white: integer expected";
            return null;
        };

        /**
         * Creates an OthelloScore message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.OthelloScore
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.OthelloScore} OthelloScore
         */
        OthelloScore.fromObject = function (object, _depth) {
            if (object instanceof $root.game.OthelloScore)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.OthelloScore: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.OthelloScore();
            if (object.black != null)
                if ($Number(object.black) !== 0)
                    message.black = object.black | 0;
            if (object.white != null)
                if ($Number(object.white) !== 0)
                    message.white = object.white | 0;
            return message;
        };

        /**
         * Creates a plain object from an OthelloScore message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.OthelloScore
         * @static
         * @param {game.OthelloScore} message OthelloScore
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OthelloScore.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.black = 0;
                object.white = 0;
            }
            if (message.black != null && $Object.hasOwnProperty.call(message, "black"))
                object.black = message.black;
            if (message.white != null && $Object.hasOwnProperty.call(message, "white"))
                object.white = message.white;
            return object;
        };

        /**
         * Converts this OthelloScore to JSON.
         * @function toJSON
         * @memberof game.OthelloScore
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OthelloScore.prototype.toJSON = function() {
            return OthelloScore.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OthelloScore
         * @function getTypeUrl
         * @memberof game.OthelloScore
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OthelloScore.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.OthelloScore";
        };

        return OthelloScore;
    })();

    game.PunishmentTask = (function() {

        /**
         * Properties of a PunishmentTask.
         * @typedef {Object} game.PunishmentTask.$Properties
         * @property {string|null} [playerId] PunishmentTask playerId
         * @property {string|null} [playerName] PunishmentTask playerName
         * @property {string|null} [factionId] PunishmentTask factionId
         * @property {string|null} [factionLabel] PunishmentTask factionLabel
         * @property {string|null} [taskText] PunishmentTask taskText
         * @property {string|null} [backgroundImage] PunishmentTask backgroundImage
         * @property {number|null} [backgroundOpacity] PunishmentTask backgroundOpacity
         * @property {string|null} [assignedBy] PunishmentTask assignedBy
         * @property {string|null} [assignedByName] PunishmentTask assignedByName
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PunishmentTask.
         * @memberof game
         * @interface IPunishmentTask
         * @augments game.PunishmentTask.$Properties
         * @deprecated Use game.PunishmentTask.$Properties instead.
         */

        /**
         * Shape of a PunishmentTask.
         * @typedef {game.PunishmentTask.$Properties} game.PunishmentTask.$Shape
         */

        /**
         * Constructs a new PunishmentTask.
         * @memberof game
         * @classdesc Represents a PunishmentTask.
         * @constructor
         * @param {game.PunishmentTask.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PunishmentTask = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PunishmentTask playerId.
         * @member {string} playerId
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.playerId = "";

        /**
         * PunishmentTask playerName.
         * @member {string} playerName
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.playerName = "";

        /**
         * PunishmentTask factionId.
         * @member {string} factionId
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.factionId = "";

        /**
         * PunishmentTask factionLabel.
         * @member {string} factionLabel
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.factionLabel = "";

        /**
         * PunishmentTask taskText.
         * @member {string} taskText
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.taskText = "";

        /**
         * PunishmentTask backgroundImage.
         * @member {string} backgroundImage
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.backgroundImage = "";

        /**
         * PunishmentTask backgroundOpacity.
         * @member {number} backgroundOpacity
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.backgroundOpacity = 0;

        /**
         * PunishmentTask assignedBy.
         * @member {string} assignedBy
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.assignedBy = "";

        /**
         * PunishmentTask assignedByName.
         * @member {string} assignedByName
         * @memberof game.PunishmentTask
         * @instance
         */
        PunishmentTask.prototype.assignedByName = "";

        /**
         * Creates a new PunishmentTask instance using the specified properties.
         * @function create
         * @memberof game.PunishmentTask
         * @static
         * @param {game.PunishmentTask.$Properties=} [properties] Properties to set
         * @returns {game.PunishmentTask} PunishmentTask instance
         * @type {{
         *   (properties: game.PunishmentTask.$Shape): game.PunishmentTask & game.PunishmentTask.$Shape;
         *   (properties?: game.PunishmentTask.$Properties): game.PunishmentTask;
         * }}
         */
        PunishmentTask.create = function(properties) {
            return new PunishmentTask(properties);
        };

        /**
         * Encodes the specified PunishmentTask message. Does not implicitly {@link game.PunishmentTask.verify|verify} messages.
         * @function encode
         * @memberof game.PunishmentTask
         * @static
         * @param {game.PunishmentTask.$Properties} message PunishmentTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PunishmentTask.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId") && message.playerId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.playerId);
            if (message.playerName != null && $Object.hasOwnProperty.call(message, "playerName") && message.playerName !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.playerName);
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId") && message.factionId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.factionId);
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel") && message.factionLabel !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.factionLabel);
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText") && message.taskText !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.taskText);
            if (message.backgroundImage != null && $Object.hasOwnProperty.call(message, "backgroundImage") && message.backgroundImage !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.backgroundImage);
            if (message.backgroundOpacity != null && $Object.hasOwnProperty.call(message, "backgroundOpacity") && !$Object.is(message.backgroundOpacity, 0))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.backgroundOpacity);
            if (message.assignedBy != null && $Object.hasOwnProperty.call(message, "assignedBy") && message.assignedBy !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.assignedBy);
            if (message.assignedByName != null && $Object.hasOwnProperty.call(message, "assignedByName") && message.assignedByName !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.assignedByName);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PunishmentTask message, length delimited. Does not implicitly {@link game.PunishmentTask.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PunishmentTask
         * @static
         * @param {game.PunishmentTask.$Properties} message PunishmentTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PunishmentTask.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PunishmentTask message from the specified reader or buffer.
         * @function decode
         * @memberof game.PunishmentTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PunishmentTask & game.PunishmentTask.$Shape} PunishmentTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PunishmentTask.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PunishmentTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerId = value;
                        else
                            delete message.playerId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerName = value;
                        else
                            delete message.playerName;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.factionId = value;
                        else
                            delete message.factionId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.factionLabel = value;
                        else
                            delete message.factionLabel;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.taskText = value;
                        else
                            delete message.taskText;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.backgroundImage = value;
                        else
                            delete message.backgroundImage;
                        continue;
                    }
                case 7: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.backgroundOpacity = value;
                        else
                            delete message.backgroundOpacity;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.assignedBy = value;
                        else
                            delete message.assignedBy;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.assignedByName = value;
                        else
                            delete message.assignedByName;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PunishmentTask message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PunishmentTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PunishmentTask & game.PunishmentTask.$Shape} PunishmentTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PunishmentTask.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PunishmentTask message.
         * @function verify
         * @memberof game.PunishmentTask
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PunishmentTask.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                if (!$util.isString(message.playerId))
                    return "playerId: string expected";
            if (message.playerName != null && $Object.hasOwnProperty.call(message, "playerName"))
                if (!$util.isString(message.playerName))
                    return "playerName: string expected";
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                if (!$util.isString(message.factionId))
                    return "factionId: string expected";
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel"))
                if (!$util.isString(message.factionLabel))
                    return "factionLabel: string expected";
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText"))
                if (!$util.isString(message.taskText))
                    return "taskText: string expected";
            if (message.backgroundImage != null && $Object.hasOwnProperty.call(message, "backgroundImage"))
                if (!$util.isString(message.backgroundImage))
                    return "backgroundImage: string expected";
            if (message.backgroundOpacity != null && $Object.hasOwnProperty.call(message, "backgroundOpacity"))
                if (typeof message.backgroundOpacity !== "number")
                    return "backgroundOpacity: number expected";
            if (message.assignedBy != null && $Object.hasOwnProperty.call(message, "assignedBy"))
                if (!$util.isString(message.assignedBy))
                    return "assignedBy: string expected";
            if (message.assignedByName != null && $Object.hasOwnProperty.call(message, "assignedByName"))
                if (!$util.isString(message.assignedByName))
                    return "assignedByName: string expected";
            return null;
        };

        /**
         * Creates a PunishmentTask message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PunishmentTask
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PunishmentTask} PunishmentTask
         */
        PunishmentTask.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PunishmentTask)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PunishmentTask: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PunishmentTask();
            if (object.playerId != null)
                if (typeof object.playerId !== "string" || object.playerId.length)
                    message.playerId = $String(object.playerId);
            if (object.playerName != null)
                if (typeof object.playerName !== "string" || object.playerName.length)
                    message.playerName = $String(object.playerName);
            if (object.factionId != null)
                if (typeof object.factionId !== "string" || object.factionId.length)
                    message.factionId = $String(object.factionId);
            if (object.factionLabel != null)
                if (typeof object.factionLabel !== "string" || object.factionLabel.length)
                    message.factionLabel = $String(object.factionLabel);
            if (object.taskText != null)
                if (typeof object.taskText !== "string" || object.taskText.length)
                    message.taskText = $String(object.taskText);
            if (object.backgroundImage != null)
                if (typeof object.backgroundImage !== "string" || object.backgroundImage.length)
                    message.backgroundImage = $String(object.backgroundImage);
            if (object.backgroundOpacity != null)
                if (!$Object.is($Number(object.backgroundOpacity), 0))
                    message.backgroundOpacity = $Number(object.backgroundOpacity);
            if (object.assignedBy != null)
                if (typeof object.assignedBy !== "string" || object.assignedBy.length)
                    message.assignedBy = $String(object.assignedBy);
            if (object.assignedByName != null)
                if (typeof object.assignedByName !== "string" || object.assignedByName.length)
                    message.assignedByName = $String(object.assignedByName);
            return message;
        };

        /**
         * Creates a plain object from a PunishmentTask message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PunishmentTask
         * @static
         * @param {game.PunishmentTask} message PunishmentTask
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PunishmentTask.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.playerId = "";
                object.playerName = "";
                object.factionId = "";
                object.factionLabel = "";
                object.taskText = "";
                object.backgroundImage = "";
                object.backgroundOpacity = 0;
                object.assignedBy = "";
                object.assignedByName = "";
            }
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                object.playerId = message.playerId;
            if (message.playerName != null && $Object.hasOwnProperty.call(message, "playerName"))
                object.playerName = message.playerName;
            if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                object.factionId = message.factionId;
            if (message.factionLabel != null && $Object.hasOwnProperty.call(message, "factionLabel"))
                object.factionLabel = message.factionLabel;
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText"))
                object.taskText = message.taskText;
            if (message.backgroundImage != null && $Object.hasOwnProperty.call(message, "backgroundImage"))
                object.backgroundImage = message.backgroundImage;
            if (message.backgroundOpacity != null && $Object.hasOwnProperty.call(message, "backgroundOpacity"))
                object.backgroundOpacity = options.json && !$isFinite(message.backgroundOpacity) ? $String(message.backgroundOpacity) : message.backgroundOpacity;
            if (message.assignedBy != null && $Object.hasOwnProperty.call(message, "assignedBy"))
                object.assignedBy = message.assignedBy;
            if (message.assignedByName != null && $Object.hasOwnProperty.call(message, "assignedByName"))
                object.assignedByName = message.assignedByName;
            return object;
        };

        /**
         * Converts this PunishmentTask to JSON.
         * @function toJSON
         * @memberof game.PunishmentTask
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PunishmentTask.prototype.toJSON = function() {
            return PunishmentTask.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PunishmentTask
         * @function getTypeUrl
         * @memberof game.PunishmentTask
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PunishmentTask.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PunishmentTask";
        };

        return PunishmentTask;
    })();

    game.HistoryProof = (function() {

        /**
         * Properties of a HistoryProof.
         * @typedef {Object} game.HistoryProof.$Properties
         * @property {string|null} [playerId] HistoryProof playerId
         * @property {string|null} [playerName] HistoryProof playerName
         * @property {string|null} [text] HistoryProof text
         * @property {string|null} [imageUrl] HistoryProof imageUrl
         * @property {string|null} [taskText] HistoryProof taskText
         * @property {string|null} [status] HistoryProof status
         * @property {string|null} [reviewedBy] HistoryProof reviewedBy
         * @property {number|Long|null} [reviewedAt] HistoryProof reviewedAt
         * @property {string|null} [rejectReason] HistoryProof rejectReason
         * @property {string|null} [redoTaskText] HistoryProof redoTaskText
         * @property {number|Long|null} [submittedAt] HistoryProof submittedAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a HistoryProof.
         * @memberof game
         * @interface IHistoryProof
         * @augments game.HistoryProof.$Properties
         * @deprecated Use game.HistoryProof.$Properties instead.
         */

        /**
         * Shape of a HistoryProof.
         * @typedef {game.HistoryProof.$Properties} game.HistoryProof.$Shape
         */

        /**
         * Constructs a new HistoryProof.
         * @memberof game
         * @classdesc Represents a HistoryProof.
         * @constructor
         * @param {game.HistoryProof.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const HistoryProof = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HistoryProof playerId.
         * @member {string} playerId
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.playerId = "";

        /**
         * HistoryProof playerName.
         * @member {string} playerName
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.playerName = "";

        /**
         * HistoryProof text.
         * @member {string} text
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.text = "";

        /**
         * HistoryProof imageUrl.
         * @member {string} imageUrl
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.imageUrl = "";

        /**
         * HistoryProof taskText.
         * @member {string} taskText
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.taskText = "";

        /**
         * HistoryProof status.
         * @member {string} status
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.status = "";

        /**
         * HistoryProof reviewedBy.
         * @member {string} reviewedBy
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.reviewedBy = "";

        /**
         * HistoryProof reviewedAt.
         * @member {number|Long} reviewedAt
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.reviewedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * HistoryProof rejectReason.
         * @member {string} rejectReason
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.rejectReason = "";

        /**
         * HistoryProof redoTaskText.
         * @member {string} redoTaskText
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.redoTaskText = "";

        /**
         * HistoryProof submittedAt.
         * @member {number|Long} submittedAt
         * @memberof game.HistoryProof
         * @instance
         */
        HistoryProof.prototype.submittedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new HistoryProof instance using the specified properties.
         * @function create
         * @memberof game.HistoryProof
         * @static
         * @param {game.HistoryProof.$Properties=} [properties] Properties to set
         * @returns {game.HistoryProof} HistoryProof instance
         * @type {{
         *   (properties: game.HistoryProof.$Shape): game.HistoryProof & game.HistoryProof.$Shape;
         *   (properties?: game.HistoryProof.$Properties): game.HistoryProof;
         * }}
         */
        HistoryProof.create = function(properties) {
            return new HistoryProof(properties);
        };

        /**
         * Encodes the specified HistoryProof message. Does not implicitly {@link game.HistoryProof.verify|verify} messages.
         * @function encode
         * @memberof game.HistoryProof
         * @static
         * @param {game.HistoryProof.$Properties} message HistoryProof message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HistoryProof.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId") && message.playerId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.playerId);
            if (message.playerName != null && $Object.hasOwnProperty.call(message, "playerName") && message.playerName !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.playerName);
            if (message.text != null && $Object.hasOwnProperty.call(message, "text") && message.text !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.text);
            if (message.imageUrl != null && $Object.hasOwnProperty.call(message, "imageUrl") && message.imageUrl !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.imageUrl);
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText") && message.taskText !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.taskText);
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.status);
            if (message.reviewedBy != null && $Object.hasOwnProperty.call(message, "reviewedBy") && message.reviewedBy !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.reviewedBy);
            if (message.reviewedAt != null && $Object.hasOwnProperty.call(message, "reviewedAt") && (typeof message.reviewedAt === "object" ? message.reviewedAt.low || message.reviewedAt.high : message.reviewedAt !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.reviewedAt);
            if (message.rejectReason != null && $Object.hasOwnProperty.call(message, "rejectReason") && message.rejectReason !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.rejectReason);
            if (message.redoTaskText != null && $Object.hasOwnProperty.call(message, "redoTaskText") && message.redoTaskText !== "")
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.redoTaskText);
            if (message.submittedAt != null && $Object.hasOwnProperty.call(message, "submittedAt") && (typeof message.submittedAt === "object" ? message.submittedAt.low || message.submittedAt.high : message.submittedAt !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.submittedAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HistoryProof message, length delimited. Does not implicitly {@link game.HistoryProof.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.HistoryProof
         * @static
         * @param {game.HistoryProof.$Properties} message HistoryProof message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HistoryProof.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a HistoryProof message from the specified reader or buffer.
         * @function decode
         * @memberof game.HistoryProof
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.HistoryProof & game.HistoryProof.$Shape} HistoryProof
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HistoryProof.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.HistoryProof(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerId = value;
                        else
                            delete message.playerId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerName = value;
                        else
                            delete message.playerName;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.text = value;
                        else
                            delete message.text;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.imageUrl = value;
                        else
                            delete message.imageUrl;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.taskText = value;
                        else
                            delete message.taskText;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.reviewedBy = value;
                        else
                            delete message.reviewedBy;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.reviewedAt = value;
                        else
                            delete message.reviewedAt;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.rejectReason = value;
                        else
                            delete message.rejectReason;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.redoTaskText = value;
                        else
                            delete message.redoTaskText;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.submittedAt = value;
                        else
                            delete message.submittedAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HistoryProof message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.HistoryProof
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.HistoryProof & game.HistoryProof.$Shape} HistoryProof
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HistoryProof.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HistoryProof message.
         * @function verify
         * @memberof game.HistoryProof
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HistoryProof.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                if (!$util.isString(message.playerId))
                    return "playerId: string expected";
            if (message.playerName != null && $Object.hasOwnProperty.call(message, "playerName"))
                if (!$util.isString(message.playerName))
                    return "playerName: string expected";
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                if (!$util.isString(message.text))
                    return "text: string expected";
            if (message.imageUrl != null && $Object.hasOwnProperty.call(message, "imageUrl"))
                if (!$util.isString(message.imageUrl))
                    return "imageUrl: string expected";
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText"))
                if (!$util.isString(message.taskText))
                    return "taskText: string expected";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.reviewedBy != null && $Object.hasOwnProperty.call(message, "reviewedBy"))
                if (!$util.isString(message.reviewedBy))
                    return "reviewedBy: string expected";
            if (message.reviewedAt != null && $Object.hasOwnProperty.call(message, "reviewedAt"))
                if (!$util.isInteger(message.reviewedAt) && !(message.reviewedAt && $util.isInteger(message.reviewedAt.low) && $util.isInteger(message.reviewedAt.high)))
                    return "reviewedAt: integer|Long expected";
            if (message.rejectReason != null && $Object.hasOwnProperty.call(message, "rejectReason"))
                if (!$util.isString(message.rejectReason))
                    return "rejectReason: string expected";
            if (message.redoTaskText != null && $Object.hasOwnProperty.call(message, "redoTaskText"))
                if (!$util.isString(message.redoTaskText))
                    return "redoTaskText: string expected";
            if (message.submittedAt != null && $Object.hasOwnProperty.call(message, "submittedAt"))
                if (!$util.isInteger(message.submittedAt) && !(message.submittedAt && $util.isInteger(message.submittedAt.low) && $util.isInteger(message.submittedAt.high)))
                    return "submittedAt: integer|Long expected";
            return null;
        };

        /**
         * Creates a HistoryProof message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.HistoryProof
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.HistoryProof} HistoryProof
         */
        HistoryProof.fromObject = function (object, _depth) {
            if (object instanceof $root.game.HistoryProof)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.HistoryProof: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.HistoryProof();
            if (object.playerId != null)
                if (typeof object.playerId !== "string" || object.playerId.length)
                    message.playerId = $String(object.playerId);
            if (object.playerName != null)
                if (typeof object.playerName !== "string" || object.playerName.length)
                    message.playerName = $String(object.playerName);
            if (object.text != null)
                if (typeof object.text !== "string" || object.text.length)
                    message.text = $String(object.text);
            if (object.imageUrl != null)
                if (typeof object.imageUrl !== "string" || object.imageUrl.length)
                    message.imageUrl = $String(object.imageUrl);
            if (object.taskText != null)
                if (typeof object.taskText !== "string" || object.taskText.length)
                    message.taskText = $String(object.taskText);
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            if (object.reviewedBy != null)
                if (typeof object.reviewedBy !== "string" || object.reviewedBy.length)
                    message.reviewedBy = $String(object.reviewedBy);
            if (object.reviewedAt != null)
                if (typeof object.reviewedAt === "object" ? object.reviewedAt.low || object.reviewedAt.high : $Number(object.reviewedAt) !== 0)
                    if ($util.Long)
                        message.reviewedAt = $util.Long.fromValue(object.reviewedAt, false);
                    else if (typeof object.reviewedAt === "string")
                        message.reviewedAt = $parseInt(object.reviewedAt, 10);
                    else if (typeof object.reviewedAt === "number")
                        message.reviewedAt = object.reviewedAt;
                    else if (typeof object.reviewedAt === "object")
                        message.reviewedAt = new $util.LongBits(object.reviewedAt.low >>> 0, object.reviewedAt.high >>> 0).toNumber();
            if (object.rejectReason != null)
                if (typeof object.rejectReason !== "string" || object.rejectReason.length)
                    message.rejectReason = $String(object.rejectReason);
            if (object.redoTaskText != null)
                if (typeof object.redoTaskText !== "string" || object.redoTaskText.length)
                    message.redoTaskText = $String(object.redoTaskText);
            if (object.submittedAt != null)
                if (typeof object.submittedAt === "object" ? object.submittedAt.low || object.submittedAt.high : $Number(object.submittedAt) !== 0)
                    if ($util.Long)
                        message.submittedAt = $util.Long.fromValue(object.submittedAt, false);
                    else if (typeof object.submittedAt === "string")
                        message.submittedAt = $parseInt(object.submittedAt, 10);
                    else if (typeof object.submittedAt === "number")
                        message.submittedAt = object.submittedAt;
                    else if (typeof object.submittedAt === "object")
                        message.submittedAt = new $util.LongBits(object.submittedAt.low >>> 0, object.submittedAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a HistoryProof message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.HistoryProof
         * @static
         * @param {game.HistoryProof} message HistoryProof
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HistoryProof.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.playerId = "";
                object.playerName = "";
                object.text = "";
                object.imageUrl = "";
                object.taskText = "";
                object.status = "";
                object.reviewedBy = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.reviewedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.reviewedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.rejectReason = "";
                object.redoTaskText = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.submittedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.submittedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                object.playerId = message.playerId;
            if (message.playerName != null && $Object.hasOwnProperty.call(message, "playerName"))
                object.playerName = message.playerName;
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            if (message.imageUrl != null && $Object.hasOwnProperty.call(message, "imageUrl"))
                object.imageUrl = message.imageUrl;
            if (message.taskText != null && $Object.hasOwnProperty.call(message, "taskText"))
                object.taskText = message.taskText;
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            if (message.reviewedBy != null && $Object.hasOwnProperty.call(message, "reviewedBy"))
                object.reviewedBy = message.reviewedBy;
            if (message.reviewedAt != null && $Object.hasOwnProperty.call(message, "reviewedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.reviewedAt = typeof message.reviewedAt === "number" ? $BigInt(message.reviewedAt) : $util.Long.fromBits(message.reviewedAt.low >>> 0, message.reviewedAt.high >>> 0, false).toBigInt();
                else if (typeof message.reviewedAt === "number")
                    object.reviewedAt = options.longs === $String ? $String(message.reviewedAt) : message.reviewedAt;
                else
                    object.reviewedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.reviewedAt) : options.longs === $Number ? new $util.LongBits(message.reviewedAt.low >>> 0, message.reviewedAt.high >>> 0).toNumber() : message.reviewedAt;
            if (message.rejectReason != null && $Object.hasOwnProperty.call(message, "rejectReason"))
                object.rejectReason = message.rejectReason;
            if (message.redoTaskText != null && $Object.hasOwnProperty.call(message, "redoTaskText"))
                object.redoTaskText = message.redoTaskText;
            if (message.submittedAt != null && $Object.hasOwnProperty.call(message, "submittedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.submittedAt = typeof message.submittedAt === "number" ? $BigInt(message.submittedAt) : $util.Long.fromBits(message.submittedAt.low >>> 0, message.submittedAt.high >>> 0, false).toBigInt();
                else if (typeof message.submittedAt === "number")
                    object.submittedAt = options.longs === $String ? $String(message.submittedAt) : message.submittedAt;
                else
                    object.submittedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.submittedAt) : options.longs === $Number ? new $util.LongBits(message.submittedAt.low >>> 0, message.submittedAt.high >>> 0).toNumber() : message.submittedAt;
            return object;
        };

        /**
         * Converts this HistoryProof to JSON.
         * @function toJSON
         * @memberof game.HistoryProof
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HistoryProof.prototype.toJSON = function() {
            return HistoryProof.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HistoryProof
         * @function getTypeUrl
         * @memberof game.HistoryProof
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HistoryProof.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.HistoryProof";
        };

        return HistoryProof;
    })();

    game.RoundHistoryItem = (function() {

        /**
         * Properties of a RoundHistoryItem.
         * @typedef {Object} game.RoundHistoryItem.$Properties
         * @property {string|null} [id] RoundHistoryItem id
         * @property {number|null} [round] RoundHistoryItem round
         * @property {number|Long|null} [at] RoundHistoryItem at
         * @property {string|null} [playerA] RoundHistoryItem playerA
         * @property {string|null} [playerB] RoundHistoryItem playerB
         * @property {string|null} [moveA] RoundHistoryItem moveA
         * @property {string|null} [moveB] RoundHistoryItem moveB
         * @property {string|null} [result] RoundHistoryItem result
         * @property {string|null} [resultLabel] RoundHistoryItem resultLabel
         * @property {string|null} [resultText] RoundHistoryItem resultText
         * @property {string|null} [gameId] RoundHistoryItem gameId
         * @property {game.OthelloScore.$Properties|null} [othelloScore] RoundHistoryItem othelloScore
         * @property {string|null} [othelloBlackSeat] RoundHistoryItem othelloBlackSeat
         * @property {string|null} [tictactoeXSeat] RoundHistoryItem tictactoeXSeat
         * @property {Array.<game.Pos.$Properties>|null} [tictactoeLine] RoundHistoryItem tictactoeLine
         * @property {boolean|null} [ranked] RoundHistoryItem ranked
         * @property {number|null} [stake] RoundHistoryItem stake
         * @property {number|null} [rankMultiplier] RoundHistoryItem rankMultiplier
         * @property {number|null} [effectiveStake] RoundHistoryItem effectiveStake
         * @property {boolean|null} [extremeRanked] RoundHistoryItem extremeRanked
         * @property {string|null} [punishmentName] RoundHistoryItem punishmentName
         * @property {string|null} [punishmentDescription] RoundHistoryItem punishmentDescription
         * @property {Array.<game.PunishmentTask.$Properties>|null} [punishmentTasks] RoundHistoryItem punishmentTasks
         * @property {Array.<string>|null} [punishedNames] RoundHistoryItem punishedNames
         * @property {Array.<game.HistoryProof.$Properties>|null} [proofs] RoundHistoryItem proofs
         * @property {string|null} [liarsDiceWinnerId] RoundHistoryItem liarsDiceWinnerId
         * @property {string|null} [liarsDiceLoserId] RoundHistoryItem liarsDiceLoserId
         * @property {number|null} [liarsDiceBidCount] RoundHistoryItem liarsDiceBidCount
         * @property {number|null} [liarsDiceBidFace] RoundHistoryItem liarsDiceBidFace
         * @property {number|null} [liarsDiceActualCount] RoundHistoryItem liarsDiceActualCount
         * @property {Array.<game.LiarsDiceHandsPair.$Properties>|null} [liarsDiceHands] RoundHistoryItem liarsDiceHands
         * @property {Array.<string>|null} [liarsDiceHandOrder] RoundHistoryItem liarsDiceHandOrder
         * @property {Array.<game.StringPair.$Properties>|null} [liarsDiceNames] RoundHistoryItem liarsDiceNames
         * @property {string|null} [gomokuBlackSeat] RoundHistoryItem gomokuBlackSeat
         * @property {Array.<game.Pos.$Properties>|null} [gomokuLine] RoundHistoryItem gomokuLine
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoundHistoryItem.
         * @memberof game
         * @interface IRoundHistoryItem
         * @augments game.RoundHistoryItem.$Properties
         * @deprecated Use game.RoundHistoryItem.$Properties instead.
         */

        /**
         * Shape of a RoundHistoryItem.
         * @typedef {game.RoundHistoryItem.$Properties} game.RoundHistoryItem.$Shape
         */

        /**
         * Constructs a new RoundHistoryItem.
         * @memberof game
         * @classdesc Represents a RoundHistoryItem.
         * @constructor
         * @param {game.RoundHistoryItem.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoundHistoryItem = function (properties) {
            this.tictactoeLine = [];
            this.punishmentTasks = [];
            this.punishedNames = [];
            this.proofs = [];
            this.liarsDiceHands = [];
            this.liarsDiceHandOrder = [];
            this.liarsDiceNames = [];
            this.gomokuLine = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoundHistoryItem id.
         * @member {string} id
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.id = "";

        /**
         * RoundHistoryItem round.
         * @member {number} round
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.round = 0;

        /**
         * RoundHistoryItem at.
         * @member {number|Long} at
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.at = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoundHistoryItem playerA.
         * @member {string} playerA
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.playerA = "";

        /**
         * RoundHistoryItem playerB.
         * @member {string} playerB
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.playerB = "";

        /**
         * RoundHistoryItem moveA.
         * @member {string} moveA
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.moveA = "";

        /**
         * RoundHistoryItem moveB.
         * @member {string} moveB
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.moveB = "";

        /**
         * RoundHistoryItem result.
         * @member {string} result
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.result = "";

        /**
         * RoundHistoryItem resultLabel.
         * @member {string} resultLabel
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.resultLabel = "";

        /**
         * RoundHistoryItem resultText.
         * @member {string} resultText
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.resultText = "";

        /**
         * RoundHistoryItem gameId.
         * @member {string} gameId
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.gameId = "";

        /**
         * RoundHistoryItem othelloScore.
         * @member {game.OthelloScore.$Properties|null|undefined} othelloScore
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.othelloScore = null;

        /**
         * RoundHistoryItem othelloBlackSeat.
         * @member {string} othelloBlackSeat
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.othelloBlackSeat = "";

        /**
         * RoundHistoryItem tictactoeXSeat.
         * @member {string} tictactoeXSeat
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.tictactoeXSeat = "";

        /**
         * RoundHistoryItem tictactoeLine.
         * @member {Array.<game.Pos.$Properties>} tictactoeLine
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.tictactoeLine = $util.emptyArray;

        /**
         * RoundHistoryItem ranked.
         * @member {boolean} ranked
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.ranked = false;

        /**
         * RoundHistoryItem stake.
         * @member {number} stake
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.stake = 0;

        /**
         * RoundHistoryItem rankMultiplier.
         * @member {number} rankMultiplier
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.rankMultiplier = 0;

        /**
         * RoundHistoryItem effectiveStake.
         * @member {number} effectiveStake
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.effectiveStake = 0;

        /**
         * RoundHistoryItem extremeRanked.
         * @member {boolean} extremeRanked
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.extremeRanked = false;

        /**
         * RoundHistoryItem punishmentName.
         * @member {string} punishmentName
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.punishmentName = "";

        /**
         * RoundHistoryItem punishmentDescription.
         * @member {string} punishmentDescription
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.punishmentDescription = "";

        /**
         * RoundHistoryItem punishmentTasks.
         * @member {Array.<game.PunishmentTask.$Properties>} punishmentTasks
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.punishmentTasks = $util.emptyArray;

        /**
         * RoundHistoryItem punishedNames.
         * @member {Array.<string>} punishedNames
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.punishedNames = $util.emptyArray;

        /**
         * RoundHistoryItem proofs.
         * @member {Array.<game.HistoryProof.$Properties>} proofs
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.proofs = $util.emptyArray;

        /**
         * RoundHistoryItem liarsDiceWinnerId.
         * @member {string} liarsDiceWinnerId
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.liarsDiceWinnerId = "";

        /**
         * RoundHistoryItem liarsDiceLoserId.
         * @member {string} liarsDiceLoserId
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.liarsDiceLoserId = "";

        /**
         * RoundHistoryItem liarsDiceBidCount.
         * @member {number} liarsDiceBidCount
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.liarsDiceBidCount = 0;

        /**
         * RoundHistoryItem liarsDiceBidFace.
         * @member {number} liarsDiceBidFace
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.liarsDiceBidFace = 0;

        /**
         * RoundHistoryItem liarsDiceActualCount.
         * @member {number} liarsDiceActualCount
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.liarsDiceActualCount = 0;

        /**
         * RoundHistoryItem liarsDiceHands.
         * @member {Array.<game.LiarsDiceHandsPair.$Properties>} liarsDiceHands
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.liarsDiceHands = $util.emptyArray;

        /**
         * RoundHistoryItem liarsDiceHandOrder.
         * @member {Array.<string>} liarsDiceHandOrder
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.liarsDiceHandOrder = $util.emptyArray;

        /**
         * RoundHistoryItem liarsDiceNames.
         * @member {Array.<game.StringPair.$Properties>} liarsDiceNames
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.liarsDiceNames = $util.emptyArray;

        /**
         * RoundHistoryItem gomokuBlackSeat.
         * @member {string} gomokuBlackSeat
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.gomokuBlackSeat = "";

        /**
         * RoundHistoryItem gomokuLine.
         * @member {Array.<game.Pos.$Properties>} gomokuLine
         * @memberof game.RoundHistoryItem
         * @instance
         */
        RoundHistoryItem.prototype.gomokuLine = $util.emptyArray;

        /**
         * Creates a new RoundHistoryItem instance using the specified properties.
         * @function create
         * @memberof game.RoundHistoryItem
         * @static
         * @param {game.RoundHistoryItem.$Properties=} [properties] Properties to set
         * @returns {game.RoundHistoryItem} RoundHistoryItem instance
         * @type {{
         *   (properties: game.RoundHistoryItem.$Shape): game.RoundHistoryItem & game.RoundHistoryItem.$Shape;
         *   (properties?: game.RoundHistoryItem.$Properties): game.RoundHistoryItem;
         * }}
         */
        RoundHistoryItem.create = function(properties) {
            return new RoundHistoryItem(properties);
        };

        /**
         * Encodes the specified RoundHistoryItem message. Does not implicitly {@link game.RoundHistoryItem.verify|verify} messages.
         * @function encode
         * @memberof game.RoundHistoryItem
         * @static
         * @param {game.RoundHistoryItem.$Properties} message RoundHistoryItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundHistoryItem.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.round != null && $Object.hasOwnProperty.call(message, "round") && message.round !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.round);
            if (message.at != null && $Object.hasOwnProperty.call(message, "at") && (typeof message.at === "object" ? message.at.low || message.at.high : message.at !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.at);
            if (message.playerA != null && $Object.hasOwnProperty.call(message, "playerA") && message.playerA !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.playerA);
            if (message.playerB != null && $Object.hasOwnProperty.call(message, "playerB") && message.playerB !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.playerB);
            if (message.moveA != null && $Object.hasOwnProperty.call(message, "moveA") && message.moveA !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.moveA);
            if (message.moveB != null && $Object.hasOwnProperty.call(message, "moveB") && message.moveB !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.moveB);
            if (message.result != null && $Object.hasOwnProperty.call(message, "result") && message.result !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.result);
            if (message.resultLabel != null && $Object.hasOwnProperty.call(message, "resultLabel") && message.resultLabel !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.resultLabel);
            if (message.resultText != null && $Object.hasOwnProperty.call(message, "resultText") && message.resultText !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.resultText);
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId") && message.gameId !== "")
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.gameId);
            if (message.othelloScore != null && $Object.hasOwnProperty.call(message, "othelloScore"))
                $root.game.OthelloScore.encode(message.othelloScore, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.othelloBlackSeat != null && $Object.hasOwnProperty.call(message, "othelloBlackSeat") && message.othelloBlackSeat !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.othelloBlackSeat);
            if (message.tictactoeXSeat != null && $Object.hasOwnProperty.call(message, "tictactoeXSeat") && message.tictactoeXSeat !== "")
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.tictactoeXSeat);
            if (message.tictactoeLine != null && message.tictactoeLine.length)
                for (let i = 0; i < message.tictactoeLine.length; ++i)
                    $root.game.Pos.encode(message.tictactoeLine[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.ranked != null && $Object.hasOwnProperty.call(message, "ranked") && message.ranked !== false)
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.ranked);
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake") && message.stake !== 0)
                writer.uint32(/* id 17, wireType 0 =*/136).int32(message.stake);
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier") && message.rankMultiplier !== 0)
                writer.uint32(/* id 19, wireType 0 =*/152).int32(message.rankMultiplier);
            if (message.effectiveStake != null && $Object.hasOwnProperty.call(message, "effectiveStake") && message.effectiveStake !== 0)
                writer.uint32(/* id 21, wireType 0 =*/168).int32(message.effectiveStake);
            if (message.extremeRanked != null && $Object.hasOwnProperty.call(message, "extremeRanked") && message.extremeRanked !== false)
                writer.uint32(/* id 23, wireType 0 =*/184).bool(message.extremeRanked);
            if (message.punishmentName != null && $Object.hasOwnProperty.call(message, "punishmentName") && message.punishmentName !== "")
                writer.uint32(/* id 24, wireType 2 =*/194).string(message.punishmentName);
            if (message.punishmentDescription != null && $Object.hasOwnProperty.call(message, "punishmentDescription") && message.punishmentDescription !== "")
                writer.uint32(/* id 25, wireType 2 =*/202).string(message.punishmentDescription);
            if (message.punishmentTasks != null && message.punishmentTasks.length)
                for (let i = 0; i < message.punishmentTasks.length; ++i)
                    $root.game.PunishmentTask.encode(message.punishmentTasks[i], writer.uint32(/* id 26, wireType 2 =*/210).fork(), _depth + 1).ldelim();
            if (message.punishedNames != null && message.punishedNames.length)
                for (let i = 0; i < message.punishedNames.length; ++i)
                    writer.uint32(/* id 27, wireType 2 =*/218).string(message.punishedNames[i]);
            if (message.proofs != null && message.proofs.length)
                for (let i = 0; i < message.proofs.length; ++i)
                    $root.game.HistoryProof.encode(message.proofs[i], writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
            if (message.liarsDiceWinnerId != null && $Object.hasOwnProperty.call(message, "liarsDiceWinnerId") && message.liarsDiceWinnerId !== "")
                writer.uint32(/* id 29, wireType 2 =*/234).string(message.liarsDiceWinnerId);
            if (message.liarsDiceLoserId != null && $Object.hasOwnProperty.call(message, "liarsDiceLoserId") && message.liarsDiceLoserId !== "")
                writer.uint32(/* id 30, wireType 2 =*/242).string(message.liarsDiceLoserId);
            if (message.liarsDiceBidCount != null && $Object.hasOwnProperty.call(message, "liarsDiceBidCount") && message.liarsDiceBidCount !== 0)
                writer.uint32(/* id 31, wireType 0 =*/248).int32(message.liarsDiceBidCount);
            if (message.liarsDiceBidFace != null && $Object.hasOwnProperty.call(message, "liarsDiceBidFace") && message.liarsDiceBidFace !== 0)
                writer.uint32(/* id 32, wireType 0 =*/256).int32(message.liarsDiceBidFace);
            if (message.liarsDiceActualCount != null && $Object.hasOwnProperty.call(message, "liarsDiceActualCount") && message.liarsDiceActualCount !== 0)
                writer.uint32(/* id 33, wireType 0 =*/264).int32(message.liarsDiceActualCount);
            if (message.liarsDiceHands != null && message.liarsDiceHands.length)
                for (let i = 0; i < message.liarsDiceHands.length; ++i)
                    $root.game.LiarsDiceHandsPair.encode(message.liarsDiceHands[i], writer.uint32(/* id 34, wireType 2 =*/274).fork(), _depth + 1).ldelim();
            if (message.liarsDiceHandOrder != null && message.liarsDiceHandOrder.length)
                for (let i = 0; i < message.liarsDiceHandOrder.length; ++i)
                    writer.uint32(/* id 35, wireType 2 =*/282).string(message.liarsDiceHandOrder[i]);
            if (message.liarsDiceNames != null && message.liarsDiceNames.length)
                for (let i = 0; i < message.liarsDiceNames.length; ++i)
                    $root.game.StringPair.encode(message.liarsDiceNames[i], writer.uint32(/* id 36, wireType 2 =*/290).fork(), _depth + 1).ldelim();
            if (message.gomokuBlackSeat != null && $Object.hasOwnProperty.call(message, "gomokuBlackSeat") && message.gomokuBlackSeat !== "")
                writer.uint32(/* id 37, wireType 2 =*/298).string(message.gomokuBlackSeat);
            if (message.gomokuLine != null && message.gomokuLine.length)
                for (let i = 0; i < message.gomokuLine.length; ++i)
                    $root.game.Pos.encode(message.gomokuLine[i], writer.uint32(/* id 38, wireType 2 =*/306).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoundHistoryItem message, length delimited. Does not implicitly {@link game.RoundHistoryItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RoundHistoryItem
         * @static
         * @param {game.RoundHistoryItem.$Properties} message RoundHistoryItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundHistoryItem.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RoundHistoryItem message from the specified reader or buffer.
         * @function decode
         * @memberof game.RoundHistoryItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RoundHistoryItem & game.RoundHistoryItem.$Shape} RoundHistoryItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundHistoryItem.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RoundHistoryItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.at = value;
                        else
                            delete message.at;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerA = value;
                        else
                            delete message.playerA;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerB = value;
                        else
                            delete message.playerB;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.moveA = value;
                        else
                            delete message.moveA;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.moveB = value;
                        else
                            delete message.moveB;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.resultLabel = value;
                        else
                            delete message.resultLabel;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.resultText = value;
                        else
                            delete message.resultText;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.gameId = value;
                        else
                            delete message.gameId;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.othelloScore = $root.game.OthelloScore.decode(reader, reader.uint32(), $undefined, _depth + 1, message.othelloScore);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.othelloBlackSeat = value;
                        else
                            delete message.othelloBlackSeat;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.tictactoeXSeat = value;
                        else
                            delete message.tictactoeXSeat;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tictactoeLine && message.tictactoeLine.length))
                            message.tictactoeLine = [];
                        message.tictactoeLine.push($root.game.Pos.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ranked = value;
                        else
                            delete message.ranked;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.stake = value;
                        else
                            delete message.stake;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.rankMultiplier = value;
                        else
                            delete message.rankMultiplier;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.effectiveStake = value;
                        else
                            delete message.effectiveStake;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.extremeRanked = value;
                        else
                            delete message.extremeRanked;
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.punishmentName = value;
                        else
                            delete message.punishmentName;
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.punishmentDescription = value;
                        else
                            delete message.punishmentDescription;
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        if (!(message.punishmentTasks && message.punishmentTasks.length))
                            message.punishmentTasks = [];
                        message.punishmentTasks.push($root.game.PunishmentTask.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        if (!(message.punishedNames && message.punishedNames.length))
                            message.punishedNames = [];
                        message.punishedNames.push(reader.stringVerify());
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        if (!(message.proofs && message.proofs.length))
                            message.proofs = [];
                        message.proofs.push($root.game.HistoryProof.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.liarsDiceWinnerId = value;
                        else
                            delete message.liarsDiceWinnerId;
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.liarsDiceLoserId = value;
                        else
                            delete message.liarsDiceLoserId;
                        continue;
                    }
                case 31: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.liarsDiceBidCount = value;
                        else
                            delete message.liarsDiceBidCount;
                        continue;
                    }
                case 32: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.liarsDiceBidFace = value;
                        else
                            delete message.liarsDiceBidFace;
                        continue;
                    }
                case 33: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.liarsDiceActualCount = value;
                        else
                            delete message.liarsDiceActualCount;
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        if (!(message.liarsDiceHands && message.liarsDiceHands.length))
                            message.liarsDiceHands = [];
                        message.liarsDiceHands.push($root.game.LiarsDiceHandsPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 35: {
                        if (wireType !== 2)
                            break;
                        if (!(message.liarsDiceHandOrder && message.liarsDiceHandOrder.length))
                            message.liarsDiceHandOrder = [];
                        message.liarsDiceHandOrder.push(reader.stringVerify());
                        continue;
                    }
                case 36: {
                        if (wireType !== 2)
                            break;
                        if (!(message.liarsDiceNames && message.liarsDiceNames.length))
                            message.liarsDiceNames = [];
                        message.liarsDiceNames.push($root.game.StringPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 37: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.gomokuBlackSeat = value;
                        else
                            delete message.gomokuBlackSeat;
                        continue;
                    }
                case 38: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gomokuLine && message.gomokuLine.length))
                            message.gomokuLine = [];
                        message.gomokuLine.push($root.game.Pos.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoundHistoryItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RoundHistoryItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RoundHistoryItem & game.RoundHistoryItem.$Shape} RoundHistoryItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundHistoryItem.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundHistoryItem message.
         * @function verify
         * @memberof game.RoundHistoryItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundHistoryItem.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                if (!$util.isInteger(message.at) && !(message.at && $util.isInteger(message.at.low) && $util.isInteger(message.at.high)))
                    return "at: integer|Long expected";
            if (message.playerA != null && $Object.hasOwnProperty.call(message, "playerA"))
                if (!$util.isString(message.playerA))
                    return "playerA: string expected";
            if (message.playerB != null && $Object.hasOwnProperty.call(message, "playerB"))
                if (!$util.isString(message.playerB))
                    return "playerB: string expected";
            if (message.moveA != null && $Object.hasOwnProperty.call(message, "moveA"))
                if (!$util.isString(message.moveA))
                    return "moveA: string expected";
            if (message.moveB != null && $Object.hasOwnProperty.call(message, "moveB"))
                if (!$util.isString(message.moveB))
                    return "moveB: string expected";
            if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
                if (!$util.isString(message.result))
                    return "result: string expected";
            if (message.resultLabel != null && $Object.hasOwnProperty.call(message, "resultLabel"))
                if (!$util.isString(message.resultLabel))
                    return "resultLabel: string expected";
            if (message.resultText != null && $Object.hasOwnProperty.call(message, "resultText"))
                if (!$util.isString(message.resultText))
                    return "resultText: string expected";
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId"))
                if (!$util.isString(message.gameId))
                    return "gameId: string expected";
            if (message.othelloScore != null && $Object.hasOwnProperty.call(message, "othelloScore")) {
                let error = $root.game.OthelloScore.verify(message.othelloScore, _depth + 1);
                if (error)
                    return "othelloScore." + error;
            }
            if (message.othelloBlackSeat != null && $Object.hasOwnProperty.call(message, "othelloBlackSeat"))
                if (!$util.isString(message.othelloBlackSeat))
                    return "othelloBlackSeat: string expected";
            if (message.tictactoeXSeat != null && $Object.hasOwnProperty.call(message, "tictactoeXSeat"))
                if (!$util.isString(message.tictactoeXSeat))
                    return "tictactoeXSeat: string expected";
            if (message.tictactoeLine != null && $Object.hasOwnProperty.call(message, "tictactoeLine")) {
                if (!$Array.isArray(message.tictactoeLine))
                    return "tictactoeLine: array expected";
                for (let i = 0; i < message.tictactoeLine.length; ++i) {
                    let error = $root.game.Pos.verify(message.tictactoeLine[i], _depth + 1);
                    if (error)
                        return "tictactoeLine." + error;
                }
            }
            if (message.ranked != null && $Object.hasOwnProperty.call(message, "ranked"))
                if (typeof message.ranked !== "boolean")
                    return "ranked: boolean expected";
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (!$util.isInteger(message.stake))
                    return "stake: integer expected";
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier"))
                if (!$util.isInteger(message.rankMultiplier))
                    return "rankMultiplier: integer expected";
            if (message.effectiveStake != null && $Object.hasOwnProperty.call(message, "effectiveStake"))
                if (!$util.isInteger(message.effectiveStake))
                    return "effectiveStake: integer expected";
            if (message.extremeRanked != null && $Object.hasOwnProperty.call(message, "extremeRanked"))
                if (typeof message.extremeRanked !== "boolean")
                    return "extremeRanked: boolean expected";
            if (message.punishmentName != null && $Object.hasOwnProperty.call(message, "punishmentName"))
                if (!$util.isString(message.punishmentName))
                    return "punishmentName: string expected";
            if (message.punishmentDescription != null && $Object.hasOwnProperty.call(message, "punishmentDescription"))
                if (!$util.isString(message.punishmentDescription))
                    return "punishmentDescription: string expected";
            if (message.punishmentTasks != null && $Object.hasOwnProperty.call(message, "punishmentTasks")) {
                if (!$Array.isArray(message.punishmentTasks))
                    return "punishmentTasks: array expected";
                for (let i = 0; i < message.punishmentTasks.length; ++i) {
                    let error = $root.game.PunishmentTask.verify(message.punishmentTasks[i], _depth + 1);
                    if (error)
                        return "punishmentTasks." + error;
                }
            }
            if (message.punishedNames != null && $Object.hasOwnProperty.call(message, "punishedNames")) {
                if (!$Array.isArray(message.punishedNames))
                    return "punishedNames: array expected";
                for (let i = 0; i < message.punishedNames.length; ++i)
                    if (!$util.isString(message.punishedNames[i]))
                        return "punishedNames: string[] expected";
            }
            if (message.proofs != null && $Object.hasOwnProperty.call(message, "proofs")) {
                if (!$Array.isArray(message.proofs))
                    return "proofs: array expected";
                for (let i = 0; i < message.proofs.length; ++i) {
                    let error = $root.game.HistoryProof.verify(message.proofs[i], _depth + 1);
                    if (error)
                        return "proofs." + error;
                }
            }
            if (message.liarsDiceWinnerId != null && $Object.hasOwnProperty.call(message, "liarsDiceWinnerId"))
                if (!$util.isString(message.liarsDiceWinnerId))
                    return "liarsDiceWinnerId: string expected";
            if (message.liarsDiceLoserId != null && $Object.hasOwnProperty.call(message, "liarsDiceLoserId"))
                if (!$util.isString(message.liarsDiceLoserId))
                    return "liarsDiceLoserId: string expected";
            if (message.liarsDiceBidCount != null && $Object.hasOwnProperty.call(message, "liarsDiceBidCount"))
                if (!$util.isInteger(message.liarsDiceBidCount))
                    return "liarsDiceBidCount: integer expected";
            if (message.liarsDiceBidFace != null && $Object.hasOwnProperty.call(message, "liarsDiceBidFace"))
                if (!$util.isInteger(message.liarsDiceBidFace))
                    return "liarsDiceBidFace: integer expected";
            if (message.liarsDiceActualCount != null && $Object.hasOwnProperty.call(message, "liarsDiceActualCount"))
                if (!$util.isInteger(message.liarsDiceActualCount))
                    return "liarsDiceActualCount: integer expected";
            if (message.liarsDiceHands != null && $Object.hasOwnProperty.call(message, "liarsDiceHands")) {
                if (!$Array.isArray(message.liarsDiceHands))
                    return "liarsDiceHands: array expected";
                for (let i = 0; i < message.liarsDiceHands.length; ++i) {
                    let error = $root.game.LiarsDiceHandsPair.verify(message.liarsDiceHands[i], _depth + 1);
                    if (error)
                        return "liarsDiceHands." + error;
                }
            }
            if (message.liarsDiceHandOrder != null && $Object.hasOwnProperty.call(message, "liarsDiceHandOrder")) {
                if (!$Array.isArray(message.liarsDiceHandOrder))
                    return "liarsDiceHandOrder: array expected";
                for (let i = 0; i < message.liarsDiceHandOrder.length; ++i)
                    if (!$util.isString(message.liarsDiceHandOrder[i]))
                        return "liarsDiceHandOrder: string[] expected";
            }
            if (message.liarsDiceNames != null && $Object.hasOwnProperty.call(message, "liarsDiceNames")) {
                if (!$Array.isArray(message.liarsDiceNames))
                    return "liarsDiceNames: array expected";
                for (let i = 0; i < message.liarsDiceNames.length; ++i) {
                    let error = $root.game.StringPair.verify(message.liarsDiceNames[i], _depth + 1);
                    if (error)
                        return "liarsDiceNames." + error;
                }
            }
            if (message.gomokuBlackSeat != null && $Object.hasOwnProperty.call(message, "gomokuBlackSeat"))
                if (!$util.isString(message.gomokuBlackSeat))
                    return "gomokuBlackSeat: string expected";
            if (message.gomokuLine != null && $Object.hasOwnProperty.call(message, "gomokuLine")) {
                if (!$Array.isArray(message.gomokuLine))
                    return "gomokuLine: array expected";
                for (let i = 0; i < message.gomokuLine.length; ++i) {
                    let error = $root.game.Pos.verify(message.gomokuLine[i], _depth + 1);
                    if (error)
                        return "gomokuLine." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundHistoryItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RoundHistoryItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RoundHistoryItem} RoundHistoryItem
         */
        RoundHistoryItem.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RoundHistoryItem)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RoundHistoryItem: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RoundHistoryItem();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.round != null)
                if ($Number(object.round) !== 0)
                    message.round = object.round | 0;
            if (object.at != null)
                if (typeof object.at === "object" ? object.at.low || object.at.high : $Number(object.at) !== 0)
                    if ($util.Long)
                        message.at = $util.Long.fromValue(object.at, false);
                    else if (typeof object.at === "string")
                        message.at = $parseInt(object.at, 10);
                    else if (typeof object.at === "number")
                        message.at = object.at;
                    else if (typeof object.at === "object")
                        message.at = new $util.LongBits(object.at.low >>> 0, object.at.high >>> 0).toNumber();
            if (object.playerA != null)
                if (typeof object.playerA !== "string" || object.playerA.length)
                    message.playerA = $String(object.playerA);
            if (object.playerB != null)
                if (typeof object.playerB !== "string" || object.playerB.length)
                    message.playerB = $String(object.playerB);
            if (object.moveA != null)
                if (typeof object.moveA !== "string" || object.moveA.length)
                    message.moveA = $String(object.moveA);
            if (object.moveB != null)
                if (typeof object.moveB !== "string" || object.moveB.length)
                    message.moveB = $String(object.moveB);
            if (object.result != null)
                if (typeof object.result !== "string" || object.result.length)
                    message.result = $String(object.result);
            if (object.resultLabel != null)
                if (typeof object.resultLabel !== "string" || object.resultLabel.length)
                    message.resultLabel = $String(object.resultLabel);
            if (object.resultText != null)
                if (typeof object.resultText !== "string" || object.resultText.length)
                    message.resultText = $String(object.resultText);
            if (object.gameId != null)
                if (typeof object.gameId !== "string" || object.gameId.length)
                    message.gameId = $String(object.gameId);
            if (object.othelloScore != null) {
                if (!$util.isObject(object.othelloScore))
                    throw $TypeError(".game.RoundHistoryItem.othelloScore: object expected");
                message.othelloScore = $root.game.OthelloScore.fromObject(object.othelloScore, _depth + 1);
            }
            if (object.othelloBlackSeat != null)
                if (typeof object.othelloBlackSeat !== "string" || object.othelloBlackSeat.length)
                    message.othelloBlackSeat = $String(object.othelloBlackSeat);
            if (object.tictactoeXSeat != null)
                if (typeof object.tictactoeXSeat !== "string" || object.tictactoeXSeat.length)
                    message.tictactoeXSeat = $String(object.tictactoeXSeat);
            if (object.tictactoeLine) {
                if (!$Array.isArray(object.tictactoeLine))
                    throw $TypeError(".game.RoundHistoryItem.tictactoeLine: array expected");
                message.tictactoeLine = $Array(object.tictactoeLine.length);
                for (let i = 0; i < object.tictactoeLine.length; ++i) {
                    if (!$util.isObject(object.tictactoeLine[i]))
                        throw $TypeError(".game.RoundHistoryItem.tictactoeLine: object expected");
                    message.tictactoeLine[i] = $root.game.Pos.fromObject(object.tictactoeLine[i], _depth + 1);
                }
            }
            if (object.ranked != null)
                if (object.ranked)
                    message.ranked = $Boolean(object.ranked);
            if (object.stake != null)
                if ($Number(object.stake) !== 0)
                    message.stake = object.stake | 0;
            if (object.rankMultiplier != null)
                if ($Number(object.rankMultiplier) !== 0)
                    message.rankMultiplier = object.rankMultiplier | 0;
            if (object.effectiveStake != null)
                if ($Number(object.effectiveStake) !== 0)
                    message.effectiveStake = object.effectiveStake | 0;
            if (object.extremeRanked != null)
                if (object.extremeRanked)
                    message.extremeRanked = $Boolean(object.extremeRanked);
            if (object.punishmentName != null)
                if (typeof object.punishmentName !== "string" || object.punishmentName.length)
                    message.punishmentName = $String(object.punishmentName);
            if (object.punishmentDescription != null)
                if (typeof object.punishmentDescription !== "string" || object.punishmentDescription.length)
                    message.punishmentDescription = $String(object.punishmentDescription);
            if (object.punishmentTasks) {
                if (!$Array.isArray(object.punishmentTasks))
                    throw $TypeError(".game.RoundHistoryItem.punishmentTasks: array expected");
                message.punishmentTasks = $Array(object.punishmentTasks.length);
                for (let i = 0; i < object.punishmentTasks.length; ++i) {
                    if (!$util.isObject(object.punishmentTasks[i]))
                        throw $TypeError(".game.RoundHistoryItem.punishmentTasks: object expected");
                    message.punishmentTasks[i] = $root.game.PunishmentTask.fromObject(object.punishmentTasks[i], _depth + 1);
                }
            }
            if (object.punishedNames) {
                if (!$Array.isArray(object.punishedNames))
                    throw $TypeError(".game.RoundHistoryItem.punishedNames: array expected");
                message.punishedNames = $Array(object.punishedNames.length);
                for (let i = 0; i < object.punishedNames.length; ++i)
                    message.punishedNames[i] = $String(object.punishedNames[i]);
            }
            if (object.proofs) {
                if (!$Array.isArray(object.proofs))
                    throw $TypeError(".game.RoundHistoryItem.proofs: array expected");
                message.proofs = $Array(object.proofs.length);
                for (let i = 0; i < object.proofs.length; ++i) {
                    if (!$util.isObject(object.proofs[i]))
                        throw $TypeError(".game.RoundHistoryItem.proofs: object expected");
                    message.proofs[i] = $root.game.HistoryProof.fromObject(object.proofs[i], _depth + 1);
                }
            }
            if (object.liarsDiceWinnerId != null)
                if (typeof object.liarsDiceWinnerId !== "string" || object.liarsDiceWinnerId.length)
                    message.liarsDiceWinnerId = $String(object.liarsDiceWinnerId);
            if (object.liarsDiceLoserId != null)
                if (typeof object.liarsDiceLoserId !== "string" || object.liarsDiceLoserId.length)
                    message.liarsDiceLoserId = $String(object.liarsDiceLoserId);
            if (object.liarsDiceBidCount != null)
                if ($Number(object.liarsDiceBidCount) !== 0)
                    message.liarsDiceBidCount = object.liarsDiceBidCount | 0;
            if (object.liarsDiceBidFace != null)
                if ($Number(object.liarsDiceBidFace) !== 0)
                    message.liarsDiceBidFace = object.liarsDiceBidFace | 0;
            if (object.liarsDiceActualCount != null)
                if ($Number(object.liarsDiceActualCount) !== 0)
                    message.liarsDiceActualCount = object.liarsDiceActualCount | 0;
            if (object.liarsDiceHands) {
                if (!$Array.isArray(object.liarsDiceHands))
                    throw $TypeError(".game.RoundHistoryItem.liarsDiceHands: array expected");
                message.liarsDiceHands = $Array(object.liarsDiceHands.length);
                for (let i = 0; i < object.liarsDiceHands.length; ++i) {
                    if (!$util.isObject(object.liarsDiceHands[i]))
                        throw $TypeError(".game.RoundHistoryItem.liarsDiceHands: object expected");
                    message.liarsDiceHands[i] = $root.game.LiarsDiceHandsPair.fromObject(object.liarsDiceHands[i], _depth + 1);
                }
            }
            if (object.liarsDiceHandOrder) {
                if (!$Array.isArray(object.liarsDiceHandOrder))
                    throw $TypeError(".game.RoundHistoryItem.liarsDiceHandOrder: array expected");
                message.liarsDiceHandOrder = $Array(object.liarsDiceHandOrder.length);
                for (let i = 0; i < object.liarsDiceHandOrder.length; ++i)
                    message.liarsDiceHandOrder[i] = $String(object.liarsDiceHandOrder[i]);
            }
            if (object.liarsDiceNames) {
                if (!$Array.isArray(object.liarsDiceNames))
                    throw $TypeError(".game.RoundHistoryItem.liarsDiceNames: array expected");
                message.liarsDiceNames = $Array(object.liarsDiceNames.length);
                for (let i = 0; i < object.liarsDiceNames.length; ++i) {
                    if (!$util.isObject(object.liarsDiceNames[i]))
                        throw $TypeError(".game.RoundHistoryItem.liarsDiceNames: object expected");
                    message.liarsDiceNames[i] = $root.game.StringPair.fromObject(object.liarsDiceNames[i], _depth + 1);
                }
            }
            if (object.gomokuBlackSeat != null)
                if (typeof object.gomokuBlackSeat !== "string" || object.gomokuBlackSeat.length)
                    message.gomokuBlackSeat = $String(object.gomokuBlackSeat);
            if (object.gomokuLine) {
                if (!$Array.isArray(object.gomokuLine))
                    throw $TypeError(".game.RoundHistoryItem.gomokuLine: array expected");
                message.gomokuLine = $Array(object.gomokuLine.length);
                for (let i = 0; i < object.gomokuLine.length; ++i) {
                    if (!$util.isObject(object.gomokuLine[i]))
                        throw $TypeError(".game.RoundHistoryItem.gomokuLine: object expected");
                    message.gomokuLine[i] = $root.game.Pos.fromObject(object.gomokuLine[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundHistoryItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RoundHistoryItem
         * @static
         * @param {game.RoundHistoryItem} message RoundHistoryItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundHistoryItem.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.tictactoeLine = [];
                object.punishmentTasks = [];
                object.punishedNames = [];
                object.proofs = [];
                object.liarsDiceHands = [];
                object.liarsDiceHandOrder = [];
                object.liarsDiceNames = [];
                object.gomokuLine = [];
            }
            if (options.defaults) {
                object.id = "";
                object.round = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.at = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.at = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.playerA = "";
                object.playerB = "";
                object.moveA = "";
                object.moveB = "";
                object.result = "";
                object.resultLabel = "";
                object.resultText = "";
                object.gameId = "";
                object.othelloScore = null;
                object.othelloBlackSeat = "";
                object.tictactoeXSeat = "";
                object.ranked = false;
                object.stake = 0;
                object.rankMultiplier = 0;
                object.effectiveStake = 0;
                object.extremeRanked = false;
                object.punishmentName = "";
                object.punishmentDescription = "";
                object.liarsDiceWinnerId = "";
                object.liarsDiceLoserId = "";
                object.liarsDiceBidCount = 0;
                object.liarsDiceBidFace = 0;
                object.liarsDiceActualCount = 0;
                object.gomokuBlackSeat = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                object.round = message.round;
            if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.at = typeof message.at === "number" ? $BigInt(message.at) : $util.Long.fromBits(message.at.low >>> 0, message.at.high >>> 0, false).toBigInt();
                else if (typeof message.at === "number")
                    object.at = options.longs === $String ? $String(message.at) : message.at;
                else
                    object.at = options.longs === $String ? $util.Long.prototype.toString.call(message.at) : options.longs === $Number ? new $util.LongBits(message.at.low >>> 0, message.at.high >>> 0).toNumber() : message.at;
            if (message.playerA != null && $Object.hasOwnProperty.call(message, "playerA"))
                object.playerA = message.playerA;
            if (message.playerB != null && $Object.hasOwnProperty.call(message, "playerB"))
                object.playerB = message.playerB;
            if (message.moveA != null && $Object.hasOwnProperty.call(message, "moveA"))
                object.moveA = message.moveA;
            if (message.moveB != null && $Object.hasOwnProperty.call(message, "moveB"))
                object.moveB = message.moveB;
            if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
                object.result = message.result;
            if (message.resultLabel != null && $Object.hasOwnProperty.call(message, "resultLabel"))
                object.resultLabel = message.resultLabel;
            if (message.resultText != null && $Object.hasOwnProperty.call(message, "resultText"))
                object.resultText = message.resultText;
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId"))
                object.gameId = message.gameId;
            if (message.othelloScore != null && $Object.hasOwnProperty.call(message, "othelloScore"))
                object.othelloScore = $root.game.OthelloScore.toObject(message.othelloScore, options, _depth + 1);
            if (message.othelloBlackSeat != null && $Object.hasOwnProperty.call(message, "othelloBlackSeat"))
                object.othelloBlackSeat = message.othelloBlackSeat;
            if (message.tictactoeXSeat != null && $Object.hasOwnProperty.call(message, "tictactoeXSeat"))
                object.tictactoeXSeat = message.tictactoeXSeat;
            if (message.tictactoeLine && message.tictactoeLine.length) {
                object.tictactoeLine = $Array(message.tictactoeLine.length);
                for (let j = 0; j < message.tictactoeLine.length; ++j)
                    object.tictactoeLine[j] = $root.game.Pos.toObject(message.tictactoeLine[j], options, _depth + 1);
            }
            if (message.ranked != null && $Object.hasOwnProperty.call(message, "ranked"))
                object.ranked = message.ranked;
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                object.stake = message.stake;
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier"))
                object.rankMultiplier = message.rankMultiplier;
            if (message.effectiveStake != null && $Object.hasOwnProperty.call(message, "effectiveStake"))
                object.effectiveStake = message.effectiveStake;
            if (message.extremeRanked != null && $Object.hasOwnProperty.call(message, "extremeRanked"))
                object.extremeRanked = message.extremeRanked;
            if (message.punishmentName != null && $Object.hasOwnProperty.call(message, "punishmentName"))
                object.punishmentName = message.punishmentName;
            if (message.punishmentDescription != null && $Object.hasOwnProperty.call(message, "punishmentDescription"))
                object.punishmentDescription = message.punishmentDescription;
            if (message.punishmentTasks && message.punishmentTasks.length) {
                object.punishmentTasks = $Array(message.punishmentTasks.length);
                for (let j = 0; j < message.punishmentTasks.length; ++j)
                    object.punishmentTasks[j] = $root.game.PunishmentTask.toObject(message.punishmentTasks[j], options, _depth + 1);
            }
            if (message.punishedNames && message.punishedNames.length) {
                object.punishedNames = $Array(message.punishedNames.length);
                for (let j = 0; j < message.punishedNames.length; ++j)
                    object.punishedNames[j] = message.punishedNames[j];
            }
            if (message.proofs && message.proofs.length) {
                object.proofs = $Array(message.proofs.length);
                for (let j = 0; j < message.proofs.length; ++j)
                    object.proofs[j] = $root.game.HistoryProof.toObject(message.proofs[j], options, _depth + 1);
            }
            if (message.liarsDiceWinnerId != null && $Object.hasOwnProperty.call(message, "liarsDiceWinnerId"))
                object.liarsDiceWinnerId = message.liarsDiceWinnerId;
            if (message.liarsDiceLoserId != null && $Object.hasOwnProperty.call(message, "liarsDiceLoserId"))
                object.liarsDiceLoserId = message.liarsDiceLoserId;
            if (message.liarsDiceBidCount != null && $Object.hasOwnProperty.call(message, "liarsDiceBidCount"))
                object.liarsDiceBidCount = message.liarsDiceBidCount;
            if (message.liarsDiceBidFace != null && $Object.hasOwnProperty.call(message, "liarsDiceBidFace"))
                object.liarsDiceBidFace = message.liarsDiceBidFace;
            if (message.liarsDiceActualCount != null && $Object.hasOwnProperty.call(message, "liarsDiceActualCount"))
                object.liarsDiceActualCount = message.liarsDiceActualCount;
            if (message.liarsDiceHands && message.liarsDiceHands.length) {
                object.liarsDiceHands = $Array(message.liarsDiceHands.length);
                for (let j = 0; j < message.liarsDiceHands.length; ++j)
                    object.liarsDiceHands[j] = $root.game.LiarsDiceHandsPair.toObject(message.liarsDiceHands[j], options, _depth + 1);
            }
            if (message.liarsDiceHandOrder && message.liarsDiceHandOrder.length) {
                object.liarsDiceHandOrder = $Array(message.liarsDiceHandOrder.length);
                for (let j = 0; j < message.liarsDiceHandOrder.length; ++j)
                    object.liarsDiceHandOrder[j] = message.liarsDiceHandOrder[j];
            }
            if (message.liarsDiceNames && message.liarsDiceNames.length) {
                object.liarsDiceNames = $Array(message.liarsDiceNames.length);
                for (let j = 0; j < message.liarsDiceNames.length; ++j)
                    object.liarsDiceNames[j] = $root.game.StringPair.toObject(message.liarsDiceNames[j], options, _depth + 1);
            }
            if (message.gomokuBlackSeat != null && $Object.hasOwnProperty.call(message, "gomokuBlackSeat"))
                object.gomokuBlackSeat = message.gomokuBlackSeat;
            if (message.gomokuLine && message.gomokuLine.length) {
                object.gomokuLine = $Array(message.gomokuLine.length);
                for (let j = 0; j < message.gomokuLine.length; ++j)
                    object.gomokuLine[j] = $root.game.Pos.toObject(message.gomokuLine[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this RoundHistoryItem to JSON.
         * @function toJSON
         * @memberof game.RoundHistoryItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundHistoryItem.prototype.toJSON = function() {
            return RoundHistoryItem.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoundHistoryItem
         * @function getTypeUrl
         * @memberof game.RoundHistoryItem
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoundHistoryItem.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RoundHistoryItem";
        };

        return RoundHistoryItem;
    })();

    game.Int32List = (function() {

        /**
         * Properties of an Int32List.
         * @typedef {Object} game.Int32List.$Properties
         * @property {Array.<number>|null} [values] Int32List values
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an Int32List.
         * @memberof game
         * @interface IInt32List
         * @augments game.Int32List.$Properties
         * @deprecated Use game.Int32List.$Properties instead.
         */

        /**
         * Shape of an Int32List.
         * @typedef {game.Int32List.$Properties} game.Int32List.$Shape
         */

        /**
         * Constructs a new Int32List.
         * @memberof game
         * @classdesc Represents an Int32List.
         * @constructor
         * @param {game.Int32List.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Int32List = function (properties) {
            this.values = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Int32List values.
         * @member {Array.<number>} values
         * @memberof game.Int32List
         * @instance
         */
        Int32List.prototype.values = $util.emptyArray;

        /**
         * Creates a new Int32List instance using the specified properties.
         * @function create
         * @memberof game.Int32List
         * @static
         * @param {game.Int32List.$Properties=} [properties] Properties to set
         * @returns {game.Int32List} Int32List instance
         * @type {{
         *   (properties: game.Int32List.$Shape): game.Int32List & game.Int32List.$Shape;
         *   (properties?: game.Int32List.$Properties): game.Int32List;
         * }}
         */
        Int32List.create = function(properties) {
            return new Int32List(properties);
        };

        /**
         * Encodes the specified Int32List message. Does not implicitly {@link game.Int32List.verify|verify} messages.
         * @function encode
         * @memberof game.Int32List
         * @static
         * @param {game.Int32List.$Properties} message Int32List message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Int32List.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.values != null && message.values.length)
                writer.uint32(/* id 1, wireType 2 =*/10).int32s(message.values);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Int32List message, length delimited. Does not implicitly {@link game.Int32List.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.Int32List
         * @static
         * @param {game.Int32List.$Properties} message Int32List message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Int32List.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an Int32List message from the specified reader or buffer.
         * @function decode
         * @memberof game.Int32List
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.Int32List & game.Int32List.$Shape} Int32List
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Int32List.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.Int32List();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.values && message.values.length))
                                message.values = [];
                            reader.int32s(message.values);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.values && message.values.length))
                            message.values = [];
                        message.values.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Int32List message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.Int32List
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.Int32List & game.Int32List.$Shape} Int32List
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Int32List.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Int32List message.
         * @function verify
         * @memberof game.Int32List
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Int32List.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                if (!$Array.isArray(message.values))
                    return "values: array expected";
                for (let i = 0; i < message.values.length; ++i)
                    if (!$util.isInteger(message.values[i]))
                        return "values: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an Int32List message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.Int32List
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.Int32List} Int32List
         */
        Int32List.fromObject = function (object, _depth) {
            if (object instanceof $root.game.Int32List)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.Int32List: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.Int32List();
            if (object.values) {
                if (!$Array.isArray(object.values))
                    throw $TypeError(".game.Int32List.values: array expected");
                message.values = $Array(object.values.length);
                for (let i = 0; i < object.values.length; ++i)
                    message.values[i] = object.values[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an Int32List message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.Int32List
         * @static
         * @param {game.Int32List} message Int32List
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Int32List.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.values = [];
            if (message.values && message.values.length) {
                object.values = $Array(message.values.length);
                for (let j = 0; j < message.values.length; ++j)
                    object.values[j] = message.values[j];
            }
            return object;
        };

        /**
         * Converts this Int32List to JSON.
         * @function toJSON
         * @memberof game.Int32List
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Int32List.prototype.toJSON = function() {
            return Int32List.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Int32List
         * @function getTypeUrl
         * @memberof game.Int32List
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Int32List.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.Int32List";
        };

        return Int32List;
    })();

    game.LiarsDiceHandsPair = (function() {

        /**
         * Properties of a LiarsDiceHandsPair.
         * @typedef {Object} game.LiarsDiceHandsPair.$Properties
         * @property {string|null} [key] LiarsDiceHandsPair key
         * @property {game.Int32List.$Properties|null} [value] LiarsDiceHandsPair value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LiarsDiceHandsPair.
         * @memberof game
         * @interface ILiarsDiceHandsPair
         * @augments game.LiarsDiceHandsPair.$Properties
         * @deprecated Use game.LiarsDiceHandsPair.$Properties instead.
         */

        /**
         * Shape of a LiarsDiceHandsPair.
         * @typedef {game.LiarsDiceHandsPair.$Properties} game.LiarsDiceHandsPair.$Shape
         */

        /**
         * Constructs a new LiarsDiceHandsPair.
         * @memberof game
         * @classdesc Represents a LiarsDiceHandsPair.
         * @constructor
         * @param {game.LiarsDiceHandsPair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LiarsDiceHandsPair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LiarsDiceHandsPair key.
         * @member {string} key
         * @memberof game.LiarsDiceHandsPair
         * @instance
         */
        LiarsDiceHandsPair.prototype.key = "";

        /**
         * LiarsDiceHandsPair value.
         * @member {game.Int32List.$Properties|null|undefined} value
         * @memberof game.LiarsDiceHandsPair
         * @instance
         */
        LiarsDiceHandsPair.prototype.value = null;

        /**
         * Creates a new LiarsDiceHandsPair instance using the specified properties.
         * @function create
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {game.LiarsDiceHandsPair.$Properties=} [properties] Properties to set
         * @returns {game.LiarsDiceHandsPair} LiarsDiceHandsPair instance
         * @type {{
         *   (properties: game.LiarsDiceHandsPair.$Shape): game.LiarsDiceHandsPair & game.LiarsDiceHandsPair.$Shape;
         *   (properties?: game.LiarsDiceHandsPair.$Properties): game.LiarsDiceHandsPair;
         * }}
         */
        LiarsDiceHandsPair.create = function(properties) {
            return new LiarsDiceHandsPair(properties);
        };

        /**
         * Encodes the specified LiarsDiceHandsPair message. Does not implicitly {@link game.LiarsDiceHandsPair.verify|verify} messages.
         * @function encode
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {game.LiarsDiceHandsPair.$Properties} message LiarsDiceHandsPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LiarsDiceHandsPair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                $root.game.Int32List.encode(message.value, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LiarsDiceHandsPair message, length delimited. Does not implicitly {@link game.LiarsDiceHandsPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {game.LiarsDiceHandsPair.$Properties} message LiarsDiceHandsPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LiarsDiceHandsPair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LiarsDiceHandsPair message from the specified reader or buffer.
         * @function decode
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LiarsDiceHandsPair & game.LiarsDiceHandsPair.$Shape} LiarsDiceHandsPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LiarsDiceHandsPair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LiarsDiceHandsPair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value = $root.game.Int32List.decode(reader, reader.uint32(), $undefined, _depth + 1, message.value);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LiarsDiceHandsPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LiarsDiceHandsPair & game.LiarsDiceHandsPair.$Shape} LiarsDiceHandsPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LiarsDiceHandsPair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LiarsDiceHandsPair message.
         * @function verify
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LiarsDiceHandsPair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                let error = $root.game.Int32List.verify(message.value, _depth + 1);
                if (error)
                    return "value." + error;
            }
            return null;
        };

        /**
         * Creates a LiarsDiceHandsPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LiarsDiceHandsPair} LiarsDiceHandsPair
         */
        LiarsDiceHandsPair.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LiarsDiceHandsPair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LiarsDiceHandsPair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LiarsDiceHandsPair();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.value != null) {
                if (!$util.isObject(object.value))
                    throw $TypeError(".game.LiarsDiceHandsPair.value: object expected");
                message.value = $root.game.Int32List.fromObject(object.value, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LiarsDiceHandsPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {game.LiarsDiceHandsPair} message LiarsDiceHandsPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LiarsDiceHandsPair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = null;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = $root.game.Int32List.toObject(message.value, options, _depth + 1);
            return object;
        };

        /**
         * Converts this LiarsDiceHandsPair to JSON.
         * @function toJSON
         * @memberof game.LiarsDiceHandsPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LiarsDiceHandsPair.prototype.toJSON = function() {
            return LiarsDiceHandsPair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LiarsDiceHandsPair
         * @function getTypeUrl
         * @memberof game.LiarsDiceHandsPair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LiarsDiceHandsPair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LiarsDiceHandsPair";
        };

        return LiarsDiceHandsPair;
    })();

    game.LiarsDiceBid = (function() {

        /**
         * Properties of a LiarsDiceBid.
         * @typedef {Object} game.LiarsDiceBid.$Properties
         * @property {string|null} [playerId] LiarsDiceBid playerId
         * @property {number|null} [count] LiarsDiceBid count
         * @property {number|null} [face] LiarsDiceBid face
         * @property {number|Long|null} [at] LiarsDiceBid at
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LiarsDiceBid.
         * @memberof game
         * @interface ILiarsDiceBid
         * @augments game.LiarsDiceBid.$Properties
         * @deprecated Use game.LiarsDiceBid.$Properties instead.
         */

        /**
         * Shape of a LiarsDiceBid.
         * @typedef {game.LiarsDiceBid.$Properties} game.LiarsDiceBid.$Shape
         */

        /**
         * Constructs a new LiarsDiceBid.
         * @memberof game
         * @classdesc Represents a LiarsDiceBid.
         * @constructor
         * @param {game.LiarsDiceBid.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LiarsDiceBid = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LiarsDiceBid playerId.
         * @member {string} playerId
         * @memberof game.LiarsDiceBid
         * @instance
         */
        LiarsDiceBid.prototype.playerId = "";

        /**
         * LiarsDiceBid count.
         * @member {number} count
         * @memberof game.LiarsDiceBid
         * @instance
         */
        LiarsDiceBid.prototype.count = 0;

        /**
         * LiarsDiceBid face.
         * @member {number} face
         * @memberof game.LiarsDiceBid
         * @instance
         */
        LiarsDiceBid.prototype.face = 0;

        /**
         * LiarsDiceBid at.
         * @member {number|Long} at
         * @memberof game.LiarsDiceBid
         * @instance
         */
        LiarsDiceBid.prototype.at = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new LiarsDiceBid instance using the specified properties.
         * @function create
         * @memberof game.LiarsDiceBid
         * @static
         * @param {game.LiarsDiceBid.$Properties=} [properties] Properties to set
         * @returns {game.LiarsDiceBid} LiarsDiceBid instance
         * @type {{
         *   (properties: game.LiarsDiceBid.$Shape): game.LiarsDiceBid & game.LiarsDiceBid.$Shape;
         *   (properties?: game.LiarsDiceBid.$Properties): game.LiarsDiceBid;
         * }}
         */
        LiarsDiceBid.create = function(properties) {
            return new LiarsDiceBid(properties);
        };

        /**
         * Encodes the specified LiarsDiceBid message. Does not implicitly {@link game.LiarsDiceBid.verify|verify} messages.
         * @function encode
         * @memberof game.LiarsDiceBid
         * @static
         * @param {game.LiarsDiceBid.$Properties} message LiarsDiceBid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LiarsDiceBid.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId") && message.playerId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.playerId);
            if (message.count != null && $Object.hasOwnProperty.call(message, "count") && message.count !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.count);
            if (message.face != null && $Object.hasOwnProperty.call(message, "face") && message.face !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.face);
            if (message.at != null && $Object.hasOwnProperty.call(message, "at") && (typeof message.at === "object" ? message.at.low || message.at.high : message.at !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.at);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LiarsDiceBid message, length delimited. Does not implicitly {@link game.LiarsDiceBid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LiarsDiceBid
         * @static
         * @param {game.LiarsDiceBid.$Properties} message LiarsDiceBid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LiarsDiceBid.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LiarsDiceBid message from the specified reader or buffer.
         * @function decode
         * @memberof game.LiarsDiceBid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LiarsDiceBid & game.LiarsDiceBid.$Shape} LiarsDiceBid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LiarsDiceBid.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LiarsDiceBid(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.playerId = value;
                        else
                            delete message.playerId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.face = value;
                        else
                            delete message.face;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.at = value;
                        else
                            delete message.at;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LiarsDiceBid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LiarsDiceBid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LiarsDiceBid & game.LiarsDiceBid.$Shape} LiarsDiceBid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LiarsDiceBid.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LiarsDiceBid message.
         * @function verify
         * @memberof game.LiarsDiceBid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LiarsDiceBid.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                if (!$util.isString(message.playerId))
                    return "playerId: string expected";
            if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.face != null && $Object.hasOwnProperty.call(message, "face"))
                if (!$util.isInteger(message.face))
                    return "face: integer expected";
            if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                if (!$util.isInteger(message.at) && !(message.at && $util.isInteger(message.at.low) && $util.isInteger(message.at.high)))
                    return "at: integer|Long expected";
            return null;
        };

        /**
         * Creates a LiarsDiceBid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LiarsDiceBid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LiarsDiceBid} LiarsDiceBid
         */
        LiarsDiceBid.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LiarsDiceBid)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LiarsDiceBid: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LiarsDiceBid();
            if (object.playerId != null)
                if (typeof object.playerId !== "string" || object.playerId.length)
                    message.playerId = $String(object.playerId);
            if (object.count != null)
                if ($Number(object.count) !== 0)
                    message.count = object.count | 0;
            if (object.face != null)
                if ($Number(object.face) !== 0)
                    message.face = object.face | 0;
            if (object.at != null)
                if (typeof object.at === "object" ? object.at.low || object.at.high : $Number(object.at) !== 0)
                    if ($util.Long)
                        message.at = $util.Long.fromValue(object.at, false);
                    else if (typeof object.at === "string")
                        message.at = $parseInt(object.at, 10);
                    else if (typeof object.at === "number")
                        message.at = object.at;
                    else if (typeof object.at === "object")
                        message.at = new $util.LongBits(object.at.low >>> 0, object.at.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a LiarsDiceBid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LiarsDiceBid
         * @static
         * @param {game.LiarsDiceBid} message LiarsDiceBid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LiarsDiceBid.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.playerId = "";
                object.count = 0;
                object.face = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.at = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.at = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.playerId != null && $Object.hasOwnProperty.call(message, "playerId"))
                object.playerId = message.playerId;
            if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                object.count = message.count;
            if (message.face != null && $Object.hasOwnProperty.call(message, "face"))
                object.face = message.face;
            if (message.at != null && $Object.hasOwnProperty.call(message, "at"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.at = typeof message.at === "number" ? $BigInt(message.at) : $util.Long.fromBits(message.at.low >>> 0, message.at.high >>> 0, false).toBigInt();
                else if (typeof message.at === "number")
                    object.at = options.longs === $String ? $String(message.at) : message.at;
                else
                    object.at = options.longs === $String ? $util.Long.prototype.toString.call(message.at) : options.longs === $Number ? new $util.LongBits(message.at.low >>> 0, message.at.high >>> 0).toNumber() : message.at;
            return object;
        };

        /**
         * Converts this LiarsDiceBid to JSON.
         * @function toJSON
         * @memberof game.LiarsDiceBid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LiarsDiceBid.prototype.toJSON = function() {
            return LiarsDiceBid.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LiarsDiceBid
         * @function getTypeUrl
         * @memberof game.LiarsDiceBid
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LiarsDiceBid.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LiarsDiceBid";
        };

        return LiarsDiceBid;
    })();

    game.LiarsDiceState = (function() {

        /**
         * Properties of a LiarsDiceState.
         * @typedef {Object} game.LiarsDiceState.$Properties
         * @property {Array.<string>|null} [participantIds] LiarsDiceState participantIds
         * @property {Array.<string>|null} [readyPlayerIds] LiarsDiceState readyPlayerIds
         * @property {Array.<game.IntPair.$Properties>|null} [diceCounts] LiarsDiceState diceCounts
         * @property {string|null} [currentTurn] LiarsDiceState currentTurn
         * @property {game.LiarsDiceBid.$Properties|null} [currentBid] LiarsDiceState currentBid
         * @property {Array.<game.LiarsDiceBid.$Properties>|null} [bidHistory] LiarsDiceState bidHistory
         * @property {boolean|null} [onesWildDisabled] LiarsDiceState onesWildDisabled
         * @property {number|null} [roundNumber] LiarsDiceState roundNumber
         * @property {boolean|null} [ended] LiarsDiceState ended
         * @property {string|null} [winnerId] LiarsDiceState winnerId
         * @property {string|null} [loserId] LiarsDiceState loserId
         * @property {Array.<game.LiarsDiceHandsPair.$Properties>|null} [revealedHands] LiarsDiceState revealedHands
         * @property {number|null} [actualCount] LiarsDiceState actualCount
         * @property {number|null} [minPlayers] LiarsDiceState minPlayers
         * @property {number|null} [maxPlayers] LiarsDiceState maxPlayers
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LiarsDiceState.
         * @memberof game
         * @interface ILiarsDiceState
         * @augments game.LiarsDiceState.$Properties
         * @deprecated Use game.LiarsDiceState.$Properties instead.
         */

        /**
         * Shape of a LiarsDiceState.
         * @typedef {game.LiarsDiceState.$Properties} game.LiarsDiceState.$Shape
         */

        /**
         * Constructs a new LiarsDiceState.
         * @memberof game
         * @classdesc Represents a LiarsDiceState.
         * @constructor
         * @param {game.LiarsDiceState.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LiarsDiceState = function (properties) {
            this.participantIds = [];
            this.readyPlayerIds = [];
            this.diceCounts = [];
            this.bidHistory = [];
            this.revealedHands = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LiarsDiceState participantIds.
         * @member {Array.<string>} participantIds
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.participantIds = $util.emptyArray;

        /**
         * LiarsDiceState readyPlayerIds.
         * @member {Array.<string>} readyPlayerIds
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.readyPlayerIds = $util.emptyArray;

        /**
         * LiarsDiceState diceCounts.
         * @member {Array.<game.IntPair.$Properties>} diceCounts
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.diceCounts = $util.emptyArray;

        /**
         * LiarsDiceState currentTurn.
         * @member {string} currentTurn
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.currentTurn = "";

        /**
         * LiarsDiceState currentBid.
         * @member {game.LiarsDiceBid.$Properties|null|undefined} currentBid
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.currentBid = null;

        /**
         * LiarsDiceState bidHistory.
         * @member {Array.<game.LiarsDiceBid.$Properties>} bidHistory
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.bidHistory = $util.emptyArray;

        /**
         * LiarsDiceState onesWildDisabled.
         * @member {boolean} onesWildDisabled
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.onesWildDisabled = false;

        /**
         * LiarsDiceState roundNumber.
         * @member {number} roundNumber
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.roundNumber = 0;

        /**
         * LiarsDiceState ended.
         * @member {boolean} ended
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.ended = false;

        /**
         * LiarsDiceState winnerId.
         * @member {string} winnerId
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.winnerId = "";

        /**
         * LiarsDiceState loserId.
         * @member {string} loserId
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.loserId = "";

        /**
         * LiarsDiceState revealedHands.
         * @member {Array.<game.LiarsDiceHandsPair.$Properties>} revealedHands
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.revealedHands = $util.emptyArray;

        /**
         * LiarsDiceState actualCount.
         * @member {number} actualCount
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.actualCount = 0;

        /**
         * LiarsDiceState minPlayers.
         * @member {number} minPlayers
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.minPlayers = 0;

        /**
         * LiarsDiceState maxPlayers.
         * @member {number} maxPlayers
         * @memberof game.LiarsDiceState
         * @instance
         */
        LiarsDiceState.prototype.maxPlayers = 0;

        /**
         * Creates a new LiarsDiceState instance using the specified properties.
         * @function create
         * @memberof game.LiarsDiceState
         * @static
         * @param {game.LiarsDiceState.$Properties=} [properties] Properties to set
         * @returns {game.LiarsDiceState} LiarsDiceState instance
         * @type {{
         *   (properties: game.LiarsDiceState.$Shape): game.LiarsDiceState & game.LiarsDiceState.$Shape;
         *   (properties?: game.LiarsDiceState.$Properties): game.LiarsDiceState;
         * }}
         */
        LiarsDiceState.create = function(properties) {
            return new LiarsDiceState(properties);
        };

        /**
         * Encodes the specified LiarsDiceState message. Does not implicitly {@link game.LiarsDiceState.verify|verify} messages.
         * @function encode
         * @memberof game.LiarsDiceState
         * @static
         * @param {game.LiarsDiceState.$Properties} message LiarsDiceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LiarsDiceState.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.participantIds != null && message.participantIds.length)
                for (let i = 0; i < message.participantIds.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.participantIds[i]);
            if (message.readyPlayerIds != null && message.readyPlayerIds.length)
                for (let i = 0; i < message.readyPlayerIds.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.readyPlayerIds[i]);
            if (message.diceCounts != null && message.diceCounts.length)
                for (let i = 0; i < message.diceCounts.length; ++i)
                    $root.game.IntPair.encode(message.diceCounts[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.currentTurn != null && $Object.hasOwnProperty.call(message, "currentTurn") && message.currentTurn !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.currentTurn);
            if (message.currentBid != null && $Object.hasOwnProperty.call(message, "currentBid"))
                $root.game.LiarsDiceBid.encode(message.currentBid, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.bidHistory != null && message.bidHistory.length)
                for (let i = 0; i < message.bidHistory.length; ++i)
                    $root.game.LiarsDiceBid.encode(message.bidHistory[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.onesWildDisabled != null && $Object.hasOwnProperty.call(message, "onesWildDisabled") && message.onesWildDisabled !== false)
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.onesWildDisabled);
            if (message.roundNumber != null && $Object.hasOwnProperty.call(message, "roundNumber") && message.roundNumber !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.roundNumber);
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended") && message.ended !== false)
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.ended);
            if (message.winnerId != null && $Object.hasOwnProperty.call(message, "winnerId") && message.winnerId !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.winnerId);
            if (message.loserId != null && $Object.hasOwnProperty.call(message, "loserId") && message.loserId !== "")
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.loserId);
            if (message.revealedHands != null && message.revealedHands.length)
                for (let i = 0; i < message.revealedHands.length; ++i)
                    $root.game.LiarsDiceHandsPair.encode(message.revealedHands[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.actualCount != null && $Object.hasOwnProperty.call(message, "actualCount") && message.actualCount !== 0)
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.actualCount);
            if (message.minPlayers != null && $Object.hasOwnProperty.call(message, "minPlayers") && message.minPlayers !== 0)
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.minPlayers);
            if (message.maxPlayers != null && $Object.hasOwnProperty.call(message, "maxPlayers") && message.maxPlayers !== 0)
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.maxPlayers);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LiarsDiceState message, length delimited. Does not implicitly {@link game.LiarsDiceState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LiarsDiceState
         * @static
         * @param {game.LiarsDiceState.$Properties} message LiarsDiceState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LiarsDiceState.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LiarsDiceState message from the specified reader or buffer.
         * @function decode
         * @memberof game.LiarsDiceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LiarsDiceState & game.LiarsDiceState.$Shape} LiarsDiceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LiarsDiceState.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LiarsDiceState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.participantIds && message.participantIds.length))
                            message.participantIds = [];
                        message.participantIds.push(reader.stringVerify());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.readyPlayerIds && message.readyPlayerIds.length))
                            message.readyPlayerIds = [];
                        message.readyPlayerIds.push(reader.stringVerify());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.diceCounts && message.diceCounts.length))
                            message.diceCounts = [];
                        message.diceCounts.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.currentTurn = value;
                        else
                            delete message.currentTurn;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.currentBid = $root.game.LiarsDiceBid.decode(reader, reader.uint32(), $undefined, _depth + 1, message.currentBid);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bidHistory && message.bidHistory.length))
                            message.bidHistory = [];
                        message.bidHistory.push($root.game.LiarsDiceBid.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.onesWildDisabled = value;
                        else
                            delete message.onesWildDisabled;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.roundNumber = value;
                        else
                            delete message.roundNumber;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ended = value;
                        else
                            delete message.ended;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.winnerId = value;
                        else
                            delete message.winnerId;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.loserId = value;
                        else
                            delete message.loserId;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.revealedHands && message.revealedHands.length))
                            message.revealedHands = [];
                        message.revealedHands.push($root.game.LiarsDiceHandsPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.actualCount = value;
                        else
                            delete message.actualCount;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.minPlayers = value;
                        else
                            delete message.minPlayers;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxPlayers = value;
                        else
                            delete message.maxPlayers;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LiarsDiceState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LiarsDiceState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LiarsDiceState & game.LiarsDiceState.$Shape} LiarsDiceState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LiarsDiceState.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LiarsDiceState message.
         * @function verify
         * @memberof game.LiarsDiceState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LiarsDiceState.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.participantIds != null && $Object.hasOwnProperty.call(message, "participantIds")) {
                if (!$Array.isArray(message.participantIds))
                    return "participantIds: array expected";
                for (let i = 0; i < message.participantIds.length; ++i)
                    if (!$util.isString(message.participantIds[i]))
                        return "participantIds: string[] expected";
            }
            if (message.readyPlayerIds != null && $Object.hasOwnProperty.call(message, "readyPlayerIds")) {
                if (!$Array.isArray(message.readyPlayerIds))
                    return "readyPlayerIds: array expected";
                for (let i = 0; i < message.readyPlayerIds.length; ++i)
                    if (!$util.isString(message.readyPlayerIds[i]))
                        return "readyPlayerIds: string[] expected";
            }
            if (message.diceCounts != null && $Object.hasOwnProperty.call(message, "diceCounts")) {
                if (!$Array.isArray(message.diceCounts))
                    return "diceCounts: array expected";
                for (let i = 0; i < message.diceCounts.length; ++i) {
                    let error = $root.game.IntPair.verify(message.diceCounts[i], _depth + 1);
                    if (error)
                        return "diceCounts." + error;
                }
            }
            if (message.currentTurn != null && $Object.hasOwnProperty.call(message, "currentTurn"))
                if (!$util.isString(message.currentTurn))
                    return "currentTurn: string expected";
            if (message.currentBid != null && $Object.hasOwnProperty.call(message, "currentBid")) {
                let error = $root.game.LiarsDiceBid.verify(message.currentBid, _depth + 1);
                if (error)
                    return "currentBid." + error;
            }
            if (message.bidHistory != null && $Object.hasOwnProperty.call(message, "bidHistory")) {
                if (!$Array.isArray(message.bidHistory))
                    return "bidHistory: array expected";
                for (let i = 0; i < message.bidHistory.length; ++i) {
                    let error = $root.game.LiarsDiceBid.verify(message.bidHistory[i], _depth + 1);
                    if (error)
                        return "bidHistory." + error;
                }
            }
            if (message.onesWildDisabled != null && $Object.hasOwnProperty.call(message, "onesWildDisabled"))
                if (typeof message.onesWildDisabled !== "boolean")
                    return "onesWildDisabled: boolean expected";
            if (message.roundNumber != null && $Object.hasOwnProperty.call(message, "roundNumber"))
                if (!$util.isInteger(message.roundNumber))
                    return "roundNumber: integer expected";
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended"))
                if (typeof message.ended !== "boolean")
                    return "ended: boolean expected";
            if (message.winnerId != null && $Object.hasOwnProperty.call(message, "winnerId"))
                if (!$util.isString(message.winnerId))
                    return "winnerId: string expected";
            if (message.loserId != null && $Object.hasOwnProperty.call(message, "loserId"))
                if (!$util.isString(message.loserId))
                    return "loserId: string expected";
            if (message.revealedHands != null && $Object.hasOwnProperty.call(message, "revealedHands")) {
                if (!$Array.isArray(message.revealedHands))
                    return "revealedHands: array expected";
                for (let i = 0; i < message.revealedHands.length; ++i) {
                    let error = $root.game.LiarsDiceHandsPair.verify(message.revealedHands[i], _depth + 1);
                    if (error)
                        return "revealedHands." + error;
                }
            }
            if (message.actualCount != null && $Object.hasOwnProperty.call(message, "actualCount"))
                if (!$util.isInteger(message.actualCount))
                    return "actualCount: integer expected";
            if (message.minPlayers != null && $Object.hasOwnProperty.call(message, "minPlayers"))
                if (!$util.isInteger(message.minPlayers))
                    return "minPlayers: integer expected";
            if (message.maxPlayers != null && $Object.hasOwnProperty.call(message, "maxPlayers"))
                if (!$util.isInteger(message.maxPlayers))
                    return "maxPlayers: integer expected";
            return null;
        };

        /**
         * Creates a LiarsDiceState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LiarsDiceState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LiarsDiceState} LiarsDiceState
         */
        LiarsDiceState.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LiarsDiceState)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LiarsDiceState: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LiarsDiceState();
            if (object.participantIds) {
                if (!$Array.isArray(object.participantIds))
                    throw $TypeError(".game.LiarsDiceState.participantIds: array expected");
                message.participantIds = $Array(object.participantIds.length);
                for (let i = 0; i < object.participantIds.length; ++i)
                    message.participantIds[i] = $String(object.participantIds[i]);
            }
            if (object.readyPlayerIds) {
                if (!$Array.isArray(object.readyPlayerIds))
                    throw $TypeError(".game.LiarsDiceState.readyPlayerIds: array expected");
                message.readyPlayerIds = $Array(object.readyPlayerIds.length);
                for (let i = 0; i < object.readyPlayerIds.length; ++i)
                    message.readyPlayerIds[i] = $String(object.readyPlayerIds[i]);
            }
            if (object.diceCounts) {
                if (!$Array.isArray(object.diceCounts))
                    throw $TypeError(".game.LiarsDiceState.diceCounts: array expected");
                message.diceCounts = $Array(object.diceCounts.length);
                for (let i = 0; i < object.diceCounts.length; ++i) {
                    if (!$util.isObject(object.diceCounts[i]))
                        throw $TypeError(".game.LiarsDiceState.diceCounts: object expected");
                    message.diceCounts[i] = $root.game.IntPair.fromObject(object.diceCounts[i], _depth + 1);
                }
            }
            if (object.currentTurn != null)
                if (typeof object.currentTurn !== "string" || object.currentTurn.length)
                    message.currentTurn = $String(object.currentTurn);
            if (object.currentBid != null) {
                if (!$util.isObject(object.currentBid))
                    throw $TypeError(".game.LiarsDiceState.currentBid: object expected");
                message.currentBid = $root.game.LiarsDiceBid.fromObject(object.currentBid, _depth + 1);
            }
            if (object.bidHistory) {
                if (!$Array.isArray(object.bidHistory))
                    throw $TypeError(".game.LiarsDiceState.bidHistory: array expected");
                message.bidHistory = $Array(object.bidHistory.length);
                for (let i = 0; i < object.bidHistory.length; ++i) {
                    if (!$util.isObject(object.bidHistory[i]))
                        throw $TypeError(".game.LiarsDiceState.bidHistory: object expected");
                    message.bidHistory[i] = $root.game.LiarsDiceBid.fromObject(object.bidHistory[i], _depth + 1);
                }
            }
            if (object.onesWildDisabled != null)
                if (object.onesWildDisabled)
                    message.onesWildDisabled = $Boolean(object.onesWildDisabled);
            if (object.roundNumber != null)
                if ($Number(object.roundNumber) !== 0)
                    message.roundNumber = object.roundNumber | 0;
            if (object.ended != null)
                if (object.ended)
                    message.ended = $Boolean(object.ended);
            if (object.winnerId != null)
                if (typeof object.winnerId !== "string" || object.winnerId.length)
                    message.winnerId = $String(object.winnerId);
            if (object.loserId != null)
                if (typeof object.loserId !== "string" || object.loserId.length)
                    message.loserId = $String(object.loserId);
            if (object.revealedHands) {
                if (!$Array.isArray(object.revealedHands))
                    throw $TypeError(".game.LiarsDiceState.revealedHands: array expected");
                message.revealedHands = $Array(object.revealedHands.length);
                for (let i = 0; i < object.revealedHands.length; ++i) {
                    if (!$util.isObject(object.revealedHands[i]))
                        throw $TypeError(".game.LiarsDiceState.revealedHands: object expected");
                    message.revealedHands[i] = $root.game.LiarsDiceHandsPair.fromObject(object.revealedHands[i], _depth + 1);
                }
            }
            if (object.actualCount != null)
                if ($Number(object.actualCount) !== 0)
                    message.actualCount = object.actualCount | 0;
            if (object.minPlayers != null)
                if ($Number(object.minPlayers) !== 0)
                    message.minPlayers = object.minPlayers | 0;
            if (object.maxPlayers != null)
                if ($Number(object.maxPlayers) !== 0)
                    message.maxPlayers = object.maxPlayers | 0;
            return message;
        };

        /**
         * Creates a plain object from a LiarsDiceState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LiarsDiceState
         * @static
         * @param {game.LiarsDiceState} message LiarsDiceState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LiarsDiceState.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.participantIds = [];
                object.readyPlayerIds = [];
                object.diceCounts = [];
                object.bidHistory = [];
                object.revealedHands = [];
            }
            if (options.defaults) {
                object.currentTurn = "";
                object.currentBid = null;
                object.onesWildDisabled = false;
                object.roundNumber = 0;
                object.ended = false;
                object.winnerId = "";
                object.loserId = "";
                object.actualCount = 0;
                object.minPlayers = 0;
                object.maxPlayers = 0;
            }
            if (message.participantIds && message.participantIds.length) {
                object.participantIds = $Array(message.participantIds.length);
                for (let j = 0; j < message.participantIds.length; ++j)
                    object.participantIds[j] = message.participantIds[j];
            }
            if (message.readyPlayerIds && message.readyPlayerIds.length) {
                object.readyPlayerIds = $Array(message.readyPlayerIds.length);
                for (let j = 0; j < message.readyPlayerIds.length; ++j)
                    object.readyPlayerIds[j] = message.readyPlayerIds[j];
            }
            if (message.diceCounts && message.diceCounts.length) {
                object.diceCounts = $Array(message.diceCounts.length);
                for (let j = 0; j < message.diceCounts.length; ++j)
                    object.diceCounts[j] = $root.game.IntPair.toObject(message.diceCounts[j], options, _depth + 1);
            }
            if (message.currentTurn != null && $Object.hasOwnProperty.call(message, "currentTurn"))
                object.currentTurn = message.currentTurn;
            if (message.currentBid != null && $Object.hasOwnProperty.call(message, "currentBid"))
                object.currentBid = $root.game.LiarsDiceBid.toObject(message.currentBid, options, _depth + 1);
            if (message.bidHistory && message.bidHistory.length) {
                object.bidHistory = $Array(message.bidHistory.length);
                for (let j = 0; j < message.bidHistory.length; ++j)
                    object.bidHistory[j] = $root.game.LiarsDiceBid.toObject(message.bidHistory[j], options, _depth + 1);
            }
            if (message.onesWildDisabled != null && $Object.hasOwnProperty.call(message, "onesWildDisabled"))
                object.onesWildDisabled = message.onesWildDisabled;
            if (message.roundNumber != null && $Object.hasOwnProperty.call(message, "roundNumber"))
                object.roundNumber = message.roundNumber;
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended"))
                object.ended = message.ended;
            if (message.winnerId != null && $Object.hasOwnProperty.call(message, "winnerId"))
                object.winnerId = message.winnerId;
            if (message.loserId != null && $Object.hasOwnProperty.call(message, "loserId"))
                object.loserId = message.loserId;
            if (message.revealedHands && message.revealedHands.length) {
                object.revealedHands = $Array(message.revealedHands.length);
                for (let j = 0; j < message.revealedHands.length; ++j)
                    object.revealedHands[j] = $root.game.LiarsDiceHandsPair.toObject(message.revealedHands[j], options, _depth + 1);
            }
            if (message.actualCount != null && $Object.hasOwnProperty.call(message, "actualCount"))
                object.actualCount = message.actualCount;
            if (message.minPlayers != null && $Object.hasOwnProperty.call(message, "minPlayers"))
                object.minPlayers = message.minPlayers;
            if (message.maxPlayers != null && $Object.hasOwnProperty.call(message, "maxPlayers"))
                object.maxPlayers = message.maxPlayers;
            return object;
        };

        /**
         * Converts this LiarsDiceState to JSON.
         * @function toJSON
         * @memberof game.LiarsDiceState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LiarsDiceState.prototype.toJSON = function() {
            return LiarsDiceState.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LiarsDiceState
         * @function getTypeUrl
         * @memberof game.LiarsDiceState
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LiarsDiceState.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LiarsDiceState";
        };

        return LiarsDiceState;
    })();

    game.OthelloPendingSettlement = (function() {

        /**
         * Properties of an OthelloPendingSettlement.
         * @typedef {Object} game.OthelloPendingSettlement.$Properties
         * @property {string|null} [id] OthelloPendingSettlement id
         * @property {string|null} [seat] OthelloPendingSettlement seat
         * @property {string|null} [opponentSeat] OthelloPendingSettlement opponentSeat
         * @property {number|null} [flips] OthelloPendingSettlement flips
         * @property {number|null} [stake] OthelloPendingSettlement stake
         * @property {string|null} [nextTurn] OthelloPendingSettlement nextTurn
         * @property {number|Long|null} [expiresAt] OthelloPendingSettlement expiresAt
         * @property {string|null} [forced] OthelloPendingSettlement forced
         * @property {string|null} [resolvedAs] OthelloPendingSettlement resolvedAs
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an OthelloPendingSettlement.
         * @memberof game
         * @interface IOthelloPendingSettlement
         * @augments game.OthelloPendingSettlement.$Properties
         * @deprecated Use game.OthelloPendingSettlement.$Properties instead.
         */

        /**
         * Shape of an OthelloPendingSettlement.
         * @typedef {game.OthelloPendingSettlement.$Properties} game.OthelloPendingSettlement.$Shape
         */

        /**
         * Constructs a new OthelloPendingSettlement.
         * @memberof game
         * @classdesc Represents an OthelloPendingSettlement.
         * @constructor
         * @param {game.OthelloPendingSettlement.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const OthelloPendingSettlement = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OthelloPendingSettlement id.
         * @member {string} id
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.id = "";

        /**
         * OthelloPendingSettlement seat.
         * @member {string} seat
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.seat = "";

        /**
         * OthelloPendingSettlement opponentSeat.
         * @member {string} opponentSeat
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.opponentSeat = "";

        /**
         * OthelloPendingSettlement flips.
         * @member {number} flips
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.flips = 0;

        /**
         * OthelloPendingSettlement stake.
         * @member {number} stake
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.stake = 0;

        /**
         * OthelloPendingSettlement nextTurn.
         * @member {string} nextTurn
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.nextTurn = "";

        /**
         * OthelloPendingSettlement expiresAt.
         * @member {number|Long} expiresAt
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.expiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OthelloPendingSettlement forced.
         * @member {string} forced
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.forced = "";

        /**
         * OthelloPendingSettlement resolvedAs.
         * @member {string} resolvedAs
         * @memberof game.OthelloPendingSettlement
         * @instance
         */
        OthelloPendingSettlement.prototype.resolvedAs = "";

        /**
         * Creates a new OthelloPendingSettlement instance using the specified properties.
         * @function create
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {game.OthelloPendingSettlement.$Properties=} [properties] Properties to set
         * @returns {game.OthelloPendingSettlement} OthelloPendingSettlement instance
         * @type {{
         *   (properties: game.OthelloPendingSettlement.$Shape): game.OthelloPendingSettlement & game.OthelloPendingSettlement.$Shape;
         *   (properties?: game.OthelloPendingSettlement.$Properties): game.OthelloPendingSettlement;
         * }}
         */
        OthelloPendingSettlement.create = function(properties) {
            return new OthelloPendingSettlement(properties);
        };

        /**
         * Encodes the specified OthelloPendingSettlement message. Does not implicitly {@link game.OthelloPendingSettlement.verify|verify} messages.
         * @function encode
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {game.OthelloPendingSettlement.$Properties} message OthelloPendingSettlement message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OthelloPendingSettlement.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.seat != null && $Object.hasOwnProperty.call(message, "seat") && message.seat !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.seat);
            if (message.opponentSeat != null && $Object.hasOwnProperty.call(message, "opponentSeat") && message.opponentSeat !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.opponentSeat);
            if (message.flips != null && $Object.hasOwnProperty.call(message, "flips") && message.flips !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.flips);
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake") && message.stake !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.stake);
            if (message.nextTurn != null && $Object.hasOwnProperty.call(message, "nextTurn") && message.nextTurn !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.nextTurn);
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt") && (typeof message.expiresAt === "object" ? message.expiresAt.low || message.expiresAt.high : message.expiresAt !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.expiresAt);
            if (message.forced != null && $Object.hasOwnProperty.call(message, "forced") && message.forced !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.forced);
            if (message.resolvedAs != null && $Object.hasOwnProperty.call(message, "resolvedAs") && message.resolvedAs !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.resolvedAs);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OthelloPendingSettlement message, length delimited. Does not implicitly {@link game.OthelloPendingSettlement.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {game.OthelloPendingSettlement.$Properties} message OthelloPendingSettlement message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OthelloPendingSettlement.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an OthelloPendingSettlement message from the specified reader or buffer.
         * @function decode
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.OthelloPendingSettlement & game.OthelloPendingSettlement.$Shape} OthelloPendingSettlement
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OthelloPendingSettlement.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.OthelloPendingSettlement(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.opponentSeat = value;
                        else
                            delete message.opponentSeat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.flips = value;
                        else
                            delete message.flips;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.stake = value;
                        else
                            delete message.stake;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nextTurn = value;
                        else
                            delete message.nextTurn;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiresAt = value;
                        else
                            delete message.expiresAt;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.forced = value;
                        else
                            delete message.forced;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.resolvedAs = value;
                        else
                            delete message.resolvedAs;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OthelloPendingSettlement message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.OthelloPendingSettlement & game.OthelloPendingSettlement.$Shape} OthelloPendingSettlement
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OthelloPendingSettlement.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OthelloPendingSettlement message.
         * @function verify
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OthelloPendingSettlement.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.seat != null && $Object.hasOwnProperty.call(message, "seat"))
                if (!$util.isString(message.seat))
                    return "seat: string expected";
            if (message.opponentSeat != null && $Object.hasOwnProperty.call(message, "opponentSeat"))
                if (!$util.isString(message.opponentSeat))
                    return "opponentSeat: string expected";
            if (message.flips != null && $Object.hasOwnProperty.call(message, "flips"))
                if (!$util.isInteger(message.flips))
                    return "flips: integer expected";
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (!$util.isInteger(message.stake))
                    return "stake: integer expected";
            if (message.nextTurn != null && $Object.hasOwnProperty.call(message, "nextTurn"))
                if (!$util.isString(message.nextTurn))
                    return "nextTurn: string expected";
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (!$util.isInteger(message.expiresAt) && !(message.expiresAt && $util.isInteger(message.expiresAt.low) && $util.isInteger(message.expiresAt.high)))
                    return "expiresAt: integer|Long expected";
            if (message.forced != null && $Object.hasOwnProperty.call(message, "forced"))
                if (!$util.isString(message.forced))
                    return "forced: string expected";
            if (message.resolvedAs != null && $Object.hasOwnProperty.call(message, "resolvedAs"))
                if (!$util.isString(message.resolvedAs))
                    return "resolvedAs: string expected";
            return null;
        };

        /**
         * Creates an OthelloPendingSettlement message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.OthelloPendingSettlement} OthelloPendingSettlement
         */
        OthelloPendingSettlement.fromObject = function (object, _depth) {
            if (object instanceof $root.game.OthelloPendingSettlement)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.OthelloPendingSettlement: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.OthelloPendingSettlement();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.seat != null)
                if (typeof object.seat !== "string" || object.seat.length)
                    message.seat = $String(object.seat);
            if (object.opponentSeat != null)
                if (typeof object.opponentSeat !== "string" || object.opponentSeat.length)
                    message.opponentSeat = $String(object.opponentSeat);
            if (object.flips != null)
                if ($Number(object.flips) !== 0)
                    message.flips = object.flips | 0;
            if (object.stake != null)
                if ($Number(object.stake) !== 0)
                    message.stake = object.stake | 0;
            if (object.nextTurn != null)
                if (typeof object.nextTurn !== "string" || object.nextTurn.length)
                    message.nextTurn = $String(object.nextTurn);
            if (object.expiresAt != null)
                if (typeof object.expiresAt === "object" ? object.expiresAt.low || object.expiresAt.high : $Number(object.expiresAt) !== 0)
                    if ($util.Long)
                        message.expiresAt = $util.Long.fromValue(object.expiresAt, false);
                    else if (typeof object.expiresAt === "string")
                        message.expiresAt = $parseInt(object.expiresAt, 10);
                    else if (typeof object.expiresAt === "number")
                        message.expiresAt = object.expiresAt;
                    else if (typeof object.expiresAt === "object")
                        message.expiresAt = new $util.LongBits(object.expiresAt.low >>> 0, object.expiresAt.high >>> 0).toNumber();
            if (object.forced != null)
                if (typeof object.forced !== "string" || object.forced.length)
                    message.forced = $String(object.forced);
            if (object.resolvedAs != null)
                if (typeof object.resolvedAs !== "string" || object.resolvedAs.length)
                    message.resolvedAs = $String(object.resolvedAs);
            return message;
        };

        /**
         * Creates a plain object from an OthelloPendingSettlement message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {game.OthelloPendingSettlement} message OthelloPendingSettlement
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OthelloPendingSettlement.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.seat = "";
                object.opponentSeat = "";
                object.flips = 0;
                object.stake = 0;
                object.nextTurn = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.forced = "";
                object.resolvedAs = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.seat != null && $Object.hasOwnProperty.call(message, "seat"))
                object.seat = message.seat;
            if (message.opponentSeat != null && $Object.hasOwnProperty.call(message, "opponentSeat"))
                object.opponentSeat = message.opponentSeat;
            if (message.flips != null && $Object.hasOwnProperty.call(message, "flips"))
                object.flips = message.flips;
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                object.stake = message.stake;
            if (message.nextTurn != null && $Object.hasOwnProperty.call(message, "nextTurn"))
                object.nextTurn = message.nextTurn;
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiresAt = typeof message.expiresAt === "number" ? $BigInt(message.expiresAt) : $util.Long.fromBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.expiresAt === "number")
                    object.expiresAt = options.longs === $String ? $String(message.expiresAt) : message.expiresAt;
                else
                    object.expiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.expiresAt) : options.longs === $Number ? new $util.LongBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0).toNumber() : message.expiresAt;
            if (message.forced != null && $Object.hasOwnProperty.call(message, "forced"))
                object.forced = message.forced;
            if (message.resolvedAs != null && $Object.hasOwnProperty.call(message, "resolvedAs"))
                object.resolvedAs = message.resolvedAs;
            return object;
        };

        /**
         * Converts this OthelloPendingSettlement to JSON.
         * @function toJSON
         * @memberof game.OthelloPendingSettlement
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OthelloPendingSettlement.prototype.toJSON = function() {
            return OthelloPendingSettlement.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OthelloPendingSettlement
         * @function getTypeUrl
         * @memberof game.OthelloPendingSettlement
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OthelloPendingSettlement.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.OthelloPendingSettlement";
        };

        return OthelloPendingSettlement;
    })();

    game.OthelloSurrenderRequest = (function() {

        /**
         * Properties of an OthelloSurrenderRequest.
         * @typedef {Object} game.OthelloSurrenderRequest.$Properties
         * @property {string|null} [fromSeat] OthelloSurrenderRequest fromSeat
         * @property {string|null} [toSeat] OthelloSurrenderRequest toSeat
         * @property {number|Long|null} [createdAt] OthelloSurrenderRequest createdAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an OthelloSurrenderRequest.
         * @memberof game
         * @interface IOthelloSurrenderRequest
         * @augments game.OthelloSurrenderRequest.$Properties
         * @deprecated Use game.OthelloSurrenderRequest.$Properties instead.
         */

        /**
         * Shape of an OthelloSurrenderRequest.
         * @typedef {game.OthelloSurrenderRequest.$Properties} game.OthelloSurrenderRequest.$Shape
         */

        /**
         * Constructs a new OthelloSurrenderRequest.
         * @memberof game
         * @classdesc Represents an OthelloSurrenderRequest.
         * @constructor
         * @param {game.OthelloSurrenderRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const OthelloSurrenderRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OthelloSurrenderRequest fromSeat.
         * @member {string} fromSeat
         * @memberof game.OthelloSurrenderRequest
         * @instance
         */
        OthelloSurrenderRequest.prototype.fromSeat = "";

        /**
         * OthelloSurrenderRequest toSeat.
         * @member {string} toSeat
         * @memberof game.OthelloSurrenderRequest
         * @instance
         */
        OthelloSurrenderRequest.prototype.toSeat = "";

        /**
         * OthelloSurrenderRequest createdAt.
         * @member {number|Long} createdAt
         * @memberof game.OthelloSurrenderRequest
         * @instance
         */
        OthelloSurrenderRequest.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new OthelloSurrenderRequest instance using the specified properties.
         * @function create
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {game.OthelloSurrenderRequest.$Properties=} [properties] Properties to set
         * @returns {game.OthelloSurrenderRequest} OthelloSurrenderRequest instance
         * @type {{
         *   (properties: game.OthelloSurrenderRequest.$Shape): game.OthelloSurrenderRequest & game.OthelloSurrenderRequest.$Shape;
         *   (properties?: game.OthelloSurrenderRequest.$Properties): game.OthelloSurrenderRequest;
         * }}
         */
        OthelloSurrenderRequest.create = function(properties) {
            return new OthelloSurrenderRequest(properties);
        };

        /**
         * Encodes the specified OthelloSurrenderRequest message. Does not implicitly {@link game.OthelloSurrenderRequest.verify|verify} messages.
         * @function encode
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {game.OthelloSurrenderRequest.$Properties} message OthelloSurrenderRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OthelloSurrenderRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat") && message.fromSeat !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fromSeat);
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat") && message.toSeat !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.toSeat);
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.createdAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OthelloSurrenderRequest message, length delimited. Does not implicitly {@link game.OthelloSurrenderRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {game.OthelloSurrenderRequest.$Properties} message OthelloSurrenderRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OthelloSurrenderRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an OthelloSurrenderRequest message from the specified reader or buffer.
         * @function decode
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.OthelloSurrenderRequest & game.OthelloSurrenderRequest.$Shape} OthelloSurrenderRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OthelloSurrenderRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.OthelloSurrenderRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.fromSeat = value;
                        else
                            delete message.fromSeat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.toSeat = value;
                        else
                            delete message.toSeat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.createdAt = value;
                        else
                            delete message.createdAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OthelloSurrenderRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.OthelloSurrenderRequest & game.OthelloSurrenderRequest.$Shape} OthelloSurrenderRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OthelloSurrenderRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OthelloSurrenderRequest message.
         * @function verify
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OthelloSurrenderRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat"))
                if (!$util.isString(message.fromSeat))
                    return "fromSeat: string expected";
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat"))
                if (!$util.isString(message.toSeat))
                    return "toSeat: string expected";
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
                if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                    return "createdAt: integer|Long expected";
            return null;
        };

        /**
         * Creates an OthelloSurrenderRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.OthelloSurrenderRequest} OthelloSurrenderRequest
         */
        OthelloSurrenderRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.game.OthelloSurrenderRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.OthelloSurrenderRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.OthelloSurrenderRequest();
            if (object.fromSeat != null)
                if (typeof object.fromSeat !== "string" || object.fromSeat.length)
                    message.fromSeat = $String(object.fromSeat);
            if (object.toSeat != null)
                if (typeof object.toSeat !== "string" || object.toSeat.length)
                    message.toSeat = $String(object.toSeat);
            if (object.createdAt != null)
                if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = $parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an OthelloSurrenderRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {game.OthelloSurrenderRequest} message OthelloSurrenderRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OthelloSurrenderRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fromSeat = "";
                object.toSeat = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.fromSeat != null && $Object.hasOwnProperty.call(message, "fromSeat"))
                object.fromSeat = message.fromSeat;
            if (message.toSeat != null && $Object.hasOwnProperty.call(message, "toSeat"))
                object.toSeat = message.toSeat;
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                else if (typeof message.createdAt === "number")
                    object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
                else
                    object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
            return object;
        };

        /**
         * Converts this OthelloSurrenderRequest to JSON.
         * @function toJSON
         * @memberof game.OthelloSurrenderRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OthelloSurrenderRequest.prototype.toJSON = function() {
            return OthelloSurrenderRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OthelloSurrenderRequest
         * @function getTypeUrl
         * @memberof game.OthelloSurrenderRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OthelloSurrenderRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.OthelloSurrenderRequest";
        };

        return OthelloSurrenderRequest;
    })();

    game.BoardRow = (function() {

        /**
         * Properties of a BoardRow.
         * @typedef {Object} game.BoardRow.$Properties
         * @property {Array.<string>|null} [cells] BoardRow cells
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BoardRow.
         * @memberof game
         * @interface IBoardRow
         * @augments game.BoardRow.$Properties
         * @deprecated Use game.BoardRow.$Properties instead.
         */

        /**
         * Shape of a BoardRow.
         * @typedef {game.BoardRow.$Properties} game.BoardRow.$Shape
         */

        /**
         * Constructs a new BoardRow.
         * @memberof game
         * @classdesc Represents a BoardRow.
         * @constructor
         * @param {game.BoardRow.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BoardRow = function (properties) {
            this.cells = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BoardRow cells.
         * @member {Array.<string>} cells
         * @memberof game.BoardRow
         * @instance
         */
        BoardRow.prototype.cells = $util.emptyArray;

        /**
         * Creates a new BoardRow instance using the specified properties.
         * @function create
         * @memberof game.BoardRow
         * @static
         * @param {game.BoardRow.$Properties=} [properties] Properties to set
         * @returns {game.BoardRow} BoardRow instance
         * @type {{
         *   (properties: game.BoardRow.$Shape): game.BoardRow & game.BoardRow.$Shape;
         *   (properties?: game.BoardRow.$Properties): game.BoardRow;
         * }}
         */
        BoardRow.create = function(properties) {
            return new BoardRow(properties);
        };

        /**
         * Encodes the specified BoardRow message. Does not implicitly {@link game.BoardRow.verify|verify} messages.
         * @function encode
         * @memberof game.BoardRow
         * @static
         * @param {game.BoardRow.$Properties} message BoardRow message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BoardRow.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.cells != null && message.cells.length)
                for (let i = 0; i < message.cells.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.cells[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BoardRow message, length delimited. Does not implicitly {@link game.BoardRow.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.BoardRow
         * @static
         * @param {game.BoardRow.$Properties} message BoardRow message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BoardRow.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a BoardRow message from the specified reader or buffer.
         * @function decode
         * @memberof game.BoardRow
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.BoardRow & game.BoardRow.$Shape} BoardRow
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BoardRow.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.BoardRow();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cells && message.cells.length))
                            message.cells = [];
                        message.cells.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BoardRow message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.BoardRow
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.BoardRow & game.BoardRow.$Shape} BoardRow
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BoardRow.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BoardRow message.
         * @function verify
         * @memberof game.BoardRow
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BoardRow.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.cells != null && $Object.hasOwnProperty.call(message, "cells")) {
                if (!$Array.isArray(message.cells))
                    return "cells: array expected";
                for (let i = 0; i < message.cells.length; ++i)
                    if (!$util.isString(message.cells[i]))
                        return "cells: string[] expected";
            }
            return null;
        };

        /**
         * Creates a BoardRow message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.BoardRow
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.BoardRow} BoardRow
         */
        BoardRow.fromObject = function (object, _depth) {
            if (object instanceof $root.game.BoardRow)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.BoardRow: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.BoardRow();
            if (object.cells) {
                if (!$Array.isArray(object.cells))
                    throw $TypeError(".game.BoardRow.cells: array expected");
                message.cells = $Array(object.cells.length);
                for (let i = 0; i < object.cells.length; ++i)
                    message.cells[i] = $String(object.cells[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a BoardRow message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.BoardRow
         * @static
         * @param {game.BoardRow} message BoardRow
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BoardRow.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.cells = [];
            if (message.cells && message.cells.length) {
                object.cells = $Array(message.cells.length);
                for (let j = 0; j < message.cells.length; ++j)
                    object.cells[j] = message.cells[j];
            }
            return object;
        };

        /**
         * Converts this BoardRow to JSON.
         * @function toJSON
         * @memberof game.BoardRow
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BoardRow.prototype.toJSON = function() {
            return BoardRow.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BoardRow
         * @function getTypeUrl
         * @memberof game.BoardRow
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BoardRow.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.BoardRow";
        };

        return BoardRow;
    })();

    game.IntPair = (function() {

        /**
         * Properties of an IntPair.
         * @typedef {Object} game.IntPair.$Properties
         * @property {string|null} [key] IntPair key
         * @property {number|null} [value] IntPair value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an IntPair.
         * @memberof game
         * @interface IIntPair
         * @augments game.IntPair.$Properties
         * @deprecated Use game.IntPair.$Properties instead.
         */

        /**
         * Shape of an IntPair.
         * @typedef {game.IntPair.$Properties} game.IntPair.$Shape
         */

        /**
         * Constructs a new IntPair.
         * @memberof game
         * @classdesc Represents an IntPair.
         * @constructor
         * @param {game.IntPair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const IntPair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * IntPair key.
         * @member {string} key
         * @memberof game.IntPair
         * @instance
         */
        IntPair.prototype.key = "";

        /**
         * IntPair value.
         * @member {number} value
         * @memberof game.IntPair
         * @instance
         */
        IntPair.prototype.value = 0;

        /**
         * Creates a new IntPair instance using the specified properties.
         * @function create
         * @memberof game.IntPair
         * @static
         * @param {game.IntPair.$Properties=} [properties] Properties to set
         * @returns {game.IntPair} IntPair instance
         * @type {{
         *   (properties: game.IntPair.$Shape): game.IntPair & game.IntPair.$Shape;
         *   (properties?: game.IntPair.$Properties): game.IntPair;
         * }}
         */
        IntPair.create = function(properties) {
            return new IntPair(properties);
        };

        /**
         * Encodes the specified IntPair message. Does not implicitly {@link game.IntPair.verify|verify} messages.
         * @function encode
         * @memberof game.IntPair
         * @static
         * @param {game.IntPair.$Properties} message IntPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IntPair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified IntPair message, length delimited. Does not implicitly {@link game.IntPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.IntPair
         * @static
         * @param {game.IntPair.$Properties} message IntPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IntPair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an IntPair message from the specified reader or buffer.
         * @function decode
         * @memberof game.IntPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.IntPair & game.IntPair.$Shape} IntPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IntPair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.IntPair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an IntPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.IntPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.IntPair & game.IntPair.$Shape} IntPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IntPair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IntPair message.
         * @function verify
         * @memberof game.IntPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IntPair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value))
                    return "value: integer expected";
            return null;
        };

        /**
         * Creates an IntPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.IntPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.IntPair} IntPair
         */
        IntPair.fromObject = function (object, _depth) {
            if (object instanceof $root.game.IntPair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.IntPair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.IntPair();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.value != null)
                if ($Number(object.value) !== 0)
                    message.value = object.value | 0;
            return message;
        };

        /**
         * Creates a plain object from an IntPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.IntPair
         * @static
         * @param {game.IntPair} message IntPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IntPair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = 0;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this IntPair to JSON.
         * @function toJSON
         * @memberof game.IntPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IntPair.prototype.toJSON = function() {
            return IntPair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for IntPair
         * @function getTypeUrl
         * @memberof game.IntPair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        IntPair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.IntPair";
        };

        return IntPair;
    })();

    game.OthelloState = (function() {

        /**
         * Properties of an OthelloState.
         * @typedef {Object} game.OthelloState.$Properties
         * @property {Array.<game.BoardRow.$Properties>|null} [board] OthelloState board
         * @property {string|null} [turn] OthelloState turn
         * @property {string|null} [blackSeat] OthelloState blackSeat
         * @property {Array.<game.Pos.$Properties>|null} [legalMoves] OthelloState legalMoves
         * @property {number|null} [passCount] OthelloState passCount
         * @property {number|null} [blackCount] OthelloState blackCount
         * @property {number|null} [whiteCount] OthelloState whiteCount
         * @property {Array.<game.IntPair.$Properties>|null} [rankedDelta] OthelloState rankedDelta
         * @property {Array.<string>|null} [settlementEvents] OthelloState settlementEvents
         * @property {game.OthelloPendingSettlement.$Properties|null} [pendingSettlement] OthelloState pendingSettlement
         * @property {game.OthelloSurrenderRequest.$Properties|null} [surrenderRequest] OthelloState surrenderRequest
         * @property {boolean|null} [ended] OthelloState ended
         * @property {string|null} [winner] OthelloState winner
         * @property {number|Long|null} [moveDeadlineAt] OthelloState moveDeadlineAt
         * @property {number|Long|null} [clockDeadlineAt] OthelloState clockDeadlineAt
         * @property {Array.<game.IntPair.$Properties>|null} [clockRemaining] OthelloState clockRemaining
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an OthelloState.
         * @memberof game
         * @interface IOthelloState
         * @augments game.OthelloState.$Properties
         * @deprecated Use game.OthelloState.$Properties instead.
         */

        /**
         * Shape of an OthelloState.
         * @typedef {game.OthelloState.$Properties} game.OthelloState.$Shape
         */

        /**
         * Constructs a new OthelloState.
         * @memberof game
         * @classdesc Represents an OthelloState.
         * @constructor
         * @param {game.OthelloState.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const OthelloState = function (properties) {
            this.board = [];
            this.legalMoves = [];
            this.rankedDelta = [];
            this.settlementEvents = [];
            this.clockRemaining = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OthelloState board.
         * @member {Array.<game.BoardRow.$Properties>} board
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.board = $util.emptyArray;

        /**
         * OthelloState turn.
         * @member {string} turn
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.turn = "";

        /**
         * OthelloState blackSeat.
         * @member {string} blackSeat
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.blackSeat = "";

        /**
         * OthelloState legalMoves.
         * @member {Array.<game.Pos.$Properties>} legalMoves
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.legalMoves = $util.emptyArray;

        /**
         * OthelloState passCount.
         * @member {number} passCount
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.passCount = 0;

        /**
         * OthelloState blackCount.
         * @member {number} blackCount
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.blackCount = 0;

        /**
         * OthelloState whiteCount.
         * @member {number} whiteCount
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.whiteCount = 0;

        /**
         * OthelloState rankedDelta.
         * @member {Array.<game.IntPair.$Properties>} rankedDelta
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.rankedDelta = $util.emptyArray;

        /**
         * OthelloState settlementEvents.
         * @member {Array.<string>} settlementEvents
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.settlementEvents = $util.emptyArray;

        /**
         * OthelloState pendingSettlement.
         * @member {game.OthelloPendingSettlement.$Properties|null|undefined} pendingSettlement
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.pendingSettlement = null;

        /**
         * OthelloState surrenderRequest.
         * @member {game.OthelloSurrenderRequest.$Properties|null|undefined} surrenderRequest
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.surrenderRequest = null;

        /**
         * OthelloState ended.
         * @member {boolean} ended
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.ended = false;

        /**
         * OthelloState winner.
         * @member {string} winner
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.winner = "";

        /**
         * OthelloState moveDeadlineAt.
         * @member {number|Long} moveDeadlineAt
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.moveDeadlineAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OthelloState clockDeadlineAt.
         * @member {number|Long} clockDeadlineAt
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.clockDeadlineAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * OthelloState clockRemaining.
         * @member {Array.<game.IntPair.$Properties>} clockRemaining
         * @memberof game.OthelloState
         * @instance
         */
        OthelloState.prototype.clockRemaining = $util.emptyArray;

        /**
         * Creates a new OthelloState instance using the specified properties.
         * @function create
         * @memberof game.OthelloState
         * @static
         * @param {game.OthelloState.$Properties=} [properties] Properties to set
         * @returns {game.OthelloState} OthelloState instance
         * @type {{
         *   (properties: game.OthelloState.$Shape): game.OthelloState & game.OthelloState.$Shape;
         *   (properties?: game.OthelloState.$Properties): game.OthelloState;
         * }}
         */
        OthelloState.create = function(properties) {
            return new OthelloState(properties);
        };

        /**
         * Encodes the specified OthelloState message. Does not implicitly {@link game.OthelloState.verify|verify} messages.
         * @function encode
         * @memberof game.OthelloState
         * @static
         * @param {game.OthelloState.$Properties} message OthelloState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OthelloState.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.board != null && message.board.length)
                for (let i = 0; i < message.board.length; ++i)
                    $root.game.BoardRow.encode(message.board[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn") && message.turn !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.turn);
            if (message.blackSeat != null && $Object.hasOwnProperty.call(message, "blackSeat") && message.blackSeat !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.blackSeat);
            if (message.legalMoves != null && message.legalMoves.length)
                for (let i = 0; i < message.legalMoves.length; ++i)
                    $root.game.Pos.encode(message.legalMoves[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.passCount != null && $Object.hasOwnProperty.call(message, "passCount") && message.passCount !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.passCount);
            if (message.blackCount != null && $Object.hasOwnProperty.call(message, "blackCount") && message.blackCount !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.blackCount);
            if (message.whiteCount != null && $Object.hasOwnProperty.call(message, "whiteCount") && message.whiteCount !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.whiteCount);
            if (message.rankedDelta != null && message.rankedDelta.length)
                for (let i = 0; i < message.rankedDelta.length; ++i)
                    $root.game.IntPair.encode(message.rankedDelta[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.settlementEvents != null && message.settlementEvents.length)
                for (let i = 0; i < message.settlementEvents.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.settlementEvents[i]);
            if (message.pendingSettlement != null && $Object.hasOwnProperty.call(message, "pendingSettlement"))
                $root.game.OthelloPendingSettlement.encode(message.pendingSettlement, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.surrenderRequest != null && $Object.hasOwnProperty.call(message, "surrenderRequest"))
                $root.game.OthelloSurrenderRequest.encode(message.surrenderRequest, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended") && message.ended !== false)
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.ended);
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner") && message.winner !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.winner);
            if (message.moveDeadlineAt != null && $Object.hasOwnProperty.call(message, "moveDeadlineAt") && (typeof message.moveDeadlineAt === "object" ? message.moveDeadlineAt.low || message.moveDeadlineAt.high : message.moveDeadlineAt !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.moveDeadlineAt);
            if (message.clockDeadlineAt != null && $Object.hasOwnProperty.call(message, "clockDeadlineAt") && (typeof message.clockDeadlineAt === "object" ? message.clockDeadlineAt.low || message.clockDeadlineAt.high : message.clockDeadlineAt !== 0))
                writer.uint32(/* id 15, wireType 0 =*/120).int64(message.clockDeadlineAt);
            if (message.clockRemaining != null && message.clockRemaining.length)
                for (let i = 0; i < message.clockRemaining.length; ++i)
                    $root.game.IntPair.encode(message.clockRemaining[i], writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OthelloState message, length delimited. Does not implicitly {@link game.OthelloState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.OthelloState
         * @static
         * @param {game.OthelloState.$Properties} message OthelloState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OthelloState.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an OthelloState message from the specified reader or buffer.
         * @function decode
         * @memberof game.OthelloState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.OthelloState & game.OthelloState.$Shape} OthelloState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OthelloState.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.OthelloState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.board && message.board.length))
                            message.board = [];
                        message.board.push($root.game.BoardRow.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.turn = value;
                        else
                            delete message.turn;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.blackSeat = value;
                        else
                            delete message.blackSeat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.legalMoves && message.legalMoves.length))
                            message.legalMoves = [];
                        message.legalMoves.push($root.game.Pos.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.passCount = value;
                        else
                            delete message.passCount;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.blackCount = value;
                        else
                            delete message.blackCount;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.whiteCount = value;
                        else
                            delete message.whiteCount;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rankedDelta && message.rankedDelta.length))
                            message.rankedDelta = [];
                        message.rankedDelta.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.settlementEvents && message.settlementEvents.length))
                            message.settlementEvents = [];
                        message.settlementEvents.push(reader.stringVerify());
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.pendingSettlement = $root.game.OthelloPendingSettlement.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pendingSettlement);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.surrenderRequest = $root.game.OthelloSurrenderRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.surrenderRequest);
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ended = value;
                        else
                            delete message.ended;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.winner = value;
                        else
                            delete message.winner;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.moveDeadlineAt = value;
                        else
                            delete message.moveDeadlineAt;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.clockDeadlineAt = value;
                        else
                            delete message.clockDeadlineAt;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.clockRemaining && message.clockRemaining.length))
                            message.clockRemaining = [];
                        message.clockRemaining.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OthelloState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.OthelloState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.OthelloState & game.OthelloState.$Shape} OthelloState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OthelloState.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OthelloState message.
         * @function verify
         * @memberof game.OthelloState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OthelloState.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.board != null && $Object.hasOwnProperty.call(message, "board")) {
                if (!$Array.isArray(message.board))
                    return "board: array expected";
                for (let i = 0; i < message.board.length; ++i) {
                    let error = $root.game.BoardRow.verify(message.board[i], _depth + 1);
                    if (error)
                        return "board." + error;
                }
            }
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn"))
                if (!$util.isString(message.turn))
                    return "turn: string expected";
            if (message.blackSeat != null && $Object.hasOwnProperty.call(message, "blackSeat"))
                if (!$util.isString(message.blackSeat))
                    return "blackSeat: string expected";
            if (message.legalMoves != null && $Object.hasOwnProperty.call(message, "legalMoves")) {
                if (!$Array.isArray(message.legalMoves))
                    return "legalMoves: array expected";
                for (let i = 0; i < message.legalMoves.length; ++i) {
                    let error = $root.game.Pos.verify(message.legalMoves[i], _depth + 1);
                    if (error)
                        return "legalMoves." + error;
                }
            }
            if (message.passCount != null && $Object.hasOwnProperty.call(message, "passCount"))
                if (!$util.isInteger(message.passCount))
                    return "passCount: integer expected";
            if (message.blackCount != null && $Object.hasOwnProperty.call(message, "blackCount"))
                if (!$util.isInteger(message.blackCount))
                    return "blackCount: integer expected";
            if (message.whiteCount != null && $Object.hasOwnProperty.call(message, "whiteCount"))
                if (!$util.isInteger(message.whiteCount))
                    return "whiteCount: integer expected";
            if (message.rankedDelta != null && $Object.hasOwnProperty.call(message, "rankedDelta")) {
                if (!$Array.isArray(message.rankedDelta))
                    return "rankedDelta: array expected";
                for (let i = 0; i < message.rankedDelta.length; ++i) {
                    let error = $root.game.IntPair.verify(message.rankedDelta[i], _depth + 1);
                    if (error)
                        return "rankedDelta." + error;
                }
            }
            if (message.settlementEvents != null && $Object.hasOwnProperty.call(message, "settlementEvents")) {
                if (!$Array.isArray(message.settlementEvents))
                    return "settlementEvents: array expected";
                for (let i = 0; i < message.settlementEvents.length; ++i)
                    if (!$util.isString(message.settlementEvents[i]))
                        return "settlementEvents: string[] expected";
            }
            if (message.pendingSettlement != null && $Object.hasOwnProperty.call(message, "pendingSettlement")) {
                let error = $root.game.OthelloPendingSettlement.verify(message.pendingSettlement, _depth + 1);
                if (error)
                    return "pendingSettlement." + error;
            }
            if (message.surrenderRequest != null && $Object.hasOwnProperty.call(message, "surrenderRequest")) {
                let error = $root.game.OthelloSurrenderRequest.verify(message.surrenderRequest, _depth + 1);
                if (error)
                    return "surrenderRequest." + error;
            }
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended"))
                if (typeof message.ended !== "boolean")
                    return "ended: boolean expected";
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner"))
                if (!$util.isString(message.winner))
                    return "winner: string expected";
            if (message.moveDeadlineAt != null && $Object.hasOwnProperty.call(message, "moveDeadlineAt"))
                if (!$util.isInteger(message.moveDeadlineAt) && !(message.moveDeadlineAt && $util.isInteger(message.moveDeadlineAt.low) && $util.isInteger(message.moveDeadlineAt.high)))
                    return "moveDeadlineAt: integer|Long expected";
            if (message.clockDeadlineAt != null && $Object.hasOwnProperty.call(message, "clockDeadlineAt"))
                if (!$util.isInteger(message.clockDeadlineAt) && !(message.clockDeadlineAt && $util.isInteger(message.clockDeadlineAt.low) && $util.isInteger(message.clockDeadlineAt.high)))
                    return "clockDeadlineAt: integer|Long expected";
            if (message.clockRemaining != null && $Object.hasOwnProperty.call(message, "clockRemaining")) {
                if (!$Array.isArray(message.clockRemaining))
                    return "clockRemaining: array expected";
                for (let i = 0; i < message.clockRemaining.length; ++i) {
                    let error = $root.game.IntPair.verify(message.clockRemaining[i], _depth + 1);
                    if (error)
                        return "clockRemaining." + error;
                }
            }
            return null;
        };

        /**
         * Creates an OthelloState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.OthelloState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.OthelloState} OthelloState
         */
        OthelloState.fromObject = function (object, _depth) {
            if (object instanceof $root.game.OthelloState)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.OthelloState: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.OthelloState();
            if (object.board) {
                if (!$Array.isArray(object.board))
                    throw $TypeError(".game.OthelloState.board: array expected");
                message.board = $Array(object.board.length);
                for (let i = 0; i < object.board.length; ++i) {
                    if (!$util.isObject(object.board[i]))
                        throw $TypeError(".game.OthelloState.board: object expected");
                    message.board[i] = $root.game.BoardRow.fromObject(object.board[i], _depth + 1);
                }
            }
            if (object.turn != null)
                if (typeof object.turn !== "string" || object.turn.length)
                    message.turn = $String(object.turn);
            if (object.blackSeat != null)
                if (typeof object.blackSeat !== "string" || object.blackSeat.length)
                    message.blackSeat = $String(object.blackSeat);
            if (object.legalMoves) {
                if (!$Array.isArray(object.legalMoves))
                    throw $TypeError(".game.OthelloState.legalMoves: array expected");
                message.legalMoves = $Array(object.legalMoves.length);
                for (let i = 0; i < object.legalMoves.length; ++i) {
                    if (!$util.isObject(object.legalMoves[i]))
                        throw $TypeError(".game.OthelloState.legalMoves: object expected");
                    message.legalMoves[i] = $root.game.Pos.fromObject(object.legalMoves[i], _depth + 1);
                }
            }
            if (object.passCount != null)
                if ($Number(object.passCount) !== 0)
                    message.passCount = object.passCount | 0;
            if (object.blackCount != null)
                if ($Number(object.blackCount) !== 0)
                    message.blackCount = object.blackCount | 0;
            if (object.whiteCount != null)
                if ($Number(object.whiteCount) !== 0)
                    message.whiteCount = object.whiteCount | 0;
            if (object.rankedDelta) {
                if (!$Array.isArray(object.rankedDelta))
                    throw $TypeError(".game.OthelloState.rankedDelta: array expected");
                message.rankedDelta = $Array(object.rankedDelta.length);
                for (let i = 0; i < object.rankedDelta.length; ++i) {
                    if (!$util.isObject(object.rankedDelta[i]))
                        throw $TypeError(".game.OthelloState.rankedDelta: object expected");
                    message.rankedDelta[i] = $root.game.IntPair.fromObject(object.rankedDelta[i], _depth + 1);
                }
            }
            if (object.settlementEvents) {
                if (!$Array.isArray(object.settlementEvents))
                    throw $TypeError(".game.OthelloState.settlementEvents: array expected");
                message.settlementEvents = $Array(object.settlementEvents.length);
                for (let i = 0; i < object.settlementEvents.length; ++i)
                    message.settlementEvents[i] = $String(object.settlementEvents[i]);
            }
            if (object.pendingSettlement != null) {
                if (!$util.isObject(object.pendingSettlement))
                    throw $TypeError(".game.OthelloState.pendingSettlement: object expected");
                message.pendingSettlement = $root.game.OthelloPendingSettlement.fromObject(object.pendingSettlement, _depth + 1);
            }
            if (object.surrenderRequest != null) {
                if (!$util.isObject(object.surrenderRequest))
                    throw $TypeError(".game.OthelloState.surrenderRequest: object expected");
                message.surrenderRequest = $root.game.OthelloSurrenderRequest.fromObject(object.surrenderRequest, _depth + 1);
            }
            if (object.ended != null)
                if (object.ended)
                    message.ended = $Boolean(object.ended);
            if (object.winner != null)
                if (typeof object.winner !== "string" || object.winner.length)
                    message.winner = $String(object.winner);
            if (object.moveDeadlineAt != null)
                if (typeof object.moveDeadlineAt === "object" ? object.moveDeadlineAt.low || object.moveDeadlineAt.high : $Number(object.moveDeadlineAt) !== 0)
                    if ($util.Long)
                        message.moveDeadlineAt = $util.Long.fromValue(object.moveDeadlineAt, false);
                    else if (typeof object.moveDeadlineAt === "string")
                        message.moveDeadlineAt = $parseInt(object.moveDeadlineAt, 10);
                    else if (typeof object.moveDeadlineAt === "number")
                        message.moveDeadlineAt = object.moveDeadlineAt;
                    else if (typeof object.moveDeadlineAt === "object")
                        message.moveDeadlineAt = new $util.LongBits(object.moveDeadlineAt.low >>> 0, object.moveDeadlineAt.high >>> 0).toNumber();
            if (object.clockDeadlineAt != null)
                if (typeof object.clockDeadlineAt === "object" ? object.clockDeadlineAt.low || object.clockDeadlineAt.high : $Number(object.clockDeadlineAt) !== 0)
                    if ($util.Long)
                        message.clockDeadlineAt = $util.Long.fromValue(object.clockDeadlineAt, false);
                    else if (typeof object.clockDeadlineAt === "string")
                        message.clockDeadlineAt = $parseInt(object.clockDeadlineAt, 10);
                    else if (typeof object.clockDeadlineAt === "number")
                        message.clockDeadlineAt = object.clockDeadlineAt;
                    else if (typeof object.clockDeadlineAt === "object")
                        message.clockDeadlineAt = new $util.LongBits(object.clockDeadlineAt.low >>> 0, object.clockDeadlineAt.high >>> 0).toNumber();
            if (object.clockRemaining) {
                if (!$Array.isArray(object.clockRemaining))
                    throw $TypeError(".game.OthelloState.clockRemaining: array expected");
                message.clockRemaining = $Array(object.clockRemaining.length);
                for (let i = 0; i < object.clockRemaining.length; ++i) {
                    if (!$util.isObject(object.clockRemaining[i]))
                        throw $TypeError(".game.OthelloState.clockRemaining: object expected");
                    message.clockRemaining[i] = $root.game.IntPair.fromObject(object.clockRemaining[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an OthelloState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.OthelloState
         * @static
         * @param {game.OthelloState} message OthelloState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OthelloState.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.board = [];
                object.legalMoves = [];
                object.rankedDelta = [];
                object.settlementEvents = [];
                object.clockRemaining = [];
            }
            if (options.defaults) {
                object.turn = "";
                object.blackSeat = "";
                object.passCount = 0;
                object.blackCount = 0;
                object.whiteCount = 0;
                object.pendingSettlement = null;
                object.surrenderRequest = null;
                object.ended = false;
                object.winner = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.moveDeadlineAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.moveDeadlineAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.clockDeadlineAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.clockDeadlineAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.board && message.board.length) {
                object.board = $Array(message.board.length);
                for (let j = 0; j < message.board.length; ++j)
                    object.board[j] = $root.game.BoardRow.toObject(message.board[j], options, _depth + 1);
            }
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn"))
                object.turn = message.turn;
            if (message.blackSeat != null && $Object.hasOwnProperty.call(message, "blackSeat"))
                object.blackSeat = message.blackSeat;
            if (message.legalMoves && message.legalMoves.length) {
                object.legalMoves = $Array(message.legalMoves.length);
                for (let j = 0; j < message.legalMoves.length; ++j)
                    object.legalMoves[j] = $root.game.Pos.toObject(message.legalMoves[j], options, _depth + 1);
            }
            if (message.passCount != null && $Object.hasOwnProperty.call(message, "passCount"))
                object.passCount = message.passCount;
            if (message.blackCount != null && $Object.hasOwnProperty.call(message, "blackCount"))
                object.blackCount = message.blackCount;
            if (message.whiteCount != null && $Object.hasOwnProperty.call(message, "whiteCount"))
                object.whiteCount = message.whiteCount;
            if (message.rankedDelta && message.rankedDelta.length) {
                object.rankedDelta = $Array(message.rankedDelta.length);
                for (let j = 0; j < message.rankedDelta.length; ++j)
                    object.rankedDelta[j] = $root.game.IntPair.toObject(message.rankedDelta[j], options, _depth + 1);
            }
            if (message.settlementEvents && message.settlementEvents.length) {
                object.settlementEvents = $Array(message.settlementEvents.length);
                for (let j = 0; j < message.settlementEvents.length; ++j)
                    object.settlementEvents[j] = message.settlementEvents[j];
            }
            if (message.pendingSettlement != null && $Object.hasOwnProperty.call(message, "pendingSettlement"))
                object.pendingSettlement = $root.game.OthelloPendingSettlement.toObject(message.pendingSettlement, options, _depth + 1);
            if (message.surrenderRequest != null && $Object.hasOwnProperty.call(message, "surrenderRequest"))
                object.surrenderRequest = $root.game.OthelloSurrenderRequest.toObject(message.surrenderRequest, options, _depth + 1);
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended"))
                object.ended = message.ended;
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner"))
                object.winner = message.winner;
            if (message.moveDeadlineAt != null && $Object.hasOwnProperty.call(message, "moveDeadlineAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.moveDeadlineAt = typeof message.moveDeadlineAt === "number" ? $BigInt(message.moveDeadlineAt) : $util.Long.fromBits(message.moveDeadlineAt.low >>> 0, message.moveDeadlineAt.high >>> 0, false).toBigInt();
                else if (typeof message.moveDeadlineAt === "number")
                    object.moveDeadlineAt = options.longs === $String ? $String(message.moveDeadlineAt) : message.moveDeadlineAt;
                else
                    object.moveDeadlineAt = options.longs === $String ? $util.Long.prototype.toString.call(message.moveDeadlineAt) : options.longs === $Number ? new $util.LongBits(message.moveDeadlineAt.low >>> 0, message.moveDeadlineAt.high >>> 0).toNumber() : message.moveDeadlineAt;
            if (message.clockDeadlineAt != null && $Object.hasOwnProperty.call(message, "clockDeadlineAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.clockDeadlineAt = typeof message.clockDeadlineAt === "number" ? $BigInt(message.clockDeadlineAt) : $util.Long.fromBits(message.clockDeadlineAt.low >>> 0, message.clockDeadlineAt.high >>> 0, false).toBigInt();
                else if (typeof message.clockDeadlineAt === "number")
                    object.clockDeadlineAt = options.longs === $String ? $String(message.clockDeadlineAt) : message.clockDeadlineAt;
                else
                    object.clockDeadlineAt = options.longs === $String ? $util.Long.prototype.toString.call(message.clockDeadlineAt) : options.longs === $Number ? new $util.LongBits(message.clockDeadlineAt.low >>> 0, message.clockDeadlineAt.high >>> 0).toNumber() : message.clockDeadlineAt;
            if (message.clockRemaining && message.clockRemaining.length) {
                object.clockRemaining = $Array(message.clockRemaining.length);
                for (let j = 0; j < message.clockRemaining.length; ++j)
                    object.clockRemaining[j] = $root.game.IntPair.toObject(message.clockRemaining[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this OthelloState to JSON.
         * @function toJSON
         * @memberof game.OthelloState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OthelloState.prototype.toJSON = function() {
            return OthelloState.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OthelloState
         * @function getTypeUrl
         * @memberof game.OthelloState
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OthelloState.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.OthelloState";
        };

        return OthelloState;
    })();

    game.TicTacToeGiveawayPrompt = (function() {

        /**
         * Properties of a TicTacToeGiveawayPrompt.
         * @typedef {Object} game.TicTacToeGiveawayPrompt.$Properties
         * @property {string|null} [seat] TicTacToeGiveawayPrompt seat
         * @property {boolean|null} [forced] TicTacToeGiveawayPrompt forced
         * @property {number|Long|null} [startedAt] TicTacToeGiveawayPrompt startedAt
         * @property {number|Long|null} [expiresAt] TicTacToeGiveawayPrompt expiresAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TicTacToeGiveawayPrompt.
         * @memberof game
         * @interface ITicTacToeGiveawayPrompt
         * @augments game.TicTacToeGiveawayPrompt.$Properties
         * @deprecated Use game.TicTacToeGiveawayPrompt.$Properties instead.
         */

        /**
         * Shape of a TicTacToeGiveawayPrompt.
         * @typedef {game.TicTacToeGiveawayPrompt.$Properties} game.TicTacToeGiveawayPrompt.$Shape
         */

        /**
         * Constructs a new TicTacToeGiveawayPrompt.
         * @memberof game
         * @classdesc Represents a TicTacToeGiveawayPrompt.
         * @constructor
         * @param {game.TicTacToeGiveawayPrompt.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TicTacToeGiveawayPrompt = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TicTacToeGiveawayPrompt seat.
         * @member {string} seat
         * @memberof game.TicTacToeGiveawayPrompt
         * @instance
         */
        TicTacToeGiveawayPrompt.prototype.seat = "";

        /**
         * TicTacToeGiveawayPrompt forced.
         * @member {boolean} forced
         * @memberof game.TicTacToeGiveawayPrompt
         * @instance
         */
        TicTacToeGiveawayPrompt.prototype.forced = false;

        /**
         * TicTacToeGiveawayPrompt startedAt.
         * @member {number|Long} startedAt
         * @memberof game.TicTacToeGiveawayPrompt
         * @instance
         */
        TicTacToeGiveawayPrompt.prototype.startedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * TicTacToeGiveawayPrompt expiresAt.
         * @member {number|Long} expiresAt
         * @memberof game.TicTacToeGiveawayPrompt
         * @instance
         */
        TicTacToeGiveawayPrompt.prototype.expiresAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TicTacToeGiveawayPrompt instance using the specified properties.
         * @function create
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {game.TicTacToeGiveawayPrompt.$Properties=} [properties] Properties to set
         * @returns {game.TicTacToeGiveawayPrompt} TicTacToeGiveawayPrompt instance
         * @type {{
         *   (properties: game.TicTacToeGiveawayPrompt.$Shape): game.TicTacToeGiveawayPrompt & game.TicTacToeGiveawayPrompt.$Shape;
         *   (properties?: game.TicTacToeGiveawayPrompt.$Properties): game.TicTacToeGiveawayPrompt;
         * }}
         */
        TicTacToeGiveawayPrompt.create = function(properties) {
            return new TicTacToeGiveawayPrompt(properties);
        };

        /**
         * Encodes the specified TicTacToeGiveawayPrompt message. Does not implicitly {@link game.TicTacToeGiveawayPrompt.verify|verify} messages.
         * @function encode
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {game.TicTacToeGiveawayPrompt.$Properties} message TicTacToeGiveawayPrompt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TicTacToeGiveawayPrompt.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.seat != null && $Object.hasOwnProperty.call(message, "seat") && message.seat !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.seat);
            if (message.forced != null && $Object.hasOwnProperty.call(message, "forced") && message.forced !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.forced);
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt") && (typeof message.startedAt === "object" ? message.startedAt.low || message.startedAt.high : message.startedAt !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.startedAt);
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt") && (typeof message.expiresAt === "object" ? message.expiresAt.low || message.expiresAt.high : message.expiresAt !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.expiresAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TicTacToeGiveawayPrompt message, length delimited. Does not implicitly {@link game.TicTacToeGiveawayPrompt.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {game.TicTacToeGiveawayPrompt.$Properties} message TicTacToeGiveawayPrompt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TicTacToeGiveawayPrompt.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TicTacToeGiveawayPrompt message from the specified reader or buffer.
         * @function decode
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.TicTacToeGiveawayPrompt & game.TicTacToeGiveawayPrompt.$Shape} TicTacToeGiveawayPrompt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TicTacToeGiveawayPrompt.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.TicTacToeGiveawayPrompt(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.forced = value;
                        else
                            delete message.forced;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.startedAt = value;
                        else
                            delete message.startedAt;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expiresAt = value;
                        else
                            delete message.expiresAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TicTacToeGiveawayPrompt message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.TicTacToeGiveawayPrompt & game.TicTacToeGiveawayPrompt.$Shape} TicTacToeGiveawayPrompt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TicTacToeGiveawayPrompt.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TicTacToeGiveawayPrompt message.
         * @function verify
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TicTacToeGiveawayPrompt.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && $Object.hasOwnProperty.call(message, "seat"))
                if (!$util.isString(message.seat))
                    return "seat: string expected";
            if (message.forced != null && $Object.hasOwnProperty.call(message, "forced"))
                if (typeof message.forced !== "boolean")
                    return "forced: boolean expected";
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt"))
                if (!$util.isInteger(message.startedAt) && !(message.startedAt && $util.isInteger(message.startedAt.low) && $util.isInteger(message.startedAt.high)))
                    return "startedAt: integer|Long expected";
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (!$util.isInteger(message.expiresAt) && !(message.expiresAt && $util.isInteger(message.expiresAt.low) && $util.isInteger(message.expiresAt.high)))
                    return "expiresAt: integer|Long expected";
            return null;
        };

        /**
         * Creates a TicTacToeGiveawayPrompt message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.TicTacToeGiveawayPrompt} TicTacToeGiveawayPrompt
         */
        TicTacToeGiveawayPrompt.fromObject = function (object, _depth) {
            if (object instanceof $root.game.TicTacToeGiveawayPrompt)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.TicTacToeGiveawayPrompt: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.TicTacToeGiveawayPrompt();
            if (object.seat != null)
                if (typeof object.seat !== "string" || object.seat.length)
                    message.seat = $String(object.seat);
            if (object.forced != null)
                if (object.forced)
                    message.forced = $Boolean(object.forced);
            if (object.startedAt != null)
                if (typeof object.startedAt === "object" ? object.startedAt.low || object.startedAt.high : $Number(object.startedAt) !== 0)
                    if ($util.Long)
                        message.startedAt = $util.Long.fromValue(object.startedAt, false);
                    else if (typeof object.startedAt === "string")
                        message.startedAt = $parseInt(object.startedAt, 10);
                    else if (typeof object.startedAt === "number")
                        message.startedAt = object.startedAt;
                    else if (typeof object.startedAt === "object")
                        message.startedAt = new $util.LongBits(object.startedAt.low >>> 0, object.startedAt.high >>> 0).toNumber();
            if (object.expiresAt != null)
                if (typeof object.expiresAt === "object" ? object.expiresAt.low || object.expiresAt.high : $Number(object.expiresAt) !== 0)
                    if ($util.Long)
                        message.expiresAt = $util.Long.fromValue(object.expiresAt, false);
                    else if (typeof object.expiresAt === "string")
                        message.expiresAt = $parseInt(object.expiresAt, 10);
                    else if (typeof object.expiresAt === "number")
                        message.expiresAt = object.expiresAt;
                    else if (typeof object.expiresAt === "object")
                        message.expiresAt = new $util.LongBits(object.expiresAt.low >>> 0, object.expiresAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TicTacToeGiveawayPrompt message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {game.TicTacToeGiveawayPrompt} message TicTacToeGiveawayPrompt
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TicTacToeGiveawayPrompt.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.seat = "";
                object.forced = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.startedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.startedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiresAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expiresAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.seat != null && $Object.hasOwnProperty.call(message, "seat"))
                object.seat = message.seat;
            if (message.forced != null && $Object.hasOwnProperty.call(message, "forced"))
                object.forced = message.forced;
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.startedAt = typeof message.startedAt === "number" ? $BigInt(message.startedAt) : $util.Long.fromBits(message.startedAt.low >>> 0, message.startedAt.high >>> 0, false).toBigInt();
                else if (typeof message.startedAt === "number")
                    object.startedAt = options.longs === $String ? $String(message.startedAt) : message.startedAt;
                else
                    object.startedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.startedAt) : options.longs === $Number ? new $util.LongBits(message.startedAt.low >>> 0, message.startedAt.high >>> 0).toNumber() : message.startedAt;
            if (message.expiresAt != null && $Object.hasOwnProperty.call(message, "expiresAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expiresAt = typeof message.expiresAt === "number" ? $BigInt(message.expiresAt) : $util.Long.fromBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0, false).toBigInt();
                else if (typeof message.expiresAt === "number")
                    object.expiresAt = options.longs === $String ? $String(message.expiresAt) : message.expiresAt;
                else
                    object.expiresAt = options.longs === $String ? $util.Long.prototype.toString.call(message.expiresAt) : options.longs === $Number ? new $util.LongBits(message.expiresAt.low >>> 0, message.expiresAt.high >>> 0).toNumber() : message.expiresAt;
            return object;
        };

        /**
         * Converts this TicTacToeGiveawayPrompt to JSON.
         * @function toJSON
         * @memberof game.TicTacToeGiveawayPrompt
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TicTacToeGiveawayPrompt.prototype.toJSON = function() {
            return TicTacToeGiveawayPrompt.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TicTacToeGiveawayPrompt
         * @function getTypeUrl
         * @memberof game.TicTacToeGiveawayPrompt
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TicTacToeGiveawayPrompt.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.TicTacToeGiveawayPrompt";
        };

        return TicTacToeGiveawayPrompt;
    })();

    game.TicTacToeState = (function() {

        /**
         * Properties of a TicTacToeState.
         * @typedef {Object} game.TicTacToeState.$Properties
         * @property {Array.<game.BoardRow.$Properties>|null} [board] TicTacToeState board
         * @property {string|null} [turn] TicTacToeState turn
         * @property {string|null} [xSeat] TicTacToeState xSeat
         * @property {number|null} [moveCount] TicTacToeState moveCount
         * @property {game.TicTacToeGiveawayPrompt.$Properties|null} [giveawayPrompt] TicTacToeState giveawayPrompt
         * @property {Array.<game.Pos.$Properties>|null} [winningLine] TicTacToeState winningLine
         * @property {Array.<game.IntPair.$Properties>|null} [rankedDelta] TicTacToeState rankedDelta
         * @property {boolean|null} [ended] TicTacToeState ended
         * @property {string|null} [winner] TicTacToeState winner
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TicTacToeState.
         * @memberof game
         * @interface ITicTacToeState
         * @augments game.TicTacToeState.$Properties
         * @deprecated Use game.TicTacToeState.$Properties instead.
         */

        /**
         * Shape of a TicTacToeState.
         * @typedef {game.TicTacToeState.$Properties} game.TicTacToeState.$Shape
         */

        /**
         * Constructs a new TicTacToeState.
         * @memberof game
         * @classdesc Represents a TicTacToeState.
         * @constructor
         * @param {game.TicTacToeState.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TicTacToeState = function (properties) {
            this.board = [];
            this.winningLine = [];
            this.rankedDelta = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TicTacToeState board.
         * @member {Array.<game.BoardRow.$Properties>} board
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.board = $util.emptyArray;

        /**
         * TicTacToeState turn.
         * @member {string} turn
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.turn = "";

        /**
         * TicTacToeState xSeat.
         * @member {string} xSeat
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.xSeat = "";

        /**
         * TicTacToeState moveCount.
         * @member {number} moveCount
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.moveCount = 0;

        /**
         * TicTacToeState giveawayPrompt.
         * @member {game.TicTacToeGiveawayPrompt.$Properties|null|undefined} giveawayPrompt
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.giveawayPrompt = null;

        /**
         * TicTacToeState winningLine.
         * @member {Array.<game.Pos.$Properties>} winningLine
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.winningLine = $util.emptyArray;

        /**
         * TicTacToeState rankedDelta.
         * @member {Array.<game.IntPair.$Properties>} rankedDelta
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.rankedDelta = $util.emptyArray;

        /**
         * TicTacToeState ended.
         * @member {boolean} ended
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.ended = false;

        /**
         * TicTacToeState winner.
         * @member {string} winner
         * @memberof game.TicTacToeState
         * @instance
         */
        TicTacToeState.prototype.winner = "";

        /**
         * Creates a new TicTacToeState instance using the specified properties.
         * @function create
         * @memberof game.TicTacToeState
         * @static
         * @param {game.TicTacToeState.$Properties=} [properties] Properties to set
         * @returns {game.TicTacToeState} TicTacToeState instance
         * @type {{
         *   (properties: game.TicTacToeState.$Shape): game.TicTacToeState & game.TicTacToeState.$Shape;
         *   (properties?: game.TicTacToeState.$Properties): game.TicTacToeState;
         * }}
         */
        TicTacToeState.create = function(properties) {
            return new TicTacToeState(properties);
        };

        /**
         * Encodes the specified TicTacToeState message. Does not implicitly {@link game.TicTacToeState.verify|verify} messages.
         * @function encode
         * @memberof game.TicTacToeState
         * @static
         * @param {game.TicTacToeState.$Properties} message TicTacToeState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TicTacToeState.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.board != null && message.board.length)
                for (let i = 0; i < message.board.length; ++i)
                    $root.game.BoardRow.encode(message.board[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn") && message.turn !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.turn);
            if (message.xSeat != null && $Object.hasOwnProperty.call(message, "xSeat") && message.xSeat !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.xSeat);
            if (message.moveCount != null && $Object.hasOwnProperty.call(message, "moveCount") && message.moveCount !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.moveCount);
            if (message.giveawayPrompt != null && $Object.hasOwnProperty.call(message, "giveawayPrompt"))
                $root.game.TicTacToeGiveawayPrompt.encode(message.giveawayPrompt, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.winningLine != null && message.winningLine.length)
                for (let i = 0; i < message.winningLine.length; ++i)
                    $root.game.Pos.encode(message.winningLine[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.rankedDelta != null && message.rankedDelta.length)
                for (let i = 0; i < message.rankedDelta.length; ++i)
                    $root.game.IntPair.encode(message.rankedDelta[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended") && message.ended !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.ended);
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner") && message.winner !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.winner);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TicTacToeState message, length delimited. Does not implicitly {@link game.TicTacToeState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.TicTacToeState
         * @static
         * @param {game.TicTacToeState.$Properties} message TicTacToeState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TicTacToeState.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TicTacToeState message from the specified reader or buffer.
         * @function decode
         * @memberof game.TicTacToeState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.TicTacToeState & game.TicTacToeState.$Shape} TicTacToeState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TicTacToeState.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.TicTacToeState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.board && message.board.length))
                            message.board = [];
                        message.board.push($root.game.BoardRow.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.turn = value;
                        else
                            delete message.turn;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.xSeat = value;
                        else
                            delete message.xSeat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.moveCount = value;
                        else
                            delete message.moveCount;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.giveawayPrompt = $root.game.TicTacToeGiveawayPrompt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.giveawayPrompt);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.winningLine && message.winningLine.length))
                            message.winningLine = [];
                        message.winningLine.push($root.game.Pos.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rankedDelta && message.rankedDelta.length))
                            message.rankedDelta = [];
                        message.rankedDelta.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ended = value;
                        else
                            delete message.ended;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.winner = value;
                        else
                            delete message.winner;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TicTacToeState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.TicTacToeState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.TicTacToeState & game.TicTacToeState.$Shape} TicTacToeState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TicTacToeState.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TicTacToeState message.
         * @function verify
         * @memberof game.TicTacToeState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TicTacToeState.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.board != null && $Object.hasOwnProperty.call(message, "board")) {
                if (!$Array.isArray(message.board))
                    return "board: array expected";
                for (let i = 0; i < message.board.length; ++i) {
                    let error = $root.game.BoardRow.verify(message.board[i], _depth + 1);
                    if (error)
                        return "board." + error;
                }
            }
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn"))
                if (!$util.isString(message.turn))
                    return "turn: string expected";
            if (message.xSeat != null && $Object.hasOwnProperty.call(message, "xSeat"))
                if (!$util.isString(message.xSeat))
                    return "xSeat: string expected";
            if (message.moveCount != null && $Object.hasOwnProperty.call(message, "moveCount"))
                if (!$util.isInteger(message.moveCount))
                    return "moveCount: integer expected";
            if (message.giveawayPrompt != null && $Object.hasOwnProperty.call(message, "giveawayPrompt")) {
                let error = $root.game.TicTacToeGiveawayPrompt.verify(message.giveawayPrompt, _depth + 1);
                if (error)
                    return "giveawayPrompt." + error;
            }
            if (message.winningLine != null && $Object.hasOwnProperty.call(message, "winningLine")) {
                if (!$Array.isArray(message.winningLine))
                    return "winningLine: array expected";
                for (let i = 0; i < message.winningLine.length; ++i) {
                    let error = $root.game.Pos.verify(message.winningLine[i], _depth + 1);
                    if (error)
                        return "winningLine." + error;
                }
            }
            if (message.rankedDelta != null && $Object.hasOwnProperty.call(message, "rankedDelta")) {
                if (!$Array.isArray(message.rankedDelta))
                    return "rankedDelta: array expected";
                for (let i = 0; i < message.rankedDelta.length; ++i) {
                    let error = $root.game.IntPair.verify(message.rankedDelta[i], _depth + 1);
                    if (error)
                        return "rankedDelta." + error;
                }
            }
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended"))
                if (typeof message.ended !== "boolean")
                    return "ended: boolean expected";
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner"))
                if (!$util.isString(message.winner))
                    return "winner: string expected";
            return null;
        };

        /**
         * Creates a TicTacToeState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.TicTacToeState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.TicTacToeState} TicTacToeState
         */
        TicTacToeState.fromObject = function (object, _depth) {
            if (object instanceof $root.game.TicTacToeState)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.TicTacToeState: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.TicTacToeState();
            if (object.board) {
                if (!$Array.isArray(object.board))
                    throw $TypeError(".game.TicTacToeState.board: array expected");
                message.board = $Array(object.board.length);
                for (let i = 0; i < object.board.length; ++i) {
                    if (!$util.isObject(object.board[i]))
                        throw $TypeError(".game.TicTacToeState.board: object expected");
                    message.board[i] = $root.game.BoardRow.fromObject(object.board[i], _depth + 1);
                }
            }
            if (object.turn != null)
                if (typeof object.turn !== "string" || object.turn.length)
                    message.turn = $String(object.turn);
            if (object.xSeat != null)
                if (typeof object.xSeat !== "string" || object.xSeat.length)
                    message.xSeat = $String(object.xSeat);
            if (object.moveCount != null)
                if ($Number(object.moveCount) !== 0)
                    message.moveCount = object.moveCount | 0;
            if (object.giveawayPrompt != null) {
                if (!$util.isObject(object.giveawayPrompt))
                    throw $TypeError(".game.TicTacToeState.giveawayPrompt: object expected");
                message.giveawayPrompt = $root.game.TicTacToeGiveawayPrompt.fromObject(object.giveawayPrompt, _depth + 1);
            }
            if (object.winningLine) {
                if (!$Array.isArray(object.winningLine))
                    throw $TypeError(".game.TicTacToeState.winningLine: array expected");
                message.winningLine = $Array(object.winningLine.length);
                for (let i = 0; i < object.winningLine.length; ++i) {
                    if (!$util.isObject(object.winningLine[i]))
                        throw $TypeError(".game.TicTacToeState.winningLine: object expected");
                    message.winningLine[i] = $root.game.Pos.fromObject(object.winningLine[i], _depth + 1);
                }
            }
            if (object.rankedDelta) {
                if (!$Array.isArray(object.rankedDelta))
                    throw $TypeError(".game.TicTacToeState.rankedDelta: array expected");
                message.rankedDelta = $Array(object.rankedDelta.length);
                for (let i = 0; i < object.rankedDelta.length; ++i) {
                    if (!$util.isObject(object.rankedDelta[i]))
                        throw $TypeError(".game.TicTacToeState.rankedDelta: object expected");
                    message.rankedDelta[i] = $root.game.IntPair.fromObject(object.rankedDelta[i], _depth + 1);
                }
            }
            if (object.ended != null)
                if (object.ended)
                    message.ended = $Boolean(object.ended);
            if (object.winner != null)
                if (typeof object.winner !== "string" || object.winner.length)
                    message.winner = $String(object.winner);
            return message;
        };

        /**
         * Creates a plain object from a TicTacToeState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.TicTacToeState
         * @static
         * @param {game.TicTacToeState} message TicTacToeState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TicTacToeState.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.board = [];
                object.winningLine = [];
                object.rankedDelta = [];
            }
            if (options.defaults) {
                object.turn = "";
                object.xSeat = "";
                object.moveCount = 0;
                object.giveawayPrompt = null;
                object.ended = false;
                object.winner = "";
            }
            if (message.board && message.board.length) {
                object.board = $Array(message.board.length);
                for (let j = 0; j < message.board.length; ++j)
                    object.board[j] = $root.game.BoardRow.toObject(message.board[j], options, _depth + 1);
            }
            if (message.turn != null && $Object.hasOwnProperty.call(message, "turn"))
                object.turn = message.turn;
            if (message.xSeat != null && $Object.hasOwnProperty.call(message, "xSeat"))
                object.xSeat = message.xSeat;
            if (message.moveCount != null && $Object.hasOwnProperty.call(message, "moveCount"))
                object.moveCount = message.moveCount;
            if (message.giveawayPrompt != null && $Object.hasOwnProperty.call(message, "giveawayPrompt"))
                object.giveawayPrompt = $root.game.TicTacToeGiveawayPrompt.toObject(message.giveawayPrompt, options, _depth + 1);
            if (message.winningLine && message.winningLine.length) {
                object.winningLine = $Array(message.winningLine.length);
                for (let j = 0; j < message.winningLine.length; ++j)
                    object.winningLine[j] = $root.game.Pos.toObject(message.winningLine[j], options, _depth + 1);
            }
            if (message.rankedDelta && message.rankedDelta.length) {
                object.rankedDelta = $Array(message.rankedDelta.length);
                for (let j = 0; j < message.rankedDelta.length; ++j)
                    object.rankedDelta[j] = $root.game.IntPair.toObject(message.rankedDelta[j], options, _depth + 1);
            }
            if (message.ended != null && $Object.hasOwnProperty.call(message, "ended"))
                object.ended = message.ended;
            if (message.winner != null && $Object.hasOwnProperty.call(message, "winner"))
                object.winner = message.winner;
            return object;
        };

        /**
         * Converts this TicTacToeState to JSON.
         * @function toJSON
         * @memberof game.TicTacToeState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TicTacToeState.prototype.toJSON = function() {
            return TicTacToeState.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TicTacToeState
         * @function getTypeUrl
         * @memberof game.TicTacToeState
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TicTacToeState.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.TicTacToeState";
        };

        return TicTacToeState;
    })();

    game.BoolPair = (function() {

        /**
         * Properties of a BoolPair.
         * @typedef {Object} game.BoolPair.$Properties
         * @property {string|null} [key] BoolPair key
         * @property {boolean|null} [value] BoolPair value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BoolPair.
         * @memberof game
         * @interface IBoolPair
         * @augments game.BoolPair.$Properties
         * @deprecated Use game.BoolPair.$Properties instead.
         */

        /**
         * Shape of a BoolPair.
         * @typedef {game.BoolPair.$Properties} game.BoolPair.$Shape
         */

        /**
         * Constructs a new BoolPair.
         * @memberof game
         * @classdesc Represents a BoolPair.
         * @constructor
         * @param {game.BoolPair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BoolPair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * BoolPair key.
         * @member {string} key
         * @memberof game.BoolPair
         * @instance
         */
        BoolPair.prototype.key = "";

        /**
         * BoolPair value.
         * @member {boolean} value
         * @memberof game.BoolPair
         * @instance
         */
        BoolPair.prototype.value = false;

        /**
         * Creates a new BoolPair instance using the specified properties.
         * @function create
         * @memberof game.BoolPair
         * @static
         * @param {game.BoolPair.$Properties=} [properties] Properties to set
         * @returns {game.BoolPair} BoolPair instance
         * @type {{
         *   (properties: game.BoolPair.$Shape): game.BoolPair & game.BoolPair.$Shape;
         *   (properties?: game.BoolPair.$Properties): game.BoolPair;
         * }}
         */
        BoolPair.create = function(properties) {
            return new BoolPair(properties);
        };

        /**
         * Encodes the specified BoolPair message. Does not implicitly {@link game.BoolPair.verify|verify} messages.
         * @function encode
         * @memberof game.BoolPair
         * @static
         * @param {game.BoolPair.$Properties} message BoolPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BoolPair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BoolPair message, length delimited. Does not implicitly {@link game.BoolPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.BoolPair
         * @static
         * @param {game.BoolPair.$Properties} message BoolPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BoolPair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a BoolPair message from the specified reader or buffer.
         * @function decode
         * @memberof game.BoolPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.BoolPair & game.BoolPair.$Shape} BoolPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BoolPair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.BoolPair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BoolPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.BoolPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.BoolPair & game.BoolPair.$Shape} BoolPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BoolPair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BoolPair message.
         * @function verify
         * @memberof game.BoolPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BoolPair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof message.value !== "boolean")
                    return "value: boolean expected";
            return null;
        };

        /**
         * Creates a BoolPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.BoolPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.BoolPair} BoolPair
         */
        BoolPair.fromObject = function (object, _depth) {
            if (object instanceof $root.game.BoolPair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.BoolPair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.BoolPair();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.value != null)
                if (object.value)
                    message.value = $Boolean(object.value);
            return message;
        };

        /**
         * Creates a plain object from a BoolPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.BoolPair
         * @static
         * @param {game.BoolPair} message BoolPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BoolPair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = false;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this BoolPair to JSON.
         * @function toJSON
         * @memberof game.BoolPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BoolPair.prototype.toJSON = function() {
            return BoolPair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BoolPair
         * @function getTypeUrl
         * @memberof game.BoolPair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BoolPair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.BoolPair";
        };

        return BoolPair;
    })();

    game.StringPair = (function() {

        /**
         * Properties of a StringPair.
         * @typedef {Object} game.StringPair.$Properties
         * @property {string|null} [key] StringPair key
         * @property {string|null} [value] StringPair value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a StringPair.
         * @memberof game
         * @interface IStringPair
         * @augments game.StringPair.$Properties
         * @deprecated Use game.StringPair.$Properties instead.
         */

        /**
         * Shape of a StringPair.
         * @typedef {game.StringPair.$Properties} game.StringPair.$Shape
         */

        /**
         * Constructs a new StringPair.
         * @memberof game
         * @classdesc Represents a StringPair.
         * @constructor
         * @param {game.StringPair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const StringPair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StringPair key.
         * @member {string} key
         * @memberof game.StringPair
         * @instance
         */
        StringPair.prototype.key = "";

        /**
         * StringPair value.
         * @member {string} value
         * @memberof game.StringPair
         * @instance
         */
        StringPair.prototype.value = "";

        /**
         * Creates a new StringPair instance using the specified properties.
         * @function create
         * @memberof game.StringPair
         * @static
         * @param {game.StringPair.$Properties=} [properties] Properties to set
         * @returns {game.StringPair} StringPair instance
         * @type {{
         *   (properties: game.StringPair.$Shape): game.StringPair & game.StringPair.$Shape;
         *   (properties?: game.StringPair.$Properties): game.StringPair;
         * }}
         */
        StringPair.create = function(properties) {
            return new StringPair(properties);
        };

        /**
         * Encodes the specified StringPair message. Does not implicitly {@link game.StringPair.verify|verify} messages.
         * @function encode
         * @memberof game.StringPair
         * @static
         * @param {game.StringPair.$Properties} message StringPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StringPair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StringPair message, length delimited. Does not implicitly {@link game.StringPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.StringPair
         * @static
         * @param {game.StringPair.$Properties} message StringPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StringPair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a StringPair message from the specified reader or buffer.
         * @function decode
         * @memberof game.StringPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.StringPair & game.StringPair.$Shape} StringPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StringPair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.StringPair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StringPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.StringPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.StringPair & game.StringPair.$Shape} StringPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StringPair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StringPair message.
         * @function verify
         * @memberof game.StringPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StringPair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isString(message.value))
                    return "value: string expected";
            return null;
        };

        /**
         * Creates a StringPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.StringPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.StringPair} StringPair
         */
        StringPair.fromObject = function (object, _depth) {
            if (object instanceof $root.game.StringPair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.StringPair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.StringPair();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.value != null)
                if (typeof object.value !== "string" || object.value.length)
                    message.value = $String(object.value);
            return message;
        };

        /**
         * Creates a plain object from a StringPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.StringPair
         * @static
         * @param {game.StringPair} message StringPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StringPair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = "";
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this StringPair to JSON.
         * @function toJSON
         * @memberof game.StringPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StringPair.prototype.toJSON = function() {
            return StringPair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StringPair
         * @function getTypeUrl
         * @memberof game.StringPair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StringPair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.StringPair";
        };

        return StringPair;
    })();

    game.SeatOccupantPair = (function() {

        /**
         * Properties of a SeatOccupantPair.
         * @typedef {Object} game.SeatOccupantPair.$Properties
         * @property {string|null} [key] SeatOccupantPair key
         * @property {game.SeatOccupant.$Properties|null} [value] SeatOccupantPair value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SeatOccupantPair.
         * @memberof game
         * @interface ISeatOccupantPair
         * @augments game.SeatOccupantPair.$Properties
         * @deprecated Use game.SeatOccupantPair.$Properties instead.
         */

        /**
         * Shape of a SeatOccupantPair.
         * @typedef {{
         *   key?: string|null;
         *   value?: game.SeatOccupant.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} game.SeatOccupantPair.$Shape
         */

        /**
         * Constructs a new SeatOccupantPair.
         * @memberof game
         * @classdesc Represents a SeatOccupantPair.
         * @constructor
         * @param {game.SeatOccupantPair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SeatOccupantPair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SeatOccupantPair key.
         * @member {string} key
         * @memberof game.SeatOccupantPair
         * @instance
         */
        SeatOccupantPair.prototype.key = "";

        /**
         * SeatOccupantPair value.
         * @member {game.SeatOccupant.$Properties|null|undefined} value
         * @memberof game.SeatOccupantPair
         * @instance
         */
        SeatOccupantPair.prototype.value = null;

        /**
         * Creates a new SeatOccupantPair instance using the specified properties.
         * @function create
         * @memberof game.SeatOccupantPair
         * @static
         * @param {game.SeatOccupantPair.$Properties=} [properties] Properties to set
         * @returns {game.SeatOccupantPair} SeatOccupantPair instance
         * @type {{
         *   (properties: game.SeatOccupantPair.$Shape): game.SeatOccupantPair & game.SeatOccupantPair.$Shape;
         *   (properties?: game.SeatOccupantPair.$Properties): game.SeatOccupantPair;
         * }}
         */
        SeatOccupantPair.create = function(properties) {
            return new SeatOccupantPair(properties);
        };

        /**
         * Encodes the specified SeatOccupantPair message. Does not implicitly {@link game.SeatOccupantPair.verify|verify} messages.
         * @function encode
         * @memberof game.SeatOccupantPair
         * @static
         * @param {game.SeatOccupantPair.$Properties} message SeatOccupantPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SeatOccupantPair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                $root.game.SeatOccupant.encode(message.value, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SeatOccupantPair message, length delimited. Does not implicitly {@link game.SeatOccupantPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SeatOccupantPair
         * @static
         * @param {game.SeatOccupantPair.$Properties} message SeatOccupantPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SeatOccupantPair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SeatOccupantPair message from the specified reader or buffer.
         * @function decode
         * @memberof game.SeatOccupantPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SeatOccupantPair & game.SeatOccupantPair.$Shape} SeatOccupantPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SeatOccupantPair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SeatOccupantPair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value = $root.game.SeatOccupant.decode(reader, reader.uint32(), $undefined, _depth + 1, message.value);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SeatOccupantPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SeatOccupantPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SeatOccupantPair & game.SeatOccupantPair.$Shape} SeatOccupantPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SeatOccupantPair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SeatOccupantPair message.
         * @function verify
         * @memberof game.SeatOccupantPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SeatOccupantPair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                let error = $root.game.SeatOccupant.verify(message.value, _depth + 1);
                if (error)
                    return "value." + error;
            }
            return null;
        };

        /**
         * Creates a SeatOccupantPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SeatOccupantPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SeatOccupantPair} SeatOccupantPair
         */
        SeatOccupantPair.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SeatOccupantPair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SeatOccupantPair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SeatOccupantPair();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.value != null) {
                if (!$util.isObject(object.value))
                    throw $TypeError(".game.SeatOccupantPair.value: object expected");
                message.value = $root.game.SeatOccupant.fromObject(object.value, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SeatOccupantPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SeatOccupantPair
         * @static
         * @param {game.SeatOccupantPair} message SeatOccupantPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SeatOccupantPair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = null;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = $root.game.SeatOccupant.toObject(message.value, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SeatOccupantPair to JSON.
         * @function toJSON
         * @memberof game.SeatOccupantPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SeatOccupantPair.prototype.toJSON = function() {
            return SeatOccupantPair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SeatOccupantPair
         * @function getTypeUrl
         * @memberof game.SeatOccupantPair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SeatOccupantPair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SeatOccupantPair";
        };

        return SeatOccupantPair;
    })();

    game.SeatStatsPair = (function() {

        /**
         * Properties of a SeatStatsPair.
         * @typedef {Object} game.SeatStatsPair.$Properties
         * @property {string|null} [key] SeatStatsPair key
         * @property {game.SeatStats.$Properties|null} [value] SeatStatsPair value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SeatStatsPair.
         * @memberof game
         * @interface ISeatStatsPair
         * @augments game.SeatStatsPair.$Properties
         * @deprecated Use game.SeatStatsPair.$Properties instead.
         */

        /**
         * Shape of a SeatStatsPair.
         * @typedef {game.SeatStatsPair.$Properties} game.SeatStatsPair.$Shape
         */

        /**
         * Constructs a new SeatStatsPair.
         * @memberof game
         * @classdesc Represents a SeatStatsPair.
         * @constructor
         * @param {game.SeatStatsPair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SeatStatsPair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SeatStatsPair key.
         * @member {string} key
         * @memberof game.SeatStatsPair
         * @instance
         */
        SeatStatsPair.prototype.key = "";

        /**
         * SeatStatsPair value.
         * @member {game.SeatStats.$Properties|null|undefined} value
         * @memberof game.SeatStatsPair
         * @instance
         */
        SeatStatsPair.prototype.value = null;

        /**
         * Creates a new SeatStatsPair instance using the specified properties.
         * @function create
         * @memberof game.SeatStatsPair
         * @static
         * @param {game.SeatStatsPair.$Properties=} [properties] Properties to set
         * @returns {game.SeatStatsPair} SeatStatsPair instance
         * @type {{
         *   (properties: game.SeatStatsPair.$Shape): game.SeatStatsPair & game.SeatStatsPair.$Shape;
         *   (properties?: game.SeatStatsPair.$Properties): game.SeatStatsPair;
         * }}
         */
        SeatStatsPair.create = function(properties) {
            return new SeatStatsPair(properties);
        };

        /**
         * Encodes the specified SeatStatsPair message. Does not implicitly {@link game.SeatStatsPair.verify|verify} messages.
         * @function encode
         * @memberof game.SeatStatsPair
         * @static
         * @param {game.SeatStatsPair.$Properties} message SeatStatsPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SeatStatsPair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                $root.game.SeatStats.encode(message.value, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SeatStatsPair message, length delimited. Does not implicitly {@link game.SeatStatsPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SeatStatsPair
         * @static
         * @param {game.SeatStatsPair.$Properties} message SeatStatsPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SeatStatsPair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SeatStatsPair message from the specified reader or buffer.
         * @function decode
         * @memberof game.SeatStatsPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SeatStatsPair & game.SeatStatsPair.$Shape} SeatStatsPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SeatStatsPair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SeatStatsPair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value = $root.game.SeatStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.value);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SeatStatsPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SeatStatsPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SeatStatsPair & game.SeatStatsPair.$Shape} SeatStatsPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SeatStatsPair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SeatStatsPair message.
         * @function verify
         * @memberof game.SeatStatsPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SeatStatsPair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                let error = $root.game.SeatStats.verify(message.value, _depth + 1);
                if (error)
                    return "value." + error;
            }
            return null;
        };

        /**
         * Creates a SeatStatsPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SeatStatsPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SeatStatsPair} SeatStatsPair
         */
        SeatStatsPair.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SeatStatsPair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SeatStatsPair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SeatStatsPair();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.value != null) {
                if (!$util.isObject(object.value))
                    throw $TypeError(".game.SeatStatsPair.value: object expected");
                message.value = $root.game.SeatStats.fromObject(object.value, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SeatStatsPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SeatStatsPair
         * @static
         * @param {game.SeatStatsPair} message SeatStatsPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SeatStatsPair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = null;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = $root.game.SeatStats.toObject(message.value, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SeatStatsPair to JSON.
         * @function toJSON
         * @memberof game.SeatStatsPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SeatStatsPair.prototype.toJSON = function() {
            return SeatStatsPair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SeatStatsPair
         * @function getTypeUrl
         * @memberof game.SeatStatsPair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SeatStatsPair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SeatStatsPair";
        };

        return SeatStatsPair;
    })();

    game.RoomSnapshot = (function() {

        /**
         * Properties of a RoomSnapshot.
         * @typedef {Object} game.RoomSnapshot.$Properties
         * @property {string|null} [id] RoomSnapshot id
         * @property {number|Long|null} [updatedAt] RoomSnapshot updatedAt
         * @property {game.RoomSettings.$Properties|null} [settings] RoomSnapshot settings
         * @property {string|null} [status] RoomSnapshot status
         * @property {string|null} [phase] RoomSnapshot phase
         * @property {Array.<game.SeatOccupantPair.$Properties>|null} [seats] RoomSnapshot seats
         * @property {Array.<game.PublicPlayer.$Properties>|null} [spectators] RoomSnapshot spectators
         * @property {Array.<game.BoolPair.$Properties>|null} [ready] RoomSnapshot ready
         * @property {Array.<game.StringPair.$Properties>|null} [choices] RoomSnapshot choices
         * @property {Array.<game.StringPair.$Properties>|null} [revealedChoices] RoomSnapshot revealedChoices
         * @property {game.OthelloState.$Properties|null} [othello] RoomSnapshot othello
         * @property {game.TicTacToeState.$Properties|null} [tictactoe] RoomSnapshot tictactoe
         * @property {game.LiarsDiceState.$Properties|null} [liarsDice] RoomSnapshot liarsDice
         * @property {game.GomokuState.$Properties|null} [gomoku] RoomSnapshot gomoku
         * @property {string|null} [resultText] RoomSnapshot resultText
         * @property {Array.<string>|null} [punishedPlayerIds] RoomSnapshot punishedPlayerIds
         * @property {Array.<game.PunishmentProof.$Properties>|null} [proofs] RoomSnapshot proofs
         * @property {Array.<game.IntPair.$Properties>|null} [score] RoomSnapshot score
         * @property {Array.<game.IntPair.$Properties>|null} [seatedScore] RoomSnapshot seatedScore
         * @property {Array.<game.SeatStatsPair.$Properties>|null} [seatStats] RoomSnapshot seatStats
         * @property {Array.<game.RoundHistoryItem.$Properties>|null} [roundHistory] RoomSnapshot roundHistory
         * @property {number|null} [roundHistoryTotal] RoomSnapshot roundHistoryTotal
         * @property {Array.<game.ChatMessage.$Properties>|null} [chat] RoomSnapshot chat
         * @property {string|null} [forgiveAdvantageTargetId] RoomSnapshot forgiveAdvantageTargetId
         * @property {string|null} [forgiveAdvantageBeneficiaryId] RoomSnapshot forgiveAdvantageBeneficiaryId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomSnapshot.
         * @memberof game
         * @interface IRoomSnapshot
         * @augments game.RoomSnapshot.$Properties
         * @deprecated Use game.RoomSnapshot.$Properties instead.
         */

        /**
         * Shape of a RoomSnapshot.
         * @typedef {{
         *   id?: string|null;
         *   updatedAt?: number|Long|null;
         *   settings?: game.RoomSettings.$Shape|null;
         *   status?: string|null;
         *   phase?: string|null;
         *   seats?: Array.<game.SeatOccupantPair.$Shape>|null;
         *   spectators?: Array.<game.PublicPlayer.$Shape>|null;
         *   ready?: Array.<game.BoolPair.$Shape>|null;
         *   choices?: Array.<game.StringPair.$Shape>|null;
         *   revealedChoices?: Array.<game.StringPair.$Shape>|null;
         *   othello?: game.OthelloState.$Shape|null;
         *   tictactoe?: game.TicTacToeState.$Shape|null;
         *   liarsDice?: game.LiarsDiceState.$Shape|null;
         *   gomoku?: game.GomokuState.$Shape|null;
         *   resultText?: string|null;
         *   punishedPlayerIds?: Array.<string>|null;
         *   proofs?: Array.<game.PunishmentProof.$Shape>|null;
         *   score?: Array.<game.IntPair.$Shape>|null;
         *   seatedScore?: Array.<game.IntPair.$Shape>|null;
         *   seatStats?: Array.<game.SeatStatsPair.$Shape>|null;
         *   roundHistory?: Array.<game.RoundHistoryItem.$Shape>|null;
         *   roundHistoryTotal?: number|null;
         *   chat?: Array.<game.ChatMessage.$Shape>|null;
         *   forgiveAdvantageTargetId?: string|null;
         *   forgiveAdvantageBeneficiaryId?: string|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} game.RoomSnapshot.$Shape
         */

        /**
         * Constructs a new RoomSnapshot.
         * @memberof game
         * @classdesc Represents a RoomSnapshot.
         * @constructor
         * @param {game.RoomSnapshot.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomSnapshot = function (properties) {
            this.seats = [];
            this.spectators = [];
            this.ready = [];
            this.choices = [];
            this.revealedChoices = [];
            this.punishedPlayerIds = [];
            this.proofs = [];
            this.score = [];
            this.seatedScore = [];
            this.seatStats = [];
            this.roundHistory = [];
            this.chat = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomSnapshot id.
         * @member {string} id
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.id = "";

        /**
         * RoomSnapshot updatedAt.
         * @member {number|Long} updatedAt
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.updatedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoomSnapshot settings.
         * @member {game.RoomSettings.$Properties|null|undefined} settings
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.settings = null;

        /**
         * RoomSnapshot status.
         * @member {string} status
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.status = "";

        /**
         * RoomSnapshot phase.
         * @member {string} phase
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.phase = "";

        /**
         * RoomSnapshot seats.
         * @member {Array.<game.SeatOccupantPair.$Properties>} seats
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.seats = $util.emptyArray;

        /**
         * RoomSnapshot spectators.
         * @member {Array.<game.PublicPlayer.$Properties>} spectators
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.spectators = $util.emptyArray;

        /**
         * RoomSnapshot ready.
         * @member {Array.<game.BoolPair.$Properties>} ready
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.ready = $util.emptyArray;

        /**
         * RoomSnapshot choices.
         * @member {Array.<game.StringPair.$Properties>} choices
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.choices = $util.emptyArray;

        /**
         * RoomSnapshot revealedChoices.
         * @member {Array.<game.StringPair.$Properties>} revealedChoices
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.revealedChoices = $util.emptyArray;

        /**
         * RoomSnapshot othello.
         * @member {game.OthelloState.$Properties|null|undefined} othello
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.othello = null;

        /**
         * RoomSnapshot tictactoe.
         * @member {game.TicTacToeState.$Properties|null|undefined} tictactoe
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.tictactoe = null;

        /**
         * RoomSnapshot liarsDice.
         * @member {game.LiarsDiceState.$Properties|null|undefined} liarsDice
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.liarsDice = null;

        /**
         * RoomSnapshot gomoku.
         * @member {game.GomokuState.$Properties|null|undefined} gomoku
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.gomoku = null;

        /**
         * RoomSnapshot resultText.
         * @member {string} resultText
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.resultText = "";

        /**
         * RoomSnapshot punishedPlayerIds.
         * @member {Array.<string>} punishedPlayerIds
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.punishedPlayerIds = $util.emptyArray;

        /**
         * RoomSnapshot proofs.
         * @member {Array.<game.PunishmentProof.$Properties>} proofs
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.proofs = $util.emptyArray;

        /**
         * RoomSnapshot score.
         * @member {Array.<game.IntPair.$Properties>} score
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.score = $util.emptyArray;

        /**
         * RoomSnapshot seatedScore.
         * @member {Array.<game.IntPair.$Properties>} seatedScore
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.seatedScore = $util.emptyArray;

        /**
         * RoomSnapshot seatStats.
         * @member {Array.<game.SeatStatsPair.$Properties>} seatStats
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.seatStats = $util.emptyArray;

        /**
         * RoomSnapshot roundHistory.
         * @member {Array.<game.RoundHistoryItem.$Properties>} roundHistory
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.roundHistory = $util.emptyArray;

        /**
         * RoomSnapshot roundHistoryTotal.
         * @member {number} roundHistoryTotal
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.roundHistoryTotal = 0;

        /**
         * RoomSnapshot chat.
         * @member {Array.<game.ChatMessage.$Properties>} chat
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.chat = $util.emptyArray;

        /**
         * RoomSnapshot forgiveAdvantageTargetId.
         * @member {string} forgiveAdvantageTargetId
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.forgiveAdvantageTargetId = "";

        /**
         * RoomSnapshot forgiveAdvantageBeneficiaryId.
         * @member {string} forgiveAdvantageBeneficiaryId
         * @memberof game.RoomSnapshot
         * @instance
         */
        RoomSnapshot.prototype.forgiveAdvantageBeneficiaryId = "";

        /**
         * Creates a new RoomSnapshot instance using the specified properties.
         * @function create
         * @memberof game.RoomSnapshot
         * @static
         * @param {game.RoomSnapshot.$Properties=} [properties] Properties to set
         * @returns {game.RoomSnapshot} RoomSnapshot instance
         * @type {{
         *   (properties: game.RoomSnapshot.$Shape): game.RoomSnapshot & game.RoomSnapshot.$Shape;
         *   (properties?: game.RoomSnapshot.$Properties): game.RoomSnapshot;
         * }}
         */
        RoomSnapshot.create = function(properties) {
            return new RoomSnapshot(properties);
        };

        /**
         * Encodes the specified RoomSnapshot message. Does not implicitly {@link game.RoomSnapshot.verify|verify} messages.
         * @function encode
         * @memberof game.RoomSnapshot
         * @static
         * @param {game.RoomSnapshot.$Properties} message RoomSnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomSnapshot.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt") && (typeof message.updatedAt === "object" ? message.updatedAt.low || message.updatedAt.high : message.updatedAt !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.updatedAt);
            if (message.settings != null && $Object.hasOwnProperty.call(message, "settings"))
                $root.game.RoomSettings.encode(message.settings, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.status);
            if (message.phase != null && $Object.hasOwnProperty.call(message, "phase") && message.phase !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.phase);
            if (message.seats != null && message.seats.length)
                for (let i = 0; i < message.seats.length; ++i)
                    $root.game.SeatOccupantPair.encode(message.seats[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.spectators != null && message.spectators.length)
                for (let i = 0; i < message.spectators.length; ++i)
                    $root.game.PublicPlayer.encode(message.spectators[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.ready != null && message.ready.length)
                for (let i = 0; i < message.ready.length; ++i)
                    $root.game.BoolPair.encode(message.ready[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.choices != null && message.choices.length)
                for (let i = 0; i < message.choices.length; ++i)
                    $root.game.StringPair.encode(message.choices[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.revealedChoices != null && message.revealedChoices.length)
                for (let i = 0; i < message.revealedChoices.length; ++i)
                    $root.game.StringPair.encode(message.revealedChoices[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.othello != null && $Object.hasOwnProperty.call(message, "othello"))
                $root.game.OthelloState.encode(message.othello, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.tictactoe != null && $Object.hasOwnProperty.call(message, "tictactoe"))
                $root.game.TicTacToeState.encode(message.tictactoe, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.resultText != null && $Object.hasOwnProperty.call(message, "resultText") && message.resultText !== "")
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.resultText);
            if (message.punishedPlayerIds != null && message.punishedPlayerIds.length)
                for (let i = 0; i < message.punishedPlayerIds.length; ++i)
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.punishedPlayerIds[i]);
            if (message.proofs != null && message.proofs.length)
                for (let i = 0; i < message.proofs.length; ++i)
                    $root.game.PunishmentProof.encode(message.proofs[i], writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.score != null && message.score.length)
                for (let i = 0; i < message.score.length; ++i)
                    $root.game.IntPair.encode(message.score[i], writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.seatedScore != null && message.seatedScore.length)
                for (let i = 0; i < message.seatedScore.length; ++i)
                    $root.game.IntPair.encode(message.seatedScore[i], writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
            if (message.seatStats != null && message.seatStats.length)
                for (let i = 0; i < message.seatStats.length; ++i)
                    $root.game.SeatStatsPair.encode(message.seatStats[i], writer.uint32(/* id 19, wireType 2 =*/154).fork(), _depth + 1).ldelim();
            if (message.roundHistory != null && message.roundHistory.length)
                for (let i = 0; i < message.roundHistory.length; ++i)
                    $root.game.RoundHistoryItem.encode(message.roundHistory[i], writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
            if (message.roundHistoryTotal != null && $Object.hasOwnProperty.call(message, "roundHistoryTotal") && message.roundHistoryTotal !== 0)
                writer.uint32(/* id 21, wireType 0 =*/168).int32(message.roundHistoryTotal);
            if (message.chat != null && message.chat.length)
                for (let i = 0; i < message.chat.length; ++i)
                    $root.game.ChatMessage.encode(message.chat[i], writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
            if (message.liarsDice != null && $Object.hasOwnProperty.call(message, "liarsDice"))
                $root.game.LiarsDiceState.encode(message.liarsDice, writer.uint32(/* id 23, wireType 2 =*/186).fork(), _depth + 1).ldelim();
            if (message.gomoku != null && $Object.hasOwnProperty.call(message, "gomoku"))
                $root.game.GomokuState.encode(message.gomoku, writer.uint32(/* id 24, wireType 2 =*/194).fork(), _depth + 1).ldelim();
            if (message.forgiveAdvantageTargetId != null && $Object.hasOwnProperty.call(message, "forgiveAdvantageTargetId") && message.forgiveAdvantageTargetId !== "")
                writer.uint32(/* id 25, wireType 2 =*/202).string(message.forgiveAdvantageTargetId);
            if (message.forgiveAdvantageBeneficiaryId != null && $Object.hasOwnProperty.call(message, "forgiveAdvantageBeneficiaryId") && message.forgiveAdvantageBeneficiaryId !== "")
                writer.uint32(/* id 26, wireType 2 =*/210).string(message.forgiveAdvantageBeneficiaryId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoomSnapshot message, length delimited. Does not implicitly {@link game.RoomSnapshot.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RoomSnapshot
         * @static
         * @param {game.RoomSnapshot.$Properties} message RoomSnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomSnapshot.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RoomSnapshot message from the specified reader or buffer.
         * @function decode
         * @memberof game.RoomSnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RoomSnapshot & game.RoomSnapshot.$Shape} RoomSnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomSnapshot.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RoomSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.updatedAt = value;
                        else
                            delete message.updatedAt;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.settings = $root.game.RoomSettings.decode(reader, reader.uint32(), $undefined, _depth + 1, message.settings);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.phase = value;
                        else
                            delete message.phase;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.seats && message.seats.length))
                            message.seats = [];
                        message.seats.push($root.game.SeatOccupantPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.spectators && message.spectators.length))
                            message.spectators = [];
                        message.spectators.push($root.game.PublicPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ready && message.ready.length))
                            message.ready = [];
                        message.ready.push($root.game.BoolPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.choices && message.choices.length))
                            message.choices = [];
                        message.choices.push($root.game.StringPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.revealedChoices && message.revealedChoices.length))
                            message.revealedChoices = [];
                        message.revealedChoices.push($root.game.StringPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.othello = $root.game.OthelloState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.othello);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.tictactoe = $root.game.TicTacToeState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tictactoe);
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        message.liarsDice = $root.game.LiarsDiceState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.liarsDice);
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.gomoku = $root.game.GomokuState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gomoku);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.resultText = value;
                        else
                            delete message.resultText;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.punishedPlayerIds && message.punishedPlayerIds.length))
                            message.punishedPlayerIds = [];
                        message.punishedPlayerIds.push(reader.stringVerify());
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.proofs && message.proofs.length))
                            message.proofs = [];
                        message.proofs.push($root.game.PunishmentProof.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if (!(message.score && message.score.length))
                            message.score = [];
                        message.score.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        if (!(message.seatedScore && message.seatedScore.length))
                            message.seatedScore = [];
                        message.seatedScore.push($root.game.IntPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if (!(message.seatStats && message.seatStats.length))
                            message.seatStats = [];
                        message.seatStats.push($root.game.SeatStatsPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if (!(message.roundHistory && message.roundHistory.length))
                            message.roundHistory = [];
                        message.roundHistory.push($root.game.RoundHistoryItem.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.roundHistoryTotal = value;
                        else
                            delete message.roundHistoryTotal;
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        if (!(message.chat && message.chat.length))
                            message.chat = [];
                        message.chat.push($root.game.ChatMessage.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.forgiveAdvantageTargetId = value;
                        else
                            delete message.forgiveAdvantageTargetId;
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.forgiveAdvantageBeneficiaryId = value;
                        else
                            delete message.forgiveAdvantageBeneficiaryId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoomSnapshot message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RoomSnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RoomSnapshot & game.RoomSnapshot.$Shape} RoomSnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomSnapshot.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoomSnapshot message.
         * @function verify
         * @memberof game.RoomSnapshot
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomSnapshot.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt"))
                if (!$util.isInteger(message.updatedAt) && !(message.updatedAt && $util.isInteger(message.updatedAt.low) && $util.isInteger(message.updatedAt.high)))
                    return "updatedAt: integer|Long expected";
            if (message.settings != null && $Object.hasOwnProperty.call(message, "settings")) {
                let error = $root.game.RoomSettings.verify(message.settings, _depth + 1);
                if (error)
                    return "settings." + error;
            }
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                if (!$util.isString(message.phase))
                    return "phase: string expected";
            if (message.seats != null && $Object.hasOwnProperty.call(message, "seats")) {
                if (!$Array.isArray(message.seats))
                    return "seats: array expected";
                for (let i = 0; i < message.seats.length; ++i) {
                    let error = $root.game.SeatOccupantPair.verify(message.seats[i], _depth + 1);
                    if (error)
                        return "seats." + error;
                }
            }
            if (message.spectators != null && $Object.hasOwnProperty.call(message, "spectators")) {
                if (!$Array.isArray(message.spectators))
                    return "spectators: array expected";
                for (let i = 0; i < message.spectators.length; ++i) {
                    let error = $root.game.PublicPlayer.verify(message.spectators[i], _depth + 1);
                    if (error)
                        return "spectators." + error;
                }
            }
            if (message.ready != null && $Object.hasOwnProperty.call(message, "ready")) {
                if (!$Array.isArray(message.ready))
                    return "ready: array expected";
                for (let i = 0; i < message.ready.length; ++i) {
                    let error = $root.game.BoolPair.verify(message.ready[i], _depth + 1);
                    if (error)
                        return "ready." + error;
                }
            }
            if (message.choices != null && $Object.hasOwnProperty.call(message, "choices")) {
                if (!$Array.isArray(message.choices))
                    return "choices: array expected";
                for (let i = 0; i < message.choices.length; ++i) {
                    let error = $root.game.StringPair.verify(message.choices[i], _depth + 1);
                    if (error)
                        return "choices." + error;
                }
            }
            if (message.revealedChoices != null && $Object.hasOwnProperty.call(message, "revealedChoices")) {
                if (!$Array.isArray(message.revealedChoices))
                    return "revealedChoices: array expected";
                for (let i = 0; i < message.revealedChoices.length; ++i) {
                    let error = $root.game.StringPair.verify(message.revealedChoices[i], _depth + 1);
                    if (error)
                        return "revealedChoices." + error;
                }
            }
            if (message.othello != null && $Object.hasOwnProperty.call(message, "othello")) {
                let error = $root.game.OthelloState.verify(message.othello, _depth + 1);
                if (error)
                    return "othello." + error;
            }
            if (message.tictactoe != null && $Object.hasOwnProperty.call(message, "tictactoe")) {
                let error = $root.game.TicTacToeState.verify(message.tictactoe, _depth + 1);
                if (error)
                    return "tictactoe." + error;
            }
            if (message.liarsDice != null && $Object.hasOwnProperty.call(message, "liarsDice")) {
                let error = $root.game.LiarsDiceState.verify(message.liarsDice, _depth + 1);
                if (error)
                    return "liarsDice." + error;
            }
            if (message.gomoku != null && $Object.hasOwnProperty.call(message, "gomoku")) {
                let error = $root.game.GomokuState.verify(message.gomoku, _depth + 1);
                if (error)
                    return "gomoku." + error;
            }
            if (message.resultText != null && $Object.hasOwnProperty.call(message, "resultText"))
                if (!$util.isString(message.resultText))
                    return "resultText: string expected";
            if (message.punishedPlayerIds != null && $Object.hasOwnProperty.call(message, "punishedPlayerIds")) {
                if (!$Array.isArray(message.punishedPlayerIds))
                    return "punishedPlayerIds: array expected";
                for (let i = 0; i < message.punishedPlayerIds.length; ++i)
                    if (!$util.isString(message.punishedPlayerIds[i]))
                        return "punishedPlayerIds: string[] expected";
            }
            if (message.proofs != null && $Object.hasOwnProperty.call(message, "proofs")) {
                if (!$Array.isArray(message.proofs))
                    return "proofs: array expected";
                for (let i = 0; i < message.proofs.length; ++i) {
                    let error = $root.game.PunishmentProof.verify(message.proofs[i], _depth + 1);
                    if (error)
                        return "proofs." + error;
                }
            }
            if (message.score != null && $Object.hasOwnProperty.call(message, "score")) {
                if (!$Array.isArray(message.score))
                    return "score: array expected";
                for (let i = 0; i < message.score.length; ++i) {
                    let error = $root.game.IntPair.verify(message.score[i], _depth + 1);
                    if (error)
                        return "score." + error;
                }
            }
            if (message.seatedScore != null && $Object.hasOwnProperty.call(message, "seatedScore")) {
                if (!$Array.isArray(message.seatedScore))
                    return "seatedScore: array expected";
                for (let i = 0; i < message.seatedScore.length; ++i) {
                    let error = $root.game.IntPair.verify(message.seatedScore[i], _depth + 1);
                    if (error)
                        return "seatedScore." + error;
                }
            }
            if (message.seatStats != null && $Object.hasOwnProperty.call(message, "seatStats")) {
                if (!$Array.isArray(message.seatStats))
                    return "seatStats: array expected";
                for (let i = 0; i < message.seatStats.length; ++i) {
                    let error = $root.game.SeatStatsPair.verify(message.seatStats[i], _depth + 1);
                    if (error)
                        return "seatStats." + error;
                }
            }
            if (message.roundHistory != null && $Object.hasOwnProperty.call(message, "roundHistory")) {
                if (!$Array.isArray(message.roundHistory))
                    return "roundHistory: array expected";
                for (let i = 0; i < message.roundHistory.length; ++i) {
                    let error = $root.game.RoundHistoryItem.verify(message.roundHistory[i], _depth + 1);
                    if (error)
                        return "roundHistory." + error;
                }
            }
            if (message.roundHistoryTotal != null && $Object.hasOwnProperty.call(message, "roundHistoryTotal"))
                if (!$util.isInteger(message.roundHistoryTotal))
                    return "roundHistoryTotal: integer expected";
            if (message.chat != null && $Object.hasOwnProperty.call(message, "chat")) {
                if (!$Array.isArray(message.chat))
                    return "chat: array expected";
                for (let i = 0; i < message.chat.length; ++i) {
                    let error = $root.game.ChatMessage.verify(message.chat[i], _depth + 1);
                    if (error)
                        return "chat." + error;
                }
            }
            if (message.forgiveAdvantageTargetId != null && $Object.hasOwnProperty.call(message, "forgiveAdvantageTargetId"))
                if (!$util.isString(message.forgiveAdvantageTargetId))
                    return "forgiveAdvantageTargetId: string expected";
            if (message.forgiveAdvantageBeneficiaryId != null && $Object.hasOwnProperty.call(message, "forgiveAdvantageBeneficiaryId"))
                if (!$util.isString(message.forgiveAdvantageBeneficiaryId))
                    return "forgiveAdvantageBeneficiaryId: string expected";
            return null;
        };

        /**
         * Creates a RoomSnapshot message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RoomSnapshot
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RoomSnapshot} RoomSnapshot
         */
        RoomSnapshot.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RoomSnapshot)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RoomSnapshot: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RoomSnapshot();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.updatedAt != null)
                if (typeof object.updatedAt === "object" ? object.updatedAt.low || object.updatedAt.high : $Number(object.updatedAt) !== 0)
                    if ($util.Long)
                        message.updatedAt = $util.Long.fromValue(object.updatedAt, false);
                    else if (typeof object.updatedAt === "string")
                        message.updatedAt = $parseInt(object.updatedAt, 10);
                    else if (typeof object.updatedAt === "number")
                        message.updatedAt = object.updatedAt;
                    else if (typeof object.updatedAt === "object")
                        message.updatedAt = new $util.LongBits(object.updatedAt.low >>> 0, object.updatedAt.high >>> 0).toNumber();
            if (object.settings != null) {
                if (!$util.isObject(object.settings))
                    throw $TypeError(".game.RoomSnapshot.settings: object expected");
                message.settings = $root.game.RoomSettings.fromObject(object.settings, _depth + 1);
            }
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            if (object.phase != null)
                if (typeof object.phase !== "string" || object.phase.length)
                    message.phase = $String(object.phase);
            if (object.seats) {
                if (!$Array.isArray(object.seats))
                    throw $TypeError(".game.RoomSnapshot.seats: array expected");
                message.seats = $Array(object.seats.length);
                for (let i = 0; i < object.seats.length; ++i) {
                    if (!$util.isObject(object.seats[i]))
                        throw $TypeError(".game.RoomSnapshot.seats: object expected");
                    message.seats[i] = $root.game.SeatOccupantPair.fromObject(object.seats[i], _depth + 1);
                }
            }
            if (object.spectators) {
                if (!$Array.isArray(object.spectators))
                    throw $TypeError(".game.RoomSnapshot.spectators: array expected");
                message.spectators = $Array(object.spectators.length);
                for (let i = 0; i < object.spectators.length; ++i) {
                    if (!$util.isObject(object.spectators[i]))
                        throw $TypeError(".game.RoomSnapshot.spectators: object expected");
                    message.spectators[i] = $root.game.PublicPlayer.fromObject(object.spectators[i], _depth + 1);
                }
            }
            if (object.ready) {
                if (!$Array.isArray(object.ready))
                    throw $TypeError(".game.RoomSnapshot.ready: array expected");
                message.ready = $Array(object.ready.length);
                for (let i = 0; i < object.ready.length; ++i) {
                    if (!$util.isObject(object.ready[i]))
                        throw $TypeError(".game.RoomSnapshot.ready: object expected");
                    message.ready[i] = $root.game.BoolPair.fromObject(object.ready[i], _depth + 1);
                }
            }
            if (object.choices) {
                if (!$Array.isArray(object.choices))
                    throw $TypeError(".game.RoomSnapshot.choices: array expected");
                message.choices = $Array(object.choices.length);
                for (let i = 0; i < object.choices.length; ++i) {
                    if (!$util.isObject(object.choices[i]))
                        throw $TypeError(".game.RoomSnapshot.choices: object expected");
                    message.choices[i] = $root.game.StringPair.fromObject(object.choices[i], _depth + 1);
                }
            }
            if (object.revealedChoices) {
                if (!$Array.isArray(object.revealedChoices))
                    throw $TypeError(".game.RoomSnapshot.revealedChoices: array expected");
                message.revealedChoices = $Array(object.revealedChoices.length);
                for (let i = 0; i < object.revealedChoices.length; ++i) {
                    if (!$util.isObject(object.revealedChoices[i]))
                        throw $TypeError(".game.RoomSnapshot.revealedChoices: object expected");
                    message.revealedChoices[i] = $root.game.StringPair.fromObject(object.revealedChoices[i], _depth + 1);
                }
            }
            if (object.othello != null) {
                if (!$util.isObject(object.othello))
                    throw $TypeError(".game.RoomSnapshot.othello: object expected");
                message.othello = $root.game.OthelloState.fromObject(object.othello, _depth + 1);
            }
            if (object.tictactoe != null) {
                if (!$util.isObject(object.tictactoe))
                    throw $TypeError(".game.RoomSnapshot.tictactoe: object expected");
                message.tictactoe = $root.game.TicTacToeState.fromObject(object.tictactoe, _depth + 1);
            }
            if (object.liarsDice != null) {
                if (!$util.isObject(object.liarsDice))
                    throw $TypeError(".game.RoomSnapshot.liarsDice: object expected");
                message.liarsDice = $root.game.LiarsDiceState.fromObject(object.liarsDice, _depth + 1);
            }
            if (object.gomoku != null) {
                if (!$util.isObject(object.gomoku))
                    throw $TypeError(".game.RoomSnapshot.gomoku: object expected");
                message.gomoku = $root.game.GomokuState.fromObject(object.gomoku, _depth + 1);
            }
            if (object.resultText != null)
                if (typeof object.resultText !== "string" || object.resultText.length)
                    message.resultText = $String(object.resultText);
            if (object.punishedPlayerIds) {
                if (!$Array.isArray(object.punishedPlayerIds))
                    throw $TypeError(".game.RoomSnapshot.punishedPlayerIds: array expected");
                message.punishedPlayerIds = $Array(object.punishedPlayerIds.length);
                for (let i = 0; i < object.punishedPlayerIds.length; ++i)
                    message.punishedPlayerIds[i] = $String(object.punishedPlayerIds[i]);
            }
            if (object.proofs) {
                if (!$Array.isArray(object.proofs))
                    throw $TypeError(".game.RoomSnapshot.proofs: array expected");
                message.proofs = $Array(object.proofs.length);
                for (let i = 0; i < object.proofs.length; ++i) {
                    if (!$util.isObject(object.proofs[i]))
                        throw $TypeError(".game.RoomSnapshot.proofs: object expected");
                    message.proofs[i] = $root.game.PunishmentProof.fromObject(object.proofs[i], _depth + 1);
                }
            }
            if (object.score) {
                if (!$Array.isArray(object.score))
                    throw $TypeError(".game.RoomSnapshot.score: array expected");
                message.score = $Array(object.score.length);
                for (let i = 0; i < object.score.length; ++i) {
                    if (!$util.isObject(object.score[i]))
                        throw $TypeError(".game.RoomSnapshot.score: object expected");
                    message.score[i] = $root.game.IntPair.fromObject(object.score[i], _depth + 1);
                }
            }
            if (object.seatedScore) {
                if (!$Array.isArray(object.seatedScore))
                    throw $TypeError(".game.RoomSnapshot.seatedScore: array expected");
                message.seatedScore = $Array(object.seatedScore.length);
                for (let i = 0; i < object.seatedScore.length; ++i) {
                    if (!$util.isObject(object.seatedScore[i]))
                        throw $TypeError(".game.RoomSnapshot.seatedScore: object expected");
                    message.seatedScore[i] = $root.game.IntPair.fromObject(object.seatedScore[i], _depth + 1);
                }
            }
            if (object.seatStats) {
                if (!$Array.isArray(object.seatStats))
                    throw $TypeError(".game.RoomSnapshot.seatStats: array expected");
                message.seatStats = $Array(object.seatStats.length);
                for (let i = 0; i < object.seatStats.length; ++i) {
                    if (!$util.isObject(object.seatStats[i]))
                        throw $TypeError(".game.RoomSnapshot.seatStats: object expected");
                    message.seatStats[i] = $root.game.SeatStatsPair.fromObject(object.seatStats[i], _depth + 1);
                }
            }
            if (object.roundHistory) {
                if (!$Array.isArray(object.roundHistory))
                    throw $TypeError(".game.RoomSnapshot.roundHistory: array expected");
                message.roundHistory = $Array(object.roundHistory.length);
                for (let i = 0; i < object.roundHistory.length; ++i) {
                    if (!$util.isObject(object.roundHistory[i]))
                        throw $TypeError(".game.RoomSnapshot.roundHistory: object expected");
                    message.roundHistory[i] = $root.game.RoundHistoryItem.fromObject(object.roundHistory[i], _depth + 1);
                }
            }
            if (object.roundHistoryTotal != null)
                if ($Number(object.roundHistoryTotal) !== 0)
                    message.roundHistoryTotal = object.roundHistoryTotal | 0;
            if (object.chat) {
                if (!$Array.isArray(object.chat))
                    throw $TypeError(".game.RoomSnapshot.chat: array expected");
                message.chat = $Array(object.chat.length);
                for (let i = 0; i < object.chat.length; ++i) {
                    if (!$util.isObject(object.chat[i]))
                        throw $TypeError(".game.RoomSnapshot.chat: object expected");
                    message.chat[i] = $root.game.ChatMessage.fromObject(object.chat[i], _depth + 1);
                }
            }
            if (object.forgiveAdvantageTargetId != null)
                if (typeof object.forgiveAdvantageTargetId !== "string" || object.forgiveAdvantageTargetId.length)
                    message.forgiveAdvantageTargetId = $String(object.forgiveAdvantageTargetId);
            if (object.forgiveAdvantageBeneficiaryId != null)
                if (typeof object.forgiveAdvantageBeneficiaryId !== "string" || object.forgiveAdvantageBeneficiaryId.length)
                    message.forgiveAdvantageBeneficiaryId = $String(object.forgiveAdvantageBeneficiaryId);
            return message;
        };

        /**
         * Creates a plain object from a RoomSnapshot message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RoomSnapshot
         * @static
         * @param {game.RoomSnapshot} message RoomSnapshot
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoomSnapshot.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.seats = [];
                object.spectators = [];
                object.ready = [];
                object.choices = [];
                object.revealedChoices = [];
                object.punishedPlayerIds = [];
                object.proofs = [];
                object.score = [];
                object.seatedScore = [];
                object.seatStats = [];
                object.roundHistory = [];
                object.chat = [];
            }
            if (options.defaults) {
                object.id = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.updatedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.updatedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.settings = null;
                object.status = "";
                object.phase = "";
                object.othello = null;
                object.tictactoe = null;
                object.resultText = "";
                object.roundHistoryTotal = 0;
                object.liarsDice = null;
                object.gomoku = null;
                object.forgiveAdvantageTargetId = "";
                object.forgiveAdvantageBeneficiaryId = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.updatedAt = typeof message.updatedAt === "number" ? $BigInt(message.updatedAt) : $util.Long.fromBits(message.updatedAt.low >>> 0, message.updatedAt.high >>> 0, false).toBigInt();
                else if (typeof message.updatedAt === "number")
                    object.updatedAt = options.longs === $String ? $String(message.updatedAt) : message.updatedAt;
                else
                    object.updatedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.updatedAt) : options.longs === $Number ? new $util.LongBits(message.updatedAt.low >>> 0, message.updatedAt.high >>> 0).toNumber() : message.updatedAt;
            if (message.settings != null && $Object.hasOwnProperty.call(message, "settings"))
                object.settings = $root.game.RoomSettings.toObject(message.settings, options, _depth + 1);
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                object.phase = message.phase;
            if (message.seats && message.seats.length) {
                object.seats = $Array(message.seats.length);
                for (let j = 0; j < message.seats.length; ++j)
                    object.seats[j] = $root.game.SeatOccupantPair.toObject(message.seats[j], options, _depth + 1);
            }
            if (message.spectators && message.spectators.length) {
                object.spectators = $Array(message.spectators.length);
                for (let j = 0; j < message.spectators.length; ++j)
                    object.spectators[j] = $root.game.PublicPlayer.toObject(message.spectators[j], options, _depth + 1);
            }
            if (message.ready && message.ready.length) {
                object.ready = $Array(message.ready.length);
                for (let j = 0; j < message.ready.length; ++j)
                    object.ready[j] = $root.game.BoolPair.toObject(message.ready[j], options, _depth + 1);
            }
            if (message.choices && message.choices.length) {
                object.choices = $Array(message.choices.length);
                for (let j = 0; j < message.choices.length; ++j)
                    object.choices[j] = $root.game.StringPair.toObject(message.choices[j], options, _depth + 1);
            }
            if (message.revealedChoices && message.revealedChoices.length) {
                object.revealedChoices = $Array(message.revealedChoices.length);
                for (let j = 0; j < message.revealedChoices.length; ++j)
                    object.revealedChoices[j] = $root.game.StringPair.toObject(message.revealedChoices[j], options, _depth + 1);
            }
            if (message.othello != null && $Object.hasOwnProperty.call(message, "othello"))
                object.othello = $root.game.OthelloState.toObject(message.othello, options, _depth + 1);
            if (message.tictactoe != null && $Object.hasOwnProperty.call(message, "tictactoe"))
                object.tictactoe = $root.game.TicTacToeState.toObject(message.tictactoe, options, _depth + 1);
            if (message.resultText != null && $Object.hasOwnProperty.call(message, "resultText"))
                object.resultText = message.resultText;
            if (message.punishedPlayerIds && message.punishedPlayerIds.length) {
                object.punishedPlayerIds = $Array(message.punishedPlayerIds.length);
                for (let j = 0; j < message.punishedPlayerIds.length; ++j)
                    object.punishedPlayerIds[j] = message.punishedPlayerIds[j];
            }
            if (message.proofs && message.proofs.length) {
                object.proofs = $Array(message.proofs.length);
                for (let j = 0; j < message.proofs.length; ++j)
                    object.proofs[j] = $root.game.PunishmentProof.toObject(message.proofs[j], options, _depth + 1);
            }
            if (message.score && message.score.length) {
                object.score = $Array(message.score.length);
                for (let j = 0; j < message.score.length; ++j)
                    object.score[j] = $root.game.IntPair.toObject(message.score[j], options, _depth + 1);
            }
            if (message.seatedScore && message.seatedScore.length) {
                object.seatedScore = $Array(message.seatedScore.length);
                for (let j = 0; j < message.seatedScore.length; ++j)
                    object.seatedScore[j] = $root.game.IntPair.toObject(message.seatedScore[j], options, _depth + 1);
            }
            if (message.seatStats && message.seatStats.length) {
                object.seatStats = $Array(message.seatStats.length);
                for (let j = 0; j < message.seatStats.length; ++j)
                    object.seatStats[j] = $root.game.SeatStatsPair.toObject(message.seatStats[j], options, _depth + 1);
            }
            if (message.roundHistory && message.roundHistory.length) {
                object.roundHistory = $Array(message.roundHistory.length);
                for (let j = 0; j < message.roundHistory.length; ++j)
                    object.roundHistory[j] = $root.game.RoundHistoryItem.toObject(message.roundHistory[j], options, _depth + 1);
            }
            if (message.roundHistoryTotal != null && $Object.hasOwnProperty.call(message, "roundHistoryTotal"))
                object.roundHistoryTotal = message.roundHistoryTotal;
            if (message.chat && message.chat.length) {
                object.chat = $Array(message.chat.length);
                for (let j = 0; j < message.chat.length; ++j)
                    object.chat[j] = $root.game.ChatMessage.toObject(message.chat[j], options, _depth + 1);
            }
            if (message.liarsDice != null && $Object.hasOwnProperty.call(message, "liarsDice"))
                object.liarsDice = $root.game.LiarsDiceState.toObject(message.liarsDice, options, _depth + 1);
            if (message.gomoku != null && $Object.hasOwnProperty.call(message, "gomoku"))
                object.gomoku = $root.game.GomokuState.toObject(message.gomoku, options, _depth + 1);
            if (message.forgiveAdvantageTargetId != null && $Object.hasOwnProperty.call(message, "forgiveAdvantageTargetId"))
                object.forgiveAdvantageTargetId = message.forgiveAdvantageTargetId;
            if (message.forgiveAdvantageBeneficiaryId != null && $Object.hasOwnProperty.call(message, "forgiveAdvantageBeneficiaryId"))
                object.forgiveAdvantageBeneficiaryId = message.forgiveAdvantageBeneficiaryId;
            return object;
        };

        /**
         * Converts this RoomSnapshot to JSON.
         * @function toJSON
         * @memberof game.RoomSnapshot
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoomSnapshot.prototype.toJSON = function() {
            return RoomSnapshot.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoomSnapshot
         * @function getTypeUrl
         * @memberof game.RoomSnapshot
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoomSnapshot.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RoomSnapshot";
        };

        return RoomSnapshot;
    })();

    game.ServerStats = (function() {

        /**
         * Properties of a ServerStats.
         * @typedef {Object} game.ServerStats.$Properties
         * @property {number|Long|null} [startedAt] ServerStats startedAt
         * @property {number|null} [roomBroadcasts] ServerStats roomBroadcasts
         * @property {number|null} [lobbyBroadcasts] ServerStats lobbyBroadcasts
         * @property {number|null} [disconnects] ServerStats disconnects
         * @property {number|null} [reconnects] ServerStats reconnects
         * @property {number|null} [lastRoomSnapshotBytes] ServerStats lastRoomSnapshotBytes
         * @property {number|null} [lastLobbySnapshotBytes] ServerStats lastLobbySnapshotBytes
         * @property {number|null} [recentRoomBroadcasts] ServerStats recentRoomBroadcasts
         * @property {number|null} [recentLobbyBroadcasts] ServerStats recentLobbyBroadcasts
         * @property {number|null} [averageRoomSnapshotBytes] ServerStats averageRoomSnapshotBytes
         * @property {number|null} [averageLobbySnapshotBytes] ServerStats averageLobbySnapshotBytes
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ServerStats.
         * @memberof game
         * @interface IServerStats
         * @augments game.ServerStats.$Properties
         * @deprecated Use game.ServerStats.$Properties instead.
         */

        /**
         * Shape of a ServerStats.
         * @typedef {game.ServerStats.$Properties} game.ServerStats.$Shape
         */

        /**
         * Constructs a new ServerStats.
         * @memberof game
         * @classdesc Represents a ServerStats.
         * @constructor
         * @param {game.ServerStats.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ServerStats = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ServerStats startedAt.
         * @member {number|Long} startedAt
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.startedAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ServerStats roomBroadcasts.
         * @member {number} roomBroadcasts
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.roomBroadcasts = 0;

        /**
         * ServerStats lobbyBroadcasts.
         * @member {number} lobbyBroadcasts
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.lobbyBroadcasts = 0;

        /**
         * ServerStats disconnects.
         * @member {number} disconnects
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.disconnects = 0;

        /**
         * ServerStats reconnects.
         * @member {number} reconnects
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.reconnects = 0;

        /**
         * ServerStats lastRoomSnapshotBytes.
         * @member {number} lastRoomSnapshotBytes
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.lastRoomSnapshotBytes = 0;

        /**
         * ServerStats lastLobbySnapshotBytes.
         * @member {number} lastLobbySnapshotBytes
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.lastLobbySnapshotBytes = 0;

        /**
         * ServerStats recentRoomBroadcasts.
         * @member {number} recentRoomBroadcasts
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.recentRoomBroadcasts = 0;

        /**
         * ServerStats recentLobbyBroadcasts.
         * @member {number} recentLobbyBroadcasts
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.recentLobbyBroadcasts = 0;

        /**
         * ServerStats averageRoomSnapshotBytes.
         * @member {number} averageRoomSnapshotBytes
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.averageRoomSnapshotBytes = 0;

        /**
         * ServerStats averageLobbySnapshotBytes.
         * @member {number} averageLobbySnapshotBytes
         * @memberof game.ServerStats
         * @instance
         */
        ServerStats.prototype.averageLobbySnapshotBytes = 0;

        /**
         * Creates a new ServerStats instance using the specified properties.
         * @function create
         * @memberof game.ServerStats
         * @static
         * @param {game.ServerStats.$Properties=} [properties] Properties to set
         * @returns {game.ServerStats} ServerStats instance
         * @type {{
         *   (properties: game.ServerStats.$Shape): game.ServerStats & game.ServerStats.$Shape;
         *   (properties?: game.ServerStats.$Properties): game.ServerStats;
         * }}
         */
        ServerStats.create = function(properties) {
            return new ServerStats(properties);
        };

        /**
         * Encodes the specified ServerStats message. Does not implicitly {@link game.ServerStats.verify|verify} messages.
         * @function encode
         * @memberof game.ServerStats
         * @static
         * @param {game.ServerStats.$Properties} message ServerStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServerStats.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt") && (typeof message.startedAt === "object" ? message.startedAt.low || message.startedAt.high : message.startedAt !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.startedAt);
            if (message.roomBroadcasts != null && $Object.hasOwnProperty.call(message, "roomBroadcasts") && message.roomBroadcasts !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.roomBroadcasts);
            if (message.lobbyBroadcasts != null && $Object.hasOwnProperty.call(message, "lobbyBroadcasts") && message.lobbyBroadcasts !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.lobbyBroadcasts);
            if (message.disconnects != null && $Object.hasOwnProperty.call(message, "disconnects") && message.disconnects !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.disconnects);
            if (message.reconnects != null && $Object.hasOwnProperty.call(message, "reconnects") && message.reconnects !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.reconnects);
            if (message.lastRoomSnapshotBytes != null && $Object.hasOwnProperty.call(message, "lastRoomSnapshotBytes") && message.lastRoomSnapshotBytes !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.lastRoomSnapshotBytes);
            if (message.lastLobbySnapshotBytes != null && $Object.hasOwnProperty.call(message, "lastLobbySnapshotBytes") && message.lastLobbySnapshotBytes !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.lastLobbySnapshotBytes);
            if (message.recentRoomBroadcasts != null && $Object.hasOwnProperty.call(message, "recentRoomBroadcasts") && message.recentRoomBroadcasts !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.recentRoomBroadcasts);
            if (message.recentLobbyBroadcasts != null && $Object.hasOwnProperty.call(message, "recentLobbyBroadcasts") && message.recentLobbyBroadcasts !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.recentLobbyBroadcasts);
            if (message.averageRoomSnapshotBytes != null && $Object.hasOwnProperty.call(message, "averageRoomSnapshotBytes") && message.averageRoomSnapshotBytes !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.averageRoomSnapshotBytes);
            if (message.averageLobbySnapshotBytes != null && $Object.hasOwnProperty.call(message, "averageLobbySnapshotBytes") && message.averageLobbySnapshotBytes !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.averageLobbySnapshotBytes);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ServerStats message, length delimited. Does not implicitly {@link game.ServerStats.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.ServerStats
         * @static
         * @param {game.ServerStats.$Properties} message ServerStats message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServerStats.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ServerStats message from the specified reader or buffer.
         * @function decode
         * @memberof game.ServerStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.ServerStats & game.ServerStats.$Shape} ServerStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServerStats.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.ServerStats(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.startedAt = value;
                        else
                            delete message.startedAt;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.roomBroadcasts = value;
                        else
                            delete message.roomBroadcasts;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lobbyBroadcasts = value;
                        else
                            delete message.lobbyBroadcasts;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.disconnects = value;
                        else
                            delete message.disconnects;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.reconnects = value;
                        else
                            delete message.reconnects;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lastRoomSnapshotBytes = value;
                        else
                            delete message.lastRoomSnapshotBytes;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lastLobbySnapshotBytes = value;
                        else
                            delete message.lastLobbySnapshotBytes;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.recentRoomBroadcasts = value;
                        else
                            delete message.recentRoomBroadcasts;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.recentLobbyBroadcasts = value;
                        else
                            delete message.recentLobbyBroadcasts;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.averageRoomSnapshotBytes = value;
                        else
                            delete message.averageRoomSnapshotBytes;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.averageLobbySnapshotBytes = value;
                        else
                            delete message.averageLobbySnapshotBytes;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ServerStats message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.ServerStats
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.ServerStats & game.ServerStats.$Shape} ServerStats
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServerStats.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServerStats message.
         * @function verify
         * @memberof game.ServerStats
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServerStats.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt"))
                if (!$util.isInteger(message.startedAt) && !(message.startedAt && $util.isInteger(message.startedAt.low) && $util.isInteger(message.startedAt.high)))
                    return "startedAt: integer|Long expected";
            if (message.roomBroadcasts != null && $Object.hasOwnProperty.call(message, "roomBroadcasts"))
                if (!$util.isInteger(message.roomBroadcasts))
                    return "roomBroadcasts: integer expected";
            if (message.lobbyBroadcasts != null && $Object.hasOwnProperty.call(message, "lobbyBroadcasts"))
                if (!$util.isInteger(message.lobbyBroadcasts))
                    return "lobbyBroadcasts: integer expected";
            if (message.disconnects != null && $Object.hasOwnProperty.call(message, "disconnects"))
                if (!$util.isInteger(message.disconnects))
                    return "disconnects: integer expected";
            if (message.reconnects != null && $Object.hasOwnProperty.call(message, "reconnects"))
                if (!$util.isInteger(message.reconnects))
                    return "reconnects: integer expected";
            if (message.lastRoomSnapshotBytes != null && $Object.hasOwnProperty.call(message, "lastRoomSnapshotBytes"))
                if (!$util.isInteger(message.lastRoomSnapshotBytes))
                    return "lastRoomSnapshotBytes: integer expected";
            if (message.lastLobbySnapshotBytes != null && $Object.hasOwnProperty.call(message, "lastLobbySnapshotBytes"))
                if (!$util.isInteger(message.lastLobbySnapshotBytes))
                    return "lastLobbySnapshotBytes: integer expected";
            if (message.recentRoomBroadcasts != null && $Object.hasOwnProperty.call(message, "recentRoomBroadcasts"))
                if (!$util.isInteger(message.recentRoomBroadcasts))
                    return "recentRoomBroadcasts: integer expected";
            if (message.recentLobbyBroadcasts != null && $Object.hasOwnProperty.call(message, "recentLobbyBroadcasts"))
                if (!$util.isInteger(message.recentLobbyBroadcasts))
                    return "recentLobbyBroadcasts: integer expected";
            if (message.averageRoomSnapshotBytes != null && $Object.hasOwnProperty.call(message, "averageRoomSnapshotBytes"))
                if (!$util.isInteger(message.averageRoomSnapshotBytes))
                    return "averageRoomSnapshotBytes: integer expected";
            if (message.averageLobbySnapshotBytes != null && $Object.hasOwnProperty.call(message, "averageLobbySnapshotBytes"))
                if (!$util.isInteger(message.averageLobbySnapshotBytes))
                    return "averageLobbySnapshotBytes: integer expected";
            return null;
        };

        /**
         * Creates a ServerStats message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.ServerStats
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.ServerStats} ServerStats
         */
        ServerStats.fromObject = function (object, _depth) {
            if (object instanceof $root.game.ServerStats)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.ServerStats: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.ServerStats();
            if (object.startedAt != null)
                if (typeof object.startedAt === "object" ? object.startedAt.low || object.startedAt.high : $Number(object.startedAt) !== 0)
                    if ($util.Long)
                        message.startedAt = $util.Long.fromValue(object.startedAt, false);
                    else if (typeof object.startedAt === "string")
                        message.startedAt = $parseInt(object.startedAt, 10);
                    else if (typeof object.startedAt === "number")
                        message.startedAt = object.startedAt;
                    else if (typeof object.startedAt === "object")
                        message.startedAt = new $util.LongBits(object.startedAt.low >>> 0, object.startedAt.high >>> 0).toNumber();
            if (object.roomBroadcasts != null)
                if ($Number(object.roomBroadcasts) !== 0)
                    message.roomBroadcasts = object.roomBroadcasts | 0;
            if (object.lobbyBroadcasts != null)
                if ($Number(object.lobbyBroadcasts) !== 0)
                    message.lobbyBroadcasts = object.lobbyBroadcasts | 0;
            if (object.disconnects != null)
                if ($Number(object.disconnects) !== 0)
                    message.disconnects = object.disconnects | 0;
            if (object.reconnects != null)
                if ($Number(object.reconnects) !== 0)
                    message.reconnects = object.reconnects | 0;
            if (object.lastRoomSnapshotBytes != null)
                if ($Number(object.lastRoomSnapshotBytes) !== 0)
                    message.lastRoomSnapshotBytes = object.lastRoomSnapshotBytes | 0;
            if (object.lastLobbySnapshotBytes != null)
                if ($Number(object.lastLobbySnapshotBytes) !== 0)
                    message.lastLobbySnapshotBytes = object.lastLobbySnapshotBytes | 0;
            if (object.recentRoomBroadcasts != null)
                if ($Number(object.recentRoomBroadcasts) !== 0)
                    message.recentRoomBroadcasts = object.recentRoomBroadcasts | 0;
            if (object.recentLobbyBroadcasts != null)
                if ($Number(object.recentLobbyBroadcasts) !== 0)
                    message.recentLobbyBroadcasts = object.recentLobbyBroadcasts | 0;
            if (object.averageRoomSnapshotBytes != null)
                if ($Number(object.averageRoomSnapshotBytes) !== 0)
                    message.averageRoomSnapshotBytes = object.averageRoomSnapshotBytes | 0;
            if (object.averageLobbySnapshotBytes != null)
                if ($Number(object.averageLobbySnapshotBytes) !== 0)
                    message.averageLobbySnapshotBytes = object.averageLobbySnapshotBytes | 0;
            return message;
        };

        /**
         * Creates a plain object from a ServerStats message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.ServerStats
         * @static
         * @param {game.ServerStats} message ServerStats
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServerStats.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.startedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.startedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.roomBroadcasts = 0;
                object.lobbyBroadcasts = 0;
                object.disconnects = 0;
                object.reconnects = 0;
                object.lastRoomSnapshotBytes = 0;
                object.lastLobbySnapshotBytes = 0;
                object.recentRoomBroadcasts = 0;
                object.recentLobbyBroadcasts = 0;
                object.averageRoomSnapshotBytes = 0;
                object.averageLobbySnapshotBytes = 0;
            }
            if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.startedAt = typeof message.startedAt === "number" ? $BigInt(message.startedAt) : $util.Long.fromBits(message.startedAt.low >>> 0, message.startedAt.high >>> 0, false).toBigInt();
                else if (typeof message.startedAt === "number")
                    object.startedAt = options.longs === $String ? $String(message.startedAt) : message.startedAt;
                else
                    object.startedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.startedAt) : options.longs === $Number ? new $util.LongBits(message.startedAt.low >>> 0, message.startedAt.high >>> 0).toNumber() : message.startedAt;
            if (message.roomBroadcasts != null && $Object.hasOwnProperty.call(message, "roomBroadcasts"))
                object.roomBroadcasts = message.roomBroadcasts;
            if (message.lobbyBroadcasts != null && $Object.hasOwnProperty.call(message, "lobbyBroadcasts"))
                object.lobbyBroadcasts = message.lobbyBroadcasts;
            if (message.disconnects != null && $Object.hasOwnProperty.call(message, "disconnects"))
                object.disconnects = message.disconnects;
            if (message.reconnects != null && $Object.hasOwnProperty.call(message, "reconnects"))
                object.reconnects = message.reconnects;
            if (message.lastRoomSnapshotBytes != null && $Object.hasOwnProperty.call(message, "lastRoomSnapshotBytes"))
                object.lastRoomSnapshotBytes = message.lastRoomSnapshotBytes;
            if (message.lastLobbySnapshotBytes != null && $Object.hasOwnProperty.call(message, "lastLobbySnapshotBytes"))
                object.lastLobbySnapshotBytes = message.lastLobbySnapshotBytes;
            if (message.recentRoomBroadcasts != null && $Object.hasOwnProperty.call(message, "recentRoomBroadcasts"))
                object.recentRoomBroadcasts = message.recentRoomBroadcasts;
            if (message.recentLobbyBroadcasts != null && $Object.hasOwnProperty.call(message, "recentLobbyBroadcasts"))
                object.recentLobbyBroadcasts = message.recentLobbyBroadcasts;
            if (message.averageRoomSnapshotBytes != null && $Object.hasOwnProperty.call(message, "averageRoomSnapshotBytes"))
                object.averageRoomSnapshotBytes = message.averageRoomSnapshotBytes;
            if (message.averageLobbySnapshotBytes != null && $Object.hasOwnProperty.call(message, "averageLobbySnapshotBytes"))
                object.averageLobbySnapshotBytes = message.averageLobbySnapshotBytes;
            return object;
        };

        /**
         * Converts this ServerStats to JSON.
         * @function toJSON
         * @memberof game.ServerStats
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServerStats.prototype.toJSON = function() {
            return ServerStats.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ServerStats
         * @function getTypeUrl
         * @memberof game.ServerStats
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ServerStats.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.ServerStats";
        };

        return ServerStats;
    })();

    game.VersusSeat = (function() {

        /**
         * Properties of a VersusSeat.
         * @typedef {Object} game.VersusSeat.$Properties
         * @property {game.PublicPlayer.$Properties|null} [player] VersusSeat player
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a VersusSeat.
         * @memberof game
         * @interface IVersusSeat
         * @augments game.VersusSeat.$Properties
         * @deprecated Use game.VersusSeat.$Properties instead.
         */

        /**
         * Shape of a VersusSeat.
         * @typedef {game.VersusSeat.$Properties} game.VersusSeat.$Shape
         */

        /**
         * Constructs a new VersusSeat.
         * @memberof game
         * @classdesc Represents a VersusSeat.
         * @constructor
         * @param {game.VersusSeat.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const VersusSeat = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VersusSeat player.
         * @member {game.PublicPlayer.$Properties|null|undefined} player
         * @memberof game.VersusSeat
         * @instance
         */
        VersusSeat.prototype.player = null;

        /**
         * Creates a new VersusSeat instance using the specified properties.
         * @function create
         * @memberof game.VersusSeat
         * @static
         * @param {game.VersusSeat.$Properties=} [properties] Properties to set
         * @returns {game.VersusSeat} VersusSeat instance
         * @type {{
         *   (properties: game.VersusSeat.$Shape): game.VersusSeat & game.VersusSeat.$Shape;
         *   (properties?: game.VersusSeat.$Properties): game.VersusSeat;
         * }}
         */
        VersusSeat.create = function(properties) {
            return new VersusSeat(properties);
        };

        /**
         * Encodes the specified VersusSeat message. Does not implicitly {@link game.VersusSeat.verify|verify} messages.
         * @function encode
         * @memberof game.VersusSeat
         * @static
         * @param {game.VersusSeat.$Properties} message VersusSeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VersusSeat.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.game.PublicPlayer.encode(message.player, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VersusSeat message, length delimited. Does not implicitly {@link game.VersusSeat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.VersusSeat
         * @static
         * @param {game.VersusSeat.$Properties} message VersusSeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VersusSeat.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a VersusSeat message from the specified reader or buffer.
         * @function decode
         * @memberof game.VersusSeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.VersusSeat & game.VersusSeat.$Shape} VersusSeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VersusSeat.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.VersusSeat(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.game.PublicPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VersusSeat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.VersusSeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.VersusSeat & game.VersusSeat.$Shape} VersusSeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VersusSeat.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VersusSeat message.
         * @function verify
         * @memberof game.VersusSeat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VersusSeat.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                let error = $root.game.PublicPlayer.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            return null;
        };

        /**
         * Creates a VersusSeat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.VersusSeat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.VersusSeat} VersusSeat
         */
        VersusSeat.fromObject = function (object, _depth) {
            if (object instanceof $root.game.VersusSeat)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.VersusSeat: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.VersusSeat();
            if (object.player != null) {
                if (!$util.isObject(object.player))
                    throw $TypeError(".game.VersusSeat.player: object expected");
                message.player = $root.game.PublicPlayer.fromObject(object.player, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a VersusSeat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.VersusSeat
         * @static
         * @param {game.VersusSeat} message VersusSeat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VersusSeat.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.player = null;
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                object.player = $root.game.PublicPlayer.toObject(message.player, options, _depth + 1);
            return object;
        };

        /**
         * Converts this VersusSeat to JSON.
         * @function toJSON
         * @memberof game.VersusSeat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VersusSeat.prototype.toJSON = function() {
            return VersusSeat.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VersusSeat
         * @function getTypeUrl
         * @memberof game.VersusSeat
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VersusSeat.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.VersusSeat";
        };

        return VersusSeat;
    })();

    game.VersusPair = (function() {

        /**
         * Properties of a VersusPair.
         * @typedef {Object} game.VersusPair.$Properties
         * @property {string|null} [key] VersusPair key
         * @property {game.VersusSeat.$Properties|null} [value] VersusPair value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a VersusPair.
         * @memberof game
         * @interface IVersusPair
         * @augments game.VersusPair.$Properties
         * @deprecated Use game.VersusPair.$Properties instead.
         */

        /**
         * Shape of a VersusPair.
         * @typedef {game.VersusPair.$Properties} game.VersusPair.$Shape
         */

        /**
         * Constructs a new VersusPair.
         * @memberof game
         * @classdesc Represents a VersusPair.
         * @constructor
         * @param {game.VersusPair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const VersusPair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * VersusPair key.
         * @member {string} key
         * @memberof game.VersusPair
         * @instance
         */
        VersusPair.prototype.key = "";

        /**
         * VersusPair value.
         * @member {game.VersusSeat.$Properties|null|undefined} value
         * @memberof game.VersusPair
         * @instance
         */
        VersusPair.prototype.value = null;

        /**
         * Creates a new VersusPair instance using the specified properties.
         * @function create
         * @memberof game.VersusPair
         * @static
         * @param {game.VersusPair.$Properties=} [properties] Properties to set
         * @returns {game.VersusPair} VersusPair instance
         * @type {{
         *   (properties: game.VersusPair.$Shape): game.VersusPair & game.VersusPair.$Shape;
         *   (properties?: game.VersusPair.$Properties): game.VersusPair;
         * }}
         */
        VersusPair.create = function(properties) {
            return new VersusPair(properties);
        };

        /**
         * Encodes the specified VersusPair message. Does not implicitly {@link game.VersusPair.verify|verify} messages.
         * @function encode
         * @memberof game.VersusPair
         * @static
         * @param {game.VersusPair.$Properties} message VersusPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VersusPair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                $root.game.VersusSeat.encode(message.value, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VersusPair message, length delimited. Does not implicitly {@link game.VersusPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.VersusPair
         * @static
         * @param {game.VersusPair.$Properties} message VersusPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VersusPair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a VersusPair message from the specified reader or buffer.
         * @function decode
         * @memberof game.VersusPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.VersusPair & game.VersusPair.$Shape} VersusPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VersusPair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.VersusPair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value = $root.game.VersusSeat.decode(reader, reader.uint32(), $undefined, _depth + 1, message.value);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a VersusPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.VersusPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.VersusPair & game.VersusPair.$Shape} VersusPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VersusPair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VersusPair message.
         * @function verify
         * @memberof game.VersusPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VersusPair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                let error = $root.game.VersusSeat.verify(message.value, _depth + 1);
                if (error)
                    return "value." + error;
            }
            return null;
        };

        /**
         * Creates a VersusPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.VersusPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.VersusPair} VersusPair
         */
        VersusPair.fromObject = function (object, _depth) {
            if (object instanceof $root.game.VersusPair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.VersusPair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.VersusPair();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.value != null) {
                if (!$util.isObject(object.value))
                    throw $TypeError(".game.VersusPair.value: object expected");
                message.value = $root.game.VersusSeat.fromObject(object.value, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a VersusPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.VersusPair
         * @static
         * @param {game.VersusPair} message VersusPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VersusPair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = null;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = $root.game.VersusSeat.toObject(message.value, options, _depth + 1);
            return object;
        };

        /**
         * Converts this VersusPair to JSON.
         * @function toJSON
         * @memberof game.VersusPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VersusPair.prototype.toJSON = function() {
            return VersusPair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for VersusPair
         * @function getTypeUrl
         * @memberof game.VersusPair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        VersusPair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.VersusPair";
        };

        return VersusPair;
    })();

    game.LobbyRoomInfo = (function() {

        /**
         * Properties of a LobbyRoomInfo.
         * @typedef {Object} game.LobbyRoomInfo.$Properties
         * @property {string|null} [id] LobbyRoomInfo id
         * @property {string|null} [gameId] LobbyRoomInfo gameId
         * @property {string|null} [name] LobbyRoomInfo name
         * @property {boolean|null} [hasPassword] LobbyRoomInfo hasPassword
         * @property {number|null} [players] LobbyRoomInfo players
         * @property {number|null} [spectators] LobbyRoomInfo spectators
         * @property {Array.<game.VersusPair.$Properties>|null} [versus] LobbyRoomInfo versus
         * @property {string|null} [status] LobbyRoomInfo status
         * @property {string|null} [roomBackgroundImage] LobbyRoomInfo roomBackgroundImage
         * @property {boolean|null} [enablePunishment] LobbyRoomInfo enablePunishment
         * @property {Array.<string>|null} [punishmentIds] LobbyRoomInfo punishmentIds
         * @property {string|null} [punishmentId] LobbyRoomInfo punishmentId
         * @property {boolean|null} [tieDoublePunish] LobbyRoomInfo tieDoublePunish
         * @property {boolean|null} [requireOpponentConfirm] LobbyRoomInfo requireOpponentConfirm
         * @property {boolean|null} [enableRanked] LobbyRoomInfo enableRanked
         * @property {number|null} [stake] LobbyRoomInfo stake
         * @property {boolean|null} [enableRankMultiplier] LobbyRoomInfo enableRankMultiplier
         * @property {number|null} [rankMultiplier] LobbyRoomInfo rankMultiplier
         * @property {boolean|null} [enableExtremeRanked] LobbyRoomInfo enableExtremeRanked
         * @property {Array.<string>|null} [tags] LobbyRoomInfo tags
         * @property {number|null} [liarsDiceMinPlayers] LobbyRoomInfo liarsDiceMinPlayers
         * @property {number|null} [liarsDiceMaxPlayers] LobbyRoomInfo liarsDiceMaxPlayers
         * @property {number|null} [othelloMoveSeconds] LobbyRoomInfo othelloMoveSeconds
         * @property {number|null} [othelloGameMinutes] LobbyRoomInfo othelloGameMinutes
         * @property {number|null} [gomokuMoveSeconds] LobbyRoomInfo gomokuMoveSeconds
         * @property {number|null} [gomokuGameMinutes] LobbyRoomInfo gomokuGameMinutes
         * @property {number|null} [gomokuUndoLimit] LobbyRoomInfo gomokuUndoLimit
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LobbyRoomInfo.
         * @memberof game
         * @interface ILobbyRoomInfo
         * @augments game.LobbyRoomInfo.$Properties
         * @deprecated Use game.LobbyRoomInfo.$Properties instead.
         */

        /**
         * Shape of a LobbyRoomInfo.
         * @typedef {game.LobbyRoomInfo.$Properties} game.LobbyRoomInfo.$Shape
         */

        /**
         * Constructs a new LobbyRoomInfo.
         * @memberof game
         * @classdesc Represents a LobbyRoomInfo.
         * @constructor
         * @param {game.LobbyRoomInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LobbyRoomInfo = function (properties) {
            this.versus = [];
            this.punishmentIds = [];
            this.tags = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LobbyRoomInfo id.
         * @member {string} id
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.id = "";

        /**
         * LobbyRoomInfo gameId.
         * @member {string} gameId
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.gameId = "";

        /**
         * LobbyRoomInfo name.
         * @member {string} name
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.name = "";

        /**
         * LobbyRoomInfo hasPassword.
         * @member {boolean} hasPassword
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.hasPassword = false;

        /**
         * LobbyRoomInfo players.
         * @member {number} players
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.players = 0;

        /**
         * LobbyRoomInfo spectators.
         * @member {number} spectators
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.spectators = 0;

        /**
         * LobbyRoomInfo versus.
         * @member {Array.<game.VersusPair.$Properties>} versus
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.versus = $util.emptyArray;

        /**
         * LobbyRoomInfo status.
         * @member {string} status
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.status = "";

        /**
         * LobbyRoomInfo roomBackgroundImage.
         * @member {string} roomBackgroundImage
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.roomBackgroundImage = "";

        /**
         * LobbyRoomInfo enablePunishment.
         * @member {boolean} enablePunishment
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.enablePunishment = false;

        /**
         * LobbyRoomInfo punishmentIds.
         * @member {Array.<string>} punishmentIds
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.punishmentIds = $util.emptyArray;

        /**
         * LobbyRoomInfo punishmentId.
         * @member {string} punishmentId
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.punishmentId = "";

        /**
         * LobbyRoomInfo tieDoublePunish.
         * @member {boolean} tieDoublePunish
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.tieDoublePunish = false;

        /**
         * LobbyRoomInfo requireOpponentConfirm.
         * @member {boolean} requireOpponentConfirm
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.requireOpponentConfirm = false;

        /**
         * LobbyRoomInfo enableRanked.
         * @member {boolean} enableRanked
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.enableRanked = false;

        /**
         * LobbyRoomInfo stake.
         * @member {number} stake
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.stake = 0;

        /**
         * LobbyRoomInfo enableRankMultiplier.
         * @member {boolean} enableRankMultiplier
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.enableRankMultiplier = false;

        /**
         * LobbyRoomInfo rankMultiplier.
         * @member {number} rankMultiplier
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.rankMultiplier = 0;

        /**
         * LobbyRoomInfo enableExtremeRanked.
         * @member {boolean} enableExtremeRanked
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.enableExtremeRanked = false;

        /**
         * LobbyRoomInfo tags.
         * @member {Array.<string>} tags
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.tags = $util.emptyArray;

        /**
         * LobbyRoomInfo liarsDiceMinPlayers.
         * @member {number} liarsDiceMinPlayers
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.liarsDiceMinPlayers = 0;

        /**
         * LobbyRoomInfo liarsDiceMaxPlayers.
         * @member {number} liarsDiceMaxPlayers
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.liarsDiceMaxPlayers = 0;

        /**
         * LobbyRoomInfo othelloMoveSeconds.
         * @member {number} othelloMoveSeconds
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.othelloMoveSeconds = 0;

        /**
         * LobbyRoomInfo othelloGameMinutes.
         * @member {number} othelloGameMinutes
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.othelloGameMinutes = 0;

        /**
         * LobbyRoomInfo gomokuMoveSeconds.
         * @member {number} gomokuMoveSeconds
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.gomokuMoveSeconds = 0;

        /**
         * LobbyRoomInfo gomokuGameMinutes.
         * @member {number} gomokuGameMinutes
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.gomokuGameMinutes = 0;

        /**
         * LobbyRoomInfo gomokuUndoLimit.
         * @member {number} gomokuUndoLimit
         * @memberof game.LobbyRoomInfo
         * @instance
         */
        LobbyRoomInfo.prototype.gomokuUndoLimit = 0;

        /**
         * Creates a new LobbyRoomInfo instance using the specified properties.
         * @function create
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {game.LobbyRoomInfo.$Properties=} [properties] Properties to set
         * @returns {game.LobbyRoomInfo} LobbyRoomInfo instance
         * @type {{
         *   (properties: game.LobbyRoomInfo.$Shape): game.LobbyRoomInfo & game.LobbyRoomInfo.$Shape;
         *   (properties?: game.LobbyRoomInfo.$Properties): game.LobbyRoomInfo;
         * }}
         */
        LobbyRoomInfo.create = function(properties) {
            return new LobbyRoomInfo(properties);
        };

        /**
         * Encodes the specified LobbyRoomInfo message. Does not implicitly {@link game.LobbyRoomInfo.verify|verify} messages.
         * @function encode
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {game.LobbyRoomInfo.$Properties} message LobbyRoomInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyRoomInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId") && message.gameId !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.gameId);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.name);
            if (message.hasPassword != null && $Object.hasOwnProperty.call(message, "hasPassword") && message.hasPassword !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.hasPassword);
            if (message.players != null && $Object.hasOwnProperty.call(message, "players") && message.players !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.players);
            if (message.spectators != null && $Object.hasOwnProperty.call(message, "spectators") && message.spectators !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.spectators);
            if (message.versus != null && message.versus.length)
                for (let i = 0; i < message.versus.length; ++i)
                    $root.game.VersusPair.encode(message.versus[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.status);
            if (message.roomBackgroundImage != null && $Object.hasOwnProperty.call(message, "roomBackgroundImage") && message.roomBackgroundImage !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.roomBackgroundImage);
            if (message.enablePunishment != null && $Object.hasOwnProperty.call(message, "enablePunishment") && message.enablePunishment !== false)
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.enablePunishment);
            if (message.punishmentIds != null && message.punishmentIds.length)
                for (let i = 0; i < message.punishmentIds.length; ++i)
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.punishmentIds[i]);
            if (message.punishmentId != null && $Object.hasOwnProperty.call(message, "punishmentId") && message.punishmentId !== "")
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.punishmentId);
            if (message.tieDoublePunish != null && $Object.hasOwnProperty.call(message, "tieDoublePunish") && message.tieDoublePunish !== false)
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.tieDoublePunish);
            if (message.requireOpponentConfirm != null && $Object.hasOwnProperty.call(message, "requireOpponentConfirm") && message.requireOpponentConfirm !== false)
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.requireOpponentConfirm);
            if (message.enableRanked != null && $Object.hasOwnProperty.call(message, "enableRanked") && message.enableRanked !== false)
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.enableRanked);
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake") && message.stake !== 0)
                writer.uint32(/* id 19, wireType 0 =*/152).int32(message.stake);
            if (message.enableRankMultiplier != null && $Object.hasOwnProperty.call(message, "enableRankMultiplier") && message.enableRankMultiplier !== false)
                writer.uint32(/* id 20, wireType 0 =*/160).bool(message.enableRankMultiplier);
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier") && message.rankMultiplier !== 0)
                writer.uint32(/* id 21, wireType 0 =*/168).int32(message.rankMultiplier);
            if (message.enableExtremeRanked != null && $Object.hasOwnProperty.call(message, "enableExtremeRanked") && message.enableExtremeRanked !== false)
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.enableExtremeRanked);
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 23, wireType 2 =*/186).string(message.tags[i]);
            if (message.liarsDiceMinPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMinPlayers") && message.liarsDiceMinPlayers !== 0)
                writer.uint32(/* id 24, wireType 0 =*/192).int32(message.liarsDiceMinPlayers);
            if (message.liarsDiceMaxPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMaxPlayers") && message.liarsDiceMaxPlayers !== 0)
                writer.uint32(/* id 25, wireType 0 =*/200).int32(message.liarsDiceMaxPlayers);
            if (message.othelloMoveSeconds != null && $Object.hasOwnProperty.call(message, "othelloMoveSeconds") && message.othelloMoveSeconds !== 0)
                writer.uint32(/* id 26, wireType 0 =*/208).int32(message.othelloMoveSeconds);
            if (message.othelloGameMinutes != null && $Object.hasOwnProperty.call(message, "othelloGameMinutes") && message.othelloGameMinutes !== 0)
                writer.uint32(/* id 27, wireType 0 =*/216).int32(message.othelloGameMinutes);
            if (message.gomokuMoveSeconds != null && $Object.hasOwnProperty.call(message, "gomokuMoveSeconds") && message.gomokuMoveSeconds !== 0)
                writer.uint32(/* id 28, wireType 0 =*/224).int32(message.gomokuMoveSeconds);
            if (message.gomokuGameMinutes != null && $Object.hasOwnProperty.call(message, "gomokuGameMinutes") && message.gomokuGameMinutes !== 0)
                writer.uint32(/* id 29, wireType 0 =*/232).int32(message.gomokuGameMinutes);
            if (message.gomokuUndoLimit != null && $Object.hasOwnProperty.call(message, "gomokuUndoLimit") && message.gomokuUndoLimit !== 0)
                writer.uint32(/* id 30, wireType 0 =*/240).int32(message.gomokuUndoLimit);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LobbyRoomInfo message, length delimited. Does not implicitly {@link game.LobbyRoomInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {game.LobbyRoomInfo.$Properties} message LobbyRoomInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyRoomInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LobbyRoomInfo message from the specified reader or buffer.
         * @function decode
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LobbyRoomInfo & game.LobbyRoomInfo.$Shape} LobbyRoomInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyRoomInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LobbyRoomInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.gameId = value;
                        else
                            delete message.gameId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.hasPassword = value;
                        else
                            delete message.hasPassword;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.players = value;
                        else
                            delete message.players;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.spectators = value;
                        else
                            delete message.spectators;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.versus && message.versus.length))
                            message.versus = [];
                        message.versus.push($root.game.VersusPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.roomBackgroundImage = value;
                        else
                            delete message.roomBackgroundImage;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enablePunishment = value;
                        else
                            delete message.enablePunishment;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.punishmentIds && message.punishmentIds.length))
                            message.punishmentIds = [];
                        message.punishmentIds.push(reader.stringVerify());
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.punishmentId = value;
                        else
                            delete message.punishmentId;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.tieDoublePunish = value;
                        else
                            delete message.tieDoublePunish;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.requireOpponentConfirm = value;
                        else
                            delete message.requireOpponentConfirm;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enableRanked = value;
                        else
                            delete message.enableRanked;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.stake = value;
                        else
                            delete message.stake;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enableRankMultiplier = value;
                        else
                            delete message.enableRankMultiplier;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.rankMultiplier = value;
                        else
                            delete message.rankMultiplier;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enableExtremeRanked = value;
                        else
                            delete message.enableExtremeRanked;
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.stringVerify());
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.liarsDiceMinPlayers = value;
                        else
                            delete message.liarsDiceMinPlayers;
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.liarsDiceMaxPlayers = value;
                        else
                            delete message.liarsDiceMaxPlayers;
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.othelloMoveSeconds = value;
                        else
                            delete message.othelloMoveSeconds;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.othelloGameMinutes = value;
                        else
                            delete message.othelloGameMinutes;
                        continue;
                    }
                case 28: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.gomokuMoveSeconds = value;
                        else
                            delete message.gomokuMoveSeconds;
                        continue;
                    }
                case 29: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.gomokuGameMinutes = value;
                        else
                            delete message.gomokuGameMinutes;
                        continue;
                    }
                case 30: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.gomokuUndoLimit = value;
                        else
                            delete message.gomokuUndoLimit;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LobbyRoomInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LobbyRoomInfo & game.LobbyRoomInfo.$Shape} LobbyRoomInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyRoomInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LobbyRoomInfo message.
         * @function verify
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LobbyRoomInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId"))
                if (!$util.isString(message.gameId))
                    return "gameId: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.hasPassword != null && $Object.hasOwnProperty.call(message, "hasPassword"))
                if (typeof message.hasPassword !== "boolean")
                    return "hasPassword: boolean expected";
            if (message.players != null && $Object.hasOwnProperty.call(message, "players"))
                if (!$util.isInteger(message.players))
                    return "players: integer expected";
            if (message.spectators != null && $Object.hasOwnProperty.call(message, "spectators"))
                if (!$util.isInteger(message.spectators))
                    return "spectators: integer expected";
            if (message.versus != null && $Object.hasOwnProperty.call(message, "versus")) {
                if (!$Array.isArray(message.versus))
                    return "versus: array expected";
                for (let i = 0; i < message.versus.length; ++i) {
                    let error = $root.game.VersusPair.verify(message.versus[i], _depth + 1);
                    if (error)
                        return "versus." + error;
                }
            }
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.roomBackgroundImage != null && $Object.hasOwnProperty.call(message, "roomBackgroundImage"))
                if (!$util.isString(message.roomBackgroundImage))
                    return "roomBackgroundImage: string expected";
            if (message.enablePunishment != null && $Object.hasOwnProperty.call(message, "enablePunishment"))
                if (typeof message.enablePunishment !== "boolean")
                    return "enablePunishment: boolean expected";
            if (message.punishmentIds != null && $Object.hasOwnProperty.call(message, "punishmentIds")) {
                if (!$Array.isArray(message.punishmentIds))
                    return "punishmentIds: array expected";
                for (let i = 0; i < message.punishmentIds.length; ++i)
                    if (!$util.isString(message.punishmentIds[i]))
                        return "punishmentIds: string[] expected";
            }
            if (message.punishmentId != null && $Object.hasOwnProperty.call(message, "punishmentId"))
                if (!$util.isString(message.punishmentId))
                    return "punishmentId: string expected";
            if (message.tieDoublePunish != null && $Object.hasOwnProperty.call(message, "tieDoublePunish"))
                if (typeof message.tieDoublePunish !== "boolean")
                    return "tieDoublePunish: boolean expected";
            if (message.requireOpponentConfirm != null && $Object.hasOwnProperty.call(message, "requireOpponentConfirm"))
                if (typeof message.requireOpponentConfirm !== "boolean")
                    return "requireOpponentConfirm: boolean expected";
            if (message.enableRanked != null && $Object.hasOwnProperty.call(message, "enableRanked"))
                if (typeof message.enableRanked !== "boolean")
                    return "enableRanked: boolean expected";
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (!$util.isInteger(message.stake))
                    return "stake: integer expected";
            if (message.enableRankMultiplier != null && $Object.hasOwnProperty.call(message, "enableRankMultiplier"))
                if (typeof message.enableRankMultiplier !== "boolean")
                    return "enableRankMultiplier: boolean expected";
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier"))
                if (!$util.isInteger(message.rankMultiplier))
                    return "rankMultiplier: integer expected";
            if (message.enableExtremeRanked != null && $Object.hasOwnProperty.call(message, "enableExtremeRanked"))
                if (typeof message.enableExtremeRanked !== "boolean")
                    return "enableExtremeRanked: boolean expected";
            if (message.tags != null && $Object.hasOwnProperty.call(message, "tags")) {
                if (!$Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.liarsDiceMinPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMinPlayers"))
                if (!$util.isInteger(message.liarsDiceMinPlayers))
                    return "liarsDiceMinPlayers: integer expected";
            if (message.liarsDiceMaxPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMaxPlayers"))
                if (!$util.isInteger(message.liarsDiceMaxPlayers))
                    return "liarsDiceMaxPlayers: integer expected";
            if (message.othelloMoveSeconds != null && $Object.hasOwnProperty.call(message, "othelloMoveSeconds"))
                if (!$util.isInteger(message.othelloMoveSeconds))
                    return "othelloMoveSeconds: integer expected";
            if (message.othelloGameMinutes != null && $Object.hasOwnProperty.call(message, "othelloGameMinutes"))
                if (!$util.isInteger(message.othelloGameMinutes))
                    return "othelloGameMinutes: integer expected";
            if (message.gomokuMoveSeconds != null && $Object.hasOwnProperty.call(message, "gomokuMoveSeconds"))
                if (!$util.isInteger(message.gomokuMoveSeconds))
                    return "gomokuMoveSeconds: integer expected";
            if (message.gomokuGameMinutes != null && $Object.hasOwnProperty.call(message, "gomokuGameMinutes"))
                if (!$util.isInteger(message.gomokuGameMinutes))
                    return "gomokuGameMinutes: integer expected";
            if (message.gomokuUndoLimit != null && $Object.hasOwnProperty.call(message, "gomokuUndoLimit"))
                if (!$util.isInteger(message.gomokuUndoLimit))
                    return "gomokuUndoLimit: integer expected";
            return null;
        };

        /**
         * Creates a LobbyRoomInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LobbyRoomInfo} LobbyRoomInfo
         */
        LobbyRoomInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LobbyRoomInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LobbyRoomInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LobbyRoomInfo();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.gameId != null)
                if (typeof object.gameId !== "string" || object.gameId.length)
                    message.gameId = $String(object.gameId);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.hasPassword != null)
                if (object.hasPassword)
                    message.hasPassword = $Boolean(object.hasPassword);
            if (object.players != null)
                if ($Number(object.players) !== 0)
                    message.players = object.players | 0;
            if (object.spectators != null)
                if ($Number(object.spectators) !== 0)
                    message.spectators = object.spectators | 0;
            if (object.versus) {
                if (!$Array.isArray(object.versus))
                    throw $TypeError(".game.LobbyRoomInfo.versus: array expected");
                message.versus = $Array(object.versus.length);
                for (let i = 0; i < object.versus.length; ++i) {
                    if (!$util.isObject(object.versus[i]))
                        throw $TypeError(".game.LobbyRoomInfo.versus: object expected");
                    message.versus[i] = $root.game.VersusPair.fromObject(object.versus[i], _depth + 1);
                }
            }
            if (object.status != null)
                if (typeof object.status !== "string" || object.status.length)
                    message.status = $String(object.status);
            if (object.roomBackgroundImage != null)
                if (typeof object.roomBackgroundImage !== "string" || object.roomBackgroundImage.length)
                    message.roomBackgroundImage = $String(object.roomBackgroundImage);
            if (object.enablePunishment != null)
                if (object.enablePunishment)
                    message.enablePunishment = $Boolean(object.enablePunishment);
            if (object.punishmentIds) {
                if (!$Array.isArray(object.punishmentIds))
                    throw $TypeError(".game.LobbyRoomInfo.punishmentIds: array expected");
                message.punishmentIds = $Array(object.punishmentIds.length);
                for (let i = 0; i < object.punishmentIds.length; ++i)
                    message.punishmentIds[i] = $String(object.punishmentIds[i]);
            }
            if (object.punishmentId != null)
                if (typeof object.punishmentId !== "string" || object.punishmentId.length)
                    message.punishmentId = $String(object.punishmentId);
            if (object.tieDoublePunish != null)
                if (object.tieDoublePunish)
                    message.tieDoublePunish = $Boolean(object.tieDoublePunish);
            if (object.requireOpponentConfirm != null)
                if (object.requireOpponentConfirm)
                    message.requireOpponentConfirm = $Boolean(object.requireOpponentConfirm);
            if (object.enableRanked != null)
                if (object.enableRanked)
                    message.enableRanked = $Boolean(object.enableRanked);
            if (object.stake != null)
                if ($Number(object.stake) !== 0)
                    message.stake = object.stake | 0;
            if (object.enableRankMultiplier != null)
                if (object.enableRankMultiplier)
                    message.enableRankMultiplier = $Boolean(object.enableRankMultiplier);
            if (object.rankMultiplier != null)
                if ($Number(object.rankMultiplier) !== 0)
                    message.rankMultiplier = object.rankMultiplier | 0;
            if (object.enableExtremeRanked != null)
                if (object.enableExtremeRanked)
                    message.enableExtremeRanked = $Boolean(object.enableExtremeRanked);
            if (object.tags) {
                if (!$Array.isArray(object.tags))
                    throw $TypeError(".game.LobbyRoomInfo.tags: array expected");
                message.tags = $Array(object.tags.length);
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = $String(object.tags[i]);
            }
            if (object.liarsDiceMinPlayers != null)
                if ($Number(object.liarsDiceMinPlayers) !== 0)
                    message.liarsDiceMinPlayers = object.liarsDiceMinPlayers | 0;
            if (object.liarsDiceMaxPlayers != null)
                if ($Number(object.liarsDiceMaxPlayers) !== 0)
                    message.liarsDiceMaxPlayers = object.liarsDiceMaxPlayers | 0;
            if (object.othelloMoveSeconds != null)
                if ($Number(object.othelloMoveSeconds) !== 0)
                    message.othelloMoveSeconds = object.othelloMoveSeconds | 0;
            if (object.othelloGameMinutes != null)
                if ($Number(object.othelloGameMinutes) !== 0)
                    message.othelloGameMinutes = object.othelloGameMinutes | 0;
            if (object.gomokuMoveSeconds != null)
                if ($Number(object.gomokuMoveSeconds) !== 0)
                    message.gomokuMoveSeconds = object.gomokuMoveSeconds | 0;
            if (object.gomokuGameMinutes != null)
                if ($Number(object.gomokuGameMinutes) !== 0)
                    message.gomokuGameMinutes = object.gomokuGameMinutes | 0;
            if (object.gomokuUndoLimit != null)
                if ($Number(object.gomokuUndoLimit) !== 0)
                    message.gomokuUndoLimit = object.gomokuUndoLimit | 0;
            return message;
        };

        /**
         * Creates a plain object from a LobbyRoomInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {game.LobbyRoomInfo} message LobbyRoomInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LobbyRoomInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.versus = [];
                object.punishmentIds = [];
                object.tags = [];
            }
            if (options.defaults) {
                object.id = "";
                object.gameId = "";
                object.name = "";
                object.hasPassword = false;
                object.players = 0;
                object.spectators = 0;
                object.status = "";
                object.roomBackgroundImage = "";
                object.enablePunishment = false;
                object.punishmentId = "";
                object.tieDoublePunish = false;
                object.requireOpponentConfirm = false;
                object.enableRanked = false;
                object.stake = 0;
                object.enableRankMultiplier = false;
                object.rankMultiplier = 0;
                object.enableExtremeRanked = false;
                object.liarsDiceMinPlayers = 0;
                object.liarsDiceMaxPlayers = 0;
                object.othelloMoveSeconds = 0;
                object.othelloGameMinutes = 0;
                object.gomokuMoveSeconds = 0;
                object.gomokuGameMinutes = 0;
                object.gomokuUndoLimit = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.gameId != null && $Object.hasOwnProperty.call(message, "gameId"))
                object.gameId = message.gameId;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.hasPassword != null && $Object.hasOwnProperty.call(message, "hasPassword"))
                object.hasPassword = message.hasPassword;
            if (message.players != null && $Object.hasOwnProperty.call(message, "players"))
                object.players = message.players;
            if (message.spectators != null && $Object.hasOwnProperty.call(message, "spectators"))
                object.spectators = message.spectators;
            if (message.versus && message.versus.length) {
                object.versus = $Array(message.versus.length);
                for (let j = 0; j < message.versus.length; ++j)
                    object.versus[j] = $root.game.VersusPair.toObject(message.versus[j], options, _depth + 1);
            }
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            if (message.roomBackgroundImage != null && $Object.hasOwnProperty.call(message, "roomBackgroundImage"))
                object.roomBackgroundImage = message.roomBackgroundImage;
            if (message.enablePunishment != null && $Object.hasOwnProperty.call(message, "enablePunishment"))
                object.enablePunishment = message.enablePunishment;
            if (message.punishmentIds && message.punishmentIds.length) {
                object.punishmentIds = $Array(message.punishmentIds.length);
                for (let j = 0; j < message.punishmentIds.length; ++j)
                    object.punishmentIds[j] = message.punishmentIds[j];
            }
            if (message.punishmentId != null && $Object.hasOwnProperty.call(message, "punishmentId"))
                object.punishmentId = message.punishmentId;
            if (message.tieDoublePunish != null && $Object.hasOwnProperty.call(message, "tieDoublePunish"))
                object.tieDoublePunish = message.tieDoublePunish;
            if (message.requireOpponentConfirm != null && $Object.hasOwnProperty.call(message, "requireOpponentConfirm"))
                object.requireOpponentConfirm = message.requireOpponentConfirm;
            if (message.enableRanked != null && $Object.hasOwnProperty.call(message, "enableRanked"))
                object.enableRanked = message.enableRanked;
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                object.stake = message.stake;
            if (message.enableRankMultiplier != null && $Object.hasOwnProperty.call(message, "enableRankMultiplier"))
                object.enableRankMultiplier = message.enableRankMultiplier;
            if (message.rankMultiplier != null && $Object.hasOwnProperty.call(message, "rankMultiplier"))
                object.rankMultiplier = message.rankMultiplier;
            if (message.enableExtremeRanked != null && $Object.hasOwnProperty.call(message, "enableExtremeRanked"))
                object.enableExtremeRanked = message.enableExtremeRanked;
            if (message.tags && message.tags.length) {
                object.tags = $Array(message.tags.length);
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.liarsDiceMinPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMinPlayers"))
                object.liarsDiceMinPlayers = message.liarsDiceMinPlayers;
            if (message.liarsDiceMaxPlayers != null && $Object.hasOwnProperty.call(message, "liarsDiceMaxPlayers"))
                object.liarsDiceMaxPlayers = message.liarsDiceMaxPlayers;
            if (message.othelloMoveSeconds != null && $Object.hasOwnProperty.call(message, "othelloMoveSeconds"))
                object.othelloMoveSeconds = message.othelloMoveSeconds;
            if (message.othelloGameMinutes != null && $Object.hasOwnProperty.call(message, "othelloGameMinutes"))
                object.othelloGameMinutes = message.othelloGameMinutes;
            if (message.gomokuMoveSeconds != null && $Object.hasOwnProperty.call(message, "gomokuMoveSeconds"))
                object.gomokuMoveSeconds = message.gomokuMoveSeconds;
            if (message.gomokuGameMinutes != null && $Object.hasOwnProperty.call(message, "gomokuGameMinutes"))
                object.gomokuGameMinutes = message.gomokuGameMinutes;
            if (message.gomokuUndoLimit != null && $Object.hasOwnProperty.call(message, "gomokuUndoLimit"))
                object.gomokuUndoLimit = message.gomokuUndoLimit;
            return object;
        };

        /**
         * Converts this LobbyRoomInfo to JSON.
         * @function toJSON
         * @memberof game.LobbyRoomInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LobbyRoomInfo.prototype.toJSON = function() {
            return LobbyRoomInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LobbyRoomInfo
         * @function getTypeUrl
         * @memberof game.LobbyRoomInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LobbyRoomInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LobbyRoomInfo";
        };

        return LobbyRoomInfo;
    })();

    game.LobbyPlayerEntry = (function() {

        /**
         * Properties of a LobbyPlayerEntry.
         * @typedef {Object} game.LobbyPlayerEntry.$Properties
         * @property {string|null} [id] LobbyPlayerEntry id
         * @property {game.LobbyPlayer.$Properties|null} [player] LobbyPlayerEntry player
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LobbyPlayerEntry.
         * @memberof game
         * @interface ILobbyPlayerEntry
         * @augments game.LobbyPlayerEntry.$Properties
         * @deprecated Use game.LobbyPlayerEntry.$Properties instead.
         */

        /**
         * Shape of a LobbyPlayerEntry.
         * @typedef {game.LobbyPlayerEntry.$Properties} game.LobbyPlayerEntry.$Shape
         */

        /**
         * Constructs a new LobbyPlayerEntry.
         * @memberof game
         * @classdesc Represents a LobbyPlayerEntry.
         * @constructor
         * @param {game.LobbyPlayerEntry.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LobbyPlayerEntry = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LobbyPlayerEntry id.
         * @member {string} id
         * @memberof game.LobbyPlayerEntry
         * @instance
         */
        LobbyPlayerEntry.prototype.id = "";

        /**
         * LobbyPlayerEntry player.
         * @member {game.LobbyPlayer.$Properties|null|undefined} player
         * @memberof game.LobbyPlayerEntry
         * @instance
         */
        LobbyPlayerEntry.prototype.player = null;

        /**
         * Creates a new LobbyPlayerEntry instance using the specified properties.
         * @function create
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {game.LobbyPlayerEntry.$Properties=} [properties] Properties to set
         * @returns {game.LobbyPlayerEntry} LobbyPlayerEntry instance
         * @type {{
         *   (properties: game.LobbyPlayerEntry.$Shape): game.LobbyPlayerEntry & game.LobbyPlayerEntry.$Shape;
         *   (properties?: game.LobbyPlayerEntry.$Properties): game.LobbyPlayerEntry;
         * }}
         */
        LobbyPlayerEntry.create = function(properties) {
            return new LobbyPlayerEntry(properties);
        };

        /**
         * Encodes the specified LobbyPlayerEntry message. Does not implicitly {@link game.LobbyPlayerEntry.verify|verify} messages.
         * @function encode
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {game.LobbyPlayerEntry.$Properties} message LobbyPlayerEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyPlayerEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.game.LobbyPlayer.encode(message.player, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LobbyPlayerEntry message, length delimited. Does not implicitly {@link game.LobbyPlayerEntry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {game.LobbyPlayerEntry.$Properties} message LobbyPlayerEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyPlayerEntry.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LobbyPlayerEntry message from the specified reader or buffer.
         * @function decode
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LobbyPlayerEntry & game.LobbyPlayerEntry.$Shape} LobbyPlayerEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyPlayerEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LobbyPlayerEntry(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.game.LobbyPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LobbyPlayerEntry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LobbyPlayerEntry & game.LobbyPlayerEntry.$Shape} LobbyPlayerEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyPlayerEntry.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LobbyPlayerEntry message.
         * @function verify
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LobbyPlayerEntry.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                let error = $root.game.LobbyPlayer.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            return null;
        };

        /**
         * Creates a LobbyPlayerEntry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LobbyPlayerEntry} LobbyPlayerEntry
         */
        LobbyPlayerEntry.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LobbyPlayerEntry)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LobbyPlayerEntry: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LobbyPlayerEntry();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.player != null) {
                if (!$util.isObject(object.player))
                    throw $TypeError(".game.LobbyPlayerEntry.player: object expected");
                message.player = $root.game.LobbyPlayer.fromObject(object.player, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LobbyPlayerEntry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {game.LobbyPlayerEntry} message LobbyPlayerEntry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LobbyPlayerEntry.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.player = null;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                object.player = $root.game.LobbyPlayer.toObject(message.player, options, _depth + 1);
            return object;
        };

        /**
         * Converts this LobbyPlayerEntry to JSON.
         * @function toJSON
         * @memberof game.LobbyPlayerEntry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LobbyPlayerEntry.prototype.toJSON = function() {
            return LobbyPlayerEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LobbyPlayerEntry
         * @function getTypeUrl
         * @memberof game.LobbyPlayerEntry
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LobbyPlayerEntry.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LobbyPlayerEntry";
        };

        return LobbyPlayerEntry;
    })();

    game.LobbyRoomEntry = (function() {

        /**
         * Properties of a LobbyRoomEntry.
         * @typedef {Object} game.LobbyRoomEntry.$Properties
         * @property {string|null} [id] LobbyRoomEntry id
         * @property {game.LobbyRoomInfo.$Properties|null} [room] LobbyRoomEntry room
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LobbyRoomEntry.
         * @memberof game
         * @interface ILobbyRoomEntry
         * @augments game.LobbyRoomEntry.$Properties
         * @deprecated Use game.LobbyRoomEntry.$Properties instead.
         */

        /**
         * Shape of a LobbyRoomEntry.
         * @typedef {game.LobbyRoomEntry.$Properties} game.LobbyRoomEntry.$Shape
         */

        /**
         * Constructs a new LobbyRoomEntry.
         * @memberof game
         * @classdesc Represents a LobbyRoomEntry.
         * @constructor
         * @param {game.LobbyRoomEntry.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LobbyRoomEntry = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LobbyRoomEntry id.
         * @member {string} id
         * @memberof game.LobbyRoomEntry
         * @instance
         */
        LobbyRoomEntry.prototype.id = "";

        /**
         * LobbyRoomEntry room.
         * @member {game.LobbyRoomInfo.$Properties|null|undefined} room
         * @memberof game.LobbyRoomEntry
         * @instance
         */
        LobbyRoomEntry.prototype.room = null;

        /**
         * Creates a new LobbyRoomEntry instance using the specified properties.
         * @function create
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {game.LobbyRoomEntry.$Properties=} [properties] Properties to set
         * @returns {game.LobbyRoomEntry} LobbyRoomEntry instance
         * @type {{
         *   (properties: game.LobbyRoomEntry.$Shape): game.LobbyRoomEntry & game.LobbyRoomEntry.$Shape;
         *   (properties?: game.LobbyRoomEntry.$Properties): game.LobbyRoomEntry;
         * }}
         */
        LobbyRoomEntry.create = function(properties) {
            return new LobbyRoomEntry(properties);
        };

        /**
         * Encodes the specified LobbyRoomEntry message. Does not implicitly {@link game.LobbyRoomEntry.verify|verify} messages.
         * @function encode
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {game.LobbyRoomEntry.$Properties} message LobbyRoomEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyRoomEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.room != null && $Object.hasOwnProperty.call(message, "room"))
                $root.game.LobbyRoomInfo.encode(message.room, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LobbyRoomEntry message, length delimited. Does not implicitly {@link game.LobbyRoomEntry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {game.LobbyRoomEntry.$Properties} message LobbyRoomEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbyRoomEntry.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LobbyRoomEntry message from the specified reader or buffer.
         * @function decode
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LobbyRoomEntry & game.LobbyRoomEntry.$Shape} LobbyRoomEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyRoomEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LobbyRoomEntry(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.game.LobbyRoomInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.room);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LobbyRoomEntry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LobbyRoomEntry & game.LobbyRoomEntry.$Shape} LobbyRoomEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbyRoomEntry.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LobbyRoomEntry message.
         * @function verify
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LobbyRoomEntry.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.room != null && $Object.hasOwnProperty.call(message, "room")) {
                let error = $root.game.LobbyRoomInfo.verify(message.room, _depth + 1);
                if (error)
                    return "room." + error;
            }
            return null;
        };

        /**
         * Creates a LobbyRoomEntry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LobbyRoomEntry} LobbyRoomEntry
         */
        LobbyRoomEntry.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LobbyRoomEntry)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LobbyRoomEntry: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LobbyRoomEntry();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.room != null) {
                if (!$util.isObject(object.room))
                    throw $TypeError(".game.LobbyRoomEntry.room: object expected");
                message.room = $root.game.LobbyRoomInfo.fromObject(object.room, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LobbyRoomEntry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {game.LobbyRoomEntry} message LobbyRoomEntry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LobbyRoomEntry.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.room = null;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.room != null && $Object.hasOwnProperty.call(message, "room"))
                object.room = $root.game.LobbyRoomInfo.toObject(message.room, options, _depth + 1);
            return object;
        };

        /**
         * Converts this LobbyRoomEntry to JSON.
         * @function toJSON
         * @memberof game.LobbyRoomEntry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LobbyRoomEntry.prototype.toJSON = function() {
            return LobbyRoomEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LobbyRoomEntry
         * @function getTypeUrl
         * @memberof game.LobbyRoomEntry
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LobbyRoomEntry.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LobbyRoomEntry";
        };

        return LobbyRoomEntry;
    })();

    game.LobbySnapshot = (function() {

        /**
         * Properties of a LobbySnapshot.
         * @typedef {Object} game.LobbySnapshot.$Properties
         * @property {game.AppConfig.$Properties|null} [config] LobbySnapshot config
         * @property {number|null} [onlineCount] LobbySnapshot onlineCount
         * @property {Array.<game.LobbyPlayerEntry.$Properties>|null} [players] LobbySnapshot players
         * @property {Array.<game.LobbyRoomEntry.$Properties>|null} [rooms] LobbySnapshot rooms
         * @property {Array.<game.LobbyPlayer.$Properties>|null} [normalLeaderboard] LobbySnapshot normalLeaderboard
         * @property {Array.<game.LobbyPlayer.$Properties>|null} [rankedLeaderboard] LobbySnapshot rankedLeaderboard
         * @property {Array.<game.Suggestion.$Properties>|null} [suggestions] LobbySnapshot suggestions
         * @property {Array.<game.ChatMessage.$Properties>|null} [lobbyChat] LobbySnapshot lobbyChat
         * @property {game.ServerStats.$Properties|null} [serverStats] LobbySnapshot serverStats
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LobbySnapshot.
         * @memberof game
         * @interface ILobbySnapshot
         * @augments game.LobbySnapshot.$Properties
         * @deprecated Use game.LobbySnapshot.$Properties instead.
         */

        /**
         * Shape of a LobbySnapshot.
         * @typedef {game.LobbySnapshot.$Properties} game.LobbySnapshot.$Shape
         */

        /**
         * Constructs a new LobbySnapshot.
         * @memberof game
         * @classdesc Represents a LobbySnapshot.
         * @constructor
         * @param {game.LobbySnapshot.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LobbySnapshot = function (properties) {
            this.players = [];
            this.rooms = [];
            this.normalLeaderboard = [];
            this.rankedLeaderboard = [];
            this.suggestions = [];
            this.lobbyChat = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * LobbySnapshot config.
         * @member {game.AppConfig.$Properties|null|undefined} config
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.config = null;

        /**
         * LobbySnapshot onlineCount.
         * @member {number} onlineCount
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.onlineCount = 0;

        /**
         * LobbySnapshot players.
         * @member {Array.<game.LobbyPlayerEntry.$Properties>} players
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.players = $util.emptyArray;

        /**
         * LobbySnapshot rooms.
         * @member {Array.<game.LobbyRoomEntry.$Properties>} rooms
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.rooms = $util.emptyArray;

        /**
         * LobbySnapshot normalLeaderboard.
         * @member {Array.<game.LobbyPlayer.$Properties>} normalLeaderboard
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.normalLeaderboard = $util.emptyArray;

        /**
         * LobbySnapshot rankedLeaderboard.
         * @member {Array.<game.LobbyPlayer.$Properties>} rankedLeaderboard
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.rankedLeaderboard = $util.emptyArray;

        /**
         * LobbySnapshot suggestions.
         * @member {Array.<game.Suggestion.$Properties>} suggestions
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.suggestions = $util.emptyArray;

        /**
         * LobbySnapshot lobbyChat.
         * @member {Array.<game.ChatMessage.$Properties>} lobbyChat
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.lobbyChat = $util.emptyArray;

        /**
         * LobbySnapshot serverStats.
         * @member {game.ServerStats.$Properties|null|undefined} serverStats
         * @memberof game.LobbySnapshot
         * @instance
         */
        LobbySnapshot.prototype.serverStats = null;

        /**
         * Creates a new LobbySnapshot instance using the specified properties.
         * @function create
         * @memberof game.LobbySnapshot
         * @static
         * @param {game.LobbySnapshot.$Properties=} [properties] Properties to set
         * @returns {game.LobbySnapshot} LobbySnapshot instance
         * @type {{
         *   (properties: game.LobbySnapshot.$Shape): game.LobbySnapshot & game.LobbySnapshot.$Shape;
         *   (properties?: game.LobbySnapshot.$Properties): game.LobbySnapshot;
         * }}
         */
        LobbySnapshot.create = function(properties) {
            return new LobbySnapshot(properties);
        };

        /**
         * Encodes the specified LobbySnapshot message. Does not implicitly {@link game.LobbySnapshot.verify|verify} messages.
         * @function encode
         * @memberof game.LobbySnapshot
         * @static
         * @param {game.LobbySnapshot.$Properties} message LobbySnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbySnapshot.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                $root.game.AppConfig.encode(message.config, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.onlineCount != null && $Object.hasOwnProperty.call(message, "onlineCount") && message.onlineCount !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.onlineCount);
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.game.LobbyPlayerEntry.encode(message.players[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.rooms != null && message.rooms.length)
                for (let i = 0; i < message.rooms.length; ++i)
                    $root.game.LobbyRoomEntry.encode(message.rooms[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.normalLeaderboard != null && message.normalLeaderboard.length)
                for (let i = 0; i < message.normalLeaderboard.length; ++i)
                    $root.game.LobbyPlayer.encode(message.normalLeaderboard[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.rankedLeaderboard != null && message.rankedLeaderboard.length)
                for (let i = 0; i < message.rankedLeaderboard.length; ++i)
                    $root.game.LobbyPlayer.encode(message.rankedLeaderboard[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.suggestions != null && message.suggestions.length)
                for (let i = 0; i < message.suggestions.length; ++i)
                    $root.game.Suggestion.encode(message.suggestions[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.lobbyChat != null && message.lobbyChat.length)
                for (let i = 0; i < message.lobbyChat.length; ++i)
                    $root.game.ChatMessage.encode(message.lobbyChat[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.serverStats != null && $Object.hasOwnProperty.call(message, "serverStats"))
                $root.game.ServerStats.encode(message.serverStats, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LobbySnapshot message, length delimited. Does not implicitly {@link game.LobbySnapshot.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.LobbySnapshot
         * @static
         * @param {game.LobbySnapshot.$Properties} message LobbySnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LobbySnapshot.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LobbySnapshot message from the specified reader or buffer.
         * @function decode
         * @memberof game.LobbySnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.LobbySnapshot & game.LobbySnapshot.$Shape} LobbySnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbySnapshot.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.LobbySnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.config = $root.game.AppConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.onlineCount = value;
                        else
                            delete message.onlineCount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.game.LobbyPlayerEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rooms && message.rooms.length))
                            message.rooms = [];
                        message.rooms.push($root.game.LobbyRoomEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.normalLeaderboard && message.normalLeaderboard.length))
                            message.normalLeaderboard = [];
                        message.normalLeaderboard.push($root.game.LobbyPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rankedLeaderboard && message.rankedLeaderboard.length))
                            message.rankedLeaderboard = [];
                        message.rankedLeaderboard.push($root.game.LobbyPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.suggestions && message.suggestions.length))
                            message.suggestions = [];
                        message.suggestions.push($root.game.Suggestion.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.lobbyChat && message.lobbyChat.length))
                            message.lobbyChat = [];
                        message.lobbyChat.push($root.game.ChatMessage.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.serverStats = $root.game.ServerStats.decode(reader, reader.uint32(), $undefined, _depth + 1, message.serverStats);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LobbySnapshot message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.LobbySnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.LobbySnapshot & game.LobbySnapshot.$Shape} LobbySnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LobbySnapshot.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LobbySnapshot message.
         * @function verify
         * @memberof game.LobbySnapshot
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LobbySnapshot.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                let error = $root.game.AppConfig.verify(message.config, _depth + 1);
                if (error)
                    return "config." + error;
            }
            if (message.onlineCount != null && $Object.hasOwnProperty.call(message, "onlineCount"))
                if (!$util.isInteger(message.onlineCount))
                    return "onlineCount: integer expected";
            if (message.players != null && $Object.hasOwnProperty.call(message, "players")) {
                if (!$Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.game.LobbyPlayerEntry.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.rooms != null && $Object.hasOwnProperty.call(message, "rooms")) {
                if (!$Array.isArray(message.rooms))
                    return "rooms: array expected";
                for (let i = 0; i < message.rooms.length; ++i) {
                    let error = $root.game.LobbyRoomEntry.verify(message.rooms[i], _depth + 1);
                    if (error)
                        return "rooms." + error;
                }
            }
            if (message.normalLeaderboard != null && $Object.hasOwnProperty.call(message, "normalLeaderboard")) {
                if (!$Array.isArray(message.normalLeaderboard))
                    return "normalLeaderboard: array expected";
                for (let i = 0; i < message.normalLeaderboard.length; ++i) {
                    let error = $root.game.LobbyPlayer.verify(message.normalLeaderboard[i], _depth + 1);
                    if (error)
                        return "normalLeaderboard." + error;
                }
            }
            if (message.rankedLeaderboard != null && $Object.hasOwnProperty.call(message, "rankedLeaderboard")) {
                if (!$Array.isArray(message.rankedLeaderboard))
                    return "rankedLeaderboard: array expected";
                for (let i = 0; i < message.rankedLeaderboard.length; ++i) {
                    let error = $root.game.LobbyPlayer.verify(message.rankedLeaderboard[i], _depth + 1);
                    if (error)
                        return "rankedLeaderboard." + error;
                }
            }
            if (message.suggestions != null && $Object.hasOwnProperty.call(message, "suggestions")) {
                if (!$Array.isArray(message.suggestions))
                    return "suggestions: array expected";
                for (let i = 0; i < message.suggestions.length; ++i) {
                    let error = $root.game.Suggestion.verify(message.suggestions[i], _depth + 1);
                    if (error)
                        return "suggestions." + error;
                }
            }
            if (message.lobbyChat != null && $Object.hasOwnProperty.call(message, "lobbyChat")) {
                if (!$Array.isArray(message.lobbyChat))
                    return "lobbyChat: array expected";
                for (let i = 0; i < message.lobbyChat.length; ++i) {
                    let error = $root.game.ChatMessage.verify(message.lobbyChat[i], _depth + 1);
                    if (error)
                        return "lobbyChat." + error;
                }
            }
            if (message.serverStats != null && $Object.hasOwnProperty.call(message, "serverStats")) {
                let error = $root.game.ServerStats.verify(message.serverStats, _depth + 1);
                if (error)
                    return "serverStats." + error;
            }
            return null;
        };

        /**
         * Creates a LobbySnapshot message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.LobbySnapshot
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.LobbySnapshot} LobbySnapshot
         */
        LobbySnapshot.fromObject = function (object, _depth) {
            if (object instanceof $root.game.LobbySnapshot)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.LobbySnapshot: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.LobbySnapshot();
            if (object.config != null) {
                if (!$util.isObject(object.config))
                    throw $TypeError(".game.LobbySnapshot.config: object expected");
                message.config = $root.game.AppConfig.fromObject(object.config, _depth + 1);
            }
            if (object.onlineCount != null)
                if ($Number(object.onlineCount) !== 0)
                    message.onlineCount = object.onlineCount | 0;
            if (object.players) {
                if (!$Array.isArray(object.players))
                    throw $TypeError(".game.LobbySnapshot.players: array expected");
                message.players = $Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (!$util.isObject(object.players[i]))
                        throw $TypeError(".game.LobbySnapshot.players: object expected");
                    message.players[i] = $root.game.LobbyPlayerEntry.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.rooms) {
                if (!$Array.isArray(object.rooms))
                    throw $TypeError(".game.LobbySnapshot.rooms: array expected");
                message.rooms = $Array(object.rooms.length);
                for (let i = 0; i < object.rooms.length; ++i) {
                    if (!$util.isObject(object.rooms[i]))
                        throw $TypeError(".game.LobbySnapshot.rooms: object expected");
                    message.rooms[i] = $root.game.LobbyRoomEntry.fromObject(object.rooms[i], _depth + 1);
                }
            }
            if (object.normalLeaderboard) {
                if (!$Array.isArray(object.normalLeaderboard))
                    throw $TypeError(".game.LobbySnapshot.normalLeaderboard: array expected");
                message.normalLeaderboard = $Array(object.normalLeaderboard.length);
                for (let i = 0; i < object.normalLeaderboard.length; ++i) {
                    if (!$util.isObject(object.normalLeaderboard[i]))
                        throw $TypeError(".game.LobbySnapshot.normalLeaderboard: object expected");
                    message.normalLeaderboard[i] = $root.game.LobbyPlayer.fromObject(object.normalLeaderboard[i], _depth + 1);
                }
            }
            if (object.rankedLeaderboard) {
                if (!$Array.isArray(object.rankedLeaderboard))
                    throw $TypeError(".game.LobbySnapshot.rankedLeaderboard: array expected");
                message.rankedLeaderboard = $Array(object.rankedLeaderboard.length);
                for (let i = 0; i < object.rankedLeaderboard.length; ++i) {
                    if (!$util.isObject(object.rankedLeaderboard[i]))
                        throw $TypeError(".game.LobbySnapshot.rankedLeaderboard: object expected");
                    message.rankedLeaderboard[i] = $root.game.LobbyPlayer.fromObject(object.rankedLeaderboard[i], _depth + 1);
                }
            }
            if (object.suggestions) {
                if (!$Array.isArray(object.suggestions))
                    throw $TypeError(".game.LobbySnapshot.suggestions: array expected");
                message.suggestions = $Array(object.suggestions.length);
                for (let i = 0; i < object.suggestions.length; ++i) {
                    if (!$util.isObject(object.suggestions[i]))
                        throw $TypeError(".game.LobbySnapshot.suggestions: object expected");
                    message.suggestions[i] = $root.game.Suggestion.fromObject(object.suggestions[i], _depth + 1);
                }
            }
            if (object.lobbyChat) {
                if (!$Array.isArray(object.lobbyChat))
                    throw $TypeError(".game.LobbySnapshot.lobbyChat: array expected");
                message.lobbyChat = $Array(object.lobbyChat.length);
                for (let i = 0; i < object.lobbyChat.length; ++i) {
                    if (!$util.isObject(object.lobbyChat[i]))
                        throw $TypeError(".game.LobbySnapshot.lobbyChat: object expected");
                    message.lobbyChat[i] = $root.game.ChatMessage.fromObject(object.lobbyChat[i], _depth + 1);
                }
            }
            if (object.serverStats != null) {
                if (!$util.isObject(object.serverStats))
                    throw $TypeError(".game.LobbySnapshot.serverStats: object expected");
                message.serverStats = $root.game.ServerStats.fromObject(object.serverStats, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LobbySnapshot message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.LobbySnapshot
         * @static
         * @param {game.LobbySnapshot} message LobbySnapshot
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LobbySnapshot.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.players = [];
                object.rooms = [];
                object.normalLeaderboard = [];
                object.rankedLeaderboard = [];
                object.suggestions = [];
                object.lobbyChat = [];
            }
            if (options.defaults) {
                object.config = null;
                object.onlineCount = 0;
                object.serverStats = null;
            }
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                object.config = $root.game.AppConfig.toObject(message.config, options, _depth + 1);
            if (message.onlineCount != null && $Object.hasOwnProperty.call(message, "onlineCount"))
                object.onlineCount = message.onlineCount;
            if (message.players && message.players.length) {
                object.players = $Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.game.LobbyPlayerEntry.toObject(message.players[j], options, _depth + 1);
            }
            if (message.rooms && message.rooms.length) {
                object.rooms = $Array(message.rooms.length);
                for (let j = 0; j < message.rooms.length; ++j)
                    object.rooms[j] = $root.game.LobbyRoomEntry.toObject(message.rooms[j], options, _depth + 1);
            }
            if (message.normalLeaderboard && message.normalLeaderboard.length) {
                object.normalLeaderboard = $Array(message.normalLeaderboard.length);
                for (let j = 0; j < message.normalLeaderboard.length; ++j)
                    object.normalLeaderboard[j] = $root.game.LobbyPlayer.toObject(message.normalLeaderboard[j], options, _depth + 1);
            }
            if (message.rankedLeaderboard && message.rankedLeaderboard.length) {
                object.rankedLeaderboard = $Array(message.rankedLeaderboard.length);
                for (let j = 0; j < message.rankedLeaderboard.length; ++j)
                    object.rankedLeaderboard[j] = $root.game.LobbyPlayer.toObject(message.rankedLeaderboard[j], options, _depth + 1);
            }
            if (message.suggestions && message.suggestions.length) {
                object.suggestions = $Array(message.suggestions.length);
                for (let j = 0; j < message.suggestions.length; ++j)
                    object.suggestions[j] = $root.game.Suggestion.toObject(message.suggestions[j], options, _depth + 1);
            }
            if (message.lobbyChat && message.lobbyChat.length) {
                object.lobbyChat = $Array(message.lobbyChat.length);
                for (let j = 0; j < message.lobbyChat.length; ++j)
                    object.lobbyChat[j] = $root.game.ChatMessage.toObject(message.lobbyChat[j], options, _depth + 1);
            }
            if (message.serverStats != null && $Object.hasOwnProperty.call(message, "serverStats"))
                object.serverStats = $root.game.ServerStats.toObject(message.serverStats, options, _depth + 1);
            return object;
        };

        /**
         * Converts this LobbySnapshot to JSON.
         * @function toJSON
         * @memberof game.LobbySnapshot
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LobbySnapshot.prototype.toJSON = function() {
            return LobbySnapshot.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LobbySnapshot
         * @function getTypeUrl
         * @memberof game.LobbySnapshot
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LobbySnapshot.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.LobbySnapshot";
        };

        return LobbySnapshot;
    })();

    game.RoomNamePool = (function() {

        /**
         * Properties of a RoomNamePool.
         * @typedef {Object} game.RoomNamePool.$Properties
         * @property {Array.<string>|null} [adjectives] RoomNamePool adjectives
         * @property {Array.<string>|null} [subjects] RoomNamePool subjects
         * @property {Array.<string>|null} [roomWords] RoomNamePool roomWords
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomNamePool.
         * @memberof game
         * @interface IRoomNamePool
         * @augments game.RoomNamePool.$Properties
         * @deprecated Use game.RoomNamePool.$Properties instead.
         */

        /**
         * Shape of a RoomNamePool.
         * @typedef {game.RoomNamePool.$Properties} game.RoomNamePool.$Shape
         */

        /**
         * Constructs a new RoomNamePool.
         * @memberof game
         * @classdesc Represents a RoomNamePool.
         * @constructor
         * @param {game.RoomNamePool.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomNamePool = function (properties) {
            this.adjectives = [];
            this.subjects = [];
            this.roomWords = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomNamePool adjectives.
         * @member {Array.<string>} adjectives
         * @memberof game.RoomNamePool
         * @instance
         */
        RoomNamePool.prototype.adjectives = $util.emptyArray;

        /**
         * RoomNamePool subjects.
         * @member {Array.<string>} subjects
         * @memberof game.RoomNamePool
         * @instance
         */
        RoomNamePool.prototype.subjects = $util.emptyArray;

        /**
         * RoomNamePool roomWords.
         * @member {Array.<string>} roomWords
         * @memberof game.RoomNamePool
         * @instance
         */
        RoomNamePool.prototype.roomWords = $util.emptyArray;

        /**
         * Creates a new RoomNamePool instance using the specified properties.
         * @function create
         * @memberof game.RoomNamePool
         * @static
         * @param {game.RoomNamePool.$Properties=} [properties] Properties to set
         * @returns {game.RoomNamePool} RoomNamePool instance
         * @type {{
         *   (properties: game.RoomNamePool.$Shape): game.RoomNamePool & game.RoomNamePool.$Shape;
         *   (properties?: game.RoomNamePool.$Properties): game.RoomNamePool;
         * }}
         */
        RoomNamePool.create = function(properties) {
            return new RoomNamePool(properties);
        };

        /**
         * Encodes the specified RoomNamePool message. Does not implicitly {@link game.RoomNamePool.verify|verify} messages.
         * @function encode
         * @memberof game.RoomNamePool
         * @static
         * @param {game.RoomNamePool.$Properties} message RoomNamePool message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomNamePool.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.adjectives != null && message.adjectives.length)
                for (let i = 0; i < message.adjectives.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.adjectives[i]);
            if (message.subjects != null && message.subjects.length)
                for (let i = 0; i < message.subjects.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.subjects[i]);
            if (message.roomWords != null && message.roomWords.length)
                for (let i = 0; i < message.roomWords.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.roomWords[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoomNamePool message, length delimited. Does not implicitly {@link game.RoomNamePool.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RoomNamePool
         * @static
         * @param {game.RoomNamePool.$Properties} message RoomNamePool message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomNamePool.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RoomNamePool message from the specified reader or buffer.
         * @function decode
         * @memberof game.RoomNamePool
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RoomNamePool & game.RoomNamePool.$Shape} RoomNamePool
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomNamePool.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RoomNamePool();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.adjectives && message.adjectives.length))
                            message.adjectives = [];
                        message.adjectives.push(reader.stringVerify());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.subjects && message.subjects.length))
                            message.subjects = [];
                        message.subjects.push(reader.stringVerify());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.roomWords && message.roomWords.length))
                            message.roomWords = [];
                        message.roomWords.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoomNamePool message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RoomNamePool
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RoomNamePool & game.RoomNamePool.$Shape} RoomNamePool
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomNamePool.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoomNamePool message.
         * @function verify
         * @memberof game.RoomNamePool
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomNamePool.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.adjectives != null && $Object.hasOwnProperty.call(message, "adjectives")) {
                if (!$Array.isArray(message.adjectives))
                    return "adjectives: array expected";
                for (let i = 0; i < message.adjectives.length; ++i)
                    if (!$util.isString(message.adjectives[i]))
                        return "adjectives: string[] expected";
            }
            if (message.subjects != null && $Object.hasOwnProperty.call(message, "subjects")) {
                if (!$Array.isArray(message.subjects))
                    return "subjects: array expected";
                for (let i = 0; i < message.subjects.length; ++i)
                    if (!$util.isString(message.subjects[i]))
                        return "subjects: string[] expected";
            }
            if (message.roomWords != null && $Object.hasOwnProperty.call(message, "roomWords")) {
                if (!$Array.isArray(message.roomWords))
                    return "roomWords: array expected";
                for (let i = 0; i < message.roomWords.length; ++i)
                    if (!$util.isString(message.roomWords[i]))
                        return "roomWords: string[] expected";
            }
            return null;
        };

        /**
         * Creates a RoomNamePool message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RoomNamePool
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RoomNamePool} RoomNamePool
         */
        RoomNamePool.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RoomNamePool)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RoomNamePool: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RoomNamePool();
            if (object.adjectives) {
                if (!$Array.isArray(object.adjectives))
                    throw $TypeError(".game.RoomNamePool.adjectives: array expected");
                message.adjectives = $Array(object.adjectives.length);
                for (let i = 0; i < object.adjectives.length; ++i)
                    message.adjectives[i] = $String(object.adjectives[i]);
            }
            if (object.subjects) {
                if (!$Array.isArray(object.subjects))
                    throw $TypeError(".game.RoomNamePool.subjects: array expected");
                message.subjects = $Array(object.subjects.length);
                for (let i = 0; i < object.subjects.length; ++i)
                    message.subjects[i] = $String(object.subjects[i]);
            }
            if (object.roomWords) {
                if (!$Array.isArray(object.roomWords))
                    throw $TypeError(".game.RoomNamePool.roomWords: array expected");
                message.roomWords = $Array(object.roomWords.length);
                for (let i = 0; i < object.roomWords.length; ++i)
                    message.roomWords[i] = $String(object.roomWords[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a RoomNamePool message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RoomNamePool
         * @static
         * @param {game.RoomNamePool} message RoomNamePool
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoomNamePool.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.adjectives = [];
                object.subjects = [];
                object.roomWords = [];
            }
            if (message.adjectives && message.adjectives.length) {
                object.adjectives = $Array(message.adjectives.length);
                for (let j = 0; j < message.adjectives.length; ++j)
                    object.adjectives[j] = message.adjectives[j];
            }
            if (message.subjects && message.subjects.length) {
                object.subjects = $Array(message.subjects.length);
                for (let j = 0; j < message.subjects.length; ++j)
                    object.subjects[j] = message.subjects[j];
            }
            if (message.roomWords && message.roomWords.length) {
                object.roomWords = $Array(message.roomWords.length);
                for (let j = 0; j < message.roomWords.length; ++j)
                    object.roomWords[j] = message.roomWords[j];
            }
            return object;
        };

        /**
         * Converts this RoomNamePool to JSON.
         * @function toJSON
         * @memberof game.RoomNamePool
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoomNamePool.prototype.toJSON = function() {
            return RoomNamePool.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoomNamePool
         * @function getTypeUrl
         * @memberof game.RoomNamePool
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoomNamePool.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RoomNamePool";
        };

        return RoomNamePool;
    })();

    game.RoomInfoTagStyle = (function() {

        /**
         * Properties of a RoomInfoTagStyle.
         * @typedef {Object} game.RoomInfoTagStyle.$Properties
         * @property {string|null} [label] RoomInfoTagStyle label
         * @property {string|null} [textColor] RoomInfoTagStyle textColor
         * @property {string|null} [backgroundColor] RoomInfoTagStyle backgroundColor
         * @property {string|null} [borderColor] RoomInfoTagStyle borderColor
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomInfoTagStyle.
         * @memberof game
         * @interface IRoomInfoTagStyle
         * @augments game.RoomInfoTagStyle.$Properties
         * @deprecated Use game.RoomInfoTagStyle.$Properties instead.
         */

        /**
         * Shape of a RoomInfoTagStyle.
         * @typedef {game.RoomInfoTagStyle.$Properties} game.RoomInfoTagStyle.$Shape
         */

        /**
         * Constructs a new RoomInfoTagStyle.
         * @memberof game
         * @classdesc Represents a RoomInfoTagStyle.
         * @constructor
         * @param {game.RoomInfoTagStyle.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomInfoTagStyle = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomInfoTagStyle label.
         * @member {string} label
         * @memberof game.RoomInfoTagStyle
         * @instance
         */
        RoomInfoTagStyle.prototype.label = "";

        /**
         * RoomInfoTagStyle textColor.
         * @member {string} textColor
         * @memberof game.RoomInfoTagStyle
         * @instance
         */
        RoomInfoTagStyle.prototype.textColor = "";

        /**
         * RoomInfoTagStyle backgroundColor.
         * @member {string} backgroundColor
         * @memberof game.RoomInfoTagStyle
         * @instance
         */
        RoomInfoTagStyle.prototype.backgroundColor = "";

        /**
         * RoomInfoTagStyle borderColor.
         * @member {string} borderColor
         * @memberof game.RoomInfoTagStyle
         * @instance
         */
        RoomInfoTagStyle.prototype.borderColor = "";

        /**
         * Creates a new RoomInfoTagStyle instance using the specified properties.
         * @function create
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {game.RoomInfoTagStyle.$Properties=} [properties] Properties to set
         * @returns {game.RoomInfoTagStyle} RoomInfoTagStyle instance
         * @type {{
         *   (properties: game.RoomInfoTagStyle.$Shape): game.RoomInfoTagStyle & game.RoomInfoTagStyle.$Shape;
         *   (properties?: game.RoomInfoTagStyle.$Properties): game.RoomInfoTagStyle;
         * }}
         */
        RoomInfoTagStyle.create = function(properties) {
            return new RoomInfoTagStyle(properties);
        };

        /**
         * Encodes the specified RoomInfoTagStyle message. Does not implicitly {@link game.RoomInfoTagStyle.verify|verify} messages.
         * @function encode
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {game.RoomInfoTagStyle.$Properties} message RoomInfoTagStyle message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomInfoTagStyle.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.label);
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor") && message.textColor !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.textColor);
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor") && message.backgroundColor !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.backgroundColor);
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor") && message.borderColor !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.borderColor);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoomInfoTagStyle message, length delimited. Does not implicitly {@link game.RoomInfoTagStyle.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {game.RoomInfoTagStyle.$Properties} message RoomInfoTagStyle message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomInfoTagStyle.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RoomInfoTagStyle message from the specified reader or buffer.
         * @function decode
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RoomInfoTagStyle & game.RoomInfoTagStyle.$Shape} RoomInfoTagStyle
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomInfoTagStyle.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RoomInfoTagStyle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.textColor = value;
                        else
                            delete message.textColor;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.backgroundColor = value;
                        else
                            delete message.backgroundColor;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.borderColor = value;
                        else
                            delete message.borderColor;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoomInfoTagStyle message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RoomInfoTagStyle & game.RoomInfoTagStyle.$Shape} RoomInfoTagStyle
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomInfoTagStyle.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoomInfoTagStyle message.
         * @function verify
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomInfoTagStyle.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor"))
                if (!$util.isString(message.textColor))
                    return "textColor: string expected";
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor"))
                if (!$util.isString(message.backgroundColor))
                    return "backgroundColor: string expected";
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor"))
                if (!$util.isString(message.borderColor))
                    return "borderColor: string expected";
            return null;
        };

        /**
         * Creates a RoomInfoTagStyle message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RoomInfoTagStyle} RoomInfoTagStyle
         */
        RoomInfoTagStyle.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RoomInfoTagStyle)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RoomInfoTagStyle: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RoomInfoTagStyle();
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.textColor != null)
                if (typeof object.textColor !== "string" || object.textColor.length)
                    message.textColor = $String(object.textColor);
            if (object.backgroundColor != null)
                if (typeof object.backgroundColor !== "string" || object.backgroundColor.length)
                    message.backgroundColor = $String(object.backgroundColor);
            if (object.borderColor != null)
                if (typeof object.borderColor !== "string" || object.borderColor.length)
                    message.borderColor = $String(object.borderColor);
            return message;
        };

        /**
         * Creates a plain object from a RoomInfoTagStyle message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {game.RoomInfoTagStyle} message RoomInfoTagStyle
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoomInfoTagStyle.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.label = "";
                object.textColor = "";
                object.backgroundColor = "";
                object.borderColor = "";
            }
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.textColor != null && $Object.hasOwnProperty.call(message, "textColor"))
                object.textColor = message.textColor;
            if (message.backgroundColor != null && $Object.hasOwnProperty.call(message, "backgroundColor"))
                object.backgroundColor = message.backgroundColor;
            if (message.borderColor != null && $Object.hasOwnProperty.call(message, "borderColor"))
                object.borderColor = message.borderColor;
            return object;
        };

        /**
         * Converts this RoomInfoTagStyle to JSON.
         * @function toJSON
         * @memberof game.RoomInfoTagStyle
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoomInfoTagStyle.prototype.toJSON = function() {
            return RoomInfoTagStyle.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoomInfoTagStyle
         * @function getTypeUrl
         * @memberof game.RoomInfoTagStyle
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoomInfoTagStyle.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RoomInfoTagStyle";
        };

        return RoomInfoTagStyle;
    })();

    game.RoomInfoTagEntry = (function() {

        /**
         * Properties of a RoomInfoTagEntry.
         * @typedef {Object} game.RoomInfoTagEntry.$Properties
         * @property {string|null} [key] RoomInfoTagEntry key
         * @property {game.RoomInfoTagStyle.$Properties|null} [style] RoomInfoTagEntry style
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomInfoTagEntry.
         * @memberof game
         * @interface IRoomInfoTagEntry
         * @augments game.RoomInfoTagEntry.$Properties
         * @deprecated Use game.RoomInfoTagEntry.$Properties instead.
         */

        /**
         * Shape of a RoomInfoTagEntry.
         * @typedef {game.RoomInfoTagEntry.$Properties} game.RoomInfoTagEntry.$Shape
         */

        /**
         * Constructs a new RoomInfoTagEntry.
         * @memberof game
         * @classdesc Represents a RoomInfoTagEntry.
         * @constructor
         * @param {game.RoomInfoTagEntry.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomInfoTagEntry = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomInfoTagEntry key.
         * @member {string} key
         * @memberof game.RoomInfoTagEntry
         * @instance
         */
        RoomInfoTagEntry.prototype.key = "";

        /**
         * RoomInfoTagEntry style.
         * @member {game.RoomInfoTagStyle.$Properties|null|undefined} style
         * @memberof game.RoomInfoTagEntry
         * @instance
         */
        RoomInfoTagEntry.prototype.style = null;

        /**
         * Creates a new RoomInfoTagEntry instance using the specified properties.
         * @function create
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {game.RoomInfoTagEntry.$Properties=} [properties] Properties to set
         * @returns {game.RoomInfoTagEntry} RoomInfoTagEntry instance
         * @type {{
         *   (properties: game.RoomInfoTagEntry.$Shape): game.RoomInfoTagEntry & game.RoomInfoTagEntry.$Shape;
         *   (properties?: game.RoomInfoTagEntry.$Properties): game.RoomInfoTagEntry;
         * }}
         */
        RoomInfoTagEntry.create = function(properties) {
            return new RoomInfoTagEntry(properties);
        };

        /**
         * Encodes the specified RoomInfoTagEntry message. Does not implicitly {@link game.RoomInfoTagEntry.verify|verify} messages.
         * @function encode
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {game.RoomInfoTagEntry.$Properties} message RoomInfoTagEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomInfoTagEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.style != null && $Object.hasOwnProperty.call(message, "style"))
                $root.game.RoomInfoTagStyle.encode(message.style, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoomInfoTagEntry message, length delimited. Does not implicitly {@link game.RoomInfoTagEntry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {game.RoomInfoTagEntry.$Properties} message RoomInfoTagEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomInfoTagEntry.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RoomInfoTagEntry message from the specified reader or buffer.
         * @function decode
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RoomInfoTagEntry & game.RoomInfoTagEntry.$Shape} RoomInfoTagEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomInfoTagEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RoomInfoTagEntry(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.style = $root.game.RoomInfoTagStyle.decode(reader, reader.uint32(), $undefined, _depth + 1, message.style);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoomInfoTagEntry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RoomInfoTagEntry & game.RoomInfoTagEntry.$Shape} RoomInfoTagEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomInfoTagEntry.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoomInfoTagEntry message.
         * @function verify
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomInfoTagEntry.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.style != null && $Object.hasOwnProperty.call(message, "style")) {
                let error = $root.game.RoomInfoTagStyle.verify(message.style, _depth + 1);
                if (error)
                    return "style." + error;
            }
            return null;
        };

        /**
         * Creates a RoomInfoTagEntry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RoomInfoTagEntry} RoomInfoTagEntry
         */
        RoomInfoTagEntry.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RoomInfoTagEntry)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RoomInfoTagEntry: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RoomInfoTagEntry();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.style != null) {
                if (!$util.isObject(object.style))
                    throw $TypeError(".game.RoomInfoTagEntry.style: object expected");
                message.style = $root.game.RoomInfoTagStyle.fromObject(object.style, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RoomInfoTagEntry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {game.RoomInfoTagEntry} message RoomInfoTagEntry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoomInfoTagEntry.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.style = null;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.style != null && $Object.hasOwnProperty.call(message, "style"))
                object.style = $root.game.RoomInfoTagStyle.toObject(message.style, options, _depth + 1);
            return object;
        };

        /**
         * Converts this RoomInfoTagEntry to JSON.
         * @function toJSON
         * @memberof game.RoomInfoTagEntry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoomInfoTagEntry.prototype.toJSON = function() {
            return RoomInfoTagEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoomInfoTagEntry
         * @function getTypeUrl
         * @memberof game.RoomInfoTagEntry
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoomInfoTagEntry.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RoomInfoTagEntry";
        };

        return RoomInfoTagEntry;
    })();

    game.PunishmentTaskConfig = (function() {

        /**
         * Properties of a PunishmentTaskConfig.
         * @typedef {Object} game.PunishmentTaskConfig.$Properties
         * @property {string|null} [id] PunishmentTaskConfig id
         * @property {string|null} [name] PunishmentTaskConfig name
         * @property {Array.<game.StringPair.$Properties>|null} [variants] PunishmentTaskConfig variants
         * @property {Array.<string>|null} [backgroundImages] PunishmentTaskConfig backgroundImages
         * @property {number|null} [backgroundOpacity] PunishmentTaskConfig backgroundOpacity
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PunishmentTaskConfig.
         * @memberof game
         * @interface IPunishmentTaskConfig
         * @augments game.PunishmentTaskConfig.$Properties
         * @deprecated Use game.PunishmentTaskConfig.$Properties instead.
         */

        /**
         * Shape of a PunishmentTaskConfig.
         * @typedef {game.PunishmentTaskConfig.$Properties} game.PunishmentTaskConfig.$Shape
         */

        /**
         * Constructs a new PunishmentTaskConfig.
         * @memberof game
         * @classdesc Represents a PunishmentTaskConfig.
         * @constructor
         * @param {game.PunishmentTaskConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PunishmentTaskConfig = function (properties) {
            this.variants = [];
            this.backgroundImages = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PunishmentTaskConfig id.
         * @member {string} id
         * @memberof game.PunishmentTaskConfig
         * @instance
         */
        PunishmentTaskConfig.prototype.id = "";

        /**
         * PunishmentTaskConfig name.
         * @member {string} name
         * @memberof game.PunishmentTaskConfig
         * @instance
         */
        PunishmentTaskConfig.prototype.name = "";

        /**
         * PunishmentTaskConfig variants.
         * @member {Array.<game.StringPair.$Properties>} variants
         * @memberof game.PunishmentTaskConfig
         * @instance
         */
        PunishmentTaskConfig.prototype.variants = $util.emptyArray;

        /**
         * PunishmentTaskConfig backgroundImages.
         * @member {Array.<string>} backgroundImages
         * @memberof game.PunishmentTaskConfig
         * @instance
         */
        PunishmentTaskConfig.prototype.backgroundImages = $util.emptyArray;

        /**
         * PunishmentTaskConfig backgroundOpacity.
         * @member {number} backgroundOpacity
         * @memberof game.PunishmentTaskConfig
         * @instance
         */
        PunishmentTaskConfig.prototype.backgroundOpacity = 0;

        /**
         * Creates a new PunishmentTaskConfig instance using the specified properties.
         * @function create
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {game.PunishmentTaskConfig.$Properties=} [properties] Properties to set
         * @returns {game.PunishmentTaskConfig} PunishmentTaskConfig instance
         * @type {{
         *   (properties: game.PunishmentTaskConfig.$Shape): game.PunishmentTaskConfig & game.PunishmentTaskConfig.$Shape;
         *   (properties?: game.PunishmentTaskConfig.$Properties): game.PunishmentTaskConfig;
         * }}
         */
        PunishmentTaskConfig.create = function(properties) {
            return new PunishmentTaskConfig(properties);
        };

        /**
         * Encodes the specified PunishmentTaskConfig message. Does not implicitly {@link game.PunishmentTaskConfig.verify|verify} messages.
         * @function encode
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {game.PunishmentTaskConfig.$Properties} message PunishmentTaskConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PunishmentTaskConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.variants != null && message.variants.length)
                for (let i = 0; i < message.variants.length; ++i)
                    $root.game.StringPair.encode(message.variants[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.backgroundImages != null && message.backgroundImages.length)
                for (let i = 0; i < message.backgroundImages.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.backgroundImages[i]);
            if (message.backgroundOpacity != null && $Object.hasOwnProperty.call(message, "backgroundOpacity") && !$Object.is(message.backgroundOpacity, 0))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.backgroundOpacity);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PunishmentTaskConfig message, length delimited. Does not implicitly {@link game.PunishmentTaskConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {game.PunishmentTaskConfig.$Properties} message PunishmentTaskConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PunishmentTaskConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PunishmentTaskConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PunishmentTaskConfig & game.PunishmentTaskConfig.$Shape} PunishmentTaskConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PunishmentTaskConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PunishmentTaskConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.variants && message.variants.length))
                            message.variants = [];
                        message.variants.push($root.game.StringPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.backgroundImages && message.backgroundImages.length))
                            message.backgroundImages = [];
                        message.backgroundImages.push(reader.stringVerify());
                        continue;
                    }
                case 5: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.backgroundOpacity = value;
                        else
                            delete message.backgroundOpacity;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PunishmentTaskConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PunishmentTaskConfig & game.PunishmentTaskConfig.$Shape} PunishmentTaskConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PunishmentTaskConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PunishmentTaskConfig message.
         * @function verify
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PunishmentTaskConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.variants != null && $Object.hasOwnProperty.call(message, "variants")) {
                if (!$Array.isArray(message.variants))
                    return "variants: array expected";
                for (let i = 0; i < message.variants.length; ++i) {
                    let error = $root.game.StringPair.verify(message.variants[i], _depth + 1);
                    if (error)
                        return "variants." + error;
                }
            }
            if (message.backgroundImages != null && $Object.hasOwnProperty.call(message, "backgroundImages")) {
                if (!$Array.isArray(message.backgroundImages))
                    return "backgroundImages: array expected";
                for (let i = 0; i < message.backgroundImages.length; ++i)
                    if (!$util.isString(message.backgroundImages[i]))
                        return "backgroundImages: string[] expected";
            }
            if (message.backgroundOpacity != null && $Object.hasOwnProperty.call(message, "backgroundOpacity"))
                if (typeof message.backgroundOpacity !== "number")
                    return "backgroundOpacity: number expected";
            return null;
        };

        /**
         * Creates a PunishmentTaskConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PunishmentTaskConfig} PunishmentTaskConfig
         */
        PunishmentTaskConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PunishmentTaskConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PunishmentTaskConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PunishmentTaskConfig();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.variants) {
                if (!$Array.isArray(object.variants))
                    throw $TypeError(".game.PunishmentTaskConfig.variants: array expected");
                message.variants = $Array(object.variants.length);
                for (let i = 0; i < object.variants.length; ++i) {
                    if (!$util.isObject(object.variants[i]))
                        throw $TypeError(".game.PunishmentTaskConfig.variants: object expected");
                    message.variants[i] = $root.game.StringPair.fromObject(object.variants[i], _depth + 1);
                }
            }
            if (object.backgroundImages) {
                if (!$Array.isArray(object.backgroundImages))
                    throw $TypeError(".game.PunishmentTaskConfig.backgroundImages: array expected");
                message.backgroundImages = $Array(object.backgroundImages.length);
                for (let i = 0; i < object.backgroundImages.length; ++i)
                    message.backgroundImages[i] = $String(object.backgroundImages[i]);
            }
            if (object.backgroundOpacity != null)
                if (!$Object.is($Number(object.backgroundOpacity), 0))
                    message.backgroundOpacity = $Number(object.backgroundOpacity);
            return message;
        };

        /**
         * Creates a plain object from a PunishmentTaskConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {game.PunishmentTaskConfig} message PunishmentTaskConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PunishmentTaskConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.variants = [];
                object.backgroundImages = [];
            }
            if (options.defaults) {
                object.id = "";
                object.name = "";
                object.backgroundOpacity = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.variants && message.variants.length) {
                object.variants = $Array(message.variants.length);
                for (let j = 0; j < message.variants.length; ++j)
                    object.variants[j] = $root.game.StringPair.toObject(message.variants[j], options, _depth + 1);
            }
            if (message.backgroundImages && message.backgroundImages.length) {
                object.backgroundImages = $Array(message.backgroundImages.length);
                for (let j = 0; j < message.backgroundImages.length; ++j)
                    object.backgroundImages[j] = message.backgroundImages[j];
            }
            if (message.backgroundOpacity != null && $Object.hasOwnProperty.call(message, "backgroundOpacity"))
                object.backgroundOpacity = options.json && !$isFinite(message.backgroundOpacity) ? $String(message.backgroundOpacity) : message.backgroundOpacity;
            return object;
        };

        /**
         * Converts this PunishmentTaskConfig to JSON.
         * @function toJSON
         * @memberof game.PunishmentTaskConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PunishmentTaskConfig.prototype.toJSON = function() {
            return PunishmentTaskConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PunishmentTaskConfig
         * @function getTypeUrl
         * @memberof game.PunishmentTaskConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PunishmentTaskConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PunishmentTaskConfig";
        };

        return PunishmentTaskConfig;
    })();

    game.TitleSegment = (function() {

        /**
         * Properties of a TitleSegment.
         * @typedef {Object} game.TitleSegment.$Properties
         * @property {string|null} [id] TitleSegment id
         * @property {number|null} [minPercent] TitleSegment minPercent
         * @property {number|null} [maxPercent] TitleSegment maxPercent
         * @property {Array.<string>|null} [names] TitleSegment names
         * @property {Array.<game.TitleSegment.FactionNames.$Properties>|null} [factionNames] TitleSegment factionNames
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TitleSegment.
         * @memberof game
         * @interface ITitleSegment
         * @augments game.TitleSegment.$Properties
         * @deprecated Use game.TitleSegment.$Properties instead.
         */

        /**
         * Shape of a TitleSegment.
         * @typedef {game.TitleSegment.$Properties} game.TitleSegment.$Shape
         */

        /**
         * Constructs a new TitleSegment.
         * @memberof game
         * @classdesc Represents a TitleSegment.
         * @constructor
         * @param {game.TitleSegment.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TitleSegment = function (properties) {
            this.names = [];
            this.factionNames = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TitleSegment id.
         * @member {string} id
         * @memberof game.TitleSegment
         * @instance
         */
        TitleSegment.prototype.id = "";

        /**
         * TitleSegment minPercent.
         * @member {number} minPercent
         * @memberof game.TitleSegment
         * @instance
         */
        TitleSegment.prototype.minPercent = 0;

        /**
         * TitleSegment maxPercent.
         * @member {number} maxPercent
         * @memberof game.TitleSegment
         * @instance
         */
        TitleSegment.prototype.maxPercent = 0;

        /**
         * TitleSegment names.
         * @member {Array.<string>} names
         * @memberof game.TitleSegment
         * @instance
         */
        TitleSegment.prototype.names = $util.emptyArray;

        /**
         * TitleSegment factionNames.
         * @member {Array.<game.TitleSegment.FactionNames.$Properties>} factionNames
         * @memberof game.TitleSegment
         * @instance
         */
        TitleSegment.prototype.factionNames = $util.emptyArray;

        /**
         * Creates a new TitleSegment instance using the specified properties.
         * @function create
         * @memberof game.TitleSegment
         * @static
         * @param {game.TitleSegment.$Properties=} [properties] Properties to set
         * @returns {game.TitleSegment} TitleSegment instance
         * @type {{
         *   (properties: game.TitleSegment.$Shape): game.TitleSegment & game.TitleSegment.$Shape;
         *   (properties?: game.TitleSegment.$Properties): game.TitleSegment;
         * }}
         */
        TitleSegment.create = function(properties) {
            return new TitleSegment(properties);
        };

        /**
         * Encodes the specified TitleSegment message. Does not implicitly {@link game.TitleSegment.verify|verify} messages.
         * @function encode
         * @memberof game.TitleSegment
         * @static
         * @param {game.TitleSegment.$Properties} message TitleSegment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TitleSegment.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.minPercent != null && $Object.hasOwnProperty.call(message, "minPercent") && !$Object.is(message.minPercent, 0))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.minPercent);
            if (message.maxPercent != null && $Object.hasOwnProperty.call(message, "maxPercent") && !$Object.is(message.maxPercent, 0))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.maxPercent);
            if (message.names != null && message.names.length)
                for (let i = 0; i < message.names.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.names[i]);
            if (message.factionNames != null && message.factionNames.length)
                for (let i = 0; i < message.factionNames.length; ++i)
                    $root.game.TitleSegment.FactionNames.encode(message.factionNames[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TitleSegment message, length delimited. Does not implicitly {@link game.TitleSegment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.TitleSegment
         * @static
         * @param {game.TitleSegment.$Properties} message TitleSegment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TitleSegment.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TitleSegment message from the specified reader or buffer.
         * @function decode
         * @memberof game.TitleSegment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.TitleSegment & game.TitleSegment.$Shape} TitleSegment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TitleSegment.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.TitleSegment(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.minPercent = value;
                        else
                            delete message.minPercent;
                        continue;
                    }
                case 3: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.maxPercent = value;
                        else
                            delete message.maxPercent;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.names && message.names.length))
                            message.names = [];
                        message.names.push(reader.stringVerify());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.factionNames && message.factionNames.length))
                            message.factionNames = [];
                        message.factionNames.push($root.game.TitleSegment.FactionNames.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TitleSegment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.TitleSegment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.TitleSegment & game.TitleSegment.$Shape} TitleSegment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TitleSegment.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TitleSegment message.
         * @function verify
         * @memberof game.TitleSegment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TitleSegment.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.minPercent != null && $Object.hasOwnProperty.call(message, "minPercent"))
                if (typeof message.minPercent !== "number")
                    return "minPercent: number expected";
            if (message.maxPercent != null && $Object.hasOwnProperty.call(message, "maxPercent"))
                if (typeof message.maxPercent !== "number")
                    return "maxPercent: number expected";
            if (message.names != null && $Object.hasOwnProperty.call(message, "names")) {
                if (!$Array.isArray(message.names))
                    return "names: array expected";
                for (let i = 0; i < message.names.length; ++i)
                    if (!$util.isString(message.names[i]))
                        return "names: string[] expected";
            }
            if (message.factionNames != null && $Object.hasOwnProperty.call(message, "factionNames")) {
                if (!$Array.isArray(message.factionNames))
                    return "factionNames: array expected";
                for (let i = 0; i < message.factionNames.length; ++i) {
                    let error = $root.game.TitleSegment.FactionNames.verify(message.factionNames[i], _depth + 1);
                    if (error)
                        return "factionNames." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TitleSegment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.TitleSegment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.TitleSegment} TitleSegment
         */
        TitleSegment.fromObject = function (object, _depth) {
            if (object instanceof $root.game.TitleSegment)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.TitleSegment: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.TitleSegment();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.minPercent != null)
                if (!$Object.is($Number(object.minPercent), 0))
                    message.minPercent = $Number(object.minPercent);
            if (object.maxPercent != null)
                if (!$Object.is($Number(object.maxPercent), 0))
                    message.maxPercent = $Number(object.maxPercent);
            if (object.names) {
                if (!$Array.isArray(object.names))
                    throw $TypeError(".game.TitleSegment.names: array expected");
                message.names = $Array(object.names.length);
                for (let i = 0; i < object.names.length; ++i)
                    message.names[i] = $String(object.names[i]);
            }
            if (object.factionNames) {
                if (!$Array.isArray(object.factionNames))
                    throw $TypeError(".game.TitleSegment.factionNames: array expected");
                message.factionNames = $Array(object.factionNames.length);
                for (let i = 0; i < object.factionNames.length; ++i) {
                    if (!$util.isObject(object.factionNames[i]))
                        throw $TypeError(".game.TitleSegment.factionNames: object expected");
                    message.factionNames[i] = $root.game.TitleSegment.FactionNames.fromObject(object.factionNames[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TitleSegment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.TitleSegment
         * @static
         * @param {game.TitleSegment} message TitleSegment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TitleSegment.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.names = [];
                object.factionNames = [];
            }
            if (options.defaults) {
                object.id = "";
                object.minPercent = 0;
                object.maxPercent = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.minPercent != null && $Object.hasOwnProperty.call(message, "minPercent"))
                object.minPercent = options.json && !$isFinite(message.minPercent) ? $String(message.minPercent) : message.minPercent;
            if (message.maxPercent != null && $Object.hasOwnProperty.call(message, "maxPercent"))
                object.maxPercent = options.json && !$isFinite(message.maxPercent) ? $String(message.maxPercent) : message.maxPercent;
            if (message.names && message.names.length) {
                object.names = $Array(message.names.length);
                for (let j = 0; j < message.names.length; ++j)
                    object.names[j] = message.names[j];
            }
            if (message.factionNames && message.factionNames.length) {
                object.factionNames = $Array(message.factionNames.length);
                for (let j = 0; j < message.factionNames.length; ++j)
                    object.factionNames[j] = $root.game.TitleSegment.FactionNames.toObject(message.factionNames[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TitleSegment to JSON.
         * @function toJSON
         * @memberof game.TitleSegment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TitleSegment.prototype.toJSON = function() {
            return TitleSegment.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TitleSegment
         * @function getTypeUrl
         * @memberof game.TitleSegment
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TitleSegment.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.TitleSegment";
        };

        TitleSegment.FactionNames = (function() {

            /**
             * Properties of a FactionNames.
             * @typedef {Object} game.TitleSegment.FactionNames.$Properties
             * @property {string|null} [factionId] FactionNames factionId
             * @property {Array.<string>|null} [names] FactionNames names
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FactionNames.
             * @memberof game.TitleSegment
             * @interface IFactionNames
             * @augments game.TitleSegment.FactionNames.$Properties
             * @deprecated Use game.TitleSegment.FactionNames.$Properties instead.
             */

            /**
             * Shape of a FactionNames.
             * @typedef {game.TitleSegment.FactionNames.$Properties} game.TitleSegment.FactionNames.$Shape
             */

            /**
             * Constructs a new FactionNames.
             * @memberof game.TitleSegment
             * @classdesc Represents a FactionNames.
             * @constructor
             * @param {game.TitleSegment.FactionNames.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FactionNames = function (properties) {
                this.names = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FactionNames factionId.
             * @member {string} factionId
             * @memberof game.TitleSegment.FactionNames
             * @instance
             */
            FactionNames.prototype.factionId = "";

            /**
             * FactionNames names.
             * @member {Array.<string>} names
             * @memberof game.TitleSegment.FactionNames
             * @instance
             */
            FactionNames.prototype.names = $util.emptyArray;

            /**
             * Creates a new FactionNames instance using the specified properties.
             * @function create
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {game.TitleSegment.FactionNames.$Properties=} [properties] Properties to set
             * @returns {game.TitleSegment.FactionNames} FactionNames instance
             * @type {{
             *   (properties: game.TitleSegment.FactionNames.$Shape): game.TitleSegment.FactionNames & game.TitleSegment.FactionNames.$Shape;
             *   (properties?: game.TitleSegment.FactionNames.$Properties): game.TitleSegment.FactionNames;
             * }}
             */
            FactionNames.create = function(properties) {
                return new FactionNames(properties);
            };

            /**
             * Encodes the specified FactionNames message. Does not implicitly {@link game.TitleSegment.FactionNames.verify|verify} messages.
             * @function encode
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {game.TitleSegment.FactionNames.$Properties} message FactionNames message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactionNames.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId") && message.factionId !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.factionId);
                if (message.names != null && message.names.length)
                    for (let i = 0; i < message.names.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.names[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FactionNames message, length delimited. Does not implicitly {@link game.TitleSegment.FactionNames.verify|verify} messages.
             * @function encodeDelimited
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {game.TitleSegment.FactionNames.$Properties} message FactionNames message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FactionNames.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a FactionNames message from the specified reader or buffer.
             * @function decode
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {game.TitleSegment.FactionNames & game.TitleSegment.FactionNames.$Shape} FactionNames
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactionNames.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.TitleSegment.FactionNames(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.factionId = value;
                            else
                                delete message.factionId;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.names && message.names.length))
                                message.names = [];
                            message.names.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FactionNames message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {game.TitleSegment.FactionNames & game.TitleSegment.FactionNames.$Shape} FactionNames
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FactionNames.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FactionNames message.
             * @function verify
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FactionNames.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                    if (!$util.isString(message.factionId))
                        return "factionId: string expected";
                if (message.names != null && $Object.hasOwnProperty.call(message, "names")) {
                    if (!$Array.isArray(message.names))
                        return "names: array expected";
                    for (let i = 0; i < message.names.length; ++i)
                        if (!$util.isString(message.names[i]))
                            return "names: string[] expected";
                }
                return null;
            };

            /**
             * Creates a FactionNames message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {game.TitleSegment.FactionNames} FactionNames
             */
            FactionNames.fromObject = function (object, _depth) {
                if (object instanceof $root.game.TitleSegment.FactionNames)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".game.TitleSegment.FactionNames: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.game.TitleSegment.FactionNames();
                if (object.factionId != null)
                    if (typeof object.factionId !== "string" || object.factionId.length)
                        message.factionId = $String(object.factionId);
                if (object.names) {
                    if (!$Array.isArray(object.names))
                        throw $TypeError(".game.TitleSegment.FactionNames.names: array expected");
                    message.names = $Array(object.names.length);
                    for (let i = 0; i < object.names.length; ++i)
                        message.names[i] = $String(object.names[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a FactionNames message. Also converts values to other types if specified.
             * @function toObject
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {game.TitleSegment.FactionNames} message FactionNames
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FactionNames.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.names = [];
                if (options.defaults)
                    object.factionId = "";
                if (message.factionId != null && $Object.hasOwnProperty.call(message, "factionId"))
                    object.factionId = message.factionId;
                if (message.names && message.names.length) {
                    object.names = $Array(message.names.length);
                    for (let j = 0; j < message.names.length; ++j)
                        object.names[j] = message.names[j];
                }
                return object;
            };

            /**
             * Converts this FactionNames to JSON.
             * @function toJSON
             * @memberof game.TitleSegment.FactionNames
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FactionNames.prototype.toJSON = function() {
                return FactionNames.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FactionNames
             * @function getTypeUrl
             * @memberof game.TitleSegment.FactionNames
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FactionNames.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/game.TitleSegment.FactionNames";
            };

            return FactionNames;
        })();

        return TitleSegment;
    })();

    game.PunishmentConfig = (function() {

        /**
         * Properties of a PunishmentConfig.
         * @typedef {Object} game.PunishmentConfig.$Properties
         * @property {string|null} [id] PunishmentConfig id
         * @property {string|null} [name] PunishmentConfig name
         * @property {string|null} [description] PunishmentConfig description
         * @property {Array.<game.StringPair.$Properties>|null} [variants] PunishmentConfig variants
         * @property {Array.<game.PunishmentTaskConfig.$Properties>|null} [tasks] PunishmentConfig tasks
         * @property {string|null} [cardImageUrl] PunishmentConfig cardImageUrl
         * @property {number|null} [cardImageOpacity] PunishmentConfig cardImageOpacity
         * @property {Array.<string>|null} [roomBackgroundImages] PunishmentConfig roomBackgroundImages
         * @property {game.RoomNamePool.$Properties|null} [roomNamePool] PunishmentConfig roomNamePool
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PunishmentConfig.
         * @memberof game
         * @interface IPunishmentConfig
         * @augments game.PunishmentConfig.$Properties
         * @deprecated Use game.PunishmentConfig.$Properties instead.
         */

        /**
         * Shape of a PunishmentConfig.
         * @typedef {game.PunishmentConfig.$Properties} game.PunishmentConfig.$Shape
         */

        /**
         * Constructs a new PunishmentConfig.
         * @memberof game
         * @classdesc Represents a PunishmentConfig.
         * @constructor
         * @param {game.PunishmentConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PunishmentConfig = function (properties) {
            this.variants = [];
            this.tasks = [];
            this.roomBackgroundImages = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PunishmentConfig id.
         * @member {string} id
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.id = "";

        /**
         * PunishmentConfig name.
         * @member {string} name
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.name = "";

        /**
         * PunishmentConfig description.
         * @member {string} description
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.description = "";

        /**
         * PunishmentConfig variants.
         * @member {Array.<game.StringPair.$Properties>} variants
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.variants = $util.emptyArray;

        /**
         * PunishmentConfig tasks.
         * @member {Array.<game.PunishmentTaskConfig.$Properties>} tasks
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.tasks = $util.emptyArray;

        /**
         * PunishmentConfig cardImageUrl.
         * @member {string} cardImageUrl
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.cardImageUrl = "";

        /**
         * PunishmentConfig cardImageOpacity.
         * @member {number} cardImageOpacity
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.cardImageOpacity = 0;

        /**
         * PunishmentConfig roomBackgroundImages.
         * @member {Array.<string>} roomBackgroundImages
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.roomBackgroundImages = $util.emptyArray;

        /**
         * PunishmentConfig roomNamePool.
         * @member {game.RoomNamePool.$Properties|null|undefined} roomNamePool
         * @memberof game.PunishmentConfig
         * @instance
         */
        PunishmentConfig.prototype.roomNamePool = null;

        /**
         * Creates a new PunishmentConfig instance using the specified properties.
         * @function create
         * @memberof game.PunishmentConfig
         * @static
         * @param {game.PunishmentConfig.$Properties=} [properties] Properties to set
         * @returns {game.PunishmentConfig} PunishmentConfig instance
         * @type {{
         *   (properties: game.PunishmentConfig.$Shape): game.PunishmentConfig & game.PunishmentConfig.$Shape;
         *   (properties?: game.PunishmentConfig.$Properties): game.PunishmentConfig;
         * }}
         */
        PunishmentConfig.create = function(properties) {
            return new PunishmentConfig(properties);
        };

        /**
         * Encodes the specified PunishmentConfig message. Does not implicitly {@link game.PunishmentConfig.verify|verify} messages.
         * @function encode
         * @memberof game.PunishmentConfig
         * @static
         * @param {game.PunishmentConfig.$Properties} message PunishmentConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PunishmentConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
            if (message.variants != null && message.variants.length)
                for (let i = 0; i < message.variants.length; ++i)
                    $root.game.StringPair.encode(message.variants[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.tasks != null && message.tasks.length)
                for (let i = 0; i < message.tasks.length; ++i)
                    $root.game.PunishmentTaskConfig.encode(message.tasks[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.cardImageUrl != null && $Object.hasOwnProperty.call(message, "cardImageUrl") && message.cardImageUrl !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.cardImageUrl);
            if (message.cardImageOpacity != null && $Object.hasOwnProperty.call(message, "cardImageOpacity") && !$Object.is(message.cardImageOpacity, 0))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.cardImageOpacity);
            if (message.roomBackgroundImages != null && message.roomBackgroundImages.length)
                for (let i = 0; i < message.roomBackgroundImages.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.roomBackgroundImages[i]);
            if (message.roomNamePool != null && $Object.hasOwnProperty.call(message, "roomNamePool"))
                $root.game.RoomNamePool.encode(message.roomNamePool, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PunishmentConfig message, length delimited. Does not implicitly {@link game.PunishmentConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PunishmentConfig
         * @static
         * @param {game.PunishmentConfig.$Properties} message PunishmentConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PunishmentConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PunishmentConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.PunishmentConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PunishmentConfig & game.PunishmentConfig.$Shape} PunishmentConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PunishmentConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PunishmentConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.description = value;
                        else
                            delete message.description;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.variants && message.variants.length))
                            message.variants = [];
                        message.variants.push($root.game.StringPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tasks && message.tasks.length))
                            message.tasks = [];
                        message.tasks.push($root.game.PunishmentTaskConfig.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.cardImageUrl = value;
                        else
                            delete message.cardImageUrl;
                        continue;
                    }
                case 7: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.cardImageOpacity = value;
                        else
                            delete message.cardImageOpacity;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.roomBackgroundImages && message.roomBackgroundImages.length))
                            message.roomBackgroundImages = [];
                        message.roomBackgroundImages.push(reader.stringVerify());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.roomNamePool = $root.game.RoomNamePool.decode(reader, reader.uint32(), $undefined, _depth + 1, message.roomNamePool);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PunishmentConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PunishmentConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PunishmentConfig & game.PunishmentConfig.$Shape} PunishmentConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PunishmentConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PunishmentConfig message.
         * @function verify
         * @memberof game.PunishmentConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PunishmentConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.variants != null && $Object.hasOwnProperty.call(message, "variants")) {
                if (!$Array.isArray(message.variants))
                    return "variants: array expected";
                for (let i = 0; i < message.variants.length; ++i) {
                    let error = $root.game.StringPair.verify(message.variants[i], _depth + 1);
                    if (error)
                        return "variants." + error;
                }
            }
            if (message.tasks != null && $Object.hasOwnProperty.call(message, "tasks")) {
                if (!$Array.isArray(message.tasks))
                    return "tasks: array expected";
                for (let i = 0; i < message.tasks.length; ++i) {
                    let error = $root.game.PunishmentTaskConfig.verify(message.tasks[i], _depth + 1);
                    if (error)
                        return "tasks." + error;
                }
            }
            if (message.cardImageUrl != null && $Object.hasOwnProperty.call(message, "cardImageUrl"))
                if (!$util.isString(message.cardImageUrl))
                    return "cardImageUrl: string expected";
            if (message.cardImageOpacity != null && $Object.hasOwnProperty.call(message, "cardImageOpacity"))
                if (typeof message.cardImageOpacity !== "number")
                    return "cardImageOpacity: number expected";
            if (message.roomBackgroundImages != null && $Object.hasOwnProperty.call(message, "roomBackgroundImages")) {
                if (!$Array.isArray(message.roomBackgroundImages))
                    return "roomBackgroundImages: array expected";
                for (let i = 0; i < message.roomBackgroundImages.length; ++i)
                    if (!$util.isString(message.roomBackgroundImages[i]))
                        return "roomBackgroundImages: string[] expected";
            }
            if (message.roomNamePool != null && $Object.hasOwnProperty.call(message, "roomNamePool")) {
                let error = $root.game.RoomNamePool.verify(message.roomNamePool, _depth + 1);
                if (error)
                    return "roomNamePool." + error;
            }
            return null;
        };

        /**
         * Creates a PunishmentConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PunishmentConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PunishmentConfig} PunishmentConfig
         */
        PunishmentConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PunishmentConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PunishmentConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PunishmentConfig();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.description != null)
                if (typeof object.description !== "string" || object.description.length)
                    message.description = $String(object.description);
            if (object.variants) {
                if (!$Array.isArray(object.variants))
                    throw $TypeError(".game.PunishmentConfig.variants: array expected");
                message.variants = $Array(object.variants.length);
                for (let i = 0; i < object.variants.length; ++i) {
                    if (!$util.isObject(object.variants[i]))
                        throw $TypeError(".game.PunishmentConfig.variants: object expected");
                    message.variants[i] = $root.game.StringPair.fromObject(object.variants[i], _depth + 1);
                }
            }
            if (object.tasks) {
                if (!$Array.isArray(object.tasks))
                    throw $TypeError(".game.PunishmentConfig.tasks: array expected");
                message.tasks = $Array(object.tasks.length);
                for (let i = 0; i < object.tasks.length; ++i) {
                    if (!$util.isObject(object.tasks[i]))
                        throw $TypeError(".game.PunishmentConfig.tasks: object expected");
                    message.tasks[i] = $root.game.PunishmentTaskConfig.fromObject(object.tasks[i], _depth + 1);
                }
            }
            if (object.cardImageUrl != null)
                if (typeof object.cardImageUrl !== "string" || object.cardImageUrl.length)
                    message.cardImageUrl = $String(object.cardImageUrl);
            if (object.cardImageOpacity != null)
                if (!$Object.is($Number(object.cardImageOpacity), 0))
                    message.cardImageOpacity = $Number(object.cardImageOpacity);
            if (object.roomBackgroundImages) {
                if (!$Array.isArray(object.roomBackgroundImages))
                    throw $TypeError(".game.PunishmentConfig.roomBackgroundImages: array expected");
                message.roomBackgroundImages = $Array(object.roomBackgroundImages.length);
                for (let i = 0; i < object.roomBackgroundImages.length; ++i)
                    message.roomBackgroundImages[i] = $String(object.roomBackgroundImages[i]);
            }
            if (object.roomNamePool != null) {
                if (!$util.isObject(object.roomNamePool))
                    throw $TypeError(".game.PunishmentConfig.roomNamePool: object expected");
                message.roomNamePool = $root.game.RoomNamePool.fromObject(object.roomNamePool, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PunishmentConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PunishmentConfig
         * @static
         * @param {game.PunishmentConfig} message PunishmentConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PunishmentConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.variants = [];
                object.tasks = [];
                object.roomBackgroundImages = [];
            }
            if (options.defaults) {
                object.id = "";
                object.name = "";
                object.description = "";
                object.cardImageUrl = "";
                object.cardImageOpacity = 0;
                object.roomNamePool = null;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.variants && message.variants.length) {
                object.variants = $Array(message.variants.length);
                for (let j = 0; j < message.variants.length; ++j)
                    object.variants[j] = $root.game.StringPair.toObject(message.variants[j], options, _depth + 1);
            }
            if (message.tasks && message.tasks.length) {
                object.tasks = $Array(message.tasks.length);
                for (let j = 0; j < message.tasks.length; ++j)
                    object.tasks[j] = $root.game.PunishmentTaskConfig.toObject(message.tasks[j], options, _depth + 1);
            }
            if (message.cardImageUrl != null && $Object.hasOwnProperty.call(message, "cardImageUrl"))
                object.cardImageUrl = message.cardImageUrl;
            if (message.cardImageOpacity != null && $Object.hasOwnProperty.call(message, "cardImageOpacity"))
                object.cardImageOpacity = options.json && !$isFinite(message.cardImageOpacity) ? $String(message.cardImageOpacity) : message.cardImageOpacity;
            if (message.roomBackgroundImages && message.roomBackgroundImages.length) {
                object.roomBackgroundImages = $Array(message.roomBackgroundImages.length);
                for (let j = 0; j < message.roomBackgroundImages.length; ++j)
                    object.roomBackgroundImages[j] = message.roomBackgroundImages[j];
            }
            if (message.roomNamePool != null && $Object.hasOwnProperty.call(message, "roomNamePool"))
                object.roomNamePool = $root.game.RoomNamePool.toObject(message.roomNamePool, options, _depth + 1);
            return object;
        };

        /**
         * Converts this PunishmentConfig to JSON.
         * @function toJSON
         * @memberof game.PunishmentConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PunishmentConfig.prototype.toJSON = function() {
            return PunishmentConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PunishmentConfig
         * @function getTypeUrl
         * @memberof game.PunishmentConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PunishmentConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PunishmentConfig";
        };

        return PunishmentConfig;
    })();

    game.GameConfig = (function() {

        /**
         * Properties of a GameConfig.
         * @typedef {Object} game.GameConfig.$Properties
         * @property {string|null} [id] GameConfig id
         * @property {string|null} [name] GameConfig name
         * @property {string|null} [description] GameConfig description
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GameConfig.
         * @memberof game
         * @interface IGameConfig
         * @augments game.GameConfig.$Properties
         * @deprecated Use game.GameConfig.$Properties instead.
         */

        /**
         * Shape of a GameConfig.
         * @typedef {game.GameConfig.$Properties} game.GameConfig.$Shape
         */

        /**
         * Constructs a new GameConfig.
         * @memberof game
         * @classdesc Represents a GameConfig.
         * @constructor
         * @param {game.GameConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GameConfig = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GameConfig id.
         * @member {string} id
         * @memberof game.GameConfig
         * @instance
         */
        GameConfig.prototype.id = "";

        /**
         * GameConfig name.
         * @member {string} name
         * @memberof game.GameConfig
         * @instance
         */
        GameConfig.prototype.name = "";

        /**
         * GameConfig description.
         * @member {string} description
         * @memberof game.GameConfig
         * @instance
         */
        GameConfig.prototype.description = "";

        /**
         * Creates a new GameConfig instance using the specified properties.
         * @function create
         * @memberof game.GameConfig
         * @static
         * @param {game.GameConfig.$Properties=} [properties] Properties to set
         * @returns {game.GameConfig} GameConfig instance
         * @type {{
         *   (properties: game.GameConfig.$Shape): game.GameConfig & game.GameConfig.$Shape;
         *   (properties?: game.GameConfig.$Properties): game.GameConfig;
         * }}
         */
        GameConfig.create = function(properties) {
            return new GameConfig(properties);
        };

        /**
         * Encodes the specified GameConfig message. Does not implicitly {@link game.GameConfig.verify|verify} messages.
         * @function encode
         * @memberof game.GameConfig
         * @static
         * @param {game.GameConfig.$Properties} message GameConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GameConfig message, length delimited. Does not implicitly {@link game.GameConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GameConfig
         * @static
         * @param {game.GameConfig.$Properties} message GameConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GameConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.GameConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GameConfig & game.GameConfig.$Shape} GameConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GameConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.description = value;
                        else
                            delete message.description;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GameConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GameConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GameConfig & game.GameConfig.$Shape} GameConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameConfig message.
         * @function verify
         * @memberof game.GameConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            return null;
        };

        /**
         * Creates a GameConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GameConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GameConfig} GameConfig
         */
        GameConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GameConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GameConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GameConfig();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.description != null)
                if (typeof object.description !== "string" || object.description.length)
                    message.description = $String(object.description);
            return message;
        };

        /**
         * Creates a plain object from a GameConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GameConfig
         * @static
         * @param {game.GameConfig} message GameConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.name = "";
                object.description = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            return object;
        };

        /**
         * Converts this GameConfig to JSON.
         * @function toJSON
         * @memberof game.GameConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameConfig.prototype.toJSON = function() {
            return GameConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GameConfig
         * @function getTypeUrl
         * @memberof game.GameConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GameConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GameConfig";
        };

        return GameConfig;
    })();

    game.AnnouncementBoard = (function() {

        /**
         * Properties of an AnnouncementBoard.
         * @typedef {Object} game.AnnouncementBoard.$Properties
         * @property {boolean|null} [enabled] AnnouncementBoard enabled
         * @property {string|null} [title] AnnouncementBoard title
         * @property {string|null} [content] AnnouncementBoard content
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AnnouncementBoard.
         * @memberof game
         * @interface IAnnouncementBoard
         * @augments game.AnnouncementBoard.$Properties
         * @deprecated Use game.AnnouncementBoard.$Properties instead.
         */

        /**
         * Shape of an AnnouncementBoard.
         * @typedef {game.AnnouncementBoard.$Properties} game.AnnouncementBoard.$Shape
         */

        /**
         * Constructs a new AnnouncementBoard.
         * @memberof game
         * @classdesc Represents an AnnouncementBoard.
         * @constructor
         * @param {game.AnnouncementBoard.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AnnouncementBoard = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AnnouncementBoard enabled.
         * @member {boolean} enabled
         * @memberof game.AnnouncementBoard
         * @instance
         */
        AnnouncementBoard.prototype.enabled = false;

        /**
         * AnnouncementBoard title.
         * @member {string} title
         * @memberof game.AnnouncementBoard
         * @instance
         */
        AnnouncementBoard.prototype.title = "";

        /**
         * AnnouncementBoard content.
         * @member {string} content
         * @memberof game.AnnouncementBoard
         * @instance
         */
        AnnouncementBoard.prototype.content = "";

        /**
         * Creates a new AnnouncementBoard instance using the specified properties.
         * @function create
         * @memberof game.AnnouncementBoard
         * @static
         * @param {game.AnnouncementBoard.$Properties=} [properties] Properties to set
         * @returns {game.AnnouncementBoard} AnnouncementBoard instance
         * @type {{
         *   (properties: game.AnnouncementBoard.$Shape): game.AnnouncementBoard & game.AnnouncementBoard.$Shape;
         *   (properties?: game.AnnouncementBoard.$Properties): game.AnnouncementBoard;
         * }}
         */
        AnnouncementBoard.create = function(properties) {
            return new AnnouncementBoard(properties);
        };

        /**
         * Encodes the specified AnnouncementBoard message. Does not implicitly {@link game.AnnouncementBoard.verify|verify} messages.
         * @function encode
         * @memberof game.AnnouncementBoard
         * @static
         * @param {game.AnnouncementBoard.$Properties} message AnnouncementBoard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AnnouncementBoard.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.enabled != null && $Object.hasOwnProperty.call(message, "enabled") && message.enabled !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.enabled);
            if (message.title != null && $Object.hasOwnProperty.call(message, "title") && message.title !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.title);
            if (message.content != null && $Object.hasOwnProperty.call(message, "content") && message.content !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.content);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AnnouncementBoard message, length delimited. Does not implicitly {@link game.AnnouncementBoard.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.AnnouncementBoard
         * @static
         * @param {game.AnnouncementBoard.$Properties} message AnnouncementBoard message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AnnouncementBoard.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AnnouncementBoard message from the specified reader or buffer.
         * @function decode
         * @memberof game.AnnouncementBoard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.AnnouncementBoard & game.AnnouncementBoard.$Shape} AnnouncementBoard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AnnouncementBoard.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.AnnouncementBoard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enabled = value;
                        else
                            delete message.enabled;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AnnouncementBoard message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.AnnouncementBoard
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.AnnouncementBoard & game.AnnouncementBoard.$Shape} AnnouncementBoard
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AnnouncementBoard.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AnnouncementBoard message.
         * @function verify
         * @memberof game.AnnouncementBoard
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AnnouncementBoard.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enabled != null && $Object.hasOwnProperty.call(message, "enabled"))
                if (typeof message.enabled !== "boolean")
                    return "enabled: boolean expected";
            if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            return null;
        };

        /**
         * Creates an AnnouncementBoard message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.AnnouncementBoard
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.AnnouncementBoard} AnnouncementBoard
         */
        AnnouncementBoard.fromObject = function (object, _depth) {
            if (object instanceof $root.game.AnnouncementBoard)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.AnnouncementBoard: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.AnnouncementBoard();
            if (object.enabled != null)
                if (object.enabled)
                    message.enabled = $Boolean(object.enabled);
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = $String(object.title);
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = $String(object.content);
            return message;
        };

        /**
         * Creates a plain object from an AnnouncementBoard message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.AnnouncementBoard
         * @static
         * @param {game.AnnouncementBoard} message AnnouncementBoard
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AnnouncementBoard.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.enabled = false;
                object.title = "";
                object.content = "";
            }
            if (message.enabled != null && $Object.hasOwnProperty.call(message, "enabled"))
                object.enabled = message.enabled;
            if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                object.content = message.content;
            return object;
        };

        /**
         * Converts this AnnouncementBoard to JSON.
         * @function toJSON
         * @memberof game.AnnouncementBoard
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AnnouncementBoard.prototype.toJSON = function() {
            return AnnouncementBoard.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AnnouncementBoard
         * @function getTypeUrl
         * @memberof game.AnnouncementBoard
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AnnouncementBoard.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.AnnouncementBoard";
        };

        return AnnouncementBoard;
    })();

    game.SecurityDisclaimerConfig = (function() {

        /**
         * Properties of a SecurityDisclaimerConfig.
         * @typedef {Object} game.SecurityDisclaimerConfig.$Properties
         * @property {boolean|null} [enabled] SecurityDisclaimerConfig enabled
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SecurityDisclaimerConfig.
         * @memberof game
         * @interface ISecurityDisclaimerConfig
         * @augments game.SecurityDisclaimerConfig.$Properties
         * @deprecated Use game.SecurityDisclaimerConfig.$Properties instead.
         */

        /**
         * Shape of a SecurityDisclaimerConfig.
         * @typedef {game.SecurityDisclaimerConfig.$Properties} game.SecurityDisclaimerConfig.$Shape
         */

        /**
         * Constructs a new SecurityDisclaimerConfig.
         * @memberof game
         * @classdesc Represents a SecurityDisclaimerConfig.
         * @constructor
         * @param {game.SecurityDisclaimerConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SecurityDisclaimerConfig = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SecurityDisclaimerConfig enabled.
         * @member {boolean} enabled
         * @memberof game.SecurityDisclaimerConfig
         * @instance
         */
        SecurityDisclaimerConfig.prototype.enabled = false;

        /**
         * Creates a new SecurityDisclaimerConfig instance using the specified properties.
         * @function create
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {game.SecurityDisclaimerConfig.$Properties=} [properties] Properties to set
         * @returns {game.SecurityDisclaimerConfig} SecurityDisclaimerConfig instance
         * @type {{
         *   (properties: game.SecurityDisclaimerConfig.$Shape): game.SecurityDisclaimerConfig & game.SecurityDisclaimerConfig.$Shape;
         *   (properties?: game.SecurityDisclaimerConfig.$Properties): game.SecurityDisclaimerConfig;
         * }}
         */
        SecurityDisclaimerConfig.create = function(properties) {
            return new SecurityDisclaimerConfig(properties);
        };

        /**
         * Encodes the specified SecurityDisclaimerConfig message. Does not implicitly {@link game.SecurityDisclaimerConfig.verify|verify} messages.
         * @function encode
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {game.SecurityDisclaimerConfig.$Properties} message SecurityDisclaimerConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityDisclaimerConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.enabled != null && $Object.hasOwnProperty.call(message, "enabled") && message.enabled !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.enabled);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SecurityDisclaimerConfig message, length delimited. Does not implicitly {@link game.SecurityDisclaimerConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {game.SecurityDisclaimerConfig.$Properties} message SecurityDisclaimerConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityDisclaimerConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SecurityDisclaimerConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SecurityDisclaimerConfig & game.SecurityDisclaimerConfig.$Shape} SecurityDisclaimerConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityDisclaimerConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SecurityDisclaimerConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enabled = value;
                        else
                            delete message.enabled;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SecurityDisclaimerConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SecurityDisclaimerConfig & game.SecurityDisclaimerConfig.$Shape} SecurityDisclaimerConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityDisclaimerConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SecurityDisclaimerConfig message.
         * @function verify
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SecurityDisclaimerConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enabled != null && $Object.hasOwnProperty.call(message, "enabled"))
                if (typeof message.enabled !== "boolean")
                    return "enabled: boolean expected";
            return null;
        };

        /**
         * Creates a SecurityDisclaimerConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SecurityDisclaimerConfig} SecurityDisclaimerConfig
         */
        SecurityDisclaimerConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SecurityDisclaimerConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SecurityDisclaimerConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SecurityDisclaimerConfig();
            if (object.enabled != null)
                if (object.enabled)
                    message.enabled = $Boolean(object.enabled);
            return message;
        };

        /**
         * Creates a plain object from a SecurityDisclaimerConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {game.SecurityDisclaimerConfig} message SecurityDisclaimerConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityDisclaimerConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.enabled = false;
            if (message.enabled != null && $Object.hasOwnProperty.call(message, "enabled"))
                object.enabled = message.enabled;
            return object;
        };

        /**
         * Converts this SecurityDisclaimerConfig to JSON.
         * @function toJSON
         * @memberof game.SecurityDisclaimerConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityDisclaimerConfig.prototype.toJSON = function() {
            return SecurityDisclaimerConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SecurityDisclaimerConfig
         * @function getTypeUrl
         * @memberof game.SecurityDisclaimerConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SecurityDisclaimerConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SecurityDisclaimerConfig";
        };

        return SecurityDisclaimerConfig;
    })();

    game.DoublePair = (function() {

        /**
         * Properties of a DoublePair.
         * @typedef {Object} game.DoublePair.$Properties
         * @property {string|null} [key] DoublePair key
         * @property {number|null} [value] DoublePair value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a DoublePair.
         * @memberof game
         * @interface IDoublePair
         * @augments game.DoublePair.$Properties
         * @deprecated Use game.DoublePair.$Properties instead.
         */

        /**
         * Shape of a DoublePair.
         * @typedef {game.DoublePair.$Properties} game.DoublePair.$Shape
         */

        /**
         * Constructs a new DoublePair.
         * @memberof game
         * @classdesc Represents a DoublePair.
         * @constructor
         * @param {game.DoublePair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const DoublePair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * DoublePair key.
         * @member {string} key
         * @memberof game.DoublePair
         * @instance
         */
        DoublePair.prototype.key = "";

        /**
         * DoublePair value.
         * @member {number} value
         * @memberof game.DoublePair
         * @instance
         */
        DoublePair.prototype.value = 0;

        /**
         * Creates a new DoublePair instance using the specified properties.
         * @function create
         * @memberof game.DoublePair
         * @static
         * @param {game.DoublePair.$Properties=} [properties] Properties to set
         * @returns {game.DoublePair} DoublePair instance
         * @type {{
         *   (properties: game.DoublePair.$Shape): game.DoublePair & game.DoublePair.$Shape;
         *   (properties?: game.DoublePair.$Properties): game.DoublePair;
         * }}
         */
        DoublePair.create = function(properties) {
            return new DoublePair(properties);
        };

        /**
         * Encodes the specified DoublePair message. Does not implicitly {@link game.DoublePair.verify|verify} messages.
         * @function encode
         * @memberof game.DoublePair
         * @static
         * @param {game.DoublePair.$Properties} message DoublePair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DoublePair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DoublePair message, length delimited. Does not implicitly {@link game.DoublePair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.DoublePair
         * @static
         * @param {game.DoublePair.$Properties} message DoublePair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DoublePair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a DoublePair message from the specified reader or buffer.
         * @function decode
         * @memberof game.DoublePair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.DoublePair & game.DoublePair.$Shape} DoublePair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DoublePair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.DoublePair(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a DoublePair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.DoublePair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.DoublePair & game.DoublePair.$Shape} DoublePair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DoublePair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DoublePair message.
         * @function verify
         * @memberof game.DoublePair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DoublePair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!$util.isString(message.key))
                    return "key: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            return null;
        };

        /**
         * Creates a DoublePair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.DoublePair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.DoublePair} DoublePair
         */
        DoublePair.fromObject = function (object, _depth) {
            if (object instanceof $root.game.DoublePair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.DoublePair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.DoublePair();
            if (object.key != null)
                if (typeof object.key !== "string" || object.key.length)
                    message.key = $String(object.key);
            if (object.value != null)
                if (!$Object.is($Number(object.value), 0))
                    message.value = $Number(object.value);
            return message;
        };

        /**
         * Creates a plain object from a DoublePair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.DoublePair
         * @static
         * @param {game.DoublePair} message DoublePair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DoublePair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = 0;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
            return object;
        };

        /**
         * Converts this DoublePair to JSON.
         * @function toJSON
         * @memberof game.DoublePair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DoublePair.prototype.toJSON = function() {
            return DoublePair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for DoublePair
         * @function getTypeUrl
         * @memberof game.DoublePair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        DoublePair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.DoublePair";
        };

        return DoublePair;
    })();

    game.ExtremeModeConfig = (function() {

        /**
         * Properties of an ExtremeModeConfig.
         * @typedef {Object} game.ExtremeModeConfig.$Properties
         * @property {string|null} [label] ExtremeModeConfig label
         * @property {string|null} [emoji] ExtremeModeConfig emoji
         * @property {number|null} [cooldownHours] ExtremeModeConfig cooldownHours
         * @property {Array.<game.DoublePair.$Properties>|null} [positiveLossRates] ExtremeModeConfig positiveLossRates
         * @property {Array.<game.DoublePair.$Properties>|null} [negativeWinRates] ExtremeModeConfig negativeWinRates
         * @property {Array.<game.DoublePair.$Properties>|null} [hourlyDecay] ExtremeModeConfig hourlyDecay
         * @property {number|null} [winStreakThreshold] ExtremeModeConfig winStreakThreshold
         * @property {number|null} [winStreakCrashChance] ExtremeModeConfig winStreakCrashChance
         * @property {number|null} [crashTargetPoints] ExtremeModeConfig crashTargetPoints
         * @property {string|null} [forceCloseWarning] ExtremeModeConfig forceCloseWarning
         * @property {number|null} [forceRenameMinPoints] ExtremeModeConfig forceRenameMinPoints
         * @property {number|null} [forceRenameProtectHours] ExtremeModeConfig forceRenameProtectHours
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an ExtremeModeConfig.
         * @memberof game
         * @interface IExtremeModeConfig
         * @augments game.ExtremeModeConfig.$Properties
         * @deprecated Use game.ExtremeModeConfig.$Properties instead.
         */

        /**
         * Shape of an ExtremeModeConfig.
         * @typedef {game.ExtremeModeConfig.$Properties} game.ExtremeModeConfig.$Shape
         */

        /**
         * Constructs a new ExtremeModeConfig.
         * @memberof game
         * @classdesc Represents an ExtremeModeConfig.
         * @constructor
         * @param {game.ExtremeModeConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ExtremeModeConfig = function (properties) {
            this.positiveLossRates = [];
            this.negativeWinRates = [];
            this.hourlyDecay = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ExtremeModeConfig label.
         * @member {string} label
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.label = "";

        /**
         * ExtremeModeConfig emoji.
         * @member {string} emoji
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.emoji = "";

        /**
         * ExtremeModeConfig cooldownHours.
         * @member {number} cooldownHours
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.cooldownHours = 0;

        /**
         * ExtremeModeConfig positiveLossRates.
         * @member {Array.<game.DoublePair.$Properties>} positiveLossRates
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.positiveLossRates = $util.emptyArray;

        /**
         * ExtremeModeConfig negativeWinRates.
         * @member {Array.<game.DoublePair.$Properties>} negativeWinRates
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.negativeWinRates = $util.emptyArray;

        /**
         * ExtremeModeConfig hourlyDecay.
         * @member {Array.<game.DoublePair.$Properties>} hourlyDecay
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.hourlyDecay = $util.emptyArray;

        /**
         * ExtremeModeConfig winStreakThreshold.
         * @member {number} winStreakThreshold
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.winStreakThreshold = 0;

        /**
         * ExtremeModeConfig winStreakCrashChance.
         * @member {number} winStreakCrashChance
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.winStreakCrashChance = 0;

        /**
         * ExtremeModeConfig crashTargetPoints.
         * @member {number} crashTargetPoints
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.crashTargetPoints = 0;

        /**
         * ExtremeModeConfig forceCloseWarning.
         * @member {string} forceCloseWarning
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.forceCloseWarning = "";

        /**
         * ExtremeModeConfig forceRenameMinPoints.
         * @member {number} forceRenameMinPoints
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.forceRenameMinPoints = 0;

        /**
         * ExtremeModeConfig forceRenameProtectHours.
         * @member {number} forceRenameProtectHours
         * @memberof game.ExtremeModeConfig
         * @instance
         */
        ExtremeModeConfig.prototype.forceRenameProtectHours = 0;

        /**
         * Creates a new ExtremeModeConfig instance using the specified properties.
         * @function create
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {game.ExtremeModeConfig.$Properties=} [properties] Properties to set
         * @returns {game.ExtremeModeConfig} ExtremeModeConfig instance
         * @type {{
         *   (properties: game.ExtremeModeConfig.$Shape): game.ExtremeModeConfig & game.ExtremeModeConfig.$Shape;
         *   (properties?: game.ExtremeModeConfig.$Properties): game.ExtremeModeConfig;
         * }}
         */
        ExtremeModeConfig.create = function(properties) {
            return new ExtremeModeConfig(properties);
        };

        /**
         * Encodes the specified ExtremeModeConfig message. Does not implicitly {@link game.ExtremeModeConfig.verify|verify} messages.
         * @function encode
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {game.ExtremeModeConfig.$Properties} message ExtremeModeConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExtremeModeConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.label != null && $Object.hasOwnProperty.call(message, "label") && message.label !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.label);
            if (message.emoji != null && $Object.hasOwnProperty.call(message, "emoji") && message.emoji !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.emoji);
            if (message.cooldownHours != null && $Object.hasOwnProperty.call(message, "cooldownHours") && message.cooldownHours !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.cooldownHours);
            if (message.positiveLossRates != null && message.positiveLossRates.length)
                for (let i = 0; i < message.positiveLossRates.length; ++i)
                    $root.game.DoublePair.encode(message.positiveLossRates[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.negativeWinRates != null && message.negativeWinRates.length)
                for (let i = 0; i < message.negativeWinRates.length; ++i)
                    $root.game.DoublePair.encode(message.negativeWinRates[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.hourlyDecay != null && message.hourlyDecay.length)
                for (let i = 0; i < message.hourlyDecay.length; ++i)
                    $root.game.DoublePair.encode(message.hourlyDecay[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.winStreakThreshold != null && $Object.hasOwnProperty.call(message, "winStreakThreshold") && message.winStreakThreshold !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.winStreakThreshold);
            if (message.winStreakCrashChance != null && $Object.hasOwnProperty.call(message, "winStreakCrashChance") && !$Object.is(message.winStreakCrashChance, 0))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.winStreakCrashChance);
            if (message.crashTargetPoints != null && $Object.hasOwnProperty.call(message, "crashTargetPoints") && message.crashTargetPoints !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.crashTargetPoints);
            if (message.forceCloseWarning != null && $Object.hasOwnProperty.call(message, "forceCloseWarning") && message.forceCloseWarning !== "")
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.forceCloseWarning);
            if (message.forceRenameMinPoints != null && $Object.hasOwnProperty.call(message, "forceRenameMinPoints") && message.forceRenameMinPoints !== 0)
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.forceRenameMinPoints);
            if (message.forceRenameProtectHours != null && $Object.hasOwnProperty.call(message, "forceRenameProtectHours") && message.forceRenameProtectHours !== 0)
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.forceRenameProtectHours);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ExtremeModeConfig message, length delimited. Does not implicitly {@link game.ExtremeModeConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {game.ExtremeModeConfig.$Properties} message ExtremeModeConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExtremeModeConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an ExtremeModeConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.ExtremeModeConfig & game.ExtremeModeConfig.$Shape} ExtremeModeConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExtremeModeConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.ExtremeModeConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.label = value;
                        else
                            delete message.label;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.emoji = value;
                        else
                            delete message.emoji;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.cooldownHours = value;
                        else
                            delete message.cooldownHours;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.positiveLossRates && message.positiveLossRates.length))
                            message.positiveLossRates = [];
                        message.positiveLossRates.push($root.game.DoublePair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.negativeWinRates && message.negativeWinRates.length))
                            message.negativeWinRates = [];
                        message.negativeWinRates.push($root.game.DoublePair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hourlyDecay && message.hourlyDecay.length))
                            message.hourlyDecay = [];
                        message.hourlyDecay.push($root.game.DoublePair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.winStreakThreshold = value;
                        else
                            delete message.winStreakThreshold;
                        continue;
                    }
                case 8: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.winStreakCrashChance = value;
                        else
                            delete message.winStreakCrashChance;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.crashTargetPoints = value;
                        else
                            delete message.crashTargetPoints;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.forceCloseWarning = value;
                        else
                            delete message.forceCloseWarning;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.forceRenameMinPoints = value;
                        else
                            delete message.forceRenameMinPoints;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.forceRenameProtectHours = value;
                        else
                            delete message.forceRenameProtectHours;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ExtremeModeConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.ExtremeModeConfig & game.ExtremeModeConfig.$Shape} ExtremeModeConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExtremeModeConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExtremeModeConfig message.
         * @function verify
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExtremeModeConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                if (!$util.isString(message.label))
                    return "label: string expected";
            if (message.emoji != null && $Object.hasOwnProperty.call(message, "emoji"))
                if (!$util.isString(message.emoji))
                    return "emoji: string expected";
            if (message.cooldownHours != null && $Object.hasOwnProperty.call(message, "cooldownHours"))
                if (!$util.isInteger(message.cooldownHours))
                    return "cooldownHours: integer expected";
            if (message.positiveLossRates != null && $Object.hasOwnProperty.call(message, "positiveLossRates")) {
                if (!$Array.isArray(message.positiveLossRates))
                    return "positiveLossRates: array expected";
                for (let i = 0; i < message.positiveLossRates.length; ++i) {
                    let error = $root.game.DoublePair.verify(message.positiveLossRates[i], _depth + 1);
                    if (error)
                        return "positiveLossRates." + error;
                }
            }
            if (message.negativeWinRates != null && $Object.hasOwnProperty.call(message, "negativeWinRates")) {
                if (!$Array.isArray(message.negativeWinRates))
                    return "negativeWinRates: array expected";
                for (let i = 0; i < message.negativeWinRates.length; ++i) {
                    let error = $root.game.DoublePair.verify(message.negativeWinRates[i], _depth + 1);
                    if (error)
                        return "negativeWinRates." + error;
                }
            }
            if (message.hourlyDecay != null && $Object.hasOwnProperty.call(message, "hourlyDecay")) {
                if (!$Array.isArray(message.hourlyDecay))
                    return "hourlyDecay: array expected";
                for (let i = 0; i < message.hourlyDecay.length; ++i) {
                    let error = $root.game.DoublePair.verify(message.hourlyDecay[i], _depth + 1);
                    if (error)
                        return "hourlyDecay." + error;
                }
            }
            if (message.winStreakThreshold != null && $Object.hasOwnProperty.call(message, "winStreakThreshold"))
                if (!$util.isInteger(message.winStreakThreshold))
                    return "winStreakThreshold: integer expected";
            if (message.winStreakCrashChance != null && $Object.hasOwnProperty.call(message, "winStreakCrashChance"))
                if (typeof message.winStreakCrashChance !== "number")
                    return "winStreakCrashChance: number expected";
            if (message.crashTargetPoints != null && $Object.hasOwnProperty.call(message, "crashTargetPoints"))
                if (!$util.isInteger(message.crashTargetPoints))
                    return "crashTargetPoints: integer expected";
            if (message.forceCloseWarning != null && $Object.hasOwnProperty.call(message, "forceCloseWarning"))
                if (!$util.isString(message.forceCloseWarning))
                    return "forceCloseWarning: string expected";
            if (message.forceRenameMinPoints != null && $Object.hasOwnProperty.call(message, "forceRenameMinPoints"))
                if (!$util.isInteger(message.forceRenameMinPoints))
                    return "forceRenameMinPoints: integer expected";
            if (message.forceRenameProtectHours != null && $Object.hasOwnProperty.call(message, "forceRenameProtectHours"))
                if (!$util.isInteger(message.forceRenameProtectHours))
                    return "forceRenameProtectHours: integer expected";
            return null;
        };

        /**
         * Creates an ExtremeModeConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.ExtremeModeConfig} ExtremeModeConfig
         */
        ExtremeModeConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.ExtremeModeConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.ExtremeModeConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.ExtremeModeConfig();
            if (object.label != null)
                if (typeof object.label !== "string" || object.label.length)
                    message.label = $String(object.label);
            if (object.emoji != null)
                if (typeof object.emoji !== "string" || object.emoji.length)
                    message.emoji = $String(object.emoji);
            if (object.cooldownHours != null)
                if ($Number(object.cooldownHours) !== 0)
                    message.cooldownHours = object.cooldownHours | 0;
            if (object.positiveLossRates) {
                if (!$Array.isArray(object.positiveLossRates))
                    throw $TypeError(".game.ExtremeModeConfig.positiveLossRates: array expected");
                message.positiveLossRates = $Array(object.positiveLossRates.length);
                for (let i = 0; i < object.positiveLossRates.length; ++i) {
                    if (!$util.isObject(object.positiveLossRates[i]))
                        throw $TypeError(".game.ExtremeModeConfig.positiveLossRates: object expected");
                    message.positiveLossRates[i] = $root.game.DoublePair.fromObject(object.positiveLossRates[i], _depth + 1);
                }
            }
            if (object.negativeWinRates) {
                if (!$Array.isArray(object.negativeWinRates))
                    throw $TypeError(".game.ExtremeModeConfig.negativeWinRates: array expected");
                message.negativeWinRates = $Array(object.negativeWinRates.length);
                for (let i = 0; i < object.negativeWinRates.length; ++i) {
                    if (!$util.isObject(object.negativeWinRates[i]))
                        throw $TypeError(".game.ExtremeModeConfig.negativeWinRates: object expected");
                    message.negativeWinRates[i] = $root.game.DoublePair.fromObject(object.negativeWinRates[i], _depth + 1);
                }
            }
            if (object.hourlyDecay) {
                if (!$Array.isArray(object.hourlyDecay))
                    throw $TypeError(".game.ExtremeModeConfig.hourlyDecay: array expected");
                message.hourlyDecay = $Array(object.hourlyDecay.length);
                for (let i = 0; i < object.hourlyDecay.length; ++i) {
                    if (!$util.isObject(object.hourlyDecay[i]))
                        throw $TypeError(".game.ExtremeModeConfig.hourlyDecay: object expected");
                    message.hourlyDecay[i] = $root.game.DoublePair.fromObject(object.hourlyDecay[i], _depth + 1);
                }
            }
            if (object.winStreakThreshold != null)
                if ($Number(object.winStreakThreshold) !== 0)
                    message.winStreakThreshold = object.winStreakThreshold | 0;
            if (object.winStreakCrashChance != null)
                if (!$Object.is($Number(object.winStreakCrashChance), 0))
                    message.winStreakCrashChance = $Number(object.winStreakCrashChance);
            if (object.crashTargetPoints != null)
                if ($Number(object.crashTargetPoints) !== 0)
                    message.crashTargetPoints = object.crashTargetPoints | 0;
            if (object.forceCloseWarning != null)
                if (typeof object.forceCloseWarning !== "string" || object.forceCloseWarning.length)
                    message.forceCloseWarning = $String(object.forceCloseWarning);
            if (object.forceRenameMinPoints != null)
                if ($Number(object.forceRenameMinPoints) !== 0)
                    message.forceRenameMinPoints = object.forceRenameMinPoints | 0;
            if (object.forceRenameProtectHours != null)
                if ($Number(object.forceRenameProtectHours) !== 0)
                    message.forceRenameProtectHours = object.forceRenameProtectHours | 0;
            return message;
        };

        /**
         * Creates a plain object from an ExtremeModeConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {game.ExtremeModeConfig} message ExtremeModeConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExtremeModeConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.positiveLossRates = [];
                object.negativeWinRates = [];
                object.hourlyDecay = [];
            }
            if (options.defaults) {
                object.label = "";
                object.emoji = "";
                object.cooldownHours = 0;
                object.winStreakThreshold = 0;
                object.winStreakCrashChance = 0;
                object.crashTargetPoints = 0;
                object.forceCloseWarning = "";
                object.forceRenameMinPoints = 0;
                object.forceRenameProtectHours = 0;
            }
            if (message.label != null && $Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.emoji != null && $Object.hasOwnProperty.call(message, "emoji"))
                object.emoji = message.emoji;
            if (message.cooldownHours != null && $Object.hasOwnProperty.call(message, "cooldownHours"))
                object.cooldownHours = message.cooldownHours;
            if (message.positiveLossRates && message.positiveLossRates.length) {
                object.positiveLossRates = $Array(message.positiveLossRates.length);
                for (let j = 0; j < message.positiveLossRates.length; ++j)
                    object.positiveLossRates[j] = $root.game.DoublePair.toObject(message.positiveLossRates[j], options, _depth + 1);
            }
            if (message.negativeWinRates && message.negativeWinRates.length) {
                object.negativeWinRates = $Array(message.negativeWinRates.length);
                for (let j = 0; j < message.negativeWinRates.length; ++j)
                    object.negativeWinRates[j] = $root.game.DoublePair.toObject(message.negativeWinRates[j], options, _depth + 1);
            }
            if (message.hourlyDecay && message.hourlyDecay.length) {
                object.hourlyDecay = $Array(message.hourlyDecay.length);
                for (let j = 0; j < message.hourlyDecay.length; ++j)
                    object.hourlyDecay[j] = $root.game.DoublePair.toObject(message.hourlyDecay[j], options, _depth + 1);
            }
            if (message.winStreakThreshold != null && $Object.hasOwnProperty.call(message, "winStreakThreshold"))
                object.winStreakThreshold = message.winStreakThreshold;
            if (message.winStreakCrashChance != null && $Object.hasOwnProperty.call(message, "winStreakCrashChance"))
                object.winStreakCrashChance = options.json && !$isFinite(message.winStreakCrashChance) ? $String(message.winStreakCrashChance) : message.winStreakCrashChance;
            if (message.crashTargetPoints != null && $Object.hasOwnProperty.call(message, "crashTargetPoints"))
                object.crashTargetPoints = message.crashTargetPoints;
            if (message.forceCloseWarning != null && $Object.hasOwnProperty.call(message, "forceCloseWarning"))
                object.forceCloseWarning = message.forceCloseWarning;
            if (message.forceRenameMinPoints != null && $Object.hasOwnProperty.call(message, "forceRenameMinPoints"))
                object.forceRenameMinPoints = message.forceRenameMinPoints;
            if (message.forceRenameProtectHours != null && $Object.hasOwnProperty.call(message, "forceRenameProtectHours"))
                object.forceRenameProtectHours = message.forceRenameProtectHours;
            return object;
        };

        /**
         * Converts this ExtremeModeConfig to JSON.
         * @function toJSON
         * @memberof game.ExtremeModeConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExtremeModeConfig.prototype.toJSON = function() {
            return ExtremeModeConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ExtremeModeConfig
         * @function getTypeUrl
         * @memberof game.ExtremeModeConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ExtremeModeConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.ExtremeModeConfig";
        };

        return ExtremeModeConfig;
    })();

    game.SiteConfig = (function() {

        /**
         * Properties of a SiteConfig.
         * @typedef {Object} game.SiteConfig.$Properties
         * @property {string|null} [name] SiteConfig name
         * @property {string|null} [description] SiteConfig description
         * @property {string|null} [adminPassword] SiteConfig adminPassword
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SiteConfig.
         * @memberof game
         * @interface ISiteConfig
         * @augments game.SiteConfig.$Properties
         * @deprecated Use game.SiteConfig.$Properties instead.
         */

        /**
         * Shape of a SiteConfig.
         * @typedef {game.SiteConfig.$Properties} game.SiteConfig.$Shape
         */

        /**
         * Constructs a new SiteConfig.
         * @memberof game
         * @classdesc Represents a SiteConfig.
         * @constructor
         * @param {game.SiteConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SiteConfig = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SiteConfig name.
         * @member {string} name
         * @memberof game.SiteConfig
         * @instance
         */
        SiteConfig.prototype.name = "";

        /**
         * SiteConfig description.
         * @member {string} description
         * @memberof game.SiteConfig
         * @instance
         */
        SiteConfig.prototype.description = "";

        /**
         * SiteConfig adminPassword.
         * @member {string} adminPassword
         * @memberof game.SiteConfig
         * @instance
         */
        SiteConfig.prototype.adminPassword = "";

        /**
         * Creates a new SiteConfig instance using the specified properties.
         * @function create
         * @memberof game.SiteConfig
         * @static
         * @param {game.SiteConfig.$Properties=} [properties] Properties to set
         * @returns {game.SiteConfig} SiteConfig instance
         * @type {{
         *   (properties: game.SiteConfig.$Shape): game.SiteConfig & game.SiteConfig.$Shape;
         *   (properties?: game.SiteConfig.$Properties): game.SiteConfig;
         * }}
         */
        SiteConfig.create = function(properties) {
            return new SiteConfig(properties);
        };

        /**
         * Encodes the specified SiteConfig message. Does not implicitly {@link game.SiteConfig.verify|verify} messages.
         * @function encode
         * @memberof game.SiteConfig
         * @static
         * @param {game.SiteConfig.$Properties} message SiteConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SiteConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
            if (message.adminPassword != null && $Object.hasOwnProperty.call(message, "adminPassword") && message.adminPassword !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.adminPassword);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SiteConfig message, length delimited. Does not implicitly {@link game.SiteConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SiteConfig
         * @static
         * @param {game.SiteConfig.$Properties} message SiteConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SiteConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SiteConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.SiteConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SiteConfig & game.SiteConfig.$Shape} SiteConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SiteConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SiteConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.description = value;
                        else
                            delete message.description;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.adminPassword = value;
                        else
                            delete message.adminPassword;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SiteConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SiteConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SiteConfig & game.SiteConfig.$Shape} SiteConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SiteConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SiteConfig message.
         * @function verify
         * @memberof game.SiteConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SiteConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.adminPassword != null && $Object.hasOwnProperty.call(message, "adminPassword"))
                if (!$util.isString(message.adminPassword))
                    return "adminPassword: string expected";
            return null;
        };

        /**
         * Creates a SiteConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SiteConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SiteConfig} SiteConfig
         */
        SiteConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SiteConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SiteConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SiteConfig();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.description != null)
                if (typeof object.description !== "string" || object.description.length)
                    message.description = $String(object.description);
            if (object.adminPassword != null)
                if (typeof object.adminPassword !== "string" || object.adminPassword.length)
                    message.adminPassword = $String(object.adminPassword);
            return message;
        };

        /**
         * Creates a plain object from a SiteConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SiteConfig
         * @static
         * @param {game.SiteConfig} message SiteConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SiteConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.description = "";
                object.adminPassword = "";
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.adminPassword != null && $Object.hasOwnProperty.call(message, "adminPassword"))
                object.adminPassword = message.adminPassword;
            return object;
        };

        /**
         * Converts this SiteConfig to JSON.
         * @function toJSON
         * @memberof game.SiteConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SiteConfig.prototype.toJSON = function() {
            return SiteConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SiteConfig
         * @function getTypeUrl
         * @memberof game.SiteConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SiteConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SiteConfig";
        };

        return SiteConfig;
    })();

    game.AccessControlConfig = (function() {

        /**
         * Properties of an AccessControlConfig.
         * @typedef {Object} game.AccessControlConfig.$Properties
         * @property {number|null} [maxOnlinePerIp] AccessControlConfig maxOnlinePerIp
         * @property {number|null} [maxCreatesPer_10Min] AccessControlConfig maxCreatesPer_10Min
         * @property {number|null} [ipBackstopMultiplier] AccessControlConfig ipBackstopMultiplier
         * @property {number|null} [ipBackstopMinLimit] AccessControlConfig ipBackstopMinLimit
         * @property {number|null} [maxSessionIssuePerIp] AccessControlConfig maxSessionIssuePerIp
         * @property {number|null} [maxOnlinePerIpTotal] AccessControlConfig maxOnlinePerIpTotal
         * @property {number|null} [maxCreatesPerIp] AccessControlConfig maxCreatesPerIp
         * @property {number|null} [maxActiveRoomsPerOwner] AccessControlConfig maxActiveRoomsPerOwner
         * @property {number|null} [maxProofUploadsPerPlayer] AccessControlConfig maxProofUploadsPerPlayer
         * @property {boolean|null} [registrationDisabled] AccessControlConfig registrationDisabled
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AccessControlConfig.
         * @memberof game
         * @interface IAccessControlConfig
         * @augments game.AccessControlConfig.$Properties
         * @deprecated Use game.AccessControlConfig.$Properties instead.
         */

        /**
         * Shape of an AccessControlConfig.
         * @typedef {game.AccessControlConfig.$Properties} game.AccessControlConfig.$Shape
         */

        /**
         * Constructs a new AccessControlConfig.
         * @memberof game
         * @classdesc Represents an AccessControlConfig.
         * @constructor
         * @param {game.AccessControlConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AccessControlConfig = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AccessControlConfig maxOnlinePerIp.
         * @member {number} maxOnlinePerIp
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.maxOnlinePerIp = 0;

        /**
         * AccessControlConfig maxCreatesPer_10Min.
         * @member {number} maxCreatesPer_10Min
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.maxCreatesPer_10Min = 0;

        /**
         * AccessControlConfig ipBackstopMultiplier.
         * @member {number} ipBackstopMultiplier
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.ipBackstopMultiplier = 0;

        /**
         * AccessControlConfig ipBackstopMinLimit.
         * @member {number} ipBackstopMinLimit
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.ipBackstopMinLimit = 0;

        /**
         * AccessControlConfig maxSessionIssuePerIp.
         * @member {number} maxSessionIssuePerIp
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.maxSessionIssuePerIp = 0;

        /**
         * AccessControlConfig maxOnlinePerIpTotal.
         * @member {number} maxOnlinePerIpTotal
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.maxOnlinePerIpTotal = 0;

        /**
         * AccessControlConfig maxCreatesPerIp.
         * @member {number} maxCreatesPerIp
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.maxCreatesPerIp = 0;

        /**
         * AccessControlConfig maxActiveRoomsPerOwner.
         * @member {number} maxActiveRoomsPerOwner
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.maxActiveRoomsPerOwner = 0;

        /**
         * AccessControlConfig maxProofUploadsPerPlayer.
         * @member {number} maxProofUploadsPerPlayer
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.maxProofUploadsPerPlayer = 0;

        /**
         * AccessControlConfig registrationDisabled.
         * @member {boolean} registrationDisabled
         * @memberof game.AccessControlConfig
         * @instance
         */
        AccessControlConfig.prototype.registrationDisabled = false;

        /**
         * Creates a new AccessControlConfig instance using the specified properties.
         * @function create
         * @memberof game.AccessControlConfig
         * @static
         * @param {game.AccessControlConfig.$Properties=} [properties] Properties to set
         * @returns {game.AccessControlConfig} AccessControlConfig instance
         * @type {{
         *   (properties: game.AccessControlConfig.$Shape): game.AccessControlConfig & game.AccessControlConfig.$Shape;
         *   (properties?: game.AccessControlConfig.$Properties): game.AccessControlConfig;
         * }}
         */
        AccessControlConfig.create = function(properties) {
            return new AccessControlConfig(properties);
        };

        /**
         * Encodes the specified AccessControlConfig message. Does not implicitly {@link game.AccessControlConfig.verify|verify} messages.
         * @function encode
         * @memberof game.AccessControlConfig
         * @static
         * @param {game.AccessControlConfig.$Properties} message AccessControlConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccessControlConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.maxOnlinePerIp != null && $Object.hasOwnProperty.call(message, "maxOnlinePerIp") && message.maxOnlinePerIp !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.maxOnlinePerIp);
            if (message.maxCreatesPer_10Min != null && $Object.hasOwnProperty.call(message, "maxCreatesPer_10Min") && message.maxCreatesPer_10Min !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.maxCreatesPer_10Min);
            if (message.ipBackstopMultiplier != null && $Object.hasOwnProperty.call(message, "ipBackstopMultiplier") && message.ipBackstopMultiplier !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ipBackstopMultiplier);
            if (message.ipBackstopMinLimit != null && $Object.hasOwnProperty.call(message, "ipBackstopMinLimit") && message.ipBackstopMinLimit !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.ipBackstopMinLimit);
            if (message.maxSessionIssuePerIp != null && $Object.hasOwnProperty.call(message, "maxSessionIssuePerIp") && message.maxSessionIssuePerIp !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.maxSessionIssuePerIp);
            if (message.maxOnlinePerIpTotal != null && $Object.hasOwnProperty.call(message, "maxOnlinePerIpTotal") && message.maxOnlinePerIpTotal !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.maxOnlinePerIpTotal);
            if (message.maxCreatesPerIp != null && $Object.hasOwnProperty.call(message, "maxCreatesPerIp") && message.maxCreatesPerIp !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.maxCreatesPerIp);
            if (message.maxActiveRoomsPerOwner != null && $Object.hasOwnProperty.call(message, "maxActiveRoomsPerOwner") && message.maxActiveRoomsPerOwner !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.maxActiveRoomsPerOwner);
            if (message.maxProofUploadsPerPlayer != null && $Object.hasOwnProperty.call(message, "maxProofUploadsPerPlayer") && message.maxProofUploadsPerPlayer !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.maxProofUploadsPerPlayer);
            if (message.registrationDisabled != null && $Object.hasOwnProperty.call(message, "registrationDisabled") && message.registrationDisabled !== false)
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.registrationDisabled);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccessControlConfig message, length delimited. Does not implicitly {@link game.AccessControlConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.AccessControlConfig
         * @static
         * @param {game.AccessControlConfig.$Properties} message AccessControlConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccessControlConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AccessControlConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.AccessControlConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.AccessControlConfig & game.AccessControlConfig.$Shape} AccessControlConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccessControlConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.AccessControlConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxOnlinePerIp = value;
                        else
                            delete message.maxOnlinePerIp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxCreatesPer_10Min = value;
                        else
                            delete message.maxCreatesPer_10Min;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.ipBackstopMultiplier = value;
                        else
                            delete message.ipBackstopMultiplier;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.ipBackstopMinLimit = value;
                        else
                            delete message.ipBackstopMinLimit;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxSessionIssuePerIp = value;
                        else
                            delete message.maxSessionIssuePerIp;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxOnlinePerIpTotal = value;
                        else
                            delete message.maxOnlinePerIpTotal;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxCreatesPerIp = value;
                        else
                            delete message.maxCreatesPerIp;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxActiveRoomsPerOwner = value;
                        else
                            delete message.maxActiveRoomsPerOwner;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxProofUploadsPerPlayer = value;
                        else
                            delete message.maxProofUploadsPerPlayer;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.registrationDisabled = value;
                        else
                            delete message.registrationDisabled;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccessControlConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.AccessControlConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.AccessControlConfig & game.AccessControlConfig.$Shape} AccessControlConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccessControlConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccessControlConfig message.
         * @function verify
         * @memberof game.AccessControlConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccessControlConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.maxOnlinePerIp != null && $Object.hasOwnProperty.call(message, "maxOnlinePerIp"))
                if (!$util.isInteger(message.maxOnlinePerIp))
                    return "maxOnlinePerIp: integer expected";
            if (message.maxCreatesPer_10Min != null && $Object.hasOwnProperty.call(message, "maxCreatesPer_10Min"))
                if (!$util.isInteger(message.maxCreatesPer_10Min))
                    return "maxCreatesPer_10Min: integer expected";
            if (message.ipBackstopMultiplier != null && $Object.hasOwnProperty.call(message, "ipBackstopMultiplier"))
                if (!$util.isInteger(message.ipBackstopMultiplier))
                    return "ipBackstopMultiplier: integer expected";
            if (message.ipBackstopMinLimit != null && $Object.hasOwnProperty.call(message, "ipBackstopMinLimit"))
                if (!$util.isInteger(message.ipBackstopMinLimit))
                    return "ipBackstopMinLimit: integer expected";
            if (message.maxSessionIssuePerIp != null && $Object.hasOwnProperty.call(message, "maxSessionIssuePerIp"))
                if (!$util.isInteger(message.maxSessionIssuePerIp))
                    return "maxSessionIssuePerIp: integer expected";
            if (message.maxOnlinePerIpTotal != null && $Object.hasOwnProperty.call(message, "maxOnlinePerIpTotal"))
                if (!$util.isInteger(message.maxOnlinePerIpTotal))
                    return "maxOnlinePerIpTotal: integer expected";
            if (message.maxCreatesPerIp != null && $Object.hasOwnProperty.call(message, "maxCreatesPerIp"))
                if (!$util.isInteger(message.maxCreatesPerIp))
                    return "maxCreatesPerIp: integer expected";
            if (message.maxActiveRoomsPerOwner != null && $Object.hasOwnProperty.call(message, "maxActiveRoomsPerOwner"))
                if (!$util.isInteger(message.maxActiveRoomsPerOwner))
                    return "maxActiveRoomsPerOwner: integer expected";
            if (message.maxProofUploadsPerPlayer != null && $Object.hasOwnProperty.call(message, "maxProofUploadsPerPlayer"))
                if (!$util.isInteger(message.maxProofUploadsPerPlayer))
                    return "maxProofUploadsPerPlayer: integer expected";
            if (message.registrationDisabled != null && $Object.hasOwnProperty.call(message, "registrationDisabled"))
                if (typeof message.registrationDisabled !== "boolean")
                    return "registrationDisabled: boolean expected";
            return null;
        };

        /**
         * Creates an AccessControlConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.AccessControlConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.AccessControlConfig} AccessControlConfig
         */
        AccessControlConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.AccessControlConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.AccessControlConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.AccessControlConfig();
            if (object.maxOnlinePerIp != null)
                if ($Number(object.maxOnlinePerIp) !== 0)
                    message.maxOnlinePerIp = object.maxOnlinePerIp | 0;
            if (object.maxCreatesPer_10Min != null)
                if ($Number(object.maxCreatesPer_10Min) !== 0)
                    message.maxCreatesPer_10Min = object.maxCreatesPer_10Min | 0;
            if (object.ipBackstopMultiplier != null)
                if ($Number(object.ipBackstopMultiplier) !== 0)
                    message.ipBackstopMultiplier = object.ipBackstopMultiplier | 0;
            if (object.ipBackstopMinLimit != null)
                if ($Number(object.ipBackstopMinLimit) !== 0)
                    message.ipBackstopMinLimit = object.ipBackstopMinLimit | 0;
            if (object.maxSessionIssuePerIp != null)
                if ($Number(object.maxSessionIssuePerIp) !== 0)
                    message.maxSessionIssuePerIp = object.maxSessionIssuePerIp | 0;
            if (object.maxOnlinePerIpTotal != null)
                if ($Number(object.maxOnlinePerIpTotal) !== 0)
                    message.maxOnlinePerIpTotal = object.maxOnlinePerIpTotal | 0;
            if (object.maxCreatesPerIp != null)
                if ($Number(object.maxCreatesPerIp) !== 0)
                    message.maxCreatesPerIp = object.maxCreatesPerIp | 0;
            if (object.maxActiveRoomsPerOwner != null)
                if ($Number(object.maxActiveRoomsPerOwner) !== 0)
                    message.maxActiveRoomsPerOwner = object.maxActiveRoomsPerOwner | 0;
            if (object.maxProofUploadsPerPlayer != null)
                if ($Number(object.maxProofUploadsPerPlayer) !== 0)
                    message.maxProofUploadsPerPlayer = object.maxProofUploadsPerPlayer | 0;
            if (object.registrationDisabled != null)
                if (object.registrationDisabled)
                    message.registrationDisabled = $Boolean(object.registrationDisabled);
            return message;
        };

        /**
         * Creates a plain object from an AccessControlConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.AccessControlConfig
         * @static
         * @param {game.AccessControlConfig} message AccessControlConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccessControlConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.maxOnlinePerIp = 0;
                object.maxCreatesPer_10Min = 0;
                object.ipBackstopMultiplier = 0;
                object.ipBackstopMinLimit = 0;
                object.maxSessionIssuePerIp = 0;
                object.maxOnlinePerIpTotal = 0;
                object.maxCreatesPerIp = 0;
                object.maxActiveRoomsPerOwner = 0;
                object.maxProofUploadsPerPlayer = 0;
                object.registrationDisabled = false;
            }
            if (message.maxOnlinePerIp != null && $Object.hasOwnProperty.call(message, "maxOnlinePerIp"))
                object.maxOnlinePerIp = message.maxOnlinePerIp;
            if (message.maxCreatesPer_10Min != null && $Object.hasOwnProperty.call(message, "maxCreatesPer_10Min"))
                object.maxCreatesPer_10Min = message.maxCreatesPer_10Min;
            if (message.ipBackstopMultiplier != null && $Object.hasOwnProperty.call(message, "ipBackstopMultiplier"))
                object.ipBackstopMultiplier = message.ipBackstopMultiplier;
            if (message.ipBackstopMinLimit != null && $Object.hasOwnProperty.call(message, "ipBackstopMinLimit"))
                object.ipBackstopMinLimit = message.ipBackstopMinLimit;
            if (message.maxSessionIssuePerIp != null && $Object.hasOwnProperty.call(message, "maxSessionIssuePerIp"))
                object.maxSessionIssuePerIp = message.maxSessionIssuePerIp;
            if (message.maxOnlinePerIpTotal != null && $Object.hasOwnProperty.call(message, "maxOnlinePerIpTotal"))
                object.maxOnlinePerIpTotal = message.maxOnlinePerIpTotal;
            if (message.maxCreatesPerIp != null && $Object.hasOwnProperty.call(message, "maxCreatesPerIp"))
                object.maxCreatesPerIp = message.maxCreatesPerIp;
            if (message.maxActiveRoomsPerOwner != null && $Object.hasOwnProperty.call(message, "maxActiveRoomsPerOwner"))
                object.maxActiveRoomsPerOwner = message.maxActiveRoomsPerOwner;
            if (message.maxProofUploadsPerPlayer != null && $Object.hasOwnProperty.call(message, "maxProofUploadsPerPlayer"))
                object.maxProofUploadsPerPlayer = message.maxProofUploadsPerPlayer;
            if (message.registrationDisabled != null && $Object.hasOwnProperty.call(message, "registrationDisabled"))
                object.registrationDisabled = message.registrationDisabled;
            return object;
        };

        /**
         * Converts this AccessControlConfig to JSON.
         * @function toJSON
         * @memberof game.AccessControlConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccessControlConfig.prototype.toJSON = function() {
            return AccessControlConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccessControlConfig
         * @function getTypeUrl
         * @memberof game.AccessControlConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccessControlConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.AccessControlConfig";
        };

        return AccessControlConfig;
    })();

    game.NameWarConfig = (function() {

        /**
         * Properties of a NameWarConfig.
         * @typedef {Object} game.NameWarConfig.$Properties
         * @property {string|null} [penaltyPrefix] NameWarConfig penaltyPrefix
         * @property {string|null} [loserPanelTitle] NameWarConfig loserPanelTitle
         * @property {string|null} [escapeTitle] NameWarConfig escapeTitle
         * @property {string|null} [renamePanelTitle] NameWarConfig renamePanelTitle
         * @property {string|null} [nameWarLoserLabel] NameWarConfig nameWarLoserLabel
         * @property {string|null} [extremeForceClosedLabel] NameWarConfig extremeForceClosedLabel
         * @property {number|null} [penaltyThreshold] NameWarConfig penaltyThreshold
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NameWarConfig.
         * @memberof game
         * @interface INameWarConfig
         * @augments game.NameWarConfig.$Properties
         * @deprecated Use game.NameWarConfig.$Properties instead.
         */

        /**
         * Shape of a NameWarConfig.
         * @typedef {game.NameWarConfig.$Properties} game.NameWarConfig.$Shape
         */

        /**
         * Constructs a new NameWarConfig.
         * @memberof game
         * @classdesc Represents a NameWarConfig.
         * @constructor
         * @param {game.NameWarConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NameWarConfig = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * NameWarConfig penaltyPrefix.
         * @member {string} penaltyPrefix
         * @memberof game.NameWarConfig
         * @instance
         */
        NameWarConfig.prototype.penaltyPrefix = "";

        /**
         * NameWarConfig loserPanelTitle.
         * @member {string} loserPanelTitle
         * @memberof game.NameWarConfig
         * @instance
         */
        NameWarConfig.prototype.loserPanelTitle = "";

        /**
         * NameWarConfig escapeTitle.
         * @member {string} escapeTitle
         * @memberof game.NameWarConfig
         * @instance
         */
        NameWarConfig.prototype.escapeTitle = "";

        /**
         * NameWarConfig renamePanelTitle.
         * @member {string} renamePanelTitle
         * @memberof game.NameWarConfig
         * @instance
         */
        NameWarConfig.prototype.renamePanelTitle = "";

        /**
         * NameWarConfig nameWarLoserLabel.
         * @member {string} nameWarLoserLabel
         * @memberof game.NameWarConfig
         * @instance
         */
        NameWarConfig.prototype.nameWarLoserLabel = "";

        /**
         * NameWarConfig extremeForceClosedLabel.
         * @member {string} extremeForceClosedLabel
         * @memberof game.NameWarConfig
         * @instance
         */
        NameWarConfig.prototype.extremeForceClosedLabel = "";

        /**
         * NameWarConfig penaltyThreshold.
         * @member {number} penaltyThreshold
         * @memberof game.NameWarConfig
         * @instance
         */
        NameWarConfig.prototype.penaltyThreshold = 0;

        /**
         * Creates a new NameWarConfig instance using the specified properties.
         * @function create
         * @memberof game.NameWarConfig
         * @static
         * @param {game.NameWarConfig.$Properties=} [properties] Properties to set
         * @returns {game.NameWarConfig} NameWarConfig instance
         * @type {{
         *   (properties: game.NameWarConfig.$Shape): game.NameWarConfig & game.NameWarConfig.$Shape;
         *   (properties?: game.NameWarConfig.$Properties): game.NameWarConfig;
         * }}
         */
        NameWarConfig.create = function(properties) {
            return new NameWarConfig(properties);
        };

        /**
         * Encodes the specified NameWarConfig message. Does not implicitly {@link game.NameWarConfig.verify|verify} messages.
         * @function encode
         * @memberof game.NameWarConfig
         * @static
         * @param {game.NameWarConfig.$Properties} message NameWarConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NameWarConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.penaltyPrefix != null && $Object.hasOwnProperty.call(message, "penaltyPrefix") && message.penaltyPrefix !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.penaltyPrefix);
            if (message.loserPanelTitle != null && $Object.hasOwnProperty.call(message, "loserPanelTitle") && message.loserPanelTitle !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.loserPanelTitle);
            if (message.escapeTitle != null && $Object.hasOwnProperty.call(message, "escapeTitle") && message.escapeTitle !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.escapeTitle);
            if (message.renamePanelTitle != null && $Object.hasOwnProperty.call(message, "renamePanelTitle") && message.renamePanelTitle !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.renamePanelTitle);
            if (message.nameWarLoserLabel != null && $Object.hasOwnProperty.call(message, "nameWarLoserLabel") && message.nameWarLoserLabel !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.nameWarLoserLabel);
            if (message.extremeForceClosedLabel != null && $Object.hasOwnProperty.call(message, "extremeForceClosedLabel") && message.extremeForceClosedLabel !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.extremeForceClosedLabel);
            if (message.penaltyThreshold != null && $Object.hasOwnProperty.call(message, "penaltyThreshold") && message.penaltyThreshold !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.penaltyThreshold);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NameWarConfig message, length delimited. Does not implicitly {@link game.NameWarConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.NameWarConfig
         * @static
         * @param {game.NameWarConfig.$Properties} message NameWarConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NameWarConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NameWarConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.NameWarConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.NameWarConfig & game.NameWarConfig.$Shape} NameWarConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NameWarConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.NameWarConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.penaltyPrefix = value;
                        else
                            delete message.penaltyPrefix;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.loserPanelTitle = value;
                        else
                            delete message.loserPanelTitle;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.escapeTitle = value;
                        else
                            delete message.escapeTitle;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.renamePanelTitle = value;
                        else
                            delete message.renamePanelTitle;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.nameWarLoserLabel = value;
                        else
                            delete message.nameWarLoserLabel;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.extremeForceClosedLabel = value;
                        else
                            delete message.extremeForceClosedLabel;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.penaltyThreshold = value;
                        else
                            delete message.penaltyThreshold;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NameWarConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.NameWarConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.NameWarConfig & game.NameWarConfig.$Shape} NameWarConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NameWarConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NameWarConfig message.
         * @function verify
         * @memberof game.NameWarConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NameWarConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.penaltyPrefix != null && $Object.hasOwnProperty.call(message, "penaltyPrefix"))
                if (!$util.isString(message.penaltyPrefix))
                    return "penaltyPrefix: string expected";
            if (message.loserPanelTitle != null && $Object.hasOwnProperty.call(message, "loserPanelTitle"))
                if (!$util.isString(message.loserPanelTitle))
                    return "loserPanelTitle: string expected";
            if (message.escapeTitle != null && $Object.hasOwnProperty.call(message, "escapeTitle"))
                if (!$util.isString(message.escapeTitle))
                    return "escapeTitle: string expected";
            if (message.renamePanelTitle != null && $Object.hasOwnProperty.call(message, "renamePanelTitle"))
                if (!$util.isString(message.renamePanelTitle))
                    return "renamePanelTitle: string expected";
            if (message.nameWarLoserLabel != null && $Object.hasOwnProperty.call(message, "nameWarLoserLabel"))
                if (!$util.isString(message.nameWarLoserLabel))
                    return "nameWarLoserLabel: string expected";
            if (message.extremeForceClosedLabel != null && $Object.hasOwnProperty.call(message, "extremeForceClosedLabel"))
                if (!$util.isString(message.extremeForceClosedLabel))
                    return "extremeForceClosedLabel: string expected";
            if (message.penaltyThreshold != null && $Object.hasOwnProperty.call(message, "penaltyThreshold"))
                if (!$util.isInteger(message.penaltyThreshold))
                    return "penaltyThreshold: integer expected";
            return null;
        };

        /**
         * Creates a NameWarConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.NameWarConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.NameWarConfig} NameWarConfig
         */
        NameWarConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.NameWarConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.NameWarConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.NameWarConfig();
            if (object.penaltyPrefix != null)
                if (typeof object.penaltyPrefix !== "string" || object.penaltyPrefix.length)
                    message.penaltyPrefix = $String(object.penaltyPrefix);
            if (object.loserPanelTitle != null)
                if (typeof object.loserPanelTitle !== "string" || object.loserPanelTitle.length)
                    message.loserPanelTitle = $String(object.loserPanelTitle);
            if (object.escapeTitle != null)
                if (typeof object.escapeTitle !== "string" || object.escapeTitle.length)
                    message.escapeTitle = $String(object.escapeTitle);
            if (object.renamePanelTitle != null)
                if (typeof object.renamePanelTitle !== "string" || object.renamePanelTitle.length)
                    message.renamePanelTitle = $String(object.renamePanelTitle);
            if (object.nameWarLoserLabel != null)
                if (typeof object.nameWarLoserLabel !== "string" || object.nameWarLoserLabel.length)
                    message.nameWarLoserLabel = $String(object.nameWarLoserLabel);
            if (object.extremeForceClosedLabel != null)
                if (typeof object.extremeForceClosedLabel !== "string" || object.extremeForceClosedLabel.length)
                    message.extremeForceClosedLabel = $String(object.extremeForceClosedLabel);
            if (object.penaltyThreshold != null)
                if ($Number(object.penaltyThreshold) !== 0)
                    message.penaltyThreshold = object.penaltyThreshold | 0;
            return message;
        };

        /**
         * Creates a plain object from a NameWarConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.NameWarConfig
         * @static
         * @param {game.NameWarConfig} message NameWarConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NameWarConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.penaltyPrefix = "";
                object.loserPanelTitle = "";
                object.escapeTitle = "";
                object.renamePanelTitle = "";
                object.nameWarLoserLabel = "";
                object.extremeForceClosedLabel = "";
                object.penaltyThreshold = 0;
            }
            if (message.penaltyPrefix != null && $Object.hasOwnProperty.call(message, "penaltyPrefix"))
                object.penaltyPrefix = message.penaltyPrefix;
            if (message.loserPanelTitle != null && $Object.hasOwnProperty.call(message, "loserPanelTitle"))
                object.loserPanelTitle = message.loserPanelTitle;
            if (message.escapeTitle != null && $Object.hasOwnProperty.call(message, "escapeTitle"))
                object.escapeTitle = message.escapeTitle;
            if (message.renamePanelTitle != null && $Object.hasOwnProperty.call(message, "renamePanelTitle"))
                object.renamePanelTitle = message.renamePanelTitle;
            if (message.nameWarLoserLabel != null && $Object.hasOwnProperty.call(message, "nameWarLoserLabel"))
                object.nameWarLoserLabel = message.nameWarLoserLabel;
            if (message.extremeForceClosedLabel != null && $Object.hasOwnProperty.call(message, "extremeForceClosedLabel"))
                object.extremeForceClosedLabel = message.extremeForceClosedLabel;
            if (message.penaltyThreshold != null && $Object.hasOwnProperty.call(message, "penaltyThreshold"))
                object.penaltyThreshold = message.penaltyThreshold;
            return object;
        };

        /**
         * Converts this NameWarConfig to JSON.
         * @function toJSON
         * @memberof game.NameWarConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NameWarConfig.prototype.toJSON = function() {
            return NameWarConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NameWarConfig
         * @function getTypeUrl
         * @memberof game.NameWarConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NameWarConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.NameWarConfig";
        };

        return NameWarConfig;
    })();

    game.GiveawayConfig = (function() {

        /**
         * Properties of a GiveawayConfig.
         * @typedef {Object} game.GiveawayConfig.$Properties
         * @property {string|null} [panelTitle] GiveawayConfig panelTitle
         * @property {string|null} [panelDescription] GiveawayConfig panelDescription
         * @property {string|null} [submitPlaceholder] GiveawayConfig submitPlaceholder
         * @property {string|null} [emptyText] GiveawayConfig emptyText
         * @property {number|null} [activeBoostValue] GiveawayConfig activeBoostValue
         * @property {number|null} [winPenaltyValue] GiveawayConfig winPenaltyValue
         * @property {number|null} [likeVoteLimitPerHour] GiveawayConfig likeVoteLimitPerHour
         * @property {number|null} [likeVoteValue] GiveawayConfig likeVoteValue
         * @property {number|null} [dislikeVoteLimitPerHour] GiveawayConfig dislikeVoteLimitPerHour
         * @property {number|null} [dislikeVoteValue] GiveawayConfig dislikeVoteValue
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GiveawayConfig.
         * @memberof game
         * @interface IGiveawayConfig
         * @augments game.GiveawayConfig.$Properties
         * @deprecated Use game.GiveawayConfig.$Properties instead.
         */

        /**
         * Shape of a GiveawayConfig.
         * @typedef {game.GiveawayConfig.$Properties} game.GiveawayConfig.$Shape
         */

        /**
         * Constructs a new GiveawayConfig.
         * @memberof game
         * @classdesc Represents a GiveawayConfig.
         * @constructor
         * @param {game.GiveawayConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GiveawayConfig = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * GiveawayConfig panelTitle.
         * @member {string} panelTitle
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.panelTitle = "";

        /**
         * GiveawayConfig panelDescription.
         * @member {string} panelDescription
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.panelDescription = "";

        /**
         * GiveawayConfig submitPlaceholder.
         * @member {string} submitPlaceholder
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.submitPlaceholder = "";

        /**
         * GiveawayConfig emptyText.
         * @member {string} emptyText
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.emptyText = "";

        /**
         * GiveawayConfig activeBoostValue.
         * @member {number} activeBoostValue
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.activeBoostValue = 0;

        /**
         * GiveawayConfig winPenaltyValue.
         * @member {number} winPenaltyValue
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.winPenaltyValue = 0;

        /**
         * GiveawayConfig likeVoteLimitPerHour.
         * @member {number} likeVoteLimitPerHour
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.likeVoteLimitPerHour = 0;

        /**
         * GiveawayConfig likeVoteValue.
         * @member {number} likeVoteValue
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.likeVoteValue = 0;

        /**
         * GiveawayConfig dislikeVoteLimitPerHour.
         * @member {number} dislikeVoteLimitPerHour
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.dislikeVoteLimitPerHour = 0;

        /**
         * GiveawayConfig dislikeVoteValue.
         * @member {number} dislikeVoteValue
         * @memberof game.GiveawayConfig
         * @instance
         */
        GiveawayConfig.prototype.dislikeVoteValue = 0;

        /**
         * Creates a new GiveawayConfig instance using the specified properties.
         * @function create
         * @memberof game.GiveawayConfig
         * @static
         * @param {game.GiveawayConfig.$Properties=} [properties] Properties to set
         * @returns {game.GiveawayConfig} GiveawayConfig instance
         * @type {{
         *   (properties: game.GiveawayConfig.$Shape): game.GiveawayConfig & game.GiveawayConfig.$Shape;
         *   (properties?: game.GiveawayConfig.$Properties): game.GiveawayConfig;
         * }}
         */
        GiveawayConfig.create = function(properties) {
            return new GiveawayConfig(properties);
        };

        /**
         * Encodes the specified GiveawayConfig message. Does not implicitly {@link game.GiveawayConfig.verify|verify} messages.
         * @function encode
         * @memberof game.GiveawayConfig
         * @static
         * @param {game.GiveawayConfig.$Properties} message GiveawayConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GiveawayConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.panelTitle != null && $Object.hasOwnProperty.call(message, "panelTitle") && message.panelTitle !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.panelTitle);
            if (message.panelDescription != null && $Object.hasOwnProperty.call(message, "panelDescription") && message.panelDescription !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.panelDescription);
            if (message.submitPlaceholder != null && $Object.hasOwnProperty.call(message, "submitPlaceholder") && message.submitPlaceholder !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.submitPlaceholder);
            if (message.emptyText != null && $Object.hasOwnProperty.call(message, "emptyText") && message.emptyText !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.emptyText);
            if (message.activeBoostValue != null && $Object.hasOwnProperty.call(message, "activeBoostValue") && !$Object.is(message.activeBoostValue, 0))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.activeBoostValue);
            if (message.winPenaltyValue != null && $Object.hasOwnProperty.call(message, "winPenaltyValue") && !$Object.is(message.winPenaltyValue, 0))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.winPenaltyValue);
            if (message.likeVoteLimitPerHour != null && $Object.hasOwnProperty.call(message, "likeVoteLimitPerHour") && message.likeVoteLimitPerHour !== 0)
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.likeVoteLimitPerHour);
            if (message.likeVoteValue != null && $Object.hasOwnProperty.call(message, "likeVoteValue") && !$Object.is(message.likeVoteValue, 0))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.likeVoteValue);
            if (message.dislikeVoteLimitPerHour != null && $Object.hasOwnProperty.call(message, "dislikeVoteLimitPerHour") && message.dislikeVoteLimitPerHour !== 0)
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.dislikeVoteLimitPerHour);
            if (message.dislikeVoteValue != null && $Object.hasOwnProperty.call(message, "dislikeVoteValue") && !$Object.is(message.dislikeVoteValue, 0))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.dislikeVoteValue);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GiveawayConfig message, length delimited. Does not implicitly {@link game.GiveawayConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.GiveawayConfig
         * @static
         * @param {game.GiveawayConfig.$Properties} message GiveawayConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GiveawayConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GiveawayConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.GiveawayConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.GiveawayConfig & game.GiveawayConfig.$Shape} GiveawayConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GiveawayConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.GiveawayConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.panelTitle = value;
                        else
                            delete message.panelTitle;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.panelDescription = value;
                        else
                            delete message.panelDescription;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.submitPlaceholder = value;
                        else
                            delete message.submitPlaceholder;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.emptyText = value;
                        else
                            delete message.emptyText;
                        continue;
                    }
                case 5: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.activeBoostValue = value;
                        else
                            delete message.activeBoostValue;
                        continue;
                    }
                case 6: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.winPenaltyValue = value;
                        else
                            delete message.winPenaltyValue;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.likeVoteLimitPerHour = value;
                        else
                            delete message.likeVoteLimitPerHour;
                        continue;
                    }
                case 8: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.likeVoteValue = value;
                        else
                            delete message.likeVoteValue;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.dislikeVoteLimitPerHour = value;
                        else
                            delete message.dislikeVoteLimitPerHour;
                        continue;
                    }
                case 10: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.dislikeVoteValue = value;
                        else
                            delete message.dislikeVoteValue;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GiveawayConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.GiveawayConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.GiveawayConfig & game.GiveawayConfig.$Shape} GiveawayConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GiveawayConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GiveawayConfig message.
         * @function verify
         * @memberof game.GiveawayConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GiveawayConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.panelTitle != null && $Object.hasOwnProperty.call(message, "panelTitle"))
                if (!$util.isString(message.panelTitle))
                    return "panelTitle: string expected";
            if (message.panelDescription != null && $Object.hasOwnProperty.call(message, "panelDescription"))
                if (!$util.isString(message.panelDescription))
                    return "panelDescription: string expected";
            if (message.submitPlaceholder != null && $Object.hasOwnProperty.call(message, "submitPlaceholder"))
                if (!$util.isString(message.submitPlaceholder))
                    return "submitPlaceholder: string expected";
            if (message.emptyText != null && $Object.hasOwnProperty.call(message, "emptyText"))
                if (!$util.isString(message.emptyText))
                    return "emptyText: string expected";
            if (message.activeBoostValue != null && $Object.hasOwnProperty.call(message, "activeBoostValue"))
                if (typeof message.activeBoostValue !== "number")
                    return "activeBoostValue: number expected";
            if (message.winPenaltyValue != null && $Object.hasOwnProperty.call(message, "winPenaltyValue"))
                if (typeof message.winPenaltyValue !== "number")
                    return "winPenaltyValue: number expected";
            if (message.likeVoteLimitPerHour != null && $Object.hasOwnProperty.call(message, "likeVoteLimitPerHour"))
                if (!$util.isInteger(message.likeVoteLimitPerHour))
                    return "likeVoteLimitPerHour: integer expected";
            if (message.likeVoteValue != null && $Object.hasOwnProperty.call(message, "likeVoteValue"))
                if (typeof message.likeVoteValue !== "number")
                    return "likeVoteValue: number expected";
            if (message.dislikeVoteLimitPerHour != null && $Object.hasOwnProperty.call(message, "dislikeVoteLimitPerHour"))
                if (!$util.isInteger(message.dislikeVoteLimitPerHour))
                    return "dislikeVoteLimitPerHour: integer expected";
            if (message.dislikeVoteValue != null && $Object.hasOwnProperty.call(message, "dislikeVoteValue"))
                if (typeof message.dislikeVoteValue !== "number")
                    return "dislikeVoteValue: number expected";
            return null;
        };

        /**
         * Creates a GiveawayConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.GiveawayConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.GiveawayConfig} GiveawayConfig
         */
        GiveawayConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.GiveawayConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.GiveawayConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.GiveawayConfig();
            if (object.panelTitle != null)
                if (typeof object.panelTitle !== "string" || object.panelTitle.length)
                    message.panelTitle = $String(object.panelTitle);
            if (object.panelDescription != null)
                if (typeof object.panelDescription !== "string" || object.panelDescription.length)
                    message.panelDescription = $String(object.panelDescription);
            if (object.submitPlaceholder != null)
                if (typeof object.submitPlaceholder !== "string" || object.submitPlaceholder.length)
                    message.submitPlaceholder = $String(object.submitPlaceholder);
            if (object.emptyText != null)
                if (typeof object.emptyText !== "string" || object.emptyText.length)
                    message.emptyText = $String(object.emptyText);
            if (object.activeBoostValue != null)
                if (!$Object.is($Number(object.activeBoostValue), 0))
                    message.activeBoostValue = $Number(object.activeBoostValue);
            if (object.winPenaltyValue != null)
                if (!$Object.is($Number(object.winPenaltyValue), 0))
                    message.winPenaltyValue = $Number(object.winPenaltyValue);
            if (object.likeVoteLimitPerHour != null)
                if ($Number(object.likeVoteLimitPerHour) !== 0)
                    message.likeVoteLimitPerHour = object.likeVoteLimitPerHour | 0;
            if (object.likeVoteValue != null)
                if (!$Object.is($Number(object.likeVoteValue), 0))
                    message.likeVoteValue = $Number(object.likeVoteValue);
            if (object.dislikeVoteLimitPerHour != null)
                if ($Number(object.dislikeVoteLimitPerHour) !== 0)
                    message.dislikeVoteLimitPerHour = object.dislikeVoteLimitPerHour | 0;
            if (object.dislikeVoteValue != null)
                if (!$Object.is($Number(object.dislikeVoteValue), 0))
                    message.dislikeVoteValue = $Number(object.dislikeVoteValue);
            return message;
        };

        /**
         * Creates a plain object from a GiveawayConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.GiveawayConfig
         * @static
         * @param {game.GiveawayConfig} message GiveawayConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GiveawayConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.panelTitle = "";
                object.panelDescription = "";
                object.submitPlaceholder = "";
                object.emptyText = "";
                object.activeBoostValue = 0;
                object.winPenaltyValue = 0;
                object.likeVoteLimitPerHour = 0;
                object.likeVoteValue = 0;
                object.dislikeVoteLimitPerHour = 0;
                object.dislikeVoteValue = 0;
            }
            if (message.panelTitle != null && $Object.hasOwnProperty.call(message, "panelTitle"))
                object.panelTitle = message.panelTitle;
            if (message.panelDescription != null && $Object.hasOwnProperty.call(message, "panelDescription"))
                object.panelDescription = message.panelDescription;
            if (message.submitPlaceholder != null && $Object.hasOwnProperty.call(message, "submitPlaceholder"))
                object.submitPlaceholder = message.submitPlaceholder;
            if (message.emptyText != null && $Object.hasOwnProperty.call(message, "emptyText"))
                object.emptyText = message.emptyText;
            if (message.activeBoostValue != null && $Object.hasOwnProperty.call(message, "activeBoostValue"))
                object.activeBoostValue = options.json && !$isFinite(message.activeBoostValue) ? $String(message.activeBoostValue) : message.activeBoostValue;
            if (message.winPenaltyValue != null && $Object.hasOwnProperty.call(message, "winPenaltyValue"))
                object.winPenaltyValue = options.json && !$isFinite(message.winPenaltyValue) ? $String(message.winPenaltyValue) : message.winPenaltyValue;
            if (message.likeVoteLimitPerHour != null && $Object.hasOwnProperty.call(message, "likeVoteLimitPerHour"))
                object.likeVoteLimitPerHour = message.likeVoteLimitPerHour;
            if (message.likeVoteValue != null && $Object.hasOwnProperty.call(message, "likeVoteValue"))
                object.likeVoteValue = options.json && !$isFinite(message.likeVoteValue) ? $String(message.likeVoteValue) : message.likeVoteValue;
            if (message.dislikeVoteLimitPerHour != null && $Object.hasOwnProperty.call(message, "dislikeVoteLimitPerHour"))
                object.dislikeVoteLimitPerHour = message.dislikeVoteLimitPerHour;
            if (message.dislikeVoteValue != null && $Object.hasOwnProperty.call(message, "dislikeVoteValue"))
                object.dislikeVoteValue = options.json && !$isFinite(message.dislikeVoteValue) ? $String(message.dislikeVoteValue) : message.dislikeVoteValue;
            return object;
        };

        /**
         * Converts this GiveawayConfig to JSON.
         * @function toJSON
         * @memberof game.GiveawayConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GiveawayConfig.prototype.toJSON = function() {
            return GiveawayConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GiveawayConfig
         * @function getTypeUrl
         * @memberof game.GiveawayConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GiveawayConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.GiveawayConfig";
        };

        return GiveawayConfig;
    })();

    game.AppConfig = (function() {

        /**
         * Properties of an AppConfig.
         * @typedef {Object} game.AppConfig.$Properties
         * @property {game.SiteConfig.$Properties|null} [site] AppConfig site
         * @property {game.AnnouncementBoard.$Properties|null} [announcementBoard] AppConfig announcementBoard
         * @property {Array.<game.GenderOption.$Properties>|null} [genders] AppConfig genders
         * @property {Array.<game.GenderFaction.$Properties>|null} [genderFactions] AppConfig genderFactions
         * @property {Array.<game.TitleSegment.$Properties>|null} [titles] AppConfig titles
         * @property {Array.<game.PunishmentConfig.$Properties>|null} [punishments] AppConfig punishments
         * @property {game.RoomNamePool.$Properties|null} [playerPunishmentRoomNamePool] AppConfig playerPunishmentRoomNamePool
         * @property {Array.<string>|null} [roomTags] AppConfig roomTags
         * @property {Array.<game.RoomInfoTagEntry.$Properties>|null} [roomInfoTags] AppConfig roomInfoTags
         * @property {game.AccessControlConfig.$Properties|null} [accessControl] AppConfig accessControl
         * @property {game.NameWarConfig.$Properties|null} [nameWar] AppConfig nameWar
         * @property {game.GiveawayConfig.$Properties|null} [giveaway] AppConfig giveaway
         * @property {game.ExtremeModeConfig.$Properties|null} [extremeMode] AppConfig extremeMode
         * @property {Array.<game.GameConfig.$Properties>|null} [games] AppConfig games
         * @property {Array.<game.StringPair.$Properties>|null} [messages] AppConfig messages
         * @property {game.SecurityDisclaimerConfig.$Properties|null} [securityDisclaimer] AppConfig securityDisclaimer
         * @property {game.RankedScoreConfig.$Properties|null} [rankedScore] AppConfig rankedScore
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AppConfig.
         * @memberof game
         * @interface IAppConfig
         * @augments game.AppConfig.$Properties
         * @deprecated Use game.AppConfig.$Properties instead.
         */

        /**
         * Shape of an AppConfig.
         * @typedef {game.AppConfig.$Properties} game.AppConfig.$Shape
         */

        /**
         * Constructs a new AppConfig.
         * @memberof game
         * @classdesc Represents an AppConfig.
         * @constructor
         * @param {game.AppConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AppConfig = function (properties) {
            this.genders = [];
            this.genderFactions = [];
            this.titles = [];
            this.punishments = [];
            this.roomTags = [];
            this.roomInfoTags = [];
            this.games = [];
            this.messages = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AppConfig site.
         * @member {game.SiteConfig.$Properties|null|undefined} site
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.site = null;

        /**
         * AppConfig announcementBoard.
         * @member {game.AnnouncementBoard.$Properties|null|undefined} announcementBoard
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.announcementBoard = null;

        /**
         * AppConfig genders.
         * @member {Array.<game.GenderOption.$Properties>} genders
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.genders = $util.emptyArray;

        /**
         * AppConfig genderFactions.
         * @member {Array.<game.GenderFaction.$Properties>} genderFactions
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.genderFactions = $util.emptyArray;

        /**
         * AppConfig titles.
         * @member {Array.<game.TitleSegment.$Properties>} titles
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.titles = $util.emptyArray;

        /**
         * AppConfig punishments.
         * @member {Array.<game.PunishmentConfig.$Properties>} punishments
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.punishments = $util.emptyArray;

        /**
         * AppConfig playerPunishmentRoomNamePool.
         * @member {game.RoomNamePool.$Properties|null|undefined} playerPunishmentRoomNamePool
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.playerPunishmentRoomNamePool = null;

        /**
         * AppConfig roomTags.
         * @member {Array.<string>} roomTags
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.roomTags = $util.emptyArray;

        /**
         * AppConfig roomInfoTags.
         * @member {Array.<game.RoomInfoTagEntry.$Properties>} roomInfoTags
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.roomInfoTags = $util.emptyArray;

        /**
         * AppConfig accessControl.
         * @member {game.AccessControlConfig.$Properties|null|undefined} accessControl
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.accessControl = null;

        /**
         * AppConfig nameWar.
         * @member {game.NameWarConfig.$Properties|null|undefined} nameWar
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.nameWar = null;

        /**
         * AppConfig giveaway.
         * @member {game.GiveawayConfig.$Properties|null|undefined} giveaway
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.giveaway = null;

        /**
         * AppConfig extremeMode.
         * @member {game.ExtremeModeConfig.$Properties|null|undefined} extremeMode
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.extremeMode = null;

        /**
         * AppConfig games.
         * @member {Array.<game.GameConfig.$Properties>} games
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.games = $util.emptyArray;

        /**
         * AppConfig messages.
         * @member {Array.<game.StringPair.$Properties>} messages
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.messages = $util.emptyArray;

        /**
         * AppConfig securityDisclaimer.
         * @member {game.SecurityDisclaimerConfig.$Properties|null|undefined} securityDisclaimer
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.securityDisclaimer = null;

        /**
         * AppConfig rankedScore.
         * @member {game.RankedScoreConfig.$Properties|null|undefined} rankedScore
         * @memberof game.AppConfig
         * @instance
         */
        AppConfig.prototype.rankedScore = null;

        /**
         * Creates a new AppConfig instance using the specified properties.
         * @function create
         * @memberof game.AppConfig
         * @static
         * @param {game.AppConfig.$Properties=} [properties] Properties to set
         * @returns {game.AppConfig} AppConfig instance
         * @type {{
         *   (properties: game.AppConfig.$Shape): game.AppConfig & game.AppConfig.$Shape;
         *   (properties?: game.AppConfig.$Properties): game.AppConfig;
         * }}
         */
        AppConfig.create = function(properties) {
            return new AppConfig(properties);
        };

        /**
         * Encodes the specified AppConfig message. Does not implicitly {@link game.AppConfig.verify|verify} messages.
         * @function encode
         * @memberof game.AppConfig
         * @static
         * @param {game.AppConfig.$Properties} message AppConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AppConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.site != null && $Object.hasOwnProperty.call(message, "site"))
                $root.game.SiteConfig.encode(message.site, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.announcementBoard != null && $Object.hasOwnProperty.call(message, "announcementBoard"))
                $root.game.AnnouncementBoard.encode(message.announcementBoard, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.genders != null && message.genders.length)
                for (let i = 0; i < message.genders.length; ++i)
                    $root.game.GenderOption.encode(message.genders[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.genderFactions != null && message.genderFactions.length)
                for (let i = 0; i < message.genderFactions.length; ++i)
                    $root.game.GenderFaction.encode(message.genderFactions[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.titles != null && message.titles.length)
                for (let i = 0; i < message.titles.length; ++i)
                    $root.game.TitleSegment.encode(message.titles[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.punishments != null && message.punishments.length)
                for (let i = 0; i < message.punishments.length; ++i)
                    $root.game.PunishmentConfig.encode(message.punishments[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.playerPunishmentRoomNamePool != null && $Object.hasOwnProperty.call(message, "playerPunishmentRoomNamePool"))
                $root.game.RoomNamePool.encode(message.playerPunishmentRoomNamePool, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.roomTags != null && message.roomTags.length)
                for (let i = 0; i < message.roomTags.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.roomTags[i]);
            if (message.roomInfoTags != null && message.roomInfoTags.length)
                for (let i = 0; i < message.roomInfoTags.length; ++i)
                    $root.game.RoomInfoTagEntry.encode(message.roomInfoTags[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.accessControl != null && $Object.hasOwnProperty.call(message, "accessControl"))
                $root.game.AccessControlConfig.encode(message.accessControl, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.nameWar != null && $Object.hasOwnProperty.call(message, "nameWar"))
                $root.game.NameWarConfig.encode(message.nameWar, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.giveaway != null && $Object.hasOwnProperty.call(message, "giveaway"))
                $root.game.GiveawayConfig.encode(message.giveaway, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.extremeMode != null && $Object.hasOwnProperty.call(message, "extremeMode"))
                $root.game.ExtremeModeConfig.encode(message.extremeMode, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.games != null && message.games.length)
                for (let i = 0; i < message.games.length; ++i)
                    $root.game.GameConfig.encode(message.games[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.messages != null && message.messages.length)
                for (let i = 0; i < message.messages.length; ++i)
                    $root.game.StringPair.encode(message.messages[i], writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.securityDisclaimer != null && $Object.hasOwnProperty.call(message, "securityDisclaimer"))
                $root.game.SecurityDisclaimerConfig.encode(message.securityDisclaimer, writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.rankedScore != null && $Object.hasOwnProperty.call(message, "rankedScore"))
                $root.game.RankedScoreConfig.encode(message.rankedScore, writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AppConfig message, length delimited. Does not implicitly {@link game.AppConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.AppConfig
         * @static
         * @param {game.AppConfig.$Properties} message AppConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AppConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AppConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.AppConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.AppConfig & game.AppConfig.$Shape} AppConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AppConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.AppConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.site = $root.game.SiteConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.site);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.announcementBoard = $root.game.AnnouncementBoard.decode(reader, reader.uint32(), $undefined, _depth + 1, message.announcementBoard);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.genders && message.genders.length))
                            message.genders = [];
                        message.genders.push($root.game.GenderOption.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.genderFactions && message.genderFactions.length))
                            message.genderFactions = [];
                        message.genderFactions.push($root.game.GenderFaction.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.titles && message.titles.length))
                            message.titles = [];
                        message.titles.push($root.game.TitleSegment.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.punishments && message.punishments.length))
                            message.punishments = [];
                        message.punishments.push($root.game.PunishmentConfig.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.playerPunishmentRoomNamePool = $root.game.RoomNamePool.decode(reader, reader.uint32(), $undefined, _depth + 1, message.playerPunishmentRoomNamePool);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.roomTags && message.roomTags.length))
                            message.roomTags = [];
                        message.roomTags.push(reader.stringVerify());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.roomInfoTags && message.roomInfoTags.length))
                            message.roomInfoTags = [];
                        message.roomInfoTags.push($root.game.RoomInfoTagEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.accessControl = $root.game.AccessControlConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accessControl);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.nameWar = $root.game.NameWarConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nameWar);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.giveaway = $root.game.GiveawayConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.giveaway);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.extremeMode = $root.game.ExtremeModeConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.extremeMode);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.games && message.games.length))
                            message.games = [];
                        message.games.push($root.game.GameConfig.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.messages && message.messages.length))
                            message.messages = [];
                        message.messages.push($root.game.StringPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.securityDisclaimer = $root.game.SecurityDisclaimerConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.securityDisclaimer);
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.rankedScore = $root.game.RankedScoreConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rankedScore);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AppConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.AppConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.AppConfig & game.AppConfig.$Shape} AppConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AppConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AppConfig message.
         * @function verify
         * @memberof game.AppConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AppConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.site != null && $Object.hasOwnProperty.call(message, "site")) {
                let error = $root.game.SiteConfig.verify(message.site, _depth + 1);
                if (error)
                    return "site." + error;
            }
            if (message.announcementBoard != null && $Object.hasOwnProperty.call(message, "announcementBoard")) {
                let error = $root.game.AnnouncementBoard.verify(message.announcementBoard, _depth + 1);
                if (error)
                    return "announcementBoard." + error;
            }
            if (message.genders != null && $Object.hasOwnProperty.call(message, "genders")) {
                if (!$Array.isArray(message.genders))
                    return "genders: array expected";
                for (let i = 0; i < message.genders.length; ++i) {
                    let error = $root.game.GenderOption.verify(message.genders[i], _depth + 1);
                    if (error)
                        return "genders." + error;
                }
            }
            if (message.genderFactions != null && $Object.hasOwnProperty.call(message, "genderFactions")) {
                if (!$Array.isArray(message.genderFactions))
                    return "genderFactions: array expected";
                for (let i = 0; i < message.genderFactions.length; ++i) {
                    let error = $root.game.GenderFaction.verify(message.genderFactions[i], _depth + 1);
                    if (error)
                        return "genderFactions." + error;
                }
            }
            if (message.titles != null && $Object.hasOwnProperty.call(message, "titles")) {
                if (!$Array.isArray(message.titles))
                    return "titles: array expected";
                for (let i = 0; i < message.titles.length; ++i) {
                    let error = $root.game.TitleSegment.verify(message.titles[i], _depth + 1);
                    if (error)
                        return "titles." + error;
                }
            }
            if (message.punishments != null && $Object.hasOwnProperty.call(message, "punishments")) {
                if (!$Array.isArray(message.punishments))
                    return "punishments: array expected";
                for (let i = 0; i < message.punishments.length; ++i) {
                    let error = $root.game.PunishmentConfig.verify(message.punishments[i], _depth + 1);
                    if (error)
                        return "punishments." + error;
                }
            }
            if (message.playerPunishmentRoomNamePool != null && $Object.hasOwnProperty.call(message, "playerPunishmentRoomNamePool")) {
                let error = $root.game.RoomNamePool.verify(message.playerPunishmentRoomNamePool, _depth + 1);
                if (error)
                    return "playerPunishmentRoomNamePool." + error;
            }
            if (message.roomTags != null && $Object.hasOwnProperty.call(message, "roomTags")) {
                if (!$Array.isArray(message.roomTags))
                    return "roomTags: array expected";
                for (let i = 0; i < message.roomTags.length; ++i)
                    if (!$util.isString(message.roomTags[i]))
                        return "roomTags: string[] expected";
            }
            if (message.roomInfoTags != null && $Object.hasOwnProperty.call(message, "roomInfoTags")) {
                if (!$Array.isArray(message.roomInfoTags))
                    return "roomInfoTags: array expected";
                for (let i = 0; i < message.roomInfoTags.length; ++i) {
                    let error = $root.game.RoomInfoTagEntry.verify(message.roomInfoTags[i], _depth + 1);
                    if (error)
                        return "roomInfoTags." + error;
                }
            }
            if (message.accessControl != null && $Object.hasOwnProperty.call(message, "accessControl")) {
                let error = $root.game.AccessControlConfig.verify(message.accessControl, _depth + 1);
                if (error)
                    return "accessControl." + error;
            }
            if (message.nameWar != null && $Object.hasOwnProperty.call(message, "nameWar")) {
                let error = $root.game.NameWarConfig.verify(message.nameWar, _depth + 1);
                if (error)
                    return "nameWar." + error;
            }
            if (message.giveaway != null && $Object.hasOwnProperty.call(message, "giveaway")) {
                let error = $root.game.GiveawayConfig.verify(message.giveaway, _depth + 1);
                if (error)
                    return "giveaway." + error;
            }
            if (message.extremeMode != null && $Object.hasOwnProperty.call(message, "extremeMode")) {
                let error = $root.game.ExtremeModeConfig.verify(message.extremeMode, _depth + 1);
                if (error)
                    return "extremeMode." + error;
            }
            if (message.games != null && $Object.hasOwnProperty.call(message, "games")) {
                if (!$Array.isArray(message.games))
                    return "games: array expected";
                for (let i = 0; i < message.games.length; ++i) {
                    let error = $root.game.GameConfig.verify(message.games[i], _depth + 1);
                    if (error)
                        return "games." + error;
                }
            }
            if (message.messages != null && $Object.hasOwnProperty.call(message, "messages")) {
                if (!$Array.isArray(message.messages))
                    return "messages: array expected";
                for (let i = 0; i < message.messages.length; ++i) {
                    let error = $root.game.StringPair.verify(message.messages[i], _depth + 1);
                    if (error)
                        return "messages." + error;
                }
            }
            if (message.securityDisclaimer != null && $Object.hasOwnProperty.call(message, "securityDisclaimer")) {
                let error = $root.game.SecurityDisclaimerConfig.verify(message.securityDisclaimer, _depth + 1);
                if (error)
                    return "securityDisclaimer." + error;
            }
            if (message.rankedScore != null && $Object.hasOwnProperty.call(message, "rankedScore")) {
                let error = $root.game.RankedScoreConfig.verify(message.rankedScore, _depth + 1);
                if (error)
                    return "rankedScore." + error;
            }
            return null;
        };

        /**
         * Creates an AppConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.AppConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.AppConfig} AppConfig
         */
        AppConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.AppConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.AppConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.AppConfig();
            if (object.site != null) {
                if (!$util.isObject(object.site))
                    throw $TypeError(".game.AppConfig.site: object expected");
                message.site = $root.game.SiteConfig.fromObject(object.site, _depth + 1);
            }
            if (object.announcementBoard != null) {
                if (!$util.isObject(object.announcementBoard))
                    throw $TypeError(".game.AppConfig.announcementBoard: object expected");
                message.announcementBoard = $root.game.AnnouncementBoard.fromObject(object.announcementBoard, _depth + 1);
            }
            if (object.genders) {
                if (!$Array.isArray(object.genders))
                    throw $TypeError(".game.AppConfig.genders: array expected");
                message.genders = $Array(object.genders.length);
                for (let i = 0; i < object.genders.length; ++i) {
                    if (!$util.isObject(object.genders[i]))
                        throw $TypeError(".game.AppConfig.genders: object expected");
                    message.genders[i] = $root.game.GenderOption.fromObject(object.genders[i], _depth + 1);
                }
            }
            if (object.genderFactions) {
                if (!$Array.isArray(object.genderFactions))
                    throw $TypeError(".game.AppConfig.genderFactions: array expected");
                message.genderFactions = $Array(object.genderFactions.length);
                for (let i = 0; i < object.genderFactions.length; ++i) {
                    if (!$util.isObject(object.genderFactions[i]))
                        throw $TypeError(".game.AppConfig.genderFactions: object expected");
                    message.genderFactions[i] = $root.game.GenderFaction.fromObject(object.genderFactions[i], _depth + 1);
                }
            }
            if (object.titles) {
                if (!$Array.isArray(object.titles))
                    throw $TypeError(".game.AppConfig.titles: array expected");
                message.titles = $Array(object.titles.length);
                for (let i = 0; i < object.titles.length; ++i) {
                    if (!$util.isObject(object.titles[i]))
                        throw $TypeError(".game.AppConfig.titles: object expected");
                    message.titles[i] = $root.game.TitleSegment.fromObject(object.titles[i], _depth + 1);
                }
            }
            if (object.punishments) {
                if (!$Array.isArray(object.punishments))
                    throw $TypeError(".game.AppConfig.punishments: array expected");
                message.punishments = $Array(object.punishments.length);
                for (let i = 0; i < object.punishments.length; ++i) {
                    if (!$util.isObject(object.punishments[i]))
                        throw $TypeError(".game.AppConfig.punishments: object expected");
                    message.punishments[i] = $root.game.PunishmentConfig.fromObject(object.punishments[i], _depth + 1);
                }
            }
            if (object.playerPunishmentRoomNamePool != null) {
                if (!$util.isObject(object.playerPunishmentRoomNamePool))
                    throw $TypeError(".game.AppConfig.playerPunishmentRoomNamePool: object expected");
                message.playerPunishmentRoomNamePool = $root.game.RoomNamePool.fromObject(object.playerPunishmentRoomNamePool, _depth + 1);
            }
            if (object.roomTags) {
                if (!$Array.isArray(object.roomTags))
                    throw $TypeError(".game.AppConfig.roomTags: array expected");
                message.roomTags = $Array(object.roomTags.length);
                for (let i = 0; i < object.roomTags.length; ++i)
                    message.roomTags[i] = $String(object.roomTags[i]);
            }
            if (object.roomInfoTags) {
                if (!$Array.isArray(object.roomInfoTags))
                    throw $TypeError(".game.AppConfig.roomInfoTags: array expected");
                message.roomInfoTags = $Array(object.roomInfoTags.length);
                for (let i = 0; i < object.roomInfoTags.length; ++i) {
                    if (!$util.isObject(object.roomInfoTags[i]))
                        throw $TypeError(".game.AppConfig.roomInfoTags: object expected");
                    message.roomInfoTags[i] = $root.game.RoomInfoTagEntry.fromObject(object.roomInfoTags[i], _depth + 1);
                }
            }
            if (object.accessControl != null) {
                if (!$util.isObject(object.accessControl))
                    throw $TypeError(".game.AppConfig.accessControl: object expected");
                message.accessControl = $root.game.AccessControlConfig.fromObject(object.accessControl, _depth + 1);
            }
            if (object.nameWar != null) {
                if (!$util.isObject(object.nameWar))
                    throw $TypeError(".game.AppConfig.nameWar: object expected");
                message.nameWar = $root.game.NameWarConfig.fromObject(object.nameWar, _depth + 1);
            }
            if (object.giveaway != null) {
                if (!$util.isObject(object.giveaway))
                    throw $TypeError(".game.AppConfig.giveaway: object expected");
                message.giveaway = $root.game.GiveawayConfig.fromObject(object.giveaway, _depth + 1);
            }
            if (object.extremeMode != null) {
                if (!$util.isObject(object.extremeMode))
                    throw $TypeError(".game.AppConfig.extremeMode: object expected");
                message.extremeMode = $root.game.ExtremeModeConfig.fromObject(object.extremeMode, _depth + 1);
            }
            if (object.games) {
                if (!$Array.isArray(object.games))
                    throw $TypeError(".game.AppConfig.games: array expected");
                message.games = $Array(object.games.length);
                for (let i = 0; i < object.games.length; ++i) {
                    if (!$util.isObject(object.games[i]))
                        throw $TypeError(".game.AppConfig.games: object expected");
                    message.games[i] = $root.game.GameConfig.fromObject(object.games[i], _depth + 1);
                }
            }
            if (object.messages) {
                if (!$Array.isArray(object.messages))
                    throw $TypeError(".game.AppConfig.messages: array expected");
                message.messages = $Array(object.messages.length);
                for (let i = 0; i < object.messages.length; ++i) {
                    if (!$util.isObject(object.messages[i]))
                        throw $TypeError(".game.AppConfig.messages: object expected");
                    message.messages[i] = $root.game.StringPair.fromObject(object.messages[i], _depth + 1);
                }
            }
            if (object.securityDisclaimer != null) {
                if (!$util.isObject(object.securityDisclaimer))
                    throw $TypeError(".game.AppConfig.securityDisclaimer: object expected");
                message.securityDisclaimer = $root.game.SecurityDisclaimerConfig.fromObject(object.securityDisclaimer, _depth + 1);
            }
            if (object.rankedScore != null) {
                if (!$util.isObject(object.rankedScore))
                    throw $TypeError(".game.AppConfig.rankedScore: object expected");
                message.rankedScore = $root.game.RankedScoreConfig.fromObject(object.rankedScore, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an AppConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.AppConfig
         * @static
         * @param {game.AppConfig} message AppConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AppConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.genders = [];
                object.genderFactions = [];
                object.titles = [];
                object.punishments = [];
                object.roomTags = [];
                object.roomInfoTags = [];
                object.games = [];
                object.messages = [];
            }
            if (options.defaults) {
                object.site = null;
                object.announcementBoard = null;
                object.playerPunishmentRoomNamePool = null;
                object.accessControl = null;
                object.nameWar = null;
                object.giveaway = null;
                object.extremeMode = null;
                object.securityDisclaimer = null;
                object.rankedScore = null;
            }
            if (message.site != null && $Object.hasOwnProperty.call(message, "site"))
                object.site = $root.game.SiteConfig.toObject(message.site, options, _depth + 1);
            if (message.announcementBoard != null && $Object.hasOwnProperty.call(message, "announcementBoard"))
                object.announcementBoard = $root.game.AnnouncementBoard.toObject(message.announcementBoard, options, _depth + 1);
            if (message.genders && message.genders.length) {
                object.genders = $Array(message.genders.length);
                for (let j = 0; j < message.genders.length; ++j)
                    object.genders[j] = $root.game.GenderOption.toObject(message.genders[j], options, _depth + 1);
            }
            if (message.genderFactions && message.genderFactions.length) {
                object.genderFactions = $Array(message.genderFactions.length);
                for (let j = 0; j < message.genderFactions.length; ++j)
                    object.genderFactions[j] = $root.game.GenderFaction.toObject(message.genderFactions[j], options, _depth + 1);
            }
            if (message.titles && message.titles.length) {
                object.titles = $Array(message.titles.length);
                for (let j = 0; j < message.titles.length; ++j)
                    object.titles[j] = $root.game.TitleSegment.toObject(message.titles[j], options, _depth + 1);
            }
            if (message.punishments && message.punishments.length) {
                object.punishments = $Array(message.punishments.length);
                for (let j = 0; j < message.punishments.length; ++j)
                    object.punishments[j] = $root.game.PunishmentConfig.toObject(message.punishments[j], options, _depth + 1);
            }
            if (message.playerPunishmentRoomNamePool != null && $Object.hasOwnProperty.call(message, "playerPunishmentRoomNamePool"))
                object.playerPunishmentRoomNamePool = $root.game.RoomNamePool.toObject(message.playerPunishmentRoomNamePool, options, _depth + 1);
            if (message.roomTags && message.roomTags.length) {
                object.roomTags = $Array(message.roomTags.length);
                for (let j = 0; j < message.roomTags.length; ++j)
                    object.roomTags[j] = message.roomTags[j];
            }
            if (message.roomInfoTags && message.roomInfoTags.length) {
                object.roomInfoTags = $Array(message.roomInfoTags.length);
                for (let j = 0; j < message.roomInfoTags.length; ++j)
                    object.roomInfoTags[j] = $root.game.RoomInfoTagEntry.toObject(message.roomInfoTags[j], options, _depth + 1);
            }
            if (message.accessControl != null && $Object.hasOwnProperty.call(message, "accessControl"))
                object.accessControl = $root.game.AccessControlConfig.toObject(message.accessControl, options, _depth + 1);
            if (message.nameWar != null && $Object.hasOwnProperty.call(message, "nameWar"))
                object.nameWar = $root.game.NameWarConfig.toObject(message.nameWar, options, _depth + 1);
            if (message.giveaway != null && $Object.hasOwnProperty.call(message, "giveaway"))
                object.giveaway = $root.game.GiveawayConfig.toObject(message.giveaway, options, _depth + 1);
            if (message.extremeMode != null && $Object.hasOwnProperty.call(message, "extremeMode"))
                object.extremeMode = $root.game.ExtremeModeConfig.toObject(message.extremeMode, options, _depth + 1);
            if (message.games && message.games.length) {
                object.games = $Array(message.games.length);
                for (let j = 0; j < message.games.length; ++j)
                    object.games[j] = $root.game.GameConfig.toObject(message.games[j], options, _depth + 1);
            }
            if (message.messages && message.messages.length) {
                object.messages = $Array(message.messages.length);
                for (let j = 0; j < message.messages.length; ++j)
                    object.messages[j] = $root.game.StringPair.toObject(message.messages[j], options, _depth + 1);
            }
            if (message.securityDisclaimer != null && $Object.hasOwnProperty.call(message, "securityDisclaimer"))
                object.securityDisclaimer = $root.game.SecurityDisclaimerConfig.toObject(message.securityDisclaimer, options, _depth + 1);
            if (message.rankedScore != null && $Object.hasOwnProperty.call(message, "rankedScore"))
                object.rankedScore = $root.game.RankedScoreConfig.toObject(message.rankedScore, options, _depth + 1);
            return object;
        };

        /**
         * Converts this AppConfig to JSON.
         * @function toJSON
         * @memberof game.AppConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AppConfig.prototype.toJSON = function() {
            return AppConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AppConfig
         * @function getTypeUrl
         * @memberof game.AppConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AppConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.AppConfig";
        };

        return AppConfig;
    })();

    game.RankedScoreConfig = (function() {

        /**
         * Properties of a RankedScoreConfig.
         * @typedef {Object} game.RankedScoreConfig.$Properties
         * @property {number|null} [max] RankedScoreConfig max
         * @property {number|null} [min] RankedScoreConfig min
         * @property {number|null} [nameWarMin] RankedScoreConfig nameWarMin
         * @property {number|null} [dailyDecayRatio] RankedScoreConfig dailyDecayRatio
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RankedScoreConfig.
         * @memberof game
         * @interface IRankedScoreConfig
         * @augments game.RankedScoreConfig.$Properties
         * @deprecated Use game.RankedScoreConfig.$Properties instead.
         */

        /**
         * Shape of a RankedScoreConfig.
         * @typedef {game.RankedScoreConfig.$Properties} game.RankedScoreConfig.$Shape
         */

        /**
         * Constructs a new RankedScoreConfig.
         * @memberof game
         * @classdesc Represents a RankedScoreConfig.
         * @constructor
         * @param {game.RankedScoreConfig.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RankedScoreConfig = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RankedScoreConfig max.
         * @member {number} max
         * @memberof game.RankedScoreConfig
         * @instance
         */
        RankedScoreConfig.prototype.max = 0;

        /**
         * RankedScoreConfig min.
         * @member {number} min
         * @memberof game.RankedScoreConfig
         * @instance
         */
        RankedScoreConfig.prototype.min = 0;

        /**
         * RankedScoreConfig nameWarMin.
         * @member {number} nameWarMin
         * @memberof game.RankedScoreConfig
         * @instance
         */
        RankedScoreConfig.prototype.nameWarMin = 0;

        /**
         * RankedScoreConfig dailyDecayRatio.
         * @member {number} dailyDecayRatio
         * @memberof game.RankedScoreConfig
         * @instance
         */
        RankedScoreConfig.prototype.dailyDecayRatio = 0;

        /**
         * Creates a new RankedScoreConfig instance using the specified properties.
         * @function create
         * @memberof game.RankedScoreConfig
         * @static
         * @param {game.RankedScoreConfig.$Properties=} [properties] Properties to set
         * @returns {game.RankedScoreConfig} RankedScoreConfig instance
         * @type {{
         *   (properties: game.RankedScoreConfig.$Shape): game.RankedScoreConfig & game.RankedScoreConfig.$Shape;
         *   (properties?: game.RankedScoreConfig.$Properties): game.RankedScoreConfig;
         * }}
         */
        RankedScoreConfig.create = function(properties) {
            return new RankedScoreConfig(properties);
        };

        /**
         * Encodes the specified RankedScoreConfig message. Does not implicitly {@link game.RankedScoreConfig.verify|verify} messages.
         * @function encode
         * @memberof game.RankedScoreConfig
         * @static
         * @param {game.RankedScoreConfig.$Properties} message RankedScoreConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RankedScoreConfig.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.max != null && $Object.hasOwnProperty.call(message, "max") && message.max !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.max);
            if (message.min != null && $Object.hasOwnProperty.call(message, "min") && message.min !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.min);
            if (message.nameWarMin != null && $Object.hasOwnProperty.call(message, "nameWarMin") && message.nameWarMin !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.nameWarMin);
            if (message.dailyDecayRatio != null && $Object.hasOwnProperty.call(message, "dailyDecayRatio") && !$Object.is(message.dailyDecayRatio, 0))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.dailyDecayRatio);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RankedScoreConfig message, length delimited. Does not implicitly {@link game.RankedScoreConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RankedScoreConfig
         * @static
         * @param {game.RankedScoreConfig.$Properties} message RankedScoreConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RankedScoreConfig.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RankedScoreConfig message from the specified reader or buffer.
         * @function decode
         * @memberof game.RankedScoreConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RankedScoreConfig & game.RankedScoreConfig.$Shape} RankedScoreConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RankedScoreConfig.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RankedScoreConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.max = value;
                        else
                            delete message.max;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.min = value;
                        else
                            delete message.min;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.nameWarMin = value;
                        else
                            delete message.nameWarMin;
                        continue;
                    }
                case 4: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.dailyDecayRatio = value;
                        else
                            delete message.dailyDecayRatio;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RankedScoreConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RankedScoreConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RankedScoreConfig & game.RankedScoreConfig.$Shape} RankedScoreConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RankedScoreConfig.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RankedScoreConfig message.
         * @function verify
         * @memberof game.RankedScoreConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RankedScoreConfig.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.max != null && $Object.hasOwnProperty.call(message, "max"))
                if (!$util.isInteger(message.max))
                    return "max: integer expected";
            if (message.min != null && $Object.hasOwnProperty.call(message, "min"))
                if (!$util.isInteger(message.min))
                    return "min: integer expected";
            if (message.nameWarMin != null && $Object.hasOwnProperty.call(message, "nameWarMin"))
                if (!$util.isInteger(message.nameWarMin))
                    return "nameWarMin: integer expected";
            if (message.dailyDecayRatio != null && $Object.hasOwnProperty.call(message, "dailyDecayRatio"))
                if (typeof message.dailyDecayRatio !== "number")
                    return "dailyDecayRatio: number expected";
            return null;
        };

        /**
         * Creates a RankedScoreConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RankedScoreConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RankedScoreConfig} RankedScoreConfig
         */
        RankedScoreConfig.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RankedScoreConfig)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RankedScoreConfig: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RankedScoreConfig();
            if (object.max != null)
                if ($Number(object.max) !== 0)
                    message.max = object.max | 0;
            if (object.min != null)
                if ($Number(object.min) !== 0)
                    message.min = object.min | 0;
            if (object.nameWarMin != null)
                if ($Number(object.nameWarMin) !== 0)
                    message.nameWarMin = object.nameWarMin | 0;
            if (object.dailyDecayRatio != null)
                if (!$Object.is($Number(object.dailyDecayRatio), 0))
                    message.dailyDecayRatio = $Number(object.dailyDecayRatio);
            return message;
        };

        /**
         * Creates a plain object from a RankedScoreConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RankedScoreConfig
         * @static
         * @param {game.RankedScoreConfig} message RankedScoreConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RankedScoreConfig.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.max = 0;
                object.min = 0;
                object.nameWarMin = 0;
                object.dailyDecayRatio = 0;
            }
            if (message.max != null && $Object.hasOwnProperty.call(message, "max"))
                object.max = message.max;
            if (message.min != null && $Object.hasOwnProperty.call(message, "min"))
                object.min = message.min;
            if (message.nameWarMin != null && $Object.hasOwnProperty.call(message, "nameWarMin"))
                object.nameWarMin = message.nameWarMin;
            if (message.dailyDecayRatio != null && $Object.hasOwnProperty.call(message, "dailyDecayRatio"))
                object.dailyDecayRatio = options.json && !$isFinite(message.dailyDecayRatio) ? $String(message.dailyDecayRatio) : message.dailyDecayRatio;
            return object;
        };

        /**
         * Converts this RankedScoreConfig to JSON.
         * @function toJSON
         * @memberof game.RankedScoreConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RankedScoreConfig.prototype.toJSON = function() {
            return RankedScoreConfig.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RankedScoreConfig
         * @function getTypeUrl
         * @memberof game.RankedScoreConfig
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RankedScoreConfig.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RankedScoreConfig";
        };

        return RankedScoreConfig;
    })();

    game.StateDocument = (function() {

        /**
         * Properties of a StateDocument.
         * @typedef {Object} game.StateDocument.$Properties
         * @property {game.LobbySnapshot.$Properties|null} [lobby] StateDocument lobby
         * @property {game.RoomSnapshot.$Properties|null} [room] StateDocument room
         * @property {game.AppConfig.$Properties|null} [config] StateDocument config
         * @property {"lobby"|"room"|"config"} [doc] StateDocument doc
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a StateDocument.
         * @memberof game
         * @interface IStateDocument
         * @augments game.StateDocument.$Properties
         * @deprecated Use game.StateDocument.$Properties instead.
         */

        /**
         * Narrowed shape of a StateDocument.
         * @typedef {{
         *   lobby?: game.LobbySnapshot.$Shape|null;
         *   room?: game.RoomSnapshot.$Shape|null;
         *   config?: game.AppConfig.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ doc?: undefined; lobby?: null; room?: null; config?: null }|{ doc?: "lobby"; lobby: game.LobbySnapshot.$Shape; room?: null; config?: null }|{ doc?: "room"; lobby?: null; room: game.RoomSnapshot.$Shape; config?: null }|{ doc?: "config"; lobby?: null; room?: null; config: game.AppConfig.$Shape })
         * )} game.StateDocument.$Shape
         */

        /**
         * Constructs a new StateDocument.
         * @memberof game
         * @classdesc Represents a StateDocument.
         * @constructor
         * @param {game.StateDocument.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const StateDocument = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StateDocument lobby.
         * @member {game.LobbySnapshot.$Properties|null|undefined} lobby
         * @memberof game.StateDocument
         * @instance
         */
        StateDocument.prototype.lobby = null;

        /**
         * StateDocument room.
         * @member {game.RoomSnapshot.$Properties|null|undefined} room
         * @memberof game.StateDocument
         * @instance
         */
        StateDocument.prototype.room = null;

        /**
         * StateDocument config.
         * @member {game.AppConfig.$Properties|null|undefined} config
         * @memberof game.StateDocument
         * @instance
         */
        StateDocument.prototype.config = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * StateDocument doc.
         * @member {"lobby"|"room"|"config"|undefined} doc
         * @memberof game.StateDocument
         * @instance
         */
        $Object.defineProperty(StateDocument.prototype, "doc", {
            get: $util.oneOfGetter($oneOfFields = ["lobby", "room", "config"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new StateDocument instance using the specified properties.
         * @function create
         * @memberof game.StateDocument
         * @static
         * @param {game.StateDocument.$Properties=} [properties] Properties to set
         * @returns {game.StateDocument} StateDocument instance
         * @type {{
         *   (properties: game.StateDocument.$Shape): game.StateDocument & game.StateDocument.$Shape;
         *   (properties?: game.StateDocument.$Properties): game.StateDocument;
         * }}
         */
        StateDocument.create = function(properties) {
            return new StateDocument(properties);
        };

        /**
         * Encodes the specified StateDocument message. Does not implicitly {@link game.StateDocument.verify|verify} messages.
         * @function encode
         * @memberof game.StateDocument
         * @static
         * @param {game.StateDocument.$Properties} message StateDocument message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateDocument.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.lobby != null && $Object.hasOwnProperty.call(message, "lobby"))
                $root.game.LobbySnapshot.encode(message.lobby, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.room != null && $Object.hasOwnProperty.call(message, "room"))
                $root.game.RoomSnapshot.encode(message.room, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                $root.game.AppConfig.encode(message.config, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StateDocument message, length delimited. Does not implicitly {@link game.StateDocument.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.StateDocument
         * @static
         * @param {game.StateDocument.$Properties} message StateDocument message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateDocument.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a StateDocument message from the specified reader or buffer.
         * @function decode
         * @memberof game.StateDocument
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.StateDocument & game.StateDocument.$Shape} StateDocument
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateDocument.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.StateDocument();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.lobby = $root.game.LobbySnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lobby);
                        message.doc = "lobby";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.game.RoomSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.room);
                        message.doc = "room";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.config = $root.game.AppConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                        message.doc = "config";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StateDocument message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.StateDocument
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.StateDocument & game.StateDocument.$Shape} StateDocument
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateDocument.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateDocument message.
         * @function verify
         * @memberof game.StateDocument
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateDocument.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.lobby != null && $Object.hasOwnProperty.call(message, "lobby")) {
                properties.doc = 1;
                {
                    let error = $root.game.LobbySnapshot.verify(message.lobby, _depth + 1);
                    if (error)
                        return "lobby." + error;
                }
            }
            if (message.room != null && $Object.hasOwnProperty.call(message, "room")) {
                if (properties.doc === 1)
                    return "doc: multiple values";
                properties.doc = 1;
                {
                    let error = $root.game.RoomSnapshot.verify(message.room, _depth + 1);
                    if (error)
                        return "room." + error;
                }
            }
            if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                if (properties.doc === 1)
                    return "doc: multiple values";
                properties.doc = 1;
                {
                    let error = $root.game.AppConfig.verify(message.config, _depth + 1);
                    if (error)
                        return "config." + error;
                }
            }
            return null;
        };

        /**
         * Creates a StateDocument message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.StateDocument
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.StateDocument} StateDocument
         */
        StateDocument.fromObject = function (object, _depth) {
            if (object instanceof $root.game.StateDocument)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.StateDocument: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.StateDocument();
            if (object.lobby != null) {
                if (!$util.isObject(object.lobby))
                    throw $TypeError(".game.StateDocument.lobby: object expected");
                message.lobby = $root.game.LobbySnapshot.fromObject(object.lobby, _depth + 1);
            }
            if (object.room != null) {
                if (!$util.isObject(object.room))
                    throw $TypeError(".game.StateDocument.room: object expected");
                message.room = $root.game.RoomSnapshot.fromObject(object.room, _depth + 1);
            }
            if (object.config != null) {
                if (!$util.isObject(object.config))
                    throw $TypeError(".game.StateDocument.config: object expected");
                message.config = $root.game.AppConfig.fromObject(object.config, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a StateDocument message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.StateDocument
         * @static
         * @param {game.StateDocument} message StateDocument
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateDocument.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.lobby != null && $Object.hasOwnProperty.call(message, "lobby")) {
                object.lobby = $root.game.LobbySnapshot.toObject(message.lobby, options, _depth + 1);
                if (options.oneofs)
                    object.doc = "lobby";
            }
            if (message.room != null && $Object.hasOwnProperty.call(message, "room")) {
                object.room = $root.game.RoomSnapshot.toObject(message.room, options, _depth + 1);
                if (options.oneofs)
                    object.doc = "room";
            }
            if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                object.config = $root.game.AppConfig.toObject(message.config, options, _depth + 1);
                if (options.oneofs)
                    object.doc = "config";
            }
            return object;
        };

        /**
         * Converts this StateDocument to JSON.
         * @function toJSON
         * @memberof game.StateDocument
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateDocument.prototype.toJSON = function() {
            return StateDocument.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StateDocument
         * @function getTypeUrl
         * @memberof game.StateDocument
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StateDocument.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.StateDocument";
        };

        return StateDocument;
    })();

    game.PlayerBatch = (function() {

        /**
         * Properties of a PlayerBatch.
         * @typedef {Object} game.PlayerBatch.$Properties
         * @property {Array.<game.PublicPlayer.$Properties>|null} [players] PlayerBatch players
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerBatch.
         * @memberof game
         * @interface IPlayerBatch
         * @augments game.PlayerBatch.$Properties
         * @deprecated Use game.PlayerBatch.$Properties instead.
         */

        /**
         * Shape of a PlayerBatch.
         * @typedef {game.PlayerBatch.$Properties} game.PlayerBatch.$Shape
         */

        /**
         * Constructs a new PlayerBatch.
         * @memberof game
         * @classdesc Represents a PlayerBatch.
         * @constructor
         * @param {game.PlayerBatch.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerBatch = function (properties) {
            this.players = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerBatch players.
         * @member {Array.<game.PublicPlayer.$Properties>} players
         * @memberof game.PlayerBatch
         * @instance
         */
        PlayerBatch.prototype.players = $util.emptyArray;

        /**
         * Creates a new PlayerBatch instance using the specified properties.
         * @function create
         * @memberof game.PlayerBatch
         * @static
         * @param {game.PlayerBatch.$Properties=} [properties] Properties to set
         * @returns {game.PlayerBatch} PlayerBatch instance
         * @type {{
         *   (properties: game.PlayerBatch.$Shape): game.PlayerBatch & game.PlayerBatch.$Shape;
         *   (properties?: game.PlayerBatch.$Properties): game.PlayerBatch;
         * }}
         */
        PlayerBatch.create = function(properties) {
            return new PlayerBatch(properties);
        };

        /**
         * Encodes the specified PlayerBatch message. Does not implicitly {@link game.PlayerBatch.verify|verify} messages.
         * @function encode
         * @memberof game.PlayerBatch
         * @static
         * @param {game.PlayerBatch.$Properties} message PlayerBatch message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerBatch.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.game.PublicPlayer.encode(message.players[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PlayerBatch message, length delimited. Does not implicitly {@link game.PlayerBatch.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PlayerBatch
         * @static
         * @param {game.PlayerBatch.$Properties} message PlayerBatch message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerBatch.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PlayerBatch message from the specified reader or buffer.
         * @function decode
         * @memberof game.PlayerBatch
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PlayerBatch & game.PlayerBatch.$Shape} PlayerBatch
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerBatch.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PlayerBatch();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.game.PublicPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PlayerBatch message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PlayerBatch
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PlayerBatch & game.PlayerBatch.$Shape} PlayerBatch
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerBatch.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlayerBatch message.
         * @function verify
         * @memberof game.PlayerBatch
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerBatch.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.players != null && $Object.hasOwnProperty.call(message, "players")) {
                if (!$Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.game.PublicPlayer.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PlayerBatch message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PlayerBatch
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PlayerBatch} PlayerBatch
         */
        PlayerBatch.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PlayerBatch)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PlayerBatch: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PlayerBatch();
            if (object.players) {
                if (!$Array.isArray(object.players))
                    throw $TypeError(".game.PlayerBatch.players: array expected");
                message.players = $Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (!$util.isObject(object.players[i]))
                        throw $TypeError(".game.PlayerBatch.players: object expected");
                    message.players[i] = $root.game.PublicPlayer.fromObject(object.players[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PlayerBatch message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PlayerBatch
         * @static
         * @param {game.PlayerBatch} message PlayerBatch
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlayerBatch.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (message.players && message.players.length) {
                object.players = $Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.game.PublicPlayer.toObject(message.players[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this PlayerBatch to JSON.
         * @function toJSON
         * @memberof game.PlayerBatch
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlayerBatch.prototype.toJSON = function() {
            return PlayerBatch.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PlayerBatch
         * @function getTypeUrl
         * @memberof game.PlayerBatch
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PlayerBatch.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PlayerBatch";
        };

        return PlayerBatch;
    })();

    game.MeState = (function() {

        /**
         * Properties of a MeState.
         * @typedef {Object} game.MeState.$Properties
         * @property {game.PublicPlayer.$Properties|null} [player] MeState player
         * @property {string|null} [token] MeState token
         * @property {string|null} [roomId] MeState roomId
         * @property {game.RoomSnapshot.$Properties|null} [room] MeState room
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a MeState.
         * @memberof game
         * @interface IMeState
         * @augments game.MeState.$Properties
         * @deprecated Use game.MeState.$Properties instead.
         */

        /**
         * Shape of a MeState.
         * @typedef {{
         *   player?: game.PublicPlayer.$Shape|null;
         *   token?: string|null;
         *   roomId?: string|null;
         *   room?: game.RoomSnapshot.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} game.MeState.$Shape
         */

        /**
         * Constructs a new MeState.
         * @memberof game
         * @classdesc Represents a MeState.
         * @constructor
         * @param {game.MeState.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const MeState = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * MeState player.
         * @member {game.PublicPlayer.$Properties|null|undefined} player
         * @memberof game.MeState
         * @instance
         */
        MeState.prototype.player = null;

        /**
         * MeState token.
         * @member {string} token
         * @memberof game.MeState
         * @instance
         */
        MeState.prototype.token = "";

        /**
         * MeState roomId.
         * @member {string} roomId
         * @memberof game.MeState
         * @instance
         */
        MeState.prototype.roomId = "";

        /**
         * MeState room.
         * @member {game.RoomSnapshot.$Properties|null|undefined} room
         * @memberof game.MeState
         * @instance
         */
        MeState.prototype.room = null;

        /**
         * Creates a new MeState instance using the specified properties.
         * @function create
         * @memberof game.MeState
         * @static
         * @param {game.MeState.$Properties=} [properties] Properties to set
         * @returns {game.MeState} MeState instance
         * @type {{
         *   (properties: game.MeState.$Shape): game.MeState & game.MeState.$Shape;
         *   (properties?: game.MeState.$Properties): game.MeState;
         * }}
         */
        MeState.create = function(properties) {
            return new MeState(properties);
        };

        /**
         * Encodes the specified MeState message. Does not implicitly {@link game.MeState.verify|verify} messages.
         * @function encode
         * @memberof game.MeState
         * @static
         * @param {game.MeState.$Properties} message MeState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MeState.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.game.PublicPlayer.encode(message.player, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.token != null && $Object.hasOwnProperty.call(message, "token") && message.token !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId") && message.roomId !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.roomId);
            if (message.room != null && $Object.hasOwnProperty.call(message, "room"))
                $root.game.RoomSnapshot.encode(message.room, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MeState message, length delimited. Does not implicitly {@link game.MeState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.MeState
         * @static
         * @param {game.MeState.$Properties} message MeState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MeState.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a MeState message from the specified reader or buffer.
         * @function decode
         * @memberof game.MeState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.MeState & game.MeState.$Shape} MeState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MeState.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.MeState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.game.PublicPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.roomId = value;
                        else
                            delete message.roomId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.game.RoomSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.room);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a MeState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.MeState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.MeState & game.MeState.$Shape} MeState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MeState.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MeState message.
         * @function verify
         * @memberof game.MeState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MeState.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                let error = $root.game.PublicPlayer.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                if (!$util.isString(message.roomId))
                    return "roomId: string expected";
            if (message.room != null && $Object.hasOwnProperty.call(message, "room")) {
                let error = $root.game.RoomSnapshot.verify(message.room, _depth + 1);
                if (error)
                    return "room." + error;
            }
            return null;
        };

        /**
         * Creates a MeState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.MeState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.MeState} MeState
         */
        MeState.fromObject = function (object, _depth) {
            if (object instanceof $root.game.MeState)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.MeState: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.MeState();
            if (object.player != null) {
                if (!$util.isObject(object.player))
                    throw $TypeError(".game.MeState.player: object expected");
                message.player = $root.game.PublicPlayer.fromObject(object.player, _depth + 1);
            }
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = $String(object.token);
            if (object.roomId != null)
                if (typeof object.roomId !== "string" || object.roomId.length)
                    message.roomId = $String(object.roomId);
            if (object.room != null) {
                if (!$util.isObject(object.room))
                    throw $TypeError(".game.MeState.room: object expected");
                message.room = $root.game.RoomSnapshot.fromObject(object.room, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a MeState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.MeState
         * @static
         * @param {game.MeState} message MeState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MeState.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.player = null;
                object.token = "";
                object.roomId = "";
                object.room = null;
            }
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                object.player = $root.game.PublicPlayer.toObject(message.player, options, _depth + 1);
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = message.token;
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                object.roomId = message.roomId;
            if (message.room != null && $Object.hasOwnProperty.call(message, "room"))
                object.room = $root.game.RoomSnapshot.toObject(message.room, options, _depth + 1);
            return object;
        };

        /**
         * Converts this MeState to JSON.
         * @function toJSON
         * @memberof game.MeState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MeState.prototype.toJSON = function() {
            return MeState.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for MeState
         * @function getTypeUrl
         * @memberof game.MeState
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        MeState.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.MeState";
        };

        return MeState;
    })();

    game.AnnouncementPayload = (function() {

        /**
         * Properties of an AnnouncementPayload.
         * @typedef {Object} game.AnnouncementPayload.$Properties
         * @property {string|null} [id] AnnouncementPayload id
         * @property {string|null} [message] AnnouncementPayload message
         * @property {number|null} [durationMs] AnnouncementPayload durationMs
         * @property {number|Long|null} [createdAt] AnnouncementPayload createdAt
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AnnouncementPayload.
         * @memberof game
         * @interface IAnnouncementPayload
         * @augments game.AnnouncementPayload.$Properties
         * @deprecated Use game.AnnouncementPayload.$Properties instead.
         */

        /**
         * Shape of an AnnouncementPayload.
         * @typedef {game.AnnouncementPayload.$Properties} game.AnnouncementPayload.$Shape
         */

        /**
         * Constructs a new AnnouncementPayload.
         * @memberof game
         * @classdesc Represents an AnnouncementPayload.
         * @constructor
         * @param {game.AnnouncementPayload.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AnnouncementPayload = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * AnnouncementPayload id.
         * @member {string} id
         * @memberof game.AnnouncementPayload
         * @instance
         */
        AnnouncementPayload.prototype.id = "";

        /**
         * AnnouncementPayload message.
         * @member {string} message
         * @memberof game.AnnouncementPayload
         * @instance
         */
        AnnouncementPayload.prototype.message = "";

        /**
         * AnnouncementPayload durationMs.
         * @member {number} durationMs
         * @memberof game.AnnouncementPayload
         * @instance
         */
        AnnouncementPayload.prototype.durationMs = 0;

        /**
         * AnnouncementPayload createdAt.
         * @member {number|Long} createdAt
         * @memberof game.AnnouncementPayload
         * @instance
         */
        AnnouncementPayload.prototype.createdAt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new AnnouncementPayload instance using the specified properties.
         * @function create
         * @memberof game.AnnouncementPayload
         * @static
         * @param {game.AnnouncementPayload.$Properties=} [properties] Properties to set
         * @returns {game.AnnouncementPayload} AnnouncementPayload instance
         * @type {{
         *   (properties: game.AnnouncementPayload.$Shape): game.AnnouncementPayload & game.AnnouncementPayload.$Shape;
         *   (properties?: game.AnnouncementPayload.$Properties): game.AnnouncementPayload;
         * }}
         */
        AnnouncementPayload.create = function(properties) {
            return new AnnouncementPayload(properties);
        };

        /**
         * Encodes the specified AnnouncementPayload message. Does not implicitly {@link game.AnnouncementPayload.verify|verify} messages.
         * @function encode
         * @memberof game.AnnouncementPayload
         * @static
         * @param {game.AnnouncementPayload.$Properties} message AnnouncementPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AnnouncementPayload.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            if (message.durationMs != null && $Object.hasOwnProperty.call(message, "durationMs") && message.durationMs !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.durationMs);
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.createdAt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AnnouncementPayload message, length delimited. Does not implicitly {@link game.AnnouncementPayload.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.AnnouncementPayload
         * @static
         * @param {game.AnnouncementPayload.$Properties} message AnnouncementPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AnnouncementPayload.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AnnouncementPayload message from the specified reader or buffer.
         * @function decode
         * @memberof game.AnnouncementPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.AnnouncementPayload & game.AnnouncementPayload.$Shape} AnnouncementPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AnnouncementPayload.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.AnnouncementPayload(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.durationMs = value;
                        else
                            delete message.durationMs;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.createdAt = value;
                        else
                            delete message.createdAt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AnnouncementPayload message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.AnnouncementPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.AnnouncementPayload & game.AnnouncementPayload.$Shape} AnnouncementPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AnnouncementPayload.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AnnouncementPayload message.
         * @function verify
         * @memberof game.AnnouncementPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AnnouncementPayload.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            if (message.durationMs != null && $Object.hasOwnProperty.call(message, "durationMs"))
                if (!$util.isInteger(message.durationMs))
                    return "durationMs: integer expected";
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
                if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                    return "createdAt: integer|Long expected";
            return null;
        };

        /**
         * Creates an AnnouncementPayload message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.AnnouncementPayload
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.AnnouncementPayload} AnnouncementPayload
         */
        AnnouncementPayload.fromObject = function (object, _depth) {
            if (object instanceof $root.game.AnnouncementPayload)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.AnnouncementPayload: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.AnnouncementPayload();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = $String(object.message);
            if (object.durationMs != null)
                if ($Number(object.durationMs) !== 0)
                    message.durationMs = object.durationMs | 0;
            if (object.createdAt != null)
                if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = $parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an AnnouncementPayload message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.AnnouncementPayload
         * @static
         * @param {game.AnnouncementPayload} message AnnouncementPayload
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AnnouncementPayload.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.message = "";
                object.durationMs = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            if (message.durationMs != null && $Object.hasOwnProperty.call(message, "durationMs"))
                object.durationMs = message.durationMs;
            if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                else if (typeof message.createdAt === "number")
                    object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
                else
                    object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
            return object;
        };

        /**
         * Converts this AnnouncementPayload to JSON.
         * @function toJSON
         * @memberof game.AnnouncementPayload
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AnnouncementPayload.prototype.toJSON = function() {
            return AnnouncementPayload.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AnnouncementPayload
         * @function getTypeUrl
         * @memberof game.AnnouncementPayload
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AnnouncementPayload.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.AnnouncementPayload";
        };

        return AnnouncementPayload;
    })();

    game.RoomClosed = (function() {

        /**
         * Properties of a RoomClosed.
         * @typedef {Object} game.RoomClosed.$Properties
         * @property {string|null} [message] RoomClosed message
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoomClosed.
         * @memberof game
         * @interface IRoomClosed
         * @augments game.RoomClosed.$Properties
         * @deprecated Use game.RoomClosed.$Properties instead.
         */

        /**
         * Shape of a RoomClosed.
         * @typedef {game.RoomClosed.$Properties} game.RoomClosed.$Shape
         */

        /**
         * Constructs a new RoomClosed.
         * @memberof game
         * @classdesc Represents a RoomClosed.
         * @constructor
         * @param {game.RoomClosed.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoomClosed = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RoomClosed message.
         * @member {string} message
         * @memberof game.RoomClosed
         * @instance
         */
        RoomClosed.prototype.message = "";

        /**
         * Creates a new RoomClosed instance using the specified properties.
         * @function create
         * @memberof game.RoomClosed
         * @static
         * @param {game.RoomClosed.$Properties=} [properties] Properties to set
         * @returns {game.RoomClosed} RoomClosed instance
         * @type {{
         *   (properties: game.RoomClosed.$Shape): game.RoomClosed & game.RoomClosed.$Shape;
         *   (properties?: game.RoomClosed.$Properties): game.RoomClosed;
         * }}
         */
        RoomClosed.create = function(properties) {
            return new RoomClosed(properties);
        };

        /**
         * Encodes the specified RoomClosed message. Does not implicitly {@link game.RoomClosed.verify|verify} messages.
         * @function encode
         * @memberof game.RoomClosed
         * @static
         * @param {game.RoomClosed.$Properties} message RoomClosed message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomClosed.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.message);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoomClosed message, length delimited. Does not implicitly {@link game.RoomClosed.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RoomClosed
         * @static
         * @param {game.RoomClosed.$Properties} message RoomClosed message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomClosed.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RoomClosed message from the specified reader or buffer.
         * @function decode
         * @memberof game.RoomClosed
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RoomClosed & game.RoomClosed.$Shape} RoomClosed
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomClosed.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RoomClosed(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoomClosed message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RoomClosed
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RoomClosed & game.RoomClosed.$Shape} RoomClosed
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomClosed.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoomClosed message.
         * @function verify
         * @memberof game.RoomClosed
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomClosed.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        /**
         * Creates a RoomClosed message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RoomClosed
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RoomClosed} RoomClosed
         */
        RoomClosed.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RoomClosed)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RoomClosed: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RoomClosed();
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = $String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a RoomClosed message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RoomClosed
         * @static
         * @param {game.RoomClosed} message RoomClosed
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoomClosed.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.message = "";
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this RoomClosed to JSON.
         * @function toJSON
         * @memberof game.RoomClosed
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoomClosed.prototype.toJSON = function() {
            return RoomClosed.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoomClosed
         * @function getTypeUrl
         * @memberof game.RoomClosed
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoomClosed.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RoomClosed";
        };

        return RoomClosed;
    })();

    game.HistoryPage = (function() {

        /**
         * Properties of a HistoryPage.
         * @typedef {Object} game.HistoryPage.$Properties
         * @property {string|null} [roomId] HistoryPage roomId
         * @property {game.RoundHistoryItem.$Properties|null} [item] HistoryPage item
         * @property {number|null} [total] HistoryPage total
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a HistoryPage.
         * @memberof game
         * @interface IHistoryPage
         * @augments game.HistoryPage.$Properties
         * @deprecated Use game.HistoryPage.$Properties instead.
         */

        /**
         * Shape of a HistoryPage.
         * @typedef {game.HistoryPage.$Properties} game.HistoryPage.$Shape
         */

        /**
         * Constructs a new HistoryPage.
         * @memberof game
         * @classdesc Represents a HistoryPage.
         * @constructor
         * @param {game.HistoryPage.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const HistoryPage = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * HistoryPage roomId.
         * @member {string} roomId
         * @memberof game.HistoryPage
         * @instance
         */
        HistoryPage.prototype.roomId = "";

        /**
         * HistoryPage item.
         * @member {game.RoundHistoryItem.$Properties|null|undefined} item
         * @memberof game.HistoryPage
         * @instance
         */
        HistoryPage.prototype.item = null;

        /**
         * HistoryPage total.
         * @member {number} total
         * @memberof game.HistoryPage
         * @instance
         */
        HistoryPage.prototype.total = 0;

        /**
         * Creates a new HistoryPage instance using the specified properties.
         * @function create
         * @memberof game.HistoryPage
         * @static
         * @param {game.HistoryPage.$Properties=} [properties] Properties to set
         * @returns {game.HistoryPage} HistoryPage instance
         * @type {{
         *   (properties: game.HistoryPage.$Shape): game.HistoryPage & game.HistoryPage.$Shape;
         *   (properties?: game.HistoryPage.$Properties): game.HistoryPage;
         * }}
         */
        HistoryPage.create = function(properties) {
            return new HistoryPage(properties);
        };

        /**
         * Encodes the specified HistoryPage message. Does not implicitly {@link game.HistoryPage.verify|verify} messages.
         * @function encode
         * @memberof game.HistoryPage
         * @static
         * @param {game.HistoryPage.$Properties} message HistoryPage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HistoryPage.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId") && message.roomId !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.roomId);
            if (message.item != null && $Object.hasOwnProperty.call(message, "item"))
                $root.game.RoundHistoryItem.encode(message.item, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.total != null && $Object.hasOwnProperty.call(message, "total") && message.total !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.total);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HistoryPage message, length delimited. Does not implicitly {@link game.HistoryPage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.HistoryPage
         * @static
         * @param {game.HistoryPage.$Properties} message HistoryPage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HistoryPage.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a HistoryPage message from the specified reader or buffer.
         * @function decode
         * @memberof game.HistoryPage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.HistoryPage & game.HistoryPage.$Shape} HistoryPage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HistoryPage.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.HistoryPage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.roomId = value;
                        else
                            delete message.roomId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.item = $root.game.RoundHistoryItem.decode(reader, reader.uint32(), $undefined, _depth + 1, message.item);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.total = value;
                        else
                            delete message.total;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HistoryPage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.HistoryPage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.HistoryPage & game.HistoryPage.$Shape} HistoryPage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HistoryPage.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HistoryPage message.
         * @function verify
         * @memberof game.HistoryPage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HistoryPage.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                if (!$util.isString(message.roomId))
                    return "roomId: string expected";
            if (message.item != null && $Object.hasOwnProperty.call(message, "item")) {
                let error = $root.game.RoundHistoryItem.verify(message.item, _depth + 1);
                if (error)
                    return "item." + error;
            }
            if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
                if (!$util.isInteger(message.total))
                    return "total: integer expected";
            return null;
        };

        /**
         * Creates a HistoryPage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.HistoryPage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.HistoryPage} HistoryPage
         */
        HistoryPage.fromObject = function (object, _depth) {
            if (object instanceof $root.game.HistoryPage)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.HistoryPage: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.HistoryPage();
            if (object.roomId != null)
                if (typeof object.roomId !== "string" || object.roomId.length)
                    message.roomId = $String(object.roomId);
            if (object.item != null) {
                if (!$util.isObject(object.item))
                    throw $TypeError(".game.HistoryPage.item: object expected");
                message.item = $root.game.RoundHistoryItem.fromObject(object.item, _depth + 1);
            }
            if (object.total != null)
                if ($Number(object.total) !== 0)
                    message.total = object.total | 0;
            return message;
        };

        /**
         * Creates a plain object from a HistoryPage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.HistoryPage
         * @static
         * @param {game.HistoryPage} message HistoryPage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HistoryPage.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.roomId = "";
                object.item = null;
                object.total = 0;
            }
            if (message.roomId != null && $Object.hasOwnProperty.call(message, "roomId"))
                object.roomId = message.roomId;
            if (message.item != null && $Object.hasOwnProperty.call(message, "item"))
                object.item = $root.game.RoundHistoryItem.toObject(message.item, options, _depth + 1);
            if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
                object.total = message.total;
            return object;
        };

        /**
         * Converts this HistoryPage to JSON.
         * @function toJSON
         * @memberof game.HistoryPage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HistoryPage.prototype.toJSON = function() {
            return HistoryPage.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HistoryPage
         * @function getTypeUrl
         * @memberof game.HistoryPage
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HistoryPage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.HistoryPage";
        };

        return HistoryPage;
    })();

    game.OkResult = (function() {

        /**
         * Properties of an OkResult.
         * @typedef {Object} game.OkResult.$Properties
         * @property {boolean|null} [ok] OkResult ok
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an OkResult.
         * @memberof game
         * @interface IOkResult
         * @augments game.OkResult.$Properties
         * @deprecated Use game.OkResult.$Properties instead.
         */

        /**
         * Shape of an OkResult.
         * @typedef {game.OkResult.$Properties} game.OkResult.$Shape
         */

        /**
         * Constructs a new OkResult.
         * @memberof game
         * @classdesc Represents an OkResult.
         * @constructor
         * @param {game.OkResult.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const OkResult = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * OkResult ok.
         * @member {boolean} ok
         * @memberof game.OkResult
         * @instance
         */
        OkResult.prototype.ok = false;

        /**
         * Creates a new OkResult instance using the specified properties.
         * @function create
         * @memberof game.OkResult
         * @static
         * @param {game.OkResult.$Properties=} [properties] Properties to set
         * @returns {game.OkResult} OkResult instance
         * @type {{
         *   (properties: game.OkResult.$Shape): game.OkResult & game.OkResult.$Shape;
         *   (properties?: game.OkResult.$Properties): game.OkResult;
         * }}
         */
        OkResult.create = function(properties) {
            return new OkResult(properties);
        };

        /**
         * Encodes the specified OkResult message. Does not implicitly {@link game.OkResult.verify|verify} messages.
         * @function encode
         * @memberof game.OkResult
         * @static
         * @param {game.OkResult.$Properties} message OkResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OkResult.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ok != null && $Object.hasOwnProperty.call(message, "ok") && message.ok !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ok);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified OkResult message, length delimited. Does not implicitly {@link game.OkResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.OkResult
         * @static
         * @param {game.OkResult.$Properties} message OkResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OkResult.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an OkResult message from the specified reader or buffer.
         * @function decode
         * @memberof game.OkResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.OkResult & game.OkResult.$Shape} OkResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OkResult.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.OkResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ok = value;
                        else
                            delete message.ok;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an OkResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.OkResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.OkResult & game.OkResult.$Shape} OkResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OkResult.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OkResult message.
         * @function verify
         * @memberof game.OkResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OkResult.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ok != null && $Object.hasOwnProperty.call(message, "ok"))
                if (typeof message.ok !== "boolean")
                    return "ok: boolean expected";
            return null;
        };

        /**
         * Creates an OkResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.OkResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.OkResult} OkResult
         */
        OkResult.fromObject = function (object, _depth) {
            if (object instanceof $root.game.OkResult)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.OkResult: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.OkResult();
            if (object.ok != null)
                if (object.ok)
                    message.ok = $Boolean(object.ok);
            return message;
        };

        /**
         * Creates a plain object from an OkResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.OkResult
         * @static
         * @param {game.OkResult} message OkResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OkResult.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.ok = false;
            if (message.ok != null && $Object.hasOwnProperty.call(message, "ok"))
                object.ok = message.ok;
            return object;
        };

        /**
         * Converts this OkResult to JSON.
         * @function toJSON
         * @memberof game.OkResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OkResult.prototype.toJSON = function() {
            return OkResult.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for OkResult
         * @function getTypeUrl
         * @memberof game.OkResult
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        OkResult.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.OkResult";
        };

        return OkResult;
    })();

    game.PlayerResult = (function() {

        /**
         * Properties of a PlayerResult.
         * @typedef {Object} game.PlayerResult.$Properties
         * @property {game.PublicPlayer.$Properties|null} [player] PlayerResult player
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PlayerResult.
         * @memberof game
         * @interface IPlayerResult
         * @augments game.PlayerResult.$Properties
         * @deprecated Use game.PlayerResult.$Properties instead.
         */

        /**
         * Shape of a PlayerResult.
         * @typedef {game.PlayerResult.$Properties} game.PlayerResult.$Shape
         */

        /**
         * Constructs a new PlayerResult.
         * @memberof game
         * @classdesc Represents a PlayerResult.
         * @constructor
         * @param {game.PlayerResult.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PlayerResult = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PlayerResult player.
         * @member {game.PublicPlayer.$Properties|null|undefined} player
         * @memberof game.PlayerResult
         * @instance
         */
        PlayerResult.prototype.player = null;

        /**
         * Creates a new PlayerResult instance using the specified properties.
         * @function create
         * @memberof game.PlayerResult
         * @static
         * @param {game.PlayerResult.$Properties=} [properties] Properties to set
         * @returns {game.PlayerResult} PlayerResult instance
         * @type {{
         *   (properties: game.PlayerResult.$Shape): game.PlayerResult & game.PlayerResult.$Shape;
         *   (properties?: game.PlayerResult.$Properties): game.PlayerResult;
         * }}
         */
        PlayerResult.create = function(properties) {
            return new PlayerResult(properties);
        };

        /**
         * Encodes the specified PlayerResult message. Does not implicitly {@link game.PlayerResult.verify|verify} messages.
         * @function encode
         * @memberof game.PlayerResult
         * @static
         * @param {game.PlayerResult.$Properties} message PlayerResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerResult.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.game.PublicPlayer.encode(message.player, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PlayerResult message, length delimited. Does not implicitly {@link game.PlayerResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.PlayerResult
         * @static
         * @param {game.PlayerResult.$Properties} message PlayerResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerResult.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PlayerResult message from the specified reader or buffer.
         * @function decode
         * @memberof game.PlayerResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.PlayerResult & game.PlayerResult.$Shape} PlayerResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerResult.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.PlayerResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.game.PublicPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PlayerResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.PlayerResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.PlayerResult & game.PlayerResult.$Shape} PlayerResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerResult.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlayerResult message.
         * @function verify
         * @memberof game.PlayerResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerResult.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                let error = $root.game.PublicPlayer.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            return null;
        };

        /**
         * Creates a PlayerResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.PlayerResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.PlayerResult} PlayerResult
         */
        PlayerResult.fromObject = function (object, _depth) {
            if (object instanceof $root.game.PlayerResult)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.PlayerResult: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.PlayerResult();
            if (object.player != null) {
                if (!$util.isObject(object.player))
                    throw $TypeError(".game.PlayerResult.player: object expected");
                message.player = $root.game.PublicPlayer.fromObject(object.player, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PlayerResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.PlayerResult
         * @static
         * @param {game.PlayerResult} message PlayerResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlayerResult.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.player = null;
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                object.player = $root.game.PublicPlayer.toObject(message.player, options, _depth + 1);
            return object;
        };

        /**
         * Converts this PlayerResult to JSON.
         * @function toJSON
         * @memberof game.PlayerResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlayerResult.prototype.toJSON = function() {
            return PlayerResult.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PlayerResult
         * @function getTypeUrl
         * @memberof game.PlayerResult
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PlayerResult.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.PlayerResult";
        };

        return PlayerResult;
    })();

    game.SuggestionsResult = (function() {

        /**
         * Properties of a SuggestionsResult.
         * @typedef {Object} game.SuggestionsResult.$Properties
         * @property {Array.<game.Suggestion.$Properties>|null} [suggestions] SuggestionsResult suggestions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SuggestionsResult.
         * @memberof game
         * @interface ISuggestionsResult
         * @augments game.SuggestionsResult.$Properties
         * @deprecated Use game.SuggestionsResult.$Properties instead.
         */

        /**
         * Shape of a SuggestionsResult.
         * @typedef {game.SuggestionsResult.$Properties} game.SuggestionsResult.$Shape
         */

        /**
         * Constructs a new SuggestionsResult.
         * @memberof game
         * @classdesc Represents a SuggestionsResult.
         * @constructor
         * @param {game.SuggestionsResult.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SuggestionsResult = function (properties) {
            this.suggestions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SuggestionsResult suggestions.
         * @member {Array.<game.Suggestion.$Properties>} suggestions
         * @memberof game.SuggestionsResult
         * @instance
         */
        SuggestionsResult.prototype.suggestions = $util.emptyArray;

        /**
         * Creates a new SuggestionsResult instance using the specified properties.
         * @function create
         * @memberof game.SuggestionsResult
         * @static
         * @param {game.SuggestionsResult.$Properties=} [properties] Properties to set
         * @returns {game.SuggestionsResult} SuggestionsResult instance
         * @type {{
         *   (properties: game.SuggestionsResult.$Shape): game.SuggestionsResult & game.SuggestionsResult.$Shape;
         *   (properties?: game.SuggestionsResult.$Properties): game.SuggestionsResult;
         * }}
         */
        SuggestionsResult.create = function(properties) {
            return new SuggestionsResult(properties);
        };

        /**
         * Encodes the specified SuggestionsResult message. Does not implicitly {@link game.SuggestionsResult.verify|verify} messages.
         * @function encode
         * @memberof game.SuggestionsResult
         * @static
         * @param {game.SuggestionsResult.$Properties} message SuggestionsResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SuggestionsResult.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.suggestions != null && message.suggestions.length)
                for (let i = 0; i < message.suggestions.length; ++i)
                    $root.game.Suggestion.encode(message.suggestions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SuggestionsResult message, length delimited. Does not implicitly {@link game.SuggestionsResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.SuggestionsResult
         * @static
         * @param {game.SuggestionsResult.$Properties} message SuggestionsResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SuggestionsResult.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SuggestionsResult message from the specified reader or buffer.
         * @function decode
         * @memberof game.SuggestionsResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.SuggestionsResult & game.SuggestionsResult.$Shape} SuggestionsResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SuggestionsResult.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.SuggestionsResult();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.suggestions && message.suggestions.length))
                            message.suggestions = [];
                        message.suggestions.push($root.game.Suggestion.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SuggestionsResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.SuggestionsResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.SuggestionsResult & game.SuggestionsResult.$Shape} SuggestionsResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SuggestionsResult.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SuggestionsResult message.
         * @function verify
         * @memberof game.SuggestionsResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SuggestionsResult.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.suggestions != null && $Object.hasOwnProperty.call(message, "suggestions")) {
                if (!$Array.isArray(message.suggestions))
                    return "suggestions: array expected";
                for (let i = 0; i < message.suggestions.length; ++i) {
                    let error = $root.game.Suggestion.verify(message.suggestions[i], _depth + 1);
                    if (error)
                        return "suggestions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SuggestionsResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.SuggestionsResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.SuggestionsResult} SuggestionsResult
         */
        SuggestionsResult.fromObject = function (object, _depth) {
            if (object instanceof $root.game.SuggestionsResult)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.SuggestionsResult: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.SuggestionsResult();
            if (object.suggestions) {
                if (!$Array.isArray(object.suggestions))
                    throw $TypeError(".game.SuggestionsResult.suggestions: array expected");
                message.suggestions = $Array(object.suggestions.length);
                for (let i = 0; i < object.suggestions.length; ++i) {
                    if (!$util.isObject(object.suggestions[i]))
                        throw $TypeError(".game.SuggestionsResult.suggestions: object expected");
                    message.suggestions[i] = $root.game.Suggestion.fromObject(object.suggestions[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SuggestionsResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.SuggestionsResult
         * @static
         * @param {game.SuggestionsResult} message SuggestionsResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SuggestionsResult.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.suggestions = [];
            if (message.suggestions && message.suggestions.length) {
                object.suggestions = $Array(message.suggestions.length);
                for (let j = 0; j < message.suggestions.length; ++j)
                    object.suggestions[j] = $root.game.Suggestion.toObject(message.suggestions[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this SuggestionsResult to JSON.
         * @function toJSON
         * @memberof game.SuggestionsResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SuggestionsResult.prototype.toJSON = function() {
            return SuggestionsResult.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SuggestionsResult
         * @function getTypeUrl
         * @memberof game.SuggestionsResult
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SuggestionsResult.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.SuggestionsResult";
        };

        return SuggestionsResult;
    })();

    game.RawBody = (function() {

        /**
         * Properties of a RawBody.
         * @typedef {Object} game.RawBody.$Properties
         * @property {google.protobuf.Struct.$Properties|null} [dynamic] RawBody dynamic
         * @property {game.PlayerBatch.$Properties|null} [playerBatch] RawBody playerBatch
         * @property {game.ChatMessage.$Properties|null} [chat] RawBody chat
         * @property {game.Suggestion.$Properties|null} [suggestion] RawBody suggestion
         * @property {game.PublicPlayer.$Properties|null} [player] RawBody player
         * @property {game.MeState.$Properties|null} [me] RawBody me
         * @property {game.AnnouncementPayload.$Properties|null} [announcement] RawBody announcement
         * @property {game.RoomClosed.$Properties|null} [roomClosed] RawBody roomClosed
         * @property {game.HistoryPage.$Properties|null} [historyPage] RawBody historyPage
         * @property {game.OkResult.$Properties|null} [ok] RawBody ok
         * @property {game.PlayerResult.$Properties|null} [playerResult] RawBody playerResult
         * @property {game.SuggestionsResult.$Properties|null} [suggestions] RawBody suggestions
         * @property {game.RoomSnapshot.$Properties|null} [room] RawBody room
         * @property {game.AppConfig.$Properties|null} [config] RawBody config
         * @property {game.LobbySnapshot.$Properties|null} [lobby] RawBody lobby
         * @property {"dynamic"|"playerBatch"|"chat"|"suggestion"|"player"|"me"|"announcement"|"roomClosed"|"historyPage"|"ok"|"playerResult"|"suggestions"|"room"|"config"|"lobby"} [body] RawBody body
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RawBody.
         * @memberof game
         * @interface IRawBody
         * @augments game.RawBody.$Properties
         * @deprecated Use game.RawBody.$Properties instead.
         */

        /**
         * Narrowed shape of a RawBody.
         * @typedef {{
         *   dynamic?: google.protobuf.Struct.$Shape|null;
         *   playerBatch?: game.PlayerBatch.$Shape|null;
         *   chat?: game.ChatMessage.$Shape|null;
         *   suggestion?: game.Suggestion.$Shape|null;
         *   player?: game.PublicPlayer.$Shape|null;
         *   me?: game.MeState.$Shape|null;
         *   announcement?: game.AnnouncementPayload.$Shape|null;
         *   roomClosed?: game.RoomClosed.$Shape|null;
         *   historyPage?: game.HistoryPage.$Shape|null;
         *   ok?: game.OkResult.$Shape|null;
         *   playerResult?: game.PlayerResult.$Shape|null;
         *   suggestions?: game.SuggestionsResult.$Shape|null;
         *   room?: game.RoomSnapshot.$Shape|null;
         *   config?: game.AppConfig.$Shape|null;
         *   lobby?: game.LobbySnapshot.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ body?: undefined; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "dynamic"; dynamic: google.protobuf.Struct.$Shape; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "playerBatch"; dynamic?: null; playerBatch: game.PlayerBatch.$Shape; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "chat"; dynamic?: null; playerBatch?: null; chat: game.ChatMessage.$Shape; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "suggestion"; dynamic?: null; playerBatch?: null; chat?: null; suggestion: game.Suggestion.$Shape; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "player"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player: game.PublicPlayer.$Shape; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "me"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me: game.MeState.$Shape; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "announcement"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement: game.AnnouncementPayload.$Shape; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "roomClosed"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed: game.RoomClosed.$Shape; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "historyPage"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage: game.HistoryPage.$Shape; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "ok"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok: game.OkResult.$Shape; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "playerResult"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult: game.PlayerResult.$Shape; suggestions?: null; room?: null; config?: null; lobby?: null }|{ body?: "suggestions"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions: game.SuggestionsResult.$Shape; room?: null; config?: null; lobby?: null }|{ body?: "room"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room: game.RoomSnapshot.$Shape; config?: null; lobby?: null }|{ body?: "config"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config: game.AppConfig.$Shape; lobby?: null }|{ body?: "lobby"; dynamic?: null; playerBatch?: null; chat?: null; suggestion?: null; player?: null; me?: null; announcement?: null; roomClosed?: null; historyPage?: null; ok?: null; playerResult?: null; suggestions?: null; room?: null; config?: null; lobby: game.LobbySnapshot.$Shape })
         * )} game.RawBody.$Shape
         */

        /**
         * Constructs a new RawBody.
         * @memberof game
         * @classdesc Represents a RawBody.
         * @constructor
         * @param {game.RawBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RawBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RawBody dynamic.
         * @member {google.protobuf.Struct.$Properties|null|undefined} dynamic
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.dynamic = null;

        /**
         * RawBody playerBatch.
         * @member {game.PlayerBatch.$Properties|null|undefined} playerBatch
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.playerBatch = null;

        /**
         * RawBody chat.
         * @member {game.ChatMessage.$Properties|null|undefined} chat
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.chat = null;

        /**
         * RawBody suggestion.
         * @member {game.Suggestion.$Properties|null|undefined} suggestion
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.suggestion = null;

        /**
         * RawBody player.
         * @member {game.PublicPlayer.$Properties|null|undefined} player
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.player = null;

        /**
         * RawBody me.
         * @member {game.MeState.$Properties|null|undefined} me
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.me = null;

        /**
         * RawBody announcement.
         * @member {game.AnnouncementPayload.$Properties|null|undefined} announcement
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.announcement = null;

        /**
         * RawBody roomClosed.
         * @member {game.RoomClosed.$Properties|null|undefined} roomClosed
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.roomClosed = null;

        /**
         * RawBody historyPage.
         * @member {game.HistoryPage.$Properties|null|undefined} historyPage
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.historyPage = null;

        /**
         * RawBody ok.
         * @member {game.OkResult.$Properties|null|undefined} ok
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.ok = null;

        /**
         * RawBody playerResult.
         * @member {game.PlayerResult.$Properties|null|undefined} playerResult
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.playerResult = null;

        /**
         * RawBody suggestions.
         * @member {game.SuggestionsResult.$Properties|null|undefined} suggestions
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.suggestions = null;

        /**
         * RawBody room.
         * @member {game.RoomSnapshot.$Properties|null|undefined} room
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.room = null;

        /**
         * RawBody config.
         * @member {game.AppConfig.$Properties|null|undefined} config
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.config = null;

        /**
         * RawBody lobby.
         * @member {game.LobbySnapshot.$Properties|null|undefined} lobby
         * @memberof game.RawBody
         * @instance
         */
        RawBody.prototype.lobby = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * RawBody body.
         * @member {"dynamic"|"playerBatch"|"chat"|"suggestion"|"player"|"me"|"announcement"|"roomClosed"|"historyPage"|"ok"|"playerResult"|"suggestions"|"room"|"config"|"lobby"|undefined} body
         * @memberof game.RawBody
         * @instance
         */
        $Object.defineProperty(RawBody.prototype, "body", {
            get: $util.oneOfGetter($oneOfFields = ["dynamic", "playerBatch", "chat", "suggestion", "player", "me", "announcement", "roomClosed", "historyPage", "ok", "playerResult", "suggestions", "room", "config", "lobby"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new RawBody instance using the specified properties.
         * @function create
         * @memberof game.RawBody
         * @static
         * @param {game.RawBody.$Properties=} [properties] Properties to set
         * @returns {game.RawBody} RawBody instance
         * @type {{
         *   (properties: game.RawBody.$Shape): game.RawBody & game.RawBody.$Shape;
         *   (properties?: game.RawBody.$Properties): game.RawBody;
         * }}
         */
        RawBody.create = function(properties) {
            return new RawBody(properties);
        };

        /**
         * Encodes the specified RawBody message. Does not implicitly {@link game.RawBody.verify|verify} messages.
         * @function encode
         * @memberof game.RawBody
         * @static
         * @param {game.RawBody.$Properties} message RawBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RawBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.dynamic != null && $Object.hasOwnProperty.call(message, "dynamic"))
                $root.google.protobuf.Struct.encode(message.dynamic, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.playerBatch != null && $Object.hasOwnProperty.call(message, "playerBatch"))
                $root.game.PlayerBatch.encode(message.playerBatch, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.chat != null && $Object.hasOwnProperty.call(message, "chat"))
                $root.game.ChatMessage.encode(message.chat, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.suggestion != null && $Object.hasOwnProperty.call(message, "suggestion"))
                $root.game.Suggestion.encode(message.suggestion, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.player != null && $Object.hasOwnProperty.call(message, "player"))
                $root.game.PublicPlayer.encode(message.player, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.me != null && $Object.hasOwnProperty.call(message, "me"))
                $root.game.MeState.encode(message.me, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.announcement != null && $Object.hasOwnProperty.call(message, "announcement"))
                $root.game.AnnouncementPayload.encode(message.announcement, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.roomClosed != null && $Object.hasOwnProperty.call(message, "roomClosed"))
                $root.game.RoomClosed.encode(message.roomClosed, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.historyPage != null && $Object.hasOwnProperty.call(message, "historyPage"))
                $root.game.HistoryPage.encode(message.historyPage, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.ok != null && $Object.hasOwnProperty.call(message, "ok"))
                $root.game.OkResult.encode(message.ok, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.playerResult != null && $Object.hasOwnProperty.call(message, "playerResult"))
                $root.game.PlayerResult.encode(message.playerResult, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.suggestions != null && $Object.hasOwnProperty.call(message, "suggestions"))
                $root.game.SuggestionsResult.encode(message.suggestions, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.room != null && $Object.hasOwnProperty.call(message, "room"))
                $root.game.RoomSnapshot.encode(message.room, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                $root.game.AppConfig.encode(message.config, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.lobby != null && $Object.hasOwnProperty.call(message, "lobby"))
                $root.game.LobbySnapshot.encode(message.lobby, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RawBody message, length delimited. Does not implicitly {@link game.RawBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof game.RawBody
         * @static
         * @param {game.RawBody.$Properties} message RawBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RawBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RawBody message from the specified reader or buffer.
         * @function decode
         * @memberof game.RawBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {game.RawBody & game.RawBody.$Shape} RawBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RawBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.game.RawBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.dynamic = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.dynamic);
                        message.body = "dynamic";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.playerBatch = $root.game.PlayerBatch.decode(reader, reader.uint32(), $undefined, _depth + 1, message.playerBatch);
                        message.body = "playerBatch";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.chat = $root.game.ChatMessage.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chat);
                        message.body = "chat";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.suggestion = $root.game.Suggestion.decode(reader, reader.uint32(), $undefined, _depth + 1, message.suggestion);
                        message.body = "suggestion";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.game.PublicPlayer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.player);
                        message.body = "player";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.me = $root.game.MeState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.me);
                        message.body = "me";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.announcement = $root.game.AnnouncementPayload.decode(reader, reader.uint32(), $undefined, _depth + 1, message.announcement);
                        message.body = "announcement";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.roomClosed = $root.game.RoomClosed.decode(reader, reader.uint32(), $undefined, _depth + 1, message.roomClosed);
                        message.body = "roomClosed";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.historyPage = $root.game.HistoryPage.decode(reader, reader.uint32(), $undefined, _depth + 1, message.historyPage);
                        message.body = "historyPage";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.ok = $root.game.OkResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ok);
                        message.body = "ok";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.playerResult = $root.game.PlayerResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.playerResult);
                        message.body = "playerResult";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.suggestions = $root.game.SuggestionsResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.suggestions);
                        message.body = "suggestions";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.game.RoomSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.room);
                        message.body = "room";
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.config = $root.game.AppConfig.decode(reader, reader.uint32(), $undefined, _depth + 1, message.config);
                        message.body = "config";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.lobby = $root.game.LobbySnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lobby);
                        message.body = "lobby";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RawBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof game.RawBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {game.RawBody & game.RawBody.$Shape} RawBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RawBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RawBody message.
         * @function verify
         * @memberof game.RawBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RawBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.dynamic != null && $Object.hasOwnProperty.call(message, "dynamic")) {
                properties.body = 1;
                {
                    let error = $root.google.protobuf.Struct.verify(message.dynamic, _depth + 1);
                    if (error)
                        return "dynamic." + error;
                }
            }
            if (message.playerBatch != null && $Object.hasOwnProperty.call(message, "playerBatch")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.PlayerBatch.verify(message.playerBatch, _depth + 1);
                    if (error)
                        return "playerBatch." + error;
                }
            }
            if (message.chat != null && $Object.hasOwnProperty.call(message, "chat")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.ChatMessage.verify(message.chat, _depth + 1);
                    if (error)
                        return "chat." + error;
                }
            }
            if (message.suggestion != null && $Object.hasOwnProperty.call(message, "suggestion")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.Suggestion.verify(message.suggestion, _depth + 1);
                    if (error)
                        return "suggestion." + error;
                }
            }
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.PublicPlayer.verify(message.player, _depth + 1);
                    if (error)
                        return "player." + error;
                }
            }
            if (message.me != null && $Object.hasOwnProperty.call(message, "me")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.MeState.verify(message.me, _depth + 1);
                    if (error)
                        return "me." + error;
                }
            }
            if (message.announcement != null && $Object.hasOwnProperty.call(message, "announcement")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.AnnouncementPayload.verify(message.announcement, _depth + 1);
                    if (error)
                        return "announcement." + error;
                }
            }
            if (message.roomClosed != null && $Object.hasOwnProperty.call(message, "roomClosed")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.RoomClosed.verify(message.roomClosed, _depth + 1);
                    if (error)
                        return "roomClosed." + error;
                }
            }
            if (message.historyPage != null && $Object.hasOwnProperty.call(message, "historyPage")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.HistoryPage.verify(message.historyPage, _depth + 1);
                    if (error)
                        return "historyPage." + error;
                }
            }
            if (message.ok != null && $Object.hasOwnProperty.call(message, "ok")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.OkResult.verify(message.ok, _depth + 1);
                    if (error)
                        return "ok." + error;
                }
            }
            if (message.playerResult != null && $Object.hasOwnProperty.call(message, "playerResult")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.PlayerResult.verify(message.playerResult, _depth + 1);
                    if (error)
                        return "playerResult." + error;
                }
            }
            if (message.suggestions != null && $Object.hasOwnProperty.call(message, "suggestions")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.SuggestionsResult.verify(message.suggestions, _depth + 1);
                    if (error)
                        return "suggestions." + error;
                }
            }
            if (message.room != null && $Object.hasOwnProperty.call(message, "room")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.RoomSnapshot.verify(message.room, _depth + 1);
                    if (error)
                        return "room." + error;
                }
            }
            if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.AppConfig.verify(message.config, _depth + 1);
                    if (error)
                        return "config." + error;
                }
            }
            if (message.lobby != null && $Object.hasOwnProperty.call(message, "lobby")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.game.LobbySnapshot.verify(message.lobby, _depth + 1);
                    if (error)
                        return "lobby." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RawBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof game.RawBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {game.RawBody} RawBody
         */
        RawBody.fromObject = function (object, _depth) {
            if (object instanceof $root.game.RawBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".game.RawBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.game.RawBody();
            if (object.dynamic != null) {
                if (!$util.isObject(object.dynamic))
                    throw $TypeError(".game.RawBody.dynamic: object expected");
                message.dynamic = $root.google.protobuf.Struct.fromObject(object.dynamic, _depth + 1);
            }
            if (object.playerBatch != null) {
                if (!$util.isObject(object.playerBatch))
                    throw $TypeError(".game.RawBody.playerBatch: object expected");
                message.playerBatch = $root.game.PlayerBatch.fromObject(object.playerBatch, _depth + 1);
            }
            if (object.chat != null) {
                if (!$util.isObject(object.chat))
                    throw $TypeError(".game.RawBody.chat: object expected");
                message.chat = $root.game.ChatMessage.fromObject(object.chat, _depth + 1);
            }
            if (object.suggestion != null) {
                if (!$util.isObject(object.suggestion))
                    throw $TypeError(".game.RawBody.suggestion: object expected");
                message.suggestion = $root.game.Suggestion.fromObject(object.suggestion, _depth + 1);
            }
            if (object.player != null) {
                if (!$util.isObject(object.player))
                    throw $TypeError(".game.RawBody.player: object expected");
                message.player = $root.game.PublicPlayer.fromObject(object.player, _depth + 1);
            }
            if (object.me != null) {
                if (!$util.isObject(object.me))
                    throw $TypeError(".game.RawBody.me: object expected");
                message.me = $root.game.MeState.fromObject(object.me, _depth + 1);
            }
            if (object.announcement != null) {
                if (!$util.isObject(object.announcement))
                    throw $TypeError(".game.RawBody.announcement: object expected");
                message.announcement = $root.game.AnnouncementPayload.fromObject(object.announcement, _depth + 1);
            }
            if (object.roomClosed != null) {
                if (!$util.isObject(object.roomClosed))
                    throw $TypeError(".game.RawBody.roomClosed: object expected");
                message.roomClosed = $root.game.RoomClosed.fromObject(object.roomClosed, _depth + 1);
            }
            if (object.historyPage != null) {
                if (!$util.isObject(object.historyPage))
                    throw $TypeError(".game.RawBody.historyPage: object expected");
                message.historyPage = $root.game.HistoryPage.fromObject(object.historyPage, _depth + 1);
            }
            if (object.ok != null) {
                if (!$util.isObject(object.ok))
                    throw $TypeError(".game.RawBody.ok: object expected");
                message.ok = $root.game.OkResult.fromObject(object.ok, _depth + 1);
            }
            if (object.playerResult != null) {
                if (!$util.isObject(object.playerResult))
                    throw $TypeError(".game.RawBody.playerResult: object expected");
                message.playerResult = $root.game.PlayerResult.fromObject(object.playerResult, _depth + 1);
            }
            if (object.suggestions != null) {
                if (!$util.isObject(object.suggestions))
                    throw $TypeError(".game.RawBody.suggestions: object expected");
                message.suggestions = $root.game.SuggestionsResult.fromObject(object.suggestions, _depth + 1);
            }
            if (object.room != null) {
                if (!$util.isObject(object.room))
                    throw $TypeError(".game.RawBody.room: object expected");
                message.room = $root.game.RoomSnapshot.fromObject(object.room, _depth + 1);
            }
            if (object.config != null) {
                if (!$util.isObject(object.config))
                    throw $TypeError(".game.RawBody.config: object expected");
                message.config = $root.game.AppConfig.fromObject(object.config, _depth + 1);
            }
            if (object.lobby != null) {
                if (!$util.isObject(object.lobby))
                    throw $TypeError(".game.RawBody.lobby: object expected");
                message.lobby = $root.game.LobbySnapshot.fromObject(object.lobby, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RawBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof game.RawBody
         * @static
         * @param {game.RawBody} message RawBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RawBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.dynamic != null && $Object.hasOwnProperty.call(message, "dynamic")) {
                object.dynamic = $root.google.protobuf.Struct.toObject(message.dynamic, options, _depth + 1);
                if (options.oneofs)
                    object.body = "dynamic";
            }
            if (message.playerBatch != null && $Object.hasOwnProperty.call(message, "playerBatch")) {
                object.playerBatch = $root.game.PlayerBatch.toObject(message.playerBatch, options, _depth + 1);
                if (options.oneofs)
                    object.body = "playerBatch";
            }
            if (message.chat != null && $Object.hasOwnProperty.call(message, "chat")) {
                object.chat = $root.game.ChatMessage.toObject(message.chat, options, _depth + 1);
                if (options.oneofs)
                    object.body = "chat";
            }
            if (message.suggestion != null && $Object.hasOwnProperty.call(message, "suggestion")) {
                object.suggestion = $root.game.Suggestion.toObject(message.suggestion, options, _depth + 1);
                if (options.oneofs)
                    object.body = "suggestion";
            }
            if (message.player != null && $Object.hasOwnProperty.call(message, "player")) {
                object.player = $root.game.PublicPlayer.toObject(message.player, options, _depth + 1);
                if (options.oneofs)
                    object.body = "player";
            }
            if (message.me != null && $Object.hasOwnProperty.call(message, "me")) {
                object.me = $root.game.MeState.toObject(message.me, options, _depth + 1);
                if (options.oneofs)
                    object.body = "me";
            }
            if (message.announcement != null && $Object.hasOwnProperty.call(message, "announcement")) {
                object.announcement = $root.game.AnnouncementPayload.toObject(message.announcement, options, _depth + 1);
                if (options.oneofs)
                    object.body = "announcement";
            }
            if (message.roomClosed != null && $Object.hasOwnProperty.call(message, "roomClosed")) {
                object.roomClosed = $root.game.RoomClosed.toObject(message.roomClosed, options, _depth + 1);
                if (options.oneofs)
                    object.body = "roomClosed";
            }
            if (message.historyPage != null && $Object.hasOwnProperty.call(message, "historyPage")) {
                object.historyPage = $root.game.HistoryPage.toObject(message.historyPage, options, _depth + 1);
                if (options.oneofs)
                    object.body = "historyPage";
            }
            if (message.ok != null && $Object.hasOwnProperty.call(message, "ok")) {
                object.ok = $root.game.OkResult.toObject(message.ok, options, _depth + 1);
                if (options.oneofs)
                    object.body = "ok";
            }
            if (message.playerResult != null && $Object.hasOwnProperty.call(message, "playerResult")) {
                object.playerResult = $root.game.PlayerResult.toObject(message.playerResult, options, _depth + 1);
                if (options.oneofs)
                    object.body = "playerResult";
            }
            if (message.suggestions != null && $Object.hasOwnProperty.call(message, "suggestions")) {
                object.suggestions = $root.game.SuggestionsResult.toObject(message.suggestions, options, _depth + 1);
                if (options.oneofs)
                    object.body = "suggestions";
            }
            if (message.room != null && $Object.hasOwnProperty.call(message, "room")) {
                object.room = $root.game.RoomSnapshot.toObject(message.room, options, _depth + 1);
                if (options.oneofs)
                    object.body = "room";
            }
            if (message.config != null && $Object.hasOwnProperty.call(message, "config")) {
                object.config = $root.game.AppConfig.toObject(message.config, options, _depth + 1);
                if (options.oneofs)
                    object.body = "config";
            }
            if (message.lobby != null && $Object.hasOwnProperty.call(message, "lobby")) {
                object.lobby = $root.game.LobbySnapshot.toObject(message.lobby, options, _depth + 1);
                if (options.oneofs)
                    object.body = "lobby";
            }
            return object;
        };

        /**
         * Converts this RawBody to JSON.
         * @function toJSON
         * @memberof game.RawBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RawBody.prototype.toJSON = function() {
            return RawBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RawBody
         * @function getTypeUrl
         * @memberof game.RawBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RawBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/game.RawBody";
        };

        return RawBody;
    })();

    return game;
})();

export const google = $root.google = (() => {

    /**
     * Namespace google.
     * @exports google
     * @namespace
     */
    const google = {};

    google.protobuf = (function() {

        /**
         * Namespace protobuf.
         * @memberof google
         * @namespace
         */
        const protobuf = {};

        protobuf.Struct = (function() {

            /**
             * Properties of a Struct.
             * @typedef {Object} google.protobuf.Struct.$Properties
             * @property {Object.<string,google.protobuf.Value.$Properties>|null} [fields] Struct fields
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Struct.
             * @memberof google.protobuf
             * @interface IStruct
             * @augments google.protobuf.Struct.$Properties
             * @deprecated Use google.protobuf.Struct.$Properties instead.
             */

            /**
             * Shape of a Struct.
             * @typedef {{
             *   fields?: Object.<string,google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.Struct.$Shape
             */

            /**
             * Constructs a new Struct.
             * @memberof google.protobuf
             * @classdesc Represents a Struct.
             * @constructor
             * @param {google.protobuf.Struct.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Struct = function (properties) {
                this.fields = {};
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Struct fields.
             * @member {Object.<string,google.protobuf.Value.$Properties>} fields
             * @memberof google.protobuf.Struct
             * @instance
             */
            Struct.prototype.fields = $util.emptyObject;

            /**
             * Creates a new Struct instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Struct} Struct instance
             * @type {{
             *   (properties: google.protobuf.Struct.$Shape): google.protobuf.Struct & google.protobuf.Struct.$Shape;
             *   (properties?: google.protobuf.Struct.$Properties): google.protobuf.Struct;
             * }}
             */
            Struct.create = function(properties) {
                return new Struct(properties);
            };

            /**
             * Encodes the specified Struct message. Does not implicitly {@link google.protobuf.Struct.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct.$Properties} message Struct message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Struct.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.fields != null && $Object.hasOwnProperty.call(message, "fields"))
                    for (let keys = $Object.keys(message.fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Struct message, length delimited. Does not implicitly {@link google.protobuf.Struct.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct.$Properties} message Struct message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Struct.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Struct message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Struct
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Struct & google.protobuf.Struct.$Shape} Struct
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Struct.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.Struct(), key, value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (message.fields === $util.emptyObject)
                                message.fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.fields, key);
                            message.fields[key] = value || new $root.google.protobuf.Value();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Struct message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Struct
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Struct & google.protobuf.Struct.$Shape} Struct
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Struct.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Struct message.
             * @function verify
             * @memberof google.protobuf.Struct
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Struct.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.fields != null && $Object.hasOwnProperty.call(message, "fields")) {
                    if (!$util.isObject(message.fields))
                        return "fields: object expected";
                    let key = $Object.keys(message.fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.fields[key[i]], _depth + 1);
                        if (error)
                            return "fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Struct message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Struct
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Struct} Struct
             */
            Struct.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Struct)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Struct: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.Struct();
                if (object.fields) {
                    if (!$util.isObject(object.fields))
                        throw $TypeError(".google.protobuf.Struct.fields: object expected");
                    message.fields = {};
                    for (let keys = $Object.keys(object.fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.fields, keys[i]);
                        if (!$util.isObject(object.fields[keys[i]]))
                            throw $TypeError(".google.protobuf.Struct.fields: object expected");
                        message.fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.fields[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Struct message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct} message Struct
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Struct.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.fields = {};
                let keys2;
                if (message.fields && (keys2 = $Object.keys(message.fields)).length) {
                    object.fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.fields, keys2[j]);
                        object.fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.fields[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this Struct to JSON.
             * @function toJSON
             * @memberof google.protobuf.Struct
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Struct.prototype.toJSON = function() {
                return Struct.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Struct
             * @function getTypeUrl
             * @memberof google.protobuf.Struct
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Struct.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Struct";
            };

            return Struct;
        })();

        protobuf.Value = (function() {

            /**
             * Properties of a Value.
             * @typedef {Object} google.protobuf.Value.$Properties
             * @property {google.protobuf.NullValue|null} [nullValue] Value nullValue
             * @property {number|null} [numberValue] Value numberValue
             * @property {string|null} [stringValue] Value stringValue
             * @property {boolean|null} [boolValue] Value boolValue
             * @property {google.protobuf.Struct.$Properties|null} [structValue] Value structValue
             * @property {google.protobuf.ListValue.$Properties|null} [listValue] Value listValue
             * @property {"nullValue"|"numberValue"|"stringValue"|"boolValue"|"structValue"|"listValue"} [kind] Value kind
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Value.
             * @memberof google.protobuf
             * @interface IValue
             * @augments google.protobuf.Value.$Properties
             * @deprecated Use google.protobuf.Value.$Properties instead.
             */

            /**
             * Narrowed shape of a Value.
             * @typedef {{
             *   nullValue?: google.protobuf.NullValue|null;
             *   numberValue?: number|null;
             *   stringValue?: string|null;
             *   boolValue?: boolean|null;
             *   structValue?: google.protobuf.Struct.$Shape|null;
             *   listValue?: google.protobuf.ListValue.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * } & (
             *   ({ kind?: undefined; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "nullValue"; nullValue: google.protobuf.NullValue; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "numberValue"; nullValue?: null; numberValue: number; stringValue?: null; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "stringValue"; nullValue?: null; numberValue?: null; stringValue: string; boolValue?: null; structValue?: null; listValue?: null }|{ kind?: "boolValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue: boolean; structValue?: null; listValue?: null }|{ kind?: "structValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue: google.protobuf.Struct.$Shape; listValue?: null }|{ kind?: "listValue"; nullValue?: null; numberValue?: null; stringValue?: null; boolValue?: null; structValue?: null; listValue: google.protobuf.ListValue.$Shape })
             * )} google.protobuf.Value.$Shape
             */

            /**
             * Constructs a new Value.
             * @memberof google.protobuf
             * @classdesc Represents a Value.
             * @constructor
             * @param {google.protobuf.Value.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Value = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Value nullValue.
             * @member {google.protobuf.NullValue|null|undefined} nullValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.nullValue = null;

            /**
             * Value numberValue.
             * @member {number|null|undefined} numberValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.numberValue = null;

            /**
             * Value stringValue.
             * @member {string|null|undefined} stringValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.stringValue = null;

            /**
             * Value boolValue.
             * @member {boolean|null|undefined} boolValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.boolValue = null;

            /**
             * Value structValue.
             * @member {google.protobuf.Struct.$Properties|null|undefined} structValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.structValue = null;

            /**
             * Value listValue.
             * @member {google.protobuf.ListValue.$Properties|null|undefined} listValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.listValue = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Value kind.
             * @member {"nullValue"|"numberValue"|"stringValue"|"boolValue"|"structValue"|"listValue"|undefined} kind
             * @memberof google.protobuf.Value
             * @instance
             */
            $Object.defineProperty(Value.prototype, "kind", {
                get: $util.oneOfGetter($oneOfFields = ["nullValue", "numberValue", "stringValue", "boolValue", "structValue", "listValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Value instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Value} Value instance
             * @type {{
             *   (properties: google.protobuf.Value.$Shape): google.protobuf.Value & google.protobuf.Value.$Shape;
             *   (properties?: google.protobuf.Value.$Properties): google.protobuf.Value;
             * }}
             */
            Value.create = function(properties) {
                return new Value(properties);
            };

            /**
             * Encodes the specified Value message. Does not implicitly {@link google.protobuf.Value.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value.$Properties} message Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Value.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.nullValue);
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.numberValue);
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.stringValue);
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.boolValue);
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue"))
                    $root.google.protobuf.Struct.encode(message.structValue, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue"))
                    $root.google.protobuf.ListValue.encode(message.listValue, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Value message, length delimited. Does not implicitly {@link google.protobuf.Value.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value.$Properties} message Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Value.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Value message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Value & google.protobuf.Value.$Shape} Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Value.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.Value(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            message.nullValue = reader.int32();
                            message.kind = "nullValue";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            message.numberValue = reader.double();
                            message.kind = "numberValue";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.stringValue = reader.stringVerify();
                            message.kind = "stringValue";
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            message.boolValue = reader.bool();
                            message.kind = "boolValue";
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.structValue = $root.google.protobuf.Struct.decode(reader, reader.uint32(), $undefined, _depth + 1, message.structValue);
                            message.kind = "structValue";
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.listValue = $root.google.protobuf.ListValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.listValue);
                            message.kind = "listValue";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Value message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Value & google.protobuf.Value.$Shape} Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Value.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Value message.
             * @function verify
             * @memberof google.protobuf.Value
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Value.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                let properties = {};
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue")) {
                    properties.kind = 1;
                    if (typeof message.nullValue !== "number" || (message.nullValue | 0) !== message.nullValue)
                        return "nullValue: enum value expected";
                }
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (typeof message.numberValue !== "number")
                        return "numberValue: number expected";
                }
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (!$util.isString(message.stringValue))
                        return "stringValue: string expected";
                }
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (typeof message.boolValue !== "boolean")
                        return "boolValue: boolean expected";
                }
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.structValue, _depth + 1);
                        if (error)
                            return "structValue." + error;
                    }
                }
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    {
                        let error = $root.google.protobuf.ListValue.verify(message.listValue, _depth + 1);
                        if (error)
                            return "listValue." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Value message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Value
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Value} Value
             */
            Value.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Value)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Value: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.Value();
                switch (object.nullValue) {
                case "NULL_VALUE":
                case 0:
                    message.nullValue = 0;
                    break;
                default:
                    if (typeof object.nullValue === "number" && (object.nullValue | 0) === object.nullValue)
                        message.nullValue = object.nullValue;
                }
                if (object.numberValue != null)
                    message.numberValue = $Number(object.numberValue);
                if (object.stringValue != null)
                    message.stringValue = $String(object.stringValue);
                if (object.boolValue != null)
                    message.boolValue = $Boolean(object.boolValue);
                if (object.structValue != null) {
                    if (!$util.isObject(object.structValue))
                        throw $TypeError(".google.protobuf.Value.structValue: object expected");
                    message.structValue = $root.google.protobuf.Struct.fromObject(object.structValue, _depth + 1);
                }
                if (object.listValue != null) {
                    if (!$util.isObject(object.listValue))
                        throw $TypeError(".google.protobuf.Value.listValue: object expected");
                    message.listValue = $root.google.protobuf.ListValue.fromObject(object.listValue, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Value message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value} message Value
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Value.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (message.nullValue != null && $Object.hasOwnProperty.call(message, "nullValue")) {
                    object.nullValue = options.enums === $String ? $root.google.protobuf.NullValue[message.nullValue] === $undefined ? message.nullValue : $root.google.protobuf.NullValue[message.nullValue] : message.nullValue;
                    if (options.oneofs)
                        object.kind = "nullValue";
                }
                if (message.numberValue != null && $Object.hasOwnProperty.call(message, "numberValue")) {
                    object.numberValue = options.json && !$isFinite(message.numberValue) ? $String(message.numberValue) : message.numberValue;
                    if (options.oneofs)
                        object.kind = "numberValue";
                }
                if (message.stringValue != null && $Object.hasOwnProperty.call(message, "stringValue")) {
                    object.stringValue = message.stringValue;
                    if (options.oneofs)
                        object.kind = "stringValue";
                }
                if (message.boolValue != null && $Object.hasOwnProperty.call(message, "boolValue")) {
                    object.boolValue = message.boolValue;
                    if (options.oneofs)
                        object.kind = "boolValue";
                }
                if (message.structValue != null && $Object.hasOwnProperty.call(message, "structValue")) {
                    object.structValue = $root.google.protobuf.Struct.toObject(message.structValue, options, _depth + 1);
                    if (options.oneofs)
                        object.kind = "structValue";
                }
                if (message.listValue != null && $Object.hasOwnProperty.call(message, "listValue")) {
                    object.listValue = $root.google.protobuf.ListValue.toObject(message.listValue, options, _depth + 1);
                    if (options.oneofs)
                        object.kind = "listValue";
                }
                return object;
            };

            /**
             * Converts this Value to JSON.
             * @function toJSON
             * @memberof google.protobuf.Value
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Value.prototype.toJSON = function() {
                return Value.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Value
             * @function getTypeUrl
             * @memberof google.protobuf.Value
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Value.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Value";
            };

            return Value;
        })();

        /**
         * NullValue enum.
         * @name google.protobuf.NullValue
         * @enum {number}
         * @property {number} NULL_VALUE=0 NULL_VALUE value
         */
        protobuf.NullValue = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NULL_VALUE"] = 0;
            return values;
        })();

        protobuf.ListValue = (function() {

            /**
             * Properties of a ListValue.
             * @typedef {Object} google.protobuf.ListValue.$Properties
             * @property {Array.<google.protobuf.Value.$Properties>|null} [values] ListValue values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ListValue.
             * @memberof google.protobuf
             * @interface IListValue
             * @augments google.protobuf.ListValue.$Properties
             * @deprecated Use google.protobuf.ListValue.$Properties instead.
             */

            /**
             * Shape of a ListValue.
             * @typedef {{
             *   values?: Array.<google.protobuf.Value.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} google.protobuf.ListValue.$Shape
             */

            /**
             * Constructs a new ListValue.
             * @memberof google.protobuf
             * @classdesc Represents a ListValue.
             * @constructor
             * @param {google.protobuf.ListValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ListValue = function (properties) {
                this.values = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ListValue values.
             * @member {Array.<google.protobuf.Value.$Properties>} values
             * @memberof google.protobuf.ListValue
             * @instance
             */
            ListValue.prototype.values = $util.emptyArray;

            /**
             * Creates a new ListValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.ListValue} ListValue instance
             * @type {{
             *   (properties: google.protobuf.ListValue.$Shape): google.protobuf.ListValue & google.protobuf.ListValue.$Shape;
             *   (properties?: google.protobuf.ListValue.$Properties): google.protobuf.ListValue;
             * }}
             */
            ListValue.create = function(properties) {
                return new ListValue(properties);
            };

            /**
             * Encodes the specified ListValue message. Does not implicitly {@link google.protobuf.ListValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue.$Properties} message ListValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    for (let i = 0; i < message.values.length; ++i)
                        $root.google.protobuf.Value.encode(message.values[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ListValue message, length delimited. Does not implicitly {@link google.protobuf.ListValue.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue.$Properties} message ListValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListValue.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ListValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.ListValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.ListValue & google.protobuf.ListValue.$Shape} ListValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.google.protobuf.ListValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push($root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ListValue message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.ListValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.ListValue & google.protobuf.ListValue.$Shape} ListValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListValue.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ListValue message.
             * @function verify
             * @memberof google.protobuf.ListValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ListValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (let i = 0; i < message.values.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.values[i], _depth + 1);
                        if (error)
                            return "values." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ListValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.ListValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.ListValue} ListValue
             */
            ListValue.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.ListValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.ListValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.ListValue();
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".google.protobuf.ListValue.values: array expected");
                    message.values = $Array(object.values.length);
                    for (let i = 0; i < object.values.length; ++i) {
                        if (!$util.isObject(object.values[i]))
                            throw $TypeError(".google.protobuf.ListValue.values: object expected");
                        message.values[i] = $root.google.protobuf.Value.fromObject(object.values[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a ListValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue} message ListValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ListValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (let j = 0; j < message.values.length; ++j)
                        object.values[j] = $root.google.protobuf.Value.toObject(message.values[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this ListValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.ListValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ListValue.prototype.toJSON = function() {
                return ListValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ListValue
             * @function getTypeUrl
             * @memberof google.protobuf.ListValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ListValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.ListValue";
            };

            return ListValue;
        })();

        return protobuf;
    })();

    return google;
})();

export const wire = $root.wire = (() => {

    /**
     * Namespace wire.
     * @exports wire
     * @namespace
     */
    const wire = {};

    wire.Envelope = (function() {

        /**
         * Properties of an Envelope.
         * @typedef {Object} wire.Envelope.$Properties
         * @property {string|null} [event] Envelope event
         * @property {number|Long|null} [id] Envelope id
         * @property {string|null} [err] Envelope err
         * @property {wire.PayloadKind|null} [kind] Envelope kind
         * @property {string|null} [channel] Envelope channel
         * @property {number|Long|null} [seq] Envelope seq
         * @property {string|null} [hash] Envelope hash
         * @property {game.StateDocument.$Properties|null} [fullState] Envelope fullState
         * @property {wire.StateDelta.$Properties|null} [delta] Envelope delta
         * @property {game.RawBody.$Properties|null} [rawBody] Envelope rawBody
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an Envelope.
         * @memberof wire
         * @interface IEnvelope
         * @augments wire.Envelope.$Properties
         * @deprecated Use wire.Envelope.$Properties instead.
         */

        /**
         * Shape of an Envelope.
         * @typedef {{
         *   event?: string|null;
         *   id?: number|Long|null;
         *   err?: string|null;
         *   kind?: wire.PayloadKind|null;
         *   channel?: string|null;
         *   seq?: number|Long|null;
         *   hash?: string|null;
         *   fullState?: game.StateDocument.$Shape|null;
         *   delta?: wire.StateDelta.$Shape|null;
         *   rawBody?: game.RawBody.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} wire.Envelope.$Shape
         */

        /**
         * Constructs a new Envelope.
         * @memberof wire
         * @classdesc Represents an Envelope.
         * @constructor
         * @param {wire.Envelope.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Envelope = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Envelope event.
         * @member {string} event
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.event = "";

        /**
         * Envelope id.
         * @member {number|Long} id
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Envelope err.
         * @member {string} err
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.err = "";

        /**
         * Envelope kind.
         * @member {wire.PayloadKind} kind
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.kind = 0;

        /**
         * Envelope channel.
         * @member {string} channel
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.channel = "";

        /**
         * Envelope seq.
         * @member {number|Long} seq
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.seq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Envelope hash.
         * @member {string} hash
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.hash = "";

        /**
         * Envelope fullState.
         * @member {game.StateDocument.$Properties|null|undefined} fullState
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.fullState = null;

        /**
         * Envelope delta.
         * @member {wire.StateDelta.$Properties|null|undefined} delta
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.delta = null;

        /**
         * Envelope rawBody.
         * @member {game.RawBody.$Properties|null|undefined} rawBody
         * @memberof wire.Envelope
         * @instance
         */
        Envelope.prototype.rawBody = null;

        /**
         * Creates a new Envelope instance using the specified properties.
         * @function create
         * @memberof wire.Envelope
         * @static
         * @param {wire.Envelope.$Properties=} [properties] Properties to set
         * @returns {wire.Envelope} Envelope instance
         * @type {{
         *   (properties: wire.Envelope.$Shape): wire.Envelope & wire.Envelope.$Shape;
         *   (properties?: wire.Envelope.$Properties): wire.Envelope;
         * }}
         */
        Envelope.create = function(properties) {
            return new Envelope(properties);
        };

        /**
         * Encodes the specified Envelope message. Does not implicitly {@link wire.Envelope.verify|verify} messages.
         * @function encode
         * @memberof wire.Envelope
         * @static
         * @param {wire.Envelope.$Properties} message Envelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Envelope.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.event != null && $Object.hasOwnProperty.call(message, "event") && message.event !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.event);
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && (typeof message.id === "object" ? message.id.low || message.id.high : message.id !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.id);
            if (message.err != null && $Object.hasOwnProperty.call(message, "err") && message.err !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.err);
            if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.kind);
            if (message.channel != null && $Object.hasOwnProperty.call(message, "channel") && message.channel !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.channel);
            if (message.seq != null && $Object.hasOwnProperty.call(message, "seq") && (typeof message.seq === "object" ? message.seq.low || message.seq.high : message.seq !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.seq);
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash !== "")
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.hash);
            if (message.fullState != null && $Object.hasOwnProperty.call(message, "fullState"))
                $root.game.StateDocument.encode(message.fullState, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.delta != null && $Object.hasOwnProperty.call(message, "delta"))
                $root.wire.StateDelta.encode(message.delta, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.rawBody != null && $Object.hasOwnProperty.call(message, "rawBody"))
                $root.game.RawBody.encode(message.rawBody, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Envelope message, length delimited. Does not implicitly {@link wire.Envelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wire.Envelope
         * @static
         * @param {wire.Envelope.$Properties} message Envelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Envelope.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an Envelope message from the specified reader or buffer.
         * @function decode
         * @memberof wire.Envelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wire.Envelope & wire.Envelope.$Shape} Envelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Envelope.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wire.Envelope(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.event = value;
                        else
                            delete message.event;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.err = value;
                        else
                            delete message.err;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.kind = value;
                        else
                            delete message.kind;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.channel = value;
                        else
                            delete message.channel;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.seq = value;
                        else
                            delete message.seq;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.fullState = $root.game.StateDocument.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fullState);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.delta = $root.wire.StateDelta.decode(reader, reader.uint32(), $undefined, _depth + 1, message.delta);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.rawBody = $root.game.RawBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rawBody);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Envelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wire.Envelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wire.Envelope & wire.Envelope.$Shape} Envelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Envelope.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Envelope message.
         * @function verify
         * @memberof wire.Envelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Envelope.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.event != null && $Object.hasOwnProperty.call(message, "event"))
                if (!$util.isString(message.event))
                    return "event: string expected";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                    return "id: integer|Long expected";
            if (message.err != null && $Object.hasOwnProperty.call(message, "err"))
                if (!$util.isString(message.err))
                    return "err: string expected";
            if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                    return "kind: enum value expected";
            if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                if (!$util.isString(message.channel))
                    return "channel: string expected";
            if (message.seq != null && $Object.hasOwnProperty.call(message, "seq"))
                if (!$util.isInteger(message.seq) && !(message.seq && $util.isInteger(message.seq.low) && $util.isInteger(message.seq.high)))
                    return "seq: integer|Long expected";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!$util.isString(message.hash))
                    return "hash: string expected";
            if (message.fullState != null && $Object.hasOwnProperty.call(message, "fullState")) {
                let error = $root.game.StateDocument.verify(message.fullState, _depth + 1);
                if (error)
                    return "fullState." + error;
            }
            if (message.delta != null && $Object.hasOwnProperty.call(message, "delta")) {
                let error = $root.wire.StateDelta.verify(message.delta, _depth + 1);
                if (error)
                    return "delta." + error;
            }
            if (message.rawBody != null && $Object.hasOwnProperty.call(message, "rawBody")) {
                let error = $root.game.RawBody.verify(message.rawBody, _depth + 1);
                if (error)
                    return "rawBody." + error;
            }
            return null;
        };

        /**
         * Creates an Envelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wire.Envelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wire.Envelope} Envelope
         */
        Envelope.fromObject = function (object, _depth) {
            if (object instanceof $root.wire.Envelope)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wire.Envelope: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wire.Envelope();
            if (object.event != null)
                if (typeof object.event !== "string" || object.event.length)
                    message.event = $String(object.event);
            if (object.id != null)
                if (typeof object.id === "object" ? object.id.low || object.id.high : $Number(object.id) !== 0)
                    if ($util.Long)
                        message.id = $util.Long.fromValue(object.id, false);
                    else if (typeof object.id === "string")
                        message.id = $parseInt(object.id, 10);
                    else if (typeof object.id === "number")
                        message.id = object.id;
                    else if (typeof object.id === "object")
                        message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber();
            if (object.err != null)
                if (typeof object.err !== "string" || object.err.length)
                    message.err = $String(object.err);
            if (object.kind !== 0 && (typeof object.kind !== "string" || $root.wire.PayloadKind[object.kind] !== 0))
                switch (object.kind) {
                case "KIND_RAW":
                case 0:
                    message.kind = 0;
                    break;
                case "KIND_FULL":
                case 1:
                    message.kind = 1;
                    break;
                case "KIND_DELTA":
                case 2:
                    message.kind = 2;
                    break;
                default:
                    if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                        message.kind = object.kind;
                }
            if (object.channel != null)
                if (typeof object.channel !== "string" || object.channel.length)
                    message.channel = $String(object.channel);
            if (object.seq != null)
                if (typeof object.seq === "object" ? object.seq.low || object.seq.high : $Number(object.seq) !== 0)
                    if ($util.Long)
                        message.seq = $util.Long.fromValue(object.seq, true);
                    else if (typeof object.seq === "string")
                        message.seq = $parseInt(object.seq, 10);
                    else if (typeof object.seq === "number")
                        message.seq = object.seq;
                    else if (typeof object.seq === "object")
                        message.seq = new $util.LongBits(object.seq.low >>> 0, object.seq.high >>> 0).toNumber(true);
            if (object.hash != null)
                if (typeof object.hash !== "string" || object.hash.length)
                    message.hash = $String(object.hash);
            if (object.fullState != null) {
                if (!$util.isObject(object.fullState))
                    throw $TypeError(".wire.Envelope.fullState: object expected");
                message.fullState = $root.game.StateDocument.fromObject(object.fullState, _depth + 1);
            }
            if (object.delta != null) {
                if (!$util.isObject(object.delta))
                    throw $TypeError(".wire.Envelope.delta: object expected");
                message.delta = $root.wire.StateDelta.fromObject(object.delta, _depth + 1);
            }
            if (object.rawBody != null) {
                if (!$util.isObject(object.rawBody))
                    throw $TypeError(".wire.Envelope.rawBody: object expected");
                message.rawBody = $root.game.RawBody.fromObject(object.rawBody, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an Envelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wire.Envelope
         * @static
         * @param {wire.Envelope} message Envelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Envelope.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.event = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.err = "";
                object.kind = options.enums === $String ? "KIND_RAW" : 0;
                object.channel = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.seq = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.seq = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.hash = "";
                object.fullState = null;
                object.delta = null;
                object.rawBody = null;
            }
            if (message.event != null && $Object.hasOwnProperty.call(message, "event"))
                object.event = message.event;
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.id = typeof message.id === "number" ? $BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, false).toBigInt();
                else if (typeof message.id === "number")
                    object.id = options.longs === $String ? $String(message.id) : message.id;
                else
                    object.id = options.longs === $String ? $util.Long.prototype.toString.call(message.id) : options.longs === $Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber() : message.id;
            if (message.err != null && $Object.hasOwnProperty.call(message, "err"))
                object.err = message.err;
            if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                object.kind = options.enums === $String ? $root.wire.PayloadKind[message.kind] === $undefined ? message.kind : $root.wire.PayloadKind[message.kind] : message.kind;
            if (message.channel != null && $Object.hasOwnProperty.call(message, "channel"))
                object.channel = message.channel;
            if (message.seq != null && $Object.hasOwnProperty.call(message, "seq"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.seq = typeof message.seq === "number" ? $BigInt(message.seq) : $util.Long.fromBits(message.seq.low >>> 0, message.seq.high >>> 0, true).toBigInt();
                else if (typeof message.seq === "number")
                    object.seq = options.longs === $String ? $String(message.seq) : message.seq;
                else
                    object.seq = options.longs === $String ? $util.Long.prototype.toString.call(message.seq) : options.longs === $Number ? new $util.LongBits(message.seq.low >>> 0, message.seq.high >>> 0).toNumber(true) : message.seq;
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = message.hash;
            if (message.fullState != null && $Object.hasOwnProperty.call(message, "fullState"))
                object.fullState = $root.game.StateDocument.toObject(message.fullState, options, _depth + 1);
            if (message.delta != null && $Object.hasOwnProperty.call(message, "delta"))
                object.delta = $root.wire.StateDelta.toObject(message.delta, options, _depth + 1);
            if (message.rawBody != null && $Object.hasOwnProperty.call(message, "rawBody"))
                object.rawBody = $root.game.RawBody.toObject(message.rawBody, options, _depth + 1);
            return object;
        };

        /**
         * Converts this Envelope to JSON.
         * @function toJSON
         * @memberof wire.Envelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Envelope.prototype.toJSON = function() {
            return Envelope.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Envelope
         * @function getTypeUrl
         * @memberof wire.Envelope
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Envelope.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wire.Envelope";
        };

        return Envelope;
    })();

    /**
     * PayloadKind enum.
     * @name wire.PayloadKind
     * @enum {number}
     * @property {number} KIND_RAW=0 KIND_RAW value
     * @property {number} KIND_FULL=1 KIND_FULL value
     * @property {number} KIND_DELTA=2 KIND_DELTA value
     */
    wire.PayloadKind = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "KIND_RAW"] = 0;
        values[valuesById[1] = "KIND_FULL"] = 1;
        values[valuesById[2] = "KIND_DELTA"] = 2;
        return values;
    })();

    wire.StateDelta = (function() {

        /**
         * Properties of a StateDelta.
         * @typedef {Object} wire.StateDelta.$Properties
         * @property {Array.<wire.PatchOp.$Properties>|null} [ops] StateDelta ops
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a StateDelta.
         * @memberof wire
         * @interface IStateDelta
         * @augments wire.StateDelta.$Properties
         * @deprecated Use wire.StateDelta.$Properties instead.
         */

        /**
         * Shape of a StateDelta.
         * @typedef {{
         *   ops?: Array.<wire.PatchOp.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} wire.StateDelta.$Shape
         */

        /**
         * Constructs a new StateDelta.
         * @memberof wire
         * @classdesc Represents a StateDelta.
         * @constructor
         * @param {wire.StateDelta.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const StateDelta = function (properties) {
            this.ops = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * StateDelta ops.
         * @member {Array.<wire.PatchOp.$Properties>} ops
         * @memberof wire.StateDelta
         * @instance
         */
        StateDelta.prototype.ops = $util.emptyArray;

        /**
         * Creates a new StateDelta instance using the specified properties.
         * @function create
         * @memberof wire.StateDelta
         * @static
         * @param {wire.StateDelta.$Properties=} [properties] Properties to set
         * @returns {wire.StateDelta} StateDelta instance
         * @type {{
         *   (properties: wire.StateDelta.$Shape): wire.StateDelta & wire.StateDelta.$Shape;
         *   (properties?: wire.StateDelta.$Properties): wire.StateDelta;
         * }}
         */
        StateDelta.create = function(properties) {
            return new StateDelta(properties);
        };

        /**
         * Encodes the specified StateDelta message. Does not implicitly {@link wire.StateDelta.verify|verify} messages.
         * @function encode
         * @memberof wire.StateDelta
         * @static
         * @param {wire.StateDelta.$Properties} message StateDelta message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateDelta.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ops != null && message.ops.length)
                for (let i = 0; i < message.ops.length; ++i)
                    $root.wire.PatchOp.encode(message.ops[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StateDelta message, length delimited. Does not implicitly {@link wire.StateDelta.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wire.StateDelta
         * @static
         * @param {wire.StateDelta.$Properties} message StateDelta message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateDelta.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a StateDelta message from the specified reader or buffer.
         * @function decode
         * @memberof wire.StateDelta
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wire.StateDelta & wire.StateDelta.$Shape} StateDelta
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateDelta.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wire.StateDelta();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ops && message.ops.length))
                            message.ops = [];
                        message.ops.push($root.wire.PatchOp.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StateDelta message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wire.StateDelta
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wire.StateDelta & wire.StateDelta.$Shape} StateDelta
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateDelta.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateDelta message.
         * @function verify
         * @memberof wire.StateDelta
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateDelta.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ops != null && $Object.hasOwnProperty.call(message, "ops")) {
                if (!$Array.isArray(message.ops))
                    return "ops: array expected";
                for (let i = 0; i < message.ops.length; ++i) {
                    let error = $root.wire.PatchOp.verify(message.ops[i], _depth + 1);
                    if (error)
                        return "ops." + error;
                }
            }
            return null;
        };

        /**
         * Creates a StateDelta message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wire.StateDelta
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wire.StateDelta} StateDelta
         */
        StateDelta.fromObject = function (object, _depth) {
            if (object instanceof $root.wire.StateDelta)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wire.StateDelta: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wire.StateDelta();
            if (object.ops) {
                if (!$Array.isArray(object.ops))
                    throw $TypeError(".wire.StateDelta.ops: array expected");
                message.ops = $Array(object.ops.length);
                for (let i = 0; i < object.ops.length; ++i) {
                    if (!$util.isObject(object.ops[i]))
                        throw $TypeError(".wire.StateDelta.ops: object expected");
                    message.ops[i] = $root.wire.PatchOp.fromObject(object.ops[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a StateDelta message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wire.StateDelta
         * @static
         * @param {wire.StateDelta} message StateDelta
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateDelta.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.ops = [];
            if (message.ops && message.ops.length) {
                object.ops = $Array(message.ops.length);
                for (let j = 0; j < message.ops.length; ++j)
                    object.ops[j] = $root.wire.PatchOp.toObject(message.ops[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this StateDelta to JSON.
         * @function toJSON
         * @memberof wire.StateDelta
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateDelta.prototype.toJSON = function() {
            return StateDelta.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StateDelta
         * @function getTypeUrl
         * @memberof wire.StateDelta
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StateDelta.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wire.StateDelta";
        };

        return StateDelta;
    })();

    wire.PatchOp = (function() {

        /**
         * Properties of a PatchOp.
         * @typedef {Object} wire.PatchOp.$Properties
         * @property {string|null} [path] PatchOp path
         * @property {google.protobuf.Value.$Properties|null} [value] PatchOp value
         * @property {boolean|null} [remove] PatchOp remove
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PatchOp.
         * @memberof wire
         * @interface IPatchOp
         * @augments wire.PatchOp.$Properties
         * @deprecated Use wire.PatchOp.$Properties instead.
         */

        /**
         * Shape of a PatchOp.
         * @typedef {{
         *   path?: string|null;
         *   value?: google.protobuf.Value.$Shape|null;
         *   remove?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} wire.PatchOp.$Shape
         */

        /**
         * Constructs a new PatchOp.
         * @memberof wire
         * @classdesc Represents a PatchOp.
         * @constructor
         * @param {wire.PatchOp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PatchOp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * PatchOp path.
         * @member {string} path
         * @memberof wire.PatchOp
         * @instance
         */
        PatchOp.prototype.path = "";

        /**
         * PatchOp value.
         * @member {google.protobuf.Value.$Properties|null|undefined} value
         * @memberof wire.PatchOp
         * @instance
         */
        PatchOp.prototype.value = null;

        /**
         * PatchOp remove.
         * @member {boolean} remove
         * @memberof wire.PatchOp
         * @instance
         */
        PatchOp.prototype.remove = false;

        /**
         * Creates a new PatchOp instance using the specified properties.
         * @function create
         * @memberof wire.PatchOp
         * @static
         * @param {wire.PatchOp.$Properties=} [properties] Properties to set
         * @returns {wire.PatchOp} PatchOp instance
         * @type {{
         *   (properties: wire.PatchOp.$Shape): wire.PatchOp & wire.PatchOp.$Shape;
         *   (properties?: wire.PatchOp.$Properties): wire.PatchOp;
         * }}
         */
        PatchOp.create = function(properties) {
            return new PatchOp(properties);
        };

        /**
         * Encodes the specified PatchOp message. Does not implicitly {@link wire.PatchOp.verify|verify} messages.
         * @function encode
         * @memberof wire.PatchOp
         * @static
         * @param {wire.PatchOp.$Properties} message PatchOp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PatchOp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.path);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                $root.google.protobuf.Value.encode(message.value, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.remove != null && $Object.hasOwnProperty.call(message, "remove") && message.remove !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.remove);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PatchOp message, length delimited. Does not implicitly {@link wire.PatchOp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof wire.PatchOp
         * @static
         * @param {wire.PatchOp.$Properties} message PatchOp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PatchOp.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PatchOp message from the specified reader or buffer.
         * @function decode
         * @memberof wire.PatchOp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {wire.PatchOp & wire.PatchOp.$Shape} PatchOp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PatchOp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.wire.PatchOp(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value = $root.google.protobuf.Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.value);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.remove = value;
                        else
                            delete message.remove;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PatchOp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof wire.PatchOp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {wire.PatchOp & wire.PatchOp.$Shape} PatchOp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PatchOp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PatchOp message.
         * @function verify
         * @memberof wire.PatchOp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PatchOp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                if (!$util.isString(message.path))
                    return "path: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                let error = $root.google.protobuf.Value.verify(message.value, _depth + 1);
                if (error)
                    return "value." + error;
            }
            if (message.remove != null && $Object.hasOwnProperty.call(message, "remove"))
                if (typeof message.remove !== "boolean")
                    return "remove: boolean expected";
            return null;
        };

        /**
         * Creates a PatchOp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof wire.PatchOp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {wire.PatchOp} PatchOp
         */
        PatchOp.fromObject = function (object, _depth) {
            if (object instanceof $root.wire.PatchOp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".wire.PatchOp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.wire.PatchOp();
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.value != null) {
                if (!$util.isObject(object.value))
                    throw $TypeError(".wire.PatchOp.value: object expected");
                message.value = $root.google.protobuf.Value.fromObject(object.value, _depth + 1);
            }
            if (object.remove != null)
                if (object.remove)
                    message.remove = $Boolean(object.remove);
            return message;
        };

        /**
         * Creates a plain object from a PatchOp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof wire.PatchOp
         * @static
         * @param {wire.PatchOp} message PatchOp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PatchOp.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.path = "";
                object.value = null;
                object.remove = false;
            }
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = $root.google.protobuf.Value.toObject(message.value, options, _depth + 1);
            if (message.remove != null && $Object.hasOwnProperty.call(message, "remove"))
                object.remove = message.remove;
            return object;
        };

        /**
         * Converts this PatchOp to JSON.
         * @function toJSON
         * @memberof wire.PatchOp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PatchOp.prototype.toJSON = function() {
            return PatchOp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PatchOp
         * @function getTypeUrl
         * @memberof wire.PatchOp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PatchOp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/wire.PatchOp";
        };

        return PatchOp;
    })();

    return wire;
})();

export {
  $root as default
};
